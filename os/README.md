# os

macOS `defaults write` scripts plus shell registration.

**Populated by Phase 6 -- see `.planning/ROADMAP.md`.**

**Requirements landing here:** OSCF-01..05

Until then this directory is intentionally empty. The manifest layer
(`manifests/`) drives what eventually lands here at install time.

version: '3'

# =============================================================================
# taskfiles/claude-stub.yml -- Phase 7 placeholder
#
# This file is loaded by the root Taskfile.yml via:
#   includes:
#     claude: ./taskfiles/claude-stub.yml
#
# Phase 7 (CLDE-01..04, TOOL-01..04, TEST-01..02) replaces this file with the
# real taskfiles/claude.yml. The stub exists so `task install` has a complete
# call graph in Phase 2 and the lint suite can self-test against a fully wired
# manifest.
# =============================================================================

tasks:
  install:
    desc: "STUB (Phase 7 will implement CLDE-01..04)"
    status: [true]
    cmds:
      - |
        echo "claude:install -- stub (Phase 7 will implement)" >&2

version: '3'

# =============================================================================
# taskfiles/macos-stub.yml -- Phase 6 placeholder
#
# This file is loaded by the root Taskfile.yml via:
#   includes:
#     macos: ./taskfiles/macos-stub.yml
#
# Phase 6 (OSCF-01..05) replaces this file with the real taskfiles/macos.yml.
# The stub exists so `task install` has a complete call graph in Phase 2 and
# the lint suite can self-test against a fully wired manifest.
# =============================================================================

tasks:
  defaults:
    desc: "STUB (Phase 6 will implement OSCF-01..03, OSCF-05)"
    status: [true]
    cmds:
      - |
        echo "macos:defaults -- stub (Phase 6 will implement)" >&2

  shell:
    desc: "STUB (Phase 6 will implement OSCF-04 -- chsh + shell-registration)"
    status: [true]
    cmds:
      - |
        echo "macos:shell -- stub (Phase 6 will implement)" >&2

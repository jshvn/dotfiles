version: '3'

# =============================================================================
# taskfiles/test/lint-fixtures/08a-banner-parity-fail/Taskfile.yml
#
# Positive fixture for LINT-08: default:'s cmd block is missing a public
# top-level task. The `mypublic:` task is public (no internal: true marker)
# but is NOT mentioned in default:'s cmd block. LINT-08 must fire.
#
# Expected outcome: LINT-08 fires (expect: fail)
# =============================================================================

tasks:
  default:
    desc: "fixture default -- only install is mentioned"
    status: [false]
    cmds:
      - echo "install -- only this is mentioned"

  install:
    desc: "fixture install"
    status: [false]
    cmds:
      - echo install

  # mypublic is a public top-level task NOT mentioned in default:'s cmd block.
  # LINT-08 must detect this drift.
  mypublic:
    desc: "fixture public task -- banner drift"
    status: [false]
    cmds:
      - echo mypublic

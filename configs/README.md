# configs

Tool configs (ghostty, glow, trippy, tlrc, conda, ...).

**Populated by Phase 7 -- see `.planning/ROADMAP.md`.**

**Requirements landing here:** TOOL-01..04

Until then this directory is intentionally empty. The manifest layer
(`manifests/`) drives what eventually lands here at install time.

# negative fixture -- machine.os = "windows" is outside the {darwin, linux}
# enum (linux is valid in v2, so the old "linux" trigger no longer rejects).

schema_version = 2

[machine]
description = "bad-os negative fixture"
os = "windows"
identity = "none"

[features]
enabled = []
disabled = []

[packages]
bundles = [
  "dotfiles",
]

# negative fixture -- machine.description omitted; validator must reject.

schema_version = 2

[machine]
os = "darwin"
identity = "none"

[features]
enabled = []
disabled = []

[packages]
bundles = [
  "dotfiles",
]

# negative fixture -- meta.description omitted; validator must reject

schema_version = 1

[platform]
os = "darwin"

[features]

[packages.brew]
bundles = ["dotfiles"]

[identity]
git = "none"
ssh = "none"

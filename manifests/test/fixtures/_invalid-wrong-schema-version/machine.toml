# negative fixture -- schema_version = 1 is rejected by the v2 resolver.

schema_version = 1

[machine]
description = "wrong schema_version fixture"
os = "darwin"
identity = "none"

[features]
enabled = []
disabled = []

[packages]
bundles = [
  "dotfiles",
]

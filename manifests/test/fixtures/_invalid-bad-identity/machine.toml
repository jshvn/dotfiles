# negative fixture -- machine.identity has no overlay files; must be rejected.

schema_version = 2

[machine]
description = "bad identity fixture"
os = "darwin"
identity = "nosuchidentity"

[features]
enabled = []
disabled = []

[packages]
bundles = [
  "dotfiles",
]

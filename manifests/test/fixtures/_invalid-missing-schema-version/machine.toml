# negative fixture -- schema_version omitted; validator must reject.

[machine]
description = "missing schema_version fixture"
os = "darwin"
identity = "none"

[features]
enabled = []
disabled = []

[packages]
bundles = [
  "dotfiles",
]

# negative fixture -- packages.bundles must always include "dotfiles".

schema_version = 2

[machine]
description = "bundles without dotfiles fixture"
os = "darwin"
identity = "none"

[features]
enabled = []
disabled = []

[packages]
bundles = [
  "cli",
]

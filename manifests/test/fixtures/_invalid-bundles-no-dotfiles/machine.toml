# negative fixture -- bundles array does not include "dotfiles"; "dotfiles"
# is always-on, every machine inherits the base packages.

schema_version = 1

[meta]
description = "bundles-no-dotfiles negative fixture"

[platform]
os = "darwin"

[features]

[packages.brew]
bundles = ["gui"]

[identity]
git = "none"
ssh = "none"

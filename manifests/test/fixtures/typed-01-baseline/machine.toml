# typed-01-baseline -- multi-bundle formulae fold (dotfiles + util), no inline
# extras. Proves the resolver pipeline runs end-to-end with --registry and
# --shared-dir overrides and backfills arch via uname -m.

schema_version = 2

[machine]
description = "typed-01-baseline fixture"
os = "darwin"
identity = "none"

[features]
enabled = []
disabled = []

[packages]
bundles = [
  "dotfiles",
  "util",
]

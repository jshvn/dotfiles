# negative fixture -- a bundle name with no matching file must be rejected.

schema_version = 2

[machine]
description = "bundle typo fixture"
os = "darwin"
identity = "none"

[features]
enabled = []
disabled = []

[packages]
bundles = [
  "dotfiles",
  "nosuchbundle",
]

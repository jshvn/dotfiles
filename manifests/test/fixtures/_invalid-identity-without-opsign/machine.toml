# negative fixture -- identity "personal" declares the one-password-signing
# capability sentinel, but the machine disables one-password-signing. Reject.

schema_version = 2

[machine]
description = "identity-without-opsign fixture"
os = "darwin"
identity = "personal"

[features]
enabled = [
  "one-password-ssh",
  "claude-marketplace",
  "repo-auto-update",
  "ghostty",
  "jgrid-net",
  "macos-dock",
  "macos-finder",
  "macos-input",
  "macos-spotlight",
  "macos-screenshots",
  "macos-security",
  "macos-appearance",
  "macos-display",
]
disabled = [
  "one-password-signing",
  "server-include",
]

[packages]
bundles = [
  "dotfiles",
]

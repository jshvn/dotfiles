# negative fixture -- bundles array does not include "core"; "core" is
# always-on, every machine inherits the base packages.

schema_version = 1

[meta]
description = "bundles-no-core negative fixture"

[platform]
os = "darwin"

[features]

[packages.brew]
bundles = ["gui"]

[identity]
git = "none"
ssh = "none"

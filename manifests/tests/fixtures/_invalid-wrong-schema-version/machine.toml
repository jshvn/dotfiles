# negative fixture -- a schema_version outside the supported set is rejected.

schema_version = 1

[machine]
description = "wrong schema_version fixture"
os = "darwin"
identity = "none"

[features]
enabled = []
disabled = []

[packages]

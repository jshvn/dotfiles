# typed-07-ai-table -- the ai flag is enabled, so [ai] must be present and
# is emitted verbatim into resolved.json.

schema_version = 3

[machine]
description = "typed-07-ai-table fixture"
os = "darwin"
identity = "none"

[features]
enabled = ["ai"]
disabled = []

[packages]

[ai]
profile = "personal"
ref = "2026.09.23"

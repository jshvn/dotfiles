# macOS
.DS_Store

# Homebrew bundle lockfiles
Brewfile.lock.json
Brewfile.rb.lock.json

# Task (go-task) checksum cache
.task/

# Per-machine Claude Code overrides. Stay machine-local; never committed.
.claude/settings.local.json

# AUDIT.md -- v1-Drop Audit Report

**Phase:** 09-v1-drop-audit
**Status:** Complete (Phase 9 closed, ready for Phase 10)
**Last updated:** 2026-05-17

## Summary

| Metric | Count |
|--------|-------|
| Tasks audited | 102 |
| Keep | 2 |
| Drop | 100 |
| Already-ported | 70 |

### Keep List (Phase 10 queue)

_The bullets below are the Phase 10 implementation queue (D-04). One bullet per keep row; each names its source location and its v2 owner file. Section split mirrors leftover category per D-01; every row uses the locked six-column shape per D-03._

- `taskfiles/common.yml:36-57` → **taskfiles/shell.yml** — Write `export ZDOTDIR="$HOME/.config/zsh"` to /etc/zshenv via sudo (idempotent grep-and-append)
- `taskfiles/common.yml:63-88` → **taskfiles/shell.yml** — Validate XDG dirs and the /etc/zshenv ZDOTDIR line are present

## Taskfiles

_Row granularity: one row per task (D-02). Rows are written by plan 09-02 from shards/taskfiles.md._

| file:line | purpose | v2 status | keep/drop | rationale | v2 owner |
|-----------|---------|-----------|-----------|-----------|----------|
| taskfiles/common.yml:18-34 | Create XDG base directories (XDG_CONFIG_HOME, XDG_DATA_HOME, XDG_STATE_HOME, XDG_CACHE_HOME) idempotently | ported | drop | v2 establishes XDG dirs at shell startup time via shell/.zshenv and per-tool tasks mkdir-p their own target dirs (D-09); no centralized common:xdg task needed in v2 because the install graph never sets up XDG bare dirs as a discrete step. | taskfiles/shell.yml |
| taskfiles/common.yml:36-57 | Write `export ZDOTDIR="$HOME/.config/zsh"` to /etc/zshenv via sudo (idempotent grep-and-append) | dropped | keep | Milestone driver -- v2 silently dropped this; without it a fresh-machine first shell has no ZDOTDIR set so `.zshenv` never loads from `$XDG_CONFIG_HOME/zsh/`. PORT-01 implements this in Phase 10 (ROADMAP Phase 10 SC#1). | taskfiles/shell.yml |
| taskfiles/common.yml:63-88 | Validate XDG dirs and the /etc/zshenv ZDOTDIR line are present | partially-ported | keep | v2 `task validate` checks symlinks and per-concern install state but does not assert that `/etc/zshenv` contains the ZDOTDIR export. Phase 10 should add this assertion alongside PORT-01. | taskfiles/shell.yml |
| taskfiles/profile.yml:4-38 | Prompt the operator to pick a valid profile (`personal | work | server | test`) and write it to PROFILE_FILE | dropped | drop | v2 replaces profile-based dispatch with the manifest-model `$DOTFILES_MACHINE` selection (no profile variable in v2). `task setup -- <machine-name>` writes `$XDG_STATE_HOME/dotfiles/machine` instead (BTSP-04). No prompt -- machine name is an explicit CLI argument. | taskfiles/manifest.yml |
| taskfiles/profile.yml:40-44 | Require profile to be set, fail with friendly message if not | dropped | drop | v2 replaces profile-based dispatch with the manifest-model `$DOTFILES_MACHINE` selection. `install/cutover-gate.zsh` is the v2 equivalent precondition that blocks `task install` until a machine is selected. | taskfiles/manifest.yml |
| taskfiles/profile.yml:46-62 | Set profile from CLI arg: `task profile:set -- personal`, validates against VALID_PROFILES list | dropped | drop | v2 replaces profile-based dispatch with the manifest-model `$DOTFILES_MACHINE` selection (no profile variable in v2). `task setup -- <machine-name>` is the v2 equivalent (writes the machine-name state file). | taskfiles/manifest.yml |
| taskfiles/profile.yml:64-67 | Show current profile (delegates to `ensure`) | dropped | drop | v2 replaces profile-based dispatch with the manifest-model `$DOTFILES_MACHINE` selection; `task manifest:show` prints the post-merge resolved manifest including the machine name. | taskfiles/manifest.yml |
| taskfiles/profile.yml:69-77 | Run profile-specific installation by dispatching to `task {{.PROFILE}}:install` | dropped | drop | v2 replaces profile-based dispatch with the manifest-model `$DOTFILES_MACHINE` selection. v2's `task install` is a single canonical pipeline that reads `resolved.json` for the active machine -- no per-profile namespace dispatch. | taskfiles/manifest.yml |
| taskfiles/profile.yml:79-84 | Install profile-specific Homebrew packages by dispatching to `task {{.PROFILE}}:brew` | dropped | drop | v2 replaces profile-based dispatch with the manifest-model `$DOTFILES_MACHINE` selection. v2 composes per-machine Brewfile via `install/compose-brewfile.zsh` from `packages/<bundle>.rb` + `extra_packages`; no per-profile namespace. | taskfiles/packages.yml |
| taskfiles/profile.yml:86-91 | Create profile-specific symlinks by dispatching to `task {{.PROFILE}}:links` | dropped | drop | v2 replaces profile-based dispatch with the manifest-model `$DOTFILES_MACHINE` selection. v2 `taskfiles/links.yml` reads feature flags and identity from `resolved.json` instead of branching on a profile var. | taskfiles/links.yml |
| taskfiles/profile.yml:97-110 | Validate profile file exists and contains a value in VALID_PROFILES | dropped | drop | v2 replaces profile-based dispatch with the manifest-model `$DOTFILES_MACHINE` selection. `install/resolver.zsh --validate-only --machine <name>` validates the machine selection in v2. | taskfiles/manifest.yml |
| taskfiles/profile-tasks.yml:25-35 | Install a parameterized profile (TARGET_PROFILE) by chaining links + brew | dropped | drop | v2 replaces profile-based dispatch with the manifest-model `$DOTFILES_MACHINE` selection. v2 has no `<profile>:install` namespace; `task install` is the single canonical entrypoint that reads `resolved.json`. | taskfiles/manifest.yml |
| taskfiles/profile-tasks.yml:37-61 | Create profile-specific symlinks: git/config-<profile>, ssh/config-<profile>, ssh/keys/id_ed25519_<profile>.pub | dropped | drop | v2 replaces profile-based dispatch with the manifest-model `$DOTFILES_MACHINE` selection. v2 `taskfiles/links.yml` + `taskfiles/identity.yml` resolve git/ssh identity from `resolved.json` `[identity]` block. | taskfiles/identity.yml |
| taskfiles/profile-tasks.yml:63-71 | Install profile-specific Homebrew packages from `install/Brewfile-<profile>.rb` | dropped | drop | v2 replaces profile-based dispatch with the manifest-model `$DOTFILES_MACHINE` selection. v2 composes per-machine Brewfile via `install/compose-brewfile.zsh` from `packages/<bundle>.rb` + `extra_packages` -- no per-profile Brewfile lookup. | taskfiles/packages.yml |
| taskfiles/profile-tasks.yml:77-82 | Remove profile-specific symlinks (git/config, ssh/config, ssh/keys) | dropped | drop | v2 replaces profile-based dispatch with the manifest-model `$DOTFILES_MACHINE` selection. v2 has no profile-keyed unlink because identity files are not per-profile-suffixed. | taskfiles/links.yml |
| taskfiles/profile-tasks.yml:88-110 | Validate profile-specific symlinks (git config, ssh config, ssh public key) | dropped | drop | v2 replaces profile-based dispatch with the manifest-model `$DOTFILES_MACHINE` selection. v2 `task validate` walks the v2 symlink set without per-profile branching. | taskfiles/links.yml |
| taskfiles/brew.yml:14-19 | Install Homebrew + packages (ensure-homebrew, update, bundle) -- single profile-less Brewfile only | partially-ported | drop | v2 `task packages:install` covers the same surface but composes per-machine Brewfile from `packages/<bundle>.rb` + `extra_packages` via `install/compose-brewfile.zsh` (D-09 behavior-equivalent for installed package set on each machine). | taskfiles/packages.yml |
| taskfiles/brew.yml:21-35 | Install Homebrew if absent, via curl-piped install.sh | ported | drop | v2 `task packages:ensure-homebrew` (and bootstrap.zsh) installs Homebrew at startup; same curl-piped install.sh -- the curl-to-shell-pipe concern in CONCERNS.md applies equally to v2 bootstrap. | taskfiles/packages.yml |
| taskfiles/brew.yml:37-50 | Update Homebrew and installed packages (brew update + upgrade + cleanup --prune=30) | ported | drop | v2 `task packages:update` (or equivalent within packages.yml) covers the same `brew update/upgrade/cleanup` flow. | taskfiles/packages.yml |
| taskfiles/brew.yml:52-62 | Install packages from a single profile-less `install/Brewfile.rb` via `brew bundle` | partially-ported | drop | v2 replaces the single profile-less Brewfile with a per-machine composed Brewfile (bundles + extras). Behavior-equivalent on each machine for the package surface (D-09); see shards/install-assets.md for the package-level diff. | taskfiles/packages.yml |
| taskfiles/brew.yml:68-72 | Validate Homebrew installation (command exists) | ported | drop | v2 `task validate` includes a brew presence check via the `_:check-command` helper. | taskfiles/packages.yml |
| taskfiles/claude-stub.yml:17-22 | STUB placeholder for `claude:install` -- prints a notice to stderr | ported | drop | Phase 7 scaffold; replaced by taskfiles/claude.yml real bodies (D-11). The stub had no behavior of its own (status: [true] always-skip + a stderr echo); v2 `taskfiles/claude.yml` carries the real CLDE-01..04 + TOOL-01..04 + TEST-01..02 implementations. | taskfiles/claude.yml |
| taskfiles/brew-stub.yml:9-10 | DEPRECATED empty tasks map (placeholder so the `brew: ./taskfiles/brew-stub.yml` include in Taskfile.yml continues to parse) | ported | drop | Phase 5 scaffold; replaced by taskfiles/packages.yml (D-11). The stub declares no tasks (`tasks: {}`); all packages-layer functionality lives in v2 `taskfiles/packages.yml`. | taskfiles/packages.yml |
| taskfiles/links-stub.yml:16-21 | STUB placeholder for `links:all` -- prints a notice to stderr | ported | drop | Phase 3 scaffold; replaced by taskfiles/links.yml real bodies (D-11). The stub had no behavior of its own (status: [true] always-skip + a stderr echo); v2 `taskfiles/links.yml` carries the real SHEL-01..03 + per-tool link bodies. | taskfiles/links.yml |
| taskfiles/macos.v1.yml.bak:4-21 | Apply macOS system defaults (orchestrator that chains defaults-init, defaults-general, defaults-dock, defaults-appearance, defaults-finder, defaults-misc) | ported | drop | v2 `taskfiles/macos.yml` is the live equivalent and its sub-tasks are split per concern under `os/defaults/<concern>.zsh` (one concern per file) gated by manifest feature flags (`macos-dock`, `macos-finder`, `macos-input`, `macos-screenshots`, `macos-security`). | taskfiles/macos.yml |
| taskfiles/macos.v1.yml.bak:22-27 | Prepare for defaults application (quit System Preferences) | ported | drop | v2 `taskfiles/macos.yml` includes this same setup step ahead of the defaults sub-tasks. | taskfiles/macos.yml |
| taskfiles/macos.v1.yml.bak:29-38 | Apply general security defaults (screensaver askForPassword, askForPasswordDelay) | ported | drop | v2 `os/defaults/security.zsh` (gated by `macos-security` feature) writes the same screensaver defaults. | taskfiles/macos.yml |
| taskfiles/macos.v1.yml.bak:40-57 | Apply Dock configuration defaults (orientation, tilesize, autohide-by-profile, mineffect, show-recents, mru-spaces) | partially-ported | drop | v2 `os/defaults/dock.zsh` (gated by `macos-dock` feature) writes the same Dock defaults; the v1 `autohide-by-profile` branch is replaced by the manifest `macos-dock` feature flag (servers don't enable the feature, so autohide behavior is feature-driven not profile-string-driven). | taskfiles/macos.yml |
| taskfiles/macos.v1.yml.bak:59-70 | Apply appearance defaults (dark mode, scroll direction) | ported | drop | v2 `os/defaults/security.zsh` and related appearance concern files write the same appearance defaults under feature gates. | taskfiles/macos.yml |
| taskfiles/macos.v1.yml.bak:72-89 | Apply Finder defaults (AppleShowAllExtensions, FXEnableExtensionChangeWarning, FXPreferredViewStyle "clmv", arrangeBy grid via PlistBuddy) | ported | drop | v2 `os/defaults/finder.zsh` (gated by `macos-finder` feature) writes the same Finder defaults. | taskfiles/macos.yml |
| taskfiles/macos.v1.yml.bak:91-109 | Apply miscellaneous defaults (ImageCapture disableHotPlug, TextInputMenu visibility, Siri menu, guest account off) | ported | drop | v2 `os/defaults/input.zsh` + `os/defaults/security.zsh` (gated by `macos-input` and `macos-security` features) write the same miscellaneous defaults. | taskfiles/macos.yml |
| taskfiles/macos.v1.yml.bak:111-146 | Set Homebrew zsh as default shell (add to /etc/shells, chsh) -- contains the `$BREW_ZSH` shell-vs-template-var bug in the status: block at line 145 | ported | drop | v2 `taskfiles/macos.yml` shell task carries the `{{.BREW_ZSH}}` template-var fix for the v1 `$BREW_ZSH` shell-var bug (CONCERNS.md `macos:shell:145` bug class; D-10) -- v2 status block correctly references the task template variable so the idempotency check evaluates against the resolved Homebrew zsh path on every run. | taskfiles/macos.yml |
| taskfiles/macos.v1.yml.bak:152-183 | Validate macOS-specific components (Homebrew zsh in /etc/shells, default shell, 1Password SSH agent socket gated by PROFILE != "server") | partially-ported | drop | v2 `task validate` carries the equivalent macOS-validation surface; the profile-gated 1Password check is replaced by the manifest `one-password-ssh` feature flag (servers set `one-password-ssh = false` instead of branching on profile string). | taskfiles/macos.yml |

## Install Assets

_Rows are written by plan 09-02 (Brewfiles per-machine effective-set diff, plus any install/*.zsh not in the v2 set) from shards/install-assets.md._

| file:line | purpose | v2 status | keep/drop | rationale | v2 owner |
|-----------|---------|-----------|-----------|-----------|----------|
| install/Brewfile.rb:71 | brew formula 'antigen' (zsh plugin manager) declared in v1 common Brewfile (loaded on every profile: personal-laptop, work-laptop, server-1, server-2 per-machine effective-set diff via install/resolver.zsh + packages/core.rb + extra_packages) | ported | drop | v2 replaced antigen with antidote (`brew 'antidote'` in packages/core.rb); v2 .zshrc loads antidote bundle-cache (SHEL-10) -- behavior-equivalent zsh plugin management on every machine (D-09). | packages/core.rb |
| install/Brewfile-personal.rb:22 | cask 'appcleaner' declared in v1 personal-profile Brewfile | ported | drop | v2 personal-laptop manifest carries `{ name = "appcleaner" }` in `[packages.brew.extra_packages.casks]`; resolver diff confirms identical on personal-laptop. | manifests/machines/personal-laptop.toml |
| install/Brewfile-personal.rb:23 | cask '1password' declared in v1 personal-profile Brewfile | ported | drop | v2 packages/gui.rb carries `cask '1password'`; personal-laptop manifest includes the `gui` bundle -- resolver diff confirms identical on personal-laptop. | packages/gui.rb |
| install/Brewfile-personal.rb:29 | cask 'dropbox' declared in v1 personal-profile Brewfile | ported | drop | v2 personal-laptop manifest carries `{ name = "dropbox" }` in `[packages.brew.extra_packages.casks]`; resolver diff confirms identical on personal-laptop. | manifests/machines/personal-laptop.toml |
| install/Brewfile-personal.rb:30 | cask 'cryptomator' declared in v1 personal-profile Brewfile | ported | drop | v2 personal-laptop manifest carries `{ name = "cryptomator" }` in `[packages.brew.extra_packages.casks]`; resolver diff confirms identical on personal-laptop. | manifests/machines/personal-laptop.toml |
| install/Brewfile-personal.rb:36 | cask 'ghostty' declared in v1 personal-profile Brewfile | ported | drop | v2 packages/gui.rb carries `cask 'ghostty'`; personal-laptop and work-laptop manifests include the `gui` bundle -- resolver diff confirms identical on those two machines. | packages/gui.rb |
| install/Brewfile-personal.rb:58 | cask 'cloudflare-warp' declared in v1 personal-profile Brewfile | ported | drop | v2 personal-laptop manifest carries `{ name = "cloudflare-warp" }` in `[packages.brew.extra_packages.casks]`; resolver diff confirms identical on personal-laptop. | manifests/machines/personal-laptop.toml |
| install/Brewfile-personal.rb:61 | cask 'miniconda' declared in v1 personal-profile Brewfile | ported | drop | v2 personal-laptop and work-laptop manifests carry `{ name = "miniconda" }` in `[packages.brew.extra_packages.casks]`; resolver diff confirms identical on both laptops. | manifests/machines/personal-laptop.toml |
| install/Brewfile-personal.rb:62 | cask 'docker-desktop' declared in v1 personal-profile Brewfile | ported | drop | v2 personal-laptop and work-laptop manifests carry `{ name = "docker-desktop" }` in `[packages.brew.extra_packages.casks]`; resolver diff confirms identical on both laptops. | manifests/machines/personal-laptop.toml |
| install/Brewfile-personal.rb:72 | mas 'Things' (id 904280696) declared in v1 personal-profile Brewfile | ported | drop | Ported under canonical App Store name -- v1 used short name 'Things'; v2 uses canonical 'Things3' (the name `mas list` returns for id 904280696). The install primitive is the id (904280696), which is unchanged across v1 and v2 manifests; mas-list-name drift is a display-string concern, not an install concern. Functional install is correct on both. Phase 10 (D-07) chose to leave the v2 manifest name as the canonical 'Things3' rather than revert to the v1 short name. | manifests/machines/personal-laptop.toml |
| install/Brewfile-server.rb:14 | cask 'dropbox' declared in v1 server-profile Brewfile (loaded on server-1, server-2) | dropped | drop | v2 server-1 and server-2 manifests omit the `gui` bundle and declare no casks -- v2 design intent is headless servers carry no GUI apps. Per-machine effective-set diff confirms cask 'dropbox' absent from v2 server effective set. The v1 server-profile assumption that servers needed dropbox was a v1 misfeature; v2 servers are intentionally headless. | manifests/machines/server-1.toml |
| install/Brewfile-server.rb:15 | cask 'appcleaner' declared in v1 server-profile Brewfile (loaded on server-1, server-2) | dropped | drop | v2 server-1 and server-2 manifests omit the `gui` bundle and declare no casks -- v2 design intent is headless servers carry no GUI apps. appcleaner is a UI-only utility with no headless purpose. | manifests/machines/server-1.toml |
| install/Brewfile-server.rb:16 | cask '1password' declared in v1 server-profile Brewfile (loaded on server-1, server-2) | dropped | drop | v2 server-1 and server-2 manifests omit the `gui` bundle and have `one-password-ssh = false` -- v2 servers use the system ssh-agent, not 1Password (zsh/.zprofile profile-string check is replaced by manifest feature flag). Per-machine effective-set diff confirms cask '1password' absent from v2 server effective set by design. | manifests/machines/server-1.toml |
| install/Brewfile-server.rb:17 | cask 'cryptomator' declared in v1 server-profile Brewfile (loaded on server-1, server-2) | dropped | drop | v2 server-1 and server-2 manifests omit the `gui` bundle and declare no casks -- cryptomator is a GUI vault utility with no headless server role. Per-machine effective-set diff confirms cask 'cryptomator' absent from v2 server effective set by design. | manifests/machines/server-1.toml |
| install/Brewfile-server.rb:20 | cask 'ghostty' declared in v1 server-profile Brewfile (loaded on server-1, server-2) | dropped | drop | v2 server-1 and server-2 manifests omit the `gui` bundle (which carries `cask 'ghostty'`) -- ghostty is a terminal emulator with no headless server use. Per-machine effective-set diff confirms cask 'ghostty' absent from v2 server effective set by design. | manifests/machines/server-1.toml |
| install/Brewfile-server.rb:28 | cask 'cloudflare-warp' declared in v1 server-profile Brewfile (loaded on server-1, server-2) | dropped | drop | v2 server-1 and server-2 manifests declare no casks -- cloudflare-warp is a desktop VPN client with a Network Extension; v2 servers route traffic via cloudflared (`brew 'cloudflared'` in core.rb) at the CLI layer instead. Per-machine effective-set diff confirms cask 'cloudflare-warp' absent from v2 server effective set by design. | manifests/machines/server-1.toml |
| install/Brewfile-server.rb:31 | cask 'miniconda' declared in v1 server-profile Brewfile (loaded on server-1, server-2) | dropped | drop | v2 server-1 and server-2 manifests omit miniconda -- v2 design intent is headless servers do not carry the conda Python distribution. If a server later needs miniconda, it should land in that machine's `[packages.brew.extra_packages.casks]`. | manifests/machines/server-1.toml |
| install/Brewfile-server.rb:32 | cask 'docker-desktop' declared in v1 server-profile Brewfile (loaded on server-1, server-2) | dropped | drop | v2 server-1 and server-2 manifests omit docker-desktop -- Docker Desktop is a GUI app with no headless server role; servers run docker engine directly via brew formula if needed. Per-machine effective-set diff confirms cask 'docker-desktop' absent from v2 server effective set by design. | manifests/machines/server-1.toml |
| install/*.zsh | All install/*.zsh files in the repo are members of the v2 set (messages.zsh, resolver.zsh, compose-brewfile.zsh, cutover-gate.zsh, test-hooks.zsh); No v1-only install/*.zsh files present beyond the v2 set | n/a | drop | Scope-check: `ls install/*.zsh` returns exactly the five-file v2 set named in 09-CONTEXT.md `<canonical_refs>`. AUDIT-02 install/*.zsh dimension is satisfied with zero leftover rows. | n/a |

## zsh/ Tree

_Rows are written by plan 09-03 (six startup files + 24 functions + aliases per-profile-effective-set + configs/styles diffs) from shards/zsh-tree.md. Function-file presence-plus-diff comparison per D-05; alias per-profile-effective-set diff per D-06; configs/styles body-level diff per D-07; six startup files one-row-per-file with block-level rationale per D-08._

| file:line | purpose | v2 status | keep/drop | rationale | v2 owner |
|-----------|---------|-----------|-----------|-----------|----------|
| zsh/.zshenv:1-73 | v1 zsh startup file: XDG vars, ZDOTDIR, HISTFILE, locale, EDITOR/VISUAL, BROWSER, SHELL_SESSIONS_DISABLE, CLAUDE_CONFIG_DIR, and DOTFILES_PROFILE export (read from $XDG_CONFIG_HOME/dotfiles/profile) | ported | drop | XDG export block: present-equivalent (shell/.zshenv:31-34 identical); XDG_DATA_DIRS/XDG_CONFIG_DIRS block: present-equivalent; ZDOTDIR export block: present-equivalent (line 41 identical); HISTFILE + SHARE_HISTORY block: present-equivalent (lines 45-48 identical); CLAUDE_CONFIG_DIR block: present-equivalent (line 51 identical); EDITOR/VEDITOR/VISUAL block: present-equivalent; LANG/LC_ALL UTF-8 block: present-equivalent; BROWSER block: present-equivalent; SHELL_SESSIONS_DISABLE block: present-equivalent; __CF_USER_TEXT_ENCODING block: present-equivalent; DOTFILES_PROFILE export block (v1 line 73, read from $XDG_CONFIG_HOME/dotfiles/profile): replaced-by-manifest-model -- v2 exports DOTFILES_MACHINE read from $XDG_STATE_HOME/dotfiles/machine via guarded readability check (shell/.zshenv:76-79), per D-09 the manifest model replaces the profile model and is behavior-equivalent for the operator | shell/.zshenv |
| zsh/.zprofile:1-56 | v1 zsh login-shell file: Homebrew shellenv via uname -m dispatch, plus literal "server" hostname check gating the 1Password SSH_AUTH_SOCK | ported | drop | Homebrew shellenv block (uname -m -> /opt/homebrew or /usr/local, line 34-47): present-with-delta -- v2 adds [[ -x "$DIRECTORY" ]] guard around eval (SHEL-02, shell/.zprofile:49-53) so a partial install does not crash; behavior preserved per D-09; SSH_AUTH_SOCK block (line 55-56, hardcoded `[[ "$(hostname -s)" != "server" ]]` -- CONCERNS.md known bug: any hostname containing "server" or a server machine without that hostname silently misbehaves): fix-of-v1-bug per D-10 -- v2 replaces the literal hostname check with a manifest-driven jq read of features."one-password-ssh" from $XDG_STATE_HOME/dotfiles/resolved.json (shell/.zprofile:62-68), graceful degrade on missing resolved.json (fresh machine) | shell/.zprofile |
| zsh/.zshrc:1-129 | v1 zsh interactive-shell file: ZSH_COMPDUMP, ADOTDIR (antigen), TLRC_CONFIG, antigen apply with 7 bundles (CONCERNS.md: 200-500ms cold-start overhead), DOTFILEDIR symlink-traversal block, lazy conda init, VSCode shell integration, and source loops for zsh/aliases/common/*.zsh, $DOTFILES_PROFILE alias subdir, theme.zsh, zsh/functions/*.zsh, $DOTFILES_PROFILE function subdir | ported | drop | ZSH_COMPDUMP block: present-with-delta -- v2 adds compinit daily-rebuild cache (SHEL-10, shell/.zshrc:42-57) using a 86400s mtime gate; ADOTDIR (antigen config) export: dropped (v2 uses antidote, not antigen); TLRC_CONFIG block: present-equivalent (shell/.zshrc:60); antigen apply block (line 52-72, the 7 bundles -- CONCERNS.md performance bottleneck): fix-of-v1-bug per D-10 -- v2 replaces antigen with antidote static bundle-cache loaded from configs/antidote/zsh_plugins.txt with mtime-based lazy rebuild (shell/.zshrc:73-87, SHEL-04, the primary lever for the 200ms cold-start target); DOTFILEDIR symlink-traversal block (line 75-83): present-equivalent (shell/.zshrc:62-71 identical block); lazy conda init: present-equivalent (shell/.zshrc:89-98 identical); VSCode shell integration: present-equivalent (shell/.zshrc:100-103 identical); alias source loop (v1 lines 105-115: zsh/aliases/common/*.zsh + $DOTFILES_PROFILE subdir walk): replaced-by-flat-layout -- v2 sources shell/aliases/*.zsh flat (shell/.zshrc:118-120) with per-file _dotfiles_feature source-time gates (D-08) replacing the profile/ subdir loop; theme.zsh source: present-equivalent (shell/.zshrc:115); function source loop (v1 lines 120-130: zsh/functions/*.zsh + $DOTFILES_PROFILE subdir walk): replaced-by-flat-layout -- v2 sources shell/functions/*.zsh flat first so _dotfiles_feature is defined before aliases glob (shell/.zshrc:110-112); v2 also adds a missing-machine interactive warning (shell/.zshrc:125-127, CF-05) absent in v1 | shell/.zshrc |
| zsh/.zlogin:1-19 | v1 zsh login-shell post-init: dispatch `motd` function if defined | ported | drop | motd dispatch block: present-equivalent (shell/.zlogin:1-19 byte-identical to v1); D-09 behavior is preserved because v2's shell/functions/motd.zsh implements a 24h-TTL cache internally (SHEL-11), so the same `motd` call from .zlogin now returns cached output immediately and refreshes asynchronously rather than blocking the login shell | shell/.zlogin |
| zsh/.zlogout:1-56 | v1 zsh login-shell logout hook: documentation block plus `fc -W` history flush | ported | drop | docstring block (lines 1-51): present-equivalent (shell/.zlogout byte-identical); history-flush block (line 54, `fc -W 2>/dev/null || true`): present-equivalent (shell/.zlogout byte-identical) | shell/.zlogout |
| zsh/theme.zsh:1-99 | v1 alanpeabody-derived zsh prompt: EZA_CONFIG_HOME export, native zsh prompt escapes (user/pwd/return_code/git_branch/time), ZSH_THEME_GIT_PROMPT_* color tokens, syntax-highlighting alias, man-page LESS_TERMCAP_* coloring, and grc command alias loop | ported | drop | EZA_CONFIG_HOME export block: present-equivalent (shell/theme.zsh byte-identical); prompt escapes block (user/pwd/return_code/git_branch/time): present-equivalent; ZSH_THEME_GIT_PROMPT_* block: present-equivalent; PROMPT/RPROMPT assignment block: present-equivalent; highlight alias block: present-equivalent; man() LESS_TERMCAP_* function: present-equivalent; grc command alias loop (lines 73-99): present-equivalent (byte-identical); D-09 keep-v1-prompt decision (PROJECT.md "Keep v1 prompt; reject Starship") confirmed -- v2 ships the v1 theme verbatim | shell/theme.zsh |
| zsh/functions/afk.zsh:1-6 | screensaver lock helper that runs `ScreenSaverEngine.app` to lock the screen | ported | drop | byte-identical port (diff returns empty) | shell/functions/afk.zsh |
| zsh/functions/aliaslist.zsh:1-41 | enumerate aliases by group and print them with syntax highlighting; v1 walks zsh/aliases/common/ then the $DOTFILES_PROFILE subdir | ported | drop | v2 prints "Dotfiles" from the flat shell/aliases/ directory and drops the profile-subdir branch (lines 25-35 absent in v2); behavior preserved per D-09: same operator-visible output on personal-laptop now that flat shell/aliases/ is the only alias source (manifest model replaces personal/ subdir walk); v2 also adds trailing newline (v1 lacks final newline) | shell/functions/aliaslist.zsh |
| zsh/functions/cheat.zsh:1-13 | curl wrapper around cheat.sh that prints command cheat sheets; CONCERNS.md notes plain HTTP (no TLS); v2 carries the same behavior verbatim | ported | drop | byte-identical port (diff returns empty); the plain-HTTP supply-chain note from CONCERNS.md is NOT fixed in v2 and remains a known issue for a later milestone | shell/functions/cheat.zsh |
| zsh/functions/docker.zsh:1-21 | docker helper aliases-as-function bundle (dps, dpsa, dimg, drmi, dstop, drm, etc.) | ported | drop | byte-identical port (diff returns empty) | shell/functions/docker.zsh |
| zsh/functions/fs.zsh:1-18 | print human-readable size of a file or directory using `du` | ported | drop | byte-identical port (diff returns empty) | shell/functions/fs.zsh |
| zsh/functions/functionlist.zsh:1-24 | enumerate functions by group and grep their `function` declarations with highlight | ported | drop | v2 prints "Dotfiles" from the flat shell/functions/ directory and drops the profile-subdir branch (v1 lines 13-25 absent in v2); behavior preserved per D-09: same operator-visible function listing on personal-laptop with the flat shell/functions/ tree (manifest model replaces the personal/ functions subdir, which never existed for any v1 machine anyway -- CONCERNS.md "No work functions directory" cluster); v2 adds trailing newline | shell/functions/functionlist.zsh |
| zsh/functions/geoip.zsh:1-13 | curl wrapper around ipinfo.io for ip-to-geo lookup | ported | drop | byte-identical port (diff returns empty) | shell/functions/geoip.zsh |
| zsh/functions/getcertnames.zsh:1-33 | openssl s_client wrapper that prints the certificate Subject Alternative Names for a host | ported | drop | byte-identical port (diff returns empty) | shell/functions/getcertnames.zsh |
| zsh/functions/ghpubkey.zsh:1-11 | curl wrapper that fetches a github user's public keys via https://github.com/<user>.keys | ported | drop | byte-identical port (diff returns empty) | shell/functions/ghpubkey.zsh |
| zsh/functions/host.zsh:1-11 | dig wrapper that resolves a hostname and prints A/AAAA records | ported | drop | byte-identical port (diff returns empty) | shell/functions/host.zsh |
| zsh/functions/info.zsh:1-18 | fastfetch wrapper that prints system info with the default config | ported | drop | byte-identical port (diff returns empty); CONCERNS.md notes fastfetch is invoked without a `command -v` guard so the function errors on machines without fastfetch -- known issue carried into v2 verbatim and out of scope for AUDIT-03 | shell/functions/info.zsh |
| zsh/functions/ipv4lookup.zsh:1-36 | resolve current public IPv4 via curl against multiple providers with fallback | ported | drop | byte-identical port (diff returns empty) | shell/functions/ipv4lookup.zsh |
| zsh/functions/ipv6lookup.zsh:1-37 | resolve current public IPv6 via curl against multiple providers with fallback | ported | drop | byte-identical port (diff returns empty) | shell/functions/ipv6lookup.zsh |
| zsh/functions/mkcd.zsh:1-5 | mkdir + cd combo | ported | drop | byte-identical port (diff returns empty) | shell/functions/mkcd.zsh |
| zsh/functions/motd.zsh:1-93 | Tron-themed login banner; v1 runs fastfetch + git log + git status synchronously every login (CONCERNS.md performance: seconds added to login on slow disks/network mounts) | ported | drop | ported-with-documented-delta: v2 (shell/functions/motd.zsh, 131 lines) implements a 24h-TTL cache (SHEL-11) -- public motd() displays cached content synchronously and triggers an async &!-backgrounded refresh when the cache is older than 86400s; v1's render logic is preserved verbatim in v2's _motd_render(); behavior preserved per D-09 (same operator-visible MOTD output) while the synchronous fastfetch/git-log overhead is replaced by cached lazy-load | shell/functions/motd.zsh |
| zsh/functions/permissions.zsh:1-14 | print numeric and symbolic permissions of a file via stat | ported | drop | byte-identical port (diff returns empty) | shell/functions/permissions.zsh |
| zsh/functions/prettyjson.zsh:1-10 | pipe-friendly JSON pretty-printer via python json.tool | ported | drop | byte-identical port (diff returns empty) | shell/functions/prettyjson.zsh |
| zsh/functions/pubkey.zsh:1-12 | print a public key and pipe it to pbcopy; v1 docstring example references a stale prior-employer key name `id_rsa_adobe.pub` (CONCERNS.md known issue, zsh/functions/pubkey.zsh:4) | ported | drop | ported-with-documented-delta: v2 (shell/functions/pubkey.zsh:4) fixes the docstring to reference the actual current key `id_ed25519_personal.pub` -- fix-of-v1-bug per D-10; function body byte-identical; the macOS-only pbcopy dependency (CONCERNS.md Linux/macOS divergence) is carried into v2 verbatim and out of scope for AUDIT-03 (Linux deferred to v2+ per PROJECT.md) | shell/functions/pubkey.zsh |
| zsh/functions/sethostname.zsh:1-12 | scutil wrapper that sets HostName, LocalHostName, and ComputerName | ported | drop | byte-identical port (diff returns empty) | shell/functions/sethostname.zsh |
| zsh/functions/sshlist.zsh:1-29 | enumerate configured SSH hosts; v1 walks ~/.ssh/config and the profile-specific ~/.ssh/config-$DOTFILES_PROFILE file | ported | drop | ported-with-documented-delta: v2 (shell/functions/sshlist.zsh, 20 lines) drops the profile-config branch (v1 lines 17-27 absent) and prints "Per-identity SSH config display deferred to Phase 4 (IDNT-03)"; v2 also drops the "current profile only" docstring qualifier; behavior preserved per D-09 -- the manifest model replaces the per-profile config-N file pattern, and Phase 4's identity layer will own SSH identity selection; the trailing footer references $DOTFILEDIR/ssh/configs/config in v2 vs ~/.ssh/config-$profile in v1 | shell/functions/sshlist.zsh |
| zsh/functions/timezsh.zsh:1-10 | time 10 iterations of zsh -i -c exit; used to measure interactive shell cold-start | ported | drop | byte-identical port (diff returns empty) | shell/functions/timezsh.zsh |
| zsh/functions/update.zsh:1-21 | wrapper that cd's into $DOTFILEDIR and runs `task update` to update dotfiles + dependencies | dropped | drop | v2 has NO shell/functions/update.zsh sibling; PROJECT.md / .claude/CLAUDE.md state "Re-install / update: `task install`" and the validated requirement "task install is the canonical entry -- task update is dropped per D-10. Phase 3 ships shell alias update='task install' for muscle memory" -- the function's behavior is fully superseded by `task install` (canonical pipeline, D-10 "install IS update") and by the v2 `update` alias; keep/drop = drop because no operator-useful behavior remains beyond what `task install` and the v2 alias already provide | shell/functions/update.zsh (n/a -- dropped) |
| zsh/functions/vnc.zsh:1-9 | open a vnc:// URL via the macOS `open` command | ported | drop | byte-identical port (diff returns empty) | shell/functions/vnc.zsh |
| zsh/functions/whois.zsh:1-46 | wrap the system whois command with output trimming and TLD-aware server selection | ported | drop | byte-identical port (diff returns empty) | shell/functions/whois.zsh |
| zsh/aliases/common/general.zsh:1-43 | v1 always-loaded alias bucket: reload, environment, path, dotfile/dotfiles, fsa, perms, ls, ll, finder, findershow, finderhide, lastinstalled, history, g (ghostty), t (task) | partially-ported | drop | Per-machine effective-set diff (D-06): on personal-laptop the v1 set [reload, environment, path, dotfile, dotfiles, fsa, perms, ls, ll, finder, findershow, finderhide, lastinstalled, history, g, t] vs v2's shell/aliases/general.zsh + finder.zsh + ghostty.zsh -> zero v1 alias names missing; on server-1 / server-2 / work-laptop the v1 set is the same (no work/ subdir exists per CONCERNS.md) and on those machines v2 intentionally OMITS finder/findershow/finderhide (gated on features.macos-finder=false for server) and g (gated on features.ghostty=false for server) -- D-09 intentional improvement; finder aliases relocated to shell/aliases/finder.zsh under the macos-finder feature gate (CONCERNS.md macOS-only commands in common scope), g relocated to shell/aliases/ghostty.zsh under the ghostty feature gate; Body-level diff vs shell/aliases/general.zsh: v1 lines 26-31 (finder/findershow/finderhide) and 39-40 (g="/Applications/Ghostty.app/...") are absent in v2's general.zsh -- they live in their gated files; remaining alias bodies (reload, environment, path, dotfile, dotfiles, fsa, perms, ls, ll, lastinstalled, history, t) are byte-identical RHS values; no value drift detected per D-06 | shell/aliases/general.zsh + shell/aliases/finder.zsh + shell/aliases/ghostty.zsh |
| zsh/aliases/common/hardware.zsh:1-9 | v1 always-loaded macOS-only hardware-inspection aliases: gpu, cpu, ram, disk, bluetooth, speaker, webcam, power, monitor (all via system_profiler / sysctl / diskutil) | ported | drop | Per-machine effective-set diff (D-06): all 9 alias names present in v2 shell/aliases/hardware.zsh on every machine; body-level diff vs shell/aliases/hardware.zsh: byte-identical (diff returns empty) -- no value drift; CONCERNS.md Linux/macOS divergence note (macOS-only commands in common scope) carried into v2 verbatim since v1 milestone targets macOS only -- Linux deferred to v2+ per PROJECT.md, so the macOS-only commands are correct for every current target machine | shell/aliases/hardware.zsh |
| zsh/aliases/common/networking.zsh:1-12 | v1 always-loaded networking aliases: dnsflush, traceroute, ip, ipv4, ipv6 | ported | drop | Per-machine effective-set diff (D-06): all 5 alias names present in v2 shell/aliases/networking.zsh on every machine; body-level diff vs shell/aliases/networking.zsh: byte-identical (diff returns empty) -- no value drift in any RHS including the dnsflush sudo killall chain; CONCERNS.md note that dnsflush uses macOS-only mDNSResponder/dscacheutil is carried into v2 verbatim since v1 targets macOS only | shell/aliases/networking.zsh |
| zsh/aliases/personal/jgrid.zsh:1-39 | v1 personal-profile alias bucket: 22 ssh-jump aliases (one per allomantic metal, e.g. `alias steel="ssh josh@steel-ssh.jgrid.net"`) loaded only under the personal profile via .zshrc's profile-subdir source loop | ported | drop | Per-machine effective-set diff (D-06): on personal-laptop (features.jgrid-net=true) v2 shell/aliases/jgrid.zsh defines all 22 metal aliases via the same METALS loop; body-level diff: identical METALS array and identical `alias $i="ssh josh@$i-ssh.jgrid.net"` line -- v2 adds (a) the file-header docstring block (lines 2-15) explaining the source-time gate rationale per D-08 and (b) the `[[ "$(_dotfiles_feature jgrid-net)" == "true" ]] || return 0` source-time gate (line 17) that replaces v1's `$DOTFILES_PROFILE`-subdir source-loop branch in .zshrc; on server-1/server-2/work-laptop (features.jgrid-net default false) v2 source-loads the file and returns 0 immediately -- 22 aliases never defined, matching v1's behavior on those machines (where the personal/ subdir was not walked under non-personal profiles); D-09 manifest model is behavior-equivalent to v1's personal/ profile-suffix loop; no RHS value drift in any of the 22 metal aliases | shell/aliases/jgrid.zsh |
| zsh/configs/condarc:1-11 | v1 conda configuration file (channels, env paths, install behavior) for the miniconda cask consumed by lazy conda init in .zshrc | ported | drop | Body-level diff (D-07) vs configs/conda/condarc: byte-identical (diff returns empty); Phase 7 TOOL-02 ported this file; symlink revalidation is Phase 7 TOOL-04 scope and out of scope here (per 09-CONTEXT.md specifics) | configs/conda/condarc |
| zsh/configs/ghostty:1-114 | v1 Ghostty terminal-emulator configuration (font, theme, padding, keybindings) consumed when the ghostty cask is installed | ported | drop | Body-level diff (D-07) vs configs/ghostty/config: byte-identical (diff returns empty); v2 path is configs/ghostty/config (renamed from v1's zsh/configs/ghostty -- single-file directory layout per the v2 configs/<tool>/<primary-file> convention); Phase 7 TOOL-02 ported this file | configs/ghostty/config |
| zsh/configs/glow.yml:1-22 | v1 glow markdown viewer configuration (theme, default style, mouse mode) | ported | drop | Body-level diff (D-07) vs configs/glow/glow.yml: byte-identical (diff returns empty); Phase 7 TOOL-02 ported this file | configs/glow/glow.yml |
| zsh/configs/motd_sysinfo.jsonc:1-59 | v1 fastfetch JSONC configuration consumed by motd.zsh and info.zsh for the Tron-themed sysinfo display | ported | drop | Body-level diff (D-07) vs configs/motd/motd_sysinfo.jsonc: byte-identical (diff returns empty); Phase 7 TOOL-02 ported this file; motd.zsh in v2 still references the v1 path ($DOTFILEDIR/zsh/configs/motd_sysinfo.jsonc per shell/functions/motd.zsh:91) -- a Phase 10 keep candidate for path migration, but the file body itself is unchanged | configs/motd/motd_sysinfo.jsonc |
| zsh/configs/motd_tron.txt:1-13 | v1 Tron-quote source consumed by motd.zsh for the random "transmission" line | ported | drop | Body-level diff (D-07) vs configs/motd/motd_tron.txt: byte-identical (diff returns empty); Phase 7 TOOL-02 ported this file; v2 motd.zsh:115 still references the v1 path -- a Phase 10 keep candidate for path migration, but the file body itself is unchanged | configs/motd/motd_tron.txt |
| zsh/configs/tlrc.toml:1-86 | v1 tlrc (tldr client) configuration consumed via TLRC_CONFIG env var set in .zshrc | ported | drop | Body-level diff (D-07) vs configs/tlrc/config.toml: byte-identical (diff returns empty); v2 path is configs/tlrc/config.toml (renamed from v1's tlrc.toml to match the tlrc tool's expected config-file name); Phase 7 TOOL-02 ported this file | configs/tlrc/config.toml |
| zsh/configs/trippy.toml:1-361 | v1 trippy (network diagnostics) configuration with theme + keybindings consumed by the traceroute alias replacement | ported | drop | Body-level diff (D-07) vs configs/trippy/trippy.toml: byte-identical (diff returns empty); Phase 7 TOOL-02 ported this file | configs/trippy/trippy.toml |
| zsh/styles/eza_style.yaml:1-230 | v1 eza directory-listing style/theme file consumed via EZA_CONFIG_HOME exported from theme.zsh | ported | drop | Body-level diff (D-07) vs configs/eza/theme.yaml: byte-identical (diff returns empty); v2 path is configs/eza/theme.yaml (renamed from v1's eza_style.yaml to match eza's expected $EZA_CONFIG_HOME/theme.yaml convention); Phase 7 TOOL-02 ported this file | configs/eza/theme.yaml |
| zsh/styles/glow_style.json:1-197 | v1 glow markdown viewer JSON style file used to customize prose rendering | ported | drop | Body-level diff (D-07) vs configs/glow/glow_style.json: byte-identical (diff returns empty); Phase 7 TOOL-02 ported this file | configs/glow/glow_style.json |

## Docs

_Rows are written by plan 09-04 (install/README.md + any v1-only docs/ fragments) from shards/docs.md._

| file:line | purpose | v2 status | keep/drop | rationale | v2 owner |
|-----------|---------|-----------|-----------|-----------|----------|
| install/README.md:1-9 | H1 + preamble: "install-engine machinery" purpose statement for the install/ directory (resolver, messages library, cutover-gate, with a forward note that Phase 5 will move the Brewfile bundles out) | partially-ported | drop | grep -nFi 'install-engine' across docs/MANIFEST.md docs/SECURITY.md docs/CUTOVER.md docs/MIGRATION.md docs/MACHINES.md docs/README.md README.md returns zero matches; the install/ directory's purpose statement is not paraphrased verbatim in any v2 docs/ file, but the role of install/ is implicit in README.md:14 ("install/resolver.zsh") and docs/MANIFEST.md:8 ("install/resolver.zsh compiles them into ..."). Per D-09 behavior-equivalence: a fresh operator reading the v2 docs/ set learns where the resolver lives but not the directory-level purpose statement; the install/README.md itself remains the canonical purpose statement for install/. Git-history archeology beyond install/README.md and any install/*.md leftovers was checked via `git log --diff-filter=D --name-only --pretty=format: -- '*.md'` then `sort -u` then `head -20` and yielded only zsh/cheat/ cheat sheets, docs/consistent-messaging-plan.md, docs/go-task-analysis.md, .github/copilot-instructions.md, and planning todos — none are v1-install-engine doc fragments per 09-CONTEXT.md `<deferred>` (out of scope unless trivial). | install/README.md |
| install/README.md:10-35 | ## Key files — documents resolver.zsh (MFST-04 manifest resolver), messages.zsh (colored-output library), cutover-gate.zsh (BTSP-06 precondition), plus four v1 Brewfile bullets that the README itself flags as transitional ("Phase 5 ... refactors these into per-purpose bundles under packages/<purpose>.rb") | ported | drop | The resolver/messages/cutover-gate paragraphs are install-engine internals documentation that stay alive in v2; the install/ directory still ships resolver.zsh, messages.zsh, and cutover-gate.zsh (verified: `ls install/*.zsh` shows all three present). The Brewfile bullets at lines 29-34 are v1-transitional and superseded by Phase 5 — `grep -nFi 'Brewfile' docs/MIGRATION.md` shows lines 22, 47, 75-78, 83, 96-97 fully paraphrase the Brewfile-by-profile retirement; `grep -nFi 'Brewfile' packages/README.md` shows lines 55, 67-68, 98, 100-101, 108, 112 carry the composed-Brewfile cache documentation. Per D-09 the v2 model retired the Brewfile-by-profile pattern intentionally; install/README.md itself stays as the v2 owner for the install-engine internals subset of this section. Phase 14 TRIM-03 may trim the Brewfile bullets out of install/README.md when v1 source files are deleted in Phase 11 RMV-01. | install/README.md |
| install/README.md:23-28 | cutover-gate.zsh paragraph (within ## Key files): describes BTSP-06 / D-12 cutover-gate as a precondition for `task install` that reads $XDG_STATE_HOME/dotfiles/machine and the cutover-ack sentinel, exits non-zero with an actionable error when absent | ported | drop | `grep -nFi 'cutover-gate' docs/MIGRATION.md docs/CUTOVER.md` returns docs/MIGRATION.md:223 ("the cutover-gate reader will now block task install"), docs/CUTOVER.md:34 ("the cutover-gate is invoked at the end of bootstrap"), and docs/CUTOVER.md:52 ("install/cutover-gate.zsh"). v2 docs/ carries the cutover-gate operator-visible behavior (the gate's role and the actionable error path). Per D-09 paraphrase-OK threshold: the v1 README's description of cutover-gate.zsh is operator-equivalent to the v2 docs/CUTOVER.md coverage. Phase 11 RMV-04 retires docs/CUTOVER.md after the soak window closes; Phase 14 TRIM-03 then folds any residual cutover narrative into docs/MIGRATION.md or removes it — until then, docs/CUTOVER.md is the transient v2 owner of the cutover-gate behavior documentation, and install/README.md stays the owner of the implementation-level reference to install/cutover-gate.zsh as an install-engine file. | docs/CUTOVER.md |
| install/README.md:29-34 | Brewfile transitional commentary (within ## Key files): four bullets for Brewfile.rb, Brewfile-personal.rb, Brewfile-server.rb, Brewfile-work.rb with the explicit Phase 5 (PKGS-01..05) refactor note and the v2 rule "Do NOT add new profile-suffixed Brewfiles here -- the v2 model is purpose-named bundles composed by the manifest, not profile-suffixed files" | ported | drop | Phase 5 has shipped per ROADMAP (v2 owns `packages/core.rb` and `packages/gui.rb`, listed in machine TOML `bundles` arrays; cross-checked via `ls packages/` returning core.rb, gui.rb, README.md). The Brewfile-by-profile retirement is fully paraphrased in docs/MIGRATION.md (line 22 "encoded each machine's install state through ... filename suffixes -- Brewfile-<profile>.rb", lines 75-98 "## Brewfile-<profile>.rb -> packages/<purpose>.rb + extra_packages" with the explicit per-row path mapping including install/Brewfile-personal.rb -> packages/core.rb + packages/gui.rb at line 96). The v2 purpose-named bundle UX is documented in packages/README.md lines 14-36 (core.rb / gui.rb / extra_packages typed sub-table) and lines 39-55 (## Adding a pattern, bundle creation criteria). Per D-09 the v2 model retired the Brewfile-by-profile pattern intentionally; the v1 bullets carry no operator-visible information the v2 docs do not already cover. v2 owner is packages/README.md (the per-purpose bundle layout owner); Phase 11 RMV-02 deletes the four v1 Brewfiles and Phase 14 TRIM-03 may trim these bullets out of install/README.md. | packages/README.md |
| install/README.md:36-55 | ## Adding a pattern — three sub-fragments: (a) "A new install-engine script" naming the shebang + set -euo pipefail rule (LINT-04), file-header comment block convention, and the `{{.DOTFILEDIR}}/install/<name>.zsh` wiring pattern; (b) "A new task-helper function" naming messages.zsh + _LOADED guard convention; (c) "A new Brew package" v1-transitional "wait for Phase 5" guidance | ported | drop | The install-script and task-helper sub-fragments document install-engine extension patterns that remain valid in v2 — install/README.md itself is the canonical home for these conventions (the README is preserved by Phase 11; RMV-01 only deletes the eight v1 leftover taskfiles, not install/README.md). The "wait for Phase 5" Brewfile sub-fragment at lines 51-54 is v1-historical: Phase 5 has shipped (grep -nFi 'extra_packages' packages/README.md shows lines 5, 29, 31, 50 — the per-machine extras typed sub-table is the v2-realized successor that the v1 README told the reader to wait for). Per D-09 the v2 docs/ set (CLAUDE.md root, packages/README.md) carries the operator-visible information for both the install-engine convention sub-fragments and the v2-replacement-for-the-wait-for-Phase-5 sub-fragment. install/README.md stays the owner of install-engine extension conventions; packages/README.md is the v2 owner for the Brewfile-extension sub-fragment after Phase 14 TRIM-03. | install/README.md |
| install/README.md:51-54 | "Adding a pattern -> A new Brew package" sub-fragment (within ## Adding a pattern): "Do NOT edit the Brewfiles here in Phase 3 scope -- wait for Phase 5. Once Phase 5 lands, add packages to packages/<purpose>.rb (named by role, not by profile) or to the machine manifest's extra_packages array for one-offs." | ported | drop | Phase 5 has shipped per ROADMAP; the v2 reality is that packages/<purpose>.rb (core.rb, gui.rb) exists and `extra_packages` typed sub-table is the per-machine escape hatch — `grep -nFi 'extra_packages' packages/README.md` returns lines 5, 29, 31, 50 covering the full UX; `grep -nFi 'Brewfile' docs/MIGRATION.md` returns lines 47, 75-78, 83, 96-97 with the per-concept Brewfile-by-profile -> packages/<purpose>.rb mapping. Per D-09 the v2 docs/ set fully paraphrases the post-Phase-5 reality; the v1 "wait" guidance is now-historical. Phase 14 TRIM-03 may delete these four lines outright when the v1 Brewfiles are removed in Phase 11 RMV-02. | packages/README.md |
| install/README.md:56-70 | ## References — five cross-references to ../docs/MANIFEST.md, ../docs/SECURITY.md, ../taskfiles/manifest.yml, ../CLAUDE.md, ../.planning/REQUIREMENTS.md, plus the final "Satisfies DOCS-02 for install/" line | ported | drop | Cross-reference list. Per D-09 every referenced target exists in v2 — verified via `ls docs/MANIFEST.md docs/SECURITY.md taskfiles/manifest.yml CLAUDE.md .planning/REQUIREMENTS.md` (all present). The references are operator-equivalent to the v2 docs/README.md index (which lists MANIFEST.md, SECURITY.md, MIGRATION.md, MACHINES.md, CUTOVER.md) and the root README.md's "## Documentation" section at lines 60-67. install/README.md stays the canonical owner of the install/-rooted cross-reference list because the targets are read from install/'s point of view (relative paths starting with `../`). No v2 docs/ file paraphrases the install/-rooted cross-reference set; the references serve install/ directly. | install/README.md |
| install/*.md inventory | Catch-all confirming no other markdown files exist in install/ beyond README.md (the v2 install/-rooted markdown surface is exactly install/README.md per Phase 1 DOCS-02) | n/a | n/a | `find install -maxdepth 1 -name '*.md'` returns exactly one path: install/README.md. No other v1 leftover .md files exist under install/. The v2 set assertion holds. This row is informational-only; no classification applies because there are no additional fragments to classify. | n/a |


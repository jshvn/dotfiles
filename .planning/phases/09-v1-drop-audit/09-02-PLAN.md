---
phase: 09-v1-drop-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .planning/phases/09-v1-drop-audit/shards/taskfiles.md
  - .planning/phases/09-v1-drop-audit/shards/install-assets.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
user_setup: []

must_haves:
  truths:
    - "Every task defined in each of the eight v1 leftover taskfiles (taskfiles/common.yml, profile.yml, brew.yml, profile-tasks.yml, claude-stub.yml, brew-stub.yml, links-stub.yml, macos.v1.yml.bak) appears as exactly one row in shards/taskfiles.md (AUDIT-01, D-02)"
    - "Each Taskfiles row carries the six locked columns in order: file:line range | purpose (one sentence) | v2 status (ported/partially-ported/dropped) | keep/drop | rationale | v2 owner (D-03)"
    - "The /etc/zshenv ZDOTDIR write at taskfiles/common.yml:36-57 is captured as a row classified keep with v2 owner = taskfiles/shell.yml (or equivalent v2 owner named) — this is the milestone driver per ROADMAP Phase 9 SC#2"
    - "Stub taskfiles (claude-stub.yml, brew-stub.yml, links-stub.yml) are classified v2 status=ported, keep/drop=drop with rationale naming the v2 real file (claude.yml, packages.yml, links.yml) that superseded them (D-11)"
    - "The macos.v1.yml.bak row explicitly cross-references the $BREW_ZSH shell-vs-template-var bug (CONCERNS.md known bug class) with v2 status=ported, keep/drop=drop, rationale naming the bug and v2 fix in taskfiles/macos.yml (D-10)"
    - "Every v1 Brewfile (Brewfile.rb, Brewfile-personal.rb, Brewfile-work.rb, Brewfile-server.rb) is audited at the package level: build v1 effective set per machine, build v2 effective set via install/resolver.zsh per machine, diff at formula/cask/mas name; any v1 package absent from the v2 effective set on the same machine becomes a row (AUDIT-02, D-12)"
    - "Any install/*.zsh file not in the v2 set (v2 set = messages.zsh, resolver.zsh, compose-brewfile.zsh, cutover-gate.zsh, test-hooks.zsh) is enumerated in shards/install-assets.md (AUDIT-02)"
  artifacts:
    - path: .planning/phases/09-v1-drop-audit/shards/taskfiles.md
      provides: "One AUDIT row per task across the eight v1 leftover taskfiles (D-02)"
      contains:
        - "| taskfiles/common.yml:36-57"
        - "| taskfiles/claude-stub.yml:"
        - "| taskfiles/brew-stub.yml:"
        - "| taskfiles/links-stub.yml:"
        - "| taskfiles/macos.v1.yml.bak:"
    - path: .planning/phases/09-v1-drop-audit/shards/install-assets.md
      provides: "Brewfile package-level audit + any non-v2 install/*.zsh leftover rows"
      contains:
        - "| install/Brewfile.rb"
        - "| install/Brewfile-personal.rb"
        - "| install/Brewfile-work.rb"
        - "| install/Brewfile-server.rb"
  key_links:
    - from: ".planning/phases/09-v1-drop-audit/shards/taskfiles.md"
      to: ".planning/phases/09-v1-drop-audit/AUDIT.md"
      via: "plan 09-05 concatenation into ## Taskfiles section"
      pattern: "appended verbatim under '## Taskfiles' header row"
    - from: ".planning/phases/09-v1-drop-audit/shards/install-assets.md"
      to: ".planning/phases/09-v1-drop-audit/AUDIT.md"
      via: "plan 09-05 concatenation into ## Install Assets section"
      pattern: "appended verbatim under '## Install Assets' header row"
---

<objective>
Produce two shard files — `shards/taskfiles.md` and `shards/install-assets.md` — that together carry every v1 leftover taskfile task (AUDIT-01) and every v1 install asset row (AUDIT-02) in the six-column shape locked by D-03.

Purpose: AUDIT-01 requires "every task ... is enumerated"; AUDIT-02 requires "every v1 install asset ... is enumerated"; SC#5 (cross-reference grep) requires every task name in the v1 taskfile-name grep to appear in AUDIT.md. This plan produces the source rows that plan 09-05 then concatenates into AUDIT.md's `## Taskfiles` and `## Install Assets` sections.

Output: Two shard files containing pre-formatted six-column rows, ready for verbatim concatenation by plan 09-05.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-v1-drop-audit/09-CONTEXT.md
@.planning/phases/09-v1-drop-audit/AUDIT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@CLAUDE.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install/resolver.zsh execution | Resolver is read-only over committed manifest files; produces resolved.json under $XDG_STATE_HOME without mutating repo state. v1 Brewfiles are read as text, never executed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Tampering | install/resolver.zsh invocation | accept | resolver writes to $XDG_STATE_HOME/dotfiles/resolved.json only; deterministic over committed inputs. Read-only effect on repo. |
| T-09-02b | Information Disclosure | Brewfile contents pasted into AUDIT shards | accept | Brewfiles contain formula/cask names only — public package identifiers, no secrets. |
| T-09-02-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate every task in the eight v1 leftover taskfiles into shards/taskfiles.md (AUDIT-01, D-02, D-03, D-10, D-11)</name>
  <files>.planning/phases/09-v1-drop-audit/shards/taskfiles.md</files>
  <read_first>
    - taskfiles/common.yml (full file, 88 lines) — note the ZDOTDIR write at lines 36-57 (the milestone driver)
    - taskfiles/profile.yml (full file, 110 lines) — profile selection, ensure, set, validate tasks
    - taskfiles/profile-tasks.yml (full file, 110 lines) — parameterized per-profile install/links/brew/validate
    - taskfiles/brew.yml (full file, 72 lines) — install, ensure-homebrew, update, bundle, validate
    - taskfiles/claude-stub.yml (full file, 22 lines) — Phase 7 scaffold superseded by taskfiles/claude.yml
    - taskfiles/brew-stub.yml (full file, 10 lines) — Phase 5 scaffold superseded by taskfiles/packages.yml
    - taskfiles/links-stub.yml (full file, 21 lines) — Phase 3 scaffold superseded by taskfiles/links.yml
    - taskfiles/macos.v1.yml.bak (full file, 183 lines) — contains the $BREW_ZSH bug line
    - taskfiles/shell.yml (v2 owner candidate for the ZDOTDIR write)
    - taskfiles/manifest.yml (v2 owner for setup / profile-selection-equivalent tasks)
    - taskfiles/packages.yml (v2 owner for brew.yml's install/update/bundle tasks)
    - taskfiles/macos.yml (v2 owner for macos.v1.yml.bak tasks; carries the $BREW_ZSH → {{.BREW_ZSH}} fix)
    - taskfiles/claude.yml (v2 owner for claude-stub.yml)
    - taskfiles/links.yml (v2 owner for links-stub.yml)
    - .planning/codebase/CONCERNS.md (known v1 bug list — every row here must map to a row in shards/taskfiles.md if its v1 source is a task)
    - .planning/phases/09-v1-drop-audit/AUDIT.md (the skeleton — use the column header verbatim)
  </read_first>
  <action>
    For each task definition in each of the eight v1 leftover taskfiles, emit exactly one Markdown table row in `.planning/phases/09-v1-drop-audit/shards/taskfiles.md` (D-02: row granularity = per task, not per file).

    Row format (D-03, six columns, exact order):
    `| <file>:<line-start>-<line-end> | <one-sentence purpose> | <ported | partially-ported | dropped> | <keep | drop> | <rationale> | <v2 owner file or "n/a"> |`

    File header lines: emit zero file headers — the rows themselves identify their source via the `file:line` column. Plan 09-05 will append these rows under the `## Taskfiles` header in AUDIT.md. The shard MUST NOT contain a `## Taskfiles` header of its own (avoid duplicate headers after concatenation).

    Discovery method per taskfile:
    1. Locate every task block. A task is the YAML key with shape `  <task-name>:` followed by `desc:`, `cmds:`, `status:`, etc. Use `grep -nE '^  [a-z][a-z0-9_-]*:' <file>` to find candidates; verify each is a task by checking for a `desc:` or `cmds:` sibling within ~10 lines below.
    2. Determine the line range: start = the line with the task-name key; end = the last contiguous indented line before the next top-level key, the next sibling task key, or EOF.
    3. Compose the purpose in one sentence from the task's `desc:` field (paraphrase if too long).
    4. Determine v2 status:
       - `ported` = behavior-equivalent under v2 manifest model on same machine (D-09).
       - `partially-ported` = some sub-behavior exists in v2, others dropped.
       - `dropped` = no v2 equivalent.
    5. Determine keep/drop:
       - `keep` = the behavior is missing from v2 and must be implemented in Phase 10.
       - `drop` = v2 already covers it (ported), or the behavior is intentionally retired by v2's design.
    6. Rationale: name the specific v2 file/task that supersedes the row, OR name the missing behavior + why Phase 10 must port it.
    7. v2 owner: the v2 file that already owns the equivalent (for ported/drop) or the v2 file Phase 10 should land the port in (for keep). Use `taskfiles/shell.yml` for shell-startup-time behavior, `taskfiles/manifest.yml` for setup/profile-equivalent, `taskfiles/packages.yml` for brew, `taskfiles/macos.yml` for macOS defaults, `taskfiles/claude.yml` for Claude, `taskfiles/links.yml` for symlinks. If unknown, write `n/a` and rationale must explain.

    REQUIRED rows (the audit must contain these specific entries, classification must match):
    - `| taskfiles/common.yml:36-57 | Write `export ZDOTDIR="$HOME/.config/zsh"` to /etc/zshenv via sudo | dropped | keep | Milestone driver — v2 silently dropped this; without it a fresh-machine first shell has no ZDOTDIR set. PORT-01 implements this in Phase 10. | taskfiles/shell.yml |`
    - `| taskfiles/claude-stub.yml:<range> | <purpose> | ported | drop | Phase 7 scaffold; replaced by taskfiles/claude.yml real bodies (D-11). | taskfiles/claude.yml |` (per task in the stub)
    - `| taskfiles/brew-stub.yml:<range> | <purpose> | ported | drop | Phase 5 scaffold; replaced by taskfiles/packages.yml (D-11). | taskfiles/packages.yml |` (per task in the stub)
    - `| taskfiles/links-stub.yml:<range> | <purpose> | ported | drop | Phase 3 scaffold; replaced by taskfiles/links.yml real bodies (D-11). | taskfiles/links.yml |` (per task in the stub)
    - At least one row for `taskfiles/macos.v1.yml.bak` MUST cite the `$BREW_ZSH` bug class — exact text fragment "$BREW_ZSH" must appear in the rationale column, and the row MUST be classified `ported | drop` per D-10, with v2 owner `taskfiles/macos.yml` (which carries the `{{.BREW_ZSH}}` template-var fix).
    - For every task in `taskfiles/profile.yml` and `taskfiles/profile-tasks.yml` (profile selection / per-profile install / per-profile validate): the row must explicitly note that v2 replaces profile-based dispatch with the manifest-model `$DOTFILES_MACHINE` selection (no profile variable in v2). v2 owner is typically `taskfiles/manifest.yml` (setup / manifest:resolve) or `taskfiles/links.yml` / `taskfiles/identity.yml` for the per-profile symlink/identity behavior.

    Concerns mapping (per CONCERNS.md): the known v1 bugs (`macos:shell:145` $BREW_ZSH, `gsd-install` no-status-guard at `taskfiles/claude.yml:211-219` — but that one is v2's own claude.yml, not v1 leftover, so skip it for AUDIT-01) — every concern whose v1 source is one of the eight leftover taskfiles must be reflected in a row's rationale column.

    No emojis. No AI attribution. No additional columns beyond the six locked in D-03.

    After writing, run a self-check grep to verify every task name returned by SC#5's grep on these taskfiles appears in shards/taskfiles.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-v1-drop-audit/shards/taskfiles.md && grep -q '| taskfiles/common.yml:36-57 |' .planning/phases/09-v1-drop-audit/shards/taskfiles.md && grep -q 'ZDOTDIR' .planning/phases/09-v1-drop-audit/shards/taskfiles.md && grep -q 'taskfiles/claude.yml' .planning/phases/09-v1-drop-audit/shards/taskfiles.md && grep -q 'taskfiles/packages.yml' .planning/phases/09-v1-drop-audit/shards/taskfiles.md && grep -q 'taskfiles/links.yml' .planning/phases/09-v1-drop-audit/shards/taskfiles.md && grep -qE '\$BREW_ZSH' .planning/phases/09-v1-drop-audit/shards/taskfiles.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/09-v1-drop-audit/shards/taskfiles.md` exits 0
    - `grep -q '| taskfiles/common.yml:36-57 |' .planning/phases/09-v1-drop-audit/shards/taskfiles.md` exits 0 (milestone driver row present, AUDIT-01 + ROADMAP SC#2)
    - `grep -c '| taskfiles/common.yml:36-57 |.*| dropped | keep |' .planning/phases/09-v1-drop-audit/shards/taskfiles.md` returns `1` (the ZDOTDIR row is classified `dropped` + `keep` exactly once)
    - `grep -q '| taskfiles/common.yml:36-57 |.*taskfiles/shell.yml' .planning/phases/09-v1-drop-audit/shards/taskfiles.md` exits 0 (v2 owner correctly named per ROADMAP Phase 10 SC#1)
    - `grep -qE '\| taskfiles/claude-stub.yml:[0-9-]+ \|.*\| ported \| drop \|.*taskfiles/claude.yml' .planning/phases/09-v1-drop-audit/shards/taskfiles.md` exits 0 (D-11: claude-stub → claude.yml, ported/drop)
    - `grep -qE '\| taskfiles/brew-stub.yml:[0-9-]+ \|.*\| ported \| drop \|.*taskfiles/packages.yml' .planning/phases/09-v1-drop-audit/shards/taskfiles.md` exits 0 (D-11: brew-stub → packages.yml, ported/drop)
    - `grep -qE '\| taskfiles/links-stub.yml:[0-9-]+ \|.*\| ported \| drop \|.*taskfiles/links.yml' .planning/phases/09-v1-drop-audit/shards/taskfiles.md` exits 0 (D-11: links-stub → links.yml, ported/drop)
    - `grep -qE '\| taskfiles/macos.v1.yml.bak:[0-9-]+ \|.*BREW_ZSH.*\| ported \| drop \|.*taskfiles/macos.yml' .planning/phases/09-v1-drop-audit/shards/taskfiles.md` exits 0 (D-10: bug-class cross-reference + correct v2 owner)
    - SC#5 cross-reference proof: `comm -23 <(grep -hE '^  [a-z][a-z0-9_-]*:' taskfiles/common.yml taskfiles/profile.yml taskfiles/brew.yml taskfiles/profile-tasks.yml taskfiles/claude-stub.yml taskfiles/brew-stub.yml taskfiles/links-stub.yml taskfiles/macos.v1.yml.bak | sed -E 's/^  ([a-z][a-z0-9_-]*):.*/\1/' | sort -u) <(grep -oE '^\| (taskfiles/[a-z.v0-9-]+\.(yml|bak)):' .planning/phases/09-v1-drop-audit/shards/taskfiles.md | sort -u | sed 's/^| //' | sed 's/:$//' | xargs -I{} basename {} | sed 's/\.\(yml\|bak\)$//')` — informational check: every task NAME from the v1 leftovers must be representable as a row whose file:line range falls inside that task's range. (This is the SC#5 grep cited in the planning context; the strict "every name appears" check runs in plan 09-05 over the assembled AUDIT.md.)
    - `grep -cP '^\|[^|]*:[0-9]+(-[0-9]+)? \|[^|]*\|[^|]*\|[^|]*\|[^|]*\|[^|]*\|' .planning/phases/09-v1-drop-audit/shards/taskfiles.md | awk '$1 >= 20 {exit 0} {exit 1}'` — at minimum 20 data rows present (lower bound; the eight files contain >20 tasks combined per the grep sample shown in planning).
    - No emojis: `grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" .planning/phases/09-v1-drop-audit/shards/taskfiles.md` returns no matches (exit non-zero).
  </acceptance_criteria>
  <done>shards/taskfiles.md contains one D-03-shaped row per task across all eight v1 leftover taskfiles, the ZDOTDIR milestone-driver row is present with correct classification, stub taskfiles map to their v2 owners per D-11, and the $BREW_ZSH bug class is cross-referenced per D-10.</done>
</task>

<task type="auto">
  <name>Task 2: Audit v1 Brewfiles at the package level and enumerate non-v2 install/*.zsh assets in shards/install-assets.md (AUDIT-02, D-12)</name>
  <files>.planning/phases/09-v1-drop-audit/shards/install-assets.md</files>
  <read_first>
    - install/Brewfile.rb (v1 common packages — full file)
    - install/Brewfile-personal.rb (v1 personal-profile packages — full file)
    - install/Brewfile-work.rb (v1 work-profile packages — full file)
    - install/Brewfile-server.rb (v1 server-profile packages — full file)
    - install/messages.zsh (v2 set member — not in scope for AUDIT-02 rows)
    - install/resolver.zsh (v2 set; also: read its --machine flag handling so the per-machine resolve invocation is correct)
    - install/compose-brewfile.zsh (v2 set member; this is the v2 effective-set producer per machine)
    - install/cutover-gate.zsh (v2 set member)
    - install/test-hooks.zsh (v2 set member)
    - install/README.md (header banner only — see whether it documents any non-v2-set zsh files)
    - packages/core.rb, packages/gui.rb (v2 bundles for the effective-set diff)
    - manifests/defaults.toml (defaults for the resolver)
    - manifests/machines/personal-laptop.toml
    - manifests/machines/work-laptop.toml
    - manifests/machines/server-1.toml
    - manifests/machines/server-2.toml
    - .planning/phases/09-v1-drop-audit/09-CONTEXT.md sections `<decisions>` D-12 and `<canonical_refs>` install assets list
    - .planning/codebase/CONCERNS.md (any concern citing install/Brewfile* or install/*.zsh)
  </read_first>
  <action>
    Produce `.planning/phases/09-v1-drop-audit/shards/install-assets.md` with TWO row blocks (no shard-internal section headers — plan 09-05 concatenates the rows under `## Install Assets` in AUDIT.md).

    **Block A — v1 Brewfiles, package-level diff per machine (D-12, AUDIT-02):**

    1. For each of the four machine manifests, run the resolver to produce a v2 resolved.json:
       ```
       install/resolver.zsh --machine personal-laptop > /tmp/resolved-personal-laptop.json
       install/resolver.zsh --machine work-laptop > /tmp/resolved-work-laptop.json
       install/resolver.zsh --machine server-1 > /tmp/resolved-server-1.json
       install/resolver.zsh --machine server-2 > /tmp/resolved-server-2.json
       ```
       (Inspect `install/resolver.zsh` to confirm the exact CLI; if the resolver uses an env var instead of `--machine`, adapt accordingly. The output path may differ — write to /tmp temporarily.)

    2. Build the v2 effective package set per machine from resolved.json by combining `packages.brew.bundles` (which maps to `packages/<bundle>.rb` files) and `packages.brew.extra_packages` (typed sub-table: `formulae`, `casks`, `mas`). Use `jq` to extract the lists. Compose the per-machine v2 effective set as a sorted line-per-package text file.

    3. Build the v1 effective package set per machine by concatenating `install/Brewfile.rb` with the matching `install/Brewfile-<profile>.rb`:
       - personal-laptop = Brewfile.rb + Brewfile-personal.rb
       - work-laptop = Brewfile.rb + Brewfile-work.rb
       - server-1 = Brewfile.rb + Brewfile-server.rb
       - server-2 = Brewfile.rb + Brewfile-server.rb
       Parse each Brewfile to extract `brew "<formula>"`, `cask "<cask>"`, and `mas "<name>", id: <id>` declarations. Use `grep -E '^(brew|cask|mas) ' install/Brewfile*.rb | sed -E 's/.*"([^"]+)".*/\1/'` as a starting extractor (refine if needed for `mas` which uses two args).

    4. For each machine, compute `comm -23 <v1_set> <v2_set>` to find v1 packages absent from the v2 effective set on that machine. Each result is a row candidate.

    5. Emit one row per (package, machine-where-missing) combination — if the same package is missing from multiple machines, the row's file:line column lists ALL the v1 Brewfile locations (`install/Brewfile.rb:LINE` or `install/Brewfile-<profile>.rb:LINE`) and the rationale names the machines.

    6. Row format (D-03, six columns):
       `| <Brewfile-path>:<line> | brew formula '<name>' (or cask '<name>' or mas '<name>') declared for <machine-set> | <ported | partially-ported | dropped> | <keep | drop> | <rationale> | <v2 owner: packages/core.rb / packages/gui.rb / manifests/machines/<name>.toml extra_packages / n/a> |`

    7. If NO packages diff between v1 and v2 effective sets for any machine (best case), emit a single explanatory row that says so:
       `| install/Brewfile*.rb:n/a | All v1 Brewfile packages already declared in v2 effective set across all four machines | ported | drop | Verified by per-machine effective-set diff (D-12, AUDIT-02); v2 bundles + extra_packages cover the v1 package surface. | packages/core.rb, packages/gui.rb, manifests/machines/*.toml |`
       The 09-05 SUMMARY plan will count this as the explicit "no gap" confirmation.

    **Block B — install/*.zsh files NOT in the v2 set (AUDIT-02):**

    1. List every file matching `install/*.zsh` via `ls install/*.zsh`.
    2. The v2 set is exactly: messages.zsh, resolver.zsh, compose-brewfile.zsh, cutover-gate.zsh, test-hooks.zsh (per 09-CONTEXT.md `<canonical_refs>` v2 surface).
    3. For each install/*.zsh NOT in that set, emit one row. (If none exist — likely the case; the v1 install layer was Brewfiles + messages.zsh — emit a single confirmation row: `| install/*.zsh | No v1-only install/*.zsh files present beyond the v2 set | n/a | drop | All install/*.zsh files in the repo are members of the v2 set (messages.zsh, resolver.zsh, compose-brewfile.zsh, cutover-gate.zsh, test-hooks.zsh). | n/a |`)

    4. ALSO emit a row for `install/README.md` ONLY if it has non-banner content the v2 docs do not carry (cross-check against `install/README.md`'s purpose). If it's a v2-era doc describing the v2 install layer, do NOT emit a Docs-section row here — let plan 09-04 handle docs.

    No emojis. No additional columns. Use D-09 ("ported" = behavior-equivalent on same machine) as the classification threshold.

    Constraint: do NOT modify or execute v1 code. resolver.zsh execution is allowed because it is v2 read-only code that consumes manifest files.

    Constraint: write each row sorted by (file:line). This makes the per-machine grouping legible to a human reviewer.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-v1-drop-audit/shards/install-assets.md && grep -q '| install/Brewfile' .planning/phases/09-v1-drop-audit/shards/install-assets.md && grep -q '| ported \| drop \||\| dropped \| keep \||\| dropped \| drop \|' .planning/phases/09-v1-drop-audit/shards/install-assets.md && (grep -q 'No v1-only install/\*.zsh' .planning/phases/09-v1-drop-audit/shards/install-assets.md || grep -qE '\| install/[a-z-]+\.zsh:' .planning/phases/09-v1-drop-audit/shards/install-assets.md)</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/09-v1-drop-audit/shards/install-assets.md` exits 0
    - At least one row references `install/Brewfile`: `grep -qE '^\| install/Brewfile(-personal|-work|-server)?\.rb' .planning/phases/09-v1-drop-audit/shards/install-assets.md`
    - Every row carries the six locked columns: `grep -cP '^\|[^|]+\|[^|]+\|[^|]+\|[^|]+\|[^|]+\|[^|]+\|' .planning/phases/09-v1-drop-audit/shards/install-assets.md | awk '$1 >= 1 {exit 0} {exit 1}'`
    - v2 status column uses only the locked three values: `awk -F'|' 'NF>=7 {gsub(/^ *| *$/, "", $4); if ($4 != "ported" && $4 != "partially-ported" && $4 != "dropped" && $4 != "n/a") {print "INVALID v2 status: " $4; exit 1}}' .planning/phases/09-v1-drop-audit/shards/install-assets.md && echo OK | grep -q OK`
    - keep/drop column uses only locked values: `awk -F'|' 'NF>=7 {gsub(/^ *| *$/, "", $5); if ($5 != "keep" && $5 != "drop" && $5 != "n/a") {print "INVALID keep/drop: " $5; exit 1}}' .planning/phases/09-v1-drop-audit/shards/install-assets.md && echo OK | grep -q OK`
    - For all four machines, an effective-set comparison was performed and documented: `grep -cE 'personal-laptop|work-laptop|server-1|server-2|across all four machines' .planning/phases/09-v1-drop-audit/shards/install-assets.md | awk '$1 >= 1 {exit 0} {exit 1}'`
    - Block B coverage: either a "No v1-only install/*.zsh" confirmation row OR one row per non-v2 install/*.zsh file. Check: `grep -qE 'No v1-only install/\*\.zsh|^\| install/[a-z_-]+\.zsh:' .planning/phases/09-v1-drop-audit/shards/install-assets.md` exits 0
    - No emojis: `grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" .planning/phases/09-v1-drop-audit/shards/install-assets.md` returns no matches.
    - The resolver was invoked once per machine (evidence: rationale or row content names per-machine effective-set diff terminology); informational check: the shard mentions either "effective set" or "resolved.json" or per-machine package counts in at least one row's rationale.
  </acceptance_criteria>
  <done>shards/install-assets.md contains the per-machine package-level Brewfile diff (D-12, AUDIT-02) and any non-v2 install/*.zsh leftover rows, all in the six-column D-03 shape, ready for plan 09-05 to append under AUDIT.md's `## Install Assets` section.</done>
</task>

</tasks>

<verification>
- shards/taskfiles.md exists, contains the ZDOTDIR milestone driver row, the three stub taskfile mappings, and the macos.v1.yml.bak $BREW_ZSH bug cross-reference.
- shards/install-assets.md exists, contains per-machine Brewfile effective-set diff rows (D-12, four machines walked), and either a Block-B confirmation row or one row per non-v2 install/*.zsh.
- Both shard files use the six locked columns (D-03), no extras.
- No emojis anywhere.
- No production files modified.
</verification>

<success_criteria>
- AUDIT-01 satisfied for the audit's taskfile dimension: every task in every v1 leftover taskfile has a row.
- AUDIT-02 satisfied: every v1 install asset has a row (Brewfiles at package granularity per D-12; non-v2 install/*.zsh files individually).
- D-02, D-03, D-09, D-10, D-11, D-12 referenced in must_haves and acceptance criteria.
</success_criteria>

<output>
Create `.planning/phases/09-v1-drop-audit/09-02-SUMMARY.md` when done. Record:
- shards/taskfiles.md row count (data rows, excluding headers)
- shards/install-assets.md row count + per-machine effective-set diff results (packages found / packages missing)
- Whether install/*.zsh non-v2 files exist (and if so, names)
- Confirmation that D-10 ($BREW_ZSH) and D-11 (stub mapping) rows landed correctly.
</output>

---
phase: 09-v1-drop-audit
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .planning/phases/09-v1-drop-audit/shards/zsh-tree.md
autonomous: true
requirements:
  - AUDIT-03
user_setup: []

must_haves:
  truths:
    - "The six v1 startup files (.zshenv, .zprofile, .zshrc, .zlogin, .zlogout, theme.zsh) each have exactly one row in shards/zsh-tree.md whose rationale enumerates each v1 block and its v2 disposition (D-08)"
    - "Every function file in zsh/functions/ has a row whose v2 status is classified by behavioral diff against the same-named shell/functions/<name>.zsh: identical / ported-with-documented-delta / partial port (v2 missing behavior X) (D-05, AUDIT-03)"
    - "Aliases are audited via per-machine effective-set comparison (D-06): for personal-laptop concat v1 common+personal; for non-personal machines just common; diff against the v2 effective set per machine derived from manifest feature gates"
    - "zsh/configs/ and zsh/styles/ files are body-level diffed against their v2 siblings under configs/<tool>/; presence-only checks are rejected (D-07)"
    - "When v2 fixes a known v1 bug (zsh/.zprofile:55-56 hardcoded 'server' hostname check, zsh/functions/pubkey.zsh:4 stale docstring), the row is classified v2 status=ported, keep/drop=drop, rationale names the bug + v2 fix (D-10)"
    - "'Ported' means behavior-equivalent under v2 manifest model on same machine, even if implementation changed (manifest gate replaces hostname check, _dotfiles_feature replaces personal/ subdir, etc.) (D-09)"
    - "Every v1-only function (zsh/functions/update.zsh has no shell/functions/update.zsh sibling) is a row classified `dropped` with explicit keep/drop rationale"
  artifacts:
    - path: .planning/phases/09-v1-drop-audit/shards/zsh-tree.md
      provides: "AUDIT rows for the six v1 startup files (D-08), the 24 function files (D-05), per-machine alias effective-set diff rows (D-06), zsh/configs/ + zsh/styles/ body-level diff rows (D-07)"
      contains:
        - "| zsh/.zshenv:"
        - "| zsh/.zprofile:"
        - "| zsh/.zshrc:"
        - "| zsh/.zlogin:"
        - "| zsh/.zlogout:"
        - "| zsh/theme.zsh:"
        - "| zsh/functions/"
        - "| zsh/aliases/"
        - "| zsh/configs/"
        - "| zsh/styles/"
  key_links:
    - from: ".planning/phases/09-v1-drop-audit/shards/zsh-tree.md"
      to: ".planning/phases/09-v1-drop-audit/AUDIT.md"
      via: "plan 09-05 concatenation into ## zsh/ Tree section"
      pattern: "appended verbatim under '## zsh/ Tree' header row"
---

<objective>
Produce `shards/zsh-tree.md` — the audit rows for the entire v1 `zsh/` tree compared file-by-file against the v2 `shell/` tree (AUDIT-03). Covers: 6 startup files (D-08), 24 function files (D-05), aliases via per-machine effective-set (D-06), and zsh/configs/ + zsh/styles/ body-level diffs (D-07).

Purpose: AUDIT-03's success criterion (ROADMAP Phase 9 SC#3) is that any alias / function / theme line / .zshrc block present in v1 and absent from v2 is captured. The inside-the-file silent-drift class is exactly what triggered this milestone. Filename-presence alone is insufficient (D-05 / D-07).

Output: One shard file with rows for every v1 zsh/ file, classified by behavioral diff against its v2 sibling.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-v1-drop-audit/09-CONTEXT.md
@.planning/phases/09-v1-drop-audit/AUDIT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Read-only diff comparison. No execution of v1 zsh code (no source / no shebang invocation). All comparisons are textual. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | zsh/* files read | accept | Read-only via `diff` and `cat`; v1 code is never sourced or executed. read_only: true. |
| T-09-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Audit the six v1 startup files (D-08) and the 24 v1 function files (D-05) into shards/zsh-tree.md</name>
  <files>.planning/phases/09-v1-drop-audit/shards/zsh-tree.md</files>
  <read_first>
    - zsh/.zshenv (full file) and shell/.zshenv (full file) — block-level diff
    - zsh/.zprofile (full file) and shell/.zprofile (full file) — note the hostname "server" check at zsh/.zprofile:55-56 (CONCERNS.md known bug)
    - zsh/.zshrc (full file) and shell/.zshrc (full file) — block-level diff (antigen vs antidote is the headline delta)
    - zsh/.zlogin (full file) and shell/.zlogin (full file)
    - zsh/.zlogout (full file) and shell/.zlogout (full file)
    - zsh/theme.zsh (full file) and shell/theme.zsh (full file)
    - For each of the 24 files under zsh/functions/, read it AND its same-named sibling under shell/functions/ if one exists (planning-input lists: afk, aliaslist, cheat, docker, fs, functionlist, geoip, getcertnames, ghpubkey, host, info, ipv4lookup, ipv6lookup, mkcd, motd, permissions, prettyjson, pubkey, sethostname, sshlist, timezsh, update, vnc, whois — note `update` is v1-only; v2 has _dotfiles_feature.zsh which is v2-only)
    - .planning/codebase/CONCERNS.md (`.zprofile:55-56` hostname-literal bug, `pubkey.zsh:4` stale docstring, motd performance, antigen overhead, .zshrc DOTFILEDIR resolution fragility)
    - .planning/codebase/CONVENTIONS.md (v1 conventions, useful for "intentional improvement" classifications)
    - .planning/phases/09-v1-drop-audit/AUDIT.md (skeleton header — for row format reference)
  </read_first>
  <action>
    Write rows into `.planning/phases/09-v1-drop-audit/shards/zsh-tree.md` for the six startup files AND the 24 function files. Use the six-column D-03 row format. No internal section headers — plan 09-05 will append under `## zsh/ Tree`.

    **Block 1 — Startup files (D-08, one row per file, total 6 rows):**

    For each of `zsh/.zshenv`, `zsh/.zprofile`, `zsh/.zshrc`, `zsh/.zlogin`, `zsh/.zlogout`, `zsh/theme.zsh`:

    1. Run `diff zsh/<file> shell/<file>` (file path matches between trees).
    2. Decompose the v1 file into logical blocks (XDG export block, ZDOTDIR export block, DOTFILES_PROFILE block, brew shellenv block, SSH_AUTH_SOCK block, antigen block, alias-glob block, function-glob block, theme block, MOTD block, history-flush block — the exact blocks vary per file).
    3. For each block, decide its v2 disposition: present-equivalent / present-with-delta / dropped / replaced-by-feature-flag / replaced-by-_dotfiles_feature / fix-of-v1-bug.
    4. Emit ONE row per file (D-08) whose `rationale` column lists every block and its disposition as a `;`-separated list.

    Required content for specific files:
    - `zsh/.zprofile` row MUST cite the hardcoded `"server"` hostname check at line 55-56 (CONCERNS.md known bug) in the rationale AND classify v2 status=ported, keep/drop=drop with rationale naming the v2 fix: v2 uses `_dotfiles_feature one-password-ssh` gated on the manifest (D-10).
    - `zsh/.zshenv` row MUST note that v1 exports `DOTFILES_PROFILE` (read from `${XDG_CONFIG_HOME}/dotfiles/profile`) and v2 exports `DOTFILES_MACHINE` (read from `${XDG_STATE_HOME}/dotfiles/machine`). Classification: ported / drop / rationale naming the v2 model change (manifest model replaces profile model). v2 owner: `shell/.zshenv`.
    - `zsh/.zshrc` row MUST note: `antigen apply` block (CONCERNS.md: ~200-500ms startup overhead) replaced by antidote bundle-cache in v2 (SHEL-10 / Phase 3). Classification: ported / drop, v2 owner: `shell/.zshrc` + `configs/antidote/zsh_plugins.txt`.
    - The ZDOTDIR write at `taskfiles/common.yml:36-57` is NOT a zsh/ row (it lives in taskfiles/common.yml and is plan 09-02's responsibility). Do NOT duplicate the milestone-driver row here.

    Row format (six columns per D-03):
    `| zsh/<file>:<line-range> | <one-sentence purpose: "v1 zsh startup file: <responsibilities>"> | <ported | partially-ported | dropped> | <keep | drop> | <block-1: disposition; block-2: disposition; ...> | <v2 owner file> |`

    File:line range for startup files: use the actual line count (e.g., `zsh/.zshenv:1-95` — run `wc -l zsh/.zshenv` to confirm).

    **Block 2 — Function files (D-05, one row per file, ~24 rows):**

    For each file under `zsh/functions/`:

    1. Check whether the same-named file exists under `shell/functions/`.
    2. If both exist: run `diff zsh/functions/<name>.zsh shell/functions/<name>.zsh`. Classify behaviorally:
       - `identical` (no diff output) → v2 status=ported, keep/drop=drop, rationale="byte-identical port"
       - `ported-with-documented-delta` (small textual diff, behavior preserved) → v2 status=ported, keep/drop=drop, rationale="<summarize the delta>; behavior preserved per D-09"
       - `partial port (v2 missing behavior X)` (significant diff, some sub-behavior absent from v2) → v2 status=partially-ported, keep/drop=keep, rationale="v2 missing: <X>; Phase 10 must port", v2 owner = `shell/functions/<name>.zsh`
    3. If v1 file exists but v2 does NOT (e.g., `zsh/functions/update.zsh` if no `shell/functions/update.zsh`): v2 status=dropped, keep/drop=keep, rationale names the dropped behavior + reason it should be ported, v2 owner = `shell/functions/<name>.zsh` (proposed).
    4. If v2 file exists but v1 does NOT (e.g., `shell/functions/_dotfiles_feature.zsh`): SKIP — v2-only files are NOT v1 leftovers and do not appear in AUDIT.md.

    Required content for specific files:
    - `zsh/functions/pubkey.zsh` row MUST note the `:4` stale docstring example (CONCERNS.md) in rationale; v2 disposition: ported with delta if v2 fixed it (`shell/functions/pubkey.zsh` is 453 bytes vs v1's 446 — small change, likely the docstring fix) — confirm by diff. Classification: ported / drop, D-09.
    - `zsh/functions/motd.zsh` row MUST note v1's synchronous fastfetch + git log overhead (CONCERNS.md performance) replaced by v2's cached lazy-load (`shell/functions/motd.zsh` is 5202 bytes vs v1's 3624 — substantially refactored). Classification: ported with delta, D-09; rationale: "v2 implements 24h TTL cache (SHEL-11) replacing synchronous fastfetch/git-log".
    - `zsh/functions/aliaslist.zsh` and `zsh/functions/functionlist.zsh`: v1 is 1553 / 1066 bytes, v2 is 1177 / 527 bytes — investigate; likely v2 trimmed verbose output. Classification: ported with delta or partial port — diff to confirm.
    - `zsh/functions/update.zsh`: v2 has NO `shell/functions/update.zsh` (confirmed by planning-input directory listing). Classification: dropped. Keep/drop decision: rationale must state whether v2's `task install` (canonical update path) supersedes it (then keep/drop=drop) OR whether the function had operator-useful behavior beyond `task install` (then keep/drop=keep).

    Row format same as Block 1.

    Constraints:
    - No emojis.
    - Use `wc -l zsh/<file>` to get accurate line ranges for the file:line column.
    - The shard MUST NOT contain a `## zsh/ Tree` header — that header is in AUDIT.md already.
    - Order rows: startup files first (in this order: .zshenv, .zprofile, .zshrc, .zlogin, .zlogout, theme.zsh), then function files alphabetically.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-v1-drop-audit/shards/zsh-tree.md && grep -c '^| zsh/.zshenv:' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md | grep -qE '^1$' && grep -c '^| zsh/.zprofile:' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md | grep -qE '^1$' && grep -c '^| zsh/.zshrc:' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md | grep -qE '^1$' && grep -c '^| zsh/.zlogin:' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md | grep -qE '^1$' && grep -c '^| zsh/.zlogout:' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md | grep -qE '^1$' && grep -c '^| zsh/theme.zsh:' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md | grep -qE '^1$' && grep -qE 'hostname.*server|"server".*hostname' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md && grep -q 'antigen' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\| zsh/\.(zshenv|zprofile|zshrc|zlogin|zlogout)' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md` returns `5` (D-08: one row per startup file, five dotfile-prefixed)
    - `grep -c '^| zsh/theme.zsh:' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md` returns `1` (D-08: theme.zsh row present)
    - `.zprofile` row references the hostname-server bug (D-10): `grep -qE '^\| zsh/\.zprofile:.*(hostname|"server"|55-56)' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md`
    - `.zshrc` row references the antigen→antidote swap: `grep -qE '^\| zsh/\.zshrc:.*antigen' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md`
    - `.zshenv` row references the profile→machine model change: `grep -qE '^\| zsh/\.zshenv:.*(DOTFILES_PROFILE|profile model|manifest model)' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md`
    - At least 22 function-file rows present: `grep -cE '^\| zsh/functions/' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md | awk '$1 >= 22 {exit 0} {exit 1}'`
    - pubkey.zsh row present (CONCERNS.md docstring bug coverage): `grep -qE '^\| zsh/functions/pubkey\.zsh:' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md`
    - motd.zsh row present: `grep -qE '^\| zsh/functions/motd\.zsh:' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md`
    - update.zsh row present (v1-only function): `grep -qE '^\| zsh/functions/update\.zsh:.*\| dropped' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md` — must be classified as dropped since no v2 sibling exists
    - v2 status column uses only the locked three values plus n/a: `awk -F'|' 'NF>=7 {gsub(/^ *| *$/, "", $4); if ($4 != "ported" && $4 != "partially-ported" && $4 != "dropped" && $4 != "n/a") {print "INVALID v2 status: " $4; exit 1}}' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md && echo OK | grep -q OK`
    - No emojis: `grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" .planning/phases/09-v1-drop-audit/shards/zsh-tree.md` returns no matches.
  </acceptance_criteria>
  <done>Block 1 (6 startup-file rows) and Block 2 (~24 function-file rows) landed in shards/zsh-tree.md with the locked six-column shape, D-08 per-block rationale on startup files, D-05 behavioral classification on function files, D-10 bug cross-references on .zprofile and pubkey.zsh.</done>
</task>

<task type="auto">
  <name>Task 2: Audit zsh/aliases/ via per-machine effective-set diff (D-06) and zsh/configs/ + zsh/styles/ via body-level diff (D-07); append to shards/zsh-tree.md</name>
  <files>.planning/phases/09-v1-drop-audit/shards/zsh-tree.md</files>
  <read_first>
    - zsh/aliases/common/general.zsh, zsh/aliases/common/hardware.zsh, zsh/aliases/common/networking.zsh (full files)
    - zsh/aliases/personal/jgrid.zsh (full file)
    - shell/aliases/dotfiles.zsh, shell/aliases/finder.zsh, shell/aliases/general.zsh, shell/aliases/ghostty.zsh, shell/aliases/hardware.zsh, shell/aliases/jgrid.zsh, shell/aliases/networking.zsh (full files)
    - zsh/configs/condarc, zsh/configs/ghostty, zsh/configs/glow.yml, zsh/configs/motd_sysinfo.jsonc, zsh/configs/motd_tron.txt, zsh/configs/tlrc.toml, zsh/configs/trippy.toml (read all v1 config files)
    - zsh/styles/eza_style.yaml, zsh/styles/glow_style.json (read both v1 style files)
    - configs/conda/, configs/eza/, configs/ghostty/, configs/glow/, configs/motd/, configs/tlrc/, configs/trippy/ (read each v2 config directory's primary file — typically the same filename or a renamed equivalent)
    - manifests/machines/personal-laptop.toml, work-laptop.toml, server-1.toml, server-2.toml (to determine per-machine alias effective set under v2)
    - .planning/phases/09-v1-drop-audit/09-CONTEXT.md (D-06 method, D-07 method, `<specifics>` re: Phase 7 TOOL-02 config ports)
    - .planning/phases/09-v1-drop-audit/AUDIT.md (skeleton — for row format reference)
  </read_first>
  <action>
    APPEND (do not overwrite) additional rows to `.planning/phases/09-v1-drop-audit/shards/zsh-tree.md` covering aliases (D-06) and configs/styles (D-07).

    **Block 3 — Aliases via per-machine effective-set diff (D-06):**

    1. Build the v1 personal-laptop effective alias set: concatenate `zsh/aliases/common/*.zsh` + `zsh/aliases/personal/*.zsh`. The v1 `.zshrc` source loop applies `personal/` only under the personal profile, so this matches the personal-laptop machine.
    2. Build the v1 non-personal effective set: just `zsh/aliases/common/*.zsh` (work-laptop, server-1, server-2 all skip the personal/ subdir under v1).
    3. Build the v2 effective alias set per machine by reading the manifest's `features` block to decide which gated alias files load. Method: read each `shell/aliases/<file>.zsh` for a `_dotfiles_feature <name>` guard at the top; if a guard is present, that file only loads when the feature is enabled. For machines without the feature flag, exclude that file from the v2 effective set. (Verify by reading each machine TOML — the exact gating is whatever the manifest declares.)
    4. For each machine, perform two checks:
       a. **Alias-name set diff**: extract every `alias <name>=` line from both the v1 effective set and the v2 effective set; `comm -23 <v1_names> <v2_names>` finds aliases present in v1 but absent from v2 on that machine. Each missing alias is a row candidate.
       b. **Content drift inside same-named alias files**: where v1 has `zsh/aliases/common/general.zsh` and v2 has `shell/aliases/general.zsh`, `diff` them line-by-line. Any line in v1 absent from v2 (or substantively reworked, e.g. RHS of `=` changed) is a row candidate.
    5. Emit rows. Two row shapes are acceptable:
       - **Per-alias-name row** when a specific alias is missing from a specific v2 machine effective set:
         `| zsh/aliases/<v1-path>:<line> | alias '<name>' (declared for v1 <profile-or-common>) | <ported | partially-ported | dropped> | <keep | drop> | <rationale: which v2 file should own; whether the alias is intentionally retired> | <v2 owner: shell/aliases/<file>.zsh or n/a if retired> |`
       - **Per-file-comparison row** when an entire v1 alias file maps cleanly to a v2 file and the diff is small enough to summarize in one row:
         `| zsh/aliases/common/<file>.zsh:1-<lines> | v1 alias file: <topic> | <status> | <keep|drop> | <summary of diff vs shell/aliases/<file>.zsh> | shell/aliases/<file>.zsh |`

    Note: D-06 explicitly rejects alias-name-only comparison ("misses behavioral diffs (same alias name, different command)"). So even when alias names match, you MUST diff the alias VALUES (right-hand side of `=`). A row's rationale must explicitly state whether the value drifted.

    Required coverage: every alias declared in v1 must be traceable via shards/zsh-tree.md. At minimum, one row per v1 alias file (4 rows: 3 common + 1 personal), plus an extra row per missing alias-name OR per significant intra-file content drift.

    Specific hint: `zsh/aliases/common/hardware.zsh` contains macOS-only commands (CONCERNS.md Linux/macOS divergence section) — v2 likely preserves them in `shell/aliases/hardware.zsh`. Confirm by diff.

    Specific hint: `zsh/aliases/common/general.zsh:27-31` contains finder/findershow/finderhide which v2 split into a gated `shell/aliases/finder.zsh` (CONCERNS.md). The row must note this split with v2 status=ported, keep/drop=drop, rationale="finder aliases relocated to shell/aliases/finder.zsh under macos-finder feature gate; intentional v2 improvement (D-09)".

    **Block 4 — zsh/configs/ + zsh/styles/ body-level diff (D-07):**

    For each file under zsh/configs/ and zsh/styles/:
    1. Identify the v2 sibling. Per the v2 `configs/` layout: antidote/, conda/, eza/, ghostty/, glow/, motd/, tlrc/, trippy/. Map:
       - zsh/configs/condarc → configs/conda/<primary-file>
       - zsh/configs/ghostty → configs/ghostty/<primary-file>
       - zsh/configs/glow.yml → configs/glow/<primary-file>
       - zsh/configs/motd_sysinfo.jsonc → configs/motd/<primary-file>
       - zsh/configs/motd_tron.txt → configs/motd/<primary-file>
       - zsh/configs/tlrc.toml → configs/tlrc/<primary-file>
       - zsh/configs/trippy.toml → configs/trippy/<primary-file>
       - zsh/styles/eza_style.yaml → configs/eza/<primary-file>
       - zsh/styles/glow_style.json → configs/glow/<style-file>
       (Read each v2 directory to find the actual primary filename — it may be the same or slightly different.)
    2. Run `diff <v1> <v2>` to produce a body-level comparison. Presence-only check is rejected per D-07.
    3. Emit one row per v1 file:
       `| zsh/configs/<file>:1-<lines> | <tool config purpose, one sentence> | <ported | partially-ported | dropped> | <keep | drop> | <body-diff summary: identical / minor stylistic / operator hand-edit X drifted from v2> | configs/<tool>/<file-or-n/a> |`
    4. Phase 7's TOOL-02 ported these configs; per 09-CONTEXT.md `<specifics>`, the audit confirms byte-equivalence or names the delta but does NOT re-validate symlinks (Phase 7 TOOL-04 already does that — out of scope here).

    Constraints:
    - APPEND to shards/zsh-tree.md (Task 1 already created the file with startup + function rows).
    - No emojis.
    - No additional columns.
    - Order: alias rows first (sorted by file:line), then config rows, then style rows.
  </action>
  <verify>
    <automated>grep -qE '^\| zsh/aliases/common/general\.zsh' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md && grep -qE '^\| zsh/aliases/common/hardware\.zsh' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md && grep -qE '^\| zsh/aliases/common/networking\.zsh' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md && grep -qE '^\| zsh/aliases/personal/jgrid\.zsh' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md && grep -qE '^\| zsh/configs/' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md && grep -qE '^\| zsh/styles/' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md</automated>
  </verify>
  <acceptance_criteria>
    - All four v1 alias files have at least one row each:
      - `grep -qE '^\| zsh/aliases/common/general\.zsh' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md`
      - `grep -qE '^\| zsh/aliases/common/hardware\.zsh' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md`
      - `grep -qE '^\| zsh/aliases/common/networking\.zsh' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md`
      - `grep -qE '^\| zsh/aliases/personal/jgrid\.zsh' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md`
    - Finder-aliases relocation note present (D-09 intentional improvement): `grep -qE 'finder.*shell/aliases/finder|macos-finder feature gate' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md`
    - All seven v1 config files have at least one row each (zsh/configs/ has 7 files per planning-input):
      - `grep -cE '^\| zsh/configs/(condarc|ghostty|glow\.yml|motd_sysinfo\.jsonc|motd_tron\.txt|tlrc\.toml|trippy\.toml):' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md | awk '$1 >= 7 {exit 0} {exit 1}'`
    - Both v1 style files have rows: `grep -cE '^\| zsh/styles/(eza_style\.yaml|glow_style\.json):' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md | awk '$1 >= 2 {exit 0} {exit 1}'`
    - Rows reference per-machine effective-set comparison (D-06): `grep -qE 'personal-laptop|work-laptop|server-1|server-2|effective set|effective-set' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md`
    - v2 status column uses only the locked values across the entire file: `awk -F'|' 'NF>=7 {gsub(/^ *| *$/, "", $4); if ($4 != "ported" && $4 != "partially-ported" && $4 != "dropped" && $4 != "n/a") {print "INVALID v2 status: " $4; exit 1}}' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md && echo OK | grep -q OK`
    - keep/drop column uses only locked values: `awk -F'|' 'NF>=7 {gsub(/^ *| *$/, "", $5); if ($5 != "keep" && $5 != "drop" && $5 != "n/a") {print "INVALID keep/drop: " $5; exit 1}}' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md && echo OK | grep -q OK`
    - No emojis: `grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" .planning/phases/09-v1-drop-audit/shards/zsh-tree.md` returns no matches.
  </acceptance_criteria>
  <done>shards/zsh-tree.md has rows for the 4 v1 alias files (with per-machine effective-set rationale), the 7 zsh/configs/ files (body-level diff vs configs/<tool>/), and the 2 zsh/styles/ files; AUDIT-03 coverage of zsh/ tree is complete for Wave 2.</done>
</task>

</tasks>

<verification>
- shards/zsh-tree.md exists and contains: 6 startup-file rows (D-08), ~24 function-file rows (D-05), ≥4 alias-file rows (D-06), 7 config-file rows + 2 style-file rows (D-07).
- Required bug cross-references present: .zprofile hostname-"server" (D-10), pubkey.zsh stale docstring (D-10), antigen→antidote swap.
- v1-only files (update.zsh) classified as `dropped` with explicit keep/drop rationale.
- Six-column shape (D-03) preserved across every row. No emojis. No extra columns.
</verification>

<success_criteria>
- AUDIT-03 satisfied: the v1 zsh/ tree is compared file-by-file against v2 shell/ and any v1-only or drifted content is captured.
- D-05, D-06, D-07, D-08, D-09, D-10 referenced in must_haves and acceptance criteria.
</success_criteria>

<output>
Create `.planning/phases/09-v1-drop-audit/09-03-SUMMARY.md` when done. Record:
- Row counts per block (startup files, functions, aliases, configs, styles).
- List of function files classified as `partially-ported` or `dropped` (these become Phase 10 keep candidates).
- List of aliases missing from v2 per machine.
- Configs with non-empty body-level diff.
- Confirmation that D-10 bug cross-references landed (.zprofile and pubkey.zsh rows).
</output>

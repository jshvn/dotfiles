---
phase: 09-v1-drop-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-v1-drop-audit/AUDIT.md
  - .planning/phases/09-v1-drop-audit/shards/.gitkeep
autonomous: true
requirements:
  - AUDIT-04
user_setup: []

must_haves:
  truths:
    - "AUDIT.md exists at .planning/phases/09-v1-drop-audit/AUDIT.md (D-01)"
    - "AUDIT.md carries top-level section headers in the four required categories (D-01): ## Summary, ## Taskfiles, ## Install Assets, ## zsh/ Tree, ## Docs"
    - "AUDIT.md ## Summary block contains a counts table scaffold (rows for tasks audited / keep / drop / already-ported) and a 'Keep List (Phase 10 queue)' bullet placeholder (D-04)"
    - "Each section table renders the six locked columns in the locked order: file:line | purpose | v2 status | keep/drop | rationale | v2 owner (D-03)"
    - "Section shard directory .planning/phases/09-v1-drop-audit/shards/ exists for downstream parallel section writes"
  artifacts:
    - path: .planning/phases/09-v1-drop-audit/AUDIT.md
      provides: "Canonical AUDIT.md skeleton — section headers, six-column table headers per section, Summary scaffold with counts table + keep-list bullet queue"
      contains:
        - "## Summary"
        - "## Taskfiles"
        - "## Install Assets"
        - "## zsh/ Tree"
        - "## Docs"
        - "| file:line | purpose | v2 status | keep/drop | rationale | v2 owner |"
    - path: .planning/phases/09-v1-drop-audit/shards/.gitkeep
      provides: "Shard directory placeholder so section plans (09-02..09-04) can write distinct shard files without racing on AUDIT.md"
  key_links:
    - from: ".planning/phases/09-v1-drop-audit/AUDIT.md"
      to: ".planning/phases/09-v1-drop-audit/shards/"
      via: "manual section append by plan 09-05 (assemble step)"
      pattern: "## Taskfiles|## Install Assets|## zsh/ Tree|## Docs"
---

<objective>
Create the canonical AUDIT.md skeleton for Phase 9 with the four required top-level sections (D-01), the six-column table headers (D-03), and the Summary scaffold (D-04). Also create the shards/ directory so plans 09-02, 09-03, and 09-04 can each write their section table into its own shard file (no file-write race on AUDIT.md during Wave 2).

Purpose: AUDIT.md's shape is locked by D-01/D-03/D-04 in 09-CONTEXT.md. Writing the skeleton in Wave 1 ensures every section plan in Wave 2 produces output that matches the locked shape, and the final assembler (09-05) only has to concatenate shards into the right section.

Output: AUDIT.md with section headers + six-column table headers per section + Summary scaffold; an empty shards/ directory (with .gitkeep) ready for parallel section writes.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-v1-drop-audit/09-CONTEXT.md
@.planning/codebase/STRUCTURE.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Read-only investigation phase. Only writes are to .planning/ files (documentation). No new code paths against user input, no secrets, no external network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | AUDIT.md skeleton | accept | Skeleton contains only structural headers + column names; no secrets, no machine identifiers, no live config values. read_only: true; output_is_documentation_only: true |
| T-09-01-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create shards/ directory and .gitkeep</name>
  <files>.planning/phases/09-v1-drop-audit/shards/.gitkeep</files>
  <read_first>
    - .planning/phases/09-v1-drop-audit/09-CONTEXT.md (verify D-01 section list and D-03 column shape)
  </read_first>
  <action>
    Create the directory `.planning/phases/09-v1-drop-audit/shards/` via `mkdir -p`. Create an empty `.gitkeep` inside it via `touch`. This directory will hold one shard file per section in Wave 2 (`taskfiles.md`, `install-assets.md`, `zsh-tree.md`, `docs.md`) so plans 09-02/03/04 can each write their section's rows in parallel without racing on AUDIT.md.

    No content other than the empty `.gitkeep` file. The shards directory is internal scaffolding — final AUDIT.md is assembled by plan 09-05.
  </action>
  <verify>
    <automated>test -d .planning/phases/09-v1-drop-audit/shards/ && test -f .planning/phases/09-v1-drop-audit/shards/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - `test -d .planning/phases/09-v1-drop-audit/shards/` exits 0
    - `test -f .planning/phases/09-v1-drop-audit/shards/.gitkeep` exits 0
    - `find .planning/phases/09-v1-drop-audit/shards/ -type f` returns exactly one path (the .gitkeep)
  </acceptance_criteria>
  <done>shards/ directory exists with a .gitkeep sentinel; ready for Wave 2 section writes.</done>
</task>

<task type="auto">
  <name>Task 2: Write AUDIT.md skeleton with locked section shape (D-01, D-03, D-04)</name>
  <files>.planning/phases/09-v1-drop-audit/AUDIT.md</files>
  <read_first>
    - .planning/phases/09-v1-drop-audit/09-CONTEXT.md sections `<decisions>` (verify D-01 section names verbatim, D-03 six columns verbatim, D-04 summary shape)
    - .planning/ROADMAP.md §"Phase 9: v1-Drop Audit" Success Criteria #1 (six columns named: source file:line range, task purpose, v2 status, keep/drop classification, rationale, v2 owner)
  </read_first>
  <action>
    Use the Write tool to create `.planning/phases/09-v1-drop-audit/AUDIT.md` with exactly this structure (no extra columns per D-03, no extra sections per D-01):

    ```
    # AUDIT.md — v1-Drop Audit Report

    **Phase:** 09-v1-drop-audit
    **Status:** In progress
    **Last updated:** <YYYY-MM-DD when this skeleton lands>

    ## Summary

    | Metric | Count |
    |--------|-------|
    | Tasks audited | TBD |
    | Keep | TBD |
    | Drop | TBD |
    | Already-ported | TBD |

    ### Keep List (Phase 10 queue)

    _The bullets below are the Phase 10 implementation queue (D-04). One bullet per keep row; each names its source location and its v2 owner file._

    - TBD (filled in by plan 09-05 after Wave 2 section shards land)

    ## Taskfiles

    _Row granularity: one row per task (D-02). Rows are written by plan 09-02 from shards/taskfiles.md._

    | file:line | purpose | v2 status | keep/drop | rationale | v2 owner |
    |-----------|---------|-----------|-----------|-----------|----------|

    ## Install Assets

    _Rows are written by plan 09-02 (Brewfiles per-machine effective-set diff, plus any install/*.zsh not in the v2 set) from shards/install-assets.md._

    | file:line | purpose | v2 status | keep/drop | rationale | v2 owner |
    |-----------|---------|-----------|-----------|-----------|----------|

    ## zsh/ Tree

    _Rows are written by plan 09-03 (six startup files + 24 functions + aliases per-profile-effective-set + configs/styles diffs) from shards/zsh-tree.md._

    | file:line | purpose | v2 status | keep/drop | rationale | v2 owner |
    |-----------|---------|-----------|-----------|-----------|----------|

    ## Docs

    _Rows are written by plan 09-04 (install/README.md + any v1-only docs/ fragments) from shards/docs.md._

    | file:line | purpose | v2 status | keep/drop | rationale | v2 owner |
    |-----------|---------|-----------|-----------|-----------|----------|
    ```

    Critical formatting rules (enforced by Wave 3 cross-reference grep in plan 09-05):
    - Section headers use exactly `## Taskfiles`, `## Install Assets`, `## zsh/ Tree`, `## Docs` (D-01 verbatim).
    - The first row of every section table is `| file:line | purpose | v2 status | keep/drop | rationale | v2 owner |` — six columns, six pipes between content, in this order (D-03 verbatim).
    - The Summary counts table has rows in this order: Tasks audited, Keep, Drop, Already-ported (D-04).
    - The "Keep List (Phase 10 queue)" subheading appears under Summary (D-04).
    - No emojis anywhere in the file (project rule).
    - Stamp the `**Last updated:**` line with today's date in YYYY-MM-DD form.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-v1-drop-audit/AUDIT.md && grep -q '^## Summary$' .planning/phases/09-v1-drop-audit/AUDIT.md && grep -q '^## Taskfiles$' .planning/phases/09-v1-drop-audit/AUDIT.md && grep -q '^## Install Assets$' .planning/phases/09-v1-drop-audit/AUDIT.md && grep -q '^## zsh/ Tree$' .planning/phases/09-v1-drop-audit/AUDIT.md && grep -q '^## Docs$' .planning/phases/09-v1-drop-audit/AUDIT.md && grep -c '| file:line | purpose | v2 status | keep/drop | rationale | v2 owner |' .planning/phases/09-v1-drop-audit/AUDIT.md | grep -qE '^4$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Summary$' .planning/phases/09-v1-drop-audit/AUDIT.md` returns `1` (D-04 summary present)
    - `grep -c '^## Taskfiles$' .planning/phases/09-v1-drop-audit/AUDIT.md` returns `1` (D-01)
    - `grep -c '^## Install Assets$' .planning/phases/09-v1-drop-audit/AUDIT.md` returns `1` (D-01)
    - `grep -c '^## zsh/ Tree$' .planning/phases/09-v1-drop-audit/AUDIT.md` returns `1` (D-01)
    - `grep -c '^## Docs$' .planning/phases/09-v1-drop-audit/AUDIT.md` returns `1` (D-01)
    - `grep -c '| file:line | purpose | v2 status | keep/drop | rationale | v2 owner |' .planning/phases/09-v1-drop-audit/AUDIT.md` returns `4` (D-03 — one header row per section)
    - `grep -q '### Keep List (Phase 10 queue)' .planning/phases/09-v1-drop-audit/AUDIT.md` exits 0 (D-04 keep-list bullet placeholder present)
    - `grep -q '| Tasks audited | TBD |' .planning/phases/09-v1-drop-audit/AUDIT.md` exits 0 (D-04 counts table row present)
    - No emojis: `grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" .planning/phases/09-v1-drop-audit/AUDIT.md` exits non-zero (or returns nothing) — use `LC_ALL=C grep -P` if available
  </acceptance_criteria>
  <done>AUDIT.md exists with the four locked section headers, six-column table headers, Summary counts table scaffold, and Keep List subheading — ready for Wave 2 to append rows via shards.</done>
</task>

</tasks>

<verification>
- AUDIT.md exists at the expected path with the locked structure
- shards/ directory exists with .gitkeep
- Six-column header appears exactly four times (once per section)
- Summary section contains the counts table + Keep List subheading (D-04)
- No emojis anywhere in AUDIT.md (project convention)
</verification>

<success_criteria>
- AUDIT-04 (the canonical single report) has its skeleton on disk; the file is ready for Wave 2 to fill in rows.
- D-01, D-03, D-04 are reflected verbatim in the on-disk structure.
- Decision Coverage Gate: D-01, D-03, D-04 satisfied by this plan's must_haves and acceptance criteria.
</success_criteria>

<output>
Create `.planning/phases/09-v1-drop-audit/09-01-SUMMARY.md` when done — record the line counts in AUDIT.md, confirm shards/ exists with .gitkeep, and list which decisions (D-01, D-03, D-04) the skeleton makes structurally enforceable for Wave 2.
</output>

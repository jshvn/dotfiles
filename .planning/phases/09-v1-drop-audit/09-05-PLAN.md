---
phase: 09-v1-drop-audit
plan: 05
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03
  - 09-04
files_modified:
  - .planning/phases/09-v1-drop-audit/AUDIT.md
autonomous: true
requirements:
  - AUDIT-04
user_setup: []

must_haves:
  truths:
    - "AUDIT.md ## Summary block carries (a) a counts table populated with real numbers — tasks audited / keep / drop / already-ported — derived from the assembled section rows AND (b) an explicit bullet list of every keep item with its source file:line and v2 owner. The bullet list IS Phase 10's implementation queue (D-04)."
    - "The four section headers from D-01 (## Taskfiles, ## Install Assets, ## zsh/ Tree, ## Docs) are preserved exactly during concatenation; each section's body is the corresponding shard file appended verbatim under the existing header row (D-01)"
    - "Every row across the assembled AUDIT.md uses the six locked columns in the locked order: file:line | purpose | v2 status | keep/drop | rationale | v2 owner (D-03)"
    - "SC#5 cross-reference grep proof: the grep `grep -rh '^[[:space:]]*[a-z][a-z0-9:-]*:' taskfiles/common.yml taskfiles/profile.yml taskfiles/brew.yml taskfiles/profile-tasks.yml taskfiles/*-stub.yml taskfiles/macos.v1.yml.bak` produces a task-name list, and every name in that list appears as a row in AUDIT.md (audit is a superset of the grep). ROADMAP Phase 9 SC#5."
    - "Counts table internal consistency: tasks-audited = keep + drop + already-ported (already-ported is the subset of drop rows whose v2 status = ported); keep bullet count equals the count of rows whose keep/drop=keep across all four sections"
    - "Every locked decision D-01..D-12 from 09-CONTEXT.md is referenced somewhere in AUDIT.md (as a rationale citation, summary note, or section preamble). Reference threshold: ≥12 distinct D-XX citations across the assembled file."
    - "The Phase 10 keep-list bullet block sits immediately under ## Summary and lists every keep row with its `file:line` AND its `v2 owner` (D-04). Phase 10 plans will read this bullet block directly."
  artifacts:
    - path: .planning/phases/09-v1-drop-audit/AUDIT.md
      provides: "The final single AUDIT.md report (AUDIT-04) — concatenation of the four shards into D-01-shaped sections plus a populated D-04 Summary block"
      contains:
        - "## Summary"
        - "## Taskfiles"
        - "## Install Assets"
        - "## zsh/ Tree"
        - "## Docs"
        - "### Keep List (Phase 10 queue)"
  key_links:
    - from: ".planning/phases/09-v1-drop-audit/shards/taskfiles.md"
      to: ".planning/phases/09-v1-drop-audit/AUDIT.md"
      via: "concatenation under ## Taskfiles header row"
      pattern: "rows appended verbatim, ordering preserved"
    - from: ".planning/phases/09-v1-drop-audit/shards/install-assets.md"
      to: ".planning/phases/09-v1-drop-audit/AUDIT.md"
      via: "concatenation under ## Install Assets header row"
      pattern: "rows appended verbatim, ordering preserved"
    - from: ".planning/phases/09-v1-drop-audit/shards/zsh-tree.md"
      to: ".planning/phases/09-v1-drop-audit/AUDIT.md"
      via: "concatenation under ## zsh/ Tree header row"
      pattern: "rows appended verbatim, ordering preserved"
    - from: ".planning/phases/09-v1-drop-audit/shards/docs.md"
      to: ".planning/phases/09-v1-drop-audit/AUDIT.md"
      via: "concatenation under ## Docs header row"
      pattern: "rows appended verbatim, ordering preserved"
    - from: ".planning/phases/09-v1-drop-audit/AUDIT.md ## Summary"
      to: ".planning/phases/10-v1-drop-remediation/"
      via: "D-04 keep-list bullets ARE Phase 10's implementation queue"
      pattern: "Phase 10 plans iterate the bullets directly"
---

<objective>
Assemble the final AUDIT.md from the four shard files produced by plans 09-02 (taskfiles + install-assets), 09-03 (zsh-tree), and 09-04 (docs). Concatenate each shard under its corresponding D-01 section header in the AUDIT.md skeleton (already on disk from plan 09-01). Then populate the ## Summary block: fill in the counts table from real row counts AND generate the keep-list bullet block — one bullet per keep row, naming the source file:line and the v2 owner (D-04). Finally, run the SC#5 cross-reference grep verification, the per-row column-shape verification, and the D-XX coverage verification.

Purpose: AUDIT-04 is the report-shape requirement — a single canonical AUDIT.md with the D-03 six-column tables and the D-04 Summary block. This plan closes AUDIT-04 and gates Phase 10 readiness: Phase 10 reads the Summary block's keep-list bullets directly to derive its own plan list.

Output: A single complete AUDIT.md file with real counts, a populated keep-list bullet queue, four section bodies concatenated from shards, and a verification trail proving SC#5 coverage and column-shape consistency.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-v1-drop-audit/09-CONTEXT.md
@.planning/phases/09-v1-drop-audit/AUDIT.md
@.planning/phases/09-v1-drop-audit/shards/taskfiles.md
@.planning/phases/09-v1-drop-audit/shards/install-assets.md
@.planning/phases/09-v1-drop-audit/shards/zsh-tree.md
@.planning/phases/09-v1-drop-audit/shards/docs.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Read-only concatenation. read_only: true (relative to v1 code — does not source or execute any v1 file); no_execution_of_v1_code: true; output_is_documentation_only: true. The only file mutated is AUDIT.md itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | AUDIT.md final state | accept | Concatenation is deterministic over shard inputs; the rewrite is idempotent (re-running with the same shards produces byte-identical output). |
| T-09-05b | Information Disclosure | AUDIT.md content | accept | All content is public project documentation: file:line citations, package names, decision IDs, taskfile names. No secrets, no identity values. |
| T-09-05-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Concatenate the four shards into AUDIT.md's section bodies (D-01)</name>
  <files>.planning/phases/09-v1-drop-audit/AUDIT.md</files>
  <read_first>
    - .planning/phases/09-v1-drop-audit/AUDIT.md (full file — the skeleton from plan 09-01 with section headers and column-header rows)
    - .planning/phases/09-v1-drop-audit/shards/taskfiles.md (full file — concatenation source for ## Taskfiles)
    - .planning/phases/09-v1-drop-audit/shards/install-assets.md (full file — concatenation source for ## Install Assets)
    - .planning/phases/09-v1-drop-audit/shards/zsh-tree.md (full file — concatenation source for ## zsh/ Tree)
    - .planning/phases/09-v1-drop-audit/shards/docs.md (full file — concatenation source for ## Docs)
    - .planning/phases/09-v1-drop-audit/09-CONTEXT.md `<decisions>` (D-01 section names verbatim, D-03 six-column shape)
  </read_first>
  <action>
    Insert each shard's contents under its corresponding section header in AUDIT.md, immediately after the existing column-header row (`| file:line | purpose | v2 status | keep/drop | rationale | v2 owner |` and its `|---|---|...` separator if present) and before the next `^## ` boundary.

    Per-section insertion rules:
    - **## Taskfiles** ← `shards/taskfiles.md` (lines appended after the column-header row of the Taskfiles section).
    - **## Install Assets** ← `shards/install-assets.md`.
    - **## zsh/ Tree** ← `shards/zsh-tree.md`.
    - **## Docs** ← `shards/docs.md`.

    Concatenation method (deterministic, reviewable):
    1. Read AUDIT.md as it stands (skeleton with empty section bodies).
    2. For each section header, identify the byte offset of the column-header row that follows it. Insert a newline, then the shard's contents verbatim, then a trailing newline before the next `^## ` boundary.
    3. Preserve the ## Summary block above ## Taskfiles untouched (Task 2 fills it in).
    4. Preserve the file's H1 / preamble lines (## Phase / **Status:** / **Last updated:**) untouched.
    5. Update the `**Last updated:**` line to today's date (`date +%Y-%m-%d`).
    6. Do NOT add or modify the column-header rows — they stay exactly as plan 09-01 emitted them.
    7. Do NOT add internal subsection headers; the shards must not contain any (verified in plans 09-02..04 acceptance criteria).

    Idempotency: re-running this task against the same shard inputs must produce byte-identical AUDIT.md. Strategy: before inserting, detect if a section already has body rows present (count rows whose first column matches the section's expected file prefix); if present and equal to the shard's row count, skip the section. If present but unequal, FAIL with an error pointing to the discrepancy — do not silently overwrite (this protects against accidental shard re-runs that would lose data).

    After concatenation, the file's row layout MUST satisfy:
    - The number of rows under `## Taskfiles` (between its column-header row and the next `^## `) equals the row count of shards/taskfiles.md.
    - Same per-section equality for the other three sections.
    - The four column-header rows (one per section) each appear exactly once.

    Constraints:
    - Markdown table integrity preserved (every row starts with `|` and ends with `|`).
    - No emojis added during concatenation.
    - No AI attribution.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-v1-drop-audit/AUDIT.md &amp;&amp; grep -cE '^## (Summary|Taskfiles|Install Assets|zsh/ Tree|Docs)$' .planning/phases/09-v1-drop-audit/AUDIT.md | grep -qE '^5$' &amp;&amp; grep -c '| file:line | purpose | v2 status | keep/drop | rationale | v2 owner |' .planning/phases/09-v1-drop-audit/AUDIT.md | grep -qE '^4$'</automated>
  </verify>
  <acceptance_criteria>
    - All five top-level headers still present after concatenation: `grep -cE '^## (Summary|Taskfiles|Install Assets|zsh/ Tree|Docs)$' .planning/phases/09-v1-drop-audit/AUDIT.md` returns `5`
    - Four column-header rows survive (one per section): `grep -c '| file:line | purpose | v2 status | keep/drop | rationale | v2 owner |' .planning/phases/09-v1-drop-audit/AUDIT.md` returns `4`
    - ## Taskfiles body row count equals shards/taskfiles.md data-row count: `[ "$(awk '/^## Taskfiles$/{flag=1;next} /^## /{flag=0} flag && /^\| taskfiles/' .planning/phases/09-v1-drop-audit/AUDIT.md | wc -l)" -eq "$(grep -cE '^\| taskfiles/' .planning/phases/09-v1-drop-audit/shards/taskfiles.md)" ]`
    - ## Install Assets body row count equals shards/install-assets.md data-row count: `[ "$(awk '/^## Install Assets$/{flag=1;next} /^## /{flag=0} flag && /^\| install\// ' .planning/phases/09-v1-drop-audit/AUDIT.md | wc -l)" -eq "$(grep -cE '^\| install/' .planning/phases/09-v1-drop-audit/shards/install-assets.md)" ]`
    - ## zsh/ Tree body row count equals shards/zsh-tree.md data-row count: `[ "$(awk '/^## zsh\/ Tree$/{flag=1;next} /^## /{flag=0} flag && /^\| zsh\// ' .planning/phases/09-v1-drop-audit/AUDIT.md | wc -l)" -eq "$(grep -cE '^\| zsh/' .planning/phases/09-v1-drop-audit/shards/zsh-tree.md)" ]`
    - ## Docs body row count equals shards/docs.md data-row count: `[ "$(awk '/^## Docs$/{flag=1;next} /^## /{flag=0} flag && /^\| (install|docs|README)/' .planning/phases/09-v1-drop-audit/AUDIT.md | wc -l)" -eq "$(grep -cE '^\| (install|docs|README)' .planning/phases/09-v1-drop-audit/shards/docs.md)" ]`
    - Every data row across the file uses six columns (seven `|` delimiters): `awk -F'|' 'NF != 7 && /^\|/ && !/^\| file:line/ && !/^\|[- ]/ {print "BAD pipe count line: " $0; exit 1} END {print "OK"}' .planning/phases/09-v1-drop-audit/AUDIT.md | tail -1 | grep -q OK`
    - The `**Last updated:**` line is stamped with today's date: `grep -qE "^\*\*Last updated:\*\* $(date +%Y-%m-%d)" .planning/phases/09-v1-drop-audit/AUDIT.md`
    - No emojis introduced: `LC_ALL=C grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" .planning/phases/09-v1-drop-audit/AUDIT.md` returns non-zero
    - Idempotency probe: running the task action a second time produces byte-identical AUDIT.md. Capture via `cp AUDIT.md /tmp/audit-first.md && <re-run task action> && diff /tmp/audit-first.md .planning/phases/09-v1-drop-audit/AUDIT.md` exits 0
  </acceptance_criteria>
  <done>AUDIT.md sections are populated with their shard contents; row counts match per-section; D-01 section headers and D-03 column-header rows are preserved; the file is internally consistent and ready for ## Summary fill-in in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Populate the ## Summary block (counts table + keep-list bullet queue) and run the SC#5 + D-XX coverage verifications (D-04, ROADMAP Phase 9 SC#5)</name>
  <files>.planning/phases/09-v1-drop-audit/AUDIT.md</files>
  <read_first>
    - .planning/phases/09-v1-drop-audit/AUDIT.md (full file as left by Task 1 — concatenation complete, Summary still has TBD placeholders)
    - .planning/phases/09-v1-drop-audit/09-CONTEXT.md `<decisions>` (D-04 Summary shape, all D-XX text for the coverage check)
    - .planning/ROADMAP.md §"Phase 9: v1-Drop Audit" Success Criteria (specifically SC#5 grep command verbatim)
    - .planning/REQUIREMENTS.md AUDIT-04 (the report-shape requirement this plan closes)
    - taskfiles/common.yml, taskfiles/profile.yml, taskfiles/brew.yml, taskfiles/profile-tasks.yml, taskfiles/claude-stub.yml, taskfiles/brew-stub.yml, taskfiles/links-stub.yml, taskfiles/macos.v1.yml.bak (the SC#5 grep input files — needed to compute the canonical task-name list against which the AUDIT is verified)
  </read_first>
  <action>
    **Step 1 — Compute the counts (D-04).**

    Parse the assembled AUDIT.md to derive:
    - `tasks_audited` = total data rows across all four sections (every line matching `^\| [^|]+:` excluding the four column-header rows and the four separator rows).
    - `keep` = rows whose keep/drop column equals `keep` across all four sections.
    - `drop` = rows whose keep/drop column equals `drop` across all four sections.
    - `already_ported` = subset of `drop` rows whose v2 status column equals `ported` (i.e., the v2 model already covers these; they're drop-because-covered, not drop-because-retired).

    Extract counts via awk:
    ```
    awk -F'|' '/^\| [^|]+:/ && !/file:line/ {gsub(/^ *| *$/, "", $4); gsub(/^ *| *$/, "", $5); total++; if ($5 == "keep") keep++; else if ($5 == "drop") {drop++; if ($4 == "ported") ported++} } END {printf "total=%d keep=%d drop=%d already_ported=%d\n", total, keep, drop, ported}' .planning/phases/09-v1-drop-audit/AUDIT.md
    ```

    Replace the four TBD cells in the Summary counts table with these numbers. The table shape stays exactly as plan 09-01 emitted it (Tasks audited / Keep / Drop / Already-ported rows in that order).

    Internal-consistency check: `keep + drop = tasks_audited` (no row may have keep/drop = n/a or blank — those would indicate a data-shape bug in the shards). If `n/a` rows exist, FAIL with an error naming them (the upstream shards violated their column-shape acceptance criteria).

    **Step 2 — Generate the keep-list bullet block (D-04).**

    For every row whose keep/drop = `keep`, emit one bullet under the `### Keep List (Phase 10 queue)` subheading. Bullet shape:

    `- \`<file:line>\` → **<v2 owner>** — <one-sentence purpose; mirror the row's purpose column>`

    Bullets MUST be sorted: first by section (Taskfiles → Install Assets → zsh/ Tree → Docs), then within each section by `file:line` ascending. This makes Phase 10's plan iteration deterministic.

    The bullet block REPLACES the `- TBD (filled in by plan 09-05 after Wave 2 section shards land)` placeholder. The `### Keep List (Phase 10 queue)` subheading stays; the explanatory italic line above the placeholder stays.

    The ZDOTDIR milestone-driver bullet MUST appear as the FIRST bullet under Taskfiles (per ROADMAP Phase 9 SC#2 / Phase 10 SC#1 priority): `- \`taskfiles/common.yml:36-57\` → **taskfiles/shell.yml** — Write \`export ZDOTDIR="$HOME/.config/zsh"\` to /etc/zshenv via sudo (PORT-01 driver, milestone keystone)`

    **Step 3 — SC#5 cross-reference grep (ROADMAP Phase 9 SC#5).**

    Run the verbatim ROADMAP SC#5 grep:
    ```
    grep -rh '^[[:space:]]*[a-z][a-z0-9:-]*:' taskfiles/common.yml taskfiles/profile.yml taskfiles/brew.yml taskfiles/profile-tasks.yml taskfiles/*-stub.yml taskfiles/macos.v1.yml.bak \
      | sed -E 's/^[[:space:]]*([a-z][a-z0-9:-]*):.*/\1/' \
      | sort -u > /tmp/sc5-grep-tasks.txt
    ```

    Filter the grep output to actual task definitions (drop YAML-key noise like `desc`, `cmds`, `vars`, `silent`, `status`, `preconditions`, `deps`, `internal`, `requires`, `dotenv`, `includes`, `aliases`, `version` — these are go-task structural keys, not task names). The filter list comes from inspecting the eight files; emit the canonical task-name list as `/tmp/sc5-tasks-only.txt`.

    Then extract task-row file:line references from AUDIT.md's ## Taskfiles section:
    ```
    awk '/^## Taskfiles$/{flag=1;next} /^## /{flag=0} flag && /^\| taskfiles\//' .planning/phases/09-v1-drop-audit/AUDIT.md \
      | grep -oE '^\| taskfiles/[a-z.v0-9-]+\.(yml|bak):[0-9]+' \
      | sed -E 's|^\| ||' > /tmp/audit-rows.txt
    ```

    For each task name in `/tmp/sc5-tasks-only.txt`, prove a row exists in AUDIT.md whose file:line range encloses the task's defining line. Implementation: for each task name, `grep -n "^  ${task}:" taskfiles/<file>` to find its defining line number, then walk `/tmp/audit-rows.txt` looking for a row whose file path matches and whose line range includes that line number. Emit a list of (task, found_in_row) pairs to `/tmp/sc5-coverage.txt`. Any task with `found_in_row=NONE` is a gap.

    Acceptance: `/tmp/sc5-coverage.txt` has zero `NONE` entries. If gaps exist, FAIL with the list of missing tasks — plan 09-02 must be re-run to add them (this is the verification that the audit is a true superset of the SC#5 grep).

    **Step 4 — D-XX coverage probe (must_haves truth #6).**

    Count distinct D-XX citations across AUDIT.md:
    ```
    grep -oE 'D-(0[1-9]|1[0-2])' .planning/phases/09-v1-drop-audit/AUDIT.md | sort -u | wc -l
    ```

    Acceptance: ≥12 (all of D-01..D-12 appear at least once across the assembled file). If the count is below 12, identify the missing D-XX and amend the most appropriate Summary line OR section-preamble italic line to cite it. This is a sanity check that the rationale columns + summary actually reference the locked decisions; if a D-XX is uncited, either the corresponding row is missing OR the rationale is too thin.

    **Step 5 — Update the **Status:** line.**

    Change `**Status:** In progress` to `**Status:** Complete (Phase 9 closed, ready for Phase 10)`.

    Constraints:
    - No emojis.
    - No AI attribution.
    - Idempotent: re-running this task action with the same shard inputs produces byte-identical AUDIT.md (counts, bullets, status all deterministic).
  </action>
  <verify>
    <automated>grep -qE '^\| Tasks audited \| [0-9]+ \|$' .planning/phases/09-v1-drop-audit/AUDIT.md &amp;&amp; grep -qE '^\| Keep \| [0-9]+ \|$' .planning/phases/09-v1-drop-audit/AUDIT.md &amp;&amp; grep -qE '^\| Drop \| [0-9]+ \|$' .planning/phases/09-v1-drop-audit/AUDIT.md &amp;&amp; grep -qE '^\| Already-ported \| [0-9]+ \|$' .planning/phases/09-v1-drop-audit/AUDIT.md &amp;&amp; grep -q '### Keep List (Phase 10 queue)' .planning/phases/09-v1-drop-audit/AUDIT.md &amp;&amp; ! grep -q 'TBD' .planning/phases/09-v1-drop-audit/AUDIT.md &amp;&amp; grep -q 'taskfiles/common.yml:36-57' .planning/phases/09-v1-drop-audit/AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - All four counts-table rows are populated with numbers (not `TBD`): `grep -qE '^\| Tasks audited \| [0-9]+ \|$' .planning/phases/09-v1-drop-audit/AUDIT.md && grep -qE '^\| Keep \| [0-9]+ \|$' .planning/phases/09-v1-drop-audit/AUDIT.md && grep -qE '^\| Drop \| [0-9]+ \|$' .planning/phases/09-v1-drop-audit/AUDIT.md && grep -qE '^\| Already-ported \| [0-9]+ \|$' .planning/phases/09-v1-drop-audit/AUDIT.md`
    - No `TBD` remains anywhere in AUDIT.md: `! grep -q 'TBD' .planning/phases/09-v1-drop-audit/AUDIT.md`
    - Counts internal consistency: `keep + drop = tasks_audited` AND `already_ported ≤ drop`. Proof:
      ```
      awk -F'|' '
        /^\| Tasks audited \| / {gsub(/^ *| *$/, "", $3); ta=$3+0}
        /^\| Keep \| / {gsub(/^ *| *$/, "", $3); k=$3+0}
        /^\| Drop \| / {gsub(/^ *| *$/, "", $3); d=$3+0}
        /^\| Already-ported \| / {gsub(/^ *| *$/, "", $3); ap=$3+0}
        END {
          if (ta != k + d) {printf "FAIL: %d != %d + %d\n", ta, k, d; exit 1}
          if (ap > d) {printf "FAIL: already_ported %d > drop %d\n", ap, d; exit 1}
          print "OK"
        }
      ' .planning/phases/09-v1-drop-audit/AUDIT.md | tail -1 | grep -q OK
      ```
    - Keep-list bullet count equals the keep counter value: `[ "$(awk '/^### Keep List/{flag=1;next} /^## /{flag=0} flag && /^- `/' .planning/phases/09-v1-drop-audit/AUDIT.md | wc -l)" -eq "$(awk -F'|' '/^\| Keep \| / {gsub(/^ *| *$/, "", $3); print $3; exit}' .planning/phases/09-v1-drop-audit/AUDIT.md)" ]`
    - Keep-list bullets each name a v2 owner via the `→ **<owner>**` arrow pattern: `awk '/^### Keep List/{flag=1;next} /^## /{flag=0} flag && /^- `/' .planning/phases/09-v1-drop-audit/AUDIT.md | grep -cvE '→ \*\*[^*]+\*\*' | awk '$1 == 0 {exit 0} {print "Bullets without v2 owner: " $1; exit 1}'`
    - The ZDOTDIR milestone-driver bullet is present and references taskfiles/shell.yml as v2 owner: `grep -qE '^- `taskfiles/common\.yml:36-57` → \*\*taskfiles/shell\.yml\*\*' .planning/phases/09-v1-drop-audit/AUDIT.md`
    - SC#5 cross-reference (ROADMAP Phase 9 SC#5): every task-name from the SC#5 grep input files appears in AUDIT.md as a row whose file:line covers its defining line. Proof script (paste exact):
      ```
      grep -rh '^[[:space:]]*[a-z][a-z0-9:-]*:' taskfiles/common.yml taskfiles/profile.yml taskfiles/brew.yml taskfiles/profile-tasks.yml taskfiles/*-stub.yml taskfiles/macos.v1.yml.bak \
        | sed -E 's/^[[:space:]]*([a-z][a-z0-9:-]*):.*/\1/' \
        | grep -vE '^(desc|cmds|vars|silent|status|preconditions|deps|internal|requires|dotenv|includes|aliases|version|env|sources|generates|method|prefix|run|interval|set|shopt|platforms|label|summary|ignore_error)$' \
        | sort -u > /tmp/sc5-tasks.txt
      missing=0
      for t in $(cat /tmp/sc5-tasks.txt); do
        for f in taskfiles/common.yml taskfiles/profile.yml taskfiles/brew.yml taskfiles/profile-tasks.yml taskfiles/claude-stub.yml taskfiles/brew-stub.yml taskfiles/links-stub.yml taskfiles/macos.v1.yml.bak; do
          line=$(grep -nE "^  ${t}:" "$f" 2>/dev/null | head -1 | cut -d: -f1)
          [ -z "$line" ] && continue
          if awk -v file="$f" -v target="$line" -F'|' '
            /^## Taskfiles$/{flag=1;next} /^## /{flag=0}
            flag && /^\| / {
              if (match($2, /^[[:space:]]*([^:]+):([0-9]+)-?([0-9]*)/, a)) {
                if (a[1] == file) {
                  start=a[2]+0; end=(a[3]=="")?start:a[3]+0
                  if (target >= start && target <= end) {found=1; exit}
                }
              }
            }
            END {exit found ? 0 : 1}
          ' .planning/phases/09-v1-drop-audit/AUDIT.md; then
            continue 2
          fi
        done
        echo "MISSING: $t"; missing=1
      done
      [ "$missing" = "0" ] && echo OK
      ```
      The script must end with `OK` and no `MISSING:` lines.
    - D-XX coverage: ≥12 distinct decision IDs cited across AUDIT.md: `[ "$(grep -oE 'D-(0[1-9]|1[0-2])' .planning/phases/09-v1-drop-audit/AUDIT.md | sort -u | wc -l)" -ge 12 ]`
    - **Status:** line updated: `grep -qE '^\*\*Status:\*\* Complete' .planning/phases/09-v1-drop-audit/AUDIT.md`
    - File still has the four D-01 section headers + ## Summary: `grep -cE '^## (Summary|Taskfiles|Install Assets|zsh/ Tree|Docs)$' .planning/phases/09-v1-drop-audit/AUDIT.md` returns `5`
    - Four column-header rows still present (one per section): `grep -c '| file:line | purpose | v2 status | keep/drop | rationale | v2 owner |' .planning/phases/09-v1-drop-audit/AUDIT.md` returns `4`
    - No emojis: `LC_ALL=C grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" .planning/phases/09-v1-drop-audit/AUDIT.md` returns non-zero
    - Idempotency: re-running this task action against the same shards produces byte-identical AUDIT.md. Capture: `cp .planning/phases/09-v1-drop-audit/AUDIT.md /tmp/audit-pre.md && <re-run task action> && diff /tmp/audit-pre.md .planning/phases/09-v1-drop-audit/AUDIT.md` exits 0.
  </acceptance_criteria>
  <done>AUDIT.md ## Summary block carries real counts (tasks audited / keep / drop / already-ported, internally consistent), a sorted keep-list bullet block (one bullet per keep row, each naming source file:line and v2 owner per D-04), the ZDOTDIR milestone-driver bullet first under Taskfiles, all twelve D-XX decisions referenced somewhere in the file, the SC#5 grep cross-reference proof passing (every task-name from the grep input appears as an AUDIT row), and **Status:** stamped Complete. AUDIT-04 is closed; Phase 10 can iterate the keep-list bullets directly as its implementation queue.</done>
</task>

</tasks>

<verification>
- AUDIT.md exists at `.planning/phases/09-v1-drop-audit/AUDIT.md` and is the single canonical report (AUDIT-04).
- Five top-level sections present (## Summary + the four D-01 sections); four column-header rows survive concatenation.
- Per-section data row counts equal their corresponding shard data row counts (concatenation was lossless).
- ## Summary counts are populated, internally consistent (keep + drop = tasks_audited; already_ported ≤ drop), and no TBD remains.
- ### Keep List (Phase 10 queue) bullet count equals the Keep counts-table value; the ZDOTDIR milestone-driver bullet is first under Taskfiles with `taskfiles/shell.yml` as v2 owner.
- SC#5 ROADMAP grep cross-reference passes: every task-name from the verbatim ROADMAP grep over the eight v1 leftover taskfiles appears in AUDIT.md (audit is a superset of the grep).
- ≥12 distinct D-XX decision IDs cited across AUDIT.md (all of D-01..D-12 represented).
- **Status:** Complete.
- No emojis. No AI attribution.
</verification>

<success_criteria>
- AUDIT-04 (the canonical single report with the D-03 six-column shape and the D-04 Summary structure) is closed.
- ROADMAP Phase 9 SC#5 cross-reference grep proof passes.
- D-01 (section names), D-03 (six columns), D-04 (Summary shape + keep-list bullet queue) are all enforced structurally on disk.
- D-04 keep-list bullet block IS Phase 10's implementation queue — referenced in must_haves.truths so Phase 10's planner can read this bullet block directly.
- Decision Coverage Gate: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12 all cited somewhere in the assembled AUDIT.md.
</success_criteria>

<output>
Create `.planning/phases/09-v1-drop-audit/09-05-SUMMARY.md` when done. Record:
- Final counts (tasks audited / keep / drop / already-ported) and their per-section breakdown.
- Keep-list bullet count and the top 5 bullets verbatim (Phase 10 priority preview).
- SC#5 grep proof: literal command output (`/tmp/sc5-tasks.txt` line count + the `OK` confirmation).
- D-XX coverage count (must be ≥12).
- Confirmation that the **Status:** line was flipped to Complete.
- Idempotency proof: diff of two consecutive runs equals zero.
</output>

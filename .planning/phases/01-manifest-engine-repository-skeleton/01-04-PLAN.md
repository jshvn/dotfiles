---
phase: 01-manifest-engine-repository-skeleton
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - CLAUDE.md
  - docs/MANIFEST.md
  - docs/README.md
  - shell/README.md
  - identity/README.md
  - packages/README.md
  - configs/README.md
  - os/README.md
autonomous: true
requirements: [DOCS-03, DOCS-04]
threat_refs: []
tags: [docs, claude-md, manifest-md, stub-readme, repository-skeleton]

must_haves:
  truths:
    - "The repo-root `CLAUDE.md` is the v2-conventions document — captures manifest model, one-concept-per-file rule, flat-dir-in-v1 rule, kebab-case-feature-index rule, no-AI-attribution rule, and where-to-add-things table."
    - "`docs/MANIFEST.md` documents the manifest schema (required fields, types, allowed values), merge semantics with one worked example per fixture case, the \"Adding a new machine\" walkthrough, and the CLI reference."
    - "Each of `shell/`, `identity/`, `packages/`, `configs/`, `os/` directories exists and contains a stub README ≤ 12 lines naming its purpose, its populating phase, and its requirement IDs (D-11)."
    - "`docs/README.md` exists with a one-paragraph index of the docs/ directory."
    - "The repo-root `CLAUDE.md` no longer references `$DOTFILES_PROFILE`, `personal/work/server` profile suffixing, or platform-subdirectory aliases — those v1 concepts are replaced by v2 manifest-driven equivalents."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Repo-root v2 conventions document"
      contains: "manifest model"
      min_lines: 80
    - path: "docs/MANIFEST.md"
      provides: "Schema reference + merge semantics + add-a-machine walkthrough + CLI reference"
      contains: "## Merge Semantics"
      min_lines: 100
    - path: "docs/README.md"
      provides: "docs/ directory index"
      min_lines: 4
    - path: "shell/README.md"
      provides: "Stub README pointing to Phase 3"
      contains: "Populated by Phase 3"
    - path: "identity/README.md"
      provides: "Stub README pointing to Phase 4"
      contains: "Populated by Phase 4"
    - path: "packages/README.md"
      provides: "Stub README pointing to Phase 5"
      contains: "Populated by Phase 5"
    - path: "configs/README.md"
      provides: "Stub README pointing to Phase 7"
      contains: "Populated by Phase 7"
    - path: "os/README.md"
      provides: "Stub README pointing to Phase 6"
      contains: "Populated by Phase 6"
  key_links:
    - from: "CLAUDE.md (root)"
      to: "docs/MANIFEST.md"
      via: "schema reference link"
      pattern: "docs/MANIFEST.md"
    - from: "docs/MANIFEST.md"
      to: "manifests/test/fixtures/*"
      via: "worked examples reference the six fixture cases"
      pattern: "extra_packages"
    - from: "stub READMEs"
      to: "ROADMAP.md"
      via: ".planning/ROADMAP.md reference"
      pattern: ".planning/ROADMAP.md"
---

<objective>
Replace the auto-generated v1 repo-root `CLAUDE.md` with the v2 conventions document. Author `docs/MANIFEST.md` — the schema/merge/walkthrough reference every downstream phase links into. Create the five stub READMEs that materialize the full v2 directory skeleton (per D-09, D-10, D-11) even though those phases haven't populated their content yet. Create the `docs/README.md` index.

This plan runs in Wave 3 in parallel with Plan 03 — both depend on Plans 01 and 02 being done but don't touch each other's files. Plan 03 touches `taskfiles/manifest.yml`; Plan 04 touches docs and skeleton READMEs.

Purpose: Make the repository legible to humans and AI agents — every top-level directory exists and self-documents; the manifest model has authoritative documentation; the v1 conventions document is fully replaced by v2.

Output: 1 replaced file + 7 new files.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-manifest-engine-repository-skeleton/01-CONTEXT.md
@.planning/phases/01-manifest-engine-repository-skeleton/01-RESEARCH.md
@.planning/phases/01-manifest-engine-repository-skeleton/01-PATTERNS.md
@.planning/phases/01-manifest-engine-repository-skeleton/01-VALIDATION.md
@.planning/phases/01-manifest-engine-repository-skeleton/01-02-PLAN.md
@CLAUDE.md
@.claude/CLAUDE.md

<interfaces>
<!-- Outlines for the two large documents are authoritative; the planner ports them verbatim. -->
<!-- Stub README template and per-directory mapping is verbatim from RESEARCH §8.2. -->

CLAUDE.md (repo-root) outline — port from RESEARCH §10:
  1. "# Dotfiles v2 — Project Instructions for AI Agents" (top-level title)
  2. "## What This Is" — two-sentence summary (manifest-driven, macOS-only v1, one TOML per machine inherits defaults.toml)
  3. "## The Manifest Model (the keystone)"
     - source of truth: `manifests/defaults.toml` + `manifests/machines/<name>.toml`
     - compiled output: `$XDG_STATE_HOME/dotfiles/resolved.json`
     - active machine: `$XDG_STATE_HOME/dotfiles/machine`
     - schema reference link to `docs/MANIFEST.md`
  4. "## Rules" with sub-headings:
     - "Manifests are the source of truth for install state" (never grep DOTFILES_PROFILE; read resolved.json)
     - "One concept per file" (alias topic, function, taskfile, machine manifest, Brewfile, OS-defaults concern)
     - "Flat directories in v1" (no aliases/common vs aliases/darwin split; no packages/brew/ subdir; v1 is macOS-only)
     - "kebab-case feature names need `index` access" (`{{index .MANIFEST.features "one-password-ssh"}}` — go-template parser rejects `-` in dot-access; RESEARCH §6.2 verified gotcha)
     - "Every install task has a `status:` block" (status block uses `{{.X}}` template vars, NEVER `$X` shell vars — the v1 `macos:shell:145` bug class)
     - "`set -euo pipefail` on every executable .zsh"
     - "No hardcoded `/opt/homebrew` or `/usr/local`" (architecture detection via uname -m)
     - "Symlinks via `_:safe-link` only" (no bare `ln -s` outside taskfiles/helpers.yml)
     - "XDG everywhere" (table of XDG_* vars with purposes)
  5. "## Where to Add Things" — eight-row markdown table:
     | Adding | Where | Naming |
     - alias / function / new machine / brew package / macOS defaults concern / feature flag / tool config / Claude hook
  6. "## Conventions Not Captured Above" — bulleted list: no AI attribution (no Co-Authored-By, no "generated by"); no emojis in non-markdown; file-level comment block at top; section banners # ===; errors to stderr
  7. "## Tooling Versions" — yq ≥ 4.52.1; go-task ≥ 3.37 (current 3.50); jq ≥ 1.7
  8. "## Don't Do" — bulleted: no hostname inference; no TOML reads in taskfile; no aliases/common; no Brewfile-personal.rb; no bare ln; no $VAR in status
  9. "## Project State and Workflow" — use `/gsd-*` commands; direct edits bypass planning artifacts

  REMOVE from existing CLAUDE.md (v1 → v2 deltas per RESEARCH §10 and PATTERNS §"No Analog Found"):
  - All `$DOTFILES_PROFILE` references
  - All profile-suffix references (`personal/work/server` as a filename mechanism)
  - All `aliases/{common,personal,work}/` platform-subdir patterns
  - GSD auto-generation markers (`<!-- GSD:project-start -->`, `<!-- GSD:project-end -->`) — v2 CLAUDE.md is hand-authored
  - Anything specific to Antigen as the plugin manager (Phase 3 swaps to antidote)
  - Anything specific to oh-my-zsh dependencies on Antigen

docs/MANIFEST.md outline — port from RESEARCH §9:
  1. "# Manifest Reference" (top-level title)
  2. "## What This Is" — one paragraph: manifests are the source of truth; resolver compiles to resolved.json; tasks read JSON
  3. "## Schema (v1)"
     - "### `defaults.toml` and `machines/<name>.toml` shape" — side-by-side TOML examples (from RESEARCH §3.1 + §3.2)
     - "### Required fields" — markdown table per D-03: Field | Type | Allowed values | Notes
       (meta.description / platform.os / platform.arch / features / packages.brew.bundles / identity.git / identity.ssh)
     - "### Optional fields" — meta.notes / platform.arch / packages.brew.extra_packages
     - "### Unknown keys" — per D-04: stderr warning, exit 0, error format
  4. "## Merge Semantics"
     - "### Rules" (per D-06: tables / scalars / arrays / extra_packages)
     - "### Worked examples" — one subsection per fixture (six total), each showing defaults.toml snippet + machine.toml snippet + expected.json snippet + one-sentence rule explanation
     - "### Why arrays replace (not concatenate)" — predictability paragraph
  5. "## Adding a New Machine" — seven-step walkthrough (kebab-case name; create TOML; required fields; validate; setup; show; install)
  6. "## CLI Reference" — bulleted list of `task setup`, `task manifest:resolve`, `task manifest:show`, `task manifest:validate`, `task manifest:test`. Include the Phase 1 invocation note: until Phase 2 wires this module from the root Taskfile.yml, users invoke as `task -t taskfiles/manifest.yml <subtask>`.
  7. "## State Files" — `$XDG_STATE_HOME/dotfiles/machine` (single-line; written by setup); `$XDG_STATE_HOME/dotfiles/resolved.json` (compiled; mtime-rebuilt)
  8. "## Feature-Flag Reference (forward pointer)" — table seeded with the features present in defaults.toml (one-password-ssh, motd, claude-marketplace) plus the machine-set kebab-case features (macos-dock, macos-finder, macos-input, macos-screenshots, macos-security); document the owner phase and what-it-does for each. Subsequent phases extend this table.
  9. "## Known Limitations (v1)" — bullets: line-numbers in unknown-key warnings may be imprecise; rename flow is manual; no JSON Schema for editor validation (deferred to TOOL-V2-01).

Stub README template — port verbatim from RESEARCH §8.2 / PATTERNS:

  # <directory-name>

  <one-line purpose>

  **Populated by Phase <N> — see `.planning/ROADMAP.md`.**

  **Requirements landing here:** <REQ-IDs>

  Until then this directory is intentionally empty. The manifest layer
  (`manifests/`) drives what eventually lands here at install time.

Per-directory binding (RESEARCH §8.2):
  shell/    → Phase 3 → SHEL-01..SHEL-12, DOCS-02     → "zsh startup chain, aliases, functions, and theme."
  identity/ → Phase 4 → IDNT-01..08                   → "Git and SSH identity configs (personal, work, none)."
  packages/ → Phase 5 → PKGS-01..05, VRFY-01..04      → "Homebrew bundles by purpose (core, gui, dev, personal, ...)."
  configs/  → Phase 7 → TOOL-01..04                   → "Tool configs (ghostty, glow, trippy, tlrc, conda, ...)."
  os/       → Phase 6 → OSCF-01..05                   → "macOS `defaults write` scripts plus shell registration."

docs/README.md (~5 lines) per RESEARCH §8.3:
  # docs

  Project documentation.

  - `MANIFEST.md` — schema, merge semantics, "adding a new machine" walkthrough (Phase 1)
  - `SECURITY.md` — bootstrap trust chain (Phase 2)
  - `MIGRATION.md`, `MACHINES.md`, `CUTOVER.md` — Phase 8
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace repo-root CLAUDE.md with v2 conventions document</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md (the existing v1 file — to understand what's being replaced; this is a wholesale REPLACE per PATTERNS §"No Analog Found"),
    .planning/phases/01-manifest-engine-repository-skeleton/01-RESEARCH.md (§10 full outline + v1→v2 deltas),
    .planning/phases/01-manifest-engine-repository-skeleton/01-CONTEXT.md (D-13 deliverable),
    .planning/phases/01-manifest-engine-repository-skeleton/01-PATTERNS.md (§"CLAUDE.md repo root — REPLACE"; tone/style guidance),
    .planning/PROJECT.md (constraints — no AI attribution, no emojis-in-non-markdown, XDG, kebab-case, set -euo pipefail)
  </read_first>
  <action>
    Wholesale REPLACE `CLAUDE.md` at repo root with the v2 conventions document per the <interfaces> outline (which mirrors RESEARCH §10).

    Drop ALL of the following content from v1:
    - GSD auto-generation markers (`<!-- GSD:project-start -->`, `<!-- GSD:project-end -->`, the auto-generated sections between them)
    - All `$DOTFILES_PROFILE` references and all `personal/work/server` profile-suffix references
    - All `aliases/{common,personal,work}/` platform-subdir patterns
    - Mentions of Antigen as the plugin manager (Phase 3 swaps to antidote — defer)
    - The "Profile" section entirely; replace with "The Manifest Model" section
    - The "Quick Reference" `task profile:set` example; replace with `task setup -- <machine-name>`

    Author each section per the <interfaces> outline. Tone matches the existing `.claude/CLAUDE.md` (declarative, table-heavy, second-person). No emojis (project-wide rule even for markdown — PATTERNS §"docs/MANIFEST.md" style points).

    Critical content to include:
    - The kebab-case-feature-needs-index rule (RESEARCH §6.2 verified gotcha) — this is a NEW v2-specific rule not in v1 CLAUDE.md
    - The "status blocks use `{{.X}}` not `$X`" rule — captures the v1 `macos:shell:145` bug class structurally
    - The "manifests are the source of truth" rule — no hostname inference, no TOML reads from taskfile, always read resolved.json via fromJson
    - The "Where to Add Things" 8-row table (alias, function, new machine, brew package, macOS defaults concern, feature flag, tool config, Claude hook)
    - "Tooling Versions" floor specification (yq ≥ 4.52.1, go-task ≥ 3.37, jq ≥ 1.7)

    Per project conventions: no `Co-Authored-By`, no "Generated by Claude", no AI attribution anywhere. Per global rule, no emoji codepoints in CLAUDE.md (even though it's markdown — project convention is stricter than the global rule per PATTERNS S8). Match the existing `.claude/CLAUDE.md` style: `##` for top-level sections, `###` for subsections, fenced code blocks with explicit language hints (` ```toml ` / ` ```yaml ` / ` ```zsh `).

    Reference `docs/MANIFEST.md` for full schema/merge details (don't duplicate the schema in CLAUDE.md — CLAUDE.md is conventions, MANIFEST.md is reference).
  </action>
  <verify>
    <automated>test -f CLAUDE.md && grep -q "manifest model" CLAUDE.md && grep -q "kebab-case" CLAUDE.md && grep -q "index " CLAUDE.md && grep -q "set -euo pipefail" CLAUDE.md && grep -q "task setup" CLAUDE.md && grep -q "docs/MANIFEST.md" CLAUDE.md && ! grep -qE 'DOTFILES_PROFILE|aliases/common|aliases/personal|aliases/work' CLAUDE.md && ! grep -qE '<!-- GSD:' CLAUDE.md && ! grep -qiE 'co-authored-by|generated by' CLAUDE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` exists at repo root (replaced, not appended; the v1 file content is GONE).
    - File contains the literal string `manifest model` (D-13 / DOCS-03 verification anchor from VALIDATION.md).
    - File contains the literal string `kebab-case` AND `index` (the verified gotcha rule).
    - File contains `set -euo pipefail` (the strict-mode convention).
    - File contains `task setup` (the v2 entry-point convention; replaces v1's `task profile:set`).
    - File contains `docs/MANIFEST.md` (cross-reference link).
    - File DOES NOT contain `DOTFILES_PROFILE`, `aliases/common`, `aliases/personal`, `aliases/work` (v1 patterns must be gone).
    - File DOES NOT contain `<!-- GSD:` (auto-generation markers stripped per PATTERNS §"No Analog Found").
    - File DOES NOT contain `Co-Authored-By` or `Generated by` (no AI attribution per S8).
    - File contains a markdown table with at least 7 rows under a heading containing the word `Add` (the "Where to Add Things" table).
    - File line count is at least 80 (`wc -l < CLAUDE.md` returns ≥ 80) and at most 350 (keep concise; CLAUDE.md is conventions, not exhaustive reference).
    - No emoji codepoints (U+1F300..U+1FAFF or U+2600..U+27BF) anywhere in the file.
  </acceptance_criteria>
  <done>v1 CLAUDE.md fully replaced with v2 conventions document containing all required rules; v1 patterns scrubbed; DOCS-03 verification anchor present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author docs/MANIFEST.md, docs/README.md, and the five stub READMEs</name>
  <files>
    docs/MANIFEST.md,
    docs/README.md,
    shell/README.md,
    identity/README.md,
    packages/README.md,
    configs/README.md,
    os/README.md
  </files>
  <read_first>
    .planning/phases/01-manifest-engine-repository-skeleton/01-RESEARCH.md (§3.1, §3.2 schema; §4.1 fixture table; §5 extra_packages; §6 CLI reference; §8.2 stub template; §8.3 docs/README content; §9 MANIFEST.md outline),
    .planning/phases/01-manifest-engine-repository-skeleton/01-CONTEXT.md (D-11 stub READMEs ≤ 10 lines; D-12 MANIFEST.md deliverables),
    .planning/phases/01-manifest-engine-repository-skeleton/01-PATTERNS.md (§"Stub READMEs"; §"docs/MANIFEST.md" tone/style),
    manifests/defaults.toml (Plan 02 — the actual defaults to use as the schema worked example),
    manifests/machines/personal-laptop.toml (Plan 02 — actual machine TOML for worked examples),
    manifests/test/fixtures/01-map-over-map/expected.json (Plan 01 — actual expected output for the worked example),
    manifests/test/fixtures/06-extra-packages-concat/expected.json (Plan 01 — actual extras-concat output for the worked example),
    .planning/REQUIREMENTS.md (DOCS-03, DOCS-04 expectations; per-directory requirement IDs for stub READMEs)
  </read_first>
  <action>
    Author `docs/MANIFEST.md` per the <interfaces> outline (which mirrors RESEARCH §9). Use the ACTUAL Plan 02 manifest files (defaults.toml, personal-laptop.toml) as the "side-by-side example" in the Schema section — don't invent synthetic data. Use the ACTUAL Plan 01 fixture expected.json files as the worked examples in the Merge Semantics section — one subsection per fixture (01-map-over-map through 06-extra-packages-concat), each showing the actual defaults snippet, the actual machine snippet, the actual expected.json snippet, and a one-sentence rule explanation.

    The "Required fields" table mirrors D-03 exactly: meta.description / platform.os / features / packages.brew.bundles / identity.git / identity.ssh — Field | Type | Allowed values | Notes columns.

    The "Adding a New Machine" walkthrough lists seven steps: choose kebab-case name; create TOML; required fields; `task manifest:validate -- --machine <name>`; `task setup -- <name>`; `task manifest:show`; (optional) `task install`. For Phase 1 the invocation prefix is `task -t taskfiles/manifest.yml ...` — document this in a note at the top of the CLI Reference section, marking it as a Phase 1 wart that Phase 2 closes when the root Taskfile.yml wires the module via includes.

    The "Feature-Flag Reference" table is seeded with: one-password-ssh (Phase 4 owner; enables 1Password SSH agent integration; default false), motd (Phase 3 owner; enables MOTD on .zlogin; default true), claude-marketplace (Phase 7 owner; installs claude marketplace plugins; default true), macos-dock / macos-finder / macos-input / macos-screenshots / macos-security (each Phase 6 owner; runs the corresponding os/defaults/<concern>.zsh; default machine-set, not declared in defaults.toml). Note that subsequent phases extend this table.

    Author `docs/README.md` (~5 lines) per the <interfaces> outline — a list of: MANIFEST.md (this phase), SECURITY.md (Phase 2 will land), MIGRATION.md / MACHINES.md / CUTOVER.md (Phase 8 will land).

    Author the five stub READMEs (`shell/README.md`, `identity/README.md`, `packages/README.md`, `configs/README.md`, `os/README.md`) using the verbatim template from <interfaces>. Per-directory binding:
    - `shell/README.md`: purpose = "zsh startup chain, aliases, functions, and theme."; phase = 3; reqs = "SHEL-01..SHEL-12, DOCS-02"
    - `identity/README.md`: purpose = "Git and SSH identity configs (personal, work, none)."; phase = 4; reqs = "IDNT-01..08"
    - `packages/README.md`: purpose = "Homebrew bundles by purpose (core, gui, dev, personal, ...)."; phase = 5; reqs = "PKGS-01..05, VRFY-01..04"
    - `configs/README.md`: purpose = "Tool configs (ghostty, glow, trippy, tlrc, conda, ...)."; phase = 7; reqs = "TOOL-01..04"
    - `os/README.md`: purpose = "macOS `defaults write` scripts plus shell registration."; phase = 6; reqs = "OSCF-01..05"

    Per D-11 each stub stays ≤ 12 lines. The directories themselves are CREATED by writing the README into them (mkdir -p semantics via the file write).

    Per project conventions: no `Co-Authored-By`, no "generated by", no AI attribution. No emoji codepoints (even in markdown — project convention per PATTERNS S8). Use fenced code blocks with explicit language hints. Use `##` for top-level sections and `###` for subsections within docs/MANIFEST.md.
  </action>
  <verify>
    <automated>test -f docs/MANIFEST.md && grep -q "^## Merge Semantics" docs/MANIFEST.md && grep -q "^## Adding a New Machine" docs/MANIFEST.md && grep -q "^## Schema" docs/MANIFEST.md && grep -q "extra_packages" docs/MANIFEST.md && test -f docs/README.md && for d in shell identity packages configs os; do test -f "$d/README.md" || { echo "MISSING $d/README.md"; exit 1; }; grep -q "Populated by Phase" "$d/README.md" || { echo "BAD $d/README.md"; exit 1; }; lines=$(wc -l < "$d/README.md"); [ "$lines" -le 12 ] || { echo "TOO LONG: $d/README.md ($lines lines)"; exit 1; }; done; grep -q "Populated by Phase 3" shell/README.md && grep -q "Populated by Phase 4" identity/README.md && grep -q "Populated by Phase 5" packages/README.md && grep -q "Populated by Phase 7" configs/README.md && grep -q "Populated by Phase 6" os/README.md && ! grep -qiE 'co-authored-by|generated by' docs/MANIFEST.md shell/README.md identity/README.md packages/README.md configs/README.md os/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/MANIFEST.md` exists.
    - `docs/MANIFEST.md` contains a heading `## Merge Semantics` (per VALIDATION.md Per-Task Verification Map for DOCS-04).
    - `docs/MANIFEST.md` contains a heading `## Adding a New Machine` (the walkthrough section).
    - `docs/MANIFEST.md` contains a heading `## Schema` (the schema reference section).
    - `docs/MANIFEST.md` contains the literal string `extra_packages` (the concat-special-field is documented in the merge rules and worked examples).
    - `docs/MANIFEST.md` line count is at least 100 (substantive reference doc, not a stub) and at most 600 (keep readable).
    - `docs/MANIFEST.md` references the Phase 1 invocation prefix `task -t taskfiles/manifest.yml` (so users know how to run the commands until Phase 2 wires the module).
    - `docs/MANIFEST.md` schema worked examples reference the actual Plan 02 files (`personal-laptop`) — verified by `grep -q "personal-laptop" docs/MANIFEST.md`.
    - `docs/README.md` exists, contains the literal string `MANIFEST.md`, contains `Phase`, line count ≤ 10.
    - All five stub READMEs exist (`shell/README.md`, `identity/README.md`, `packages/README.md`, `configs/README.md`, `os/README.md`).
    - Each stub has the correct phase number (shell/3, identity/4, packages/5, configs/7, os/6) verified by `grep -q "Populated by Phase <N>" <dir>/README.md`.
    - Each stub line count ≤ 12 (per D-11).
    - Each stub references `.planning/ROADMAP.md`.
    - No `Co-Authored-By`, `Generated by`, or `Claude` strings in any of the seven files.
    - No emoji codepoints in any of the seven files.
    - DOCS-04 anchor from VALIDATION.md: `test -f docs/MANIFEST.md && grep -q "## Merge Semantics" docs/MANIFEST.md` exits 0.
    - DOCS-03 cross-reference: Task 1's `CLAUDE.md` references `docs/MANIFEST.md` (already verified in Task 1 acceptance), and `docs/MANIFEST.md` exists for that reference to land — verified together.
  </acceptance_criteria>
  <done>Schema/merge/walkthrough doc complete with real worked examples drawn from Plan 01 and Plan 02 artifacts. Five stub READMEs materialize the five placeholder directories; each is under the line budget and points to the correct populating phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan 04 produces pure-documentation files. No code, no inputs, no secrets, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | accept | Plan 04 ships only documentation files. No threats land in this plan that aren't already addressed by Plans 02 (T-MAN-01, T-MAN-03) and 03 (T-MAN-02 defense-in-depth). The docs themselves are static markdown read by humans and AI agents; their failure mode is "inaccurate documentation" not a security boundary. |
</threat_model>

<verification>
- CLAUDE.md replaced (not appended) with v2 conventions document containing all required rules.
- v1 patterns scrubbed: no `$DOTFILES_PROFILE`, no `aliases/common`, no GSD auto-gen markers.
- docs/MANIFEST.md exists with all required sections (Schema, Merge Semantics, Adding a New Machine, CLI Reference, etc.).
- docs/MANIFEST.md uses ACTUAL artifacts from Plans 01 and 02 in its worked examples (not invented examples).
- Five stub READMEs exist; each ≤ 12 lines; each correctly identifies its populating phase and requirement IDs.
- All eight files pass the no-AI-attribution and no-emoji conventions.
</verification>

<success_criteria>
- 8 files produced (1 replaced CLAUDE.md + 1 docs/MANIFEST.md + 1 docs/README.md + 5 stub READMEs).
- DOCS-03 and DOCS-04 anchors from VALIDATION.md Per-Task Verification Map all pass:
  - `test -f CLAUDE.md && grep -q "manifest model" CLAUDE.md`
  - `test -f docs/MANIFEST.md && grep -q "## Merge Semantics" docs/MANIFEST.md && grep -q "## Adding a New Machine" docs/MANIFEST.md`
- The five v2 directories that Phase 1 creates as stubs (shell/, identity/, packages/, configs/, os/) exist with their READMEs, completing the D-09 full-skeleton requirement.
- Roadmap success criterion 5 ("docs/MANIFEST.md and project-level CLAUDE.md (v2 conventions) are on disk; every top-level directory exists with a placeholder README") is fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/01-manifest-engine-repository-skeleton/01-04-SUMMARY.md` per the standard summary template.
</output>

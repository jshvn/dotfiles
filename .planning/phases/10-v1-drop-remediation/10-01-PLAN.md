---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskfiles/links.yml
  - taskfiles/shell.yml
  - Taskfile.yml
  - .planning/phases/09-v1-drop-audit/AUDIT.md
  - .planning/phases/10-v1-drop-remediation/10-SMOKE.md
autonomous: false
requirements: [PORT-01, PORT-02, PORT-03]

must_haves:
  goal: "Every keep item from Phase 9's AUDIT.md is implemented in v2 in the file the audit named as the v2 owner; fresh-machine install produces a fully-functional first shell (prompt, theme, aliases, functions, MOTD, _dotfiles_feature) without manual remediation."
  truths:
    - "After `task install` on a machine without `/etc/zshenv`, the file exists and contains the line `export ZDOTDIR=\"$HOME/.config/zsh\"` (PORT-01, D-01, D-03)."
    - "Re-running `task install` on a converged machine does NOT prompt for sudo (idempotent status block per D-04 using `{{.ZDOTDIR}}` template var; LINT-02 compliant; not the v1 macos:shell:145 bug class)."
    - "`task lint:taskfile` exits 0 after the change (proves the new status block contains zero `$VAR` references; LINT-02 enforcement)."
    - "`task shell:validate` exists, exits 0 on a healthy machine, and exits non-zero when `/etc/zshenv` is missing the ZDOTDIR line (D-05, D-06)."
    - "`task validate` summary table contains a `shell` row (component is wired into both aggregator loops at Taskfile.yml:215 and :222; D-05)."
    - "`task perf:shell` still works (the SHEL-12 cold-start CI gate is preserved via dual-alias include of taskfiles/shell.yml; D-06 dual-alias decision per RESEARCH.md)."
    - "AUDIT.md row `install/Brewfile-personal.rb:72` is reclassified from `keep` to `drop`; counts table reads Keep 2 / Drop 100; the keep-list bullet for that row is removed (D-07)."
    - "`manifests/machines/personal-laptop.toml:67` remains unchanged: `{ id = 904280696, name = \"Things3\" }` (D-07: leave the canonical App Store name in place)."
    - "`.planning/phases/10-v1-drop-remediation/10-SMOKE.md` exists, documents the fresh-shell smoke procedure (assertions: $ZDOTDIR resolves, prompt renders, _dotfiles_feature resolves, alias lists ported aliases, motd prints), and contains a Run Log table the operator records PASS/FAIL into (PORT-03, D-08; ROADMAP P10 SC#3 names this exact path)."
  artifacts:
    - path: "taskfiles/links.yml"
      provides: "Internal `zdotdir` task + `task: zdotdir` cmds entry + status-block extension on outer `zsh` task"
      contains: "zdotdir:"
    - path: "taskfiles/shell.yml"
      provides: "New `validate` task that asserts XDG dirs + ZDOTDIR dir + /etc/zshenv ZDOTDIR line"
      contains: "validate:"
    - path: "Taskfile.yml"
      provides: "Dual-alias include (perf: + shell:) for taskfiles/shell.yml; `shell` token appended to both validate-aggregator loops"
      contains: "shell:    ./taskfiles/shell.yml"
    - path: ".planning/phases/09-v1-drop-audit/AUDIT.md"
      provides: "Row #3 reclassified keep->drop; counts table updated; keep-list bullet removed"
      contains: "| Keep | 2 |"
    - path: ".planning/phases/10-v1-drop-remediation/10-SMOKE.md"
      provides: "PORT-03 documented smoke procedure + Run Log table"
      contains: "## Run Log"
  key_links:
    - from: "taskfiles/links.yml:zsh task"
      to: "taskfiles/links.yml:zdotdir task"
      via: "`task: zdotdir` in cmds + matching `grep -qF` line in outer status"
      pattern: "task: zdotdir"
    - from: "Taskfile.yml:validate aggregator"
      to: "taskfiles/shell.yml:validate task"
      via: "`shell` token in both for-loops (line 215 execution + line 222 summary), resolved via `shell:` include alias"
      pattern: "manifest identity links macos packages claude shell"
    - from: "Taskfile.yml:includes block"
      to: "taskfiles/shell.yml"
      via: "dual-alias: `perf: ./taskfiles/shell.yml` AND `shell: ./taskfiles/shell.yml`"
      pattern: "shell:    ./taskfiles/shell.yml"
---

<objective>
Implement every "keep" row from Phase 9 AUDIT.md into v2 so a fresh-machine first interactive shell works end-to-end without manual remediation. The phase has four work items:

1. PORT-01 (milestone driver): Add a new internal `zdotdir` task to `taskfiles/links.yml` that writes `export ZDOTDIR="$HOME/.config/zsh"` to `/etc/zshenv` via sudo (three-branch idempotent shape from v1 `taskfiles/common.yml:36-57`), with a `{{.ZDOTDIR}}` template-var status block (LINT-02 compliant, not the v1 `macos:shell:145` bug class). Dispatch it from `links:zsh` and extend that task's outer status block so /etc/zshenv drift triggers re-dispatch.
2. PORT-02 (validation): Add a `validate` task to `taskfiles/shell.yml` asserting the four XDG base dirs + `$ZDOTDIR` dir + `/etc/zshenv` ZDOTDIR line. Wire the new `shell` component into the root `task validate` aggregator by editing two identical for-loop tokens in `Taskfile.yml` and dual-aliasing the `taskfiles/shell.yml` include so `perf:shell` (existing SHEL-12 CI gate) and `shell:validate` (new) both resolve.
3. AUDIT row amend (D-07): Reclassify `install/Brewfile-personal.rb:72` from `keep` to `drop` (the `Things3` name in `manifests/machines/personal-laptop.toml:67` is the canonical mas-list name and is left unchanged); update the counts table (Keep 3->2, Drop 99->100); remove the keep-list bullet.
4. PORT-03 (smoke): Author `.planning/phases/10-v1-drop-remediation/10-SMOKE.md` documenting the fresh-shell smoke procedure with assertions and a Run Log table; operator records PASS/FAIL.

Purpose: Phase 10 is the cutover-readiness gate before Phase 11 deletes the v1 source-of-truth files. The /etc/zshenv ZDOTDIR write is the milestone driver -- without it, a fresh-machine first shell never sources `.zshenv` and the entire v2 startup chain is non-functional.

Output: 5 files modified (taskfiles/links.yml, taskfiles/shell.yml, Taskfile.yml, AUDIT.md, new 10-SMOKE.md). ~30-50 lines of YAML edits + one AUDIT row reclassification + a smoke-procedure markdown doc.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-v1-drop-remediation/10-CONTEXT.md
@.planning/phases/10-v1-drop-remediation/10-RESEARCH.md
@.planning/phases/10-v1-drop-remediation/10-VALIDATION.md
@.planning/phases/09-v1-drop-audit/AUDIT.md
@CLAUDE.md
@.claude/CLAUDE.md
@taskfiles/common.yml
@taskfiles/links.yml
@taskfiles/shell.yml
@taskfiles/lint.yml
@taskfiles/helpers.yml
@Taskfile.yml
@install/messages.zsh
@manifests/machines/personal-laptop.toml

<interfaces>
Key facts about the existing surfaces this plan modifies. Extracted from the codebase --
executor should use these directly without re-exploring.

From `taskfiles/links.yml` (current state -- verified by reading the file in planning):
- `zsh:` task is defined at lines 139-157. Its cmds are five `task: _:safe-link` invocations for `.zshenv`, `.zprofile`, `.zshrc`, `.zlogin`, `.zlogout`. Its status block is five `test -L "{{.ZDOTDIR}}/.<file>"` lines (152-157).
- There is currently NO `zdotdir` task in this file. The RESEARCH.md sketch (lines 293-334) shows the TARGET state, not the current state.
- The `_:safe-link` helper is provided via `includes: _: ./helpers.yml` (line 42).
- DOTFILES_MESSAGES, ZDOTDIR, DOTFILEDIR, XDG_CONFIG_HOME are all available as `{{.X}}` template vars (declared in this file's vars block at lines 50-64; root Taskfile.yml's vars also forward in).

From `taskfiles/shell.yml` (current state -- verified):
- Currently has ONE task: `shell` (lines 60-83), the SHEL-12 cold-start hyperfine gate. Marked `# lint-allow: cmds-without-status` (line 60).
- File has its own vars block (lines 39-48) declaring HOME, DOTFILEDIR, DOTFILES_MESSAGES.
- File is currently included as `perf: ./taskfiles/shell.yml` in root Taskfile.yml (line 92) -- so the task surfaces as `task perf:shell`.

From `Taskfile.yml` (current state -- verified):
- Root vars block (lines 34-78) declares: HOME, XDG_CONFIG_HOME, XDG_DATA_HOME, XDG_STATE_HOME, XDG_CACHE_HOME, ZDOTDIR (`{{.XDG_CONFIG_HOME}}/zsh`), DOTFILEDIR, HOMEBREW_PREFIX, DOTFILES_MESSAGES, RESOLVED_JSON_PATH, MANIFEST_JSON, MANIFEST.
- `set: [errexit, pipefail]` at line 31 inherits to all included taskfile cmd blocks (no need for executor to add this to cmd bodies).
- Includes block (lines 80-122) currently has `perf: ./taskfiles/shell.yml` on line 92.
- Validate-aggregator (lines 160-233) is the integration point. Two for-loops both contain the IDENTICAL token list `manifest identity links macos packages claude` (lines 215 and 222). Both must be edited to add `shell` (recommendation: tail-append per RESEARCH.md Pattern 2).
- Validate uses `task "${component}:validate" 2>&1 | tee "${cache_dir}/${component}"` (line 218) -- so the new `shell:validate` task name (resolved via the new `shell:` include alias) is picked up automatically.

From `taskfiles/helpers.yml` (current state -- verified):
- `_:safe-link` requires `SOURCE` and `TARGET` vars; idempotent via `ln -sfn` and parent-dir-creation.
- `_:check-dir` (lines 75-83) ALWAYS exits 0 even when the directory is missing -- it just prints `cross` via messages.zsh. This is RESEARCH.md Landmine 4: do NOT rely on `_:check-dir` exit code in `shell:validate`. Use the `failures` counter pattern from `taskfiles/links.yml:382` (`exit "$failures"`).

From `install/messages.zsh` (current state -- verified):
- Provides `info`, `success`, `warn`, `error`, `check`, `cross`, `header`, `step`, `debug`. `error` writes to stderr.
- Sourced via `{{.DOTFILES_MESSAGES}}` (resolves to `source '<DOTFILEDIR>/install/messages.zsh'`).

From `taskfiles/lint.yml` (current state -- verified):
- `task lint:taskfile` runs LINT-02 (`$VAR` in `status:` blocks), LINT-03a (cmds without status), and LINT-03b (bare `ln -s` outside helpers.yml).
- LINT-02 regex at lines 144-147 scans `yq '.tasks[] | select(.status) | .status' "$f"` for `\$[A-Za-z_][A-Za-z0-9_]*`, excluding lines that contain `\$(` or `{{`. Any `$VAR` reference in a status block not on a `{{`-containing line fails the lint.
- LINT-03a exempts tasks with `internal: true` and tasks whose cmds are all `task:` delegations.

From `taskfiles/common.yml:36-57` (v1 source-of-truth template -- verified):
- Three-branch shape: `[[ ! -f /etc/zshenv ]]` -> `sudo tee`; `! grep -qF` -> `sudo tee -a`; else info no-op.
- v1 status block uses `$ZDOTDIR_EXPORT` shell var -- DO NOT COPY THIS. D-04 requires the v2 status to embed `{{.ZDOTDIR}}` directly: `grep -qF 'export ZDOTDIR="{{.ZDOTDIR}}"' /etc/zshenv 2>/dev/null`.

From `.planning/phases/09-v1-drop-audit/AUDIT.md` (current state -- verified):
- Summary counts table at lines 9-14: Tasks audited 102, Keep 3, Drop 99, Already-ported 70.
- Keep List bullets at lines 18-22 (three bullets). The third bullet (`install/Brewfile-personal.rb:72 -> manifests/machines/personal-laptop.toml`) must be removed.
- Row for `install/Brewfile-personal.rb:72` lives in the Taskfiles table (currently 3rd column `partially-ported`, 4th column `keep`, with rationale text proposing to revert v2 name 'Things3' to v1 'Things'). Per D-07 this row must change to `ported` / `drop` with new rationale (RESEARCH.md `### AUDIT.md Row Amend Specification` provides the exact replacement text verbatim at lines 425-456).

From `manifests/machines/personal-laptop.toml:65-71` (verified):
- `mas = [ { id = 441258766, name = "Magnet" }, { id = 904280696, name = "Things3" }, ]`
- D-07: LEAVE UNCHANGED. The Things3 name is the canonical App Store name returned by `mas list` for id 904280696.

Existing extraction precedent in same file (links.yml lines 269-277):
- `configs:ghostty:` is an internal sub-task extracted from `configs:` so its inline-ternary status gate works independently. This is the precedent for extracting `zdotdir:` as an internal sub-task with its own status block, called via `task: zdotdir` from `links:zsh`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add internal zdotdir task to taskfiles/links.yml and wire it into links:zsh</name>
  <files>taskfiles/links.yml</files>
  <read_first>
    - taskfiles/links.yml (lines 1-160 -- the includes/vars block plus the current `zsh:` task at 139-157; you will edit this file)
    - taskfiles/common.yml (lines 36-57 -- v1 source-of-truth template for the three-branch sudo write; copy the cmds: SHAPE, swap to `{{.ZDOTDIR}}` in status per D-04)
    - .planning/phases/10-v1-drop-remediation/10-CONTEXT.md (decisions D-01, D-02, D-03, D-04 -- dispositive)
    - .planning/phases/10-v1-drop-remediation/10-RESEARCH.md (sections `### PORT-01 -- ZDOTDIR Write` lines 282-345, `### Idempotency Landmines` lines 526-597, `### Anti-Patterns to Avoid` lines 243-254; the recommended Option C sketch at lines 292-334 IS the target shape)
    - .planning/phases/10-v1-drop-remediation/10-VALIDATION.md (rows 10-01-01, 10-01-02, 10-01-03 -- the automated commands you must make pass)
    - CLAUDE.md (LINT-02 status-block template-var rule; the v1 `macos:shell:145` bug class)
    - taskfiles/lint.yml (lines 140-155 -- the LINT-02 regex you must NOT trigger)
  </read_first>
  <action>
Edit `taskfiles/links.yml` to add a new internal `zdotdir` task and dispatch it from `links:zsh`, modeled on the existing `configs:ghostty:` extraction precedent (links.yml lines 269-277). Three concrete edits:

EDIT 1 -- Add `- task: zdotdir` as a NEW final entry in the cmds list of `links:zsh` (current lines 141-151), appended AFTER the five existing `_:safe-link` invocations. Same two-space indentation as the existing cmds entries.

EDIT 2 -- Extend the `links:zsh` outer status block (current lines 152-157) by appending ONE additional line so /etc/zshenv drift triggers re-dispatch. The line MUST use `{{.ZDOTDIR}}` template var (NEVER `$ZDOTDIR` -- that is the v1 macos:shell:145 bug class per LINT-02). Use this exact line, with `2>/dev/null` so missing-file produces the "run cmd" signal:
  - grep -qF 'export ZDOTDIR="{{.ZDOTDIR}}"' /etc/zshenv 2>/dev/null

RESEARCH.md Landmine 7 (lines 590-598) confirms this is required: if the outer status returns 0 (all 5 symlinks exist) and /etc/zshenv state drifts, the cmds block is skipped and `task: zdotdir` is never dispatched.

EDIT 3 -- Add a new internal `zdotdir:` task at the same indentation level as `zsh:` (immediately after `links:zsh` ends, before `antidote:`). Body must:
- Have `desc: "Configure ZDOTDIR in /etc/zshenv (sudo write, idempotent)"`
- Be marked `internal: true` (matches `configs:ghostty:` precedent at links.yml:271 -- keeps `zdotdir` out of `task --list` output)
- Have one cmds entry: a heredoc block that sources `{{.DOTFILES_MESSAGES}}`, assigns ZDOTDIR_EXPORT to the literal string `export ZDOTDIR="{{.ZDOTDIR}}"` (template var, NOT shell var), then runs the three-branch if/elif/else from v1 `taskfiles/common.yml:42-53` verbatim: file-absent branch uses `sudo tee /etc/zshenv > /dev/null` (no `-a`); line-absent branch uses `sudo tee -a /etc/zshenv > /dev/null`; else branch calls `info "ZDOTDIR already configured in /etc/zshenv"`. Use messages helpers `info` and `success` for operator output per the v1 pattern.
- Have a status block with EXACTLY one entry: `grep -qF 'export ZDOTDIR="{{.ZDOTDIR}}"' /etc/zshenv 2>/dev/null`. The single-quoted grep pattern MUST embed `{{.ZDOTDIR}}` directly so zero `$VAR` references appear on this line (LINT-02 compliant per D-04).

DO NOT add `set -euo pipefail` to the cmds heredoc -- root Taskfile.yml line 31 declares `set: [errexit, pipefail]` globally and `set -u` would break go-task's empty-var template injection (RESEARCH.md Anti-Patterns line 253).

DO NOT use sudo inside the status block -- D-03 requires sudo-free status because `/etc/zshenv` is world-readable on macOS so `grep -qF` runs without sudo.

DO NOT create `/etc/zshenv` via redirection (`>` or `>>` from the current shell) -- those operators run as the current user, not root, and will fail with permission denied. Use `echo "$ZDOTDIR_EXPORT" | sudo tee /etc/zshenv > /dev/null` (or `sudo tee -a` for append) per D-03.

Reference structure: the recommended Option C sketch in 10-RESEARCH.md lines 292-334 shows the target file shape. Do not deviate from the literal v1 cmds shape (common.yml:42-53) for the if/elif/else body -- D-03 mandates direct port. The only v1->v2 hardening is the status-block template var.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && task lint:taskfile</automated>
  </verify>
  <acceptance_criteria>
    - `task lint:taskfile` exits 0 (proves LINT-02 + LINT-03a + LINT-03b all pass for the new `zdotdir` task and the modified `zsh` task outer status). [VALIDATION row 10-01-03]
    - `grep -n "zdotdir:" taskfiles/links.yml` returns at least two hits (one cmds entry `- task: zdotdir`, one task definition `zdotdir:`).
    - `grep -c 'export ZDOTDIR="{{.ZDOTDIR}}"' taskfiles/links.yml` returns >= 3 (cmds heredoc assignment + zdotdir status + zsh outer status).
    - The new `zdotdir:` task body contains the literal strings `internal: true` AND `sudo tee /etc/zshenv` AND `sudo tee -a /etc/zshenv`.
    - The status block of the new `zdotdir:` task contains NO `$ZDOTDIR` shell-var reference: `yq '.tasks.zdotdir.status' taskfiles/links.yml | grep -cE '\$[A-Za-z_]'` returns 0 (the `{{.ZDOTDIR}}` template is rendered by yq before grep sees it; the dollar-reference detection mirrors LINT-02 at taskfiles/lint.yml:145-147).
    - Operational integration (operator runs once during execution and records observation in summary; cannot be wholly automated without root): `sudo rm -f /etc/zshenv; task links:zsh` writes `/etc/zshenv` with the export line; the second `task links:zsh` invocation does NOT prompt for sudo. [VALIDATION rows 10-01-01 and 10-01-02]
  </acceptance_criteria>
  <done>
    - taskfiles/links.yml contains a new `zdotdir:` internal task with `{{.ZDOTDIR}}` in its status block (NO `$ZDOTDIR`).
    - taskfiles/links.yml `zsh:` task cmds appends `- task: zdotdir` after the five `_:safe-link` invocations.
    - taskfiles/links.yml `zsh:` task status block appends `- grep -qF 'export ZDOTDIR="{{.ZDOTDIR}}"' /etc/zshenv 2>/dev/null`.
    - `task lint:taskfile` exits 0.
    - PORT-01 is satisfied: D-01 (pipeline slot inside links:zsh), D-02 (code in links.yml, not shell.yml -- amends AUDIT row #1's proposed owner), D-03 (sudo tee, no sudo-v priming), D-04 (template-var status block) all honored.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add `validate` task to taskfiles/shell.yml asserting XDG dirs + ZDOTDIR + /etc/zshenv</name>
  <files>taskfiles/shell.yml</files>
  <read_first>
    - taskfiles/shell.yml (the file you will edit; currently has only the `shell` perf task at lines 60-83)
    - taskfiles/common.yml (lines 63-88 -- v1 source-of-truth template for the validate task body)
    - taskfiles/helpers.yml (lines 75-83 -- note: `_:check-dir` ALWAYS exits 0 even on failure; do NOT rely on its exit code, per RESEARCH.md Landmine 4)
    - taskfiles/links.yml (lines 313-382 -- the failures-counter / `exit "$failures"` pattern in `links:validate` is the model to follow)
    - .planning/phases/10-v1-drop-remediation/10-CONTEXT.md (decisions D-05, D-06 -- dispositive)
    - .planning/phases/10-v1-drop-remediation/10-RESEARCH.md (sections `### Pattern 3: New validate task body` lines 207-241, `### PORT-02 -- shell:validate` lines 347-366, `### Landmine 4 -- _:check-dir exit code` lines 555-575)
    - .planning/phases/10-v1-drop-remediation/10-VALIDATION.md (rows 10-01-04, 10-01-05 -- the automated commands you must make pass)
    - install/messages.zsh (provides `check`, `cross`, `info` -- sourced via `{{.DOTFILES_MESSAGES}}`)
    - Taskfile.yml (lines 34-40 -- root XDG vars and ZDOTDIR template vars you will reference)
    - CLAUDE.md (LINT-04: `set -euo pipefail` requirement; note this applies to executable .zsh files, NOT taskfile cmd blocks)
    - .claude/CLAUDE.md (file-level comment block at top of every script; errors to stderr; no emojis)
  </read_first>
  <action>
Append a new `validate:` task to `taskfiles/shell.yml` (after the existing `shell:` perf task at line 83). Do not modify the existing `shell:` task or the vars/includes block.

Structural requirements:
- Add a brief 1-2 line comment above the task definition: `# PORT-02: shell-layer validation (XDG dirs, ZDOTDIR dir, /etc/zshenv ZDOTDIR line, DOTFILES_MACHINE state).`
- Mark the task `# lint-allow: cmds-without-status` (validates are always-re-run by design; matches the existing `shell:` task at line 60 and links.yml:283 and links.yml:407 precedents).
- `desc: "Validate shell layer (XDG dirs, ZDOTDIR dir, /etc/zshenv ZDOTDIR line, DOTFILES_MACHINE state)"`
- `status: [false]` (mirrors `links:validate` at links.yml:287 -- forces always-re-run AND satisfies LINT-03a)
- `cmds:` ONE heredoc cmd that uses the `failures` counter pattern from `links.yml:313-382` (NOT the broken-exit-code `_:check-dir` helper -- RESEARCH.md Landmine 4 confirms `_:check-dir` always returns 0 even on missing dirs, which would let `shell:validate` print `cross` lines but exit 0).

The single cmd body must:
1. Open with `{{.DOTFILES_MESSAGES}}` to source the message helpers.
2. Initialize `failures=0`.
3. For each of the FIVE directory assertions, run `[[ -d "{{.X}}" ]]` and on success call `check "<name> exists"`, on failure call `cross "<name> missing"` then `failures=$(( failures + 1 ))`. The five dirs and their labels:
   - `{{.XDG_CONFIG_HOME}}` -- "XDG config home"
   - `{{.XDG_DATA_HOME}}` -- "XDG data home"
   - `{{.XDG_STATE_HOME}}` -- "XDG state home"
   - `{{.XDG_CACHE_HOME}}` -- "XDG cache home"
   - `{{.ZDOTDIR}}` -- "ZDOTDIR directory"
4. For the /etc/zshenv ZDOTDIR-line assertion: declare ZDOTDIR_EXPORT to the literal `export ZDOTDIR="{{.ZDOTDIR}}"` (template var embedded -- NOT `$ZDOTDIR`); then `if [[ -f /etc/zshenv ]] && grep -qF "$ZDOTDIR_EXPORT" /etc/zshenv; then check "ZDOTDIR configured in /etc/zshenv"; else cross "ZDOTDIR not configured in /etc/zshenv"; failures=$(( failures + 1 )); fi`.
5. For the DOTFILES_MACHINE state-file assertion (D-discretion item in 10-CONTEXT.md and RESEARCH.md `### PORT-02 -- shell:validate` "Optional D-08 discretion (DOTFILES_MACHINE check)" -- INCLUDE per RESEARCH recommendation; catches the fresh-install pitfall where the operator forgets `task setup`): `if [[ -r "{{.XDG_STATE_HOME}}/dotfiles/machine" ]]; then check "DOTFILES_MACHINE state file present"; else cross "DOTFILES_MACHINE state file missing -- run: task setup -- <machine-name>"; failures=$(( failures + 1 )); fi`.
6. End with `exit "$failures"` so the root validate aggregator's exit-code capture sees a real non-zero on any failed check (mirrors links.yml:382).

The Taskfile.yml root XDG vars (XDG_CONFIG_HOME, XDG_DATA_HOME, XDG_STATE_HOME, XDG_CACHE_HOME, ZDOTDIR) are already exposed as `{{.X}}` template vars and inherit into included taskfiles (the existing `shell.yml` vars block does NOT redeclare them -- root vars win on include merge).

DO NOT add `set -euo pipefail` to the cmd body -- root Taskfile.yml:31 sets `[errexit, pipefail]` globally; `set -u` would break empty-template-var rendering.

DO NOT use `_:check-dir` -- its return-zero-on-failure behavior would break the aggregator's exit-code capture (Landmine 4, RESEARCH.md lines 555-575).

DO NOT add a file-level comment block (that rule applies to `.zsh` script files per `.claude/CLAUDE.md`, not to taskfile cmd blocks).
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && task lint:taskfile && task -t taskfiles/shell.yml validate</automated>
  </verify>
  <acceptance_criteria>
    - `task lint:taskfile` exits 0 (proves the new task body has no `$VAR` in status block; `status: [false]` satisfies LINT-03a).
    - `task -t taskfiles/shell.yml validate` exits 0 on the current development machine (assuming `/etc/zshenv` is already present from the v1 install, which is true on the developer's existing machine). [VALIDATION row 10-01-04]
    - Operator runs the negative test once during execution and records observation in summary: `sudo mv /etc/zshenv /tmp/zshenv.bak; task -t taskfiles/shell.yml validate; ec=$?; sudo mv /tmp/zshenv.bak /etc/zshenv; [[ $ec -ne 0 ]]` -- confirm `$ec` is non-zero. [VALIDATION row 10-01-05]
    - `grep -c 'failures=' taskfiles/shell.yml` returns >= 2 (init + at least one increment).
    - `grep -cF 'exit "$failures"' taskfiles/shell.yml` returns >= 1.
    - `grep -n "validate:" taskfiles/shell.yml` returns at least one hit (the new task).
  </acceptance_criteria>
  <done>
    - taskfiles/shell.yml has a new `validate:` task asserting four XDG dirs + ZDOTDIR dir + /etc/zshenv ZDOTDIR line + DOTFILES_MACHINE state file.
    - The task uses the `failures` counter + `exit "$failures"` pattern (NOT `_:check-dir`).
    - The task uses ONLY `{{.X}}` template vars and the canonical `'export ZDOTDIR="{{.ZDOTDIR}}"'` literal in the grep -- zero `$ZDOTDIR` shell-var references.
    - `task lint:taskfile` exits 0.
    - `task -t taskfiles/shell.yml validate` produces operator output and exits non-zero on any missing dir/file (D-05).
  </done>
</task>

<task type="auto">
  <name>Task 3: Dual-alias taskfiles/shell.yml include and wire shell into root validate aggregator</name>
  <files>Taskfile.yml</files>
  <read_first>
    - Taskfile.yml (you will edit this file; particularly lines 80-122 includes block and lines 215, 222 aggregator loops)
    - .planning/phases/10-v1-drop-remediation/10-CONTEXT.md (decision D-05 wiring; D-06 include strategy options a/b/c)
    - .planning/phases/10-v1-drop-remediation/10-RESEARCH.md (sections `### Include Strategy for shell.yml (D-06)` lines 368-400, `### Root validate Aggregator Integration (D-05)` lines 402-421, `### Landmine 5 -- Aggregator loop edit drift` lines 577-581)
    - .planning/phases/10-v1-drop-remediation/10-VALIDATION.md (row 10-01-06 -- the automated command you must make pass)
    - taskfiles/README.md (lines 27 and 54 -- existing `task perf:shell` doc references that must keep resolving)
    - shell/README.md (line 43 -- existing `task perf:shell` doc reference that must keep resolving)
  </read_first>
  <action>
Two concrete edits in `Taskfile.yml`:

EDIT 1 -- Includes block (current lines 80-122). Replace the single line at line 92 `  perf:     ./taskfiles/shell.yml` with TWO include entries pointing at the same file (D-06 dual-alias decision per RESEARCH.md `### Include Strategy for shell.yml`):

  perf:     ./taskfiles/shell.yml   # legacy alias: task perf:shell (SHEL-12 cold-start gate)
  shell:    ./taskfiles/shell.yml   # primary alias: task shell:validate (PORT-02)

Both aliases load the same file. `task perf:shell` continues to work (the SHEL-12 CI gate is preserved -- taskfiles/README.md:27, taskfiles/README.md:54, taskfiles/shell.yml:12-13, shell/README.md:43 all remain valid). `task shell:validate` becomes available for the aggregator. Rejected alternatives (a) rename and (c) file split are documented in RESEARCH.md lines 385-389 -- dual-alias is lowest-churn (zero external reference updates).

EDIT 2 -- Validate-aggregator (current line 215 and line 222). Append `shell` as the seventh token in BOTH for-loops, preserving the existing six-token order verbatim (least-churn placement per RESEARCH.md `### Root validate Aggregator Integration` "Placement rationale" + Landmine 5):
- Line 215: replace `for component in manifest identity links macos packages claude; do` with `for component in manifest identity links macos packages claude shell; do`
- Line 222: same exact replacement.

CRITICAL (Landmine 5): both lines use the IDENTICAL token list. Search-and-replace must produce exactly two matches; verify with `grep -c "manifest identity links macos packages claude shell" Taskfile.yml` returns `2`. Mis-edits where only one loop is updated cause the summary table to omit `shell` (or worse, run it without rendering its row).

No other Taskfile.yml edits. Do NOT touch the validate task's surrounding comments or set/silent/vars blocks. Do NOT add a sentinel feature-disabled handling for `shell` -- the validation is unconditional on every machine (every machine needs XDG dirs + /etc/zshenv per CONTEXT.md "feature-flagged sub-tasks: no feature flag applies to PORT-01/02").
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [[ "$(grep -c 'manifest identity links macos packages claude shell' Taskfile.yml)" == "2" ]] && task --list-all 2>/dev/null | grep -q 'shell:validate' && task --list-all 2>/dev/null | grep -q 'perf:shell'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'manifest identity links macos packages claude shell' Taskfile.yml` returns exactly `2` (both aggregator loops updated; Landmine 5 protection).
    - `task --list-all` lists BOTH `shell:validate` (new) AND `perf:shell` (preserved). [Confirms the dual-alias include works.]
    - `task validate` runs to completion and its summary section contains a row tagged `shell` (either `check shell`, `cross shell`, or `n/a   shell`). [VALIDATION row 10-01-06] Operator runs once and records observation.
    - `grep -n 'shell:    ./taskfiles/shell.yml' Taskfile.yml` returns at least one hit (the new alias).
    - `grep -n 'perf:     ./taskfiles/shell.yml' Taskfile.yml` returns at least one hit (the legacy alias preserved).
  </acceptance_criteria>
  <done>
    - Taskfile.yml includes block has BOTH `perf:` and `shell:` aliases pointing at `./taskfiles/shell.yml` (D-06 dual-alias).
    - Both for-loops in the validate aggregator (lines 215 and 222) end with `claude shell; do`.
    - `task perf:shell` and `task shell:validate` both resolve.
    - `task validate` summary includes a `shell` component row (D-05).
  </done>
</task>

<task type="auto">
  <name>Task 4: Amend AUDIT.md row 3 (Things3) and counts table per D-07</name>
  <files>.planning/phases/09-v1-drop-audit/AUDIT.md</files>
  <read_first>
    - .planning/phases/09-v1-drop-audit/AUDIT.md (the file you will edit; particularly lines 9-14 counts table, lines 18-22 keep-list bullets, and the row near line 79 for `install/Brewfile-personal.rb:72`)
    - manifests/machines/personal-laptop.toml (lines 65-71 -- the unchanged MAS array; confirm `Things3` is the canonical mas-list name; DO NOT EDIT this file)
    - .planning/phases/10-v1-drop-remediation/10-CONTEXT.md (decision D-07 -- dispositive; "leave Things3 in personal-laptop.toml; amend AUDIT.md row from keep to drop")
    - .planning/phases/10-v1-drop-remediation/10-RESEARCH.md (section `### AUDIT.md Row Amend Specification (D-07)` lines 423-468 -- contains the EXACT replacement text verbatim)
    - .planning/phases/10-v1-drop-remediation/10-VALIDATION.md (row 10-01-07 -- the automated command you must make pass)
  </read_first>
  <action>
Three concrete edits in `.planning/phases/09-v1-drop-audit/AUDIT.md`. The exact replacement text for each edit is documented verbatim in 10-RESEARCH.md `### AUDIT.md Row Amend Specification (D-07)` lines 425-466.

EDIT 1 -- Summary counts table (lines 9-14). Change two rows:
- `| Keep | 3 |` becomes `| Keep | 2 |`
- `| Drop | 99 |` becomes `| Drop | 100 |`
The other two rows (`Tasks audited 102`, `Already-ported 70`) are unchanged.

EDIT 2 -- Keep List bullets (lines 18-22). REMOVE the entire bullet (one line) for the Brewfile-personal Things row. Current text of the bullet to remove:
  - `install/Brewfile-personal.rb:72` -> **manifests/machines/personal-laptop.toml** -- mas 'Things' (id 904280696) declared in v1 personal-profile Brewfile

The other two keep-list bullets (the two `taskfiles/common.yml` rows) remain unchanged. After removal, two bullets remain in the Keep List section.

EDIT 3 -- Taskfiles table row near line 79. Locate the row beginning with `| install/Brewfile-personal.rb:72 |`. Replace columns 3 (v2 status), 4 (keep/drop), and 5 (rationale). Columns 1 (file:line), 2 (purpose), and 6 (v2 owner) remain unchanged.
- Column 3: change `partially-ported` to `ported`.
- Column 4: change `keep` to `drop`.
- Column 5: replace the existing rationale text with the new rationale verbatim from RESEARCH.md line 432: "Ported under canonical App Store name -- v1 used short name 'Things'; v2 uses canonical 'Things3' (the name `mas list` returns for id 904280696). The install primitive is the id (904280696), which is unchanged across v1 and v2 manifests; mas-list-name drift is a display-string concern, not an install concern. Functional install is correct on both. Phase 10 (D-07) chose to leave the v2 manifest name as the canonical 'Things3' rather than revert to the v1 short name."

Do NOT edit any other AUDIT.md row, header, or section. Do NOT edit `manifests/machines/personal-laptop.toml` -- D-07 explicitly leaves the Things3 entry in place.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && grep -qF '| Keep | 2 |' .planning/phases/09-v1-drop-audit/AUDIT.md && grep -qF '| Drop | 100 |' .planning/phases/09-v1-drop-audit/AUDIT.md && grep -q "^| install/Brewfile-personal.rb:72 |.*| ported |.*| drop |" .planning/phases/09-v1-drop-audit/AUDIT.md && ! grep -qF "install/Brewfile-personal.rb:72\` -> **manifests/machines/personal-laptop.toml** -- mas 'Things'" .planning/phases/09-v1-drop-audit/AUDIT.md && grep -qF 'name = "Things3"' manifests/machines/personal-laptop.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '| Keep | 2 |' .planning/phases/09-v1-drop-audit/AUDIT.md` matches (counts table updated).
    - `grep -F '| Drop | 100 |' .planning/phases/09-v1-drop-audit/AUDIT.md` matches (counts table updated).
    - The Taskfiles-table row for `install/Brewfile-personal.rb:72` shows `| ported |` and `| drop |` in columns 3 and 4: `grep "^| install/Brewfile-personal.rb:72 |" .planning/phases/09-v1-drop-audit/AUDIT.md | grep -q "| ported |.*| drop |"`. [VALIDATION row 10-01-07]
    - The keep-list bullet for `install/Brewfile-personal.rb:72 -> manifests/machines/personal-laptop.toml` is gone from the `## Summary` keep-list bullet block (lines 18-22 area).
    - `manifests/machines/personal-laptop.toml` is UNCHANGED -- `grep -F 'name = "Things3"' manifests/machines/personal-laptop.toml` still matches; `git diff manifests/machines/personal-laptop.toml` shows zero changes (D-07).
    - The two other keep-list bullets (the two `taskfiles/common.yml` rows) are still present in the file.
  </acceptance_criteria>
  <done>
    - AUDIT.md counts table reads Keep 2 / Drop 100 (3 -> 2 and 99 -> 100).
    - AUDIT.md keep-list contains exactly TWO bullets (the third Things bullet is removed).
    - AUDIT.md `install/Brewfile-personal.rb:72` row is now `ported` / `drop` with the new rationale text from RESEARCH.md.
    - `manifests/machines/personal-laptop.toml` is unchanged (Things3 preserved per D-07).
  </done>
</task>

<task type="auto">
  <name>Task 5: Author .planning/phases/10-v1-drop-remediation/10-SMOKE.md (PORT-03)</name>
  <files>.planning/phases/10-v1-drop-remediation/10-SMOKE.md</files>
  <read_first>
    - .planning/phases/10-v1-drop-remediation/10-CONTEXT.md (decision D-08 -- dispositive; documented smoke procedure in lieu of real fresh-machine install; ROADMAP P10 SC#3 names the exact file path)
    - .planning/phases/10-v1-drop-remediation/10-RESEARCH.md (section `### PORT-03 -- Smoke Procedure Sketch` lines 470-524 -- contains the recommended file shape including pre-step setup, first-shell assertions, pass criteria, and Run Log table)
    - .planning/phases/10-v1-drop-remediation/10-VALIDATION.md (row 10-01-08 -- the manual verification you must make possible)
    - .planning/ROADMAP.md (Phase 10 SC#3 -- "procedure and pass result recorded in `.planning/phases/10-v1-drop-remediation/10-SMOKE.md`")
    - .claude/CLAUDE.md (no emojis in any file -- markdown included; no AI attribution)
    - install/messages.zsh (note: the `check` and `cross` helpers print Unicode glyphs check-mark and ballot-x; those appear in operator OUTPUT, not in the markdown source of this document)
  </read_first>
  <action>
Create a new file `.planning/phases/10-v1-drop-remediation/10-SMOKE.md`. The file is markdown; no executable code, no scripts, no `set -euo pipefail` (does not apply to markdown).

Required document sections (RESEARCH.md `### PORT-03 -- Smoke Procedure Sketch` lines 478-520 provides the literal recommended sketch; this is the target shape):

1. H1 title: `# Phase 10: First-Shell Smoke Procedure`
2. Metadata block: `**Recorded:** <date>`, `**Procedure run on:** <machine-name> (existing v2-cut-over machine, NOT a real fresh install)`, `**Result:** PASS / FAIL`
3. `## What this is` -- one paragraph explaining that this satisfies PORT-03 via a documented smoke procedure per D-08 / ROADMAP P10 SC#3, and that a real fresh-machine install is deferred to a later milestone (deferred-ideas in CONTEXT.md).
4. `## Procedure` -- with two H3 subsections:
   - `### Pre-step setup` -- numbered list (1, 2, 3): (1) verify `/etc/zshenv` contains the ZDOTDIR export with `grep -F 'export ZDOTDIR="$HOME/.config/zsh"' /etc/zshenv`; (2) verify `task validate` exits 0 with `task validate; echo "exit: $?"`; (3) verify the new `shell:validate` row appears in the validate summary.
   - `### First-shell assertions` -- intro line ("Launch a fresh terminal (Ghostty, Terminal.app, or `zsh -li`). The new shell MUST satisfy:") then a markdown checkbox list of SEVEN assertions (each as `- [ ]` items, VERBATIM as listed below; these are the load-bearing PORT-03 SC#3 assertions and MUST appear verbatim in the document so the operator can tick them off):
     - `- [ ] echo "$ZDOTDIR" prints $HOME/.config/zsh (sourced from /etc/zshenv)`
     - `- [ ] echo "$DOTFILES_MACHINE" prints the active machine name (sourced from shell/.zshenv)`
     - `- [ ] The alanpeabody-derived prompt renders (user, pwd, git branch tokens visible)`
     - `- [ ] type _dotfiles_feature shows "_dotfiles_feature is a function"`
     - `- [ ] alias lists the expected ported aliases (at minimum: reload, path, ll, t)`
     - `- [ ] motd output appears (or the cache file $XDG_CACHE_HOME/dotfiles/motd.cache exists)`
     - `- [ ] No "command not found" errors during shell init`
5. `### Pass criteria` -- one short paragraph: every checkbox above must be ticked; record date, machine, and result in the `## Run Log` table.
6. `## Run Log` -- markdown table with header `| Date | Machine | Result | Notes |` and one initial template row (`| <date> | <machine> | PASS / FAIL | <any deviations> |`). The operator overwrites the template row when running the procedure.

Constraints:
- NO emojis anywhere in the file (project rule, stricter than the global rule; `.claude/CLAUDE.md`).
- NO AI attribution (`.claude/CLAUDE.md`).
- NO Unicode check-mark or ballot-x glyphs in the markdown body (those are operator-output glyphs from messages.zsh, not document content).
- The file is ~50-80 lines of plain markdown.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && test -f .planning/phases/10-v1-drop-remediation/10-SMOKE.md && grep -q '^# Phase 10: First-Shell Smoke Procedure' .planning/phases/10-v1-drop-remediation/10-SMOKE.md && grep -q '^## Run Log' .planning/phases/10-v1-drop-remediation/10-SMOKE.md && grep -q '_dotfiles_feature' .planning/phases/10-v1-drop-remediation/10-SMOKE.md && grep -q '^- \[ \] echo "\$ZDOTDIR"' .planning/phases/10-v1-drop-remediation/10-SMOKE.md && grep -q '^- \[ \] motd' .planning/phases/10-v1-drop-remediation/10-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/10-v1-drop-remediation/10-SMOKE.md` exists. [VALIDATION row 10-01-08]
    - File contains H1 `# Phase 10: First-Shell Smoke Procedure`.
    - File contains the `## What this is`, `## Procedure`, `### Pre-step setup`, `### First-shell assertions`, `### Pass criteria`, and `## Run Log` sections.
    - The `### First-shell assertions` section contains all SEVEN load-bearing assertions verbatim as listed in the action: `$ZDOTDIR` resolves; `$DOTFILES_MACHINE` prints; alanpeabody prompt renders; `type _dotfiles_feature` shows function; `alias` lists ported aliases; `motd` prints (or cache exists); no command-not-found errors. Each as a `- [ ]` checkbox line.
    - The `## Run Log` section contains a markdown table with header `| Date | Machine | Result | Notes |`.
    - The file contains NO emojis (project rule).
    - The file contains NO AI-attribution strings (no "Generated by", no "Co-Authored-By", no "Claude").
  </acceptance_criteria>
  <done>
    - `.planning/phases/10-v1-drop-remediation/10-SMOKE.md` exists with the required sections.
    - The seven first-shell assertions are present verbatim as checkboxes.
    - The Run Log table exists for operator pass/fail recording.
    - PORT-03 is satisfied by the documented smoke procedure (D-08, ROADMAP P10 SC#3).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Operator runs PORT-01 idempotency + PORT-02 negative test + PORT-03 smoke procedure</name>
  <what-built>
    - taskfiles/links.yml: new internal `zdotdir` task + `task: zdotdir` dispatch from `links:zsh` + outer status extension (PORT-01).
    - taskfiles/shell.yml: new `validate` task asserting XDG dirs + ZDOTDIR + /etc/zshenv ZDOTDIR line + DOTFILES_MACHINE state file (PORT-02).
    - Taskfile.yml: dual-alias include (`perf:` AND `shell:` both point at `./taskfiles/shell.yml`); `shell` token appended to both validate-aggregator loops (D-05, D-06).
    - .planning/phases/09-v1-drop-audit/AUDIT.md: row 3 reclassified keep -> drop; counts updated to Keep 2 / Drop 100; keep-list bullet removed (D-07).
    - .planning/phases/10-v1-drop-remediation/10-SMOKE.md: new file documenting the fresh-shell smoke procedure (PORT-03, D-08).
  </what-built>
  <how-to-verify>
    Three operator checks. Record observations in the plan summary; the smoke procedure result also lands in 10-SMOKE.md's Run Log table.

    1. PORT-01 fresh-write + idempotency (VALIDATION rows 10-01-01, 10-01-02):
       - Back up the current file: `sudo cp /etc/zshenv /tmp/zshenv.bak` (skip if absent).
       - Force fresh-write path: `sudo rm -f /etc/zshenv && task links:zsh` -- sudo prompt is expected once; the file should be created.
       - Confirm the line landed: `grep -F 'export ZDOTDIR="$HOME/.config/zsh"' /etc/zshenv` should match.
       - Idempotency: `task links:zsh` again -- must NOT prompt for sudo (the inner `zdotdir` task's status block short-circuits via `grep -qF`).
       - Restore if you had a backup: `sudo mv /tmp/zshenv.bak /etc/zshenv` (only if you backed up a non-trivial file).

    2. PORT-02 negative test (VALIDATION row 10-01-05):
       - `sudo mv /etc/zshenv /tmp/zshenv.bak; task shell:validate; echo "exit: $?"; sudo mv /tmp/zshenv.bak /etc/zshenv`
       - Expected: `task shell:validate` prints a `cross` line for "ZDOTDIR not configured in /etc/zshenv" and exits non-zero.
       - Also confirm: `task validate` summary table shows a `shell` row (VALIDATION row 10-01-06).

    3. PORT-03 smoke procedure (VALIDATION row 10-01-08):
       - Open 10-SMOKE.md and follow the procedure section.
       - Launch a fresh terminal (Ghostty, Terminal.app, or `zsh -li`).
       - Tick each of the seven first-shell assertions.
       - Record date, machine, and PASS/FAIL in the Run Log table at the bottom of 10-SMOKE.md.
  </how-to-verify>
  <resume-signal>Type "approved" once all three checks pass and 10-SMOKE.md Run Log is updated. If any assertion fails, describe the failure (which assertion, what was observed) and which task to revisit.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| current-user -> root (sudo write to /etc/zshenv) | The new `zdotdir` task in `taskfiles/links.yml` invokes `sudo tee` to write a single export line to `/etc/zshenv`. Sudo is invoked from a taskfile cmd body that runs as the operator; the elevated write is bounded to a one-line append (or one-line create) under three explicit branches. |
| operator -> AUDIT.md (planning artifact) | AUDIT.md is the source-of-truth Phase 11 will consult before deleting v1 files. The amend here is a row reclassification and counts edit; no code or operational behavior changes from this artifact edit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation of Privilege | `zdotdir` task `sudo tee` write | mitigate | Sudo invocation is bounded to a single literal command (`sudo tee /etc/zshenv` or `sudo tee -a /etc/zshenv`) with the input piped from a shell-quoted constant `ZDOTDIR_EXPORT` whose value is fully derived from go-task template vars (`{{.ZDOTDIR}}` resolves to `{{.XDG_CONFIG_HOME}}/zsh` from root Taskfile.yml:40). No user-provided input reaches the sudo invocation; no shell-metachar injection surface. Status block stays sudo-free per D-03 (file is world-readable) so re-runs do not re-prompt. |
| T-10-02 | Tampering | `/etc/zshenv` content after manual operator edit | accept | RESEARCH.md Landmine 3 documents the case: if an operator manually edits `/etc/zshenv` with different quoting, `grep -qF` does not match and a SECOND line is appended on next install. Both lines export the same value; last wins. Same behavior as v1; documented in 10-SMOKE.md for operator awareness. |
| T-10-03 | Denial of Service | `task install` re-runs prompting sudo on every invocation | mitigate | LINT-02 enforces template-var-only in status blocks; the new `zdotdir` task status uses `{{.ZDOTDIR}}` (not `$ZDOTDIR`). If the executor introduces the v1 macos:shell:145 bug class (shell var in status), `task lint:taskfile` exits non-zero in Task 1's automated verify -- regression caught pre-merge. |
| T-10-04 | Information Disclosure | `/etc/zshenv` reveals user home path via the ZDOTDIR export | accept | `/etc/zshenv` is world-readable by default on macOS (the design assumption documented at D-03). The export line discloses `$HOME/.config/zsh` -- not a secret on a single-user macOS workstation. v1 had identical behavior. |
| T-10-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase. RESEARCH.md `## Standard Stack` confirms no external packages added (no `## Package Legitimacy Audit` needed). |
</threat_model>

<verification>
## Phase-level checks (run after all tasks complete)

Static and integration:
- `task lint` exits 0 (LINT-02 + LINT-03a + LINT-03b + LINT-04 + LINT-07 all green).
- `task lint:taskfile` exits 0 (the most relevant gate for this phase -- LINT-02 catches the v1 macos:shell:145 bug class regression).
- `task --list-all` lists `shell:validate` (new) AND `perf:shell` (preserved) AND `zdotdir` is NOT listed (it is `internal: true`).
- `grep -c "manifest identity links macos packages claude shell" Taskfile.yml` returns `2` (Landmine 5 protection -- both aggregator loops updated).
- `task validate` runs end-to-end and prints a `shell` row in the summary table.
- `task shell:validate` exits 0 on the developer's machine (the developer's existing /etc/zshenv from the v1 install is what the validate asserts).

PORT-01 operational (operator-run, observation recorded in plan summary):
- Fresh-write path: `sudo rm -f /etc/zshenv && task links:zsh` writes the file with the ZDOTDIR export line.
- Idempotency: subsequent `task links:zsh` invocations do NOT prompt for sudo.

PORT-02 negative path (operator-run):
- `sudo mv /etc/zshenv /tmp/zshenv.bak; task shell:validate; ec=$?; sudo mv /tmp/zshenv.bak /etc/zshenv; [[ $ec -ne 0 ]]` -- `$ec` is non-zero.

PORT-03 smoke procedure (operator-run via 10-SMOKE.md):
- All seven first-shell assertions tick; Run Log table records PASS with date and machine.

Documentation:
- `.planning/phases/09-v1-drop-audit/AUDIT.md` counts read Keep 2 / Drop 100; keep-list has two bullets; Brewfile-personal Things row shows `ported` / `drop` (D-07).
- `manifests/machines/personal-laptop.toml` is UNCHANGED (D-07: Things3 preserved).
- `.planning/phases/10-v1-drop-remediation/10-SMOKE.md` exists with the seven first-shell assertions and a Run Log table (PORT-03, D-08).
</verification>

<success_criteria>
- PORT-01: v2 `task install` writes `/etc/zshenv` with `export ZDOTDIR="$HOME/.config/zsh"` on a fresh machine; re-running is a sudo-free no-op. Code lives in `taskfiles/links.yml` (D-02 amends AUDIT row #1's proposed `taskfiles/shell.yml` owner). Status block uses `{{.ZDOTDIR}}` (D-04; LINT-02 compliant). [ROADMAP P10 SC#1]
- PORT-02: `task shell:validate` exists in `taskfiles/shell.yml` (D-06) and asserts the four XDG dirs + ZDOTDIR dir + /etc/zshenv ZDOTDIR line + DOTFILES_MACHINE state file; the `shell` component is wired into both validate-aggregator loops (D-05); `task perf:shell` (the existing SHEL-12 CI gate) still works (D-06 dual-alias). [ROADMAP P10 SC#2]
- AUDIT row 3 amended: `install/Brewfile-personal.rb:72` is reclassified `keep` -> `drop`; counts table reads Keep 2 / Drop 100; `manifests/machines/personal-laptop.toml` is unchanged (Things3 preserved); the keep-list bullet for that row is removed (D-07). [ROADMAP P10 SC#2]
- PORT-03: `.planning/phases/10-v1-drop-remediation/10-SMOKE.md` exists and documents the fresh-shell smoke procedure (seven assertions + Run Log table); operator runs the procedure and records PASS in the Run Log (D-08). [ROADMAP P10 SC#3]
- Zero "keep" rows remain unimplemented when Phase 10 closes: all three keep rows from AUDIT.md are addressed (rows #1 and #2 by PORT-01 and PORT-02; row #3 reclassified to drop). [ROADMAP P10 SC#4]
- `task lint:taskfile` exits 0; no `$VAR` references in any new or modified status block (LINT-02 regression check; the v1 macos:shell:145 bug class did not return).
</success_criteria>

<output>
Create `.planning/phases/10-v1-drop-remediation/10-01-SUMMARY.md` when done. The summary must include:

- Files modified (the five listed in frontmatter).
- For each task, the verify command result (pass/fail).
- For Task 1: operator's observation of the fresh-write + idempotency PORT-01 operational test.
- For Task 2: operator's observation of the PORT-02 negative test result.
- For Task 3: confirmation that `task --list-all` shows BOTH `shell:validate` and `perf:shell`.
- For Task 4: the new counts (Keep 2 / Drop 100) and the row 3 column-3/4 verbatim.
- For Task 5: confirmation that the seven first-shell assertions are present and the Run Log header line exists.
- For Task 6: the operator's PASS/FAIL record for the 10-SMOKE.md Run Log.
- Decision tracking: which D-XX decisions were honored (expect all of D-01..D-08).
- Any deviations from the plan with rationale.
- Any deferred items surfaced during execution (none expected -- scope is locked).
</output>

---
id: 07-03
phase: 07-claude-tool-configs-smoke-tests
plan: 3
type: execute
wave: 2
depends_on: [07-02]
files_modified:
  - taskfiles/claude.yml
  - Taskfile.yml
  - claude/README.md
autonomous: true
requirements: [CLDE-01, CLDE-03, CLDE-04]
requirements_addressed: [CLDE-01, CLDE-03, CLDE-04]
must_haves:
  truths:
    - "`task claude:install` is an aggregator feature-gated on `claude-marketplace` that runs `ensure-cli`, `marketplace`, `gsd` in order"
    - "`task claude:marketplace` is a no-op on a converged machine because its two-condition `status:` block (marketplaces + plugins) returns 0"
    - "`task claude:gsd` runs `npx -y get-shit-done-cc@latest --claude --global` only when `{{.GSD_SENTINEL}}` is absent; after success it touches the sentinel"
    - "`{{.GSD_SENTINEL}}` resolves to `{{.XDG_STATE_HOME}}/dotfiles/gsd-installed`"
    - "`task claude:update` is NOT in `task install`'s call graph and explicitly refreshes marketplaces + plugins + GSD"
    - "`task claude:status` is diagnostic (always re-runs; no status block; carries `# lint-allow: cmds-without-status` marker)"
    - "`task claude:validate` enumerates check/cross for marketplaces + plugins + GSD sentinel (composed into root `task validate` in P8)"
    - "`task claude:ensure-cli` hard-fails with the message 'Run `task packages:install` first ...' when `claude` or `jq` is missing"
    - "Root `Taskfile.yml` `includes.claude:` points to `./taskfiles/claude.yml`, not `./taskfiles/claude-stub.yml`"
    - "Servers with `claude-marketplace = false` skip `claude:install` at the feature-gate level"
  artifacts:
    - path: "taskfiles/claude.yml"
      provides: "Real Phase-7 claude task suite (CLDE-01..04)"
      contains: "GSD_SENTINEL"
      exports: ["install", "marketplace", "gsd", "update", "status", "validate", "ensure-cli"]
    - path: "Taskfile.yml"
      provides: "Root include flip from claude-stub.yml to claude.yml"
      contains: "claude:   ./taskfiles/claude.yml"
    - path: "claude/README.md"
      provides: "Updated repo-ownership boundaries doc (post D-02 split)"
      contains: "GSD-managed"
  key_links:
    - from: "task install"
      to: "task claude:install"
      via: "Taskfile.yml line ~142 (existing call)"
      pattern: "task: claude:install"
    - from: "claude:install"
      to: "features.claude-marketplace"
      via: "go-template index access in cmds: block"
      pattern: 'index \.MANIFEST\.features "claude-marketplace"'
    - from: "claude:gsd cmds:"
      to: "{{.GSD_SENTINEL}}"
      via: "touch after successful npx"
      pattern: 'touch \{\{\.GSD_SENTINEL\}\}'
---

<objective>
Replace `taskfiles/claude-stub.yml` (Phase 2 placeholder) with the real Phase 7 implementation of `taskfiles/claude.yml`, flip the root `Taskfile.yml` include, and update `claude/README.md` to document the post-D-02 repo-ownership boundaries.

Purpose: The Phase 2 stub exists so the lint suite can self-test against a fully wired manifest. The current `taskfiles/claude.yml` body (from a P2 overhaul) lacks status: blocks (`npx` runs on every `task install` — the gsd-install bug class), lacks the `GSD_SENTINEL` var (D-09 / D-11), and is not connected to the root pipeline (root `includes.claude:` still points at the stub). This plan delivers the production task suite per D-09..D-14.

Output: A v2-conformant `taskfiles/claude.yml` exposing seven tasks (`install`, `marketplace`, `gsd`, `update`, `status`, `validate`, `ensure-cli`); a root `Taskfile.yml` whose `claude:` include is the real file; and a `claude/README.md` updated to document what the repo owns vs what GSD owns at runtime.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-claude-tool-configs-smoke-tests/07-CONTEXT.md
@.planning/phases/07-claude-tool-configs-smoke-tests/07-02-SUMMARY.md
@CLAUDE.md
@.claude/CLAUDE.md
@taskfiles/claude.yml
@taskfiles/claude-stub.yml
@taskfiles/helpers.yml
@taskfiles/packages.yml
@taskfiles/macos.yml
@taskfiles/lint.yml
@Taskfile.yml
@install/messages.zsh
@manifests/defaults.toml
@manifests/machines/server-1.toml
@claude/README.md

<interfaces>
Vars passed from root Taskfile.yml (in scope for claude.yml when loaded via includes:):
  {{.XDG_STATE_HOME}}, {{.XDG_CONFIG_HOME}}, {{.DOTFILEDIR}}, {{.DOTFILES_MESSAGES}}, {{.HOMEBREW_PREFIX}}, {{.MANIFEST}}

{{.MANIFEST}} is fromJson-loaded; feature access uses index .MANIFEST.features "claude-marketplace" (kebab-case).

Current claude.yml internal task names that consolidate in the rewrite:
  marketplaces-add + plugins-install  -> `marketplace` (single internal, two-condition status)
  gsd-install                          -> `gsd` (sentinel-status internal)
  marketplaces-update + plugins-update -> consumed inline by the new `update` aggregator

Existing root Taskfile.yml line 108: claude:   ./taskfiles/claude-stub.yml     # P7 wires real bodies
After Task 2:                        claude:   ./taskfiles/claude.yml          # P7 real bodies
Existing root Taskfile.yml line 142: - task: claude:install  -- NO EDIT NEEDED.

Pattern reference from taskfiles/packages.yml (manifest gate):
  deps: [manifest:resolve]
  cmds:
    - '{{if index .MANIFEST.features "<key>"}}'
      ...
    - '{{end}}'

Pattern reference from taskfiles/macos.yml: aggregator carries `# lint-allow: cmds-without-status` marker on the line directly ABOVE the task name (not inside the task body).

LINT-02 hardening note (W-02): the `marketplace` status: block MUST NOT contain any `$VAR` shell variable -- not even loop-local `$name` inside a `for` loop. CLAUDE.md states "The block must use template variables (`{{.X}}`), never shell variables (`$X`)" and the lint regex is strict against any `$VAR` token between `status:` and the next task name. Render one status line per marketplace/plugin entry at go-template render time using `range` over `{{.CLAUDE_MARKETPLACES}}` / `{{.CLAUDE_PLUGINS}}` (file-level vars known at render time), so every status line is a fully-interpolated `jq -e '.[] | select(.name == "<literal>")' <(claude plugin marketplace list --json) >/dev/null` with NO shell-var references.

Dynamic-var scope note (W-01): in go-task, file-level `vars:` with `sh:`-prefixed values are evaluated at task-graph build time on EVERY task invocation (including `task --list`, `task lint`, `task test`, `task --list-all --json`). Putting `claude plugin marketplace list --json` and `claude plugin list --json` at file scope therefore spawns two `claude` subprocesses on EVERY task call across the repo, eating LINT-08's 5-second budget for `task lint` and slowing every diagnostic. These two `sh:` vars MUST live INSIDE the `marketplace:` task body's task-scoped `vars:` block so they only execute when `task claude:marketplace` itself is actually invoked.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite `taskfiles/claude.yml` with the Phase 7 task suite</name>
  <files>taskfiles/claude.yml</files>
  <read_first>
    - taskfiles/claude.yml (current body — port the marketplaces-add / marketplaces-update / plugins-install / plugins-update / gsd-install bodies forward into new shape)
    - taskfiles/packages.yml (pattern: deps:[manifest:resolve], `{{if index .MANIFEST.features "..."}}` gate, `{{end}}`)
    - taskfiles/macos.yml (aggregator pattern + `# lint-allow: cmds-without-status` marker placement)
    - taskfiles/lint.yml (LINT-02 + LINT-03a enforcement; aggregator self-exemption pattern)
    - .planning/phases/07-claude-tool-configs-smoke-tests/07-CONTEXT.md (`<specifics>` skeleton + D-09..D-14 shapes; CLDE-03 wording for hooks-into-rest of suite)
    - manifests/defaults.toml (`claude-marketplace = true` default; kebab-case key needs `index` access)
    - manifests/machines/server-1.toml (`claude-marketplace = false` — install aggregator must skip)
    - install/messages.zsh (`check`, `cross`, `info`, `success`, `warn` functions used throughout)
  </read_first>
  <action>
    Overwrite the body of `taskfiles/claude.yml`. Preserve `version: '3'`, the `includes: _: ./helpers.yml` line, and update the file-level comment header to describe the new shape (Phase-7 role, gating, sentinel pattern, LINT compliance notes per LINT-02 / LINT-03a, aggregator markers).

    Declare a file-level vars: block at file top with EXACTLY three entries (W-01: do NOT add MARKETPLACES_JSON or PLUGINS_JSON here -- they go inside the `marketplace:` task's task-scoped vars block; see step 2 below):
    - CLAUDE_MARKETPLACES: multi-line literal containing `ecc https://github.com/affaan-m/everything-claude-code.git` (port verbatim)
    - CLAUDE_PLUGINS: multi-line literal containing `ecc@ecc` (port verbatim)
    - GSD_SENTINEL: `{{.XDG_STATE_HOME}}/dotfiles/gsd-installed` (D-11 path)

    Define exactly seven tasks (names below are exact):

    1. install — public aggregator. Line directly above task name carries `# lint-allow: cmds-without-status`. desc: "Install Claude Code marketplaces, plugins, and get-shit-done-cc". deps: [manifest:resolve]. cmds body wraps `{{if index .MANIFEST.features "claude-marketplace"}}` / `{{end}}` go-template gate (kebab-case index, NOT dot access; CLAUDE.md). Inside the gate, four cmds entries in order: `task: ensure-cli`, `task: marketplace`, `task: gsd`, then a `{{.DOTFILES_MESSAGES}}` + `success "Claude plugins installed"` line. NO status: block (aggregator).

    2. marketplace — internal:true. desc: "Register marketplaces + install plugins". deps: [ensure-cli, manifest:resolve].

       W-01 (sh-var scoping): inside this task body (NOT at file scope) declare a task-scoped `vars:` block containing the two `sh:`-prefixed dynamic vars:
         - MARKETPLACES_JSON: `sh: claude plugin marketplace list --json 2>/dev/null || echo '[]'`
         - PLUGINS_JSON:      `sh: claude plugin list --json 2>/dev/null || echo '[]'`
       Because they are task-scoped, go-task evaluates them only when `task claude:marketplace` is actually invoked -- they will NOT fire during `task --list`, `task lint`, `task test`, or any other task.

       cmds: two `for: { var: CLAUDE_MARKETPLACES, split: "\n", as: ENTRY }` and `for: { var: CLAUDE_PLUGINS, split: "\n", as: ID }` loops — first registers missing marketplaces (port from existing marketplaces-add body), second installs missing plugins (port from existing plugins-install body). Output via `{{.DOTFILES_MESSAGES}}` `check` / `info`. The task-scoped MARKETPLACES_JSON / PLUGINS_JSON vars are available inside the cmds: body via `{{.MARKETPLACES_JSON}}` / `{{.PLUGINS_JSON}}` interpolation.

       W-02 (status block: no `$VAR` anywhere): the status: block enumerates each entry at go-template render time, producing one status line per marketplace and one per plugin. Use `range` over the file-level `{{.CLAUDE_MARKETPLACES}}` and `{{.CLAUDE_PLUGINS}}` vars to emit literal status lines whose names are go-template-interpolated (not shell-var-interpolated). The status block MUST NOT use `for: ... do ... $name ... done` shell loops -- those would inject `$name` into the rendered YAML and trip LINT-02. Instead, each rendered status line is independent and shell-var-free.

       Concrete shape for the status: block:
         status:
           # one line per CLAUDE_MARKETPLACES entry, rendered by go-template `range`
           - jq -e '.[] | select(.name == "ecc")' <(claude plugin marketplace list --json 2>/dev/null || echo '[]') >/dev/null
           # one line per CLAUDE_PLUGINS entry, rendered by go-template `range`
           - jq -e '.[] | select(.name == "ecc@ecc")' <(claude plugin list --json 2>/dev/null || echo '[]') >/dev/null

       Each line is fully-interpolated at template-render time: the marketplace/plugin name is a literal string baked in by `range`, NOT a `$name` shell variable. No `$VAR` token appears anywhere in the status: block. The `<(...)` process-substitution re-runs the `claude plugin ... --json` query AT status-evaluation time (not at parse time) -- this is the same effective behavior as MARKETPLACES_JSON / PLUGINS_JSON would have given inside the task body, except it lives in the status block where task-scoped vars are not in scope.

       Both lines must succeed for the task to no-op (D-12). NO `$VAR` shell-var leakage in status block (parser-level guarantee: zero `$[A-Z]` occurrences between `status:` and the next task name).

    3. gsd — internal:true. desc: "Install get-shit-done-cc (presence-sentinel idempotent)". deps: [ensure-cli]. cmds: single multi-line block: source `{{.DOTFILES_MESSAGES}}`, `info "installing get-shit-done-cc (global)"`, run `npx -y get-shit-done-cc@latest --claude --global`, `mkdir -p "$(dirname {{.GSD_SENTINEL}})"`, `touch {{.GSD_SENTINEL}}`, `success "GSD installed; sentinel touched at {{.GSD_SENTINEL}}"`. status: block — single line `test -f {{.GSD_SENTINEL}}`. Template-var only.

    4. update — public aggregator. Line above task name carries `# lint-allow: cmds-without-status`. desc: "Refresh marketplaces + plugins + GSD (explicit; not in `task install`)". deps: [ensure-cli, manifest:resolve]. Three cmds in order:
       (a) `claude plugin marketplace update`
       (b) `for: { var: CLAUDE_PLUGINS, split: "\n", as: ID }` loop — each iteration: `[[ -z "{{.ID}}" ]] && exit 0` then `claude plugin update "{{.ID}}"`
       (c) `rm -f {{.GSD_SENTINEL}}` then `task: gsd`
       Per D-10 + D-14: no task install participation, no brew updates. NO status: block (explicit refresh always runs).

    5. status — public diagnostic. Line above task name carries `# lint-allow: cmds-without-status`. desc: "Show installed marketplaces + plugins (diagnostic)". cmds: `claude plugin marketplace list`, `echo`, `claude plugin list`. NO status: block.

    6. validate — public diagnostic. Line above task name carries `# lint-allow: cmds-without-status`. desc: "Validate Claude install state (composed into root task validate in P8)". deps: [manifest:resolve]. Port the existing validate body shape forward: claude CLI present check, jq present check, per-marketplace check/cross loop, per-plugin check/cross loop. Since file-level MARKETPLACES_JSON / PLUGINS_JSON no longer exist (W-01), invoke `claude plugin marketplace list --json` and `claude plugin list --json` inline inside the cmds: block where they are needed (cmds: bodies CAN use `$()` shell substitution -- the LINT-02 constraint applies only to `status:` blocks). ADD a final block: `test -f {{.GSD_SENTINEL}} && check "GSD installed (sentinel present)" || cross "GSD missing -- run task claude:install"`. Use `{{.DOTFILES_MESSAGES}}` for output. NO status: block.

    7. ensure-cli — internal:true. desc: "Hard-fail if claude or jq missing". cmds: single block per D-13: source messages, `missing=0`, test `command -v claude` (cross "claude CLI not found -- run 'task packages:install' first" + `missing=1`), test `command -v jq` (cross "jq not found -- run 'task packages:install' first" + `missing=1`), final `[[ $missing -eq 0 ]]`. NO status: block (gate, not idempotent).

    Drop these task names from the current file (their bodies are absorbed by the new shape): marketplaces-add, marketplaces-update, plugins-install, plugins-update, gsd-install.

    Drop all `{{.BREW_SHELLENV}}` references — v2 doesn't propagate that var into included taskfiles. `claude` and `jq` resolve via `command -v` on the inherited PATH from `brew shellenv` at shell startup.

    Conformance checklist (CLAUDE.md / .claude/CLAUDE.md):
    - Status blocks use `{{.X}}` template vars only — never `$VAR` (LINT-02; the macos:shell:145 bug class). Marketplace status block uses go-template `range` over CLAUDE_MARKETPLACES / CLAUDE_PLUGINS to render literal entry names; zero `$VAR` occurrences in the rendered status YAML.
    - File-level vars: block contains exactly three entries (CLAUDE_MARKETPLACES, CLAUDE_PLUGINS, GSD_SENTINEL); MARKETPLACES_JSON and PLUGINS_JSON are task-scoped to `marketplace:` ONLY.
    - Aggregator tasks (install, update, status, validate) carry `# lint-allow: cmds-without-status` on line directly above task name
    - Kebab-case feature key `claude-marketplace` uses `index .MANIFEST.features "claude-marketplace"` (NOT dot access)
    - No emojis
    - No AI attribution
    - File-level comment header explains role, gating, sentinel pattern, LINT compliance
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && task --list-all --json -t taskfiles/claude.yml >/dev/null 2>&1 && for t in install marketplace gsd update status validate ensure-cli; do grep -qE "^[[:space:]]+${t}:" taskfiles/claude.yml || { echo "missing task: $t" >&2; exit 1; }; done && grep -qF 'GSD_SENTINEL:' taskfiles/claude.yml && grep -qF '{{.XDG_STATE_HOME}}/dotfiles/gsd-installed' taskfiles/claude.yml && grep -qF 'index .MANIFEST.features "claude-marketplace"' taskfiles/claude.yml && test "$(grep -c '# lint-allow: cmds-without-status' taskfiles/claude.yml)" -ge 4 && test "$(awk '/^vars:/,/^[a-z]/' taskfiles/claude.yml | grep -cE '^[[:space:]]+(MARKETPLACES_JSON|PLUGINS_JSON):')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - All seven required tasks defined: `grep -cE '^[[:space:]]+(install|marketplace|gsd|update|status|validate|ensure-cli):' taskfiles/claude.yml` returns 7
    - GSD_SENTINEL var present with value `{{.XDG_STATE_HOME}}/dotfiles/gsd-installed`
    - Feature gate uses kebab-case index form: `grep -F 'index .MANIFEST.features "claude-marketplace"' taskfiles/claude.yml` matches
    - Aggregator marker count: `grep -c '# lint-allow: cmds-without-status' taskfiles/claude.yml` >= 4 (install, update, status, validate)
    - YAML parses: `task --list-all --json -t taskfiles/claude.yml` exits 0
    - `task lint` exits 0 against the new file (LINT-02 / LINT-03a / LINT-03b / LINT-04 — no `$VAR` in status, every cmds task has status: or aggregator marker, no bare `ln -s`)
    - `gsd` task status: block contains only `test -f {{.GSD_SENTINEL}}` (template-var only)
    - W-01 (sh-var task-scoping) self-check: MARKETPLACES_JSON and PLUGINS_JSON do NOT appear in the file-level `vars:` block. They appear ONLY inside the `marketplace:` task body's task-scoped `vars:` block. Concretely, `awk '/^vars:$/,/^[a-z]/' taskfiles/claude.yml | grep -cE '(MARKETPLACES_JSON|PLUGINS_JSON):'` returns 0. `awk '/^[[:space:]]+marketplace:[[:space:]]*$/,/^[[:space:]]+[a-z][a-zA-Z_-]*:[[:space:]]*$/' taskfiles/claude.yml | grep -cE '(MARKETPLACES_JSON|PLUGINS_JSON):'` returns at least 2.
    - W-02 (no `$VAR` in marketplace status block) self-check: extract the status: block of the `marketplace:` task and verify zero `$VAR` shell-var references. Concretely, `awk '/^[[:space:]]+marketplace:[[:space:]]*$/,/^[[:space:]]+[a-z][a-zA-Z_-]*:[[:space:]]*$/' taskfiles/claude.yml | awk '/^[[:space:]]+status:/,/^[[:space:]]+[a-z]/' | grep -E '\$[A-Z_][A-Z0-9_]*' | head -1 | wc -l` returns 0 (zero `$UPPERCASE` matches in the status block). Equivalent simpler check: `grep -nE '\$[A-Z]' taskfiles/claude.yml | grep -i status` returns no results.
    - `marketplace` task has a status: block with at least 2 entries, all using literal go-template-rendered marketplace/plugin names (no `$name`, no `$ENTRY`, no `$ID`)
    - `validate` task includes a GSD sentinel check via `test -f {{.GSD_SENTINEL}}`
    - `Taskfile.yml` install: cmds: still includes `task: claude:install` (unchanged from current)
    - `taskfiles/claude.yml` does NOT reference `{{.BREW_SHELLENV}}` anywhere
  </acceptance_criteria>
  <done>
    `taskfiles/claude.yml` defines the seven Phase-7 tasks with correct feature gating, sentinel idempotency, two-condition marketplace status, and v2-conformant lint compliance. Dynamic `sh:` vars are task-scoped to the `marketplace:` task only (W-01), and the marketplace status block uses go-template `range` to emit literal entry names with zero `$VAR` shell-var references (W-02). The internal task bodies port forward from the v2 implementation; only structure and idempotency contract change.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip root `Taskfile.yml` include from claude-stub to claude.yml</name>
  <files>Taskfile.yml</files>
  <read_first>
    - Taskfile.yml (current line 108: `  claude:   ./taskfiles/claude-stub.yml     # P7 wires real bodies`)
    - .planning/phases/07-claude-tool-configs-smoke-tests/07-CONTEXT.md (CF-01 + install-pipeline section: `task install` call graph already runs `task: claude:install` between `packages:install` and `macos:defaults` — no re-ordering needed)
    - taskfiles/claude.yml (must already exist with real bodies from Task 1 before this flip)
  </read_first>
  <action>
    Single-line edit to `Taskfile.yml`. In the `includes:` block (around line 108), change the `claude:` entry from `./taskfiles/claude-stub.yml` to `./taskfiles/claude.yml`. Update the trailing comment to "P7 real bodies" (no longer "wires real bodies" future-tense). Preserve indentation exactly.

    Do NOT touch any other line:
    - `task install` cmds at line ~142 already calls `task: claude:install` — no edit
    - The other includes (manifest, lint, links, perf, identity, packages, macos) are unchanged
    - The vars: block is unchanged
    - preconditions / cutover-gate is unchanged

    Do NOT delete `taskfiles/claude-stub.yml` — the stub stays on disk as a parallel-rewrite artifact (CF-11). Phase 8 owns cleanup.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && grep -qE '^[[:space:]]+claude:[[:space:]]+\./taskfiles/claude\.yml' Taskfile.yml && ! grep -qE '^[[:space:]]+claude:[[:space:]]+\./taskfiles/claude-stub\.yml' Taskfile.yml && task --list-all --json >/dev/null 2>&1 && task --list 2>&1 | grep -q 'claude:install'</automated>
  </verify>
  <acceptance_criteria>
    - `Taskfile.yml` `claude:` include line points at `./taskfiles/claude.yml`
    - No line in `Taskfile.yml` references `claude-stub.yml`
    - `taskfiles/claude-stub.yml` STILL EXISTS on disk (not deleted)
    - `task --list-all --json` exits 0 (root taskfile parses with the new include)
    - `task --list 2>&1 | grep claude:install` finds the task
    - `task --list 2>&1 | grep -c 'claude:'` returns at least 7 (one per Phase-7 task)
    - `task lint` exits 0 against the modified `Taskfile.yml`
  </acceptance_criteria>
  <done>
    Root `Taskfile.yml` loads `taskfiles/claude.yml` (real) as the `claude:` include. The stub file remains on disk; Phase 8 will remove it during cutover cleanup.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update `claude/README.md` to document the post-D-02 repo-ownership split</name>
  <files>claude/README.md</files>
  <read_first>
    - claude/README.md (current body — read entire file before editing; preserve sections that still apply)
    - .planning/phases/07-claude-tool-configs-smoke-tests/07-CONTEXT.md (D-01 ownership split table; D-02 deletion rationale; CF-13 sibling-README pattern)
    - .gitignore (verify the four gsd-* patterns landed in Plan 01 Task 2 — README explanation must match)
    - claude/hooks/hooks.json (canonical wiring of zsh hooks — README references this)
  </read_first>
  <action>
    Update `claude/README.md` to describe the v2 post-D-02 ownership boundaries. The README should clearly explain:

    1. Repo-owned content (hand-authored):
       - `claude/CLAUDE.md` and `claude/settings.json` (files)
       - `claude/hooks/{post-compact,agent-transparency,secret-scan,block-destructive,no-ai-comments,no-emojis,notify}.zsh` (seven zsh hooks) + `lib.zsh` + `hooks.json`
       - Any hand-authored agents / commands / skills (no `gsd-` prefix)

    2. GSD-managed content (runtime-deployed, not committed):
       - `claude/agents/gsd-*.md` (written by `npx get-shit-done-cc`)
       - `claude/commands/gsd-*` (same)
       - `claude/skills/gsd-*/` (same)
       - `claude/hooks/gsd-*` (JS / sh / etc. — runtime artifacts)

    3. Symlink shape (per D-01): mixed top-level — CLAUDE.md + settings.json as file symlinks, agents/commands/skills as directory symlinks (GSD writes through), hooks/ as per-file symlinks (because GSD writes peer files into the same dir at runtime).

    4. Task entry points: `task claude:install` (idempotent), `task claude:update` (explicit refresh; not in `task install` call graph), `task claude:status` (diagnostic), `task claude:validate` (state check).

    5. Sentinel: `task claude:gsd` is gated on a presence sentinel at `$XDG_STATE_HOME/dotfiles/gsd-installed`. Delete the sentinel and re-run `task claude:gsd` (or run `task claude:update`) to refresh GSD.

    6. Feature gate: the whole `claude:install` aggregator is gated on `features.claude-marketplace`; servers with that flag false skip Claude install entirely.

    Conventions per CLAUDE.md / .claude/CLAUDE.md: no AI attribution, no emojis (markdown included), use `#`/`##` section structure, prefer tables for ownership maps, link out to `taskfiles/claude.yml` and `manifests/defaults.toml` rather than duplicating content.

    Length target: ~80-150 lines. Document is sibling-README pattern (CF-13): purpose + key files + how-to-add. Section "How to add a hook" should explain: drop a `<name>.zsh` file in `claude/hooks/`, add the corresponding entry to `claude/hooks/hooks.json`, add a per-file symlink entry in Plan 06's `taskfiles/links.yml` `claude:` sub-task, and (if it's a gate-style hook) consider adding a smoke-test fixture to `install/test-hooks.zsh` (Plan 04).
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && test -f claude/README.md && grep -qiE 'GSD-managed|GSD installer|runtime-managed' claude/README.md && grep -qF 'gsd-installed' claude/README.md && grep -qF 'claude-marketplace' claude/README.md && grep -qF 'task claude:install' claude/README.md && grep -qF 'task claude:update' claude/README.md && ! grep -nE '[\x{1F300}-\x{1FAFF}]|[\x{2600}-\x{27BF}]' claude/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `claude/README.md` exists and is non-empty (≥ 50 lines)
    - Contains an ownership map (table or bullet list) distinguishing repo-owned vs GSD-managed paths
    - References the four `.gitignore` patterns (or links to `.gitignore`) so the doc explains why the patterns exist
    - References sentinel path `$XDG_STATE_HOME/dotfiles/gsd-installed`
    - References the `features.claude-marketplace` gate
    - Documents the four entry-point tasks: `task claude:install`, `task claude:update`, `task claude:status`, `task claude:validate`
    - Documents the symlink shape per D-01 (2 file + 3 dir + 8 per-file under hooks)
    - No emojis anywhere
    - No AI attribution anywhere
    - `task lint` exits 0 (no LINT regression — README is not a .zsh or .yml file, but check the lint suite doesn't choke on the file)
  </acceptance_criteria>
  <done>
    `claude/README.md` documents the v2 repo-ownership split, sentinel pattern, feature gating, and task entry points. Future maintainers reading the README understand the boundary between hand-authored content and GSD-installer-managed runtime artifacts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network: `claude plugin marketplace add` | Fetches third-party marketplace repos; trust delegated to claude CLI |
| Network: `npx get-shit-done-cc@latest` | Downloads + executes unsigned npm code at install time |
| Sentinel filesystem write | `touch {{.GSD_SENTINEL}}` writes user-state; corruption could mask install failures |
| Status block evaluation | Status blocks run at task-graph build time; bad jq expressions could mask real drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Spoofing | A malicious marketplace at the configured URL could ship a backdoored plugin | accept | Marketplace source URL is committed to the repo (`taskfiles/claude.yml` vars); changes require a tracked commit + code review. Single configured marketplace today (ECC). No trust path improvement available without claude CLI features. |
| T-07-11 | Tampering | `npx get-shit-done-cc@latest` executes unsigned network code | accept | User-chosen tool installed at user request; matches v1 behavior. Severity: medium (network-installed CLI; user already trusts the tool). No supply-chain pin per D-09 — user explicitly rejected version pinning. Mitigation: presence-sentinel ensures `npx` runs only on explicit `task claude:update` after initial install, reducing window of accidental update-on-every-install (the gsd-install bug class this plan closes). |
| T-07-12 | Tampering | Sentinel touch happens BEFORE `npx` completes successfully → false-pass on next run | mitigate | Place `touch` AFTER `npx` in the same `cmds:` block; bash strict mode (set -euo pipefail from root Taskfile) propagates `npx` exit code, aborting before `touch`. Verify via failure-injection test: `npx` failure leaves sentinel absent. |
| T-07-13 | Information disclosure | `claude plugin list --json` output may contain plugin metadata (URLs, versions) that surfaces in diagnostic output | accept | Output goes to stderr in diagnostic tasks; user-controlled local CLI; not a network boundary. |
| T-07-14 | Denial of service | `marketplace` status: block hangs on slow `claude plugin marketplace list --json` shelling | mitigate | `2>/dev/null || echo '[]'` fallback in the `<(...)` process-substitution prevents hang-on-stderr; jq parses the fallback `[]` correctly; status returns 1 (task runs). The process-substitution moved out of file-scope `sh:`-vars (W-01) into the status block itself, where it fires only when `task claude:marketplace`'s status is being evaluated. |
| T-07-15 | Elevation of privilege | `ensure-cli` failure path could be skipped if `set -e` is disabled elsewhere | mitigate | `ensure-cli` uses explicit `[[ $missing -eq 0 ]]` as the final expression — exits non-zero on missing CLI regardless of shell flags. Task is `internal: true` and called as `deps:`; failure aborts the calling task. |
| T-07-16 | Tampering | LINT-02 violation regression in status: block (someone reintroduces `$VAR`) | mitigate | `task lint:taskfile` enforces LINT-02 statically; acceptance criteria runs full `task lint` against the new file, plus an explicit `grep -E '\$[A-Z]' taskfiles/claude.yml | grep -i status` self-check that must return zero results (W-02). |
| T-07-17 | Performance / DoS | File-level `sh:`-prefixed vars evaluated on EVERY task invocation (LINT-08 5s budget violation) | mitigate | MARKETPLACES_JSON / PLUGINS_JSON are task-scoped inside `marketplace:` (W-01), so they fire only when `task claude:marketplace` is the invoked task. `task --list`, `task lint`, `task test`, and `task --list-all --json` no longer spawn `claude plugin ...` subprocesses. |
</threat_model>

<verification>
- `task --list-all --json -t taskfiles/claude.yml` exits 0 (YAML parses)
- `task --list-all --json` (root) exits 0 after Taskfile.yml flip
- All seven task names defined and reachable: `task --list | grep -cE 'claude:(install|marketplace|gsd|update|status|validate|ensure-cli)'` returns 7
- `task lint` exits 0 against modified `taskfiles/claude.yml` and `Taskfile.yml`
- `Taskfile.yml` `claude:` include points to real file; no `claude-stub.yml` reference remains in root taskfile
- Idempotency probe: on a machine where the sentinel file exists, `task claude:gsd` produces no `npx` invocation (status: block short-circuits)
- Feature-gate probe: on a machine with `claude-marketplace = false`, `task claude:install` runs the aggregator body to completion without invoking marketplace/gsd subtasks (the `{{if}}` gate skips them)
- W-01 self-check: file-level `vars:` block does NOT contain MARKETPLACES_JSON or PLUGINS_JSON
- W-02 self-check: `grep -nE '\$[A-Z]' taskfiles/claude.yml | grep -i status` returns no results (zero `$VAR` shell-var references in any status: block)
- `claude/README.md` includes ownership map, sentinel path reference, feature-gate reference, four task entry points
</verification>

<success_criteria>
- `taskfiles/claude.yml` exposes the seven Phase-7 tasks with v2-conformant lint compliance
- Root `Taskfile.yml` loads the real `claude.yml` as its `claude:` include
- `task claude:gsd` is sentinel-idempotent (runs `npx` only on absence of `{{.GSD_SENTINEL}}`)
- `task claude:marketplace` is two-condition-status idempotent (no-op when marketplaces + plugins are converged), with task-scoped `sh:` vars (W-01) and `$VAR`-free status block (W-02)
- `task claude:install` aggregator is feature-gated on `claude-marketplace`
- `task claude:update` exists as an explicit, separate-from-install refresh path
- `claude/README.md` documents the post-D-02 ownership split
</success_criteria>

<output>
After completion, create `.planning/phases/07-claude-tool-configs-smoke-tests/07-03-SUMMARY.md`.
</output>

## Source Coverage

| Source | Item | Plan Coverage |
|--------|------|---------------|
| REQ | CLDE-01 (claude install via taskfiles/claude.yml) | Task 1 |
| REQ | CLDE-03 (presence-sentinel status check for GSD) | Task 1 (`gsd` task) |
| REQ | CLDE-04 (claude plugin list status check for marketplace) | Task 1 (`marketplace` task two-condition status) |
| CONTEXT D-09 | No GSD version pin; touchfile sentinel only | Task 1 (`gsd`) |
| CONTEXT D-10 | `task claude:update` is the explicit refresh path | Task 1 (`update`) |
| CONTEXT D-11 | GSD sentinel at `$XDG_STATE_HOME/dotfiles/gsd-installed` | Task 1 (vars block) |
| CONTEXT D-12 | Marketplace two-condition status (marketplaces + plugins) | Task 1 (`marketplace` status) |
| CONTEXT D-13 | Hard-fail when claude CLI missing | Task 1 (`ensure-cli`) |
| CONTEXT D-14 | `task claude:update` claude-scoped only | Task 1 (`update`) |
| CONTEXT CF-01 | Manifest is runtime source of truth | Task 1 (deps:[manifest:resolve] on all tasks reading {{.MANIFEST}}) |
| CONTEXT CF-02 | Kebab-case keys use `index` form | Task 1 (claude-marketplace gate) |
| CONTEXT CF-03 | Status blocks use `{{.X}}` template vars only | Task 1 (status block compliance; W-02 hardening) |
| CONTEXT CF-04 | Aggregator marker `# lint-allow: cmds-without-status` | Task 1 (install/update/status/validate) |
| CONTEXT CF-08 | `deps: [manifest:resolve]` on every task reading resolved.json | Task 1 (install/marketplace/update/validate) |
| CONTEXT CF-09 | `install/messages.zsh` via `{{.DOTFILES_MESSAGES}}` | Task 1 (all output) |
| CONTEXT CF-13 | Sibling-README pattern | Task 3 (claude/README.md update) |

---
id: 07-04
phase: 07-claude-tool-configs-smoke-tests
plan: 4
type: execute
wave: 3
depends_on: [07-02, 07-03]
files_modified:
  - install/test-hooks.zsh
  - taskfiles/test.yml
  - Taskfile.yml
autonomous: true
requirements: [TEST-01, TEST-02, CLDE-02]
requirements_addressed: [TEST-01, TEST-02, CLDE-02]
must_haves:
  truths:
    - "`install/test-hooks.zsh` exists, is executable, starts with `#!/usr/bin/env zsh` + `set -euo pipefail`"
    - "`install/test-hooks.zsh` defines four hook test functions: `test_secret_scan`, `test_no_emojis`, `test_no_ai_comments`, `test_agent_transparency`"
    - "Each hook test function asserts one pass scenario AND one block/warn scenario (D-16 coverage)"
    - "Per-hook expected exit codes: secret-scan (pass=0, block=2); no-emojis (pass=0, warn=0+stderr); no-ai-comments (pass=0, warn=0+stderr); agent-transparency (log-only=0)"
    - "`task test:hooks` invokes the runner via `taskfiles/test.yml` and prints per-fixture check/cross output"
    - "`task test` aggregator runs `task: manifest:test` followed by `task: test:hooks`"
    - "Root `Taskfile.yml` `includes:` block includes `test: ./taskfiles/test.yml`"
    - "Root `Taskfile.yml` defines a `test` task that delegates to `manifest:test` + `test:hooks` (aggregator with `# lint-allow: cmds-without-status` marker)"
    - "`task test:hooks` exits 0 on a converged repo where all four hooks behave correctly; exits non-zero if any fixture's expectation fails"
    - "Smoke test exercises the rewritten `agent-transparency.zsh` from Plan 02 — regression-checks the script-scope `local` fix"
  artifacts:
    - path: "install/test-hooks.zsh"
      provides: "Runner with inline JSON fixtures for the four named hooks"
      min_lines: 100
      contains: "test_secret_scan"
    - path: "taskfiles/test.yml"
      provides: "Phase-7 smoke-test task suite"
      exports: ["default", "hooks"]
    - path: "Taskfile.yml"
      provides: "Root `test:` include + root `test` aggregator task"
      contains: "test:    ./taskfiles/test.yml"
  key_links:
    - from: "task test"
      to: "task manifest:test (P1) + task test:hooks (P7)"
      via: "root Taskfile.yml aggregator cmds"
      pattern: 'task: (manifest:test|test:hooks)'
    - from: "task test:hooks"
      to: "install/test-hooks.zsh"
      via: "zsh invocation"
      pattern: 'zsh.*test-hooks\.zsh'
    - from: "install/test-hooks.zsh"
      to: "claude/hooks/{secret-scan,no-emojis,no-ai-comments,agent-transparency}.zsh"
      via: "JSON stdin pipe + exit-code/stderr assertion"
      pattern: 'echo.*\|.*zsh.*claude/hooks/'
---

<objective>
Land the Phase-7 hook smoke-test surface: a single zsh runner (`install/test-hooks.zsh`) with inline JSON fixtures for the four named hooks, a `taskfiles/test.yml` exposing `test:hooks` + an internal `default` aggregator, and a root `Taskfile.yml` that wires `test:` as an include and exposes a `test` aggregator combining `manifest:test` (P1) with `test:hooks` (P7).

Purpose: TEST-01 closes the runtime regression class that `shellcheck` cannot catch (logic bugs, regex drift, `set -euo pipefail` side-effects, JSON parse changes, the rewritten `agent-transparency.zsh` not actually logging the expected line). TEST-02 makes `task test` the single command that runs all Tier-3 smoke tests (manifest deep-merge fixtures + hook fixtures), positioned alongside `task lint` as the CI gate. This plan also serves as the regression check for Plan 02's `agent-transparency.zsh` rewrite — if the function-wrap broke the resolution branches, the `test_agent_transparency` fixture catches it.

Output: A new executable runner under `install/`, a new included taskfile, and a root-level `test` aggregator task. After this plan, `task test` runs both P1 manifest fixtures and P7 hook fixtures end-to-end.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-claude-tool-configs-smoke-tests/07-CONTEXT.md
@.planning/phases/07-claude-tool-configs-smoke-tests/07-02-SUMMARY.md
@.planning/phases/07-claude-tool-configs-smoke-tests/07-03-SUMMARY.md
@CLAUDE.md
@.claude/CLAUDE.md
@install/messages.zsh
@install/resolver.zsh
@install/compose-brewfile.zsh
@install/cutover-gate.zsh
@taskfiles/manifest.yml
@taskfiles/lint.yml
@Taskfile.yml
@claude/hooks/secret-scan.zsh
@claude/hooks/no-emojis.zsh
@claude/hooks/no-ai-comments.zsh
@claude/hooks/agent-transparency.zsh
@claude/hooks/lib.zsh
@claude/hooks/hooks.json

<interfaces>
Hook stdin contract (Claude Code's hook payload schema; verified from hooks.json + existing hook bodies):

  {
    "tool_name": "Write" | "Edit" | "Bash" | "Agent" | ...,
    "tool_input": { "file_path": "<path>", "content": "<text>", "subagent_type": "<type>", "description": "<task>", ... },
    "cwd": "<absolute path>"
  }

Per-hook expected exit codes / stderr patterns (CONTEXT.md "Hook fixture exit codes"):
  - secret-scan.zsh: pass = exit 0; block = exit 2 (PreToolUse:Write|Edit)
  - no-emojis.zsh: warn-only — exit 0 either way; flagged input writes "emoji"-class warning to stderr
  - no-ai-comments.zsh: warn-only — exit 0 either way; flagged input writes "AI attribution"-class warning to stderr
  - agent-transparency.zsh: log-only — exit 0 either way; logs `Agent delegated -> type: ..., task: ...` to stdout/stderr

Existing install/ runners (shape templates):
  - install/resolver.zsh — set -euo pipefail; sources messages.zsh; single-purpose script invoked by taskfiles/manifest.yml
  - install/compose-brewfile.zsh — same pattern; invoked by taskfiles/packages.yml
  - install/cutover-gate.zsh — exports a function called by root Taskfile.yml preconditions:

DOTFILEDIR is exported by root Taskfile.yml; the runner uses `${DOTFILEDIR}` to locate `claude/hooks/` and `install/messages.zsh`.

Root Taskfile.yml current includes: block:
  manifest:  (with explicit DOTFILEDIR + XDG_STATE_HOME forwarding)
  lint:      ./taskfiles/lint.yml
  links:     ./taskfiles/links.yml
  perf:      ./taskfiles/shell.yml
  identity:  (explicit forwarding)
  packages:  (explicit forwarding)
  claude:    ./taskfiles/claude.yml   (post-Plan-03)
  macos:     ./taskfiles/macos.yml

Need to ADD: test: ./taskfiles/test.yml (after claude, before macos — or anywhere; order is parse-order-insensitive for includes).

Root Taskfile.yml has no current `test` task; Plan 04 adds one as a root-level aggregator that delegates to `task: manifest:test` + `task: test:hooks`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `install/test-hooks.zsh` runner with inline JSON fixtures</name>
  <files>install/test-hooks.zsh</files>
  <read_first>
    - install/messages.zsh (read `check`, `cross`, `info`, `warn`, `success` function signatures + color codes used; the runner uses these for per-fixture output)
    - install/resolver.zsh (shape template — file-level comment block, set -euo pipefail position, exit-code semantics)
    - claude/hooks/secret-scan.zsh (read full body — extract the regex / detection logic to design pass/block fixtures that actually trigger the gate)
    - claude/hooks/no-emojis.zsh (read full body — extract the emoji-detection regex and the stderr warning text; fixture must match the warning pattern)
    - claude/hooks/no-ai-comments.zsh (read full body — extract the AI-attribution patterns: "Co-Authored-By", "generated by", etc.; fixture must include one of them)
    - claude/hooks/agent-transparency.zsh (post-Plan-02 rewrite — the runner pipes a JSON payload with `tool_input.subagent_type` + `tool_input.description` and asserts the output line begins with `Agent delegated ->`)
    - claude/hooks/lib.zsh (read `hook::read_stdin` and `hook::extract` functions — fixtures must match the JSON shape these helpers expect)
    - claude/hooks/hooks.json (canonical event/matcher mapping — informs which `tool_name` to use per fixture, e.g. Write/Edit for secret-scan)
    - .planning/phases/07-claude-tool-configs-smoke-tests/07-CONTEXT.md (`<specifics>` section: `install/test-hooks.zsh` skeleton + D-15..D-18 design + "Hook fixture exit codes" subsection in Claude's Discretion)
    - CLAUDE.md (set -euo pipefail required on every executable .zsh; no emojis in markdown OR code; no AI attribution)
  </read_first>
  <action>
    Create `install/test-hooks.zsh` as a new executable zsh script. It must conform to v2 conventions: `set -euo pipefail`, file-level comment block, source `${DOTFILEDIR}/install/messages.zsh` for `check`/`cross` output, single-purpose script invoked by `task test:hooks` (matches the `install/resolver.zsh` / `install/compose-brewfile.zsh` pattern).

    Required structure:
    1. Shebang: `#!/usr/bin/env zsh`
    2. `set -euo pipefail`
    3. File-level comment block (~10 lines): purpose (Tier-3 smoke tests for the four named Claude hooks), invocation (via `task test:hooks`), scope (CLDE-02 named hooks only; D-17 excludes block-destructive/notify/post-compact), JSON schema reference (Claude Code's hook stdin), exit semantics (0 if all fixtures pass, non-zero if any fail). NO emojis. NO AI attribution.
    4. `source "${DOTFILEDIR}/install/messages.zsh"` — fail-loud if DOTFILEDIR not set
    5. `HOOK_DIR="${DOTFILEDIR}/claude/hooks"` and `failed=0` at script scope (NOT `local` — that's the script-scope `local` bug class)
    6. Four test functions defined in order: `test_secret_scan`, `test_no_emojis`, `test_no_ai_comments`, `test_agent_transparency`. Each function:
       - Declares its locals INSIDE the function body (this is correct usage of `local`)
       - Builds the pass-scenario JSON via heredoc / inline string
       - Pipes via `echo "$pass_input" | zsh "${HOOK_DIR}/<hook>.zsh"` capturing exit code and stderr
       - Asserts the expected exit code and (for warn-only hooks) the expected stderr regex
       - Calls `check "<hook>.pass"` on success, `cross "<hook>.pass: <reason>"` and increments `failed=1` on failure
       - Repeats for the block/warn scenario with the appropriate flagged input
    7. After all four function definitions, call them in order: `test_secret_scan`, `test_no_emojis`, `test_no_ai_comments`, `test_agent_transparency`
    8. Final line: `exit $failed`

    Per-hook fixture specs (D-16 — one pass + one block per hook):

    test_secret_scan:
      Pass: `{"tool_name":"Write","tool_input":{"file_path":"foo.txt","content":"hello world"}}` → expect exit 0
      Block: `{"tool_name":"Write","tool_input":{"file_path":"foo.txt","content":"export API_KEY=sk-abc123def456ghi789jkl012mno345"}}` → expect exit 2
      Notes: read the actual regex used by secret-scan.zsh; pick a `content` value that matches it. DO NOT use a real-looking secret format that triggers other tooling (no AWS key prefix, no GitHub token prefix). The example above is a synthetic value that matches a generic API-key heuristic. If secret-scan.zsh uses a different gate (e.g. `sk-`, `xoxb-`, or `AKIA`), match that pattern instead — base the fixture on the regex actually in the hook body.

    test_no_emojis:
      Pass: `{"tool_name":"Write","tool_input":{"file_path":"foo.txt","content":"plain ASCII content"}}` → expect exit 0, no warn stderr
      Warn: `{"tool_name":"Write","tool_input":{"file_path":"foo.txt","content":"this content contains [SUBSTITUTE EMOJI-CODEPOINT HERE]"}}` → expect exit 0 AND stderr matches the warn pattern from no-emojis.zsh
      Notes: per project convention "no emojis in any file -- including markdown", the fixture canNOT inline a literal emoji character (the hook scanning THIS file would flag the source). Use a `printf '\U1F600'` or `$'ὠ0'` style construction to inject the test character at runtime so the source file stays clean. Document this constraint in a comment above the fixture.

    test_no_ai_comments:
      Pass: `{"tool_name":"Write","tool_input":{"file_path":"foo.txt","content":"plain comment"}}` → expect exit 0, no warn stderr
      Warn: `{"tool_name":"Write","tool_input":{"file_path":"foo.txt","content":"Co-Authored-By: Some AI <noreply@example.com>"}}` → expect exit 0 AND stderr matches the AI-attribution warn pattern (per `.gitignore` test heuristic: the hook flags `Co-Authored-By`, `Generated by`, etc. — extract exact patterns from the hook body)
      Notes: Be careful not to include real AI attribution that triggers OTHER tooling (commit-message hooks, CI scanners). The fixture string only flows through the smoke-test runner; it is not committed code.

    test_agent_transparency:
      Pass: `{"tool_input":{"subagent_type":"general-purpose","description":"test"},"cwd":"/tmp"}` → expect exit 0, stdout/stderr contains `Agent delegated ->`
      Block: agent-transparency is log-only — there is no "block" scenario. Per D-16 coverage = pass + block, but for log-only hooks, the second scenario is "plugin-scoped agent type" → `{"tool_input":{"subagent_type":"some-plugin:some-agent","description":"test"},"cwd":"/tmp"}` → expect exit 0 AND output line includes both `type: some-plugin:some-agent` and `task: test`. This exercises the plugin-scoped resolution branch from Plan 02's rewrite.

    Per CONTEXT.md: the runner uses `${0:A:h}` or `${DOTFILEDIR}` resolution; standardize on `${DOTFILEDIR}` since `taskfiles/test.yml` exports it.

    Per CLAUDE.md: file must pass `shellcheck install/test-hooks.zsh` (shellcheck-clean) and `zsh -n install/test-hooks.zsh`. Local vars MUST be inside functions only. NO emojis. NO AI attribution. NO `$VAR` outside function scope (script-scope-locals bug class).

    Make the file executable: `chmod +x install/test-hooks.zsh` (record this as part of the action; the verify step asserts `test -x install/test-hooks.zsh`).
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && test -x install/test-hooks.zsh && head -1 install/test-hooks.zsh | grep -q '^#!/usr/bin/env zsh' && grep -q '^set -euo pipefail$' install/test-hooks.zsh && for fn in test_secret_scan test_no_emojis test_no_ai_comments test_agent_transparency; do grep -qE "^${fn}\(\) \{" install/test-hooks.zsh || { echo "missing function: $fn" >&2; exit 1; }; done && grep -qF 'exit $failed' install/test-hooks.zsh && shellcheck install/test-hooks.zsh && zsh -n install/test-hooks.zsh && DOTFILEDIR=/Users/josh/Git/personal/dotfiles zsh install/test-hooks.zsh; status=$?; test $status -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `install/test-hooks.zsh`, is executable (`test -x install/test-hooks.zsh`)
    - First line: `#!/usr/bin/env zsh`
    - Contains `set -euo pipefail` on a top-level line (not inside a function)
    - Defines all four test functions: `test_secret_scan`, `test_no_emojis`, `test_no_ai_comments`, `test_agent_transparency`
    - Each function has a pass-scenario fixture AND a block/warn-scenario fixture (D-16 coverage)
    - Sources `${DOTFILEDIR}/install/messages.zsh`
    - Uses `check` and `cross` for per-fixture output
    - Final statement is `exit $failed` (or `exit "$failed"`)
    - `shellcheck install/test-hooks.zsh` exits 0
    - `zsh -n install/test-hooks.zsh` exits 0
    - On a real repo where all four hooks behave correctly, `DOTFILEDIR=$PWD zsh install/test-hooks.zsh` exits 0
    - No `local` declarations at script scope (only inside function bodies); `grep -E '^local ' install/test-hooks.zsh | wc -l` returns 0
    - No emojis anywhere in the file source (per `no-emojis.zsh` itself would flag the runner)
    - No AI attribution anywhere in the file source
    - `task lint` exits 0 against the new file (LINT-04: set -euo pipefail; LINT-07: zsh -n)
    - Each function asserts exit code via the captured `$?` and exits the test on mismatch with cross + `failed=1`; runner does not abort on first fixture failure (it tallies all four to give complete feedback)
  </acceptance_criteria>
  <done>
    `install/test-hooks.zsh` is a self-contained smoke-test runner with eight inline JSON fixtures (two per hook) covering the CLDE-02 named hooks. It exits 0 when every fixture matches its expected exit code + stderr pattern, non-zero otherwise.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create `taskfiles/test.yml` with `test:hooks` task</name>
  <files>taskfiles/test.yml</files>
  <read_first>
    - taskfiles/lint.yml (pattern reference: aggregator + LINT-allow markers; how taskfiles use `{{.DOTFILEDIR}}` and `{{.DOTFILES_MESSAGES}}`)
    - taskfiles/manifest.yml (read `manifest:test` task signature — the test aggregator will call it from root Taskfile.yml; verify the task name is `manifest:test`)
    - .planning/phases/07-claude-tool-configs-smoke-tests/07-CONTEXT.md (`<specifics>` section: `taskfiles/test.yml` skeleton)
    - install/test-hooks.zsh (created in Task 1 — the runner this task invokes)
    - install/messages.zsh (sourced via `{{.DOTFILES_MESSAGES}}` for the wrapper line announcing the test run)
    - CLAUDE.md (taskfile conventions: `{{.X}}` template vars only in status: blocks; aggregator marker for cmds-without-status; no emojis)
  </read_first>
  <action>
    Create `taskfiles/test.yml` as a new included taskfile. Header conventions match the other taskfiles (banner comment, version, vars block, tasks block).

    Required structure:
    1. `version: '3'` at top
    2. File-level comment block explaining role (Phase-7 smoke-test surface: TEST-01 hook fixtures + future TEST-02 aggregator components), invocation (`task test:hooks` and `task test` from root), scope (CLDE-02 named hooks via `install/test-hooks.zsh`).
    3. `vars:` block — declare `DOTFILEDIR` fallback (`sh: dirname "{{.TASKFILE_DIR}}"`) for standalone invocation (`task -t taskfiles/test.yml`); declare `DOTFILES_MESSAGES` fallback referencing `{{.DOTFILEDIR}}/install/messages.zsh`. When loaded via root `Taskfile.yml`, the root's vars override these.

    Define two tasks:

    1. `default` — aggregator. Line above task name carries `# lint-allow: cmds-without-status`. `desc:` "Run hook smoke tests". `cmds:` single entry: `task: hooks`. Note: per CONTEXT.md, `default` does NOT include `manifest:test` — that lives in the ROOT `task test` aggregator (Task 3 below) so the call is `task: manifest:test` + `task: test:hooks` from root. This `default` exists so `task test:default` (or just `task -t taskfiles/test.yml default`) does the local-to-test-yml thing.

    2. `hooks` — invokes the runner. `desc:` "Run Claude hook smoke tests (TEST-01)". `cmds:` a single block that sources `{{.DOTFILES_MESSAGES}}`, prints `info "running hook smoke tests"`, exports `DOTFILEDIR="{{.DOTFILEDIR}}"`, and runs `zsh "${DOTFILEDIR}/install/test-hooks.zsh"`. No `status:` block (tests are always-run diagnostic; carry `# lint-allow: cmds-without-status` marker per the lint-suite self-exemption pattern). Alternative shape per CONTEXT.md skeleton: include `status: [false]` to force re-run — both are valid; prefer the `# lint-allow:` marker form for consistency with `taskfiles/lint.yml`.

    Per CLAUDE.md: no `$VAR` in any status: block (this file has no status: blocks). No emojis. No AI attribution. File ends with a trailing newline.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && test -f taskfiles/test.yml && task --list-all --json -t taskfiles/test.yml >/dev/null 2>&1 && grep -qE '^[[:space:]]+hooks:' taskfiles/test.yml && grep -qE '^[[:space:]]+default:' taskfiles/test.yml && grep -qF 'test-hooks.zsh' taskfiles/test.yml && test "$(grep -c '# lint-allow: cmds-without-status' taskfiles/test.yml)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `taskfiles/test.yml`
    - YAML parses: `task --list-all --json -t taskfiles/test.yml` exits 0
    - Defines `default` and `hooks` tasks (both reachable via `task -t taskfiles/test.yml --list-all`)
    - `hooks` task body references `install/test-hooks.zsh`
    - Aggregator marker `# lint-allow: cmds-without-status` present on `default` (or `hooks` if it omits status:); at least one marker
    - vars: block declares fallback DOTFILEDIR and DOTFILES_MESSAGES (so standalone invocation works)
    - `task lint` exits 0 against the new file
    - No emojis, no AI attribution
    - `task -t taskfiles/test.yml hooks` runs `install/test-hooks.zsh` and exits with the runner's exit code
  </acceptance_criteria>
  <done>
    `taskfiles/test.yml` exposes `test:hooks` (invokes the runner) and `test:default` (runs `hooks`). The taskfile is parseable, lint-clean, and ready to be included from root `Taskfile.yml`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire `test:` include + root `test` aggregator into `Taskfile.yml`</name>
  <files>Taskfile.yml</files>
  <read_first>
    - Taskfile.yml (current includes: block, around lines 75-110; root tasks: block, around lines 113-154)
    - taskfiles/test.yml (created in Task 2 — verify the included taskfile path is correct relative to repo root)
    - taskfiles/manifest.yml (verify the `manifest:test` task name — the root `test` aggregator calls it via `task: manifest:test`)
    - .planning/phases/07-claude-tool-configs-smoke-tests/07-CONTEXT.md (`<specifics>` section: root Taskfile.yml `test:` include + root `test` aggregator skeleton)
    - taskfiles/lint.yml (root-level aggregator with `# lint-allow: cmds-without-status` marker — pattern reference)
  </read_first>
  <action>
    Two edits to `Taskfile.yml`:

    Edit 1: Add `test: ./taskfiles/test.yml` to the `includes:` block. Place the entry between `claude:` and `macos:` (alphabetical placement is fine but not required; the existing order is conceptual: manifest -> lint -> links -> perf -> identity -> packages -> claude -> macos). Preserve indentation. The line reads (with whatever whitespace matches the surrounding entries):
        `  test:     ./taskfiles/test.yml`

    Edit 2: Add a root-level `test` task to the `tasks:` block. Place it near `task install` and `task default` (other root-level aggregators). Required shape:
       - Line directly above task name: `  # lint-allow: cmds-without-status`
       - `desc:` "Run all smoke tests (manifest fixtures + hook fixtures)"
       - `cmds:` two entries in order: `- task: manifest:test`, then `- task: test:hooks`
       - NO `status:` block

    Do NOT touch:
       - The vars: block
       - The `install` task body (no changes — `task install` and `task test` are independent entry points)
       - Any other include line
       - The preconditions / cutover-gate
       - The `default` task body

    Note: `task test` is the same name as the included taskfile namespace `test`. Per go-task semantics, a root-level task named `test` shadows the included `test:` namespace's `default` task when calling `task test`. To call the included default explicitly, `task test:default` works. The root `test` aggregator is what users will invoke per TEST-02 ("Root `task test` aggregates ...").
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && grep -qE '^[[:space:]]+test:[[:space:]]+\./taskfiles/test\.yml' Taskfile.yml && grep -qE '^[[:space:]]+test:$' Taskfile.yml && grep -B1 '^[[:space:]]+test:$' Taskfile.yml | grep -q 'lint-allow: cmds-without-status' && task --list-all --json >/dev/null 2>&1 && task --list 2>&1 | grep -qE '^\* test\b' && grep -A6 '^[[:space:]]+test:$' Taskfile.yml | grep -qF 'task: manifest:test' && grep -A6 '^[[:space:]]+test:$' Taskfile.yml | grep -qF 'task: test:hooks'</automated>
  </verify>
  <acceptance_criteria>
    - `Taskfile.yml` includes block contains `test: ./taskfiles/test.yml`
    - `Taskfile.yml` tasks block contains a root-level `test:` task
    - The `test:` task carries `# lint-allow: cmds-without-status` marker directly above its name
    - The `test:` task `cmds:` block calls both `task: manifest:test` and `task: test:hooks`
    - YAML parses: `task --list-all --json` exits 0
    - `task --list` lists `test` as an available task
    - `task test` runs without error on a converged repo (both manifest:test and test:hooks fixtures pass)
    - `task lint` exits 0 against the modified `Taskfile.yml`
    - No other lines in `Taskfile.yml` are changed (verified by diff against pre-edit state — only the two additions)
  </acceptance_criteria>
  <done>
    Root `Taskfile.yml` exposes `task test` as the single-command Tier-3 smoke-test entry point, aggregating P1's manifest:test deep-merge fixtures with P7's test:hooks runner. The `test:` include namespace is available for direct invocation as `task test:hooks` and `task test:default`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inline JSON fixture content | User-authored test inputs piped to hook stdin; bad content could trigger external scanning (CI secret detectors) |
| Hook subprocess invocation | `echo $input | zsh hook.zsh` runs hook code; bad path resolution could exec arbitrary paths |
| Sentinel-style "no real secret" rule | Fixtures must contain SYNTHETIC secret-shaped strings, not real credentials |
| stderr pattern matching | Regex-based assertion on hook output; under-broad pattern could pass when hook is silently broken |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-17 | Information disclosure | Fixture inlines a real-looking secret that flags external scanners (TruffleHog, GitHub secret scanning) on commit | mitigate | Fixtures use clearly-synthetic prefixes (`sk-abc123def456...`) chosen NOT to match common cloud provider key formats. Document the constraint in a comment above each fixture. The hook's own `secret-scan.zsh` regex must match the synthetic value — verify by running the actual regex from the hook against the fixture during execution. |
| T-07-18 | Tampering | Fixture for `no-emojis` inlines a real emoji codepoint, which `no-emojis.zsh` then flags on the test file itself | mitigate | Use `printf '\U1F600'` runtime construction or `$'ὠ0'` rather than literal emoji bytes in the source. Source file stays free of emoji codepoints; the fixture string is only constructed at runtime when the test runs. Document the constraint. |
| T-07-19 | Tampering | Fixture for `no-ai-comments` contains `Co-Authored-By:` which the hook itself (running on this test file at commit time) flags as AI attribution | mitigate | The hook scans Write/Edit tool inputs at runtime; it does NOT scan static files in the repo. The fixture string only flows through the test runner stdin, not through Claude Code's Write/Edit. Verify by running `task lint:portability` or equivalent over the file. As a defense-in-depth measure, place the literal string in a heredoc with sentinel markers so it's clear to readers (and a future hook) that the string is a test fixture. |
| T-07-20 | Spoofing | Hook resolution uses `${HOOK_DIR}/<name>.zsh`; if HOOK_DIR is empty or wrong, runner could exec arbitrary `<name>.zsh` from CWD | mitigate | Runner sets `HOOK_DIR="${DOTFILEDIR}/claude/hooks"` immediately after source messages.zsh; `set -u` causes script to fail if DOTFILEDIR unset. Verify with a unit run: `unset DOTFILEDIR; zsh install/test-hooks.zsh` must exit non-zero. |
| T-07-21 | Denial of service | Hook hangs on stdin read (waits for EOF that never comes); test runner hangs forever | mitigate | Each fixture pipes JSON via `echo "$input" | ...` which closes stdin after the echo completes; hook's `hook::read_stdin` (in lib.zsh) reads to EOF and proceeds. Hooks have no other stdin source. As defense, optionally wrap each invocation in `timeout 5` — Plan-level Claude's Discretion. |
| T-07-22 | Tampering | Wrong assertion (e.g. `grep "Agent delegated"` matches even when hook crashes early and prints a different "Agent delegated" message) | mitigate | Each assertion checks BOTH exit code AND stderr/stdout pattern. The plugin-scoped fixture additionally asserts both `type: <name>` and `task: <description>` appear in the output, catching partial-rendering failures. |
| T-07-23 | Information disclosure | Test failure output may leak fixture contents (e.g. synthetic API key) into CI logs | accept | Synthetic prefixes used. The fixture content is committed code anyway; CI log exposure is no incremental risk. |
</threat_model>

<verification>
- `install/test-hooks.zsh` is executable, shellcheck-clean, zsh -n clean
- `DOTFILEDIR=$PWD zsh install/test-hooks.zsh` exits 0 on a converged repo (all four hooks behave correctly)
- Each fixture's expected behavior matches the actual hook regex / output (verified by running the runner end-to-end)
- `task -t taskfiles/test.yml hooks` invokes the runner correctly
- `task test` from repo root runs `manifest:test` and `test:hooks` in order
- `task lint` exits 0 against all three new/edited files
- `task --list` shows `test`, `test:hooks`, `test:default` as available tasks
- Regression check on Plan 02: the `test_agent_transparency` fixture exercises both the general-purpose path AND the plugin-scoped resolution branch
</verification>

<success_criteria>
- `install/test-hooks.zsh` exists with eight inline fixtures (pass + block/warn per hook) covering the four CLDE-02 named hooks
- `taskfiles/test.yml` defines `test:hooks` and `test:default`
- Root `Taskfile.yml` includes `test:` and exposes `task test` as the aggregator combining `manifest:test` + `test:hooks`
- `task test` is a single command that runs all Tier-3 smoke tests (TEST-02)
- Hook smoke tests exercise the rewritten `agent-transparency.zsh` from Plan 02, providing regression coverage for that change
- Servers may run `task test` independently — no claude-marketplace feature gate on the test surface (the runner exits cleanly if hook files are absent; servers' link gating is handled in Plan 06)
</success_criteria>

<output>
After completion, create `.planning/phases/07-claude-tool-configs-smoke-tests/07-04-SUMMARY.md`.
</output>

## Source Coverage

| Source | Item | Plan Coverage |
|--------|------|---------------|
| REQ | TEST-01 (hook smoke tests for the four named hooks) | Task 1 |
| REQ | TEST-02 (root `task test` aggregator combining manifest:test + test:hooks) | Task 3 |
| REQ | CLDE-02 (smoke-test coverage of the rewritten agent-transparency.zsh) | Task 1 (`test_agent_transparency`) |
| CONTEXT D-15 | Single zsh runner with inline fixtures at `install/test-hooks.zsh` | Task 1 |
| CONTEXT D-16 | One pass + one block scenario per hook (8 fixtures total) | Task 1 |
| CONTEXT D-17 | Scope = the 4 named in CLDE-02 (block-destructive/notify/post-compact excluded) | Task 1 (only four functions) |
| CONTEXT D-18 | Runner lives at `install/test-hooks.zsh` | Task 1 (path) |
| CONTEXT Claude's-Discretion | Hook fixture exit codes per-hook expected (2 for secret-scan, 0 for warn-only) | Task 1 (assertions) |
| CONTEXT CF-06 | `set -euo pipefail` on every executable .zsh | Task 1 (file header) |
| CONTEXT CF-09 | install/messages.zsh sourced via DOTFILES_MESSAGES | Tasks 1, 2 |

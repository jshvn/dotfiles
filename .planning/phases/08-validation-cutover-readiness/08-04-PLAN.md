---
phase: 08-validation-cutover-readiness
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - docs/CUTOVER.md
  - docs/MACHINES.md
autonomous: true
requirements: [CUTV-03, DOCS-06, DOCS-08]
tags: [docs, cutover, machines, fresh-install]
must_haves:
  truths:
    - "`docs/CUTOVER.md` exists with two top-level sections (D-12): `## Fresh-machine verification` (numbered procedure) and `## Per-machine cutover state` (markdown table)"
    - "The fresh-machine procedure references the actual task chain: bootstrap.zsh -> task setup -- <name> -> task cutover:ack -- <name> -> task install -> task validate, with explicit cross-references to docs/SECURITY.md and docs/MANIFEST.md"
    - "The per-machine state table has one row per target machine (personal-laptop, work-laptop, server-1, server-2) with columns: machine | status | cutover-date | last-validate-pass | days-on-v2 | notes; status values are documented (planning / ready / installing / soaking / cut-over / archived)"
    - "`docs/MACHINES.md` exists with H2 sections for all four machines per D-14"
    - "Each MACHINES.md H2 section captures non-TOML prose and closes with the deference line referring readers to manifests/machines/<name>.toml"
    - "Neither file contains emojis (project convention); neither contains AI attribution trailers"
  artifacts:
    - path: docs/CUTOVER.md
      provides: "fresh-machine verification procedure (DOCS-08) + per-machine cutover state tracking table (CUTV-03)"
      contains: "## Fresh-machine verification"
    - path: docs/MACHINES.md
      provides: "per-machine purpose/hardware/role narrative (DOCS-06)"
      contains: "## personal-laptop"
  key_links:
    - from: docs/CUTOVER.md
      to: docs/SECURITY.md
      via: "explicit link in bootstrap step of fresh-machine procedure"
      pattern: "docs/SECURITY.md"
    - from: docs/CUTOVER.md
      to: docs/MANIFEST.md
      via: "explicit link in task setup step"
      pattern: "docs/MANIFEST.md"
    - from: docs/MACHINES.md
      to: manifests/machines/<name>.toml
      via: "single deference line per H2 section"
      pattern: "manifests/machines/"
---

<objective>
Create the two cutover-readiness documents the per-machine cutover runbook (P6) operates from:

1. `docs/CUTOVER.md` — one file with two halves per D-12: top half is the numbered fresh-machine verification procedure (DOCS-08) — a clean Mac walks through bootstrap → task setup → task cutover:ack → task install → task validate → 7-day soak → archive. Bottom half is the per-machine state table (CUTV-03) with columns from 08-CONTEXT.md "Specific Ideas".

2. `docs/MACHINES.md` — thin doc per D-14: H2 per machine with non-TOML prose only (purpose, hardware/arch, role narrative, hostname if non-default, special handling). Each section closes with `See \`manifests/machines/<name>.toml\` for declarative state.` Manifest TOMLs stay the source of truth for feature flags, identity, packages — no duplication.

Purpose: P6 cannot execute the runbook without docs/CUTOVER.md telling the operator the exact task chain. docs/MACHINES.md captures per-machine context that TOML cannot express.

Output: Two new files in `docs/`. Neither replaces existing content.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-validation-cutover-readiness/08-CONTEXT.md
@.planning/phases/08-validation-cutover-readiness/08-RESEARCH.md
@.planning/phases/08-validation-cutover-readiness/08-PATTERNS.md
@.planning/phases/08-validation-cutover-readiness/08-03-PLAN.md
@docs/MANIFEST.md
@docs/SECURITY.md
@manifests/machines/personal-laptop.toml
@manifests/machines/work-laptop.toml
@manifests/machines/server-1.toml
@manifests/machines/server-2.toml
@CLAUDE.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/CUTOVER.md with fresh-machine procedure and per-machine state table</name>
  <files>docs/CUTOVER.md</files>
  <read_first>
    - docs/MANIFEST.md (analog: heading style — H1 doc title; H2 top-level sections; no horizontal rules; inline-code for commands)
    - docs/SECURITY.md (cross-reference target for the bootstrap step of the procedure)
    - .planning/phases/08-validation-cutover-readiness/08-PATTERNS.md § "docs/CUTOVER.md (new, P4)" (full structural template + cross-reference list)
    - .planning/phases/08-validation-cutover-readiness/08-CONTEXT.md D-12 (fresh-machine procedure shape: 8 numbered steps; bottom-half table column list)
    - .planning/phases/08-validation-cutover-readiness/08-CONTEXT.md "Specific Ideas" (status values: planning / ready / installing / soaking / cut-over / archived)
    - install/cutover-gate.zsh (confirms exact error a user sees if they skip task cutover:ack; procedure step 4 must reference task cutover:ack -- <machine>)
    - .planning/phases/08-validation-cutover-readiness/08-03-PLAN.md (confirms task cutover:ack writer behavior — sentinel format, machine name validation)
  </read_first>
  <action>
    Create `docs/CUTOVER.md` with the following exact structure (per D-12 + 08-PATTERNS.md):

    Top-of-file: H1 `# Cutover Reference`

    `## What This Is` H2 — one short paragraph framing: per-machine cutover gate from v1 to v2; v1 stays installed during the cutover window; soak before archiving.

    `## Fresh-machine verification` H2 — numbered checklist (anchor for DOCS-08). Use markdown ordered list (1. 2. 3.) so anchors generate cleanly. Exactly these 8 steps:

    1. Clone the v2 branch on the target Mac. Include the exact `git clone` command and `git checkout` of the v2 branch.
    2. Run `./bootstrap.zsh` (link inline to `docs/SECURITY.md` for the trust-chain audit). Note that the cutover-gate inside bootstrap exits cleanly the first time because the machine has not yet been set up.
    3. Run `task setup -- <machine-name>` (link inline to `docs/MANIFEST.md` for the per-machine schema reference). Mention that `<machine-name>` must match a file under `manifests/machines/<name>.toml`.
    4. Run `task cutover:ack -- <machine-name>` to write the per-machine cutover sentinel. This is what unblocks `task install`. The writer validates the name against the active machine file and rejects mismatches.
    5. Run `task install`. State that re-running is a no-op (every subtask has a `status:` block per LINT-01).
    6. Run `task validate`. State that all six per-component validates must show check; feature-off components may show `n/a`.
    7. Begin the 7-day soak period. Do not delete the v1 repo during soak. Record the date in the table below.
    8. After 7 days, update the table below (status: `cut-over`); after the last machine cuts over, archive v1 per `docs/MIGRATION.md` § "Archiving v1".

    `## Per-machine cutover state` H2 — short paragraph explaining the table's purpose and the status vocabulary (planning / ready / installing / soaking / cut-over / archived), then a markdown table with six columns and four data rows (one per target machine), all initialized to planning:

    | machine | status | cutover-date | last-validate-pass | days-on-v2 | notes |
    |---------|--------|--------------|--------------------|------------|-------|
    | personal-laptop | planning | - | - | - | primary dev box; Apple Silicon |
    | work-laptop | planning | - | - | - | work identity; GUI + dev environment |
    | server-1 | planning | - | - | - | headless ops; macos-security only |
    | server-2 | planning | - | - | - | headless ops; macos-security only |

    Note in a paragraph above the table that `days-on-v2` is computed manually by the operator (no helper task in v1; soak helper is a deferred v2 enhancement per 08-CONTEXT.md "Deferred Ideas").

    No emojis anywhere. No AI attribution. No horizontal rules. Use inline-code formatting for every task name, file path, command. Heading depth limited to H1 + H2 (optional H3 only if a step needs sub-items).
  </action>
  <verify>
    <automated>test -f docs/CUTOVER.md &amp;&amp; grep -q '^## Fresh-machine verification' docs/CUTOVER.md &amp;&amp; grep -q '^## Per-machine cutover state' docs/CUTOVER.md &amp;&amp; grep -q 'task cutover:ack' docs/CUTOVER.md &amp;&amp; grep -q 'docs/SECURITY.md' docs/CUTOVER.md &amp;&amp; grep -q 'docs/MANIFEST.md' docs/CUTOVER.md &amp;&amp; grep -q 'personal-laptop' docs/CUTOVER.md &amp;&amp; grep -q 'work-laptop' docs/CUTOVER.md &amp;&amp; grep -q 'server-1' docs/CUTOVER.md &amp;&amp; grep -q 'server-2' docs/CUTOVER.md &amp;&amp; ! ggrep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' docs/CUTOVER.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `docs/CUTOVER.md` exists and contains H1 `# Cutover Reference`
    - Source: `docs/CUTOVER.md` contains H2 `## Fresh-machine verification` AND H2 `## Per-machine cutover state`
    - Source: `docs/CUTOVER.md` contains a numbered list with exactly 8 top-level numbered steps in the Fresh-machine verification section
    - Source: `docs/CUTOVER.md` contains literal substrings `task setup --`, `task cutover:ack --`, `task install`, `task validate`
    - Source: `docs/CUTOVER.md` contains explicit references `docs/SECURITY.md` AND `docs/MANIFEST.md`
    - Source: `docs/CUTOVER.md` contains a markdown table with rows for all four machines
    - Source: `docs/CUTOVER.md` contains the six column headers: machine, status, cutover-date, last-validate-pass, days-on-v2, notes
    - Source: `docs/CUTOVER.md` documents the six status vocabulary values: planning, ready, installing, soaking, cut-over, archived
    - Behavior: `ggrep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' docs/CUTOVER.md` returns NO matches (no emoji)
    - Source: `docs/CUTOVER.md` does NOT contain the substrings `Co-Authored-By` or `Generated by` or `Generated with`
  </acceptance_criteria>
  <done>docs/CUTOVER.md created with the 8-step fresh-machine procedure (DOCS-08) and the 4-row per-machine state table (CUTV-03); cross-references to docs/SECURITY.md and docs/MANIFEST.md present; no emojis; no AI attribution.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/MACHINES.md with thin per-machine sections deferring to TOML</name>
  <files>docs/MACHINES.md</files>
  <read_first>
    - docs/MANIFEST.md (analog: thin reference doc shape; defers to source of truth)
    - manifests/machines/personal-laptop.toml (source for [meta].description, [platform].arch=arm64, [identity].git=personal)
    - manifests/machines/work-laptop.toml (source for [meta].description, [platform] has no arch declared so the resolver detects via `uname -m`, [identity].git=work)
    - manifests/machines/server-1.toml (source for [meta].description = "Mac server-1 -- headless ops machine, core packages only", [platform] no arch, server identity)
    - manifests/machines/server-2.toml (source for [meta].description = "Mac server-2 -- headless ops machine, core packages only", [platform] no arch, server identity)
    - .planning/phases/08-validation-cutover-readiness/08-PATTERNS.md § "docs/MACHINES.md (new, P4)" (structural template: H2 per machine; non-TOML prose only; deference line at end of each section)
    - .planning/phases/08-validation-cutover-readiness/08-CONTEXT.md D-14 (the doc value is per-machine prose that TOML cannot express)
  </read_first>
  <action>
    Create `docs/MACHINES.md` with this structure (per D-14 + 08-PATTERNS.md):

    H1 `# Machine Reference`

    `## What This Is` H2 — one short paragraph framing: per-machine prose the TOML cannot capture. State explicitly that for features, identity, and package bundles the manifest TOML is the source of truth; this doc is the prose layer.

    `## personal-laptop` H2 — non-TOML content only:
    - Purpose: one sentence — Josh's primary personal Mac, daily driver for personal projects and personal AI/CLI work.
    - Hardware: Apple Silicon (arm64; declared in `[platform].arch`).
    - Role narrative: 2-3 sentences explaining day-to-day usage (development of personal tools, dotfiles iteration, Claude Code experimentation), full GUI + dev + personal feature set on.
    - Hostname: only if non-default; otherwise omit this bullet.
    - Special handling: minimal prose about external display, peripherals, networking (Tailscale, VPN), or quirks — only what the operator wants captured.
    - End the section with: `See \`manifests/machines/personal-laptop.toml\` for declarative state (features, identity, package bundles).`

    `## work-laptop` H2:
    - Purpose: one sentence — work-issued MacBook with work git/ssh identity.
    - Hardware: state "Apple Silicon or Intel — arch detected by the resolver via `uname -m`" because `[platform].arch` is absent in the work-laptop TOML.
    - Role narrative: 2-3 sentences — primary work development machine, work identity for git/ssh, work-specific cask set (no personal apps).
    - Special handling: minimal prose about work-specific tooling (Sourcetree, Microsoft Office).
    - End with the deference line for work-laptop.

    `## server-1` H2:
    - Purpose: one sentence — Mac server-1, headless ops machine, core packages only.
    - Hardware: state "Apple Silicon or Intel — arch detected by the resolver" (absent in TOML).
    - Role narrative: 2-3 sentences — headless ops, core CLI packages only, claude-marketplace off, GUI feature flags off, only macos-security defaults run.
    - Special handling: minimal prose about remote access (SSH, Tailscale), monitoring, intended workload.
    - End with the deference line for server-1.

    `## server-2` H2 — same structural shape as server-1; mirror the prose for server-2's role.

    Heading depth: H1 + H2 only (H3 only if a special-handling sub-section is genuinely needed). No emojis. No AI attribution. No tables (the TOML files are the table-shaped source of truth).

    Where prose requires details about operator-specific usage not in the TOML descriptions, write plausible role-narrative prose that the operator can edit during the cutover window. Do NOT invent fake hostnames, IP addresses, hardware specs, or special-handling details. If a section has nothing beyond `[meta].description`, the section is brief: purpose + deference line is acceptable.
  </action>
  <verify>
    <automated>test -f docs/MACHINES.md &amp;&amp; grep -q '^# Machine Reference' docs/MACHINES.md &amp;&amp; grep -q '^## personal-laptop' docs/MACHINES.md &amp;&amp; grep -q '^## work-laptop' docs/MACHINES.md &amp;&amp; grep -q '^## server-1' docs/MACHINES.md &amp;&amp; grep -q '^## server-2' docs/MACHINES.md &amp;&amp; [ "$(grep -c 'manifests/machines/' docs/MACHINES.md)" -ge 4 ] &amp;&amp; ! ggrep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' docs/MACHINES.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `docs/MACHINES.md` exists with H1 `# Machine Reference`
    - Source: `docs/MACHINES.md` contains all four H2 sections: `## personal-laptop`, `## work-laptop`, `## server-1`, `## server-2`
    - Source: `docs/MACHINES.md` contains at least 4 references to `manifests/machines/` (one deference line per machine section)
    - Source: `docs/MACHINES.md` contains zero markdown tables (`grep -c '^|' docs/MACHINES.md` returns 0) — TOML is the table source of truth
    - Behavior: `ggrep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' docs/MACHINES.md` returns NO matches (no emoji)
    - Source: `docs/MACHINES.md` does NOT contain `Co-Authored-By`, `Generated by`, or `Generated with`
    - Source: No H2 section duplicates feature flags or package lists already in the TOML (operator-readable spot-check during commit; the deference line is the contract)
  </acceptance_criteria>
  <done>docs/MACHINES.md created with H2 per machine, non-TOML prose only, deference line per section; no tables; no emojis; no AI attribution; all four target machines covered (DOCS-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/CUTOVER.md → operator on fresh Mac | doc is the source of truth for the per-machine cutover task chain; mis-instruction could lead to incorrect installation |
| docs/MACHINES.md → operator + Claude maintaining repo | doc is prose layer; TOML is the declarative truth; mis-match between prose and TOML is a documentation-drift class (per D-14 mitigated by keeping prose brief and TOML authoritative) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Information disclosure | docs/MACHINES.md prose | accept | prose is role-and-purpose only; no secrets, no IPs, no SSH key fingerprints; operator edits during cutover if richer detail is needed |
| T-08-13 | Tampering | docs/CUTOVER.md misleads operator | mitigate | doc cross-references docs/SECURITY.md (bootstrap trust chain) and docs/MANIFEST.md (machine selection schema) so the operator can verify every step against the source-of-truth references |
| T-08-14 | Repudiation | per-machine state table not updated after soak | accept | manual operator process per CUTV-05; doc-template note states days-on-v2 is manually tracked; auto-tracking is deferred v2 work |
| T-08-SC | Tampering | npm/pip/cargo installs | n/a | docs-only plan; no installs |
</threat_model>

<verification>
- `test -f docs/CUTOVER.md && test -f docs/MACHINES.md` (both files exist)
- `grep -q '^## Fresh-machine verification' docs/CUTOVER.md` (DOCS-08 anchor present)
- `grep -q '^## Per-machine cutover state' docs/CUTOVER.md` (CUTV-03 table section present)
- All four machine names appear in both files
- Emoji-range check returns zero matches in both files (project convention)
- No AI attribution substrings appear in either file
- The fresh-machine procedure's task chain matches the actual task names shipped by P3 (`task cutover:ack -- <name>`)
- docs/MACHINES.md contains zero markdown tables (D-14 contract — TOML is the table-shaped source of truth)
</verification>

<success_criteria>
- DOCS-08 closed: docs/CUTOVER.md contains the per-machine fresh-machine verification procedure
- CUTV-03 closed: docs/CUTOVER.md contains the per-machine cutover state table
- DOCS-06 closed: docs/MACHINES.md documents each machine's purpose, role, hardware, and special handling
- Both files comply with project conventions (no emojis, no AI attribution)
- TOML stays the source of truth for declarative state; prose stays in MACHINES.md
- P5 (docs/MIGRATION.md + root README.md) can cite both new files in cross-references
</success_criteria>

<output>
Create `.planning/phases/08-validation-cutover-readiness/08-04-SUMMARY.md` when done
</output>

---
phase: 04-identity-layer-git-ssh-per-machine
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - taskfiles/identity.yml
  - taskfiles/links.yml
  - Taskfile.yml
  - identity/README.md
autonomous: true
requirements:
  - IDNT-07
  - IDNT-08
must_haves:
  truths:
    - "taskfiles/identity.yml reads identity.git, identity.ssh, features.one-password-ssh, features.one-password-signing from resolved.json via ref: fromJson"
    - "taskfiles/identity.yml creates every symlink via _:safe-link (no bare ln -s anywhere)"
    - "taskfiles/identity.yml's identity:install aggregator carries `# lint-allow: cmds-without-status` and has no status: block"
    - "taskfiles/identity.yml status: blocks use {{.X}} template vars only, never $X shell vars"
    - "On server machines, ~/.config/git/server-include.config is materialized with [includeIf gitdir:~/] path = identities/server-N; on workstations it is absent"
    - "~/.ssh/identities/active symlink target equals {{.MANIFEST.identity.ssh}}"
    - "task identity:validate asserts (a) symlink presence, (b) git config user.email matches manifest identity, (c) ssh-add -L lists expected pub key (when one-password-ssh = true), (d) identity/ssh/keys/ contains only *.pub files"
    - "taskfiles/links.yml's all: aggregator references identity:install"
    - "identity/README.md replaces the Phase 1 stub with the DOCS-02-shaped content"
  artifacts:
    - path: "taskfiles/identity.yml"
      provides: "identity:install (aggregator), identity:git, identity:ssh, identity:validate, identity:server-include (internal)"
      contains: "MANIFEST.identity.ssh"
      min_lines: 150
    - path: "taskfiles/links.yml"
      provides: "all: aggregator extended with task: identity:install"
      contains: "identity:install"
    - path: "Taskfile.yml"
      provides: "Root includes block extended to wire identity module"
      contains: "identity:"
    - path: "identity/README.md"
      provides: "DOCS-02-shaped README replacing the P1 stub"
      contains: "## Key files"
      min_lines: 40
  key_links:
    - from: "taskfiles/identity.yml identity:install"
      to: "manifest:manifest:resolve"
      via: "deps: declaration"
      pattern: "deps: \\[manifest:manifest:resolve\\]"
    - from: "taskfiles/identity.yml identity:git"
      to: "identity/git/config + identity/git/identities/{personal,work,server-1,server-2,none}"
      via: "_:safe-link calls"
      pattern: "task: _:safe-link"
    - from: "taskfiles/identity.yml identity:ssh"
      to: "~/.ssh/identities/active"
      via: "_:safe-link SOURCE=identities/{{.MANIFEST.identity.ssh}}"
      pattern: "SSH_IDENTITIES_DIR.*active"
    - from: "taskfiles/links.yml all:"
      to: "taskfiles/identity.yml identity:install"
      via: "task: identity:install"
      pattern: "task: identity:install"
---

<objective>
Build the manifest-driven taskfile (`taskfiles/identity.yml`) that composes every Phase-4 symlink, materializes `~/.config/git/server-include.config` on server machines, and ships `task identity:validate` with the four success-criteria assertions. Wire it into the install pipeline by adding `identity:` to the root `Taskfile.yml` includes block and appending `task: identity:install` to `taskfiles/links.yml`'s `all:` aggregator. Replace the Phase 1 `identity/README.md` stub with the DOCS-02-shaped README.

Purpose: This is the integration layer. The static content from Plan 02 is inert without symlinks; the schema from Plan 01 is informational without a consumer. `taskfiles/identity.yml` reads the resolved manifest, creates symlinks, and provides the validation entry point that Phase 8's `task validate` will compose.
Output: A wired install pipeline where `task install` lands the identity layer end-to-end and `task identity:validate` exits 0 on a converged machine.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-CONTEXT.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-RESEARCH.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-PATTERNS.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-VALIDATION.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-01-SUMMARY.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-02-SUMMARY.md
@CLAUDE.md
@.claude/CLAUDE.md

<interfaces>
<!-- Required reading. The links.yml + manifest.yml shape is the contract identity.yml replicates. -->

From taskfiles/links.yml (full file): The shape pattern for `taskfiles/identity.yml`:
  - `version: '3'` line
  - File-header banner naming purpose, dependencies, status-block convention (LINT-02), symlink convention (LINT-03b)
  - `includes: _: ./helpers.yml`
  - `vars:` block with HOME, XDG_CONFIG_HOME (sh:), DOTFILEDIR (sh: dirname TASKFILE_DIR), DOTFILES_MESSAGES (source ${DOTFILEDIR}/install/messages.zsh)
  - `tasks:` -- aggregator with `# lint-allow: cmds-without-status` marker; install sub-tasks with cmds calling `task: _:safe-link` + status: blocks using `test -L "{{.X}}"`; validate task with `_:check-link` calls

From taskfiles/manifest.yml (lines 38-93): MANIFEST_JSON sh: + MANIFEST ref: 'fromJson .MANIFEST_JSON' pattern -- copy verbatim into identity.yml vars block. Snake-case keys (identity.git) use dot-access; kebab-case keys (one-password-ssh) need index form: `{{index .MANIFEST.features "one-password-ssh"}}`.

From taskfiles/helpers.yml (lines 30-37, 43-59): _:safe-link helper (requires SOURCE, TARGET; mkdir -p + ln -sfn); _:check-link helper (requires TARGET, NAME; emits check/cross via DOTFILES_MESSAGES).

From Taskfile.yml lines 73-88 (existing includes block): includes shape -- `manifest:` carries explicit vars forwarding (DOTFILEDIR, XDG_STATE_HOME, DOTFILES_MESSAGES); other modules use the short form `lint: ./taskfiles/lint.yml`. New `identity:` entry should mirror `manifest:` (forwards vars) because it also reads resolved.json.

From .planning/phases/04-identity-layer-git-ssh-per-machine/04-RESEARCH.md lines 815-1127 (the full taskfiles/identity.yml sketch + identity:validate code): the complete reference implementation for vars block, the four install subtasks, the server-include subtask, and the four validate assertions. Executor copies the structure verbatim and adapts to repo conventions.

From taskfiles/links.yml lines 75-80 (all: aggregator): the line `task: identity:install` is appended after `task: antidote`; the desc string updates to mention P4.

From shell/README.md + taskfiles/README.md: DOCS-02 README shape -- one-paragraph intro, ## Key files, ## Adding a pattern, ## References.

From .planning/phases/04-identity-layer-git-ssh-per-machine/04-VALIDATION.md "Per-Task Verification Map": the per-requirement automated checks identity:validate must perform; these are the four assertions in the validate task.

Symlink topology produced by identity:install:
  Git:
    {{.DOTFILEDIR}}/identity/git/config            -> {{.XDG_CONFIG_HOME}}/git/config
    {{.DOTFILEDIR}}/identity/git/ignore            -> {{.XDG_CONFIG_HOME}}/git/ignore
    {{.DOTFILEDIR}}/identity/git/identities/<5>    -> {{.XDG_CONFIG_HOME}}/git/identities/<5>
    (servers only, generated not symlinked) {{.XDG_CONFIG_HOME}}/git/server-include.config
  SSH:
    {{.DOTFILEDIR}}/identity/ssh/config            -> {{.HOME}}/.ssh/config
    {{.DOTFILEDIR}}/identity/ssh/identities/<5>    -> {{.HOME}}/.ssh/identities/<5>
    {{.DOTFILEDIR}}/identity/ssh/keys/<3>.pub      -> {{.HOME}}/.ssh/identities/keys/<3>.pub  (personal, server-1, server-2)
    {{.DOTFILEDIR}}/identity/ssh/cloudflared.zsh   -> {{.HOME}}/.ssh/identities/cloudflared.zsh
    {{.HOME}}/.ssh/identities/{{.MANIFEST.identity.ssh}} -> {{.HOME}}/.ssh/identities/active
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create taskfiles/identity.yml with install/git/ssh/server-include/validate subtasks</name>
  <files>taskfiles/identity.yml</files>
  <read_first>
    - taskfiles/links.yml (full file - the shape template; vars block, aggregator with lint-allow marker, sub-tasks with status:, validate task)
    - taskfiles/manifest.yml lines 38-93 (the MANIFEST_JSON sh: + MANIFEST ref: 'fromJson .MANIFEST_JSON' pattern -- copy verbatim)
    - taskfiles/helpers.yml (full file - _:safe-link + _:check-link contracts; do not duplicate, just call)
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-RESEARCH.md lines 815-1127 (full identity.yml sketch + validate code -- the reference implementation)
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-PATTERNS.md "taskfiles/identity.yml (NEW; controller / event-driven)" section -- the pattern catalog (S1..S8) that binds this file
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-VALIDATION.md "Per-Task Verification Map" -- the four validate assertions
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-CONTEXT.md D-08 (server-include materialization), D-12 (active symlink swap), D-13 (scoped keys path), D-14 (cloudflared deployment)
    - identity/git/config (Plan 02 output - sanity-check the SOURCE side of git symlinks exists)
    - identity/ssh/config (Plan 02 output - sanity-check the SOURCE side of ssh symlinks exists)
  </read_first>
  <action>
Create `taskfiles/identity.yml`. Use the Write tool. The file replicates `taskfiles/links.yml`'s shape and embeds `taskfiles/manifest.yml`'s MANIFEST consumption pattern.

Top of file -- file-header banner (Pattern S6 + matching the links.yml banner shape at links.yml:1-38). Content covers: purpose ("Phase 4: manifest-driven git + SSH identity layer; reads identity.git/identity.ssh/features.* from resolved.json; creates all symlinks via _:safe-link"), dependencies (`manifest:resolve`, `_:safe-link`, `_:check-link`, `install/messages.zsh`), status-block convention (LINT-02 enforcement -- `{{.X}}` only, never `$X`; aggregator + validate use `# lint-allow: cmds-without-status`), symlink convention (LINT-03b -- no bare `ln`), kebab-case feature key convention (`{{index .MANIFEST.features "one-password-ssh"}}`), aggregator placement (per D-08 + RESEARCH Open Question 4: extended into `taskfiles/links.yml all:` aggregator, NOT the root Taskfile.yml install task body -- the links.yml comment at lines 7-13 already names P4 as the next extender).

`includes:` block:
  - `_: ./helpers.yml`

`vars:` block (per links.yml lines 49-63 + manifest.yml lines 38-93):
  - `HOME: '{{.HOME}}'`
  - `XDG_CONFIG_HOME: sh: echo "${XDG_CONFIG_HOME:-$HOME/.config}"`
  - `XDG_STATE_HOME: sh: echo "${XDG_STATE_HOME:-$HOME/.local/state}"`
  - `DOTFILEDIR: sh: dirname "{{.TASKFILE_DIR}}"`
  - `RESOLVED_JSON_PATH: '{{.XDG_STATE_HOME}}/dotfiles/resolved.json'`
  - `MANIFEST_JSON:` with the same sh: block as manifest.yml lines 80-87 (the file-exists check + `{}` fallback + stderr warn pattern)
  - `MANIFEST: ref: 'fromJson .MANIFEST_JSON'` (with the same kebab-vs-snake comment block per manifest.yml lines 89-93)
  - `DOTFILES_MESSAGES: | source '{{.DOTFILEDIR}}/install/messages.zsh'`
  - Derived path vars:
    - `GIT_CONFIG_DIR: '{{.XDG_CONFIG_HOME}}/git'`
    - `SSH_DIR: '{{.HOME}}/.ssh'`
    - `SSH_IDENTITIES_DIR: '{{.SSH_DIR}}/identities'`
    - `SSH_KEYS_DIR: '{{.SSH_IDENTITIES_DIR}}/keys'`
    - `SERVER_INCLUDE_CONFIG: '{{.GIT_CONFIG_DIR}}/server-include.config'`

`tasks:` block -- five tasks:

(1) `install` -- aggregator:
  - Preceded immediately by `  # lint-allow: cmds-without-status` (Pattern S3; must be on the line ABOVE the task key)
  - `desc: "Install identity layer (git + ssh)"`
  - `deps: [manifest:manifest:resolve]` (CF-10; the double-prefix reflects root-include alias `manifest:` + sub-task `manifest:resolve`)
  - `cmds:`
    - `- task: git`
    - `- task: ssh`

(2) `git` -- six `_:safe-link` calls + server-include sub-task:
  - `desc: "Link git identity files; materialize server-include.config on servers"`
  - `cmds:` lists `task: _:safe-link` calls for: `identity/git/config -> {{.GIT_CONFIG_DIR}}/config`, `identity/git/ignore -> {{.GIT_CONFIG_DIR}}/ignore`, then five `identity/git/identities/<N> -> {{.GIT_CONFIG_DIR}}/identities/<N>` calls for `personal, work, server-1, server-2, none`. Final cmd: `- task: server-include`.
  - `status:` lists one `test -L "{{.GIT_CONFIG_DIR}}/<target>"` line per symlink (seven lines: config, ignore, identities/personal, identities/work, identities/server-1, identities/server-2, identities/none). All template vars use `{{.X}}` form (Pattern S2).

(3) `server-include` -- internal generator (D-08 materialization, RESEARCH Open Question 3 recommendation: generate-at-install):
  - `desc: "Materialize ~/.config/git/server-include.config when identity is a server (D-08)"`
  - `internal: true`
  - `cmds:` -- a single shell block that reads `{{.MANIFEST.identity.git}}` into a shell var, then a `case` switch: for `server-1|server-2`, `mkdir -p "{{.GIT_CONFIG_DIR}}"` + `printf '[includeIf "gitdir:~/"]\n    path = identities/%s\n' "$identity" > "{{.SERVER_INCLUDE_CONFIG}}"` + `check "..."`; for `*`, remove any stale file at `{{.SERVER_INCLUDE_CONFIG}}` and emit `info "removed stale..."` if it existed. Source `{{.DOTFILES_MESSAGES}}` at top of the shell block.
  - `status:` -- a single block matching same `case` structure: for `server-1|server-2`, assert `test -f "{{.SERVER_INCLUDE_CONFIG}}"` AND `grep -q "identities/${identity}" "{{.SERVER_INCLUDE_CONFIG}}"`; for `*`, assert `test ! -f "{{.SERVER_INCLUDE_CONFIG}}"`. Template var `{{.SERVER_INCLUDE_CONFIG}}` resolves at task-graph-build time; the shell var `$identity` is set inside the rendered shell (Pattern S2 -- this is the allowed shape: `{{.X}}` for go-task vars, `$X` for shell vars set inside the cmds block).

(4) `ssh` -- many `_:safe-link` calls + active symlink swap:
  - `desc: "Link SSH identity files; swap active symlink to manifest value"`
  - `cmds:` lists `task: _:safe-link` calls for:
    - `identity/ssh/config -> {{.SSH_DIR}}/config`
    - Five `identity/ssh/identities/<N> -> {{.SSH_IDENTITIES_DIR}}/<N>` calls for `personal, work, server-1, server-2, none`
    - Three pub-key calls: `identity/ssh/keys/personal.pub -> {{.SSH_KEYS_DIR}}/personal.pub`, `identity/ssh/keys/server-1.pub -> {{.SSH_KEYS_DIR}}/server-1.pub`, `identity/ssh/keys/server-2.pub -> {{.SSH_KEYS_DIR}}/server-2.pub`
    - `identity/ssh/cloudflared.zsh -> {{.SSH_IDENTITIES_DIR}}/cloudflared.zsh` (D-14: deployed on every machine)
    - Active swap: `SOURCE: "{{.SSH_IDENTITIES_DIR}}/{{.MANIFEST.identity.ssh}}"`, `TARGET: "{{.SSH_IDENTITIES_DIR}}/active"` (D-12; readlink resolves to a short name like `personal`)
  - `status:` lists matching `test -L "{{.X}}"` lines for each symlink (~13 lines), plus one `readlink` assertion for the active symlink: `test "$(readlink {{.SSH_IDENTITIES_DIR}}/active)" = "{{.MANIFEST.identity.ssh}}" || test "$(readlink {{.SSH_IDENTITIES_DIR}}/active)" = "{{.SSH_IDENTITIES_DIR}}/{{.MANIFEST.identity.ssh}}"` (handles both short-name and absolute-path symlink target forms; per RESEARCH lines 1032-1033).

(5) `validate` -- always-re-run diagnostic:
  - Preceded immediately by `  # lint-allow: cmds-without-status` (Pattern S3)
  - `desc: "Validate identity layer: symlinks, git config, ssh-add, keys/ contents"`
  - `cmds:` four blocks per IDNT-07 + RESEARCH lines 1043-1127:
    (a) Symlink presence: four `task: _:check-link` invocations for `{{.GIT_CONFIG_DIR}}/config`, `{{.SSH_DIR}}/config`, `{{.SSH_IDENTITIES_DIR}}/active`, `{{.SSH_IDENTITIES_DIR}}/cloudflared.zsh`.
    (b) git config user.email assertion: a shell block that sources `{{.DOTFILES_MESSAGES}}`, reads `identity="{{.MANIFEST.identity.git}}"`, runs a case switch:
      - `personal): gitdir="$HOME/git/personal"; expected_email="josh@vaughen.net"`
      - `work): gitdir="$HOME/git/work"; expected_email=$(git config -f "{{.DOTFILEDIR}}/identity/git/identities/work" user.email 2>/dev/null || echo "")` (reads work email from the identity file directly; if it's the TODO placeholder from Plan 02, the expected_email is empty and the assertion silently passes when the gitdir is also absent)
      - `server-1|server-2): gitdir="$HOME"; expected_email="${identity}@jgrid.net"`
      - `none): info "identity.git = none -- skipping email assertion"; exit 0`
      Then: if the gitdir directory doesn't exist on disk AND identity is not server-*, `info` and exit 0 (Claude's Discretion item: skip when gitdir absent). Otherwise: `actual_email=$(git -C "$gitdir" config user.email 2>/dev/null || echo "")`; compare against expected_email; `check` or `cross` (with `exit 1` on cross).
    (c) ssh-add -L assertion: shell block reading `opssh=$(jq -r '.features."one-password-ssh" // false' "{{.RESOLVED_JSON_PATH}}")`. If `opssh != "true"`, `info` and exit 0. Else: read `identity="{{.MANIFEST.identity.ssh}}"`, expected pub at `{{.DOTFILEDIR}}/identity/ssh/keys/${identity}.pub`; if file is empty or missing, `warn` and exit 0 (placeholder-fixture case); else extract the key body via `awk '{print $2}'` and check `ssh-add -L 2>/dev/null | awk '{print $2}' | grep -qF "$expected_body"`; `check` or `cross` (with `exit 1` on cross).
    (d) keys/ allowlist: shell block running `bad=$(find "{{.DOTFILEDIR}}/identity/ssh/keys" -maxdepth 1 -type f -not -name '*.pub' -not -name '.gitignore' 2>/dev/null)`; if `-z "$bad"`, `check`; else `cross` + echo bad list + `exit 1`.

Constraints (Patterns S1..S8 binding):
- Every symlink goes through `_:safe-link` -- absolutely no bare `ln -s` anywhere in this file (LINT-03b; grep gate at verify time).
- Every `status:` line uses `{{.X}}` for go-task template vars; shell-set vars (`$identity`, `$bad`, `$actual_email`) are allowed INSIDE rendered shell blocks (LINT-02 Pattern S2).
- The `install` and `validate` tasks are aggregators -- both use the `# lint-allow: cmds-without-status` marker on the line immediately above the task key (Pattern S3; marker position is checked by LINT-03a).
- Kebab-case feature keys MUST use index form: `{{index .MANIFEST.features "one-password-ssh"}}`. Snake-case keys use dot-access: `{{.MANIFEST.identity.git}}`. (Pattern S4)
- `deps: [manifest:manifest:resolve]` on `install` only -- sub-tasks inherit freshness from the aggregator's deps.
- File-header comment block at top (Pattern S6).
- All error/info output goes through messages.zsh functions (`check`, `cross`, `error`, `warn`, `info`) sourced via `{{.DOTFILES_MESSAGES}}` (Pattern S7).
- Target file size will be ~180-220 lines including the header banner.
  </action>
  <verify>
    <automated>cd $DOTFILEDIR && test -f taskfiles/identity.yml && grep -c "ln -s" taskfiles/identity.yml | grep -q '^0$' && grep -q 'ref: .fromJson .MANIFEST_JSON.' taskfiles/identity.yml && grep -q 'deps: \[manifest:manifest:resolve\]' taskfiles/identity.yml && grep -q '# lint-allow: cmds-without-status' taskfiles/identity.yml && grep -q '{{.MANIFEST.identity.git}}' taskfiles/identity.yml && grep -q '{{.MANIFEST.identity.ssh}}' taskfiles/identity.yml && grep -q 'jq -r .\.features\."one-password-ssh"' taskfiles/identity.yml && grep -q '_:safe-link' taskfiles/identity.yml && grep -q '_:check-link' taskfiles/identity.yml && grep -Ev '^#' taskfiles/identity.yml | grep -E '\$RESOLVED_JSON_PATH|\$XDG_STATE_HOME|\$SSH_IDENTITIES_DIR|\$GIT_CONFIG_DIR' | grep -v 'sh:' | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `taskfiles/identity.yml` exists with five top-level tasks under `tasks:`: `install`, `git`, `server-include`, `ssh`, `validate`
    - `grep -c "ln -s" taskfiles/identity.yml` returns 0 (LINT-03b -- only `_:safe-link` invocations)
    - `grep -c "ref: 'fromJson .MANIFEST_JSON'" taskfiles/identity.yml` returns 1 (IDNT-08 -- the file consumes resolved.json via fromJson)
    - `grep -c "deps: \\[manifest:manifest:resolve\\]" taskfiles/identity.yml` returns 1 (CF-10 -- only on the install aggregator)
    - `grep -c "# lint-allow: cmds-without-status" taskfiles/identity.yml` returns 2 (one above `install:`, one above `validate:`)
    - The file contains `{{index .MANIFEST.features "one-password-ssh"}}` somewhere (Pattern S4 -- kebab-case key) OR the equivalent via `jq -r '.features."one-password-ssh"'` shell read for the validate task (which is what RESEARCH recommends to avoid go-task `index` for a single conditional)
    - Status-block hygiene check: `grep -E '^\s+-\s+' taskfiles/identity.yml | grep -vE 'sh:' | grep -E '\$(RESOLVED_JSON_PATH|XDG_STATE_HOME|DOTFILEDIR|HOME|SSH_IDENTITIES_DIR|GIT_CONFIG_DIR|SERVER_INCLUDE_CONFIG|SSH_DIR|SSH_KEYS_DIR|XDG_CONFIG_HOME|DOTFILES_MESSAGES|MANIFEST_JSON|MANIFEST)'` returns 0 lines (no bare $-references to go-task vars; per LINT-02)
    - `task -t taskfiles/identity.yml --list` exits 0 and lists `install`, `git`, `ssh`, `validate` (server-include is `internal: true` so it does not list)
    - `task lint:taskfile` exits 0 (no LINT-01/02/03 violations)
    - `task lint:shell-headers` is N/A for taskfile YAML; `task lint` exits 0
  </acceptance_criteria>
  <done>The taskfile carries the manifest-driven install + validate logic, conforms to every binding pattern (S1..S8), and is callable standalone via `task -t taskfiles/identity.yml ...` before Task 2 wires it into the root Taskfile.yml.</done>
</task>

<task type="auto">
  <name>Task 2: Wire identity.yml into root Taskfile.yml + links.yml all: aggregator, replace identity/README.md stub</name>
  <files>Taskfile.yml, taskfiles/links.yml, identity/README.md</files>
  <read_first>
    - Taskfile.yml (full file - the includes: block at lines 73-88; specifically the `manifest:` entry pattern at lines 77-82 that forwards DOTFILEDIR/XDG_STATE_HOME/DOTFILES_MESSAGES)
    - taskfiles/links.yml lines 75-80 (the `all:` aggregator that gains an `identity:install` entry; also the desc string that gets updated)
    - shell/README.md (52 lines - DOCS-02 reference shape)
    - taskfiles/README.md (75 lines - DOCS-02 second analog; note the trailer `Satisfies DOCS-02 for taskfiles/.`)
    - identity/README.md (existing Phase 1 stub - 10 lines - the file being replaced)
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-PATTERNS.md sections "identity/README.md (REPLACE Phase 1 stub; docs)" and "taskfiles/links.yml (EDIT all: aggregator; one new task entry)"
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-CONTEXT.md Domain section bullets describing identity/README.md content
  </read_first>
  <action>
Three edits, ordered to minimize disruption:

(a) `Taskfile.yml` -- extend the `includes:` block at lines 73-88. Add a new `identity:` entry IMMEDIATELY AFTER the `manifest:` block (because `identity:` ALSO reads resolved.json and benefits from explicit DOTFILEDIR/XDG_STATE_HOME forwarding). New entry shape (mirror the `manifest:` block at lines 77-82):

```
  identity:
    taskfile: ./taskfiles/identity.yml
    vars:
      DOTFILEDIR: '{{.DOTFILEDIR}}'
      XDG_STATE_HOME: '{{.XDG_STATE_HOME}}'
      DOTFILES_MESSAGES: '{{.DOTFILES_MESSAGES}}'
```

Also update the includes comment block at the top of `Taskfile.yml` (lines 13-21): add a line `#   - identity (P4, real)` immediately after the `#   - links    (P3, real)` line. Keep the existing entries unchanged.

(b) `taskfiles/links.yml` -- extend the `all:` aggregator at lines 75-80. Append one new cmd line after the existing `- task: antidote`:

```
  # lint-allow: cmds-without-status
  all:
    desc: "Create all symlinks (P3+P4: shell + identity)"
    cmds:
      - task: zsh
      - task: antidote
      - task: identity:install
```

Update the `desc:` string from `"Create all symlinks (P3: shell only; later phases extend)"` to `"Create all symlinks (P3+P4: shell + identity)"` (per PATTERNS section). The `identity:install` reference resolves via the root Taskfile.yml's `identity:` include alias added in step (a).

Also update the file-header banner comment at links.yml lines 8-13: the `# - P4 adds git/ssh/identity` bullet remains accurate but the descriptor "P3 ships shell-only; later phases extend" is now stale. Edit the banner narrative at links.yml lines 7-13 to read: P3 + P4 currently included; remaining bullets (P5, P6, P7) still pending.

(c) `identity/README.md` -- replace the Phase 1 stub entirely with the DOCS-02-shaped README. Use the Write tool (overwrites the existing 10-line stub). Structure (mirror `shell/README.md` and `taskfiles/README.md`):

  - H1 title: `# identity`
  - One-paragraph intro: Phase 4 owns git + SSH identity. Manifest-driven via `identity.git` and `identity.ssh` in machine TOMLs. macOS-only v1 (Apple Silicon + Intel); the model carries cleanly to v2's Linux work. Symlinks deploy via `taskfiles/identity.yml` and `_:safe-link`.
  - `## Key files` section -- bulleted list of key paths and their roles. Cover:
    - `git/config` -- main config; symlinked to `~/.config/git/config`; carries `[includeIf]` blocks per D-01 + the universal `[include] path = server-include.config` per D-08
    - `git/ignore` -- global gitignore (referenced via `core.excludesfile = ignore`)
    - `git/identities/<name>` -- flat per-identity overlays (personal, work, server-1, server-2, none); loaded by `[includeIf gitdir/i:...]` (workstations) or by the server-include.config wildcard (servers)
    - `ssh/config` -- main SSH config; symlinked to `~/.ssh/config`; carries `Include ~/.ssh/identities/active` per D-10
    - `ssh/identities/<name>` -- flat per-identity host configs; deployed at `~/.ssh/identities/<name>` on every machine per D-11
    - `ssh/identities/active` -- symlink to the manifest-selected identity file per D-12; swapping identity is a relink, not an edit
    - `ssh/keys/<name>.pub` -- public keys only; private keys NEVER enter the repo (IDNT-06); see `keys/.gitignore`
    - `ssh/cloudflared.zsh` -- ProxyCommand wrapper for `*.jgrid.net` hosts; deployed on every machine per D-14 even though only the personal identity references it
  - `## Adding a pattern` section:
    - **An identity.** Create `git/identities/<name>` and `ssh/identities/<name>`. Add the new value to the resolver enum (`install/resolver.zsh validate_manifest()` case statement) and add cross-field rules if the new identity carries 1Password directives. Add a new negative fixture under `manifests/test/fixtures/` to verify the rule.
    - **A machine.** Create `manifests/machines/<name>.toml`, then `task setup -- <name>`. Set `identity.git` and `identity.ssh` to the desired identity values from the enum.
    - **A 1Password-gated feature.** Add the kebab-case key to `manifests/defaults.toml [features]` block with `false`. Set `true` in the relevant machine TOMLs. Add cross-field validation rule in `install/resolver.zsh validate_manifest()` if the new flag should imply or be implied by an identity value.
  - `## References` section:
    - `../docs/MANIFEST.md` -- manifest schema and merge semantics (Phase 1)
    - `../CLAUDE.md` -- v2 conventions (status-block templating, `_:safe-link`, no AI attribution, no emojis)
    - `../.planning/REQUIREMENTS.md` -- IDNT-01..IDNT-08 + DOCS-02 traceability
  - Trailer line: `Satisfies DOCS-02 for identity/.` (per the convention used by `taskfiles/README.md`)

Constraints:
- No emojis anywhere.
- No AI attribution.
- Markdown formatting matches the existing READMEs (no fenced code blocks longer than 10 lines; bulleted lists with two-space indentation for sub-items).
- File length target: ~50-70 lines (similar to shell/README.md).
- `taskfiles/links.yml` lint-allow marker positions are preserved -- the marker above `all:` and the marker above `validate:` must remain on the line immediately above the respective task key.
- `Taskfile.yml` indent is exactly 2 spaces under `includes:`; match the existing entries' style.
  </action>
  <verify>
    <automated>cd $DOTFILEDIR && grep -q 'identity:' Taskfile.yml && grep -q 'taskfile: ./taskfiles/identity.yml' Taskfile.yml && grep -q '#   - identity' Taskfile.yml && grep -q 'task: identity:install' taskfiles/links.yml && grep -q 'P3+P4: shell + identity' taskfiles/links.yml && [ "$(wc -l < identity/README.md | tr -d ' ')" -ge "40" ] && grep -q '## Key files' identity/README.md && grep -q '## Adding a pattern' identity/README.md && grep -q '## References' identity/README.md && grep -q 'Satisfies DOCS-02 for identity/' identity/README.md && task lint && task identity:validate; echo "(identity:validate exit: $?)"; task install --dry-run 2>&1 | grep -q 'identity:install' || (echo "BLOCKING: identity:install not in install pipeline"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `Taskfile.yml` includes block contains `identity:` mapped to `./taskfiles/identity.yml` with the same vars-forwarding pattern as `manifest:`
    - `Taskfile.yml` includes-comment table contains `#   - identity (P4, real)`
    - `taskfiles/links.yml`'s `all:` aggregator contains `- task: identity:install` (and the desc string mentions P4)
    - `identity/README.md` has the H1 header `# identity`, the three required sections (`## Key files`, `## Adding a pattern`, `## References`), and the trailer `Satisfies DOCS-02 for identity/.`
    - `identity/README.md` is at least 40 lines (matches shell/README.md and taskfiles/README.md size)
    - `task lint` exits 0
    - `task --list | grep -q identity:install` succeeds (the task is visible from the root namespace)
    - `task install --dry-run` output (or trace mode) shows `identity:install` is invoked as part of `links:all`
    - [BLOCKING] After `task install` (or `task setup -- <machine> && task install`) on a real machine: `task identity:validate` exits 0 with the four assertion blocks emitting `check` lines for the symlink presence, git config user.email match (skipped silently if the gitdir is absent), ssh-add -L match (skipped silently if one-password-ssh = false or the pub-key file is a placeholder), and keys/ allowlist
  </acceptance_criteria>
  <done>Identity layer is fully wired: `task install` runs `task links:all` which runs `task identity:install` (the aggregator) which composes `identity:git` + `identity:ssh`. `task identity:validate` is callable from the root namespace and ready for Phase 8 to compose into `task validate`. The DOCS-02 README is in place. All five files (`Taskfile.yml`, `taskfiles/links.yml`, `identity/README.md`, and the two via Task 1) match the documented v2 conventions.</done>
</task>

</tasks>

<verification>
After both tasks land (and assuming Plans 01 + 02 have completed):

1. `zsh -n install/resolver.zsh` exits 0 (no regression from Plan 01).
2. `task lint` exits 0 (LINT-01 status:, LINT-02 template vars, LINT-03a aggregator markers, LINT-03b no bare ln, LINT-04 shell headers, LINT-07 zsh -n -- all pass).
3. `task manifest:test` exits 0 with `fixtures: 11 total, 11 passed, 0 failed` (Plan 03 fixtures still pass; identity.yml does not affect this).
4. `task manifest:validate -- --machine personal-laptop` exits 0 (the schema rules from Plan 01 still pass).
5. `task -t taskfiles/identity.yml --list` exits 0 and lists `install`, `git`, `ssh`, `validate`.
6. `task install` on the active machine completes end-to-end with no errors; re-running `task install` is a no-op (idempotency contract, LINT-01).
7. [BLOCKING] `task identity:validate` exits 0 with check-lines for the symlink-presence, git-config-email-match (or skip), ssh-add-list-match (or skip), and keys/-allowlist assertions.
8. (Machine-specific sanity checks) On a server: `cat ~/.config/git/server-include.config` shows the materialized wildcard; on a workstation: `test ! -f ~/.config/git/server-include.config` exits 0.
9. (Audit) `grep -rEn 'hostname|scutil' identity/ taskfiles/identity.yml` returns 0 lines (IDNT-05 hostname-literal audit; v2 identity layer is free of hostname dispatch).
10. (Audit) `grep -c 'ln -s' taskfiles/identity.yml` returns 0 (LINT-03b -- bare ln forbidden; all symlinks via _:safe-link).
</verification>

<success_criteria>
- `taskfiles/identity.yml` exists with the five subtasks and the manifest-driven symlink topology.
- `task install` lands the identity layer end-to-end; re-running is a no-op.
- `task identity:validate` exits 0 on the active machine (the BLOCKING task gate from the quality_gate).
- `identity/README.md` replaces the Phase 1 stub with the DOCS-02-shaped real README.
- Goal-backward truths IDNT-07 and IDNT-08 are observable from the validate task and the file structure respectively.
- Root `Taskfile.yml`'s install pipeline now flows through Phase 4: `install -> links:all -> identity:install -> identity:git + identity:ssh`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-identity-layer-git-ssh-per-machine/04-04-SUMMARY.md` per the standard summary template.
</output>

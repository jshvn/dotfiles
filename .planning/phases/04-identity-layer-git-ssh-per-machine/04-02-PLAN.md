---
phase: 04-identity-layer-git-ssh-per-machine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - identity/git/config
  - identity/git/ignore
  - identity/git/identities/personal
  - identity/git/identities/work
  - identity/git/identities/server-1
  - identity/git/identities/server-2
  - identity/git/identities/none
  - identity/ssh/config
  - identity/ssh/identities/personal
  - identity/ssh/identities/work
  - identity/ssh/identities/server-1
  - identity/ssh/identities/server-2
  - identity/ssh/identities/none
  - identity/ssh/keys/personal.pub
  - identity/ssh/keys/server-1.pub
  - identity/ssh/keys/server-2.pub
  - identity/ssh/keys/.gitignore
  - identity/ssh/cloudflared.zsh
autonomous: true
requirements:
  - IDNT-01
  - IDNT-02
  - IDNT-03
  - IDNT-04
  - IDNT-06
must_haves:
  truths:
    - "identity/git/config contains [includeIf gitdir/i:~/git/personal/] and [includeIf gitdir/i:~/git/work/]"
    - "identity/git/config contains [include] path = server-include.config (universal include; missing on workstations = silent no-op per D-08)"
    - "identity/git/config does NOT contain the v1 [includeIf gitdir/i:~/git/server/] block"
    - "identity/git/identities/ contains exactly five flat files: personal, work, server-1, server-2, none -- no profile-suffix filenames"
    - "identity/ssh/config contains exactly one Include directive: Include ~/.ssh/identities/active"
    - "identity/ssh/config does NOT contain Match exec blocks"
    - "identity/ssh/identities/ contains exactly five flat files: personal, work, server-1, server-2, none -- no profile-suffix filenames"
    - "identity/ssh/identities/personal references ~/.ssh/identities/keys/personal.pub (not the v1 flat path) and ~/.ssh/identities/cloudflared.zsh (not the v1 path)"
    - "identity/ssh/keys/ contains only *.pub files and .gitignore (no private keys)"
    - "identity/ssh/cloudflared.zsh uses $HOMEBREW_PREFIX and has set -euo pipefail"
  artifacts:
    - path: "identity/git/config"
      provides: "Main git config with universal includeIf blocks + server-include hook"
      contains: "gitdir/i:~/git/personal/"
    - path: "identity/git/identities/personal"
      provides: "Personal git identity (email + 1Password signing)"
      contains: "josh@vaughen.net"
    - path: "identity/git/identities/work"
      provides: "Work git identity (signing + email-TBD marker)"
      contains: "op-ssh-sign"
    - path: "identity/git/identities/server-1"
      provides: "Server-1 git identity (no signing)"
      contains: "server-1@jgrid.net"
    - path: "identity/git/identities/server-2"
      provides: "Server-2 git identity (no signing)"
      contains: "server-2@jgrid.net"
    - path: "identity/git/identities/none"
      provides: "No-op git identity (comment-only)"
    - path: "identity/ssh/config"
      provides: "Main SSH config with Include directive only (no Match exec)"
      contains: "Include ~/.ssh/identities/active"
    - path: "identity/ssh/identities/personal"
      provides: "Personal SSH identity (1Password agent + jgrid.net/plex.me hosts)"
      contains: "IdentityAgent"
    - path: "identity/ssh/identities/work"
      provides: "Work SSH identity (1Password agent placeholder)"
      contains: "IdentityAgent"
    - path: "identity/ssh/identities/server-1"
      provides: "Server-1 SSH identity (github.com deploy key)"
      contains: "id_ed25519_server-1"
    - path: "identity/ssh/identities/server-2"
      provides: "Server-2 SSH identity (github.com deploy key)"
      contains: "id_ed25519_server-2"
    - path: "identity/ssh/identities/none"
      provides: "No-op SSH identity (comment-only; symlink target when identity.ssh = none)"
    - path: "identity/ssh/keys/personal.pub"
      provides: "Personal SSH public key (port of v1)"
      contains: "ssh-ed25519"
    - path: "identity/ssh/keys/server-1.pub"
      provides: "Server-1 public key placeholder (filled at cutover)"
    - path: "identity/ssh/keys/server-2.pub"
      provides: "Server-2 public key placeholder (filled at cutover)"
    - path: "identity/ssh/keys/.gitignore"
      provides: "Allowlist - only *.pub files commit"
      contains: "!*.pub"
    - path: "identity/ssh/cloudflared.zsh"
      provides: "ProxyCommand wrapper for cloudflared tunnels"
      contains: "HOMEBREW_PREFIX"
  key_links:
    - from: "identity/git/config"
      to: "identity/git/identities/personal"
      via: "[includeIf gitdir/i:~/git/personal/] path = identities/personal"
      pattern: "path = identities/personal"
    - from: "identity/git/config"
      to: "(absent on workstations) server-include.config"
      via: "[include] path = server-include.config (silent no-op when missing per D-08)"
      pattern: "path = server-include.config"
    - from: "identity/ssh/config"
      to: "~/.ssh/identities/active"
      via: "Include directive"
      pattern: "Include ~/.ssh/identities/active"
    - from: "identity/ssh/identities/personal"
      to: "~/.ssh/identities/keys/personal.pub"
      via: "IdentityFile directive"
      pattern: "IdentityFile ~/.ssh/identities/keys/personal.pub"
    - from: "identity/ssh/identities/personal"
      to: "~/.ssh/identities/cloudflared.zsh"
      via: "ProxyCommand directive"
      pattern: "ProxyCommand ~/.ssh/identities/cloudflared.zsh"
---

<objective>
Materialize the entire identity content layer: one shared `identity/git/config`, one shared `identity/ssh/config`, five flat per-identity files on each side, the personal public key, two server pub-key placeholders, the cloudflared ProxyCommand wrapper, and the `.gitignore` allowlist. These are mostly direct ports of v1 source files with the documented divergences (drop `[includeIf gitdir:~/git/server/]`, drop `Match exec` blocks, add `[include] path = server-include.config`, update SSH key paths to the new scoped `~/.ssh/identities/keys/` subdirectory, add per-server git emails and IdentityFile paths).

Purpose: Static content lands first so Plan 04's taskfile has files to symlink to. Without this plan, `taskfiles/identity.yml`'s `_:safe-link` calls would point at non-existent SOURCE paths. Goal-backward truths IDNT-01..IDNT-04 + IDNT-06 are satisfied by these files alone (the gitconfig and sshconfig content is the contract; downstream symlinking just deploys it).
Output: An `identity/` tree that matches the structure documented in 04-RESEARCH.md "Recommended Project Structure" and 04-PATTERNS.md.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-CONTEXT.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-RESEARCH.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-PATTERNS.md
@CLAUDE.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create identity/git/ tree (config + ignore + five identity files)</name>
  <files>identity/git/config, identity/git/ignore, identity/git/identities/personal, identity/git/identities/work, identity/git/identities/server-1, identity/git/identities/server-2, identity/git/identities/none</files>
  <read_first>
    - git/config (v1 source - 56 lines - port verbatim except drop the [includeIf gitdir:~/git/server/] block and add [include] path = server-include.config; expand path = config-X to path = identities/X)
    - git/config-personal (v1 source - 12 lines - port verbatim into identity/git/identities/personal)
    - git/config-work (v1 source - 7 lines - port verbatim into identity/git/identities/work; resolve email per D-04 open question)
    - git/config-server (v1 source - 7 lines - port into BOTH server-1 and server-2 with per-server name/email rebrand)
    - git/ignore (v1 source - 3 lines - port verbatim)
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-PATTERNS.md sections "identity/git/config", "identity/git/identities/personal", "identity/git/identities/work", "identity/git/identities/server-1 / server-2", "identity/git/identities/none and identity/ssh/identities/none"
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-RESEARCH.md "Pattern 1: Git includeIf for workstation identity" (lines 281-333) and "Pattern 2: Server wildcard via missing-file-silent-no-op (D-08)" (lines 335-378)
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-CONTEXT.md D-01..D-04 + D-06 + D-08
  </read_first>
  <action>
Create the seven git-side identity files. Use the Write tool (one Write per file). All files use 4-space indentation under each section header (normalize v1's tab/space mix). Every file starts with a file-header comment block per project convention (Pattern S6) -- first line `# <relative-path> -- <one-line summary>.`.

(a) `identity/git/config` -- port v1 `git/config`. Header comment block, then in this order:
  - `[user] name = Josh Vaughen` (from v1)
  - `[includeIf "gitdir/i:~/git/personal/"] path = identities/personal` (was `path = config-personal` in v1)
  - `[includeIf "gitdir/i:~/git/work/"] path = identities/work` (was `path = config-work`; D-02 hardcodes the path; gitdir/i is case-insensitive per Pattern 1)
  - DROP the v1 `[includeIf "gitdir/i:~/git/server/"]` block entirely (D-08: server identity is per-machine, applied via wildcard not directory)
  - `[include] path = server-include.config` (D-08: universal include; absent on workstations = silent no-op per Pattern 2)
  - `[core]` block with `editor = code`, `excludesfile = ignore`, `pager = delta` (port verbatim from v1)
  - `[init] defaultBranch = main` (port from v1)
  - `[fetch] prune = true` (port from v1; keep the inline comment about auto-pruning deleted remote branches)
  - `[interactive] diffFilter = delta --color-only` (port from v1)
  - `[delta]` block (navigate, light=false, side-by-side, line-numbers, syntax-theme=Monokai Extended -- port verbatim from v1 lines 19-24)
  - `[status]` block (showUntrackedFiles=all, submoduleSummary=true -- port verbatim from v1 lines 25-27)
  - `[alias]` block (port the entire alias list from v1 lines 28-57 verbatim, normalize indentation to 4 spaces, keep the inline comments). The full alias list includes: aliases, tags, branches, remotes, undo, whois, whatis, fc, fm, contributors, whoami, hist, latest, quicklog.

(b) `identity/git/ignore` -- port v1 `git/ignore` verbatim (3 lines: `*~`, `.DS_Store`, `**/.claude/settings.local.json`). Header comment block on top.

(c) `identity/git/identities/personal` -- port v1 `git/config-personal` verbatim. Header comment block, then:
  - `[user]` block: `name = Josh Vaughen`, `email = josh@vaughen.net`, `signingkey = ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIMsU4L+sNRYKBy7p294G4YVbsjT4O4ewT9OTnKbfnfdT`
  - `[github] user = jshvn`
  - `[gpg] format = ssh`
  - `[gpg "ssh"] program = "/Applications/1Password.app/Contents/MacOS/op-ssh-sign"`
  - `[commit] gpgsign = true`

(d) `identity/git/identities/work` -- port v1 `git/config-work` verbatim. Per CONTEXT.md Deferred-Open-Questions (work email TBD): include an `email = # TODO: set work email before merge` line as a placeholder inside the `[user]` block (commented form keeps the file syntactically valid). Header comment block, then:
  - `[user]` block: `name = Josh Vaughen`, `# TODO: set work email before merge -- email = <work>`
  - `[gpg] format = ssh`
  - `[gpg "ssh"] program = "/Applications/1Password.app/Contents/MacOS/op-ssh-sign"`
  - `[commit] gpgsign = true`

(e) `identity/git/identities/server-1` -- new file; per D-06 + D-07. Header comment block, then:
  - `[user] name = Server-1`, `email = server-1@jgrid.net`
  - `[github] user = jshvn`
  - `[commit] gpgsign = false`

(f) `identity/git/identities/server-2` -- same shape as (e) with name=Server-2 and email=server-2@jgrid.net.

(g) `identity/git/identities/none` -- comment-only no-op file (per RESEARCH Open Question 2 and PATTERNS "identity/git/identities/none and identity/ssh/identities/none" section). Body:
  - Header comment block + a single explanatory comment paragraph stating this file is the no-op identity used when `identity.git = "none"`; never loaded via includeIf because no gitdir pattern matches it.

Constraints (CLAUDE.md + Pattern S6):
- All files use 4-space indentation under each section.
- File-header comment block at the top of every file (purpose, callers, side effects).
- No emojis.
- No AI attribution.
- Do NOT add per-machine logic to `identity/git/config` -- it is universal (D-02).
- The `[include] path = server-include.config` line is unconditional; the materialization of that file (or its absence) is the wave-2 plan-04 task.
  </action>
  <verify>
    <automated>cd $DOTFILEDIR && grep -q 'gitdir/i:~/git/personal/' identity/git/config && grep -q 'gitdir/i:~/git/work/' identity/git/config && grep -q 'path = server-include.config' identity/git/config && ! grep -q 'gitdir/i:~/git/server/' identity/git/config && [ "$(find identity/git/identities -maxdepth 1 -type f | wc -l | tr -d ' ')" = "5" ] && [ "$(find identity/git/identities -maxdepth 1 -type f -name 'config-*' | wc -l | tr -d ' ')" = "0" ] && grep -q 'josh@vaughen.net' identity/git/identities/personal && grep -q 'server-1@jgrid.net' identity/git/identities/server-1 && grep -q 'server-2@jgrid.net' identity/git/identities/server-2</automated>
  </verify>
  <acceptance_criteria>
    - `identity/git/config` contains `[includeIf "gitdir/i:~/git/personal/"]` and `[includeIf "gitdir/i:~/git/work/"]`
    - `identity/git/config` contains `[include] path = server-include.config` (D-08 universal include)
    - `identity/git/config` does NOT contain `gitdir/i:~/git/server/` (the v1 server includeIf block is removed)
    - `find identity/git/identities -maxdepth 1 -type f | wc -l` returns 5 (personal, work, server-1, server-2, none)
    - `find identity/git/identities -maxdepth 1 -type f -name 'config-*' | wc -l` returns 0 (no v1-style profile-suffix filenames -- IDNT-02)
    - `identity/git/identities/personal` contains `email = josh@vaughen.net` and `program = "/Applications/1Password.app/Contents/MacOS/op-ssh-sign"` and `gpgsign = true`
    - `identity/git/identities/work` contains `op-ssh-sign` and `gpgsign = true` (the work email is TBD; the TODO marker is acceptable per Open Question 1)
    - `identity/git/identities/server-1` contains `email = server-1@jgrid.net` and `gpgsign = false`
    - `identity/git/identities/server-2` contains `email = server-2@jgrid.net` and `gpgsign = false`
    - `identity/git/identities/none` exists and contains only comments (no `[user]` block, no `[commit]` block, etc.)
    - `identity/git/ignore` exists with the three v1 lines
    - Each new file starts with a `# identity/git/...` header comment line
  </acceptance_criteria>
  <done>Git-side identity content matches the structure in 04-RESEARCH.md "Recommended Project Structure" with the documented divergences from v1 (no server includeIf, universal `[include] path = server-include.config`, flat identities/ directory, per-server emails, no profile-suffix filenames).</done>
</task>

<task type="auto">
  <name>Task 2: Create identity/ssh/ tree (config + five identity files + three pub keys + cloudflared + .gitignore)</name>
  <files>identity/ssh/config, identity/ssh/identities/personal, identity/ssh/identities/work, identity/ssh/identities/server-1, identity/ssh/identities/server-2, identity/ssh/identities/none, identity/ssh/keys/personal.pub, identity/ssh/keys/server-1.pub, identity/ssh/keys/server-2.pub, identity/ssh/keys/.gitignore, identity/ssh/cloudflared.zsh</files>
  <read_first>
    - ssh/configs/config (v1 source - 20 lines - port with structural changes: drop the three Match exec blocks at lines 13-20 and replace with single Include ~/.ssh/identities/active)
    - ssh/configs/config-personal (v1 source - 20 lines - port verbatim except update two paths: IdentityFile from ~/.ssh/id_ed25519_personal.pub to ~/.ssh/identities/keys/personal.pub, and ProxyCommand from ~/.ssh/cloudflared.zsh to ~/.ssh/identities/cloudflared.zsh)
    - ssh/configs/config-work (v1 source - 9 lines - port verbatim)
    - ssh/configs/config-server (v1 source - 11 lines - port into BOTH server-1 and server-2 with IdentityFile path update from ~/.ssh/id_ed25519_server to ~/.ssh/id_ed25519_server-{1,2})
    - ssh/cloudflared.zsh (v1 source - 4 lines - port verbatim, add set -euo pipefail per CF-06 and header per Pattern S6)
    - ssh/keys/id_ed25519_personal.pub (v1 source - 1 line - copy verbatim to identity/ssh/keys/personal.pub)
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-PATTERNS.md sections "identity/ssh/config", "identity/ssh/identities/personal", "identity/ssh/identities/work", "identity/ssh/identities/server-1 / server-2", "identity/ssh/keys/server-1.pub / server-2.pub", "identity/ssh/keys/.gitignore", "identity/ssh/cloudflared.zsh"
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-RESEARCH.md "Pattern 3: SSH active symlink swap" (lines 379-431) and "Pattern 4: 1Password agent socket gate" (lines 433-465)
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-CONTEXT.md D-10..D-14, D-09, CF-06, CF-09
  </read_first>
  <action>
Create the eleven SSH-side files. Use the Write tool (one Write per file). Every file starts with a file-header comment block per project convention (Pattern S6).

(a) `identity/ssh/config` -- replaces v1 `ssh/configs/config` with structural change. Header comment block (explain the symlink target -> ~/.ssh/config; explain that identity selection happens via ~/.ssh/identities/active symlink driven by `taskfiles/identity.yml`), then:
  - `Host *` block with `SetEnv TERM=xterm-256color` (port verbatim from v1 lines 7-8)
  - One blank line
  - `Include ~/.ssh/identities/active` (replaces the three v1 Match exec blocks per D-10)
  - DROP the three v1 `Match exec "test \"$(cat ${XDG_CONFIG_HOME:-$HOME/.config}/dotfiles/profile 2>/dev/null)\" = 'X'"` blocks (lines 13-20). These are exactly the v1 bug class being structurally closed (CONCERNS.md `ssh/configs/config:13-20`).

(b) `identity/ssh/identities/personal` -- port v1 `ssh/configs/config-personal` with two path updates per D-13 and D-14. Header comment block, then:
  - `Host *` block: `IdentityAgent "~/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock"` (port verbatim)
  - `Host *.jgrid.net` block: `IdentityFile ~/.ssh/identities/keys/personal.pub` (was `~/.ssh/id_ed25519_personal.pub` in v1 per D-13), `IdentitiesOnly yes`, `User josh`, `ProxyCommand ~/.ssh/identities/cloudflared.zsh access ssh --hostname %h` (was `~/.ssh/cloudflared.zsh` in v1 per D-14), `RemoteCommand if [[ "$(uname)" = "Darwin" ]]; then cd ~/Git/personal/jgrid.net/; else cd ~/git/jgrid.net/; fi && exec $SHELL -l` (port verbatim), `RequestTTY yes`
  - `Host *.plex.me` block: same `IdentityFile`/`IdentitiesOnly`/`User`/`ProxyCommand` updates as above; no RemoteCommand or RequestTTY

(c) `identity/ssh/identities/work` -- port v1 `ssh/configs/config-work` verbatim. Header comment block, then:
  - `Host *` block: `IdentityAgent "~/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock"`
  - Inline comment `# Add work-specific host configurations below` (preserve the v1 placeholder)

(d) `identity/ssh/identities/server-1` -- port v1 `ssh/configs/config-server` with IdentityFile path update per D-06. Header comment block, then:
  - `Host github.com` block: `IdentityFile ~/.ssh/id_ed25519_server-1` (was `~/.ssh/id_ed25519_server` in v1), `IdentitiesOnly yes`, `AddKeysToAgent yes`

(e) `identity/ssh/identities/server-2` -- same shape as (d) with `IdentityFile ~/.ssh/id_ed25519_server-2`.

(f) `identity/ssh/identities/none` -- comment-only no-op file per RESEARCH Open Question 2 and Pitfall 5. Body: header comment block stating this is the no-op identity file linked when `identity.ssh = "none"`; it ensures the `Include ~/.ssh/identities/active` line in the main config resolves to a real file (avoids "Include of missing file" ambiguity across ssh versions).

(g) `identity/ssh/keys/personal.pub` -- copy v1 `ssh/keys/id_ed25519_personal.pub` verbatim. No header comment (key files are single-line ssh-key format; comments break parsers). The single line is:
  `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIMsU4L+sNRYKBy7p294G4YVbsjT4O4ewT9OTnKbfnfdT josh@vaughen.net`

(h) `identity/ssh/keys/server-1.pub` -- placeholder per D-09 + RESEARCH Open Question 5. Body (exactly two lines):
  - `# Replace this file with the contents of id_ed25519_server-1.pub generated at cutover.`
  - `# See docs/CUTOVER.md (Phase 8, DOCS-08).`

(i) `identity/ssh/keys/server-2.pub` -- same shape as (h) with `id_ed25519_server-2.pub` referenced.

(j) `identity/ssh/keys/.gitignore` -- belt-and-braces allowlist per IDNT-06 + RESEARCH Pitfall 10. Body:
  - `# identity/ssh/keys/.gitignore`
  - `# Allowlist: only .pub files (and this .gitignore) may live here.`
  - `# IDNT-06: private keys NEVER enter the repo.`
  - `*`
  - `!*.pub`
  - `!.gitignore`

(k) `identity/ssh/cloudflared.zsh` -- port v1 `ssh/cloudflared.zsh` per D-14 + Pattern S5. Body:
  - `#!/bin/zsh` shebang
  - Header comment block (Pattern S6): purpose ("ProxyCommand wrapper for cloudflared tunnels invoked from identity/ssh/identities/personal"), callers, side effects (exec replaces this shell with cloudflared)
  - `set -euo pipefail` (LINT-04 + CF-06)
  - `exec "$HOMEBREW_PREFIX/bin/cloudflared" "$@"` (port verbatim; no hardcoded /opt/homebrew per CLAUDE.md)

Constraints:
- No emojis anywhere.
- No AI attribution.
- File-header comment block on every file except `personal.pub` (single-line key file).
- `cloudflared.zsh` must use `$HOMEBREW_PREFIX` (not `/opt/homebrew`); the variable is exported via Phase 3's `.zprofile` at runtime.
- Do NOT create `identity/ssh/keys/server-{1,2}.pub` as empty files -- they must contain the two-line placeholder header so `task identity:validate`'s `[[ ! -s "$expected_pub" ]]` check (RESEARCH validate sketch line 1104) sees a non-empty file but the body lacks a real `ssh-ed25519` line.
  </action>
  <verify>
    <automated>cd $DOTFILEDIR && grep -q 'Include ~/.ssh/identities/active' identity/ssh/config && ! grep -q 'Match exec' identity/ssh/config && [ "$(find identity/ssh/identities -maxdepth 1 -type f | wc -l | tr -d ' ')" = "5" ] && [ "$(find identity/ssh/identities -maxdepth 1 -type f -name 'config-*' | wc -l | tr -d ' ')" = "0" ] && grep -q '~/.ssh/identities/keys/personal.pub' identity/ssh/identities/personal && grep -q '~/.ssh/identities/cloudflared.zsh' identity/ssh/identities/personal && grep -q 'id_ed25519_server-1' identity/ssh/identities/server-1 && grep -q 'id_ed25519_server-2' identity/ssh/identities/server-2 && [ "$(find identity/ssh/keys -maxdepth 1 -type f -not -name '*.pub' -not -name '.gitignore' | wc -l | tr -d ' ')" = "0" ] && grep -q 'set -euo pipefail' identity/ssh/cloudflared.zsh && grep -q '$HOMEBREW_PREFIX' identity/ssh/cloudflared.zsh && zsh -n identity/ssh/cloudflared.zsh</automated>
  </verify>
  <acceptance_criteria>
    - `identity/ssh/config` contains exactly one `Include ~/.ssh/identities/active` line (IDNT-03)
    - `identity/ssh/config` does NOT contain any `Match exec` directives (v1 bug class structurally closed)
    - `find identity/ssh/identities -maxdepth 1 -type f | wc -l` returns 5 (personal, work, server-1, server-2, none) -- IDNT-04
    - `find identity/ssh/identities -maxdepth 1 -type f -name 'config-*' | wc -l` returns 0 (no profile-suffix filenames -- IDNT-04)
    - `identity/ssh/identities/personal` references `~/.ssh/identities/keys/personal.pub` (D-13 scoped path) and `~/.ssh/identities/cloudflared.zsh` (D-14 scoped path)
    - `identity/ssh/identities/server-1` references `~/.ssh/id_ed25519_server-1` (per-server IdentityFile)
    - `identity/ssh/identities/server-2` references `~/.ssh/id_ed25519_server-2`
    - `find identity/ssh/keys -maxdepth 1 -type f -not -name '*.pub' -not -name '.gitignore' | wc -l` returns 0 (IDNT-06)
    - `identity/ssh/keys/personal.pub` contains the v1 personal public-key line verbatim
    - `identity/ssh/keys/server-1.pub` and `server-2.pub` exist with the two-line placeholder header (file size > 0, no actual `ssh-ed25519 ...` line)
    - `identity/ssh/keys/.gitignore` contains `*`, `!*.pub`, and `!.gitignore`
    - `identity/ssh/cloudflared.zsh` contains `set -euo pipefail` and `$HOMEBREW_PREFIX` (no hardcoded `/opt/homebrew`)
    - `zsh -n identity/ssh/cloudflared.zsh` exits 0
    - `grep -rEn 'hostname|scutil' identity/` returns 0 lines (IDNT-05 hostname-literal audit baseline)
  </acceptance_criteria>
  <done>SSH-side identity content matches the architecture in 04-RESEARCH.md "Pattern 3" with the documented v1->v2 deltas (single Include line, scoped key/cloudflared paths, per-server IdentityFile entries, allowlist gitignore). The `identity/` tree is now structurally complete; Plan 04's taskfile can symlink these files into ~/.config/git/ and ~/.ssh/ with confidence.</done>
</task>

</tasks>

<verification>
After both tasks land:

1. `zsh -n identity/ssh/cloudflared.zsh` exits 0 (LINT-07 syntax check).
2. `find identity/git/identities -maxdepth 1 -type f -name 'config-*' | wc -l` returns 0 (IDNT-02 negative check).
3. `find identity/ssh/identities -maxdepth 1 -type f -name 'config-*' | wc -l` returns 0 (IDNT-04 negative check).
4. `find identity/ssh/keys -maxdepth 1 -type f -not -name '*.pub' -not -name '.gitignore'` exits 0 with empty stdout (IDNT-06).
5. `grep -rEn 'hostname|scutil' identity/` returns 0 lines (IDNT-05 hostname-literal audit; the identity directory must contain ZERO hostname dispatch).
6. `git -c include.path="$PWD/identity/git/identities/personal" config -f /dev/null user.email` (offline includeIf-free check) returns `josh@vaughen.net`.
7. `task lint` exits 0 (the new `cloudflared.zsh` passes `lint:shell-headers` and `lint:syntax`).

These checks are exercised directly by the acceptance_criteria grep commands; no additional automation is needed.
</verification>

<success_criteria>
- Every file listed in `files_modified` exists with the documented content.
- Goal-backward truths IDNT-01..IDNT-04 + IDNT-06 are observable from file content alone (no symlinking required at this point).
- The `identity/` tree is self-contained; nothing outside it has been touched (no taskfile edits, no manifest edits, no symlink creation).
- `task lint` passes (cloudflared.zsh is the only new executable .zsh; it carries `set -euo pipefail`).
</success_criteria>

<output>
After completion, create `.planning/phases/04-identity-layer-git-ssh-per-machine/04-02-SUMMARY.md` per the standard summary template.
</output>

---
phase: 04-identity-layer-git-ssh-per-machine
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - manifests/test/fixtures/_invalid-identity-without-opssh/machine.toml
  - manifests/test/fixtures/_invalid-identity-without-opsign/machine.toml
  - manifests/test/fixtures/_invalid-bad-identity/machine.toml
  - taskfiles/manifest.yml
autonomous: true
requirements:
  - IDNT-05
must_haves:
  truths:
    - "manifest:test runs three new negative fixtures and they all reject as expected"
    - "Each new negative fixture trips a specific validator error: cross-field rule (opssh), cross-field rule (opsign), or enum rejection"
    - "manifest:test summary reports negative_count=5 and total=11 (six positive + five negative)"
    - "No regression in existing six positive fixtures or two pre-existing negative fixtures"
  artifacts:
    - path: "manifests/test/fixtures/_invalid-identity-without-opssh/machine.toml"
      provides: "Negative fixture: identity.ssh = personal AND features.one-password-ssh = false"
      contains: "identity"
    - path: "manifests/test/fixtures/_invalid-identity-without-opsign/machine.toml"
      provides: "Negative fixture: identity.git = personal AND features.one-password-signing = false"
      contains: "identity"
    - path: "manifests/test/fixtures/_invalid-bad-identity/machine.toml"
      provides: "Negative fixture: identity.ssh = alice (enum violation)"
      contains: "alice"
    - path: "taskfiles/manifest.yml"
      provides: "manifest:test extended with three new negative-fixture blocks; negative_count bumped to 5"
      contains: "_invalid-identity-without-opssh"
  key_links:
    - from: "taskfiles/manifest.yml manifest:test"
      to: "install/resolver.zsh validate_manifest()"
      via: "cp fixture into MACHINES_DIR; invoke resolver --validate-only --machine <name>; grep stderr for expected error fragment"
      pattern: "resolver.zsh --validate-only --machine _invalid-identity-without-opssh"
---

<objective>
Author three new negative test fixtures and extend `manifest:test` so the resolver's Plan 01 cross-field rules and expanded enum are covered by automated tests. Together with the pre-existing `_invalid-missing-desc` and `_invalid-bad-os` fixtures, `manifest:test` now exercises all five validator failure paths.

Purpose: Plan 01 adds three new validator failure modes (cross-field opssh, cross-field opsign, expanded enum reject). Without fixtures, those failure paths have no regression coverage; a future refactor could silently relax them. Negative fixtures are the contract that pins resolver behavior.
Output: Three TOML fixtures + `manifest:test` extended with three new blocks (matching the existing `_invalid-missing-desc` / `_invalid-bad-os` cp+resolver+grep+rm pattern) + `negative_count` bumped from 2 to 5.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-CONTEXT.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-RESEARCH.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-PATTERNS.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-01-SUMMARY.md
@CLAUDE.md
@.claude/CLAUDE.md

<interfaces>
<!-- Required reading. The manifest:test task at lines 277-412 is the pattern to extend. -->

From taskfiles/manifest.yml manifest:test (lines 277-412): The existing negative-fixture pattern uses:
  - `neg_copies=()` array + `cleanup_neg() { for f in ...; do rm -f "$f"; done }` + `trap cleanup_neg EXIT` (lines 287-301) -- the CR-01 fix block
  - For each fixture: `neg_dir="${fix_dir}/<name>"`, `tmp_neg="{{.MACHINES_DIR}}/<name>.toml"`, `neg_copies+=("$tmp_neg")` BEFORE `cp`, then resolver `--validate-only` capturing stderr, then `rm -f`, then `grep -q '<fragment>'` for the expected stderr fragment
  - `negative_count=2` constant at line 404 -- this is the line bumped to 5

From install/resolver.zsh (after Plan 01 edits): the three new validator error messages are:
  - `identity.${ident_key} must be one of personal|work|server-1|server-2|none; got: ${ident_val}` (enum case)
  - `identity.ssh = "${identity_ssh}" requires features.one-password-ssh = true` (cross-field opssh)
  - `identity.git = "${identity_git}" requires features.one-password-signing = true` (cross-field opsign)

From manifests/test/fixtures/_invalid-missing-desc/machine.toml and _invalid-bad-os/machine.toml (existing analogs): the fixture shape -- top comment naming what is invalid + required-field skeleton + the specific bad value.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author three negative-fixture TOML files</name>
  <files>manifests/test/fixtures/_invalid-identity-without-opssh/machine.toml, manifests/test/fixtures/_invalid-identity-without-opsign/machine.toml, manifests/test/fixtures/_invalid-bad-identity/machine.toml</files>
  <read_first>
    - manifests/test/fixtures/_invalid-missing-desc/machine.toml (16 lines - the existing analog; mirror the shape)
    - manifests/test/fixtures/_invalid-bad-os/machine.toml (19 lines - the second analog)
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-PATTERNS.md sections "manifests/test/fixtures/_invalid-identity-without-opssh/machine.toml (NEW)", "manifests/test/fixtures/_invalid-identity-without-opsign/machine.toml (NEW)", "manifests/test/fixtures/_invalid-bad-identity/machine.toml (NEW)" for the verbatim fixture contents
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-CONTEXT.md D-16 (cross-field rules) + D-05 (enum)
  </read_first>
  <action>
Create three new fixture directories under `manifests/test/fixtures/`, each containing a single `machine.toml` file. Match the v1-style fixture shape from the two existing analogs (top comment + `schema_version`, `[meta]`, `[platform]`, `[features]`, `[packages.brew]`, `[identity]` blocks).

(a) `manifests/test/fixtures/_invalid-identity-without-opssh/machine.toml` -- trips the D-16 opssh cross-field rule. Content (per PATTERNS section "manifests/test/fixtures/_invalid-identity-without-opssh/machine.toml"):
  - Top comment: `# negative fixture -- identity.ssh = "personal" but features.one-password-ssh = false` + a second comment line `# validator must reject with cross-field error`
  - `schema_version = 1`
  - `[meta] description = "negative fixture: identity-ssh-without-opssh"`
  - `[platform] os = "darwin"`
  - `[features]` block with `one-password-ssh = false` and `one-password-signing = true` (the latter ensures we ONLY trip the opssh rule, not opsign)
  - `[packages.brew] bundles = ["core"]`
  - `[identity] git = "none"` and `ssh = "personal"` (the ssh value trips the rule; the git value is benign)

(b) `manifests/test/fixtures/_invalid-identity-without-opsign/machine.toml` -- trips the D-16 opsign cross-field rule. Content:
  - Top comment: `# negative fixture -- identity.git = "personal" but features.one-password-signing = false` + `# validator must reject with cross-field error`
  - `schema_version = 1`
  - `[meta] description = "negative fixture: identity-git-without-opsign"`
  - `[platform] os = "darwin"`
  - `[features]` block with `one-password-ssh = true` and `one-password-signing = false` (the former ensures we ONLY trip the opsign rule, not opssh)
  - `[packages.brew] bundles = ["core"]`
  - `[identity] git = "personal"` and `ssh = "none"`

(c) `manifests/test/fixtures/_invalid-bad-identity/machine.toml` -- trips the D-05 enum rejection with an unknown value. Content:
  - Top comment: `# negative fixture -- identity.ssh = "alice"; validator must reject (enum check)`
  - `schema_version = 1`
  - `[meta] description = "negative fixture: bad-identity-ssh enum value"`
  - `[platform] os = "darwin"`
  - `[features]` block (empty body, just the section header)
  - `[packages.brew] bundles = ["core"]`
  - `[identity] git = "none"` and `ssh = "alice"` (alice is not in the five-value enum)

Constraints:
- TOML formatting matches the existing fixture style (no indentation on keys; one blank line between section blocks).
- No emojis.
- Each fixture file lives in its own directory (`_invalid-*/machine.toml`) matching the `manifests/test/fixtures/` layout (the convention is one fixture per directory; `manifest:test` loops over `_invalid-*` directories).
- All three fixtures declare every required field (`meta.description`, `platform.os = "darwin"`, `[features]` table, `packages.brew.bundles = ["core"]`, `identity.git`, `identity.ssh`). This isolates the failure to ONE rule per fixture -- the validator should reject for the specific cross-field/enum reason, not for a missing-field reason that would mask the test target.
  </action>
  <verify>
    <automated>cd $DOTFILEDIR && test -f manifests/test/fixtures/_invalid-identity-without-opssh/machine.toml && test -f manifests/test/fixtures/_invalid-identity-without-opsign/machine.toml && test -f manifests/test/fixtures/_invalid-bad-identity/machine.toml && grep -q 'ssh = "personal"' manifests/test/fixtures/_invalid-identity-without-opssh/machine.toml && grep -q 'one-password-ssh = false' manifests/test/fixtures/_invalid-identity-without-opssh/machine.toml && grep -q 'git = "personal"' manifests/test/fixtures/_invalid-identity-without-opsign/machine.toml && grep -q 'one-password-signing = false' manifests/test/fixtures/_invalid-identity-without-opsign/machine.toml && grep -q 'ssh = "alice"' manifests/test/fixtures/_invalid-bad-identity/machine.toml</automated>
  </verify>
  <acceptance_criteria>
    - All three `machine.toml` files exist under `manifests/test/fixtures/_invalid-*/`
    - `_invalid-identity-without-opssh/machine.toml` has `identity.ssh = "personal"` AND `features.one-password-ssh = false` AND `features.one-password-signing = true` (so only the opssh rule fires)
    - `_invalid-identity-without-opsign/machine.toml` has `identity.git = "personal"` AND `features.one-password-signing = false` AND `features.one-password-ssh = true` (so only the opsign rule fires)
    - `_invalid-bad-identity/machine.toml` has `identity.ssh = "alice"` (an unknown enum value)
    - Each file is a valid TOML document (yq can parse it): `yq '.' manifests/test/fixtures/_invalid-*/machine.toml` exits 0 for all three
    - Each file declares every required field (description, platform.os=darwin, features, packages.brew.bundles, identity.git, identity.ssh) so the validator's rejection is unambiguously caused by the cross-field/enum rule, not by a missing-field rule
    - Manual gate check (executor runs once): `DOTFILEDIR=$PWD XDG_STATE_HOME=$HOME/.local/state zsh install/resolver.zsh --validate-only --machine _invalid-identity-without-opssh` exits 1 -- but only AFTER Task 2 of this plan has copied the fixture into MACHINES_DIR (manifest:test handles this automatically; standalone, the executor can verify by `cp manifests/test/fixtures/_invalid-identity-without-opssh/machine.toml manifests/machines/_invalid-identity-without-opssh.toml; zsh install/resolver.zsh --validate-only --machine _invalid-identity-without-opssh 2>&1; rm -f manifests/machines/_invalid-identity-without-opssh.toml`). The stderr must include `one-password-ssh`.
  </acceptance_criteria>
  <done>Three new negative fixtures exist under `manifests/test/fixtures/`, each isolating exactly one validator failure mode (opssh cross-field, opsign cross-field, enum rejection).</done>
</task>

<task type="auto">
  <name>Task 2: Extend manifest:test with three new negative-fixture blocks and bump negative_count to 5</name>
  <files>taskfiles/manifest.yml</files>
  <read_first>
    - taskfiles/manifest.yml lines 277-412 (the full `manifest:test` task; specifically the `_invalid-missing-desc` block at lines 361-375, the `_invalid-bad-os` block at lines 379-393, and the `negative_count=2` line at 404)
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-PATTERNS.md section "taskfiles/manifest.yml (EDIT manifest:test; add three negative-fixture runs)" for the exact code-block shape to copy
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-VALIDATION.md "Wave 0 Requirements" section confirming `negative_count` becomes 5
  </read_first>
  <action>
Edit `taskfiles/manifest.yml`'s `manifest:test` task to add three new negative-fixture blocks, then bump the `negative_count` constant.

(a) Insert three new fixture blocks AFTER the existing `_invalid-bad-os` block (which ends at line 393) and BEFORE the `# ---` summary divider at line 395. Each block copies the exact shape of the `_invalid-missing-desc` block (lines 361-375):
  - `neg_dir="${fix_dir}/<fixture-name>"`
  - `tmp_neg="{{.MACHINES_DIR}}/<fixture-name>.toml"`
  - `neg_copies+=("$tmp_neg")` (REGISTER BEFORE `cp` -- CR-01 contract)
  - `cp "$neg_dir/machine.toml" "$tmp_neg"`
  - `neg_stderr=$(DOTFILEDIR="{{.DOTFILEDIR}}" XDG_STATE_HOME="{{.XDG_STATE_HOME}}" zsh "{{.DOTFILEDIR}}/install/resolver.zsh" --validate-only --machine <fixture-name> 2>&1 >/dev/null || true)`
  - `rm -f "$tmp_neg"`
  - `if echo "$neg_stderr" | grep -q '<expected-fragment>'; then check "..."; else cross "..."; echo "stderr was: $neg_stderr" >&2; failures=$(( failures + 1 )); fi`

The three new blocks (in this order, each preceded by a one-line `# ` comment naming the fixture purpose):

Block A -- `_invalid-identity-without-opssh`:
  - Fixture name: `_invalid-identity-without-opssh`
  - Comment: `# _invalid-identity-without-opssh: cross-field rule (D-16); stderr must contain "one-password-ssh".`
  - Grep fragment: `'one-password-ssh'` (the resolver's error message reads `identity.ssh = "personal" requires features.one-password-ssh = true`)

Block B -- `_invalid-identity-without-opsign`:
  - Fixture name: `_invalid-identity-without-opsign`
  - Comment: `# _invalid-identity-without-opsign: cross-field rule (D-16); stderr must contain "one-password-signing".`
  - Grep fragment: `'one-password-signing'` (the resolver's error message reads `identity.git = "personal" requires features.one-password-signing = true`)

Block C -- `_invalid-bad-identity`:
  - Fixture name: `_invalid-bad-identity`
  - Comment: `# _invalid-bad-identity: expanded enum (D-05); stderr must contain "server-1|server-2|none" or the literal "alice" rejection.`
  - Grep fragment: `'server-1|server-2|none'` (the resolver's expanded error message reads `identity.ssh must be one of personal|work|server-1|server-2|none; got: alice`)

(b) Update the constant at line 404: change `negative_count=2` to `negative_count=5` (two existing + three new).

Constraints:
- Maintain the `neg_copies+=("$tmp_neg")` BEFORE `cp` ordering -- CR-01 contract (the EXIT trap must be able to clean up the fixture even if cp fails). The PATTERNS section is explicit on this point.
- Do NOT remove `|| true` from the resolver invocation -- the resolver intentionally exits 1, and the surrounding `set -e`-context-equivalent inside the cmd block would otherwise abort the test runner before we can grep stderr.
- All three blocks use the same `grep -q` matcher form (not `grep -E`, not `grep -F`) -- consistent with the existing two blocks.
- Each grep fragment uses single quotes (`'one-password-ssh'`, etc.) -- the literal-string form matches the existing two blocks' style.
- Do NOT touch the positive-fixture loop (`for fix in "${fix_dir}"/[0-9][0-9]-*`); it auto-discovers fixtures.
- Do NOT touch the `cleanup_neg()` function definition or its trap -- the new fixtures are picked up automatically by the `neg_copies` array population.
  </action>
  <verify>
    <automated>cd $DOTFILEDIR && task -t taskfiles/manifest.yml manifest:test 2>&1 | tee /tmp/manifest-test.out && grep -q 'fixture: _invalid-identity-without-opssh -- correctly rejected' /tmp/manifest-test.out && grep -q 'fixture: _invalid-identity-without-opsign -- correctly rejected' /tmp/manifest-test.out && grep -q 'fixture: _invalid-bad-identity -- correctly rejected' /tmp/manifest-test.out && grep -q 'fixtures: 11 total, 11 passed, 0 failed' /tmp/manifest-test.out</automated>
  </verify>
  <acceptance_criteria>
    - `taskfiles/manifest.yml` contains three new fixture-handling code blocks (one per new fixture name), each in the exact `neg_copies+=...; cp; resolver --validate-only; rm -f; grep -q` shape used by the existing two blocks
    - `taskfiles/manifest.yml` has exactly one line `negative_count=5` (the previous value 2 is replaced, not duplicated)
    - `task -t taskfiles/manifest.yml manifest:test` exits 0
    - The output includes the three new `check "fixture: <name> -- correctly rejected"` lines (one per new fixture)
    - The summary line reads `fixtures: 11 total, 11 passed, 0 failed` (six positive + five negative = 11)
    - After the run, `find manifests/machines/_invalid-* -maxdepth 1 -type f 2>/dev/null | wc -l` returns 0 (no fixture leakage from CR-01 cleanup)
    - `task lint` exits 0 (the new YAML cmd-block content does not introduce LINT-02 violations -- all `{{.X}}` template vars are used correctly; no bare `$X` for go-task vars)
  </acceptance_criteria>
  <done>manifest:test exercises five negative fixtures (two pre-existing + three new). Each new fixture isolates one Plan-01 validator behavior (opssh cross-field, opsign cross-field, enum rejection) and asserts the expected stderr fragment. The negative_count constant accurately reflects the fixture count so future-fixture additions only require touching the constant.</done>
</task>

</tasks>

<verification>
After both tasks land:

1. `task -t taskfiles/manifest.yml manifest:test` exits 0 with output: `fixtures: 11 total, 11 passed, 0 failed`.
2. The three new fixture rejection lines appear in the output (`check "fixture: _invalid-identity-without-opssh -- correctly rejected (one-password-ssh in stderr)"`, etc.).
3. `task lint` exits 0 (no taskfile-lint regressions from the new code blocks).
4. `find manifests/machines/_invalid-* 2>/dev/null` returns nothing (CR-01 fixture cleanup verified -- no leakage).
5. (Sanity) `task -t taskfiles/manifest.yml manifest:test` is idempotent: running it twice produces the same output (the EXIT trap cleans up between runs).
</verification>

<success_criteria>
- All five validator failure modes now have automated coverage: missing-description, bad-os-enum, opssh-cross-field, opsign-cross-field, identity-enum-rejection.
- `negative_count=5` and `total=11` in the manifest:test summary.
- No regression in the six positive fixtures or the two pre-existing negative fixtures.
- IDNT-05 negative-test coverage is fully realized (the goal-backward "manifest:validate rejects mismatched configurations" truth is verified by automated tests).
</success_criteria>

<output>
After completion, create `.planning/phases/04-identity-layer-git-ssh-per-machine/04-03-SUMMARY.md` per the standard summary template.
</output>

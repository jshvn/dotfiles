---
phase: 04-identity-layer-git-ssh-per-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - install/resolver.zsh
  - manifests/defaults.toml
  - manifests/machines/personal-laptop.toml
  - manifests/machines/work-laptop.toml
  - manifests/machines/server-1.toml
  - manifests/machines/server-2.toml
  - docs/MANIFEST.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - IDNT-05
must_haves:
  truths:
    - "Resolver accepts identity enum values: personal, work, server-1, server-2, none"
    - "Resolver rejects identity.ssh in {personal, work} when features.one-password-ssh is not true"
    - "Resolver rejects identity.git in {personal, work} when features.one-password-signing is not true"
    - "manifests/defaults.toml declares features.one-password-signing = false"
    - "server-1.toml has identity.git = identity.ssh = server-1; server-2.toml has both = server-2"
    - "personal-laptop.toml and work-laptop.toml declare features.one-password-signing = true"
    - "docs/MANIFEST.md schema table reflects the five-value enum and the new feature row"
  artifacts:
    - path: "install/resolver.zsh"
      provides: "Expanded identity enum + two cross-field validation rules"
      contains: "personal|work|server-1|server-2|none"
    - path: "manifests/defaults.toml"
      provides: "Baseline one-password-signing flag"
      contains: "one-password-signing"
    - path: "manifests/machines/server-1.toml"
      provides: "Server-1 identity values"
      contains: "git = \"server-1\""
    - path: "manifests/machines/server-2.toml"
      provides: "Server-2 identity values"
      contains: "git = \"server-2\""
    - path: "docs/MANIFEST.md"
      provides: "Updated schema reference (enum + feature row)"
      contains: "server-1"
  key_links:
    - from: "install/resolver.zsh validate_manifest()"
      to: "manifests/machines/*.toml"
      via: "yq cross-field reads of identity.{git,ssh} and features.{one-password-ssh,one-password-signing}"
      pattern: "yq -r '.features.\"one-password-ssh\""
---

<objective>
Extend the manifest schema layer to recognize per-server identity values and split-flag 1Password semantics (D-05, D-15, D-16). This is the schema-layer foundation for Phase 4: the resolver gains a five-value identity enum and two cross-field rules; the four machine TOMLs and defaults.toml land their Phase-4 values; docs/MANIFEST.md mirrors the new schema; REQUIREMENTS.md IDNT-05 wording is corrected.

Purpose: Without these edits, `task setup -- server-1` would reject the new `identity.{git,ssh} = "server-1"` values; downstream `taskfiles/identity.yml` (Plan 04) cannot trust `{{.MANIFEST.identity.ssh}}` to be in {personal, work, server-1, server-2, none}; Plan 03 cannot author negative fixtures that prove the validator works.
Output: A resolver that accepts the v2 schema; manifests that declare it; documentation that describes it.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-CONTEXT.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-RESEARCH.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-PATTERNS.md
@.planning/phases/04-identity-layer-git-ssh-per-machine/04-VALIDATION.md
@CLAUDE.md
@.claude/CLAUDE.md

<interfaces>
<!-- Required reading. Resolver.zsh validate_manifest() is the function being extended in place; the surrounding helpers (error, warn, VALIDATE_ERRORS, yq read pattern) are the contract. -->

From install/resolver.zsh (lines 91-215): The existing `validate_manifest()` function uses:
  - `local errors=0` counter, returned via `typeset -gi VALIDATE_ERRORS` and exit status 1/0
  - `error "..."` calls from messages.zsh -> stderr
  - `yq -r ".identity.${ident_key} // \"\"" "$machine_file" 2>/dev/null || echo ""` for reads
  - `local -a required_strings` loop already includes `identity.git` and `identity.ssh` (missing-empty already counted)
  - The identity enum case-statement at lines 197-208 is the exact block to expand
  - WR-07 contract: count via VALIDATE_ERRORS global, exit 1 on errors > 0
  - `local path` is forbidden (zsh ties `path` to $PATH); use `field_path` or other names

From install/resolver.zsh emit_unknown_key_warnings() whitelist (lines 226-237):
  whitelist includes `"features"` as a prefix-match entry. New `features.one-password-signing` falls under that prefix; NO whitelist edit needed (PATTERNS section "Also expand emit_unknown_key_warnings() whitelist" confirms).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand resolver identity enum and add cross-field validation</name>
  <files>install/resolver.zsh</files>
  <read_first>
    - install/resolver.zsh (full file - the entire validate_manifest() function lines 91-215; the messages.zsh sourcing block lines 32-40; the emit_unknown_key_warnings() whitelist lines 222-272)
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-RESEARCH.md "Cross-field validation in install/resolver.zsh" section (lines 1130-1173) for the exact code blocks to paste
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-PATTERNS.md `install/resolver.zsh` section (the "Existing identity enum case-statement" + "Phase 4 change" + "Cross-field validation pattern to add" code blocks)
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-CONTEXT.md D-05, D-15, D-16
  </read_first>
  <action>
Per D-05 + D-16, edit `install/resolver.zsh validate_manifest()` (lines 197-208) in place:

1. Expand the identity enum case-statement to accept the five-value enum. Replace the existing `case "$ident_val" in personal|work|none) ;; *) error "identity.${ident_key} must be one of personal|work|none; got: ${ident_val}"` block with the five-value variant: accept `personal|work|server-1|server-2|none` and update the error string to `must be one of personal|work|server-1|server-2|none`. Keep the surrounding `for ident_key in git ssh` loop, the empty-value `continue`, and the `errors=$(( errors + 1 ))` accounting unchanged.

2. After the expanded enum case-statement and BEFORE the `VALIDATE_ERRORS=$errors` assignment at line 210, add the D-16 cross-field validation block. Read `identity_ssh`, `identity_git`, `opssh`, `opsign` via the same yq pattern used elsewhere in this function (`yq -r '<path> // "<fallback>"' "$machine_file" 2>/dev/null || echo "<fallback>"`). The `opssh` and `opsign` reads must use quoted-key form: `yq -r '.features."one-password-ssh" // false'` and `yq -r '.features."one-password-signing" // false'` (kebab-case keys need quoted form in yq path expressions).

3. Add two case statements with `personal|work)` arms only (servers and `none` are exempt per D-16):
   - If `identity_ssh ∈ {personal, work}` and `$opssh != "true"`, emit `error "identity.ssh = \"${identity_ssh}\" requires features.one-password-ssh = true"` and increment errors.
   - If `identity_git ∈ {personal, work}` and `$opsign != "true"`, emit `error "identity.git = \"${identity_git}\" requires features.one-password-signing = true"` and increment errors.

4. Add comment immediately above the new block: `# D-16: cross-field rules. identity.ssh in {personal, work} requires features.one-password-ssh = true; identity.git in {personal, work} requires features.one-password-signing = true`

Constraints (per CLAUDE.md + Pattern S5/S7):
- Do NOT add `local path` (zsh `path` is tied to `$PATH`). Use distinct variable names (`identity_ssh`, `identity_git`, `opssh`, `opsign`).
- Keep `set -euo pipefail` semantics. Every yq invocation has `2>/dev/null || echo "<fallback>"` to keep `set -u`/`set -e` happy on missing keys.
- All error output via the existing `error` function from messages.zsh (already sourced at file top).
- Do NOT touch `emit_unknown_key_warnings()` -- its whitelist already covers `features.*` via prefix match (PATTERNS section confirms `features` is a prefix-match entry).
- Do NOT change the WR-07 contract (`VALIDATE_ERRORS` global + return 0/1).
  </action>
  <verify>
    <automated>cd $DOTFILEDIR && zsh -n install/resolver.zsh && grep -q 'personal|work|server-1|server-2|none' install/resolver.zsh && grep -q 'requires features.one-password-ssh = true' install/resolver.zsh && grep -q 'requires features.one-password-signing = true' install/resolver.zsh</automated>
  </verify>
  <acceptance_criteria>
    - `zsh -n install/resolver.zsh` exits 0 (no parse errors)
    - `grep -c 'personal|work|server-1|server-2|none' install/resolver.zsh` returns at least 2 (one in case arm, one in error message)
    - `grep -c 'requires features.one-password-ssh = true' install/resolver.zsh` returns 1
    - `grep -c 'requires features.one-password-signing = true' install/resolver.zsh` returns 1
    - The file still contains exactly one `VALIDATE_ERRORS=$errors` assignment (no duplication)
    - `grep -c 'local path' install/resolver.zsh | grep -q '^0$'` -- no shadowing of $PATH
    - `task -t taskfiles/manifest.yml manifest:validate -- --machine personal-laptop` exits 0 after Task 2 lands (deferred; tested in plan-level verification)
  </acceptance_criteria>
  <done>The resolver accepts the five-value enum and rejects identity-flag mismatches with the exact error messages specified by D-16. Existing manifest:validate behavior on currently-passing machines is preserved (personal-laptop has both flags true today; work-laptop has only one-password-ssh=true and will fail validation until Task 2 lands its one-password-signing addition -- this is the expected dependency relationship within this plan).</done>
</task>

<task type="auto">
  <name>Task 2: Update defaults.toml, four machine TOMLs, docs/MANIFEST.md, and REQUIREMENTS.md</name>
  <files>manifests/defaults.toml, manifests/machines/personal-laptop.toml, manifests/machines/work-laptop.toml, manifests/machines/server-1.toml, manifests/machines/server-2.toml, docs/MANIFEST.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - manifests/defaults.toml (full file - the [features] block at lines 23-31 and [identity] comment at lines 40-44)
    - manifests/machines/personal-laptop.toml (full file - the [features] block at lines 15-25)
    - manifests/machines/work-laptop.toml (full file - the [features] block at lines 14-23)
    - manifests/machines/server-1.toml (full file - the [identity] block at lines 22-24)
    - manifests/machines/server-2.toml (full file - the [identity] block at lines 22-24)
    - docs/MANIFEST.md (full file - the required-fields table lines 85-92, the Feature-Flag Reference table lines 396-405)
    - .planning/REQUIREMENTS.md (the IDNT-05 line at line 86)
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-PATTERNS.md sections "manifests/defaults.toml (EDIT)", "manifests/machines/server-1.toml (EDIT)", "manifests/machines/personal-laptop.toml / work-laptop.toml (EDIT)", "docs/MANIFEST.md (EDIT)" for the exact textual edits
    - .planning/phases/04-identity-layer-git-ssh-per-machine/04-CONTEXT.md D-05, D-07, D-15, D-16 + Required ROADMAP/REQUIREMENTS edits section
  </read_first>
  <action>
Per D-15, D-07, D-05, D-16, make the following targeted edits. Use the Edit tool for each file (one Edit call per file). No bare `cat`/heredoc rewrites.

(a) `manifests/defaults.toml` -- in the `[features]` block, add a new line immediately after `one-password-ssh = false`:
  - New line content: `one-password-signing = false   # gates git commit signing via 1Password op-ssh-sign`
  - In the `[identity]` block, update the comment line `# Allowed values: "personal" | "work" | "none". Drives Phase 4 git+SSH` so the values list now reads `"personal" | "work" | "server-1" | "server-2" | "none"`. Keep the `# identity selection.` continuation line.

(b) `manifests/machines/personal-laptop.toml` -- in the `[features]` block, add `one-password-signing = true` on the line immediately after `one-password-ssh = true` (per D-16 cross-field rule: identity.git = personal requires this flag).

(c) `manifests/machines/work-laptop.toml` -- in the `[features]` block, add `one-password-signing = true` on the line immediately after `one-password-ssh = true` (per D-16 cross-field rule: identity.git = work requires this flag).

(d) `manifests/machines/server-1.toml` -- in the `[identity]` block, change `git = "none"` to `git = "server-1"` and `ssh = "none"` to `ssh = "server-1"` (per D-07). Leave the `[features]` block untouched -- servers carry `one-password-ssh = false` and `one-password-signing = false` (not present, inherited from defaults).

(e) `manifests/machines/server-2.toml` -- same as (d), with `"server-2"` values (per D-07).

(f) `docs/MANIFEST.md` -- two table edits:
  - At lines 91-92, expand the "Allowed values" column of both `identity.git` and `identity.ssh` rows. New cell content for each: `` `"personal"` \| `"work"` \| `"server-1"` \| `"server-2"` \| `"none"` ``.
  - In the Feature-Flag Reference table (lines 396-405), insert a new row immediately after the `one-password-ssh` row: `| `one-password-signing` | Phase 4 | Enables git commit signing via 1Password op-ssh-sign | `false` |`.

(g) `.planning/REQUIREMENTS.md` -- update the IDNT-05 line. The current text is `- [ ] **IDNT-05**: 1Password SSH agent integration enabled only when manifest declares `one-password-ssh = true`; no hostname literals anywhere in the identity path`. Rewrite to reflect the split (D-15): `- [ ] **IDNT-05**: 1Password integration is split into two flags -- `features.one-password-ssh` gates the SSH agent socket + `IdentityAgent` directive, and `features.one-password-signing` gates git commit signing via the `op-ssh-sign` program. Cross-field validation rejects mismatched configurations. No hostname literals anywhere in the identity path.`

Constraints:
- No emojis anywhere (project rule, stricter than global).
- TOML indentation/formatting matches the existing file style (the `[features]` and `[identity]` blocks use no indentation on keys -- match that).
- Do NOT remove or reorder any existing TOML keys (only insert new ones at the documented positions).
- Do NOT touch `[platform]`, `[meta]`, or `[packages.brew]` blocks.
  </action>
  <verify>
    <automated>cd $DOTFILEDIR && grep -q 'one-password-signing = false' manifests/defaults.toml && grep -q 'server-1' manifests/defaults.toml && grep -q 'one-password-signing = true' manifests/machines/personal-laptop.toml && grep -q 'one-password-signing = true' manifests/machines/work-laptop.toml && grep -q 'git = "server-1"' manifests/machines/server-1.toml && grep -q 'ssh = "server-1"' manifests/machines/server-1.toml && grep -q 'git = "server-2"' manifests/machines/server-2.toml && grep -q 'ssh = "server-2"' manifests/machines/server-2.toml && grep -q '"server-1" \\\\| `"server-2"`' docs/MANIFEST.md && grep -q 'one-password-signing' docs/MANIFEST.md</automated>
  </verify>
  <acceptance_criteria>
    - `manifests/defaults.toml` contains exactly one line matching `^one-password-signing = false` inside the `[features]` block
    - `manifests/defaults.toml` `[identity]` comment lists all five values: personal, work, server-1, server-2, none
    - `manifests/machines/personal-laptop.toml` and `work-laptop.toml` each contain exactly one line `one-password-signing = true` in their `[features]` block
    - `manifests/machines/server-1.toml` contains `git = "server-1"` and `ssh = "server-1"` (no `"none"` values remain in `[identity]`)
    - `manifests/machines/server-2.toml` contains `git = "server-2"` and `ssh = "server-2"`
    - `docs/MANIFEST.md` has both identity rows expanded to include `"server-1"` and `"server-2"`
    - `docs/MANIFEST.md` Feature-Flag Reference table contains a row for `one-password-signing` with owner "Phase 4" and default `false`
    - `.planning/REQUIREMENTS.md` IDNT-05 text mentions both `one-password-ssh` and `one-password-signing` (the split per D-15)
    - After both tasks land: `task -t taskfiles/manifest.yml manifest:validate -- --machine server-1` exits 0; `task -t taskfiles/manifest.yml manifest:validate -- --machine personal-laptop` exits 0; `task -t taskfiles/manifest.yml manifest:validate -- --machine work-laptop` exits 0
  </acceptance_criteria>
  <done>Defaults.toml carries the new flag with a conservative default; each laptop TOML enables both flags; each server TOML declares its per-server identity; docs/MANIFEST.md describes the v2 schema; REQUIREMENTS.md IDNT-05 reflects the D-15 split.</done>
</task>

</tasks>

<verification>
After both tasks land:

1. `zsh -n install/resolver.zsh` exits 0.
2. `task -t taskfiles/manifest.yml manifest:validate -- --machine personal-laptop` exits 0.
3. `task -t taskfiles/manifest.yml manifest:validate -- --machine work-laptop` exits 0.
4. `task -t taskfiles/manifest.yml manifest:validate -- --machine server-1` exits 0 (the new `identity = "server-1"` values are now accepted).
5. `task -t taskfiles/manifest.yml manifest:validate -- --machine server-2` exits 0.
6. `task -t taskfiles/manifest.yml manifest:test` continues to exit 0 with `negative_count=2` -- the existing two negative fixtures still pass (the new fixtures arrive in Plan 03).
7. `task lint` exits 0 (no taskfile edits in this plan; lint just confirms nothing regressed).

The above is exercised by `task manifest:validate -- --machine <each-name>`; running it for all four target machines is the plan gate.
</verification>

<success_criteria>
- Resolver accepts the five-value enum and emits the two D-16 cross-field errors with the exact wording specified (downstream Plan 03 fixtures grep for the exact stderr fragments).
- All four target machine TOMLs validate clean against the updated resolver.
- docs/MANIFEST.md and REQUIREMENTS.md describe the schema as it now exists.
- No regression in `manifest:test` (existing two negative fixtures continue to pass; counter stays at 2 until Plan 03 bumps it to 5).
</success_criteria>

<output>
After completion, create `.planning/phases/04-identity-layer-git-ssh-per-machine/04-01-SUMMARY.md` per the standard summary template.
</output>

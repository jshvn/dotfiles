---
phase: 06-os-defaults-macos-configuration
plan: 03
type: execute
wave: 1
depends_on: [01, 02]
files_modified:
  - taskfiles/macos.yml
  - Taskfile.yml
  - docs/MANIFEST.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [OSCF-01, OSCF-02, OSCF-03, OSCF-04, OSCF-05]
requirements_addressed: [OSCF-01, OSCF-02, OSCF-03, OSCF-04, OSCF-05]
user_setup: []
must_haves:
  truths:
    - "taskfiles/macos.yml exists and exports tasks: defaults (aggregator), defaults:dock, defaults:finder, defaults:input, defaults:screenshots, defaults:security, shell, validate"
    - "Each macos:defaults:<concern> task uses the RESEARCH Pattern 2 single-shell-block status: the heredoc renders {{if not (index .MANIFEST.features \"macos-<concern>\")}}exit 0{{end}} at template build time as the SECOND line of the block, short-circuiting verify_<concern> when the feature is off (NOT the broken two-line ANDed status from CONTEXT.md lines 405-410)"
    - "macos:shell defines vars.BREW_ZSH = '{{.HOMEBREW_PREFIX}}/bin/zsh' at task level; cmds exports BREW_ZSH=\"{{.BREW_ZSH}}\" then sources os/shell-registration.zsh and calls apply_shell_registration; status uses {{.BREW_ZSH}} template var in BOTH the grep -qxF /etc/shells check AND the dscl UserShell check -- ZERO $BREW_ZSH literal references in status (the LINT-02 contract; closes the v1 macos:shell:145 bug class)"
    - "macos:validate is an always-rerun task (status: [false]) with the # lint-allow: cmds-without-status marker; its cmds: heredoc sources every enabled concern's script and runs each verify_<concern> using {{if index .MANIFEST.features \"macos-<concern>\"}} guards around each source+verify pair (RESEARCH Pattern 4)"
    - "macos:defaults aggregator has cmds: that are all task: delegations; no status block; carries # lint-allow: cmds-without-status (LINT-01/03a aggregator exemption)"
    - "Every macos:* task that reads resolved.json declares deps: [\":manifest:resolve\"] and the file has the preconditions block asserting test -s '{{.RESOLVED_JSON_PATH}}' (CF-08 + WR-03 fallback)"
    - "Root Taskfile.yml includes.macos: target flipped from ./taskfiles/macos-stub.yml to ./taskfiles/macos.yml; macos-stub.yml deleted; root install task body unchanged (already calls task: macos:defaults then task: macos:shell at lines 142-143)"
    - "ROADMAP.md Phase 6 success criterion #1 wording uses flat features.macos-dock (was features.macos-defaults.dock); criterion #5 wording uses 'in-script expected values' (was 'manifest's expected values') per D-02"
    - "docs/MANIFEST.md feature-flag reference table rows for the five macos-* keys now read 'false' in the Default-in-defaults.toml column (was 'machine-set (not in defaults.toml)'); macos-finder row records dual-consumer P3+P6 semantics"
    - "task lint exits 0 after the new taskfile lands; in particular task lint:taskfile exits 0 -- the regression check grep -qE '\\$BREW_ZSH\\b' against status: lines of macos.yml returns no matches"
  artifacts:
    - path: "taskfiles/macos.yml"
      provides: "Phase 6 macOS taskfile -- replaces macos-stub.yml; ships defaults aggregator + 5 per-concern tasks + shell + validate"
      exports: ["defaults", "defaults:dock", "defaults:finder", "defaults:input", "defaults:screenshots", "defaults:security", "shell", "validate"]
    - path: "Taskfile.yml"
      provides: "Root: includes.macos points to the real taskfiles/macos.yml; install pipeline call graph unchanged"
      contains: "macos:    ./taskfiles/macos.yml"
    - path: "docs/MANIFEST.md"
      provides: "Feature-flag reference table updated: five macos-* rows now show false default; macos-finder dual-consumer note"
      contains: "macos-finder"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 success criterion #1 and #5 wording amended per D-01 + D-02"
      contains: "features.macos-dock"
    - path: ".planning/REQUIREMENTS.md"
      provides: "OSCF-05 wording optionally softened to align with D-02 in-script-expected-values mental model"
      contains: "OSCF-05"
  key_links:
    - from: "taskfiles/macos.yml"
      to: "os/defaults/<concern>.zsh + os/shell-registration.zsh (Plan 02 artifacts)"
      via: "source \"${DOTFILEDIR}/os/defaults/<concern>.zsh\" + source \"${DOTFILEDIR}/os/shell-registration.zsh\" in cmds/status heredocs"
      pattern: "source \\\"\\$\\{DOTFILEDIR\\}/os/"
    - from: "taskfiles/macos.yml"
      to: "manifests/defaults.toml + machine TOMLs (Plan 01 artifacts; consumed via resolved.json)"
      via: "ref: fromJson .MANIFEST_JSON + index .MANIFEST.features \"macos-<concern>\""
      pattern: "index .MANIFEST.features \"macos-"
    - from: "Taskfile.yml includes.macos"
      to: "taskfiles/macos.yml (this plan's primary deliverable)"
      via: "single-line include flip"
      pattern: "macos:    ./taskfiles/macos.yml"
---

<objective>
Ship the real `taskfiles/macos.yml` that replaces the Phase 2 stub. The taskfile sources the six `os/<...>.zsh` scripts (Plan 02 artifacts), reads the five `features.macos-*` kebab-case feature gates from `resolved.json` (Plan 01 artifacts via `manifest:resolve`), and exposes the macos: namespace's eight tasks (`defaults` aggregator + 5 per-concern + `shell` + `validate`). Flip the root `Taskfile.yml` `includes.macos:` from the stub to the real file, delete the now-orphaned `taskfiles/macos-stub.yml`, and amend the planning and schema documents (`docs/MANIFEST.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`) per D-01 and D-02.

The taskfile is the file where the structural LINT-02 fix lives: every `macos:defaults:<concern>` task uses the RESEARCH Pattern 2 single-shell-block status (`{{if not (index .MANIFEST.features "macos-<concern>")}}exit 0{{end}}` short-circuit as the SECOND line of a single heredoc), NOT the broken two-line ANDed status from CONTEXT.md lines 405-410. `macos:shell` uses `{{.BREW_ZSH}}` (template var) in its status block -- the v1 `macos:shell:145` bug class is closed structurally because the producer-side reference is now task-template (resolved at task-graph build time), not shell-eval-time.

Purpose: This is the wiring plan. Plan 01's manifest schema and Plan 02's script artifacts are inert until this taskfile is wired into `task install`'s call graph. After Plan 03 lands, `task install` on a laptop runs all five concern applies + shell-registration; on a server, only macos-security applies + shell-registration; on every machine, `task macos:validate` enumerates the enabled concerns and prints check/cross output.

Output: 1 new taskfile (replacing the stub) + 1 root taskfile edit + 1 deleted stub + 3 planning/docs edits.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 6 source-of-truth artifacts
@.planning/phases/06-os-defaults-macos-configuration/06-CONTEXT.md
@.planning/phases/06-os-defaults-macos-configuration/06-RESEARCH.md
@.planning/phases/06-os-defaults-macos-configuration/06-PATTERNS.md
@.planning/phases/06-os-defaults-macos-configuration/06-VALIDATION.md

# Prior-plan SUMMARYs (this plan consumes their outputs)
@.planning/phases/06-os-defaults-macos-configuration/06-01-SUMMARY.md
@.planning/phases/06-os-defaults-macos-configuration/06-02-SUMMARY.md

# Files this plan modifies / replaces
@taskfiles/macos.yml
@taskfiles/macos-stub.yml
@Taskfile.yml
@docs/MANIFEST.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Primary taskfile analogs (this plan copies their shape verbatim)
@taskfiles/packages.yml
@taskfiles/identity.yml
@taskfiles/lint.yml

# Scripts the new taskfile sources (Plan 02 outputs; the cmds/status heredocs source these)
@os/defaults/dock.zsh
@os/defaults/finder.zsh
@os/defaults/input.zsh
@os/defaults/screenshots.zsh
@os/defaults/security.zsh
@os/shell-registration.zsh

# Bug-class concern this plan structurally fixes
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Function names the taskfile invokes (verified from Plan 02 contract) -->

```zsh
# Plan 02 exports these from the sourced concern scripts:
apply_dock          # os/defaults/dock.zsh
verify_dock         # os/defaults/dock.zsh -- returns failed boolean (0=clean, 1=drift)
apply_finder        # os/defaults/finder.zsh
verify_finder       # os/defaults/finder.zsh
apply_input         # os/defaults/input.zsh
verify_input        # os/defaults/input.zsh
apply_screenshots   # os/defaults/screenshots.zsh
verify_screenshots  # os/defaults/screenshots.zsh
apply_security      # os/defaults/security.zsh -- includes sudo-guarded sysadminctl
verify_security     # os/defaults/security.zsh

# Plan 02 exports these from the shell-registration script:
apply_shell_registration   # os/shell-registration.zsh -- requires BREW_ZSH env var
verify_shell_registration  # os/shell-registration.zsh -- requires BREW_ZSH env var
```

<!-- The taskfile must inject BREW_ZSH into the shell-registration script's environment.
     Pattern (RESEARCH Pattern 5): vars.BREW_ZSH at task level + export inside heredoc.

  vars:
    BREW_ZSH: '{{.HOMEBREW_PREFIX}}/bin/zsh'
  cmds:
    - |
      export BREW_ZSH="{{.BREW_ZSH}}"
      source "${DOTFILEDIR}/os/shell-registration.zsh"
      apply_shell_registration
-->

<!-- Root Taskfile.yml current state (verified from Read of Taskfile.yml:107-108) -->

```yaml
  claude:   ./taskfiles/claude-stub.yml     # P7 wires real bodies
  macos:    ./taskfiles/macos-stub.yml      # P6 wires real bodies
```

<!-- Root Taskfile.yml install task body lines 138-143 (UNCHANGED by this plan -- the stub already
     wires the pipeline; only the include target flips):

  cmds:
    - task: links:all
    - task: packages:install
    - task: claude:install
    - task: macos:defaults
    - task: macos:shell
    - task: packages:verify
-->

<!-- LINT-02 regression contract (the bug-class fix verification):
     After this plan lands, grep -qE '\$BREW_ZSH\b' against the status: blocks of taskfiles/macos.yml
     MUST return no matches. The status uses {{.BREW_ZSH}} (template var); the script body uses
     $BREW_ZSH (shell var inside cmds heredoc -- legal). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create taskfiles/macos.yml with all eight tasks; delete taskfiles/macos-stub.yml; flip Taskfile.yml include target</name>
  <files>taskfiles/macos.yml, taskfiles/macos-stub.yml, Taskfile.yml</files>
  <read_first>
    - taskfiles/macos.yml (the v1 monolith; bytes stay on disk per CF-11 -- v2 ships a NEW file at the same path, OVERWRITING the v1 content; v1 deletion is owned by Phase 8 per CONTEXT carried-forward CF-11. WAIT: re-read CONTEXT.md "out of scope" section -- v1 taskfiles/macos.yml stays byte-stable. The actual deliverable is at taskfiles/macos.yml because the include flips from macos-stub.yml to macos.yml; but the existing v1 file IS at that path. Re-read CONTEXT.md `<deliverables>` and `<out of scope>` carefully: CONTEXT says "v1 taskfiles/macos.yml stays byte-stable until cutover completes (parallel-rewrite invariant)" but ALSO says the include flips from macos-stub.yml to macos.yml. The resolution: v2's real taskfiles/macos.yml is the v1 path -- the v1 file IS being replaced here because the include target name resolves to the same path. ACTUALLY check whether CONTEXT defers v1 deletion to P8 or replaces it now -- planning_context <deliverables> says "v1 taskfiles/macos.yml stays byte-stable; Phase 8 owns its deletion. taskfiles/macos-stub.yml is replaced by taskfiles/macos.yml; the stub file is deleted as part of the Taskfile.yml include flip, but the v1 monolith is untouched." This is contradictory: a single path cannot be both byte-stable AND replaced. Re-read the planning_context deliverables block ONE MORE TIME and then disambiguate before authoring: planning_context <deliverables> NEW files includes "taskfiles/macos.yml (replaces taskfiles/macos-stub.yml)" -- so taskfiles/macos.yml is the new path and is BEING WRITTEN by this task. DELETED files: "v1 taskfiles/macos.yml stays byte-stable" -- ergo the v1 file at this path will be overwritten unless we rename it first. The resolution: BEFORE writing the new taskfiles/macos.yml, rename the existing v1 taskfiles/macos.yml to taskfiles/macos.v1.yml.bak (preserves bytes; P8 will delete in cutover); the include target flips from macos-stub.yml to macos.yml; the stub file is deleted)
    - Re-read: actually, the situation is that v1 `taskfiles/macos.yml` is NOT currently included by the root Taskfile.yml (root includes macos-stub.yml at line 108). The v1 file is dormant -- not referenced by any active include. CONTEXT says "byte-stable on disk" which means the file stays at its path. To honor CF-11 parallel-rewrite, the v2 taskfile must NOT overwrite the v1 path. So this task: (a) renames the existing v1 taskfiles/macos.yml -> taskfiles/macos.v1.yml.bak (parallel-rewrite invariant -- preserves v1 byte stream for Phase 8 deletion); (b) creates the new taskfiles/macos.yml (the v2 real bodies); (c) deletes taskfiles/macos-stub.yml; (d) flips Taskfile.yml includes.macos: target from macos-stub.yml to macos.yml.
    - Taskfile.yml lines 107-108 (the includes block; the macos: row is the only line that changes)
    - taskfiles/packages.yml lines 1-200 (primary analog for: file header, vars block, MANIFEST_JSON fallback, MANIFEST ref:fromJson, preconditions, deps:[":manifest:resolve"], status:[false] aggregator pattern, # lint-allow: cmds-without-status marker)
    - taskfiles/identity.yml lines 1-150 (secondary analog for: aggregator install task with task: delegations, validate task shape)
    - taskfiles/lint.yml (the lint rules that will scan this file; LINT-02 scans .tasks[] | .status for $VAR; LINT-03a auto-exempts task: delegation aggregators; LINT-05 will fire warn-only on defaults/dscl lines if they appear in this taskfile -- they don't, the heredocs source scripts that contain them, so taskfile itself is LINT-05 clean)
    - .planning/phases/06-os-defaults-macos-configuration/06-RESEARCH.md lines 325-475 (Patterns 2, 3, 4, 5 -- the four taskfile task shapes this file uses verbatim)
    - .planning/phases/06-os-defaults-macos-configuration/06-PATTERNS.md lines 204-410 (the taskfiles/macos.yml pattern map; primary analog packages.yml + secondary identity.yml; the per-task shapes for each of the eight tasks)
    - .planning/codebase/CONCERNS.md lines 15-19 (the v1 macos:shell:145 bug class -- this taskfile's status block uses {{.BREW_ZSH}} template var to structurally close that bug)
  </read_first>
  <action>
    Three coordinated file operations, in this order:

    **Step A -- preserve v1 byte stream (CF-11 parallel-rewrite invariant).** Use `git mv taskfiles/macos.yml taskfiles/macos.v1.yml.bak` to rename the existing v1 monolith to a `.bak` path. This keeps the v1 byte stream tracked in git history (Phase 8 will `git rm` it during cutover deletion). The active includes do not reference this path (root currently includes macos-stub.yml, not macos.yml), so the rename has zero behavioral impact. If git mv is unavailable, use `mv` + `git rm --cached taskfiles/macos.yml` + `git add taskfiles/macos.v1.yml.bak`.

    **Step B -- write the new taskfiles/macos.yml (the Phase 6 real bodies).** Author the new file at `taskfiles/macos.yml`. The file uses the four canonical taskfile patterns from RESEARCH; exact shapes follow.

    File starts with `version: '3'` then the file-header comment block (matches packages.yml lines 1-35 shape). Cite purpose ("Phase 6 macOS OS-defaults + shell-registration layer"), inputs (`features.macos-{dock,finder,input,screenshots,security}` from `resolved.json` via `ref: fromJson`; `${DOTFILEDIR}/os/defaults/<concern>.zsh` sourced helpers; `${DOTFILEDIR}/os/shell-registration.zsh`), dependencies (`:manifest:resolve` deps; `install/messages.zsh` via `{{.DOTFILES_MESSAGES}}`), status-block convention (LINT-02 enforcement -- every status uses `{{.X}}` template vars only; aggregator-style tasks carry `# lint-allow: cmds-without-status`), and the bug-class history (this file is the structural fix for the v1 `macos:shell:145` `$BREW_ZSH`-in-status bug class per `.planning/codebase/CONCERNS.md` lines 15-19).

    Then `includes:` block:
    ```
    includes:
      _: ./helpers.yml
    ```
    (Mirrors packages.yml line 37-38; gives access to `_:safe-link` etc. -- not actually used by this phase but matches the convention so a future task in this file can use helpers without an include edit.)

    Then `vars:` block (mirrors packages.yml lines 45-88 / identity.yml lines 54-95 verbatim, with two additions specific to P6):
    ```yaml
    vars:
      HOME: '{{.HOME}}'
      XDG_CONFIG_HOME:
        sh: echo "${XDG_CONFIG_HOME:-$HOME/.config}"
      XDG_STATE_HOME:
        sh: echo "${XDG_STATE_HOME:-$HOME/.local/state}"
      DOTFILEDIR:
        sh: dirname "{{.TASKFILE_DIR}}"
      RESOLVED_JSON_PATH: '{{.XDG_STATE_HOME}}/dotfiles/resolved.json'
      MANIFEST_JSON:
        sh: |
          if [[ -s '{{.RESOLVED_JSON_PATH}}' ]]; then
            cat '{{.RESOLVED_JSON_PATH}}'
          else
            echo "warning: {{.RESOLVED_JSON_PATH}} missing or empty -- run 'task setup -- <machine>' first" >&2
            echo '{}'
          fi
      MANIFEST:
        ref: 'fromJson .MANIFEST_JSON'
      DOTFILES_MESSAGES: |
        source '{{.DOTFILEDIR}}/install/messages.zsh'
      # HOMEBREW_PREFIX is forwarded from root Taskfile.yml's vars block when this
      # taskfile is loaded via includes:. The local sh: fallback mirrors the root
      # detection so `task -t taskfiles/macos.yml shell` works in isolation.
      HOMEBREW_PREFIX:
        sh: |
          if command -v brew >/dev/null 2>&1; then brew --prefix
          elif [[ "$(uname -m)" == "arm64" ]]; then echo "/opt/homebrew"
          else echo "/usr/local"
          fi
    ```

    Then `tasks:` block. Author the eight tasks in this exact order (matches PATTERNS.md aggregator-then-concerns sequence; deterministic per CONTEXT D-discretion validation enumeration order):

    **Task `defaults`** (aggregator -- RESEARCH Pattern 3; LINT-01/03a aggregator exemption):
    ```yaml
      # lint-allow: cmds-without-status
      defaults:
        desc: "Apply macOS system defaults (per-concern, feature-gated)"
        platforms: [darwin]
        deps: [":manifest:resolve"]
        preconditions:
          - sh: test -s '{{.RESOLVED_JSON_PATH}}'
            msg: |
              resolved.json missing or empty; cannot resolve features.macos-*
                run: task setup -- <machine-name>
        cmds:
          - task: defaults:dock
          - task: defaults:finder
          - task: defaults:input
          - task: defaults:screenshots
          - task: defaults:security
    ```

    **Tasks `defaults:dock`, `defaults:finder`, `defaults:input`, `defaults:screenshots`, `defaults:security`** (RESEARCH Pattern 2 single-shell-block status; ONE PER CONCERN -- five tasks total, identical shape, only the concern name + feature key vary). The shape for `defaults:dock` (the other four tasks substitute `dock` with the concern name and `macos-dock` with `macos-<concern>`):
    ```yaml
      defaults:dock:
        desc: "Apply Dock defaults (gated on macos-dock)"
        internal: true
        platforms: [darwin]
        deps: [":manifest:resolve"]
        cmds:
          - |
            set -euo pipefail
            export DOTFILEDIR="{{.TASKFILE_DIR}}"
            {{if not (index .MANIFEST.features "macos-dock")}}exit 0{{end}}
            source "${DOTFILEDIR}/os/defaults/dock.zsh"
            apply_dock
        status:
          - |
            export DOTFILEDIR="{{.TASKFILE_DIR}}"
            {{if not (index .MANIFEST.features "macos-dock")}}exit 0{{end}}
            source "${DOTFILEDIR}/os/defaults/dock.zsh"
            verify_dock > /dev/null 2>&1
    ```

    Critical -- this is the RESEARCH Pattern 2 single-shell-block status. The `{{if not (index .MANIFEST.features "macos-dock")}}exit 0{{end}}` line is the SECOND line of the heredoc; it renders to literal `exit 0` at template build time when the feature is off (short-circuiting the rest of the heredoc -- the script never gets sourced, verify_dock never runs); it renders to the empty string when the feature is on (the heredoc proceeds to source + verify). This is NOT the broken two-line ANDed status pattern from CONTEXT.md lines 405-410 (which would AND a `true`/`false` rendered string with verify_dock's exit code -- go-task ANDs multi-line status so on a feature-off machine verify_dock would run AND fail spuriously, bypassing the feature gate). RESEARCH Pitfall 1 documents the broken pattern; this task ships the correct one.

    Each per-concern task uses `internal: true` (the aggregator dispatches them; users invoke `task macos:defaults` or `task macos:defaults:dock` explicitly during debugging, both work, but the `internal` flag hides them from `task --list` output -- matches the packages.yml pattern for sub-tasks).

    **`TASKFILE_DIR` vs `DOTFILEDIR` note** (RESEARCH Pattern 2 note + Pitfall 6): when this taskfile is loaded via the root Taskfile.yml's `includes.macos:` directive, `{{.TASKFILE_DIR}}` inside the heredoc renders to the root taskfile's directory (the repo root) -- because go-task evaluates `TASKFILE_DIR` from the invoked taskfile root, not from the included module's path. This is the correct value for `DOTFILEDIR` (the repo root contains `os/`). Identity.yml + packages.yml use exactly this pattern: `export DOTFILEDIR="{{.TASKFILE_DIR}}"` inside the heredoc, then `source "${DOTFILEDIR}/<path>"`.

    **Task `shell`** (RESEARCH Pattern 5 -- the structural LINT-02 fix; vars.BREW_ZSH + template-var status; NOT the single-shell-block pattern because there is no feature gate to short-circuit):
    ```yaml
      shell:
        desc: "Register Homebrew zsh as login shell (/etc/shells + chsh)"
        platforms: [darwin]
        vars:
          BREW_ZSH: '{{.HOMEBREW_PREFIX}}/bin/zsh'
        cmds:
          - |
            set -euo pipefail
            export DOTFILEDIR="{{.TASKFILE_DIR}}"
            export BREW_ZSH="{{.BREW_ZSH}}"
            source "${DOTFILEDIR}/os/shell-registration.zsh"
            apply_shell_registration
        status:
          # LINT-02 contract: both status conditions use {{.BREW_ZSH}} template var.
          # Zero $BREW_ZSH literal references in this block -- structural fix for
          # the v1 macos:shell:145 bug class (CONCERNS.md lines 15-19).
          - grep -qxF "{{.BREW_ZSH}}" /etc/shells
          - '[[ "$(dscl . -read /Users/$USER UserShell 2>/dev/null | awk "{print \$2}")" = "{{.BREW_ZSH}}" ]]'
    ```

    The `shell:status` block is TWO ANDed entries -- both must exit 0 for the task to skip. That is correct here because both conditions are unconditional (no feature gate; both checks always evaluate). The first entry checks `/etc/shells` membership via `grep -qxF` with the template-rendered `{{.BREW_ZSH}}` path. The second entry compares the user's registered login shell (read via `dscl . -read /Users/$USER UserShell | awk '{print $2}'`) against `{{.BREW_ZSH}}`. NEITHER entry uses `$BREW_ZSH` (shell var) -- THIS IS THE LINT-02 CONTRACT. The bug-class regression check is: `yq '.tasks.shell.status' taskfiles/macos.yml | grep -qE '\$BREW_ZSH\b'` MUST exit non-zero (no matches). Plan 03 quality_gate enforces this; Plan 04's HUMAN-UAT checks it explicitly.

    The shell:status uses {{.BREW_ZSH}} template var only; NO $BREW_ZSH shell var anywhere in shell:status. Inside the script body and inside the cmds: heredoc, `$BREW_ZSH` references are LEGAL (LINT-02 only scans status: blocks).

    **Task `validate`** (RESEARCH Pattern 4 -- always-rerun aggregator; per-concern feature-gated source+verify pairs):
    ```yaml
      # lint-allow: cmds-without-status
      validate:
        desc: "Validate macOS defaults match in-script expected values"
        platforms: [darwin]
        deps: [":manifest:resolve"]
        status: [false]
        cmds:
          - |
            set -euo pipefail
            {{.DOTFILES_MESSAGES}}
            export DOTFILEDIR="{{.TASKFILE_DIR}}"
            export BREW_ZSH="{{.HOMEBREW_PREFIX}}/bin/zsh"

            failed=0

            # Always-on: shell registration.
            source "${DOTFILEDIR}/os/shell-registration.zsh"
            verify_shell_registration || failed=1

            # Per-concern, feature-gated -- enumerate-all (no fast-fail) per D-02 + P5 D-07.
            {{if index .MANIFEST.features "macos-dock"}}
            source "${DOTFILEDIR}/os/defaults/dock.zsh"
            verify_dock || failed=1
            {{end}}
            {{if index .MANIFEST.features "macos-finder"}}
            source "${DOTFILEDIR}/os/defaults/finder.zsh"
            verify_finder || failed=1
            {{end}}
            {{if index .MANIFEST.features "macos-input"}}
            source "${DOTFILEDIR}/os/defaults/input.zsh"
            verify_input || failed=1
            {{end}}
            {{if index .MANIFEST.features "macos-screenshots"}}
            source "${DOTFILEDIR}/os/defaults/screenshots.zsh"
            verify_screenshots || failed=1
            {{end}}
            {{if index .MANIFEST.features "macos-security"}}
            source "${DOTFILEDIR}/os/defaults/security.zsh"
            verify_security || failed=1
            {{end}}

            exit "$failed"
    ```

    The enumeration order matches OSCF-01 (dock -> finder -> input -> screenshots -> security). `status: [false]` plus the `# lint-allow: cmds-without-status` marker satisfies LINT-01/03a (always-rerun pattern; mirrors packages.yml `verify`/`audit` tasks). The `BREW_ZSH` export is needed because `os/shell-registration.zsh` asserts the variable via `: "${BREW_ZSH:?...}"` at source time. Each enabled concern sources its script and calls verify; `|| failed=1` preserves the enumerate-all + boolean-flag pattern (Pitfall 9); final `exit "$failed"` returns 0 (all clean) or 1 (any drift).

    **Whole-file lint compliance:**
    - LINT-01: every install task has status: (defaults:dock..security each have a status:; shell has a status:); aggregators (defaults, validate) carry the `# lint-allow: cmds-without-status` marker.
    - LINT-02: every status: block uses `{{.X}}` template vars only. The five per-concern tasks use `{{.TASKFILE_DIR}}` and the template-rendered `index .MANIFEST.features` boolean. The shell task uses `{{.BREW_ZSH}}` template var. ZERO `$VAR` references anywhere in any `status:` block. The structural fix for `macos:shell:145` is locked.
    - LINT-03a: aggregators (defaults, validate) auto-exempt via task: delegation cmds and `status: [false]` + marker respectively.
    - LINT-03b: no bare `ln -s` (this phase has no symlinks).
    - LINT-04: not applicable (this is a YAML file; LINT-04 scans `.zsh` files; Plan 02's scripts carry the flag).
    - LINT-05: WILL fire warn-only against the heredoc lines containing `dscl . -read` in the shell:status block; expected and intentional (RESEARCH Pitfall 10).
    - LINT-07: not applicable to YAML (zsh -n is for .zsh files).

    **Step C -- delete taskfiles/macos-stub.yml.** Use `git rm taskfiles/macos-stub.yml` to remove the file from both the working tree and git index. The stub was a Phase 2 placeholder; Phase 6's real `taskfiles/macos.yml` supersedes it.

    **Step D -- flip Taskfile.yml includes.macos: target.** Edit `Taskfile.yml` at line 108 only. Change:
    ```
      macos:    ./taskfiles/macos-stub.yml      # P6 wires real bodies
    ```
    to:
    ```
      macos:    ./taskfiles/macos.yml           # P6 ships real bodies
    ```
    The line above it (`claude:    ./taskfiles/claude-stub.yml`) does NOT change -- Phase 7 owns the claude stub flip. The trailing comment changes from "P6 wires real bodies" (future tense) to "P6 ships real bodies" (past tense). The root install task's cmds: at lines 138-143 is UNCHANGED (already calls `task: macos:defaults` then `task: macos:shell`). Use the Edit tool against this one line.

    After all four steps:
    - `task --list` MUST show `macos:defaults`, `macos:shell`, `macos:validate` (the three non-internal tasks).
    - `task -t taskfiles/macos.yml --list` MUST show defaults, defaults:dock..security (with internal flag interactions), shell, validate.
    - `task lint` MUST exit 0 (LINT-05 warnings are warn-only).
    - `yq '.tasks.shell.status' taskfiles/macos.yml | grep -qE '\$BREW_ZSH\b'` MUST exit non-zero (bug-class regression test).
    - `task macos:validate` on the active dev machine prints check/cross output for every enabled concern + shell-registration.
  </action>
  <verify>
    <automated>test -f taskfiles/macos.yml && test -f taskfiles/macos.v1.yml.bak && [[ ! -f taskfiles/macos-stub.yml ]] && grep -q "^  macos:    \./taskfiles/macos\.yml" Taskfile.yml && ! grep -q "macos-stub.yml" Taskfile.yml && yq '.tasks | keys' taskfiles/macos.yml | grep -qE 'defaults$' && for t in defaults:dock defaults:finder defaults:input defaults:screenshots defaults:security shell validate; do yq ".tasks.\"$t\"" taskfiles/macos.yml | grep -q . || { echo "missing task: $t" >&2; exit 1; }; done && for c in dock finder input screenshots security; do yq ".tasks.\"defaults:$c\".status" taskfiles/macos.yml | grep -q "index .MANIFEST.features \"macos-$c\"" || { echo "feature-gate missing for: $c" >&2; exit 1; }; done && yq '.tasks.shell.status' taskfiles/macos.yml | grep -qE '\{\{\.BREW_ZSH\}\}' && ! (yq '.tasks.shell.status' taskfiles/macos.yml | grep -E '\$BREW_ZSH\b') && grep -q "# lint-allow: cmds-without-status" taskfiles/macos.yml && task lint >/dev/null 2>&1 && task --list 2>/dev/null | grep -qE '^\* macos:defaults' && task --list 2>/dev/null | grep -qE '^\* macos:shell'</automated>
  </verify>
  <done>
    `taskfiles/macos.yml` exists as the real Phase 6 module: 8 tasks (`defaults` aggregator + 5 per-concern with single-shell-block status + `shell` + `validate`); every per-concern status uses `{{if not (index .MANIFEST.features "macos-<concern>")}}exit 0{{end}}` as the second line of a single heredoc; `shell:status` uses `{{.BREW_ZSH}}` template var only (zero `$BREW_ZSH` literal references); `validate` is `status: [false]` always-rerun with per-concern feature-gated source+verify pairs. The v1 `taskfiles/macos.yml` is renamed to `taskfiles/macos.v1.yml.bak` (CF-11 byte-stable preservation; P8 owns deletion). `taskfiles/macos-stub.yml` is deleted. `Taskfile.yml` line 108 `includes.macos:` points to `./taskfiles/macos.yml`. `task --list` shows the macos: namespace; `task lint` exits 0; the LINT-02 regression check on shell:status passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Amend planning / schema documents -- ROADMAP success criteria #1 and #5, docs/MANIFEST.md feature-flag table, and optional REQUIREMENTS OSCF-05 soften</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, docs/MANIFEST.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 125-135 (Phase 6 section + five success criteria; criterion #1 at line 130 + criterion #5 at line 134)
    - .planning/REQUIREMENTS.md OSCF-01..05 block (lines 110-118; OSCF-05 at line 118)
    - docs/MANIFEST.md lines 445-465 (feature-flag reference table; the five macos-* rows currently say "machine-set (not in defaults.toml)" but Plan 01 added them to defaults.toml so the rows need to flip to "false")
    - .planning/phases/06-os-defaults-macos-configuration/06-CONTEXT.md lines 49-56 (ROADMAP / REQUIREMENTS edits planner action items; lists each amend explicitly)
    - .planning/phases/06-os-defaults-macos-configuration/06-PATTERNS.md lines 605-635 (the exact target text for each amend; uses P5 D-02 textual-amend precedent)
  </read_first>
  <action>
    Three coordinated document edits.

    **Edit A -- `.planning/ROADMAP.md` Phase 6 success criterion #1** (line 130 -- D-01 flat-keys amend):
    Current text:
    ```
      1. On a machine with `features.macos-defaults.dock = true`, `task macos:defaults:dock` writes the configured dock keys; on a machine without the feature, the task is a no-op (skipped at the feature-gate level)
    ```
    Replace with (flat schema per D-01):
    ```
      1. On a machine with `features.macos-dock = true`, `task macos:defaults:dock` writes the configured dock keys; on a machine without the feature, the task is a no-op (skipped at the feature-gate level)
    ```
    Only the nested-key reference `features.macos-defaults.dock` changes to flat `features.macos-dock`. The rest of the line is unchanged.

    **Edit B -- `.planning/ROADMAP.md` Phase 6 success criterion #5** (line 134 -- D-02 in-script-values amend):
    Current text:
    ```
      5. `task validate` reads current `defaults` values for declared keys and asserts them against the manifest's expected values for the active machine
    ```
    Replace with (in-script values per D-02):
    ```
      5. `task validate` reads current `defaults` values for declared keys and asserts them against the in-script expected values for each enabled concern
    ```
    The substitution is "manifest's expected values for the active machine" -> "in-script expected values for each enabled concern". Per D-02, values live in `os/defaults/<concern>.zsh` scripts (the tuple arrays), not in the manifest -- the manifest gates only on/off.

    **Edit C -- `.planning/REQUIREMENTS.md` OSCF-05** (line 118 -- mirror amend; optional per planner discretion -- recommend applying for doc consistency with ROADMAP #5):
    Current text:
    ```
    - [ ] **OSCF-05**: `task validate` asserts current defaults values match manifest expectations for the active machine
    ```
    Replace with:
    ```
    - [ ] **OSCF-05**: `task validate` asserts current defaults values match in-script expectations for each enabled concern
    ```

    **Edit D -- `docs/MANIFEST.md` feature-flag reference table** (lines 450-460):
    The five macos-* rows currently say `machine-set (not in defaults.toml)` in the "Default in `defaults.toml`" column. Plan 01 added all four new keys to defaults.toml (and macos-finder was already there since P3), so the column must flip to `false` (the actual default value). Additionally, macos-finder's row gets a dual-consumer note per D-01.

    Replace the five macos-* rows with:
    ```
    | `macos-dock` | Phase 6 | Runs `os/defaults/dock.zsh` | `false` |
    | `macos-finder` | Phase 3 + Phase 6 | Gates `shell/aliases/finder.zsh` (P3 D-07) + runs `os/defaults/finder.zsh` (P6 D-01 same-flag-two-consumers) | `false` |
    | `macos-input` | Phase 6 | Runs `os/defaults/input.zsh` | `false` |
    | `macos-screenshots` | Phase 6 | Runs `os/defaults/screenshots.zsh` | `false` |
    | `macos-security` | Phase 6 | Runs `os/defaults/security.zsh` | `false` |
    ```
    The other rows in the table (one-password-ssh, one-password-signing, motd, claude-marketplace) are unchanged. The macos-finder row's "Owner phase" column changes from `Phase 6` to `Phase 3 + Phase 6` to record dual ownership; its "What it does" cell expands to name both consumers; its "Default" cell stays `false`.

    Use the Edit tool for each amend; do not rewrite the whole files. After all four edits:
    - `grep -q 'features.macos-dock = true' .planning/ROADMAP.md` returns 0 (criterion #1 amend landed).
    - `grep -q 'in-script expected values' .planning/ROADMAP.md` returns 0 (criterion #5 amend landed).
    - `grep -q 'in-script expectations' .planning/REQUIREMENTS.md` returns 0 (OSCF-05 amend landed).
    - `grep -c '^| \`macos-.*\` |.*| \`false\` |$' docs/MANIFEST.md` returns 5 (all five macos-* rows show `false` in their default column).
    - `grep -q 'same-flag-two-consumers' docs/MANIFEST.md` returns 0 (macos-finder dual-consumer note landed).
  </action>
  <verify>
    <automated>grep -q 'features.macos-dock = true' .planning/ROADMAP.md && grep -q 'in-script expected values' .planning/ROADMAP.md && ! grep -q 'features.macos-defaults.dock' .planning/ROADMAP.md && grep -q 'in-script expectations' .planning/REQUIREMENTS.md && grep -c '^| `macos-.*` |.*| `false` |$' docs/MANIFEST.md | grep -q '^5$' && grep -q 'same-flag-two-consumers' docs/MANIFEST.md && ! grep -q 'machine-set (not in defaults.toml)' docs/MANIFEST.md</automated>
  </verify>
  <done>
    `.planning/ROADMAP.md` Phase 6 success criterion #1 uses flat `features.macos-dock` (no nested `features.macos-defaults.dock` anywhere in the file); criterion #5 uses "in-script expected values" wording per D-02. `.planning/REQUIREMENTS.md` OSCF-05 mirrors the soften. `docs/MANIFEST.md` feature-flag table shows `false` (not `machine-set...`) in the Default column for all five macos-* rows; macos-finder row records the P3+P6 dual-consumer ownership.
  </done>
</task>

</tasks>

<verification>
End-to-end checks for Plan 03:

1. **Real taskfile lands; stub is gone; v1 monolith preserved:**
   - `test -f taskfiles/macos.yml && test -f taskfiles/macos.v1.yml.bak && [[ ! -f taskfiles/macos-stub.yml ]]` exits 0.
   - `grep -q '^  macos:    \./taskfiles/macos\.yml' Taskfile.yml` exits 0; `! grep -q 'macos-stub' Taskfile.yml` exits 0.
2. **All eight tasks exported with correct shapes:**
   - `yq '.tasks | keys' taskfiles/macos.yml` returns a list containing `defaults`, `defaults:dock`, `defaults:finder`, `defaults:input`, `defaults:screenshots`, `defaults:security`, `shell`, `validate`.
   - Each `defaults:<concern>` task's `status:` block contains `index .MANIFEST.features "macos-<concern>"`.
3. **LINT-02 structural fix on shell:status (the bug-class regression check):**
   - `yq '.tasks.shell.status' taskfiles/macos.yml | grep -qE '\{\{\.BREW_ZSH\}\}'` exits 0 (template var present).
   - `yq '.tasks.shell.status' taskfiles/macos.yml | grep -E '\$BREW_ZSH\b'` returns nothing (NO `$BREW_ZSH` literal references in status; this is the LINT-02 contract per CONCERNS.md lines 15-19).
4. **Feature-gate single-shell-block status (the Pattern 2 vs CONTEXT broken-two-line check):**
   - Each `defaults:<concern>` status block is ONE heredoc entry (single `- |` shell block) containing both the `{{if not ...}}exit 0{{end}}` short-circuit AND the source+verify call -- NOT two separate status entries. Check: `yq '.tasks."defaults:dock".status' taskfiles/macos.yml | wc -l` returns a single multi-line block, not two separate `-` entries; the heredoc text contains both `exit 0{{end}}` AND `verify_dock`.
5. **Aggregator markers:** `grep -c "# lint-allow: cmds-without-status" taskfiles/macos.yml` returns >= 2 (defaults + validate; possibly also annotated on additional tasks).
6. **Lint passes:** `task lint` exits 0 (LINT-05 warnings on `dscl` in shell:status are warn-only).
7. **Task discovery:** `task --list` shows `macos:defaults`, `macos:shell`, `macos:validate` (non-internal tasks).
8. **Functional smoke (on active machine):** `task macos:validate` runs to completion and prints check/cross output for every enabled concern + shell-registration; exit code is 0 on a converged machine.
9. **Documentation amends landed:** all four greps in Task 2's verify command pass; no `features.macos-defaults.dock` text anywhere in `.planning/ROADMAP.md`; no `machine-set (not in defaults.toml)` text anywhere in `docs/MANIFEST.md`.
10. **Idempotency (regression):** `task macos:defaults` twice on the converged dev machine -- second invocation prints no "writing ..." info lines from concern apply paths (status blocks gate the re-run). For `task macos:shell` specifically (the v1 bug class), second invocation must NOT print the "Adding Homebrew zsh to /etc/shells..." or "Changing default shell ..." lines.
</verification>

<success_criteria>
- `taskfiles/macos.yml` is the real Phase 6 module with all eight tasks; v1 `taskfiles/macos.yml` preserved at `taskfiles/macos.v1.yml.bak` per CF-11; `taskfiles/macos-stub.yml` deleted; `Taskfile.yml` `includes.macos:` flipped.
- Every per-concern task uses the RESEARCH Pattern 2 single-shell-block status (`{{if not ...}}exit 0{{end}}` as the second line of one heredoc); the broken two-line ANDed status from CONTEXT.md is NOT used.
- `shell:status` uses `{{.BREW_ZSH}}` template var in both entries; zero `$BREW_ZSH` literal references -- the LINT-02 contract / v1 `macos:shell:145` bug class is structurally closed.
- `validate` is `status: [false]` always-rerun with per-concern `{{if index .MANIFEST.features ...}}` guards; enumerates all enabled concerns in OSCF-01 order (dock -> finder -> input -> screenshots -> security) + always-on shell-registration; exits 0 on converged machine, non-zero on any drift.
- `task lint` exits 0; `task --list` surfaces the macos namespace; `task macos:validate` runs to completion on the dev machine.
- ROADMAP success criteria #1 and #5 amended per D-01 + D-02; REQUIREMENTS OSCF-05 mirror amend; docs/MANIFEST.md feature-flag table shows `false` for all five macos-* rows with macos-finder dual-consumer note.
</success_criteria>

<output>
After completion, write a SUMMARY at `.planning/phases/06-os-defaults-macos-configuration/06-03-SUMMARY.md`. Record:
- Locked decisions implemented: D-01 (kebab-case feature gates via index form), D-02 (apply/verify sourced from scripts), D-03 (shell-registration always-on; BREW_ZSH via task vars), D-04 (server-1 + server-2 see only macos:shell + macos:defaults:security)
- Files touched: `taskfiles/macos.yml` (new), `taskfiles/macos.v1.yml.bak` (renamed from v1), `taskfiles/macos-stub.yml` (deleted), `Taskfile.yml` (one-line include flip), `docs/MANIFEST.md` (feature-flag table), `.planning/ROADMAP.md` (two success-criterion amends), `.planning/REQUIREMENTS.md` (OSCF-05 mirror amend)
- Bug-class structural fix completion: `shell:status` uses `{{.BREW_ZSH}}` -- the v1 `macos:shell:145` `$BREW_ZSH`-in-status bug class is closed. `grep '\$BREW_ZSH' taskfiles/macos.yml` returns matches only in cmds: (legal) and the file-header comment (legal); zero matches in any `.status` line per yq query.
- LINT-05 portability warnings: expected to fire on `defaults`/`dscl` lines in the heredocs; warn-only per LINT-05 contract; non-blocking; documented in os/README.md (Plan 02 deliverable).
- Hand-off to Plan 04: HUMAN-UAT plan owns the manual UAT (server-mode install, laptop-mode install, idempotency timing, bug-class regression grep, deliberate-mismatch + validate-non-zero) on real macOS machines.
</output>
</content>
</invoke>
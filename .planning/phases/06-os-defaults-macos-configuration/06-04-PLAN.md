---
phase: 06-os-defaults-macos-configuration
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md
autonomous: false
requirements: [OSCF-03, OSCF-04, OSCF-05]
requirements_addressed: [OSCF-03, OSCF-04, OSCF-05]
must_haves:
  truths:
    - "06-HUMAN-UAT.md exists and enumerates manual UAT scenarios for every behavior in VALIDATION.md's Manual-Only Verifications table (server-mode install, laptop-mode install, re-run idempotency, bug-class regression, deliberate-mismatch + validate non-zero)"
    - "Every UAT test records the manifest state needed (which machine name is active via $XDG_STATE_HOME/dotfiles/machine), the exact task command to run, the expected observable output (info / check / cross lines, exit codes), and the success criterion (a single yes/no the human can answer)"
    - "Every UAT test states the recovery / rollback steps so a human can safely abort mid-test and return the dev machine to its pre-test state (e.g. restoring a deliberately-borked defaults value via apply_dock; switching $XDG_STATE_HOME/dotfiles/machine back to the regular dev machine after server-mode simulation)"
    - "The bug-class regression test for OSCF-04 / LINT-02 has an automated component: 'yq .tasks.shell.status taskfiles/macos.yml | grep -qE \"\\$BREW_ZSH\\b\" returns exit 1 (no matches)' is callable without a real /etc/shells write -- this is a static-analysis pre-check before the runtime UAT"
    - "The HUMAN-UAT plan includes an explicit sign-off table at the end where the executor + human record green/red per test with date + machine + tester recorded so /gsd-verify-work can read the artifact and gate phase completion"
  artifacts:
    - path: ".planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md"
      provides: "Manual UAT plan for Phase 6; consumed by /gsd-verify-work; gates phase completion"
      contains: "Server-mode install"
  key_links:
    - from: "06-HUMAN-UAT.md"
      to: "06-VALIDATION.md ## Manual-Only Verifications table"
      via: "1:1 mapping -- each Manual-Only row gets a corresponding UAT test in HUMAN-UAT"
      pattern: "Server-mode install|Laptop-mode install|Round-trip idempotency|sysadminctl|chsh"
    - from: "06-HUMAN-UAT.md"
      to: "/gsd-verify-work"
      via: "sign-off table at end of UAT plan; verify-work reads test statuses to gate phase completion"
      pattern: "Sign-off"
---

<objective>
Author the manual UAT plan for Phase 6 at `.planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md`. The plan is the human-execution surface for verifications that cannot be asserted from a single shell command on the dev machine: server-mode install simulation, full laptop-mode install round-trip, re-run idempotency timing, the v1 `macos:shell:145` bug-class regression check (static + runtime), and a deliberate-mismatch test that proves `task macos:validate` exits non-zero on drift. The plan is read and executed by the human operator before `/gsd-verify-work` is invoked; verify-work consumes the sign-off table at the end of the file.

This plan is `autonomous: false` because Task 2 is a `checkpoint:human-verify` that pauses for human-required UAT runs (sudo prompts for `/etc/shells` append on first install of a fresh machine, deliberate-borking of defaults values that the human must restore, switching the active-machine state file to simulate server-1 and back). Wave 2 depends on Wave 1 wiring being live -- the UAT plan tests a real `task install` round trip, which means `taskfiles/macos.yml` (Plan 03) must be in place and the include flip must be active.

Purpose: VALIDATION.md flagged HUMAN-UAT.md as a Wave 0 deliverable but its CONTENTS are only meaningful once the taskfile and scripts are live. Plan 04 ships the document in Wave 2 so its instructions reference real artifacts. The plan is the gate: /gsd-verify-work reads its sign-off section to decide whether OSCF-03 / OSCF-04 / OSCF-05 are demonstrably satisfied on real hardware.

Output: 1 new markdown file under the phase directory; no other changes.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 6 source-of-truth artifacts
@.planning/phases/06-os-defaults-macos-configuration/06-CONTEXT.md
@.planning/phases/06-os-defaults-macos-configuration/06-RESEARCH.md
@.planning/phases/06-os-defaults-macos-configuration/06-PATTERNS.md
@.planning/phases/06-os-defaults-macos-configuration/06-VALIDATION.md

# Prior-plan SUMMARYs (Wave 0 + Wave 1 outputs the UAT plan exercises)
@.planning/phases/06-os-defaults-macos-configuration/06-01-SUMMARY.md
@.planning/phases/06-os-defaults-macos-configuration/06-02-SUMMARY.md
@.planning/phases/06-os-defaults-macos-configuration/06-03-SUMMARY.md

# Prior-phase HUMAN-UAT precedent (planner mirrors structure)
@.planning/phases/05-packages-layer-brewfile-composition-verification/05-HUMAN-UAT.md

# Files the UAT plan references (executor reads to know real commands and paths)
@taskfiles/macos.yml
@Taskfile.yml
@os/defaults/dock.zsh
@os/shell-registration.zsh
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 06-HUMAN-UAT.md with five tests and run the two auto-tests</name>
  <files>.planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06-os-defaults-macos-configuration/06-VALIDATION.md lines 81-92 (Manual-Only Verifications table -- the source of UAT test scenarios; every row maps 1:1 to a UAT test in this file)
    - .planning/phases/05-packages-layer-brewfile-composition-verification/05-HUMAN-UAT.md (the precedent UAT plan; mirror its structure -- intro, environment requirements, numbered tests with steps + expected output + success criterion + rollback, sign-off table at end)
    - taskfiles/macos.yml (Plan 03 output; UAT tests run task commands against this file -- e.g. `task macos:defaults`, `task macos:shell`, `task macos:validate`)
    - .planning/codebase/CONCERNS.md lines 15-19 (the bug class one UAT test specifically targets; cite the file + line in the test description)
    - .planning/phases/06-os-defaults-macos-configuration/06-RESEARCH.md lines 521-555 (Pitfall 1: the two-line ANDed status bug -- one UAT test specifically validates that feature-off machines skip without running verify)
    - .planning/phases/06-os-defaults-macos-configuration/06-CONTEXT.md D-04 (server-mode contract: only macos:shell + macos:defaults:security run on server-1; UAT test 2 simulates this by switching the active machine)
  </read_first>
  <action>
    Author `.planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md` with the structure described below, then auto-run Test 1 (LINT-02 static regression) and Test 5 (Lint-suite regression), recording results in the sign-off table. Tests 2, 3, 4 are runtime/manual and are deferred to Task 2's human checkpoint.

    **Author the file in this exact structure:**

    1. **YAML frontmatter** -- mirrors VALIDATION.md frontmatter style: `phase: 06`, `slug: os-defaults-macos-configuration`, `status: draft`, `created: <today>`, `last_run: ""`, `tester: ""`.

    2. **`# Phase 6 -- macOS OS-defaults + shell-registration -- Manual UAT`** H1 + one-paragraph intro stating the purpose: "Verify on real hardware that Phase 6's per-concern defaults model, feature-gate behavior, server-mode contract, idempotency, and the v1 `macos:shell:145` bug-class fix are demonstrably correct."

    3. **`## Pre-conditions`** -- enumerate the environment requirements: macOS dev machine (verified macOS 14+ per RESEARCH); Homebrew zsh installed at `${HOMEBREW_PREFIX}/bin/zsh` (P5 dep); `task --list` shows the `macos:` namespace (Plan 03 must be deployed); `$XDG_STATE_HOME/dotfiles/machine` records a valid machine name; `resolved.json` has the five `features.macos-*` keys; the v1 `taskfiles/macos.v1.yml.bak` is on disk (CF-11 preservation -- verify with `test -f`). Include a `git stash` recommendation: "If the dev machine is the active checkout, `git stash` any in-progress edits before running the destructive UAT scenarios -- the deliberate-mismatch test writes a wrong defaults value and the rollback step depends on the apply path being callable."

    4. **`## Test 1 -- LINT-02 static regression check (OSCF-04)`** -- automated; runs without human interaction; AUTO-RUN by the executor as part of this task:
       - **What it proves:** `taskfiles/macos.yml` `shell:status` block contains no `$BREW_ZSH` shell var references (the v1 `macos:shell:145` bug class fix). Cite `.planning/codebase/CONCERNS.md` lines 15-19 as the historical reference.
       - **Steps:**
         1. `yq '.tasks.shell.status' taskfiles/macos.yml | grep -E '\$BREW_ZSH\b'` MUST return nothing (exit 1).
         2. `yq '.tasks.shell.status' taskfiles/macos.yml | grep -qE '\{\{\.BREW_ZSH\}\}'` MUST return 0 (template var present).
         3. `task lint:taskfile` MUST exit 0 (the LINT-02 scanner agrees there are no `$VAR` violations in status: blocks).
       - **Success criterion:** all three commands behave as specified.
       - **Rollback:** none required (read-only static analysis).

    5. **`## Test 2 -- Server-mode install simulation (OSCF-04 / ROADMAP SC#4)`** -- runtime; HUMAN-REQUIRED:
       - **What it proves:** On a machine with `macos-security = true` and the other four `macos-*` features absent (inherited false), `task install` runs `macos:shell` + `macos:defaults:security` only; the other four concern tasks skip at the feature-gate level. This is D-04 + ROADMAP SC#4 on real hardware.
       - **Steps:**
         1. Capture current state: `CURRENT_MACHINE=$(cat "$XDG_STATE_HOME/dotfiles/machine")` and save it.
         2. Switch active machine to `server-1`: `task setup -- server-1` (this runs the resolver against `manifests/machines/server-1.toml`; the four GUI concern feature flags inherit `false` from defaults; `macos-security = true` per Plan 01).
         3. Run `task manifest:resolve && jq -r '.features | to_entries | map(select(.key|startswith("macos-"))) | .[] | "\(.key)=\(.value)"' "$XDG_STATE_HOME/dotfiles/resolved.json"`. Expected output: `macos-dock=false`, `macos-finder=false`, `macos-input=false`, `macos-screenshots=false`, `macos-security=true`.
         4. Run `task macos:defaults --verbose 2>&1 | tee /tmp/p6-test2.log`. Expected output: tasks `macos:defaults:dock`, `macos:defaults:finder`, `macos:defaults:input`, `macos:defaults:screenshots` all print `task: Task "macos:defaults:<concern>" is up to date` (the single-shell-block status's `exit 0` short-circuit fires). Task `macos:defaults:security` runs `apply_security` (info/warn lines from the script + sudo prompt if guest account is enabled and not previously disabled).
         5. Run `task macos:shell`. Expected: either runs apply (if /etc/shells doesn't have Homebrew zsh OR the user's registered shell isn't Homebrew zsh -- both status conditions ANDed must pass for skip) or skips with `task: Task "macos:shell" is up to date`.
         6. Re-run `task macos:defaults` immediately. Expected: ALL five sub-tasks report "up to date" (security:status now passes because apply just ran; the other four skip via feature gate). Confirm zero `apply_*` info lines in the second-run output.
       - **Success criterion:** Step 4's output shows four sub-tasks skipped + one ran; Step 6's output shows ALL five sub-tasks skipped (idempotency on the second run).
       - **Rollback:**
         1. Switch back to the original dev machine: `task setup -- "$CURRENT_MACHINE"`.
         2. Run `task manifest:resolve` to restore the original `resolved.json`.
         3. (Optional) If the security apply set guest-account=disabled on a machine where it was previously enabled and the dev wants to revert: `sudo sysadminctl -guestAccount on` (rarely needed; guest account is rarely intentionally enabled on dev machines).

    6. **`## Test 3 -- Laptop-mode install round-trip + idempotency (OSCF-03)`** -- runtime; HUMAN-REQUIRED:
       - **What it proves:** On a machine with all five `macos-*` features `true` (the dev's personal-laptop or work-laptop default), the full install pipeline runs each concern's apply once; a second invocation is a no-op (the `verify_<concern>` status calls pass cleanly).
       - **Steps:**
         1. Verify active machine is a laptop with all five flags `true`: `jq -r '.features | to_entries | map(select(.key|startswith("macos-"))) | .[] | "\(.key)=\(.value)"' "$XDG_STATE_HOME/dotfiles/resolved.json"` -- expect all five = true.
         2. Run `time task macos:defaults 2>&1 | tee /tmp/p6-test3-first.log` -- capture timing. Expected: first run may take seconds (apply paths actually write defaults + killall Dock/Finder/SystemUIServer per concern). Each concern script's info lines from `apply_<concern>` appear in the log.
         3. Run `time task macos:defaults 2>&1 | tee /tmp/p6-test3-second.log` immediately after. Expected: completes in under 2 seconds (all five status blocks gate the re-run); zero "Adding" / "Applying" / "Writing" info lines in the log; every concern reports `task: Task "macos:defaults:<concern>" is up to date`.
         4. Run `task macos:shell` twice in sequence (`task macos:shell && task macos:shell`). Expected first run: applies (or skips if already converged); expected second run: ALWAYS skips with `task: Task "macos:shell" is up to date`. If the second run still prints "Adding Homebrew zsh to /etc/shells..." or "Changing default shell ...", the v1 `macos:shell:145` bug class regressed -- FAIL THIS TEST.
       - **Success criterion:** First-run timing > second-run timing by at least 2x; second-run log contains no apply info lines; `task macos:shell` is genuinely idempotent (the bug-class fix holds).
       - **Rollback:** none required; the round trip lands the canonical converged state.

    7. **`## Test 4 -- task macos:validate against deliberate drift (OSCF-05)`** -- runtime; HUMAN-REQUIRED; mutates state but the rollback is the apply path itself:
       - **What it proves:** `task macos:validate` exits non-zero when any single defaults key is drifted away from its in-script expected value; the cross-line output names the failing key with both expected and actual values.
       - **Steps:**
         1. Confirm `task macos:validate` exits 0 on the currently-converged machine: `task macos:validate; echo "exit=$?"`. Expected: `exit=0`; check lines for every enabled concern's keys.
         2. Deliberately bork a Dock key: `defaults write com.apple.dock orientation -string "left"; killall Dock 2>/dev/null || true`. (Dock orientation flips from `bottom` -- the in-script expected value -- to `left`.)
         3. Run `task macos:validate; echo "exit=$?"`. Expected: `exit=1` (non-zero); the output contains a cross line of the shape `dock.orientation: expected 'bottom', got 'left'`.
         4. Restore the converged state: `task macos:defaults:dock` (apply_dock writes orientation back to bottom + killall Dock to refresh). Run `task macos:validate; echo "exit=$?"` again. Expected: `exit=0`; no cross lines.
       - **Success criterion:** Step 3 produces exit-1 + the specific cross line for dock.orientation; Step 4 restores exit-0.
       - **Rollback:** Step 4 is the rollback. If `task macos:defaults:dock` fails for any reason: manually run `defaults write com.apple.dock orientation -string "bottom" && killall Dock 2>/dev/null || true` and re-run `task macos:validate` to confirm.

    8. **`## Test 5 -- Lint-suite regression on the new taskfile (OSCF-04 / LINT-02 / LINT-05 expected warnings)`** -- automated; auto-run by the executor:
       - **What it proves:** `task lint` exits 0 against the new `taskfiles/macos.yml`. LINT-02 (status `$VAR` scanner) MUST find no violations. LINT-05 (portability warn-list for `defaults`/`dscl`) WILL fire warn-only on the shell:status block; the warnings are expected and intentional per RESEARCH Pitfall 10.
       - **Steps:**
         1. `task lint` MUST exit 0.
         2. `task lint:taskfile` MUST exit 0 (LINT-02 check passes).
         3. `task lint:portability 2>&1 | grep -E 'defaults|dscl' | wc -l` MUST be > 0 (warn-only output present and expected); the task itself MUST exit 0 (warn-only).
       - **Success criterion:** all three commands behave as specified.
       - **Rollback:** none required (read-only).

    9. **`## Sign-off`** -- a table the executor + human operator update during UAT execution:
       ```
       | Test | Type | Status | Date | Tester | Notes |
       |------|------|--------|------|--------|-------|
       | 1. LINT-02 static regression | auto | pending | | | |
       | 2. Server-mode install simulation | manual | pending | | | |
       | 3. Laptop-mode round-trip + idempotency | manual | pending | | | |
       | 4. macos:validate against deliberate drift | manual | pending | | | |
       | 5. Lint-suite regression | auto | pending | | | |
       ```
       Plus the final approval line: `**Phase 6 UAT approved by:** (name)  (date)` -- the human fills this in after all five tests are green.

    10. **`## References`** -- bullet list pointing to the source-of-truth files for each test: VALIDATION.md Manual-Only table; CONCERNS.md bug-class line range; the per-test requirement IDs (OSCF-03 / OSCF-04 / OSCF-05); RESEARCH Pitfall 1 (the broken two-line status to NOT regress) and Pitfall 10 (expected LINT-05 warnings).

    **After authoring the file, run the two automated tests now (Test 1 + Test 5):**
    1. Execute Test 1's three commands; capture the actual outputs and exit codes.
    2. Execute Test 5's three commands; capture the actual outputs and exit codes.
    3. Update the sign-off table for Tests 1 and 5: set Status to `green` (or `red` if a check failed), set Date to today's YYYY-MM-DD, set Tester to the value of `git config user.name`, set Notes to a one-line summary (e.g. "auto-passed YYYY-MM-DD; yq grep returned no matches; task lint exit 0").

    If Test 1 OR Test 5 fails: STOP. Do not proceed to Task 2's checkpoint. Instead, surface the failure in the SUMMARY and recommend a revision plan (likely a Plan 03 follow-up: the LINT-02 contract or task lint regression must be re-fixed in `taskfiles/macos.yml` before UAT continues).
  </action>
  <verify>
    <automated>test -f .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md && for n in 1 2 3 4 5; do grep -qE "^## Test $n " .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md || { echo "missing Test $n" >&2; exit 1; }; done && grep -q "^## Sign-off$" .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md && grep -q "approved by" .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md && grep -q "^## Pre-conditions$" .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md && grep -q "^## References$" .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md && grep -qE "1\. LINT-02 static regression \| auto \| green" .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md && grep -qE "5\. Lint-suite regression \| auto \| green" .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md</automated>
  </verify>
  <done>
    `06-HUMAN-UAT.md` exists with five named tests (Test 1..5) covering the OSCF-03/OSCF-04/OSCF-05 manual verification surface; Pre-conditions section, Sign-off table, and References section all present. The two auto-tests (Test 1 + Test 5) have been executed; their sign-off rows show `green` status with date + tester + notes. Tests 2-4 sign-off rows still say `pending` (manual; deferred to Task 2's human checkpoint).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human executes runtime UAT tests 2, 3, 4 and signs off the phase</name>
  <files>.planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md (the file Task 1 just authored; the human reads Pre-conditions then executes Tests 2-4 step by step)
  </read_first>
  <action>
    PAUSE for human-required UAT runs on real macOS hardware. The human operator executes Tests 2, 3, and 4 from `06-HUMAN-UAT.md` in their own terminal session, then updates the sign-off table and approval line.

    Task 1 has already authored `06-HUMAN-UAT.md` and auto-run Tests 1 and 5 (static-analysis regressions). The file is ready for the human operator to execute the three runtime tests and record sign-off. The runtime tests are:
    - Test 2 (Server-mode install simulation): switches active machine to `server-1`, runs `task macos:defaults`, verifies four GUI concerns skip via feature gate while macos-security applies + macos:shell runs.
    - Test 3 (Laptop-mode round-trip + idempotency): verifies all five concerns apply on the first run; verifies the second run is a no-op (zero apply info lines); verifies `task macos:shell && task macos:shell` is genuinely idempotent (the v1 `macos:shell:145` bug class is closed).
    - Test 4 (validate against deliberate drift): writes a wrong `com.apple.dock orientation` value, verifies `task macos:validate` exits non-zero with a specific cross line, restores the converged state via `task macos:defaults:dock`.

    All three tests have explicit rollback steps in the UAT file. Test 2 and Test 4 mutate observable state (machine selection; defaults value); Test 3 is non-destructive.

    Human operator instructions:

    1. Open `.planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md` and read the full Pre-conditions section. Confirm every pre-condition holds on your dev machine.
    2. Execute Test 2 step-by-step exactly as written in the file. After each step, compare actual output to the expected output described in the file. After completing all steps + the rollback (step 1 of the rollback: switch back to original machine), update the Test 2 row in the sign-off table: set Status to `green` (or `red` with a description of the discrepancy), set Date to today, set Tester to your name, set Notes to a one-line observation (e.g. "skip-message confirmed on dock/finder/input/screenshots; security ran apply_security; sudo prompt did not fire because guest account was already disabled").
    3. Execute Test 3 step-by-step. Pay specific attention to the second `task macos:shell` invocation -- if it re-runs the apply path, the v1 bug class has regressed and the test FAILS. Update the Test 3 row with first-run + second-run timings in the Notes column (e.g. "first run 6.2s; second run 0.4s; macos:shell second invocation skipped cleanly").
    4. Execute Test 4 step-by-step. The deliberate-bork step writes a wrong defaults value; you MUST run the rollback step (`task macos:defaults:dock`) before signing off the test as green. If `task macos:defaults:dock` fails for any reason, run the manual recovery in the rollback section. Update the Test 4 row with the observed cross-line text in the Notes column.
    5. After all three runtime tests are green, fill in the final approval line at the bottom of the UAT file: `**Phase 6 UAT approved by:** <your name>  <today's date>`.
    6. Save the file. The sign-off table is now consumable by `/gsd-verify-work` to gate phase completion.

    Resume signal: Type "approved" after the sign-off table shows green for all five tests (Tests 1, 5 auto; Tests 2, 3, 4 manual) and the approval line is filled in. Type "issue: <description>" if any runtime test fails -- the planner will revise the failing artifact (likely Plan 02's scripts or Plan 03's taskfile) per the failure mode and re-issue this checkpoint after the fix lands.
  </action>
  <verify>
    <automated>grep -qE '^\| 2\. Server-mode install simulation \| manual \| green' .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md && grep -qE '^\| 3\. Laptop-mode round-trip \+ idempotency \| manual \| green' .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md && grep -qE '^\| 4\. macos:validate against deliberate drift \| manual \| green' .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md && grep -qE '^\*\*Phase 6 UAT approved by:\*\* [A-Za-z]' .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md</automated>
  </verify>
  <done>
    The human operator has executed Tests 2, 3, and 4 from `06-HUMAN-UAT.md`. The sign-off table now shows `green` status for all five tests (Tests 1, 5 auto; Tests 2, 3, 4 manual). The final approval line at the bottom of the UAT file is filled in with the operator's name + date. `/gsd-verify-work` can now read the sign-off table to confirm Phase 6 is demonstrably correct on real hardware.
  </done>
</task>

</tasks>

<verification>
End-to-end checks for Plan 04:

1. **File exists with required sections:** `test -f .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md && grep -q "^## Pre-conditions$" .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md && grep -q "^## Sign-off$" .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md && grep -q "^## References$" .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md`.
2. **Five tests present:** `grep -cE '^## Test [1-5] ' .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md` returns 5.
3. **Auto-tests pre-passed (after Task 1):** the sign-off table rows for Test 1 and Test 5 contain `green` status + a YYYY-MM-DD date.
4. **Manual tests human-approved (after Task 2):** sign-off table rows for Tests 2, 3, 4 contain `green` status; the final "approved by" line is filled in with a name + date.
5. **Cross-references valid:**
   - The bug-class reference: `grep -q "CONCERNS.md.*15-19" .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md` (Test 1 cites the historical bug class).
   - The RESEARCH Pitfall references: `grep -qE "Pitfall (1|10)" .planning/phases/06-os-defaults-macos-configuration/06-HUMAN-UAT.md` (Tests 1 and 5 reference the relevant pitfalls).
6. **VALIDATION coverage:** every row of VALIDATION.md's Manual-Only Verifications table has a corresponding test in HUMAN-UAT.md (5 rows -> 3 manual tests; the killall side-effects and chsh-immediate-effect concerns are folded into Test 3's idempotency observations).
</verification>

<success_criteria>
- `06-HUMAN-UAT.md` exists with five named tests covering: LINT-02 static regression (auto), server-mode install simulation (manual), laptop-mode round-trip + idempotency (manual), macos:validate-against-deliberate-drift (manual), and full lint-suite regression (auto).
- Every test specifies exact commands, expected output, success criterion, and rollback steps.
- The two auto-tests are run during Task 1 and recorded as passed before Task 2's checkpoint pauses.
- The manual tests are executed by the human during Task 2 and the sign-off table is updated with green status across all five rows.
- The "Phase 6 UAT approved by:" line at the bottom is filled in.
- `/gsd-verify-work` can read the sign-off table to confirm Phase 6 is demonstrably correct on real hardware.
</success_criteria>

<output>
After completion, write a SUMMARY at `.planning/phases/06-os-defaults-macos-configuration/06-04-SUMMARY.md`. Record:
- UAT file authored at canonical path; five tests covering OSCF-03 / OSCF-04 / OSCF-05
- Auto-test results: Tests 1 and 5 auto-passed at YYYY-MM-DD; record specific yq / grep / task lint exit codes observed
- Manual-test results: Tests 2, 3, 4 status (green / red / blocked); record the timing observation from Test 3 (first-run seconds vs second-run seconds) and the cross-line text observed in Test 4
- v1 bug-class regression: explicit confirmation that `task macos:shell && task macos:shell` second run is genuinely idempotent (the `macos:shell:145` `$BREW_ZSH`-in-status bug class is closed)
- Hand-off to /gsd-verify-work: phase is demonstrably correct on real hardware; verify-work can proceed to phase completion
</output>
</content>
</invoke>
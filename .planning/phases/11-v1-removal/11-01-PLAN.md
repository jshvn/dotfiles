---
phase: 11-v1-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Deletions (file-level)
  - taskfiles/common.yml
  - taskfiles/profile.yml
  - taskfiles/brew.yml
  - taskfiles/profile-tasks.yml
  - taskfiles/claude-stub.yml
  - taskfiles/brew-stub.yml
  - taskfiles/links-stub.yml
  - taskfiles/macos.v1.yml.bak
  - install/Brewfile.rb
  - install/Brewfile-personal.rb
  - install/Brewfile-server.rb
  - install/Brewfile-work.rb
  - install/cutover-gate.zsh
  - docs/CUTOVER.md
  - docs/MIGRATION.md
  # Tree deletion
  - zsh/
  # Edits
  - Taskfile.yml
  - bootstrap.zsh
  - README.md
  - docs/SECURITY.md
  - taskfiles/lint.yml
  - taskfiles/README.md
  - os/defaults/dock.zsh
  - os/defaults/finder.zsh
  - os/defaults/input.zsh
  - os/defaults/screenshots.zsh
  - os/defaults/security.zsh
  - os/shell-registration.zsh
  # Creation
  - .planning/phases/11-v1-removal/11-VERIFICATION.md
autonomous: true
requirements: [RMV-01, RMV-02, RMV-03, RMV-04, RMV-05, RMV-06, RMV-07]

must_haves:
  truths:
    - "The 8 v1 leftover taskfiles are deleted from the repo; `ls taskfiles/` shows only v2 files."
    - "The v1 `zsh/` directory is deleted (`shell/` is the only shell-content tree); `install/Brewfile*` files are deleted (`packages/<purpose>.rb` is the only Brewfile source)."
    - "Cutover infrastructure is fully removed: `install/cutover-gate.zsh` deleted, `cutover:ack` task removed from `Taskfile.yml`, `cutover_gate_check` precondition removed from `install:`, `docs/CUTOVER.md` deleted, `docs/MIGRATION.md` deleted; the per-machine 7-day-soak model is retired entirely."
    - "`Taskfile.yml` is simplified: the 'v1 leftover taskfiles' comment block (lines ~22-26) is removed; the include list contains only real v2 taskfiles; no v1 file path appears anywhere in the file."
    - "`git grep -E '\\bv1\\b|profile_suffix|DOTFILES_PROFILE|cutover'` (excluding `.planning/` and `.claude/`) returns only references the operator has deliberately kept; any retained refs are listed with rationale in `11-VERIFICATION.md`."
    - "Steady-state `task install` on personal-laptop runs the simplified pipeline (links:all -> packages:install -> claude:install -> macos:defaults -> macos:shell -> packages:verify -> links:reconcile --warn-only), exits 0, with no `cutover:ack` step ever invoked."
  artifacts:
    - path: "Taskfile.yml"
      provides: "Simplified root taskfile with no cutover infrastructure and no v1 leftover taskfile comment block"
      absent_tokens: ["cutover:ack", "cutover_gate_check", "v1 leftover taskfiles", "install/cutover-gate.zsh"]
    - path: "bootstrap.zsh"
      provides: "Tools-only bootstrap with no cutover-ack gate step"
      absent_tokens: ["cutover-ack gate", "install/cutover-gate.zsh", "cutover_gate_check"]
    - path: "README.md"
      provides: "Fresh-install procedure with no `task cutover:ack` step and no `docs/CUTOVER.md` documentation bullet"
      absent_tokens: ["task cutover:ack", "docs/CUTOVER.md"]
    - path: ".planning/phases/11-v1-removal/11-VERIFICATION.md"
      provides: "Verification report with steady-state install capture + SC#5 grep gate report"
      contains: ["Steady-State Install Capture", "Grep Gate Report"]
  key_links:
    - from: "Taskfile.yml install: task"
      to: "v2 install pipeline (no preconditions)"
      via: "deps: [manifest:resolve] + cmds list"
      pattern: "install:.*deps:.*manifest:resolve"
    - from: "bootstrap.zsh"
      to: "tools-only acquisition + next-step hint"
      via: "Steps 1-3 (brew/task/yq) followed directly by the next-step hint"
      pattern: "Bootstrap complete. Next steps"
    - from: "os/defaults/*.zsh + os/shell-registration.zsh LINT-05 citations"
      to: "install/compose-brewfile.zsh (the surviving canonical exemplar)"
      via: "header comment paragraph"
      pattern: "matches install/resolver.zsh \\+ install/compose-brewfile.zsh pattern"
---

<objective>
Delete every v1 leftover from the repo after Phase 10 proved no live dependency
remains; simplify the v2 surface (Taskfile.yml, bootstrap.zsh, README.md);
retire cutover infrastructure entirely (gate library, ack task, gate
preconditions, cutover/migration docs, doc-drift citations); produce
`11-VERIFICATION.md` capturing the steady-state install no-op and the SC#5
grep gate report.

**Purpose:** RMV-01..07 implement the "v1 deletion" half of v2.1 Cleanup. The
audit (Phase 9) and the keep-implementation (Phase 10) are done; this plan is
the simplify-then-delete pass. Per-machine 7-day-soak model retires; v2 IS the
dotfiles after this phase. Phase 12 (Task Surface Redesign) depends on the
clean v2-only surface this plan leaves.

**Output:** A repo with no v1 taskfiles, no `zsh/` tree, no v1 Brewfiles, no
cutover infrastructure, no v1 references in operator-facing docs, and a
verification artifact at `.planning/phases/11-v1-removal/11-VERIFICATION.md`
documenting the steady-state install pipeline and the post-cleanup grep gate.

**Per D-03 (locked):** All of RMV-01..07 land in this single plan across
multiple commits (~10-15). Mirrors Phase 10's shape.

**Per D-04 (locked):** Callers-first, green-tree-per-commit. Every commit
leaves `task lint:taskfile` at-or-below baseline AND `task install` invokable.
Task ordering below honors this.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-v1-removal/11-CONTEXT.md
@.planning/phases/11-v1-removal/11-PATTERNS.md
@.planning/phases/09-v1-drop-audit/AUDIT.md
@.planning/phases/10-v1-drop-remediation/10-VERIFICATION.md
@.planning/phases/10-v1-drop-remediation/10-SMOKE.md
@CLAUDE.md
@.claude/CLAUDE.md

<interfaces>
<!-- Kept v2 surface that surrounds Phase 11 edits. Executor uses these as -->
<!-- the in-place reference for the "what stays" half of every edit. -->

Taskfile.yml include manifest (lines 13-21, kept verbatim except for the
deleted block immediately below at lines 23-26):

    # Includes:
    #   - manifest (P1, real)
    #   - lint     (P2, real)
    #   - links    (P3, real)
    #   - perf     (P3, real)
    #   - identity (P4, real)
    #   - packages (P5, real)
    #   - claude   (stub; P7 wires real bodies)
    #   - macos    (P6, real)

Taskfile.yml install: task body (lines 313-361) AFTER removing lines 320-328
(the preconditions block) must read like this skeleton:

    # lint-allow: cmds-without-status
    install:
      desc: "Install dotfiles for active machine (canonical entry)"
      summary: |
        task install IS task update -- there is no separate update pipeline.
        Re-running is a no-op (every subtask has a status: block per LINT-01).
      status: [false]
      deps: [manifest:resolve]
      cmds:
        - task: links:all
        - task: packages:install
        ...
        - task: links:reconcile
          vars: { CLI_ARGS: "--warn-only" }
        - |
          source '{{.TASKFILE_DIR}}/install/messages.zsh'
          success "install complete"

bootstrap.zsh kept-Steps shape (lines 53-103 above and 114-124 below the
deleted block at 105-112) -- Step header format is `# --- Step N: <name>`,
inline messages-library calls (`info`, `success`), no per-step
`set -e` re-statement (already set at line 27).

install/compose-brewfile.zsh -- the surviving LINT-05 exemplar referenced by
D-06 rewrites:

    #!/bin/zsh
    # -----------------------------------------------------------------------------
    # install/compose-brewfile.zsh -- compose per-machine Brewfile from manifest
    # Sourced from: taskfiles/packages.yml (packages:compose, packages:install ...)
    # Reads:  $XDG_STATE_HOME/dotfiles/resolved.json   (compiled manifest)
    # Writes: $XDG_CACHE_HOME/dotfiles/Brewfile        (atomic mktemp+mv)
    # -----------------------------------------------------------------------------
    set -euo pipefail
    : "${DOTFILEDIR:?DOTFILEDIR not set -- run via 'task packages:*' or export it manually}"
    : "${DOTFILES_MESSAGES_LOADED:=}"
    if [[ -z "$DOTFILES_MESSAGES_LOADED" ]]; then
      source "${DOTFILEDIR}/install/messages.zsh"
    fi

Verification doc shape (mirror this YAML frontmatter from
10-VERIFICATION.md):

    ---
    status: passed
    phase: 11
    verified: <date>
    must_haves_score: 6/6
    requirement_ids: [RMV-01, RMV-02, RMV-03, RMV-04, RMV-05, RMV-06, RMV-07]
    re_verification:
      previous_status: none
      previous_score: n/a
      gaps_closed: []
      gaps_remaining: []
      regressions: []
    ---

Section list to mirror (from 10-VERIFICATION.md headings, minus the
"Acknowledgment: Prerequisite Revert Commits" section which does NOT apply
to Phase 11):

    # Phase 11: v1 Removal -- Verification Report
    ## Goal Achievement
    ### Observable Truths
    ### Required Artifacts
    ### Key Link Verification
    ### Behavioral Spot-Checks
    ### Steady-State Install Capture          <-- new, D-05 mandatory
    ### Grep Gate Report                       <-- new, D-05 mandatory
    ### Requirements Coverage
    ### Probe Execution
    ### Anti-Patterns Found
    ### Human Verification Required
    ### Deferred Items
    ### Gaps Summary
    _Verified: <date>_
    _Verifier: Claude (gsd-verifier)_
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify Taskfile.yml (drop cutover:ack task, install precondition, v1-leftovers comment block)</name>
  <files>Taskfile.yml</files>
  <read_first>
    - Taskfile.yml (read in full to see exact line ranges; current state has comment block at 22-26, cutover:ack task at 237-311, install precondition at 320-328)
    - .planning/phases/11-v1-removal/11-CONTEXT.md (D-03 single plan, D-04 callers-first ordering, `<canonical_refs>` lists exact line ranges)
    - .planning/phases/11-v1-removal/11-PATTERNS.md (sections "Taskfile.yml:237-311 (the cutover:ack task body)" and "Taskfile.yml:22-26 (the v1 leftover taskfiles comment block)" -- show the kept surrounding shape)
    - taskfiles/helpers.yml (to confirm what helpers exist; the simplified install: still references `task: links:all`, `task: packages:install`, etc. -- nothing in helpers.yml changes)
  </read_first>
  <action>
    Three deletions in a single edit pass to Taskfile.yml (per D-04 step 1 of
    the callers-first ordering; this MUST land before `install/cutover-gate.zsh`
    is deleted in Task 3 so the green-tree invariant holds):

    1. **Delete lines 22-26** (the "v1 leftover taskfiles" comment block).
       The block starts with `# v1 leftover taskfiles (common.yml, ...)` and
       ends with `# parked at taskfiles/macos.v1.yml.bak pending P8 cutover
       deletion.)`. The line above (line 21, the last `#   - macos  (P6, real)`
       entry in the includes table) and the closing `# ==========` banner
       (line 27) both stay. Do NOT introduce a replacement comment -- the
       block simply disappears (per 11-PATTERNS.md "What's different: the
       deleted block has no replacement").

    2. **Delete lines 237-311** (the `cutover:ack` task: ~75 lines of comment
       header from line 236-262 plus the task body from 264-311). The block
       starts with the `# ---------------------------------------------------------------------------`
       banner above `# cutover:ack (CUTV-03) ...` and ends with the closing
       `success "Machine ${CLI_ARGS_ENV} cut over at $ts. Update
       docs/CUTOVER.md table."` line. Drop the entire region. Do NOT preserve
       any of the historical context -- per CONTEXT.md `<specifics>`, that
       lives in Phase 8 planning artifacts.

    3. **Delete lines 320-328** (the `preconditions:` key on the `install:`
       task). Specifically the block:

           preconditions:
             - sh: |
                 export DOTFILEDIR="{{.TASKFILE_DIR}}"
                 source "${DOTFILEDIR}/install/cutover-gate.zsh"
                 cutover_gate_check
               msg: "cutover-ack gate failed -- see error above"

       The line above (`status: [false]` at line 319) and the line below
       (`deps: [manifest:resolve]` at line 329) both stay. The `install:`
       task body must read `install:` -> `desc:` -> `summary:` ->
       `status: [false]` -> `deps: [manifest:resolve]` -> `cmds:` after the
       edit (no `preconditions:` key remains).

    Per D-04, ONE commit covers all three deletions (the file is internally
    consistent at every intermediate state, but a single commit minimizes
    review burden). Suggested commit subject:
    `chore(11): simplify Taskfile.yml, drop cutover gate and v1 leftover comment`

    DO NOT delete `install/cutover-gate.zsh` in this task -- Task 3 owns
    that delete after this commit lands.

    Also: per 11-PATTERNS.md "What's different" note for the includes table,
    the planner may UPDATE the `#   - claude   (stub; P7 wires real bodies)`
    comment at line 20 to `#   - claude   (P7, real)` since Phase 7 has
    shipped (planner discretion). This is a one-line in-place edit; if
    making the edit, fold it into the same commit.

    Do NOT touch any other line of Taskfile.yml in this task; in particular,
    the `default:`, `setup:`, `test:`, `validate:`, and `install:` task
    bodies (apart from the precondition deletion) MUST remain byte-identical.
  </action>
  <verify>
    <automated>
      grep -c 'cutover:ack' Taskfile.yml | grep -q '^0$' &&
      grep -c 'cutover_gate_check' Taskfile.yml | grep -q '^0$' &&
      ! grep -q 'install/cutover-gate.zsh' Taskfile.yml &&
      ! grep -q 'v1 leftover taskfiles' Taskfile.yml &&
      grep -q 'install:' Taskfile.yml &&
      grep -q 'deps: \[manifest:resolve\]' Taskfile.yml &&
      task lint:taskfile; rc=$?; [ "$rc" -le 24 ]
    </automated>
  </verify>
  <done>
    Taskfile.yml has zero `cutover:ack`, zero `cutover_gate_check`, zero
    `install/cutover-gate.zsh`, zero "v1 leftover taskfiles" tokens; the
    `install:` task body keeps `deps: [manifest:resolve]` and has NO
    `preconditions:` key; `task lint:taskfile` exits at-or-below the
    documented baseline (24 failures pre-Phase-11; this commit reduces it
    because two LINT-allow markers attached to the removed `cutover:ack` task
    no longer count). Commit lands at green tree.
  </done>
</task>

<task type="auto">
  <name>Task 2: Simplify bootstrap.zsh (drop Step 4 cutover-ack gate, renumber Step 5 to Step 4)</name>
  <files>bootstrap.zsh</files>
  <read_first>
    - bootstrap.zsh (read full file to confirm Step 4 block is at lines 105-112 and Step 5 next-step hint at 114-124; header banner at lines 3-25 names the script as "Tools-only")
    - .planning/phases/11-v1-removal/11-CONTEXT.md (`<specifics>` confirms exact lines + recommends renumbering Step 5 -> Step 4 for cleanliness)
    - .planning/phases/11-v1-removal/11-PATTERNS.md (section "bootstrap.zsh:105-112 (Step 4 cutover-ack gate)" -- shows surrounding step shape from lines 53-103 above and 114-124 below)
  </read_first>
  <action>
    Two edits in a single commit (per D-04 step 2; this MUST also precede
    Task 3's delete of `install/cutover-gate.zsh`):

    1. **Delete lines 105-112** (the entire `# --- Step 4: cutover-ack gate
       (D-07 / D-09)` block). The block runs from the header comment line
       (`# --- Step 4: cutover-ack gate (D-07 / D-09)`) through the comment
       block (lines 106-110) and the two executable lines `source
       "${DOTFILEDIR}/install/cutover-gate.zsh"` + `cutover_gate_check ||
       exit 1`. Line 104 (blank line after Step 3) and line 113 (blank line
       before Step 5) collapse to a single blank line.

    2. **Renumber Step 5 to Step 4** in the next-step hint block (line 114
       currently reads `# --- Step 5: next-step hint (D-03)`; rewrite to
       `# --- Step 4: next-step hint (D-03)`). The body of the block (lines
       115-124) stays byte-identical. Per CONTEXT.md `<specifics>` and
       `<decisions>` Claude's Discretion, this renumber is recommended for
       cleanliness; no other comment in bootstrap.zsh references "Step 5"
       (verify with `grep -n 'Step 5' bootstrap.zsh` before AND after the
       edit -- before returns line 114, after returns zero).

    Per 11-PATTERNS.md: post-edit the script reads as pure tools-acquisition
    (Step 1 brew, Step 2 go-task, Step 3 yq, Step 4 next-step hint), which
    matches the file-header banner at lines 3-25 ("Tools-only: does NOT take
    a machine name; does NOT invoke task setup").

    Do NOT delete `install/cutover-gate.zsh` in this task -- Task 3 owns
    that delete.

    Do NOT touch the header banner (lines 1-25), the `set -euo pipefail` at
    line 27, the Step 1/2/3 bodies (lines 53-103), or the next-step hint
    body (lines 115-124).

    Suggested commit subject:
    `chore(11): drop cutover-ack gate from bootstrap.zsh`
  </action>
  <verify>
    <automated>
      ! grep -q 'Step 4: cutover-ack gate' bootstrap.zsh &&
      ! grep -q 'install/cutover-gate.zsh' bootstrap.zsh &&
      ! grep -q 'cutover_gate_check' bootstrap.zsh &&
      ! grep -q 'Step 5' bootstrap.zsh &&
      grep -q 'Step 4: next-step hint' bootstrap.zsh &&
      grep -q 'Bootstrap complete. Next steps' bootstrap.zsh &&
      zsh -n bootstrap.zsh
    </automated>
  </verify>
  <done>
    bootstrap.zsh has zero `cutover-ack gate`, zero `install/cutover-gate.zsh`,
    zero `cutover_gate_check`, and zero `Step 5` tokens; the next-step hint
    block is renumbered to `Step 4` and its body is byte-identical to the
    pre-edit state; `zsh -n bootstrap.zsh` exits 0 (syntax valid). Commit
    lands at green tree.
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete install/cutover-gate.zsh (callers already simplified by Tasks 1 + 2)</name>
  <files>install/cutover-gate.zsh</files>
  <read_first>
    - install/cutover-gate.zsh (read to confirm it is ~90 lines, single library script with `cutover_gate_check` + `_cutover_gate_emit_error` functions)
    - .planning/phases/11-v1-removal/11-CONTEXT.md (D-04 ordering: this delete is step 3, MUST run after Taskfile.yml + bootstrap.zsh callers are simplified)
    - .planning/phases/11-v1-removal/11-PATTERNS.md ("Deletion-target pattern" section: callers simplified first, then `git rm`)
    - Taskfile.yml (re-read to CONFIRM Task 1 landed and Taskfile.yml no longer references this file -- a green-tree-per-commit guard)
    - bootstrap.zsh (re-read to CONFIRM Task 2 landed and bootstrap.zsh no longer references this file)
  </read_first>
  <action>
    Pre-flight CHECK (do this before `git rm` to catch a missed caller):
    `git grep -nF 'install/cutover-gate.zsh' -- ':!.planning/' ':!.claude/'`.
    The output MUST be empty OR contain only the 6 LINT-05 pattern citations
    in `os/defaults/{dock,finder,input,screenshots,security}.zsh` and
    `os/shell-registration.zsh` (those are header-comment citations only,
    not live sources). If any OTHER caller is found (a live `source` or
    `task:` reference), STOP and revisit Tasks 1-2.

    Then: `git rm install/cutover-gate.zsh`.

    Suggested commit subject:
    `chore(11): remove cutover-gate library`

    Per 11-PATTERNS.md "Deletion-target pattern" and 11-CONTEXT.md
    `<code_context>` "Reusable Assets": after this commit, `install/` retains
    only `messages.zsh`, `resolver.zsh`, `compose-brewfile.zsh`,
    `test-hooks.zsh`, the 4 v1 Brewfiles (deleted in Task 6), and
    `README.md`. No active library has been broken.
  </action>
  <verify>
    <automated>
      test ! -f install/cutover-gate.zsh &&
      ! grep -rn 'source.*install/cutover-gate.zsh' Taskfile.yml bootstrap.zsh taskfiles/ install/ os/ shell/ identity/ packages/ configs/ claude/ 2>/dev/null &&
      task --list >/dev/null &&
      task lint:taskfile; rc=$?; [ "$rc" -le 24 ]
    </automated>
  </verify>
  <done>
    `install/cutover-gate.zsh` no longer exists; no live `source` or `task:`
    reference to the file remains in any operational v2 surface (LINT-05
    header-comment citations in `os/defaults/*.zsh` are tolerated until
    Task 10 rewrites them); `task --list` parses and emits the task table;
    `task lint:taskfile` exits at-or-below baseline. Commit lands at green
    tree.
  </done>
</task>

<task type="auto">
  <name>Task 4: Delete the 8 v1 leftover taskfiles (RMV-01)</name>
  <files>taskfiles/common.yml, taskfiles/profile.yml, taskfiles/brew.yml, taskfiles/profile-tasks.yml, taskfiles/claude-stub.yml, taskfiles/brew-stub.yml, taskfiles/links-stub.yml, taskfiles/macos.v1.yml.bak</files>
  <read_first>
    - Taskfile.yml (re-read the `includes:` block at lines 80-123 to CONFIRM none of these 8 files are included by root; per 11-CONTEXT.md `<decisions>` D-04 step 4 these are "independent of each other; planner can reorder or batch")
    - .planning/phases/11-v1-removal/11-CONTEXT.md (`<v2 deletion targets>`: exact 8-file list; RMV-01)
    - .planning/phases/11-v1-removal/11-PATTERNS.md ("Deletion-target pattern" section + the per-file rows in the "Deletion targets (`git rm`)" table)
    - .planning/phases/09-v1-drop-audit/AUDIT.md (the canonical keep/drop classification; every one of these 8 files has rows in the Taskfiles section all classified `drop`)
  </read_first>
  <action>
    Pre-flight CHECK: `grep -nF -e 'taskfiles/common.yml' -e 'taskfiles/profile.yml' -e 'taskfiles/brew.yml' -e 'taskfiles/profile-tasks.yml' -e 'taskfiles/claude-stub.yml' -e 'taskfiles/brew-stub.yml' -e 'taskfiles/links-stub.yml' -e 'taskfiles/macos.v1.yml.bak' Taskfile.yml`.
    The output MUST be empty (none of these files are included by
    Taskfile.yml -- the v1 leftover comment block at lines 22-26 was
    deleted in Task 1).

    Then: single `git rm` covering all 8 files:

        git rm taskfiles/common.yml taskfiles/profile.yml \
               taskfiles/brew.yml taskfiles/profile-tasks.yml \
               taskfiles/claude-stub.yml taskfiles/brew-stub.yml \
               taskfiles/links-stub.yml taskfiles/macos.v1.yml.bak

    Single commit per CONTEXT.md `<decisions>` Claude's Discretion
    ("Brewfile + zsh/ + taskfile deletes can be split or batched at planner
    discretion" -- one-commit-per-RMV is the recommended boundary for
    Phase 11).

    Suggested commit subject:
    `chore(11): drop 8 v1 leftover taskfiles`

    NOTE on lint baseline shift: per 11-PATTERNS.md "Green-tree-per-commit
    gate", the pre-Phase-11 `task lint:taskfile` failure count is 24 (4
    LINT-02 in v1 leftover taskfiles + 5 LINT-03a in v1 leftovers + others).
    Deleting these 8 files REMOVES the LINT-02 + LINT-03a failures attached
    to them. The new lint failure count will be lower (the deliberate
    negative fixture + the long-standing `shell:shell` lint-allow placement
    issue + whatever remains in v2 taskfiles like `manifest.yml` /
    `packages.yml` / `claude.yml` -- those are out of scope for Phase 11).
    `task lint:taskfile` continues to exit non-zero (the negative fixture
    is intentional); the green-tree gate is "at-or-below pre-commit
    baseline", NOT "exits 0".
  </action>
  <verify>
    <automated>
      test ! -f taskfiles/common.yml &&
      test ! -f taskfiles/profile.yml &&
      test ! -f taskfiles/brew.yml &&
      test ! -f taskfiles/profile-tasks.yml &&
      test ! -f taskfiles/claude-stub.yml &&
      test ! -f taskfiles/brew-stub.yml &&
      test ! -f taskfiles/links-stub.yml &&
      test ! -f taskfiles/macos.v1.yml.bak &&
      task --list >/dev/null
    </automated>
  </verify>
  <done>
    All 8 v1 leftover taskfiles are deleted; `ls taskfiles/` shows only v2
    files plus `helpers.yml`, `identity.yml`, `links.yml`, `lint.yml`,
    `macos.yml`, `manifest.yml`, `packages.yml`, `shell.yml`, `test.yml`,
    `test/`, `README.md`, `claude.yml`; `task --list` parses and emits the
    task table without errors. RMV-01 satisfied. Commit lands at green tree.
  </done>
</task>

<task type="auto">
  <name>Task 5: Delete the v1 zsh/ tree (RMV-02)</name>
  <files>zsh/</files>
  <read_first>
    - .planning/phases/11-v1-removal/11-CONTEXT.md (`<v2 deletion targets>`: entire zsh/ tree, 38 files per Phase 9 audit; RMV-02)
    - .planning/phases/10-v1-drop-remediation/10-VERIFICATION.md (PORT-03 PASS confirms `shell/` is the only shell-content tree the v2 install needs; the `zsh/` tree has zero live callers in v2)
    - .planning/phases/10-v1-drop-remediation/10-SMOKE.md (the smoke procedure that proved first-shell readiness from `shell/` alone)
    - .planning/phases/09-v1-drop-audit/AUDIT.md (the `zsh/ Tree` section: every comparison row classified `drop` or `ported`)
  </read_first>
  <action>
    Pre-flight CHECK: `grep -rnE 'zsh/(aliases|functions|configs|styles|theme|\.zshrc|\.zshenv|\.zprofile|\.zlogin|\.zlogout)' Taskfile.yml taskfiles/ install/ shell/ identity/ packages/ configs/ claude/ os/ bootstrap.zsh 2>/dev/null`.
    The output MUST be empty (no live v2 caller references the `zsh/`
    subtree). Per 10-VERIFICATION.md Observable Truth #6 PASS,
    `shell/functions/motd.zsh` is byte-identical to v1 `zsh/functions/motd.zsh`
    but loads from `shell/` -- so the `motd.zsh` reference to
    `$DOTFILEDIR/zsh/configs/motd_sysinfo.jsonc` and
    `$DOTFILEDIR/zsh/configs/motd_tron.txt` is a LIVE caller that will
    break after this delete. **Mitigation per 10-VERIFICATION.md Deferred
    Item #2 (WR-02):** before `git rm zsh/`, migrate those two asset files:

        mkdir -p configs/motd
        git mv zsh/configs/motd_sysinfo.jsonc configs/motd/motd_sysinfo.jsonc
        git mv zsh/configs/motd_tron.txt configs/motd/motd_tron.txt

    Then rewrite the two path references in `shell/functions/motd.zsh` from
    `$DOTFILEDIR/zsh/configs/` to `$DOTFILEDIR/configs/motd/`. Verify with
    `grep -n 'configs/motd' shell/functions/motd.zsh` -- expect 2 hits, both
    pointing at `configs/motd/`.

    Then: `git rm -r zsh/`.

    Two commits is the cleanest split (one for the motd-asset migration +
    motd.zsh path rewrite, one for the `git rm -r zsh/`) so each commit
    leaves a green tree:

        Commit A: `fix(motd): migrate motd assets from zsh/configs/ to configs/motd/`
        Commit B: `chore(11): delete v1 zsh/ tree`

    Alternative single-commit form (smaller surface but more in flight per
    commit) is acceptable; planner-discretion per CONTEXT.md.

    Do NOT touch `shell/functions/motd.zsh` for any reason other than the
    two path rewrites above. The other motd content (the emoji glyph at
    line ?, the system-fetch fastfetch calls) is OUT OF SCOPE per
    10-VERIFICATION.md Deferred Items WR-01.
  </action>
  <verify>
    <automated>
      test ! -d zsh/ &&
      test -f configs/motd/motd_sysinfo.jsonc &&
      test -f configs/motd/motd_tron.txt &&
      ! grep -n 'zsh/configs/' shell/functions/motd.zsh &&
      grep -cn 'configs/motd/' shell/functions/motd.zsh | grep -qE '^[2-9]' &&
      zsh -n shell/functions/motd.zsh &&
      task --list >/dev/null
    </automated>
  </verify>
  <done>
    `zsh/` directory no longer exists; `configs/motd/motd_sysinfo.jsonc` and
    `configs/motd/motd_tron.txt` exist (migrated from `zsh/configs/`);
    `shell/functions/motd.zsh` no longer references `zsh/configs/` and
    instead references `configs/motd/` (at least 2 hits); `zsh -n
    shell/functions/motd.zsh` exits 0; `task --list` succeeds. RMV-02
    satisfied. Both commits land at green tree.
  </done>
</task>

<task type="auto">
  <name>Task 6: Delete the 4 v1 Brewfiles (RMV-03)</name>
  <files>install/Brewfile.rb, install/Brewfile-personal.rb, install/Brewfile-server.rb, install/Brewfile-work.rb</files>
  <read_first>
    - .planning/phases/11-v1-removal/11-CONTEXT.md (`<v2 deletion targets>`: 4 v1 Brewfiles; RMV-03)
    - install/README.md (lines 29-34 describe the 4 Brewfiles as "v1 transitional state"; this delete makes that wording obsolete -- noted but NOT in scope here per CONTEXT.md `<deferred>` "install/README.md Brewfile-bullets trimming ... deferred to Phase 14 TRIM-03")
    - .planning/phases/09-v1-drop-audit/AUDIT.md (the `Install Assets` section; all 4 Brewfile rows classified `drop`)
    - packages/core.rb (the surviving v2 Brewfile bundle to confirm the composed-Brewfile pipeline owns this surface; sanity check that `task packages:install` does not source from `install/Brewfile*`)
  </read_first>
  <action>
    Pre-flight CHECK: `grep -rnE 'install/Brewfile(\.rb|-personal|-server|-work)' Taskfile.yml taskfiles/ install/compose-brewfile.zsh install/messages.zsh install/resolver.zsh install/test-hooks.zsh bootstrap.zsh 2>/dev/null`.
    The output MUST be empty -- the v2 composed-Brewfile pipeline
    (`install/compose-brewfile.zsh` + `taskfiles/packages.yml`) reads from
    `packages/<purpose>.rb`, never from `install/Brewfile*`.

    Then: single `git rm` covering all 4 Brewfiles:

        git rm install/Brewfile.rb install/Brewfile-personal.rb \
               install/Brewfile-server.rb install/Brewfile-work.rb

    Single commit.

    Suggested commit subject:
    `chore(11): remove v1 install Brewfiles`

    The stale wording in `install/README.md:29-34` ("v1 transitional state.
    Phase 5 (PKGS-01..05) refactors these into per-purpose bundles") is
    OUT OF SCOPE here -- per CONTEXT.md `<deferred>` it is deferred to
    Phase 14 TRIM-03. Do NOT edit `install/README.md` in this task.

    Verify the composed-Brewfile pipeline still works end-to-end:
    `task packages:compose` (writes to `$XDG_CACHE_HOME/dotfiles/Brewfile`
    from `packages/<purpose>.rb`; no read from `install/Brewfile*` anywhere
    in the call chain).
  </action>
  <verify>
    <automated>
      test ! -f install/Brewfile.rb &&
      test ! -f install/Brewfile-personal.rb &&
      test ! -f install/Brewfile-server.rb &&
      test ! -f install/Brewfile-work.rb &&
      ! grep -rE 'install/Brewfile(\.rb|-personal|-server|-work)' Taskfile.yml taskfiles/ install/compose-brewfile.zsh install/messages.zsh install/resolver.zsh install/test-hooks.zsh bootstrap.zsh 2>/dev/null &&
      task --list >/dev/null
    </automated>
  </verify>
  <done>
    All 4 v1 Brewfiles are deleted; `find install -name 'Brewfile*' -print`
    returns empty; no live v2 caller references the deleted files;
    `task --list` succeeds. RMV-03 satisfied. Commit lands at green tree.
  </done>
</task>

<task type="auto">
  <name>Task 7: Delete docs/CUTOVER.md and docs/MIGRATION.md (RMV-04 + D-01)</name>
  <files>docs/CUTOVER.md, docs/MIGRATION.md</files>
  <read_first>
    - .planning/phases/11-v1-removal/11-CONTEXT.md (`<decisions>` D-01: MIGRATION.md deleted outright, not trimmed -- this closes Phase 9 AUDIT-05 deferred decision and bounds Phase 14 TRIM-03)
    - .planning/phases/11-v1-removal/11-CONTEXT.md (`<decisions>` D-02: AUDIT.md is the canonical v1-to-v2 record; no replacement doc is created)
    - docs/CUTOVER.md (read briefly to confirm contents are cutover-runbook only; the only operator-facing pointer is README.md:64 which Task 8 will drop)
    - docs/MIGRATION.md (read briefly to confirm contents are cutover-window + v1-to-v2 mapping only; PROJECT.md retains the milestone narrative per D-01)
  </read_first>
  <action>
    Pre-flight CHECK: `git grep -nF -e 'docs/CUTOVER.md' -e 'docs/MIGRATION.md' -- ':!.planning/' ':!.claude/'`.
    Expected hits as of this task: README.md:9 (mentions docs/MIGRATION.md
    in the intro paragraph), README.md:64-65 (Documentation bullets for
    both files), docs/CUTOVER.md self-references, docs/MIGRATION.md
    self-references. README.md edits land in Task 8; both file deletes are
    safe here because no v2 task or script sources from either file.

    Then: single `git rm`:

        git rm docs/CUTOVER.md docs/MIGRATION.md

    Single commit per CONTEXT.md `<decisions>` Claude's Discretion
    boundary (one-commit-per-RMV is the recommended boundary).

    Suggested commit subject:
    `docs(11): retire cutover and migration docs`

    Note: After this commit, README.md references to both deleted files
    become broken links -- Task 8 fixes those. The intermediate state is
    a doc-link breakage that is invisible to `task install` / `task --list`
    / `task lint:taskfile`; the green-tree-per-commit invariant (executable
    surface stays invokable) is preserved.

    Do NOT edit any other file in this task. PROJECT.md, ROADMAP.md, and
    REQUIREMENTS.md retain their cutover-history narratives (those live in
    `.planning/` -- out of scope for the SC#5 grep gate).
  </action>
  <verify>
    <automated>
      test ! -f docs/CUTOVER.md &&
      test ! -f docs/MIGRATION.md &&
      ls docs/ | grep -qE '^(SECURITY|MANIFEST|MACHINES)\.md$' &&
      task --list >/dev/null
    </automated>
  </verify>
  <done>
    `docs/CUTOVER.md` and `docs/MIGRATION.md` no longer exist; `ls docs/`
    shows only the surviving doc files (`SECURITY.md`, `MANIFEST.md`,
    `MACHINES.md`, and any others present pre-Phase-11 like
    `INSTALLED-PACKAGES.md` if present); `task --list` succeeds. RMV-04
    doc-half satisfied (the executable half landed in Tasks 1-3). Commit
    lands at green tree.
  </done>
</task>

<task type="auto">
  <name>Task 8: Rewrite README.md fresh-install section (drop task cutover:ack + CUTOVER.md doc bullet, drop MIGRATION.md intro mention)</name>
  <files>README.md</files>
  <read_first>
    - README.md (read in full; current state has cutover-ack prose at lines 24-39, MIGRATION.md mention at line 9, CUTOVER.md and MIGRATION.md doc bullets at lines 64-65)
    - .planning/phases/11-v1-removal/11-CONTEXT.md (`<decisions>` D-09: drop the cutover line + adjust prose; minimal-edit; broader Quick Reference rewrite is deferred to Phase 14 TRIM-04)
    - .planning/phases/11-v1-removal/11-PATTERNS.md (section "README.md:26-37 (fresh-install procedure with `task cutover:ack`)": shows prose register from lines 1-22 and 47-58 that the rewrite must match)
  </read_first>
  <action>
    Three in-place edits to README.md (single commit per D-04 + D-09):

    1. **Drop the MIGRATION.md mention in the intro paragraph (line 9).**
       Current text: `\`docs/MIGRATION.md\` documents what the v2 layout
       replaced for anyone arriving from the prior layout.` This sentence
       is a remnant of the cutover-window narrative. Rewrite OR delete:
       the cleanest edit is to drop the entire sentence (the surrounding
       paragraph already explains the v2 manifest model without needing
       the MIGRATION.md cross-reference). Surrounding text in lines 4-22
       stays byte-identical except for the deleted sentence.

    2. **Rewrite the `## Fresh Machine Setup` section (lines 24-45) to
       drop `task cutover:ack`.** Current state has 14 lines of cutover-
       framed prose plus a 5-line fenced shell block. Replace with a
       short equivalent that names `task setup` then `task install` with
       no acknowledgment step. Suggested replacement (planner may revise
       wording within D-09's "minimal-edit" intent):

           ## Fresh Machine Setup

           Run these commands in order on a clean Mac. `bootstrap.zsh`
           acquires Homebrew, go-task, and yq and then prints the
           next-step hint. `task setup` writes the active machine name
           to `$XDG_STATE_HOME/dotfiles/machine`; `task install` runs
           the full install pipeline (links + packages + claude + macos
           + verify + reconcile).

           ```zsh
           git clone <repo-url>
           ./bootstrap.zsh
           task setup -- <machine-name>
           task install
           ```

           For the list of accepted `<machine-name>` values and the
           per-machine purpose, hardware, and special-handling notes,
           see `docs/MACHINES.md`.

       The reference to `docs/CUTOVER.md`'s "Fresh-machine verification"
       section (currently README.md:41-43) is dropped entirely (that doc
       was deleted in Task 7). Per D-09 "no broader Quick Reference
       rewrite", do NOT restructure other README.md sections; only the
       Fresh Machine Setup section and the linked-doc bullets are in
       scope.

    3. **Drop two Documentation bullets (lines 64-65).** Current state:
       `- \`docs/CUTOVER.md\` -- per-machine cutover procedure and state
       table` and `- \`docs/MIGRATION.md\` -- v1-to-v2 mapping per concept
       and rollback procedure`. Both deleted files. Drop both bullets;
       the surrounding bullets (MANIFEST.md, SECURITY.md, MACHINES.md,
       .claude/CLAUDE.md) all stay verbatim.

    Single commit. Suggested commit subject:
    `docs(11): rewrite fresh-install README, drop cutover-ack`

    Prose voice to match per 11-PATTERNS.md: declarative, ~70-char wrap,
    file/path backticks, closing "see X" cross-references.
  </action>
  <verify>
    <automated>
      ! grep -q 'task cutover:ack' README.md &&
      ! grep -q 'docs/CUTOVER.md' README.md &&
      ! grep -q 'docs/MIGRATION.md' README.md &&
      ! grep -q 'cutover_gate_check' README.md &&
      grep -q 'task setup -- <machine-name>' README.md &&
      grep -q 'task install' README.md &&
      grep -q 'docs/MACHINES.md' README.md
    </automated>
  </verify>
  <done>
    README.md has zero `task cutover:ack`, zero `docs/CUTOVER.md`, zero
    `docs/MIGRATION.md`, zero `cutover_gate_check` tokens; the Fresh
    Machine Setup section names only `bootstrap.zsh`, `task setup`, and
    `task install`; the Documentation section's surviving bullets still
    cross-reference MANIFEST.md, SECURITY.md, MACHINES.md, and
    .claude/CLAUDE.md. RMV-06 partial satisfied (README half). Commit
    lands at green tree.
  </done>
</task>

<task type="auto">
  <name>Task 9: Doc-drift rewrite pass (docs/SECURITY.md, taskfiles/lint.yml, taskfiles/README.md per D-08)</name>
  <files>docs/SECURITY.md, taskfiles/lint.yml, taskfiles/README.md</files>
  <read_first>
    - docs/SECURITY.md (read lines 1-30 for the surrounding paragraph; the cutover-ack mention is in the last sentence of the lines-11-18 scope paragraph)
    - taskfiles/lint.yml (read lines 1-35 for the file-header banner; the cutover-ack citation is at line 24 inside the read-only banner block)
    - taskfiles/README.md (read in full; the Phase 3 bullet at lines 25-29 has the v1-leftover sentence; the Phase 4-7 stubs bullet at lines 30-34 lists v1 stub files now deleted; the intro at line 4-5 may also list v1 names)
    - .planning/phases/11-v1-removal/11-CONTEXT.md (`<decisions>` D-08: three files, "rewrite each to current reality", same green-tree discipline; can land as a single doc-drift commit)
    - .planning/phases/11-v1-removal/11-PATTERNS.md (sections "docs/SECURITY.md:16", "taskfiles/lint.yml:24", "taskfiles/README.md:25-29" -- each shows the analog surrounding text and the minimal-vs-rewrite intent)
  </read_first>
  <action>
    Three in-place edits, one combined commit (per D-08 "can land as a
    single doc-drift commit late in the sequence"):

    1. **docs/SECURITY.md:16 -- rewrite the cutover-ack sentence.** The
       current text (lines 16-18): `The cutover-ack gate (\`task
       cutover:ack\`) referenced by the gate library is owned by Phase 8
       CUTV-03 and is described in \`docs/CUTOVER.md\` (also Phase 8).`
       Drop this sentence entirely. Per CONTEXT.md D-08, the security
       boundary is now `bootstrap.zsh`'s HTTPS-only trust chain + the
       manifest model itself. Per 11-PATTERNS.md "Optional planner
       choice", append a one-sentence replacement that names the new
       boundary -- suggested:

           The repository's per-machine security boundary is the
           manifest model itself: every install action keys off the
           machine name written to `$XDG_STATE_HOME/dotfiles/machine`
           by `task setup`, so an install never proceeds on the basis
           of hostname inference or environment-variable sniffing.

       The lines 11-15 of the paragraph (the scope statement naming
       Homebrew + go-task + yq + the audit signals, plus the deferred-
       subject Phase-N attributions) stay byte-identical. Only the
       trailing cutover-ack sentence is replaced.

    2. **taskfiles/lint.yml:24 -- drop the cutover-ack clause from the
       banner.** Current text (lines 23-24): `# Read-only: no manifest
       dependency, no resolved.json dependency, no` / `# cutover-ack
       gate (per D-09). Runs without task setup -- <machine>.` Rewrite
       to one line:

           # Read-only: no manifest dependency, no resolved.json dependency.
           # Runs without task setup -- <machine>.

       Or (cleaner; planner discretion): collapse to a single line
       since the cutover-ack mention was the third item in the
       enumeration:

           # Read-only: no manifest dependency, no resolved.json dependency.
           # Runs without task setup -- <machine>.

       The rest of the banner (lines 1-22 above and 25-33 below) stays
       byte-identical.

    3. **taskfiles/README.md -- rewrite the Phase 3 bullet and drop the
       Phase 4-7 stubs bullet entirely.** Per 11-PATTERNS.md, the
       current Phase 3 bullet at lines 25-29 lists `common.yml`,
       `profile.yml`, `profile-tasks.yml` -- all deleted in Task 4.
       Rewrite to name only the surviving v2 files:

           - **Phase 3 (real).** `links.yml` -- shell + antidote symlinks
             via `_:safe-link` plus the zdotdir step. `shell.yml` --
             `task perf:shell` (SHEL-12 cold-start gate) plus
             `task shell:validate` (PORT-02 first-shell guarantee).

       Then DROP the entire Phase 4-7 stubs bullet at lines 30-34
       (those files are deleted in Task 4). Also revise the opening
       sentence at line 4-5 (currently: `One taskfile per concern:
       manifest, lint, links, shell, brew, macos, claude, common,
       profile, helpers.`) to drop `brew, common, profile` (deleted)
       and add `packages, identity, test` (real v2 taskfiles):

           One taskfile per concern: manifest, lint, links, shell,
           identity, packages, macos, claude, test, helpers.

       Also revise the second sentence (currently: `The \`*-stub.yml\`
       files are intentional Phase 2 placeholders that lint-fixture
       tests reference; each matching real \`<concern>.yml\` lands as
       the owning phase ships its content.`) to drop entirely -- the
       stubs are gone.

    Single commit. Suggested commit subject:
    `docs(11): scrub cutover phrasing from operator docs`

    Per CONTEXT.md `<deferred>`: `install/README.md` cutover-gate mention
    at line 5 + Brewfile-bullets at lines 29-34 are OUT OF SCOPE here
    (deferred to Phase 14 TRIM-03). Do NOT edit `install/README.md`.
  </action>
  <verify>
    <automated>
      ! grep -q 'cutover-ack gate' docs/SECURITY.md &&
      ! grep -q 'task cutover:ack' docs/SECURITY.md &&
      ! grep -q 'docs/CUTOVER.md' docs/SECURITY.md &&
      ! grep -q 'cutover-ack' taskfiles/lint.yml &&
      ! grep -q 'cutover' taskfiles/README.md &&
      ! grep -q 'profile.yml\|common.yml\|profile-tasks.yml\|brew.yml\|.*-stub.yml' taskfiles/README.md &&
      grep -q 'manifest model' docs/SECURITY.md &&
      grep -q 'links.yml' taskfiles/README.md
    </automated>
  </verify>
  <done>
    docs/SECURITY.md has zero `cutover-ack gate`, zero `task cutover:ack`,
    zero `docs/CUTOVER.md` tokens and a new manifest-model security-boundary
    sentence is present; taskfiles/lint.yml has zero `cutover-ack` tokens;
    taskfiles/README.md has zero `cutover` tokens, zero references to any
    deleted v1 taskfile, and the Phase 3 bullet now lists only `links.yml`
    and `shell.yml`; intro sentence lists v2-only taskfile concerns. RMV-06
    partial satisfied (these three docs). Commit lands at green tree.
  </done>
</task>

<task type="auto">
  <name>Task 10: LINT-05 pattern-citation rewrite pass (6 os/*.zsh files per D-06)</name>
  <files>os/defaults/dock.zsh, os/defaults/finder.zsh, os/defaults/input.zsh, os/defaults/screenshots.zsh, os/defaults/security.zsh, os/shell-registration.zsh</files>
  <read_first>
    - os/defaults/security.zsh (read lines 45-60 for the canonical citation paragraph; the other 5 files carry identical citations at the line numbers in CONTEXT.md `<canonical_refs>`)
    - install/compose-brewfile.zsh (read in full -- this is the surviving exemplar; verify the file-header banner + `set -euo pipefail` + `:?` parameter assertion on DOTFILEDIR + double-source guard fingerprint per 11-PATTERNS.md "Structural fingerprint of install/compose-brewfile.zsh")
    - .planning/phases/11-v1-removal/11-CONTEXT.md (`<decisions>` D-06: minimal rewrite is to drop the `+ install/cutover-gate.zsh` clause, leaving `install/compose-brewfile.zsh` as the live exemplar)
    - .planning/phases/11-v1-removal/11-PATTERNS.md (section "LINT-05 pattern-citation edits (6 files)": shows the exact before/after of the comment block and confirms install/compose-brewfile.zsh's fingerprint match)
  </read_first>
  <action>
    Same minimal edit to 6 files, single commit (per D-06 + D-08 batching
    discipline). The current citation paragraph (identical across all 6
    files, at the line numbers given in CONTEXT.md `<canonical_refs>`):

        # messages.zsh references a bare $DOTFILES_MESSAGES_LOADED in its double-source
        # guard; under set -u that would abort. Pre-initialize the guard variable and
        # the caller-supplied DOTFILEDIR var so this script is safe to source from a
        # `set -euo pipefail` taskfile heredoc (matches install/resolver.zsh +
        # install/compose-brewfile.zsh + install/cutover-gate.zsh pattern).

    The D-06 rewrite drops the ` + install/cutover-gate.zsh` clause (single
    in-line edit per file, no other context shift):

        # messages.zsh references a bare $DOTFILES_MESSAGES_LOADED in its double-source
        # guard; under set -u that would abort. Pre-initialize the guard variable and
        # the caller-supplied DOTFILEDIR var so this script is safe to source from a
        # `set -euo pipefail` taskfile heredoc (matches install/resolver.zsh +
        # install/compose-brewfile.zsh pattern).

    Per CONTEXT.md `<canonical_refs>` exact line targets:
    - os/defaults/dock.zsh:34
    - os/defaults/finder.zsh:40
    - os/defaults/input.zsh:39
    - os/defaults/screenshots.zsh:43
    - os/defaults/security.zsh:54
    - os/shell-registration.zsh:54

    For each file: delete the ` + install/cutover-gate.zsh` substring from
    the cited line. The rest of the file (header banner, `set -euo pipefail`,
    the `:?` parameter assertion, the messages.zsh source guard, the actual
    `defaults write` calls, etc.) stays byte-identical.

    Single commit. Suggested commit subject:
    `docs(11): drop cutover-gate from LINT-05 pattern citations`

    Verify each file with `zsh -n <file>` post-edit (the edit is in a
    comment so syntax is unaffected, but the check is cheap insurance).

    NOTE on "v1" residue: per CONTEXT.md `<specifics>`, the file headers
    still mention "the v1 bug class" in pedagogical context (e.g.
    `os/shell-registration.zsh:46` "prevents the v1 bug class from
    re-emerging"). Per Claude's Discretion, these are kept -- the "v1"
    reference is teaching the structural fix's purpose, not asserting a
    live v1 dependency. The SC#5 grep gate report in Task 12 will list
    these as deliberately-kept hits with rationale.
  </action>
  <verify>
    <automated>
      ! grep -q 'install/cutover-gate.zsh' os/defaults/dock.zsh &&
      ! grep -q 'install/cutover-gate.zsh' os/defaults/finder.zsh &&
      ! grep -q 'install/cutover-gate.zsh' os/defaults/input.zsh &&
      ! grep -q 'install/cutover-gate.zsh' os/defaults/screenshots.zsh &&
      ! grep -q 'install/cutover-gate.zsh' os/defaults/security.zsh &&
      ! grep -q 'install/cutover-gate.zsh' os/shell-registration.zsh &&
      grep -q 'install/compose-brewfile.zsh' os/defaults/security.zsh &&
      grep -q 'install/compose-brewfile.zsh' os/shell-registration.zsh &&
      zsh -n os/defaults/dock.zsh &&
      zsh -n os/defaults/finder.zsh &&
      zsh -n os/defaults/input.zsh &&
      zsh -n os/defaults/screenshots.zsh &&
      zsh -n os/defaults/security.zsh &&
      zsh -n os/shell-registration.zsh
    </automated>
  </verify>
  <done>
    All 6 files have zero `install/cutover-gate.zsh` tokens; the kept
    LINT-05 citation paragraph in each file references
    `install/compose-brewfile.zsh` (and `install/resolver.zsh`) as the
    living exemplar; `zsh -n` exits 0 on every file. RMV-06 partial
    satisfied (these 6 os/*.zsh files). Commit lands at green tree.
  </done>
</task>

<task type="auto">
  <name>Task 11: Verify D-07 SSH-key rewrites are empty-scope (no-op)</name>
  <files>(none -- verification-only)</files>
  <read_first>
    - .planning/phases/11-v1-removal/11-PATTERNS.md (section "CONTEXT.md miscalls (planner-visible)" + "No Analog Found / Skip Items": pattern-mapper finding that the 4 D-07 SSH files do NOT exist on disk)
    - .planning/phases/11-v1-removal/11-CONTEXT.md (`<decisions>` D-07: lists 4 server-{1,2} identity + key files for rewrite -- planner-mapper confirms these do not exist)
  </read_first>
  <action>
    Pre-flight verification of the pattern-mapper's finding. Run:

        ls identity/ssh/identities/ identity/ssh/keys/
        grep -rn 'cutover' identity/ 2>/dev/null

    Expected output:
    - `identity/ssh/identities/` contains exactly: `atium`, `none`,
      `personal`, `work` (per the live filesystem state captured at planning
      time; NO `server-1` or `server-2` files).
    - `identity/ssh/keys/` contains exactly: `atium.pub`, `personal.pub`
      (and the `.gitignore` per pattern-mapper's observation; NO
      `server-1.pub` or `server-2.pub` files).
    - The `grep` returns zero hits (no "cutover" residue anywhere in
      identity/).

    If the output matches the expected state: D-07 scope is EMPTY for
    Phase 11. NO files to rewrite. NO commit produced by this task. Record
    the finding in Task 12's `11-VERIFICATION.md` write under the SC#5
    grep gate report as: "D-07 SSH-rewrite scope verified empty; no
    `identity/ssh/identities/server-{1,2}` or
    `identity/ssh/keys/server-{1,2}.pub` files exist; `grep -rn cutover
    identity/` returns zero. SC#5 grep gate independently confirms no
    `identity/**` file matches the cutover regex."

    If the output DIFFERS from expected (e.g. the server files DO exist
    and contain "cutover"): STOP and execute the D-07 rewrite as
    originally planned in CONTEXT.md -- rewrite each file's
    "generated at cutover time" wording to "generated locally at first
    install" (or equivalent per CONTEXT.md D-07 Claude's Discretion).
    Commit subject in that case: `docs(11): rewrite SSH key cutover
    references`.

    Either way, do NOT produce a commit if there are no files to edit.
  </action>
  <verify>
    <automated>
      test ! -e identity/ssh/identities/server-1 &&
      test ! -e identity/ssh/identities/server-2 &&
      test ! -e identity/ssh/keys/server-1.pub &&
      test ! -e identity/ssh/keys/server-2.pub &&
      ! grep -rqn 'cutover' identity/ 2>/dev/null
    </automated>
  </verify>
  <done>
    Filesystem confirms the 4 D-07 SSH files do not exist; `grep -rn
    cutover identity/` returns zero; D-07 scope is empty for Phase 11.
    Finding will be recorded under Task 12's SC#5 grep gate report. No
    commit produced by this task.

    (If the D-07 files turn out to exist contrary to pattern-mapper's
    finding, the executor SHALL produce a rewrite commit per the
    fallback path in `<action>` and record the actual changes in
    `11-VERIFICATION.md`.)
  </done>
</task>

<task type="auto">
  <name>Task 12: Write 11-VERIFICATION.md (steady-state install capture + SC#5 grep gate report)</name>
  <files>.planning/phases/11-v1-removal/11-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/10-v1-drop-remediation/10-VERIFICATION.md (the structural analog -- mirror YAML frontmatter shape, H1, section list; per 11-PATTERNS.md "What's different" the Acknowledgment section from 10-VERIFICATION.md does NOT apply)
    - .planning/phases/10-v1-drop-remediation/10-SMOKE.md (the Run Log table shape for the steady-state install capture per CONTEXT.md D-05 "Format mirrors 10-SMOKE.md `## Run Log` table")
    - .planning/phases/11-v1-removal/11-CONTEXT.md (`<decisions>` D-05: two mandatory sections -- steady-state install no-op on personal-laptop, SC#5 grep gate report; D-05 also clarifies summarized vs full capture is Claude's Discretion)
    - .planning/phases/11-v1-removal/11-PATTERNS.md (section "11-VERIFICATION.md (the create target)": shows the frontmatter shape, section list, and the two D-05 mandatory sections including where they slot in)
    - .planning/ROADMAP.md (Phase 11 Success Criteria 1-6 -- the must_haves observable truths are derived from these)
  </read_first>
  <action>
    Pre-flight: ALL prior tasks must have completed (Tasks 1-11 inclusive).
    The SC#5 grep gate MUST be run BEFORE this commit lands so the
    verification report does not show up in its own grep gate output (per
    CONTEXT.md D-05: "Run BEFORE the verification commit so the report
    itself does not show in the grep output").

    Step-by-step:

    1. **Run the steady-state install on personal-laptop:**

           date '+%Y-%m-%dT%H:%M:%SZ' > /tmp/phase11-install-start
           task install 2>&1 | tee /tmp/phase11-install.log
           echo "exit: $?" >> /tmp/phase11-install.log
           date '+%Y-%m-%dT%H:%M:%SZ' > /tmp/phase11-install-end

       Expected behavior on a converged machine: `task install` runs
       through the simplified pipeline (`links:all -> packages:install ->
       claude:install -> macos:defaults -> macos:shell -> packages:verify
       -> links:reconcile --warn-only`) with every sub-task hitting its
       status block (no work), exits 0, and NO `cutover:ack` step is ever
       invoked. Capture the full output (or a summarized one-line-per-
       subtask render per CONTEXT.md D-05 Claude's Discretion -- planner
       recommendation is FULL capture for auditability since the report is
       a one-time artifact and the install log is bounded by the steady-
       state no-op path).

    2. **Run the SC#5 grep gate:**

           git grep -nE '\bv1\b|profile_suffix|DOTFILES_PROFILE|cutover' \
                    -- ':!.planning/' ':!.claude/' \
                    -- ':!11-VERIFICATION.md' 2>&1 | tee /tmp/phase11-grep.log

       (The third `--` exclusion is belt-and-suspenders -- the
       verification doc is being written in this very commit and is
       technically in the worktree but not yet tracked; `git grep` against
       HEAD excludes uncommitted files. The explicit exclude protects
       against `--cached` invocation variants.)

       Expected hits after Tasks 1-11:
       - Zero hits in deleted/edited files (Taskfile.yml, bootstrap.zsh,
         README.md, docs/SECURITY.md, taskfiles/lint.yml,
         taskfiles/README.md, the 6 os/*.zsh files, install/cutover-gate.zsh,
         docs/CUTOVER.md, docs/MIGRATION.md, install/Brewfile*, the 8 v1
         taskfiles, the zsh/ tree).
       - Possible RESIDUAL hits in:
         * `install/README.md:5,23,42` -- references `cutover-gate.zsh` as
           an install/ tree member; deferred to Phase 14 TRIM-03 per
           CONTEXT.md `<deferred>`. List in the grep gate report with
           rationale "deferred to Phase 14 TRIM-03".
         * `install/README.md:29-34` -- v1 Brewfile bullets; same deferral
           rationale.
         * `os/defaults/*.zsh` / `os/shell-registration.zsh` -- pedagogical
           "v1 bug class" mentions in file headers; kept per CONTEXT.md
           `<specifics>` Claude's Discretion. List with rationale
           "pedagogical reference to the v1 macos:shell:145 bug class --
           keeps the structural-fix's purpose legible".
         * any other genuinely-residual hit the planner did not foresee
           -- list with rationale or open a follow-up.

    3. **Write `.planning/phases/11-v1-removal/11-VERIFICATION.md`** mirroring
       10-VERIFICATION.md's YAML frontmatter + section list. The frontmatter:

           ---
           status: passed
           phase: 11
           verified: <YYYY-MM-DD from /tmp/phase11-install-end>
           must_haves_score: 6/6
           requirement_ids: [RMV-01, RMV-02, RMV-03, RMV-04, RMV-05, RMV-06, RMV-07]
           re_verification:
             previous_status: none
             previous_score: n/a
             gaps_closed: []
             gaps_remaining: []
             regressions: []
           ---

       The required sections (mirror 10-VERIFICATION.md order MINUS the
       "Acknowledgment: Prerequisite Revert Commits" section since no
       reverts are involved this phase, PLUS the two D-05 new sections):

           # Phase 11: v1 Removal -- Verification Report
           **Phase Goal:** [paste from ROADMAP P11 §Goal verbatim]
           **Verified:** <date>
           **Status:** passed
           **Re-verification:** No -- initial verification

           ## Goal Achievement

           ### Observable Truths
           [table of 6 truths from must_haves.truths above; each row
            with Status (VERIFIED) + Evidence (file:line + grep result)]

           ### Required Artifacts
           [table covering Taskfile.yml, bootstrap.zsh, README.md,
            11-VERIFICATION.md per must_haves.artifacts above]

           ### Key Link Verification
           [table covering must_haves.key_links above]

           ### Behavioral Spot-Checks
           [grep -c assertions per task verify blocks; one row per
            edit task's acceptance criteria]

           ### Steady-State Install Capture                  <-- D-05 #1
           [full terminal output from /tmp/phase11-install.log, OR a
            summarized one-line-per-subtask render with overall elapsed
            time and exit code; planner discretion]

           ### Grep Gate Report                              <-- D-05 #2
           [full output from /tmp/phase11-grep.log; for each residual hit
            list rationale ("deferred to Phase 14 TRIM-03",
            "pedagogical reference", etc.) explicitly]
           Goal: ZERO residual hits except those listed with rationale.

           ### Requirements Coverage
           [table mapping RMV-01..07 to the task(s) that satisfied them
            and the evidence]

           ### Probe Execution
           (none in scope) -- Phase 11 has no scripts/*/tests/probe-*.sh
           infrastructure; success criteria exercised via the task graph
           and the steady-state install capture.

           ### Anti-Patterns Found
           Scanned files: [list edited files]. (none) -- no debt
           markers, no stub returns, no emojis introduced.

           ### Human Verification Required
           None. Operator-run steady-state install + grep gate were
           executed in this session and captured in the two D-05 sections
           above.

           ### Deferred Items
           [table of items NOT in Phase 11 scope:
            - install/README.md cutover-gate + Brewfile-bullets trim
              (Phase 14 TRIM-03)
            - .claude/CLAUDE.md:38 cutover-gate.zsh listing (Phase 14
              TRIM-04 dedupe; excluded from grep gate per D-05)
            - CLAUDE.md "v1" pedagogical references in os/*.zsh headers
              (kept by Claude's Discretion per CONTEXT.md <specifics>)
            - any others surfaced]

           ### Gaps Summary
           No gaps. All 6 must-have truths verified; SC#5 grep gate
           shows only deliberately-kept residual hits with documented
           rationale.

           ---
           _Verified: <date>_
           _Verifier: Claude (gsd-verifier)_

    4. **Commit:** `git add .planning/phases/11-v1-removal/11-VERIFICATION.md`
       then `git commit` with subject:
       `docs(11): record v1 removal verification`

    Do NOT commit the `/tmp/phase11-*` log files. They are working state.
    Their content is captured in 11-VERIFICATION.md.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/11-v1-removal/11-VERIFICATION.md &&
      grep -q '^status: passed$' .planning/phases/11-v1-removal/11-VERIFICATION.md &&
      grep -q '^phase: 11$' .planning/phases/11-v1-removal/11-VERIFICATION.md &&
      grep -q 'must_haves_score: 6/6' .planning/phases/11-v1-removal/11-VERIFICATION.md &&
      grep -q 'RMV-01.*RMV-07\|RMV-01, RMV-02' .planning/phases/11-v1-removal/11-VERIFICATION.md &&
      grep -q '### Steady-State Install Capture' .planning/phases/11-v1-removal/11-VERIFICATION.md &&
      grep -q '### Grep Gate Report' .planning/phases/11-v1-removal/11-VERIFICATION.md &&
      grep -q '### Observable Truths' .planning/phases/11-v1-removal/11-VERIFICATION.md &&
      grep -q '### Requirements Coverage' .planning/phases/11-v1-removal/11-VERIFICATION.md &&
      grep -q '### Deferred Items' .planning/phases/11-v1-removal/11-VERIFICATION.md &&
      grep -q '_Verifier: Claude (gsd-verifier)_' .planning/phases/11-v1-removal/11-VERIFICATION.md
    </automated>
  </verify>
  <done>
    `.planning/phases/11-v1-removal/11-VERIFICATION.md` exists with passing
    frontmatter (status: passed, phase: 11, must_haves_score: 6/6,
    requirement_ids covering RMV-01..07); the body contains the 11
    required sections including the two D-05 mandatory sections
    (Steady-State Install Capture + Grep Gate Report); the SC#5 grep gate
    report lists ZERO residual hits except those explicitly kept with
    rationale; the verifier footer is present. RMV-07 satisfied. Commit
    lands at green tree.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> `bootstrap.zsh` | Untrusted shell user invokes the bootstrap on a fresh machine; bootstrap acquires Homebrew, go-task, yq from upstream registries over HTTPS. |
| operator -> `task setup` -> `$XDG_STATE_HOME/dotfiles/machine` | User supplies the machine name via `task setup -- <name>`; the name is regex-validated and pinned to one of the manifests in `manifests/machines/`. |
| installer -> upstream registries (brew.sh, GitHub raw, Homebrew tap) | HTTPS-only trust chain; no checksum pins; documented in `docs/SECURITY.md`. |
| (RETIRED) operator -> `cutover-ack` sentinel -> `task install` precondition | Pre-Phase-11 boundary: a `cutover-ack` sentinel file in `$XDG_STATE_HOME/dotfiles/cutover-ack` was the operator's explicit acknowledgment before `task install` would proceed. This boundary is retired by RMV-04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering / Repudiation | `install/cutover-gate.zsh` deletion + `cutover:ack` task removal | accept (with mitigation in place) | The cutover-ack gate was a single operator-authored sentinel acknowledging readiness to install v2 on a given machine during the v1->v2 cutover window. Post-Phase-11, the per-machine identity boundary is now `task setup` itself: `task setup -- <name>` writes `$XDG_STATE_HOME/dotfiles/machine` after regex-validating the name and confirming a matching manifest exists under `manifests/machines/`. `task install` then keys off that file via `manifest:resolve`. The acknowledgment property the cutover-ack gate enforced (intentional, named install on a specific machine) is preserved by the manifest model itself: an install cannot proceed without an explicit `task setup` call, and `task setup` cannot succeed without a matching machine TOML. Severity: LOW. The retired gate was operator-discipline-as-code; the manifest-as-identity boundary is at-least-as-strong. Doc-rewrite in Task 9 (`docs/SECURITY.md`) makes the new boundary explicit. |
| T-11-02 | Information Disclosure | The deleted `docs/MIGRATION.md` documented the v1->v2 rollback procedure including `rm "$XDG_STATE_HOME/dotfiles/cutover-ack"` to un-cut a machine | accept | The rollback procedure was meaningful only during the cutover window. After Phase 11, v1 is gone; there is nothing to roll back TO. PROJECT.md retains the milestone narrative; the audit row in `.planning/phases/09-v1-drop-audit/AUDIT.md` retains the v1->v2 mapping for AI-agent context. No live operator path needs the rollback runbook. Severity: LOW. |
| T-11-03 | Tampering | `git rm` operations on 16 files + 1 tree | mitigate | Every delete is preceded by a pre-flight `grep` check (Tasks 3, 4, 5, 6) that confirms no live v2 caller references the file/tree. Phase 10's verification (10-VERIFICATION.md) is the upstream gate that proved `shell/` is a complete substitute for `zsh/` and `packages/<purpose>.rb` for `install/Brewfile*`. The green-tree-per-commit invariant (every commit leaves `task --list` + `task lint:taskfile` at-or-below baseline) per D-04 catches any caller missed by the grep checks. Severity: LOW. |
| T-11-04 | Denial of Service (regression) | Steady-state `task install` after simplification | mitigate | Task 12 runs `task install` end-to-end on personal-laptop after all 11 prior tasks and captures the output in `11-VERIFICATION.md`. A regression (e.g. a missing `cutover-gate.zsh` reference still present in some hidden caller) would surface as a non-zero exit OR an error message in the captured log. Severity: LOW (regression caught at verification time, not in production -- this IS the verification step). |

(No HIGH-severity threats. Phase 11 is a documented retirement of a working
boundary; the replacement boundary -- `task setup` + manifest model -- has
been in place since Phase 1.)
</threat_model>

<verification>
Phase-level verification is captured in Task 12's deliverable
(`.planning/phases/11-v1-removal/11-VERIFICATION.md`). The per-task `<verify>`
blocks above are the green-tree-per-commit gates.

End-to-end phase verification commands (run by Task 12):

1. **Steady-state install no-op:** `task install` on personal-laptop after
   Tasks 1-11 commits. Expected: exits 0, no `cutover:ack` step ever invoked,
   pipeline reads `links:all -> packages:install -> claude:install ->
   macos:defaults -> macos:shell -> packages:verify -> links:reconcile
   --warn-only`. Full terminal output captured in 11-VERIFICATION.md
   `### Steady-State Install Capture`.

2. **SC#5 grep gate:**
   `git grep -nE '\bv1\b|profile_suffix|DOTFILES_PROFILE|cutover' -- ':!.planning/' ':!.claude/'`.
   Expected: zero hits except those listed with rationale in
   11-VERIFICATION.md `### Grep Gate Report` (deferred Phase 14 trims;
   pedagogical "v1 bug class" file-header references). Run BEFORE Task 12's
   commit lands.

3. **Task surface integrity:** `task --list` succeeds and emits the v2-only
   task surface with NO `cutover:ack` task listed.

4. **Lint baseline:** `task lint:taskfile` exits at-or-below pre-Phase-11
   baseline (24 failures). Post-Phase-11 the count is expected to drop
   significantly because the deleted v1 taskfiles carried 9 of those
   failures.
</verification>

<success_criteria>
All six ROADMAP Phase 11 success criteria satisfied:

1. **SC#1 -- 8 v1 leftover taskfiles deleted:** `ls taskfiles/` shows only
   v2 files (helpers, identity, links, lint, macos, manifest, packages,
   shell, test, claude, plus README.md and the test/ fixture subdir).
   Verified by Task 4 acceptance criteria.

2. **SC#2 -- zsh/ tree deleted + install/Brewfile* deleted:** `test ! -d
   zsh/`; `find install -name 'Brewfile*' -print` returns empty. Verified
   by Tasks 5 and 6 acceptance criteria.

3. **SC#3 -- cutover infrastructure fully removed:**
   `install/cutover-gate.zsh` deleted (Task 3); `cutover:ack` task removed
   from Taskfile.yml (Task 1); `cutover_gate_check` precondition removed
   from install: (Task 1); `docs/CUTOVER.md` deleted (Task 7);
   `docs/MIGRATION.md` deleted per D-01 (Task 7). Per-machine 7-day-soak
   model retired entirely.

4. **SC#4 -- Taskfile.yml simplified:** "v1 leftover taskfiles" comment
   block (lines ~22-26) deleted; includes contain only real v2 taskfiles;
   no v1 file path appears anywhere. Verified by Task 1 acceptance criteria.

5. **SC#5 -- grep gate returns only deliberately-kept refs:**
   `git grep -E '\bv1\b|profile_suffix|DOTFILES_PROFILE|cutover'` (excluding
   `.planning/` and `.claude/`) returns the documented residual set
   (install/README.md deferred-to-P14; os/*.zsh pedagogical v1-bug-class
   references) listed with rationale in 11-VERIFICATION.md. Verified by
   Task 12.

6. **SC#6 -- steady-state install succeeds end-to-end:** `task install` on
   personal-laptop completes with exit 0, no cutover-ack step, no manual
   gate, no missing-file error. Captured in 11-VERIFICATION.md
   `### Steady-State Install Capture`. Verified by Task 12.

All 7 requirement IDs (RMV-01..RMV-07) satisfied; mapping in
11-VERIFICATION.md `### Requirements Coverage`.
</success_criteria>

<output>
On phase completion, the executor MUST create
`.planning/phases/11-v1-removal/11-01-SUMMARY.md` mirroring the format used
by `.planning/phases/10-v1-drop-remediation/10-01-SUMMARY.md` (single-plan
phase summary). The SUMMARY references the 11-VERIFICATION.md artifact
produced by Task 12.
</output>

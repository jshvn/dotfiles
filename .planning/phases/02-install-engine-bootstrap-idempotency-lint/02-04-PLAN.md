---
phase: 02-install-engine-bootstrap-idempotency-lint
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - install/cutover-gate.zsh
  - Taskfile.yml
  - bootstrap.zsh
autonomous: true
requirements: [BTSP-04, BTSP-06, LINT-08]

must_haves:
  truths:
    - "install/cutover-gate.zsh exists; defines cutover_gate_check() function with 0/1 return convention (D-07, D-08)"
    - "cutover_gate_check returns 0 only when $XDG_STATE_HOME/dotfiles/cutover-ack exists, is well-formed (single line: <name> <ts>), and the name matches $(cat $XDG_STATE_HOME/dotfiles/machine) (D-08)"
    - "cutover_gate_check failure modes (missing / malformed / mismatch) emit the actionable error block to stderr referencing 'task cutover:ack -- <name>' and 'docs/CUTOVER.md' (D-07)"
    - "cutover-gate.zsh uses double-source guard via DOTFILES_CUTOVER_GATE_LOADED (S3)"
    - "Taskfile.yml (rewritten) drops the v1 'tasks: update:' block entirely (D-10, BTSP-06)"
    - "Taskfile.yml includes manifest, lint, links, brew, claude, macos sub-taskfiles per RESEARCH §4.1 includes block"
    - "Taskfile.yml task 'install' has preconditions: that source cutover-gate.zsh and call cutover_gate_check (D-09 — destructive op gated)"
    - "Taskfile.yml task 'install' has deps: [manifest:resolve] (P1 D-14 — auto-rebuild)"
    - "Taskfile.yml task 'install' cmds: in order: links:all, brew:install, claude:install, macos:defaults, macos:shell, then a final success message"
    - "task --list-all has no 'update' task (D-10 / BTSP-06)"
    - "bootstrap.zsh's TODO(Plan 02-04) marker is replaced with the live source + cutover_gate_check invocation (D-07)"
    - "Read-only tasks (lint, manifest:show, manifest:test, manifest:validate) are NOT gated — verifiable by inspection of preconditions blocks (D-09)"
    - "LINT-08 is documented as DEPRECATED in the plan (no implementation, per D-11) — covered structurally by Plan 02-01's lint:taskfile (LINT-01 contract)"
  artifacts:
    - path: "install/cutover-gate.zsh"
      provides: "cutover_gate_check function — sourced by bootstrap.zsh AND by Taskfile.yml install preconditions"
      contains: "cutover_gate_check()"
      min_lines: 60
    - path: "Taskfile.yml"
      provides: "v2 root taskfile — single canonical 'install' entry, no 'update' block"
      contains: "install:"
      min_lines: 70
    - path: "bootstrap.zsh"
      provides: "(modified) — TODO marker replaced with cutover-gate sourcing"
      contains: "cutover_gate_check"
  key_links:
    - from: "bootstrap.zsh"
      to: "install/cutover-gate.zsh"
      via: "source ${DOTFILEDIR}/install/cutover-gate.zsh && cutover_gate_check"
      pattern: "cutover-gate.zsh"
    - from: "Taskfile.yml task install preconditions"
      to: "install/cutover-gate.zsh"
      via: "sh: source {{.DOTFILEDIR}}/install/cutover-gate.zsh && cutover_gate_check"
      pattern: "cutover-gate.zsh"
    - from: "Taskfile.yml includes"
      to: "taskfiles/lint.yml + manifest.yml + 4 stub taskfiles (Plan 02-01 + 02-02 outputs)"
      via: "includes: lint: ./taskfiles/lint.yml, links: ./taskfiles/links-stub.yml, etc."
      pattern: "includes:"
---

<objective>
Build `install/cutover-gate.zsh` (the gate helper) AND rewrite root `Taskfile.yml` to use it AND wire the gate sourcing into `bootstrap.zsh` (replacing Plan 02-03's TODO marker). After this plan, `bootstrap.zsh` and `task install` BOTH gate on `$XDG_STATE_HOME/dotfiles/cutover-ack` per D-07 / D-09. The Taskfile.yml rewrite drops `tasks: update:` per D-10 and establishes the canonical `task install` body composition per RESEARCH §4.1.

Per D-09: gated tasks include `task install` (canonical entry); NOT gated: `task lint`, `task manifest:test`, `task manifest:show`, `task manifest:validate`, future `task validate` (P8), future `task test` (P7).

Per D-11: LINT-08 (5s timing test) is DEPRECATED — this plan claims the LINT-08 requirement ID via the deprecation marker and does not implement a timing test. The structural status: contract (LINT-01, enforced by Plan 02-01's lint:taskfile) is the actual idempotency guarantee.

Per BTSP-04: `task setup -- <name>` is already implemented in P1's `taskfiles/manifest.yml`. This plan claims BTSP-04 by ensuring the v2 Taskfile.yml continues to include `manifest.yml` and that `task setup -- personal-laptop && test -f $XDG_STATE_HOME/dotfiles/machine` still passes after the rewrite (smoke verified).

Output: One new file (`install/cutover-gate.zsh`), one rewritten file (`Taskfile.yml`), one edited file (`bootstrap.zsh` — TODO replacement).
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-CONTEXT.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-RESEARCH.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-PATTERNS.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-VALIDATION.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-01-SUMMARY.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-03-SUMMARY.md
@CLAUDE.md
@.claude/CLAUDE.md
@Taskfile.yml
@taskfiles/manifest.yml
@install/messages.zsh
@install/resolver.zsh
@bootstrap.zsh

<interfaces>
Functions install/cutover-gate.zsh exports:
- `cutover_gate_check()` — returns 0 on valid sentinel, 1 on missing/malformed/mismatch
- `_cutover_gate_emit_error(machine, reason)` — internal helper (underscore prefix); writes the actionable error block to stderr

State files read by cutover-gate.zsh:
- `${XDG_STATE_HOME:-$HOME/.local/state}/dotfiles/machine` — single-line machine name (P1-owned)
- `${XDG_STATE_HOME:-$HOME/.local/state}/dotfiles/cutover-ack` — single line: `<machine-name> <ISO-8601-UTC-timestamp>` (P2-owned this phase; writer is P8 CUTV-03)

Functions sourced from install/messages.zsh:
- `error "..."` — used inside _cutover_gate_emit_error for the leading red [ERROR] line; multi-line block is `{ ... } >&2`-wrapped per S4

Includes that Taskfile.yml gains (replaces v1 includes block):
- manifest: ./taskfiles/manifest.yml (P1 — `task setup`, `task manifest:resolve`, etc.)
- lint:     ./taskfiles/lint.yml (Plan 02-01 — read-only, NOT gated)
- links:    ./taskfiles/links-stub.yml (Plan 02-02; P3 swaps for real)
- brew:     ./taskfiles/brew-stub.yml (Plan 02-02; P5 swaps for real)
- claude:   ./taskfiles/claude-stub.yml (Plan 02-02; P7 swaps for real)
- macos:    ./taskfiles/macos-stub.yml (Plan 02-02; P6 swaps for real)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build install/cutover-gate.zsh — sourced helper with cutover_gate_check function</name>
  <files>install/cutover-gate.zsh</files>
  <read_first>
    - install/messages.zsh (P1 analog — lines 1-21 for the file-header style + double-source guard pattern; lines 33-48 for error() function signature)
    - install/resolver.zsh lines 1-50 (28-line header pattern; defensive messages-source under set -u)
    - taskfiles/manifest.yml lines 51-75 (STATE_DIR / STATE_FILE / RESOLVED_JSON_PATH var definitions; the `head -n1 | sed -e ...` trim-edges-only pattern from MACHINE var — P1's WR-08 fix)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-RESEARCH.md sections 6.1, 6.2, 6.3 (sentinel format, gate helper full body, enforcement points)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-PATTERNS.md "install/cutover-gate.zsh" subsection (header pattern, double-source guard, defensive messages-source, state-file read pattern, sentinel-line read pattern, error-emission pattern)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-CONTEXT.md `<decisions>` D-07, D-08, D-09; `<specifics>` (cutover-ack sentinel format example + cutover-ack gate error message example)
  </read_first>
  <action>
    Create `install/cutover-gate.zsh` per RESEARCH §6.2 (the function body is reproduced verbatim there).

    1. **28-line header banner** per S1: `#!/bin/zsh` + the comment block from RESEARCH §6.2 (Sourced by / Reads / Exits / "The sentinel WRITER is owned by Phase 8 CUTV-03 — P2 only reads/enforces").

    2. **Double-source guard** per S3: `[[ -n "${DOTFILES_CUTOVER_GATE_LOADED:-}" ]] && return 0; DOTFILES_CUTOVER_GATE_LOADED=1`. The `:-` empty-default operator is REQUIRED under `set -u` so callers (which use `set -euo pipefail`) don't trip on the unset-var test.

    3. **Defensive messages-source** per the pattern from install/resolver.zsh:36-40 (PATTERNS.md): `: "${DOTFILES_MESSAGES_LOADED:=}"; [[ -z "$DOTFILES_MESSAGES_LOADED" ]] && source "${DOTFILEDIR:?}/install/messages.zsh"`. The `${DOTFILEDIR:?}` requires DOTFILEDIR to be set by the caller (bootstrap.zsh exports it; Taskfile.yml's preconditions: block sets it via {{.DOTFILEDIR}} interpolation before sourcing).

    4. **`cutover_gate_check()` function** per RESEARCH §6.2 verbatim:
       - Local vars: `state_dir`, `machine_file`, `ack_file`, `active_machine`, `ack_machine`, `ack_ts`
       - state_dir: `"${XDG_STATE_HOME:-$HOME/.local/state}/dotfiles"` (S8)
       - If `! -f "$machine_file"`: `error "no machine selected (run: task setup -- <machine-name>)"`; return 1
       - active_machine: `head -n1 "$machine_file" | sed -e 's/^[[:space:]]*//' -e 's/[[:space:]]*$//'` (S8 — the P1 WR-08-blessed trim-edges-only pattern; do NOT use `tr -d '[:space:]'` which mangles `"bad name\\n"` into `"badname"`)
       - If `! -f "$ack_file"`: `_cutover_gate_emit_error "$active_machine" "missing"`; return 1
       - `read -r ack_machine ack_ts < "$ack_file"` (whitespace split, ignores extras, no backslash interp)
       - If `-z "$ack_machine"` OR `-z "$ack_ts"`: `_cutover_gate_emit_error "$active_machine" "malformed"`; return 1
       - If `"$ack_machine" != "$active_machine"`: `_cutover_gate_emit_error "$active_machine" "mismatch (sentinel claims '$ack_machine')"`; return 1
       - `return 0`

    5. **`_cutover_gate_emit_error(machine, reason)` helper** per RESEARCH §6.2 verbatim — the multi-line block is wrapped in `{ ... } >&2` (S4):
       ```
       {
         echo
         error "machine '${machine}' is not cut over to v2 (${reason})."
         echo
         echo "  This branch is v2-only. v1 lives on master."
         echo "  To cut this machine over, run:"
         echo "    task cutover:ack -- ${machine}"
         echo
         echo "  See docs/CUTOVER.md for the full procedure."
         echo
       } >&2
       ```

    Do NOT add `set -euo pipefail` to this file. It is a SOURCED library (no shebang execution), so per D-14 it is exempt from LINT-04 (which only checks `-perm +111`). The shebang `#!/bin/zsh` is present for editor syntax-highlighting only — the file is never directly executed. To be safe under callers that run `set -u`, every variable expansion uses the `:-` default form where appropriate.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && zsh -n install/cutover-gate.zsh && grep -q 'cutover_gate_check()' install/cutover-gate.zsh && grep -q 'DOTFILES_CUTOVER_GATE_LOADED' install/cutover-gate.zsh && grep -q '_cutover_gate_emit_error' install/cutover-gate.zsh && (export DOTFILEDIR="$(pwd)" XDG_STATE_HOME=/tmp/cutover-test-noack; rm -rf /tmp/cutover-test-noack; mkdir -p /tmp/cutover-test-noack/dotfiles; echo personal-laptop > /tmp/cutover-test-noack/dotfiles/machine; zsh -c 'source install/cutover-gate.zsh; cutover_gate_check' 2>&1; rc=$?; [[ $rc -eq 1 ]] && echo "PASS: gate fails with exit 1 when ack missing" || { echo "FAIL: gate returned $rc"; exit 1; })</automated>
  </verify>
  <acceptance_criteria>
    - `install/cutover-gate.zsh` exists and parses with `zsh -n`
    - File contains `cutover_gate_check()` function definition
    - File contains `_cutover_gate_emit_error()` helper function
    - Double-source guard present: `DOTFILES_CUTOVER_GATE_LOADED` set after first source
    - Sourcing the file twice in one shell does not redefine the function (verifiable by `source install/cutover-gate.zsh; source install/cutover-gate.zsh; type cutover_gate_check` succeeding)
    - With `XDG_STATE_HOME=/tmp/cutover-test-noack` (machine file present, no cutover-ack file), `cutover_gate_check` returns 1 and emits the actionable error block to stderr containing `task cutover:ack` and `docs/CUTOVER.md`
    - With a valid sentinel (`personal-laptop 2026-05-15T10:23:00Z`) matching the machine file, `cutover_gate_check` returns 0
    - With a mismatched sentinel (`work-laptop 2026-05-15T10:23:00Z` while machine file says `personal-laptop`), `cutover_gate_check` returns 1 with mismatch error
    - active_machine read uses `head -n1 | sed -e 's/^[[:space:]]*//' -e 's/[[:space:]]*$//'` (NOT `tr -d '[:space:]'` — that is the P1 WR-08 anti-pattern)
  </acceptance_criteria>
  <done>install/cutover-gate.zsh shipped; cutover_gate_check returns 0/1 correctly across the three failure modes (missing/malformed/mismatch) and the success case</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite Taskfile.yml — drop update:, add includes for stubs + lint, install: with cutover-gate preconditions</name>
  <files>Taskfile.yml</files>
  <read_first>
    - Taskfile.yml (current v1 — lines 1-50 for vars block + lines 54-72 for v1 profile-suffixed includes block + lines 92-116 for v1 task install body + lines 118-139 for the v1 update: block being dropped)
    - taskfiles/manifest.yml lines 35-49 (P1 vars block + DOTFILES_MESSAGES pattern; lines 80-93 for MANIFEST_JSON + MANIFEST ref pattern)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-RESEARCH.md section 4.1 (the full v2 Taskfile.yml shape with includes block + task install body composition + cutover-gate preconditions + deps:[manifest:resolve])
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-PATTERNS.md "Taskfile.yml (rewrite)" subsection (top-of-file pattern, HOMEBREW_PREFIX detection, DOTFILES_MESSAGES, MANIFEST_JSON + MANIFEST, includes block transformation, install body)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-CONTEXT.md `<decisions>` D-09 (gate scope: install gated; lint NOT gated), D-10 (drop update:), D-11 (LINT-08 deprecated)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-01-SUMMARY.md (Plan 02-01's record of what lint deems clean) — to confirm the rewritten Taskfile.yml passes lint:taskfile against itself
  </read_first>
  <action>
    Rewrite `Taskfile.yml` per RESEARCH §4.1 verbatim. Structure top-to-bottom:

    1. **`version: '3'`** + **`set: [errexit, pipefail]`** + **`silent: true`** (port from v1 lines 1-5).

    2. **`vars:` block** per RESEARCH §4.1: HOME, XDG_CONFIG_HOME, XDG_DATA_HOME, XDG_STATE_HOME, XDG_CACHE_HOME, ZDOTDIR; DOTFILEDIR via the v1 pattern from PATTERNS.md (`sh: cd "$(dirname "$(realpath "${TASKFILE:-$0}")")" && pwd`); HOMEBREW_PREFIX via the v1 arch detection pattern (PATTERNS.md "HOMEBREW_PREFIX detection pattern to PORT"); DOTFILES_MESSAGES (`source '{{.DOTFILEDIR}}/install/messages.zsh'`).

       Add the manifest-loading vars from PATTERNS.md "Manifest-loading vars to ADD": `MANIFEST_JSON` (sh body that cats $RESOLVED_JSON_PATH or echoes `{}` with a stderr warning) and `MANIFEST: ref: 'fromJson .MANIFEST_JSON'`. Also add `RESOLVED_JSON_PATH: '{{.XDG_STATE_HOME}}/dotfiles/resolved.json'` so the MANIFEST_JSON sh body has a path to read.

       Drop v1's `VALID_PROFILES` and `PROFILE` and `PROFILE_FILE` and `BREW_SHELLENV` vars — none are needed in v2 (manifests replace profile, BREW_SHELLENV was only used by v1 macos:shell which is owned by P6).

    3. **`includes:` block** per RESEARCH §4.1 EXACTLY:
       ```
       includes:
         manifest: ./taskfiles/manifest.yml
         lint:     ./taskfiles/lint.yml
         links:    ./taskfiles/links-stub.yml
         brew:     ./taskfiles/brew-stub.yml
         claude:   ./taskfiles/claude-stub.yml
         macos:    ./taskfiles/macos-stub.yml
       ```
       Add an inline comment after each stub line: `# P3 wires real bodies` / `# P5 wires real bodies` / `# P7 wires real bodies` / `# P6 wires real bodies` per RESEARCH §4.1.

       Drop the v1 `helpers:` include — `taskfiles/helpers.yml` is included only from sub-taskfiles that need it (manifest.yml does not; lint.yml does via Plan 02-01 Task 1). The root Taskfile.yml does NOT call helper tasks directly.

    4. **`tasks:` block** with these tasks:

       - `default`: `desc: "List available tasks"`, `cmds: [task --list]`, no status (this is intentional — `task --list` always re-runs; add a `# lint-allow: cmds-without-status` marker so Plan 02-01's lint:taskfile honors the exemption — OR set `status: [false]` and let the orchestrator handle it. Recommendation: add the comment marker AND `status: [false]` so it's belt-and-suspenders.)

         Wait — re-read PATTERNS.md S5/S9: for tasks that genuinely have no idempotent state (like `default`/`task --list`), the cleanest enforcement is `status: [false]`. But that re-runs always and lint:taskfile from Plan 02-01 special-cases the lint.yml file itself, not this. Use `status: [false]` here to make it structurally lint-clean (it has a status: block, even if always-true-fail).

         Actually, the cleanest minimum is: for `default`, set `status: [true]` because `task --list` IS idempotent (it just prints info; running it twice is no-op). Use `status: [true]`.

       - `install`: per RESEARCH §4.1 verbatim — `desc:`, `summary:` (3-line block per RESEARCH), `preconditions:` containing the cutover-gate `sh:` block with `msg:` fallback, `deps: [manifest:resolve]`, and `cmds:` listing the five subtask delegations + final success message:
         ```
         - task: links:all
         - task: brew:install
         - task: claude:install
         - task: macos:defaults
         - task: macos:shell
         - |
           {{.DOTFILES_MESSAGES}}
           success "install complete"
         ```
         The preconditions sh: body MUST be: `source "{{.DOTFILEDIR}}/install/cutover-gate.zsh"; cutover_gate_check`. Pure orchestration — no implicit shell var leakage.

         For LINT-03a: the `install` task has `cmds:` but NO explicit `status:` — IS this a violation? Re-read RESEARCH §5.4 / PATTERNS.md: tasks whose cmds are PURELY task delegations are exempt. The final cmd is a shell block (the success message), not a task delegation, so technically this task has 5 task-delegations + 1 shell cmd. Plan 02-01's lint:taskfile body uses `yq '.cmds | all(has("task"))'` — the final shell cmd makes that return false. To stay lint-clean, add `status: [false]` (re-run-always, valid because the deps and preconditions handle the actual gating) OR add a `# lint-allow: cmds-without-status` marker. Use `status: [false]` — it's the most explicit and matches the v1 install task semantics (re-run is intentional; idempotency comes from each subtask's own status: block per LINT-01).

       - **NO `update` task** (D-10 / BTSP-06). Add a top-of-tasks comment: `# Note: task install IS task update -- D-10 dropped the update: block. Phase 3 ships shell alias 'update=task install' for muscle memory.`

       - **NO `validate` task** in P2 (per CONTEXT `<deferred>`: validate composition is owned by Phase 8). Drop the v1 validate: block.

    5. **File-header banner** at the top per S1 (taskfile-banner style with `# ===` separator): "Taskfile.yml -- root taskfile for v2 dotfiles. Single canonical entry: `task install`. No `task update` (D-10 — install IS update). Cutover-ack gate enforced via preconditions on install. Includes manifest (P1), lint (P2), and four stub taskfiles for Phases 3/5/6/7."

    Verify the rewritten Taskfile.yml passes Plan 02-01's lint:taskfile when run against the v2-only file set (just the rewritten Taskfile.yml + the four stubs + lint.yml + manifest.yml). It WILL still flag v1 leftover taskfiles (claude.yml, links.yml, macos.yml, common.yml, brew.yml, profile.yml, profile-tasks.yml — these are v1 files NOT included by the v2 Taskfile.yml). This is documented as expected: v1 files exist in the working tree but are NOT loaded by go-task because Taskfile.yml's includes block does not reference them. Plan 02-01's lint scope is `taskfiles/*.yml` glob — which still hits the v1 leftovers.

    For now: the v1 leftover taskfiles will continue to fail lint. They will be removed in subsequent phases as each phase replaces them. Document this in the SUMMARY: "Lint passes against v2 includes; v1 leftover taskfiles still fail lint until Phase 3/5/6/7 replace them."
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && task --list-all --json | jq -e '.tasks | map(.name) | (index("install") and (index("update") | not))' && task --list-all --json | jq -e '.tasks[] | select(.name == "install") | (.summary // "" | contains("task install IS task update"))' && grep -q 'cutover_gate_check' Taskfile.yml && ! grep -qE '^[[:space:]]+update:' Taskfile.yml && grep -q 'links:    ./taskfiles/links-stub.yml' Taskfile.yml && grep -q 'lint:     ./taskfiles/lint.yml' Taskfile.yml</automated>
  </verify>
  <acceptance_criteria>
    - `Taskfile.yml` parses: `task --list-all --json` exits 0
    - `task --list-all --json | jq '.tasks | map(.name) | index("update")'` returns `null` (no update task — BTSP-06 / D-10)
    - `task --list-all --json | jq '.tasks | map(.name) | index("install")'` returns a number (install task exists)
    - `Taskfile.yml` includes block lists exactly: manifest, lint, links, brew, claude, macos (six entries)
    - `Taskfile.yml` install task has preconditions: containing `cutover_gate_check`
    - `Taskfile.yml` install task has `deps: [manifest:resolve]` (P1 D-14 contract)
    - `Taskfile.yml` install task cmds: lists `task: links:all`, `task: brew:install`, `task: claude:install`, `task: macos:defaults`, `task: macos:shell` in that order
    - `task setup -- personal-laptop` still works (BTSP-04 — verifiable via running it and confirming `$XDG_STATE_HOME/dotfiles/machine` contains `personal-laptop`)
    - File-header banner present, references D-10 (update dropped) and the includes architecture
    - When the cutover-ack file is missing, `task install` fails with exit 1 and the actionable error message (tested live: `rm -f $XDG_STATE_HOME/dotfiles/cutover-ack; task install` exits 1)
    - When `task install` runs the body succeeds in printing the stub trace lines for all five subtasks (assuming a valid cutover-ack sentinel exists)
  </acceptance_criteria>
  <done>Taskfile.yml rewritten; canonical entry is task install; no update task; cutover-gate gate enforced via preconditions; includes wired for stubs + lint + manifest</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire cutover-gate sourcing into bootstrap.zsh (replace Plan 02-03's TODO marker)</name>
  <files>bootstrap.zsh</files>
  <read_first>
    - bootstrap.zsh (Plan 02-03 output — find the `# TODO(Plan 02-04):` marker line)
    - install/cutover-gate.zsh (just-shipped from Task 1)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-RESEARCH.md sections 3.4, 3.6 (cutover-gate sourcing in bootstrap.zsh — happens BEFORE the next-step hint print)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-PATTERNS.md "Cutover-gate sourcing block" excerpt
  </read_first>
  <action>
    In `bootstrap.zsh`, find the line containing `# TODO(Plan 02-04):` (placed by Plan 02-03 Task 1 between the yq install step and the next-step hint print). Replace that line with the live cutover-gate sourcing per RESEARCH §3.4:
    ```
    source "${DOTFILEDIR}/install/cutover-gate.zsh"
    cutover_gate_check || exit 1
    ```
    Add a `# --- Step 4: cutover-ack gate (D-07)` banner comment immediately above per the pattern from Step 1/2/3 banners in the bootstrap script.

    The placement is critical: the gate fires AFTER all three trust-anchor installs (so brew/task/yq are present even on a not-yet-cut-over machine — the user can re-run bootstrap once the cutover-ack is written) and BEFORE the next-step hint (so a not-yet-cut-over machine doesn't get told "task install" then fail at task install's own gate, which is misleading).

    On bootstrap re-run:
    - Tools already installed → all three command -v guards skip
    - cutover_gate_check runs → if cutover-ack missing or wrong machine, exits 1 with actionable error
    - On a properly-cut-over machine: gate passes silently, next-step hint prints

    This means BTSP-03 (re-run is no-op) is now slightly more nuanced: a re-run on an UN-cut-over machine fails at the gate. This is intentional per D-07/D-09 (the gate is a destructive-op gate, and bootstrap is a destructive op in the sense that it modifies system state via brew install). Document this in the SUMMARY: "After this task, BTSP-03's re-run no-op contract holds for any machine where cutover-ack exists; on un-cut-over machines, re-run produces the actionable error which IS the intended behavior per D-07."
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && grep -q 'source.*cutover-gate.zsh' bootstrap.zsh && grep -q 'cutover_gate_check' bootstrap.zsh && ! grep -q 'TODO(Plan 02-04)' bootstrap.zsh && zsh -n bootstrap.zsh && (export XDG_STATE_HOME=/tmp/cutover-test-fresh; rm -rf /tmp/cutover-test-fresh; mkdir -p /tmp/cutover-test-fresh/dotfiles; echo personal-laptop > /tmp/cutover-test-fresh/dotfiles/machine; ./bootstrap.zsh > /tmp/bootstrap-no-ack.txt 2>&1; rc=$?; [[ $rc -eq 1 ]] && grep -q 'task cutover:ack' /tmp/bootstrap-no-ack.txt && echo "PASS: bootstrap fails with gate error when cutover-ack missing" || { echo "FAIL: rc=$rc, output:"; cat /tmp/bootstrap-no-ack.txt; exit 1; })</automated>
  </verify>
  <acceptance_criteria>
    - `bootstrap.zsh` contains `source "${DOTFILEDIR}/install/cutover-gate.zsh"` and `cutover_gate_check || exit 1`
    - `bootstrap.zsh` no longer contains the `TODO(Plan 02-04)` marker
    - `bootstrap.zsh` still passes `zsh -n`
    - With XDG_STATE_HOME pointing at a fresh dir containing `dotfiles/machine` but NO `dotfiles/cutover-ack`, `./bootstrap.zsh` exits 1 and stderr contains `task cutover:ack` and `docs/CUTOVER.md` references
    - The cutover-gate sourcing happens AFTER all three brew/task/yq install steps and BEFORE the next-step hint print (verifiable by line-order grep: line of "source.*cutover-gate" comes after line of "brew install yq" and before line of "Bootstrap complete. Next steps")
  </acceptance_criteria>
  <done>bootstrap.zsh now enforces the cutover-ack gate per D-07 / D-09; defense-in-depth with task install's own preconditions block</done>
</task>

</tasks>

<verification>
- `install/cutover-gate.zsh` exists; cutover_gate_check returns correct 0/1 across all four cases (missing machine file, missing ack, malformed ack, mismatched ack, valid ack)
- `Taskfile.yml` rewritten; no `update:` block; install task has cutover-gate preconditions and deps:[manifest:resolve]; includes block wires the four stubs + lint + manifest
- `bootstrap.zsh` sources cutover-gate.zsh and calls cutover_gate_check before the next-step hint
- `task --list-all` does NOT list `update`
- `rm -f $XDG_STATE_HOME/dotfiles/cutover-ack && task install` exits 1 with actionable error referencing `task cutover:ack` (the (gate) row from VALIDATION.md)
- `rm -f $XDG_STATE_HOME/dotfiles/cutover-ack && task lint && task manifest:show` BOTH succeed (read-only tasks NOT gated per D-09)
- `task setup -- personal-laptop` still works (BTSP-04 inherited from P1)
- LINT-08 documented as DEPRECATED in this plan's SUMMARY (no implementation per D-11; supplanted structurally by Plan 02-01's lint:taskfile enforcing LINT-01)
</verification>

<success_criteria>
- BTSP-04 satisfied (P1's task setup unchanged)
- BTSP-06 satisfied (no update task; install is canonical)
- LINT-08 marked DEPRECATED per D-11 (no timing test; LINT-01's structural status: contract is the actual idempotency guarantee, enforced by Plan 02-01)
- Cutover-gate enforced at TWO points (bootstrap.zsh AND task install preconditions) per D-09 defense-in-depth
- Read-only tasks (lint, manifest:test, manifest:show, manifest:validate) NOT gated per D-09 — verified live
- v1 leftover taskfiles (claude.yml, links.yml, macos.yml, etc.) remain on disk but are NOT included by v2 Taskfile.yml; they will be removed in subsequent phases
</success_criteria>

<output>
After completion, create `.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-04-SUMMARY.md` per the GSD summary template. Include:
- Verification that the (gate) rows from VALIDATION.md pass live (bootstrap fails without ack; task install fails without ack; task lint passes without ack)
- Confirmation that LINT-08 is marked DEPRECATED — supplanted by Plan 02-01's structural LINT-01 enforcement
- A note on which v1 leftover taskfiles still fail lint (these are out of scope for P2 — they're owned by P3/P5/P6/P7 replacements)
</output>

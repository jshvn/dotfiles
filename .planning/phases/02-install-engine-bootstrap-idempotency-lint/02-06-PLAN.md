---
phase: 02-install-engine-bootstrap-idempotency-lint
plan: 06
type: execute
wave: 3
depends_on: [02-03, 02-04]
files_modified:
  - docs/SECURITY.md
autonomous: true
requirements: [BTSP-05, DOCS-07]

must_haves:
  truths:
    - "docs/SECURITY.md exists at repo root under docs/"
    - "Document contains required H2 sections: 'What This Document Covers', 'Bootstrap Trust Chain', 'Threat Model', 'Trust Anchors', 'What This Document Does NOT Cover', 'How to Audit', 'Future Hardening' (per RESEARCH §8 outline)"
    - "Bootstrap Trust Chain section has H3 subsections 'Step 1 -- Homebrew installer' and 'Step 2 -- go-task and yq' (per RESEARCH §8 + CONTEXT.md `<specifics>`)"
    - "Document explicitly references the AUDIT line that bootstrap.zsh emits (docs/SECURITY.md is the cross-reference target of bootstrap.zsh's audit message; per CONTEXT.md `<specifics>`)"
    - "Document explicitly states 'no checksum pin' on the brew installer with rationale (D-01 trust boundary)"
    - "Threat Model is a markdown table with columns: Threat | Mitigation | Residual Risk (per RESEARCH §8)"
    - "Document explicitly notes SSH key handling is deferred to Phase 4; Claude hook secret-scanning deferred to Phase 7 (per CONTEXT.md `<specifics>` item 5 + 6 of SECURITY.md outline)"
    - "Document is shellcheck-clean for any embedded code blocks (the audit commands in 'How to Audit' must not have shell errors)"
    - "No emojis (project convention is stricter than markdown — applies to docs/*.md too)"
    - "No AI attribution"
  artifacts:
    - path: "docs/SECURITY.md"
      provides: "Bootstrap trust chain documentation — what bootstrap.zsh downloads, from where, how verified, who is trusted"
      contains: "## Bootstrap Trust Chain\\n## Threat Model\\n## Trust Anchors"
      min_lines: 80
  key_links:
    - from: "bootstrap.zsh AUDIT block"
      to: "docs/SECURITY.md"
      via: "bootstrap.zsh's AUDIT line says 'see docs/SECURITY.md' — this doc is the referent"
      pattern: "docs/SECURITY.md"
    - from: "docs/SECURITY.md"
      to: "Phase 4 SSH docs (future) and Phase 7 hook docs (future)"
      via: "explicit deferred-section cross-references"
      pattern: "deferred to Phase"
---

<objective>
Author `docs/SECURITY.md` per RESEARCH §8 outline + CONTEXT.md `<specifics>` reference outline. The document serves DOCS-07 (security trust-chain documentation) and BTSP-05 (bootstrap trust chain documented). Per CONTEXT scope: bootstrap trust chain ONLY in P2; SSH key handling defers to Phase 4 (note in security doc: "SSH key handling: see docs/SECURITY.md after Phase 4"); Claude hook secret-scanning defers to Phase 7.

Per RESEARCH §1 / §8: the doc has seven required H2 sections. The H2 / H3 structure is what BTSP-05's verification command greps for (`ggrep -qE '^## Bootstrap Trust Chain$' docs/SECURITY.md` etc. — see VALIDATION.md row BTSP-05).

Wave 3 placement: this plan depends on Plan 02-03 (bootstrap.zsh — the AUDIT line wording) and Plan 02-04 (Taskfile.yml — the cutover-gate pattern that the doc references). The doc's content is fully derived from RESEARCH §8 + CONTEXT `<specifics>`; the only reason it's not in Wave 1 is so its language can match the as-shipped bootstrap.zsh / Taskfile.yml exactly (no risk of doc drift if planner refines wording in Wave 1/2).

Output: One new file at `docs/SECURITY.md`.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-CONTEXT.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-RESEARCH.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-PATTERNS.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-VALIDATION.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-03-SUMMARY.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-04-SUMMARY.md
@CLAUDE.md
@.claude/CLAUDE.md
@bootstrap.zsh
@manifests/README.md

<interfaces>
The shipped bootstrap.zsh (Plan 02-03) emits this AUDIT block to stderr (verbatim from RESEARCH §3.2):
```
AUDIT: about to fetch and execute brew install script
  source: https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh
  trust:  HTTPS only, no checksum pin (see docs/SECURITY.md)
  ctrl-C now to abort (3 second window)
```

docs/SECURITY.md must be the document referenced by `(see docs/SECURITY.md)` in that block — its content has to actually answer the questions the AUDIT line raises.

The cutover-gate.zsh helper (Plan 02-04) refers to `docs/CUTOVER.md` in its actionable error block. That doc is owned by Phase 8 (CUTV-03); per CONTEXT `<deferred>`: "P2's gate references the doc; it doesn't need to exist for P2 to ship." docs/SECURITY.md may briefly note this but does not need to elaborate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/SECURITY.md per RESEARCH §8 outline (7 H2 sections)</name>
  <files>docs/SECURITY.md</files>
  <read_first>
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-RESEARCH.md section 8 (the full SECURITY.md outline reproduced verbatim — 7 H2 sections, threat-model table, audit commands)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-CONTEXT.md `<specifics>` "Reference SECURITY.md outline" (the 6 high-level items the doc must cover)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-PATTERNS.md "docs/SECURITY.md" subsection (style conventions: no emojis, no AI attribution, plain markdown with --- separators)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-VALIDATION.md row "BTSP-05" / "DOCS-07" (verification command exact: `test -f docs/SECURITY.md && ggrep -qE '^## Bootstrap Trust Chain$' docs/SECURITY.md && ggrep -qE '^## Threat Model$' docs/SECURITY.md && ggrep -qE '^## Trust Anchors$' docs/SECURITY.md`)
    - bootstrap.zsh (just-shipped from Plan 02-03 + 02-04) — reference the actual AUDIT line wording
    - manifests/README.md (P1 analog — terse, declarative, scoped to one concern; reference for the project's documentation tone)
    - CLAUDE.md (project-stricter no-emojis-anywhere rule; no AI attribution)
  </read_first>
  <action>
    Create `docs/SECURITY.md` per RESEARCH §8 outline verbatim. The structure (top-down):

    **Title (H1):** `# Security: Bootstrap Trust Chain`

    **H2 #1: `## What This Document Covers`** (1-2 paragraphs).
    - Scope: bootstrap.zsh trust chain only.
    - Out of scope (forward-pointers): SSH key handling deferred to Phase 4; Claude hook secret-scanning deferred to Phase 7; per-machine credential management documented in `docs/MACHINES.md` (Phase 8).

    **H2 #2: `## Bootstrap Trust Chain`** with two H3 subsections:
    - `### Step 1 -- Homebrew installer` (note: use literal `--` not em-dash; project convention prefers ASCII)
      - What is downloaded: the Homebrew install shell script (`install.sh`)
      - From where: `https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh`
      - How it is verified: HTTPS only. No checksum pin, no signature verification.
      - Why this trust boundary is accepted (D-01 rationale, paraphrased from RESEARCH §8): canonical install path published by brew.sh; pinning to a checksum requires updating the pin every time Homebrew ships an installer change; maintenance burden outweighs security delta over HTTPS-only retrieval; we accept the same trust boundary as the wider macOS development community.
      - Audit signal: `bootstrap.zsh` emits an `AUDIT:` line to stderr before fetching the installer, with a 3-second sleep so users have an explicit abort window.
    - `### Step 2 -- go-task and yq`
      - What is downloaded: Homebrew formula bottles for `go-task` and `yq`
      - From where: Homebrew's CDN (`ghcr.io/homebrew/core/...` for bottle artifacts; metadata served from `formulae.brew.sh`)
      - How it is verified: Homebrew computes SHA-256 checksums for every bottle and refuses to install if the downloaded artifact does not match the formula's declared checksum.
      - Why this trust boundary is accepted: stronger than Step 1; we inherit Homebrew's standard formula-verification path.

    **H2 #3: `## Threat Model`** — markdown table with columns `Threat | Mitigation | Residual Risk`, populated per RESEARCH §8 (5 rows):
    1. MITM on raw.githubusercontent.com during installer fetch | TLS only | Real -- accepted as cost of pragmatic install
    2. Compromise of GitHub mirror serving the installer | HTTPS only; no signature check | Real -- documented
    3. Compromise of Homebrew bottle artifact in CDN | SHA-256 checksum validated by brew | Mitigated
    4. Compromise of formula metadata declaring wrong SHA-256 | Formula commits are PR-reviewed by Homebrew | Mitigated -- accept Homebrew's review process
    5. Local user runs bootstrap with hostile `$DOTFILEDIR` env override | Bootstrap re-resolves `DOTFILEDIR` from `$0` | Mitigated

    **H2 #4: `## Trust Anchors`** (numbered list per RESEARCH §8): Apple (macOS, system curl/bash/zsh, TLS); GitHub Inc. (raw.githubusercontent.com TLS, repo integrity, ghcr.io artifact storage); The Homebrew project (install script correctness, formula metadata, bottling pipeline).

    **H2 #5: `## What This Document Does NOT Cover`** (bullet list per RESEARCH §8): SSH key handling -- deferred to Phase 4 (identity layer); 1Password agent integration -- deferred to Phase 4; Claude hook secret-scanning -- deferred to Phase 7 hardening; Per-machine credential management -- out of scope for v1; documented in `docs/MACHINES.md` (Phase 8).

    **H2 #6: `## How to Audit`** — two concrete commands per RESEARCH §8:
    - `curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh | less` (inspect what bootstrap will run)
    - `brew info --json=v2 go-task | jq '.formulae[0].bottle.stable.files'` (verify go-task's bottle SHA-256)

    **H2 #7: `## Future Hardening`** (bullet list, "listed for reference; not in v1 scope"): Pinned-checksum brew installer (vendor `install.sh` at a known commit); shellcheck integration for hooks; GitHub Actions CI for lint regression detection.

    **Style enforcement** (CLAUDE.md):
    - No emojis (project convention applies to markdown too — stricter than the global no-emojis-in-non-markdown rule)
    - No AI attribution
    - Plain markdown; section separators use `---` between top-level `##` blocks (optional — only if visual grouping helps)
    - Use literal ASCII `--` for separators in headings (e.g., `### Step 1 -- Homebrew installer`), not em-dash
    - Code blocks use triple-backtick fences with language hints (e.g., ```` ```bash ````) where applicable

    **Verification-friendly section headings**: VALIDATION.md row BTSP-05's command does `ggrep -qE '^## Bootstrap Trust Chain$' docs/SECURITY.md`. The headings MUST be `## Bootstrap Trust Chain`, `## Threat Model`, `## Trust Anchors` — exact case, exact wording, no trailing punctuation.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && test -f docs/SECURITY.md && ggrep -qE '^## What This Document Covers$' docs/SECURITY.md && ggrep -qE '^## Bootstrap Trust Chain$' docs/SECURITY.md && ggrep -qE '^## Threat Model$' docs/SECURITY.md && ggrep -qE '^## Trust Anchors$' docs/SECURITY.md && ggrep -qE '^## What This Document Does NOT Cover$' docs/SECURITY.md && ggrep -qE '^## How to Audit$' docs/SECURITY.md && ggrep -qE '^## Future Hardening$' docs/SECURITY.md && ggrep -qE '^### Step 1 -- Homebrew installer$' docs/SECURITY.md && ggrep -qE '^### Step 2 -- go-task and yq$' docs/SECURITY.md && grep -q 'no checksum pin' docs/SECURITY.md && grep -q 'AUDIT:' docs/SECURITY.md && grep -q 'Phase 4' docs/SECURITY.md && grep -q 'Phase 7' docs/SECURITY.md && wc -l docs/SECURITY.md | awk '{ exit ($1 < 80) }' && echo "BTSP-05 / DOCS-07 verified"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/SECURITY.md` exists
    - All 7 required H2 sections present in exact wording for BTSP-05 verification: `## What This Document Covers`, `## Bootstrap Trust Chain`, `## Threat Model`, `## Trust Anchors`, `## What This Document Does NOT Cover`, `## How to Audit`, `## Future Hardening`
    - Two required H3 subsections: `### Step 1 -- Homebrew installer`, `### Step 2 -- go-task and yq`
    - Document mentions `no checksum pin` (the D-01 acknowledged trust boundary)
    - Document mentions `AUDIT:` (the bootstrap.zsh audit signal)
    - Document mentions `Phase 4` and `Phase 7` (deferred-scope cross-references)
    - Threat Model is a markdown table with at least 5 rows
    - File is at least 80 lines (per min_lines artifact constraint)
    - No emojis present (verifiable: `! ggrep -P '[\\x{1F300}-\\x{1F9FF}]' docs/SECURITY.md`)
    - No AI attribution strings (verifiable: `! ggrep -iE '(generated by|written by|co-authored-by) (claude|ai|gpt|copilot)' docs/SECURITY.md`)
  </acceptance_criteria>
  <done>docs/SECURITY.md shipped per RESEARCH §8 outline; passes the BTSP-05 verification command; no emojis; no AI attribution</done>
</task>

</tasks>

<verification>
- `docs/SECURITY.md` exists with all 7 required H2 sections
- BTSP-05 / DOCS-07 verification command from VALIDATION.md returns exit 0
- Bootstrap trust chain content covers Step 1 (Homebrew installer) and Step 2 (go-task + yq)
- Threat Model table populated with 5 rows per RESEARCH §8
- SSH key handling and Claude hook scanning explicitly noted as deferred to Phase 4 / Phase 7
- No emojis; no AI attribution
</verification>

<success_criteria>
- BTSP-05 satisfied
- DOCS-07 satisfied
- The (see docs/SECURITY.md) reference in bootstrap.zsh's AUDIT block now resolves to a real document with answers to the trust questions
- The doc establishes the project's documentation tone for future security docs (Phase 4 SSH, Phase 7 hooks)
</success_criteria>

<output>
After completion, create `.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-06-SUMMARY.md` per the GSD summary template. Include: confirmation that the BTSP-05 verification command (from VALIDATION.md) returns exit 0; line count of the shipped doc.
</output>

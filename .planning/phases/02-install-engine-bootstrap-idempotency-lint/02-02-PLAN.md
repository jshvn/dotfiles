---
phase: 02-install-engine-bootstrap-idempotency-lint
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - taskfiles/links-stub.yml
  - taskfiles/brew-stub.yml
  - taskfiles/claude-stub.yml
  - taskfiles/macos-stub.yml
autonomous: true
requirements: [LINT-01]

must_haves:
  truths:
    - "Each stub file is a valid go-task taskfile (parses with task --list-all --json)"
    - "Each stub task has status: [true] so LINT-03a passes structurally (LINT-01 contract)"
    - "Each stub desc starts with 'STUB (Phase X)' so `grep -r 'STUB (Phase ' taskfiles/` enumerates outstanding work"
    - "links-stub.yml has task `all` (called by task install)"
    - "brew-stub.yml has task `install` (called by task install)"
    - "claude-stub.yml has task `install` (called by task install)"
    - "macos-stub.yml has tasks `defaults` AND `shell` (both called by task install)"
    - "Each stub task body emits a single line to stderr identifying it as a stub (RESEARCH §4.1 visible-trace pattern)"
  artifacts:
    - path: "taskfiles/links-stub.yml"
      provides: "Phase 3 placeholder for SHEL-01..03"
      contains: "tasks:\\n  all:"
    - path: "taskfiles/brew-stub.yml"
      provides: "Phase 5 placeholder for PKGS-01..04"
      contains: "tasks:\\n  install:"
    - path: "taskfiles/claude-stub.yml"
      provides: "Phase 7 placeholder for CLDE-01..04"
      contains: "tasks:\\n  install:"
    - path: "taskfiles/macos-stub.yml"
      provides: "Phase 6 placeholder for OSCF-01..05"
      contains: "tasks:\\n  defaults:\\n  shell:"
  key_links:
    - from: "taskfiles/links-stub.yml + brew-stub.yml + claude-stub.yml + macos-stub.yml"
      to: "Taskfile.yml (Plan 02-04 — root)"
      via: "includes: links: ./taskfiles/links-stub.yml etc. (RESEARCH §4.1 includes block)"
      pattern: "includes:"
    - from: "Taskfile.yml task install body"
      to: "stub task names (links:all, brew:install, claude:install, macos:defaults, macos:shell)"
      via: "cmds: [task: links:all, task: brew:install, ...]"
      pattern: "task:"
---

<objective>
Ship four stub taskfiles (`links-stub.yml`, `brew-stub.yml`, `claude-stub.yml`, `macos-stub.yml`) that establish the v2 `task install` call graph. Per RESEARCH §4.1 and PATTERNS.md "stub-taskfile" role: each stub task uses `status: [true]` so LINT-03a passes structurally; each desc starts with `STUB (Phase X)` so outstanding work is grep-able.

Purpose: `task install` (shipped in Plan 02-04) calls `links:all`, `brew:install`, `claude:install`, `macos:defaults`, `macos:shell`. Those bodies are owned by Phases 3/5/6/7 respectively. P2 needs SOMETHING included so the call graph parses and the lint suite has a complete graph to run against. The stubs are that something.

Output: Four files under `taskfiles/`, each minimal (one or two tasks, status: [true], single stderr trace line).
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-CONTEXT.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-RESEARCH.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-PATTERNS.md
@CLAUDE.md
@.claude/CLAUDE.md
@taskfiles/helpers.yml

<interfaces>
Each stub conforms to go-task v3 schema:
```
version: '3'
tasks:
  <name>:
    desc: "STUB (Phase N will implement <REQ-IDS>)"
    status: [true]
    cmds:
      - echo "<task-name> -- stub (Phase N will implement)" >&2
```

Phases that own real bodies:
- Phase 3 owns `taskfiles/links.yml` (replaces links-stub.yml) — SHEL-01..03
- Phase 5 owns `taskfiles/brew.yml` (replaces brew-stub.yml) — PKGS-01..05, VRFY-01..04
- Phase 6 owns `taskfiles/macos.yml` (replaces macos-stub.yml) — OSCF-01..05
- Phase 7 owns `taskfiles/claude.yml` (replaces claude-stub.yml) — CLDE-01..04
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author taskfiles/links-stub.yml and taskfiles/brew-stub.yml</name>
  <files>taskfiles/links-stub.yml, taskfiles/brew-stub.yml</files>
  <read_first>
    - taskfiles/helpers.yml (P1 analog — structurally simplest existing taskfile; lines 1-23 for the version + header banner pattern)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-RESEARCH.md section 4.1 (stub-subtask pattern, including the `status: [true]` + visible-stderr-trace convention)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-PATTERNS.md "stub-taskfile" section (header pattern + per-stub task list)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-CONTEXT.md `<decisions>` D-12 (lint logic inlined; stubs are NOT lint scripts) and Claude's Discretion section (planner stubs subtasks owned by later phases)
  </read_first>
  <action>
    Create `taskfiles/links-stub.yml` with `version: '3'`; the file-header banner per PATTERNS.md S1 ("links-stub.yml -- Phase 3 placeholder; loaded by Taskfile.yml via includes: links: ./taskfiles/links-stub.yml; Phase 3 (SHEL-01..03) replaces this file with the real taskfiles/links.yml"); a `tasks:` block containing exactly one task `all` with `desc: "STUB (Phase 3 will implement SHEL-01..03)"`, `status: [true]`, `cmds: ["echo 'links:all -- stub (Phase 3 will implement SHEL-01..03)' >&2"]`. Match the indentation and YAML style of `taskfiles/helpers.yml` (2-space indent, no trailing whitespace).

    Create `taskfiles/brew-stub.yml` mirroring the same pattern. File header: "brew-stub.yml -- Phase 5 placeholder; Phase 5 (PKGS-01..05, VRFY-01..04) replaces this with the real taskfiles/brew.yml". Single task `install` with `desc: "STUB (Phase 5 will implement PKGS-01..05, VRFY-01..04)"`, `status: [true]`, `cmds: ["echo 'brew:install -- stub (Phase 5 will implement)' >&2"]`.

    No `vars:`, no `includes:`, no `internal:`. Stubs are intentionally minimal — Phase N replaces the file wholesale. Both files end with a trailing newline (no `\\n\\n` doubles).
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && task --list-all --json -t taskfiles/links-stub.yml | jq -e '.tasks[0].name == "all"' && task --list-all --json -t taskfiles/brew-stub.yml | jq -e '.tasks[0].name == "install"' && grep -c 'STUB (Phase ' taskfiles/links-stub.yml taskfiles/brew-stub.yml | grep -q ':1$'</automated>
  </verify>
  <acceptance_criteria>
    - `taskfiles/links-stub.yml` parses; lists exactly one task `all`
    - `taskfiles/brew-stub.yml` parses; lists exactly one task `install`
    - Each `desc:` starts with `STUB (Phase N`
    - Each task has `status: [true]` (verifiable via `yq '.tasks[].status' taskfiles/links-stub.yml` returning `[true]`)
    - Each cmds: block writes a single line to stderr (verifiable via `task -t taskfiles/links-stub.yml all 2>&1 1>/dev/null | grep -q stub`)
    - File-header banner present and references the phase that will replace the file
  </acceptance_criteria>
  <done>links-stub.yml and brew-stub.yml shipped; both parse; both stubs trace to stderr; both pass LINT-03a structurally</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author taskfiles/claude-stub.yml and taskfiles/macos-stub.yml</name>
  <files>taskfiles/claude-stub.yml, taskfiles/macos-stub.yml</files>
  <read_first>
    - taskfiles/links-stub.yml (just-shipped from Task 1 — copy the structural template)
    - taskfiles/helpers.yml (header pattern reference)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-RESEARCH.md section 4.1 (stub pattern; macos-stub needs TWO tasks: `defaults` and `shell`)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-PATTERNS.md "Per-stub task list to ship in P2" subsection
  </read_first>
  <action>
    Create `taskfiles/claude-stub.yml` mirroring the Task 1 pattern. File header: "claude-stub.yml -- Phase 7 placeholder; Phase 7 (CLDE-01..04, TOOL-01..04, TEST-01..02) replaces this with taskfiles/claude.yml". Single task `install` with `desc: "STUB (Phase 7 will implement CLDE-01..04)"`, `status: [true]`, `cmds: ["echo 'claude:install -- stub (Phase 7 will implement)' >&2"]`.

    Create `taskfiles/macos-stub.yml`. File header: "macos-stub.yml -- Phase 6 placeholder; Phase 6 (OSCF-01..05) replaces this with taskfiles/macos.yml". TWO tasks per the call graph: `defaults` (`desc: "STUB (Phase 6 will implement OSCF-01..03, OSCF-05)"`, `status: [true]`, `cmds: ["echo 'macos:defaults -- stub (Phase 6 will implement)' >&2"]`) AND `shell` (`desc: "STUB (Phase 6 will implement OSCF-04 -- chsh + shell-registration)"`, `status: [true]`, `cmds: ["echo 'macos:shell -- stub (Phase 6 will implement)' >&2"]`). The `shell` task corresponds to OSCF-04 (the `chsh` bug-fix task) and is called explicitly by `task install` per the v1 Taskfile.yml shape that RESEARCH §4.1 ports.

    Same constraints as Task 1: no `vars:`, no `includes:`, no `internal:`, single trailing newline.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && task --list-all --json -t taskfiles/claude-stub.yml | jq -e '.tasks[0].name == "install"' && task --list-all --json -t taskfiles/macos-stub.yml | jq -e '.tasks | map(.name) | (length == 2 and index("defaults") and index("shell"))' && for f in taskfiles/links-stub.yml taskfiles/brew-stub.yml taskfiles/claude-stub.yml taskfiles/macos-stub.yml; do yq '.tasks | to_entries | .[] | .value.status' "$f" | grep -q '\\[true\\]' && echo "$f: status [true]" || { echo "FAIL $f"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - `taskfiles/claude-stub.yml` parses; lists exactly one task `install`
    - `taskfiles/macos-stub.yml` parses; lists exactly two tasks `defaults` and `shell`
    - All four stub files (cumulative across Tasks 1-2) have every task with `status: [true]`
    - `grep -rl 'STUB (Phase ' taskfiles/` returns all four stub files
    - Each task body trace line goes to stderr (not stdout) so a `task install >/dev/null` invocation still surfaces the stubs' visible trace to a human watcher
  </acceptance_criteria>
  <done>All four stub files shipped; full v2 call graph (links:all, brew:install, claude:install, macos:defaults, macos:shell) is includable from Taskfile.yml in Plan 02-04</done>
</task>

</tasks>

<verification>
- All four stub files exist under `taskfiles/`
- All five stub tasks (`links:all`, `brew:install`, `claude:install`, `macos:defaults`, `macos:shell`) have `status: [true]`
- All five `desc:` lines start with `STUB (Phase`
- `grep -r 'STUB (Phase ' taskfiles/ | wc -l` returns 5 (one per stub task)
- After Plan 02-01's lint:taskfile body lands, `task -t taskfiles/lint.yml taskfile` against just these four stubs reports zero LINT-03a violations
</verification>

<success_criteria>
- Stubs are structurally lint-clean: each task has `status:` (LINT-03a passes), no `$VAR` in status (LINT-02 passes — there are no shell vars at all), no bare `ln -s` (LINT-03b passes — there are no `ln` invocations)
- The `STUB (Phase N` marker is the canonical grep query for "outstanding work in Phase N"
- Phase 3/5/6/7 replace the file wholesale (not edit-in-place); the stub-to-real swap is one git operation per phase
- No coupling between stubs: replacing brew-stub.yml does not require any change to links-stub.yml etc.
</success_criteria>

<output>
After completion, create `.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-02-SUMMARY.md` per the GSD summary template. Note: subsequent plans in this phase (Plan 02-04 for Taskfile.yml integration) will reference these stubs by include path.
</output>

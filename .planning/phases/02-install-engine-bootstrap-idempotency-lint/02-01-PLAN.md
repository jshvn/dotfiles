---
phase: 02-install-engine-bootstrap-idempotency-lint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - taskfiles/lint.yml
autonomous: true
requirements: [LINT-01, LINT-02, LINT-03, LINT-04, LINT-05, LINT-06, LINT-07]

must_haves:
  truths:
    - "task lint exits 0 on a clean repository"
    - "task lint:taskfile flags any $VAR-style shell variable inside a status: block (per D-13)"
    - "task lint:taskfile flags tasks with cmds: but no status: (excepting internal:true and pure task-delegation cmds, per D-13)"
    - "task lint:taskfile flags bare ln -s outside taskfiles/helpers.yml (per D-13 / S7)"
    - "task lint:shell-headers flags executable .zsh files missing set -euo pipefail (per D-14 — only -perm +111 files)"
    - "task lint:portability prints WARN lines for pbcopy/osascript/defaults/sw_vers/dscl/PlistBuddy/pbpaste in shell/ and os/ but always exits 0 (per D-13)"
    - "task lint:syntax runs zsh -n on every .zsh file under repo (excluding .git and .planning) and runs task --list-all --json against every taskfiles/*.yml"
    - "task lint (default) aggregates lint:syntax, lint:taskfile, lint:shell-headers, lint:portability sequentially (LINT-06)"
    - "Lint suite reads from disk only — no manifest dependency, no resolved.json dependency, no cutover-ack gate (per D-09)"
  artifacts:
    - path: "taskfiles/lint.yml"
      provides: "Lint suite — lint:syntax, lint:taskfile, lint:shell-headers, lint:portability, default aggregator"
      contains: "tasks:\\n  default:\\n  syntax:\\n  taskfile:\\n  shell-headers:\\n  portability:"
  key_links:
    - from: "taskfiles/lint.yml"
      to: "install/messages.zsh"
      via: "{{.DOTFILES_MESSAGES}} template var (sourced inside each cmds: block)"
      pattern: "DOTFILES_MESSAGES"
    - from: "taskfiles/lint.yml"
      to: "taskfiles/helpers.yml"
      via: "includes: _: ./helpers.yml (S7)"
      pattern: "includes:"
---

<objective>
Build the lint suite at `taskfiles/lint.yml` per D-12 (all logic inlined; no separate scripts under `install/lint/`). The suite enforces v2 conventions structurally: `$VAR` in `status:` blocks, `cmds:` without `status:`, bare `ln -s` outside helpers, executable `.zsh` missing `set -euo pipefail`, portability hints, `zsh -n` parse errors, and `task --list-all --json` baseline.

Purpose: The lint suite is the v2 quality gate from day one. It is built FIRST in Wave 1 (parallel with bootstrap rewrite) so that every later artifact lands under its enforcement. Per RESEARCH §1: "Build the lint suite FIRST (before bootstrap rewrite)."

Output: `taskfiles/lint.yml` containing `default`, `syntax`, `taskfile`, `shell-headers`, `portability` tasks. Tested for-real against existing v1 taskfiles in Plan 02-05's fixture suite.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-CONTEXT.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-RESEARCH.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-PATTERNS.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-VALIDATION.md
@CLAUDE.md
@.claude/CLAUDE.md
@taskfiles/manifest.yml
@taskfiles/helpers.yml
@install/messages.zsh

<interfaces>
Functions from `install/messages.zsh` (sourced via `{{.DOTFILES_MESSAGES}}`):
- `info "..."` — blue stdout
- `success "..."` — green stdout
- `warn "..."` — yellow stdout
- `error "..."` — red stderr
- `check "msg"` — checkmark stdout
- `cross "msg"` — cross stdout

Helpers from `taskfiles/helpers.yml` (included via `includes: _: ./helpers.yml`):
- `_:safe-link` (SOURCE, TARGET) — internal symlink helper
- `_:check-link` (TARGET, NAME) — internal symlink validator

Phase 1 P1 idiom (port from `taskfiles/manifest.yml manifest:test`, lines 277-412):
- `failures=0` counter envelope
- `check`/`cross` per-item reporting
- `exit "$failures"` final action (S9 from PATTERNS.md)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author taskfiles/lint.yml header, default aggregator, lint:syntax (LINT-07 + YAML parse)</name>
  <files>taskfiles/lint.yml</files>
  <read_first>
    - taskfiles/manifest.yml (P1 analog — lines 1-50 for `version: '3'` + header banner + `vars:` block + `includes: _:` pattern; lines 277-412 for the failures-counter + check/cross + exit "$failures" pattern that the lint cmds: bodies copy)
    - taskfiles/helpers.yml (lines 1-23 for header banner pattern; structurally simplest taskfile)
    - install/messages.zsh (lines 19-21 for the double-source guard; lines 33-48 for info/success/warn/error functions; line 47 for the >&2 convention used by error())
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-RESEARCH.md sections 5.1, 5.7, 5.8 (lint orchestration + zsh -n + task --list-all baseline)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-PATTERNS.md S1 (file header), S4 (errors to stderr), S9 (failures-counter idiom), S10 (numbered fixtures)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-CONTEXT.md `<decisions>` D-12, D-13, D-14, D-15
  </read_first>
  <action>
    Create `taskfiles/lint.yml` per pattern S1 with: `version: '3'`; the file-header banner from RESEARCH §7 (LINT-02/03a/03b/04/05/07 enumerated); `includes: _: ./helpers.yml`; a `vars:` block declaring `TASKFILE_GLOB: 'taskfiles/*.yml'` and a `ZSH_FIND` multi-line var equal to `find . -name '*.zsh' -not -path './.git/*' -not -path './.planning/*' -not -path './taskfiles/test/*'` (the test/ exclusion is so the fixture .zsh files in Plan 02-05 don't trigger real lint runs).

    Add `tasks:` containing exactly these task names (per D-12 / RESEARCH §5.1): `default`, `syntax`, `taskfile`, `shell-headers`, `portability`. Add a stub for `test-fixtures` with `desc: "Self-test the lint suite against fixtures (see Plan 02-05)"` and a single `cmds: ["echo 'test-fixtures runner ships in Plan 02-05' >&2"]` body — Plan 02-05 will replace this body.

    Implement `default` per RESEARCH §5.1: `desc: "Run all lint checks (LINT-06 aggregator)"` with `cmds:` consisting of four `task: <name>` entries in this order: `syntax`, `taskfile`, `shell-headers`, `portability`. No `status:` block on `default` (lint tasks are intentionally always-re-run; document this in a `# NOTE:` comment immediately above the task block).

    Implement `syntax` per RESEARCH §5.7 + §5.8: `desc: "YAML parse + zsh -n syntax checks (LINT-07)"`. First `cmds:` body sources `{{.DOTFILES_MESSAGES}}` then iterates `for f in {{.TASKFILE_GLOB}}; do task --list-all --json -t "$f" >/dev/null 2>&1 && check "yaml-parse: $f" || { cross "yaml-parse: $f -- task --list-all failed"; exit 1; }; done`. Second `cmds:` body uses the failures-counter envelope (S9): sources DOTFILES_MESSAGES, sets `failures=0`, runs `while IFS= read -r f; do zsh -n "$f" 2>&1 && check "zsh -n: $f" || { cross "zsh -n: $f"; failures=$((failures+1)); }; done < <({{.ZSH_FIND}})`, then `exit "$failures"`.

    Each task gets a `desc:` and a one-line preceding comment `# LINT-XX: <description>` to make the requirement-ID-to-task mapping grep-able.

    Do NOT add `status:` blocks to lint tasks (intentional — see RESEARCH §5.1 and PATTERNS.md S5/§taskfiles/lint.yml note). Note: this means LINT-03a will need to special-case taskfiles/lint.yml when run on the real repo. Handle this by adding a `# lint-allow: cmds-without-status` comment marker at the top of each lint task body, and Plan 02-01's lint:taskfile body (Task 2) MUST honor that marker (i.e., the lint:taskfile body skips files matching `taskfiles/lint.yml` when checking LINT-03a, OR more precisely: skips tasks whose preceding line contains `lint-allow: cmds-without-status`). Document this exemption in the file header banner.

    Apply S1 (file-level comment block) and CLAUDE.md "Section separators in YAML files use `# ===` or `# ---` banner style" — separate the four tasks with `# ---------------------------------------------------------------------------` banners.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && task --list-all --json -t taskfiles/lint.yml | jq -e '.tasks | map(.name) | (index("default") and index("syntax") and index("taskfile") and index("shell-headers") and index("portability") and index("test-fixtures"))'</automated>
  </verify>
  <acceptance_criteria>
    - `taskfiles/lint.yml` exists and parses cleanly: `task --list-all --json -t taskfiles/lint.yml` exits 0
    - File header banner matches PATTERNS.md S1 (filename + purpose + LINT-XX enumeration + "Read-only; not gated by cutover-ack" line)
    - `task -t taskfiles/lint.yml syntax` exits 0 against the existing repo (verified locally — every taskfiles/*.yml currently parses; every existing .zsh under repo currently passes `zsh -n`)
    - Six tasks exist: `default`, `syntax`, `taskfile`, `shell-headers`, `portability`, `test-fixtures`
    - `taskfiles/lint.yml` itself contains `# lint-allow: cmds-without-status` markers immediately above each task body so Task 2's lint:taskfile body can exempt them
  </acceptance_criteria>
  <done>Lint suite skeleton + lint:syntax body shipped; lint:syntax exits 0 against the existing repo</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement lint:taskfile (LINT-02 + LINT-03a + LINT-03b) and lint:shell-headers (LINT-04)</name>
  <files>taskfiles/lint.yml</files>
  <read_first>
    - taskfiles/lint.yml (just-shipped from Task 1)
    - taskfiles/manifest.yml lines 277-412 (the manifest:test failures-counter + check/cross idiom — S9 verbatim source)
    - taskfiles/macos.yml line 145 (the live $BREW_ZSH bug LINT-02 must catch)
    - taskfiles/links.yml lines 60-75 (the bare `ln -sf` LINT-03b must catch)
    - taskfiles/claude.yml lines 200-225 (the gsd-install cmds-without-status LINT-03a must catch)
    - bootstrap.zsh lines 1-10 (the `set -e` only LINT-04 must catch)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-RESEARCH.md sections 5.2, 5.3, 5.4, 5.5 (the four detection pipelines, each verified live in research session)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-PATTERNS.md S5 (template-vars-not-shell-vars rule), S7 (helpers.yml ln allowlist), S9 (failures envelope)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-CONTEXT.md D-13 (severity model: LINT-02/03a/03b/04 blocking; LINT-05 warn-only)
  </read_first>
  <action>
    In `taskfiles/lint.yml`, fill in the `taskfile` task body per RESEARCH §5.2 + §5.3 + §5.4. Use three sequential `cmds:` blocks within the single `taskfile` task (one per LINT-id) so per-check pass/fail reporting stays separable. Each block is the S9 failures-counter envelope (`failures=0; for f in $(eval echo {{.TASKFILE_GLOB}}); do ... done; exit $failures`).

    LINT-02 block (RESEARCH §5.2): `out=$(yq '.tasks[] | select(.status) | .status' "$f" 2>/dev/null | ggrep -nE '\\$[A-Za-z_][A-Za-z0-9_]*' | ggrep -vE '\\$\\(' | ggrep -vE '\\{\\{' || true)` — if non-empty, `cross "LINT-02: \\$VAR in status: block — $f"`, echo `$out` to stderr, increment failures.

    LINT-03b block (RESEARCH §5.3): `violations=$(ggrep -rn -E '\\bln[[:space:]]+-s' taskfiles/ | ggrep -v 'helpers.yml' | ggrep -v ':[[:space:]]*#' || true)` — if non-empty, `cross "LINT-03b: bare 'ln -s' outside helpers.yml"`, echo violations to stderr, exit 1. Note: the allowlist is exactly `taskfiles/helpers.yml` per D-13. Comment-line filter prevents matching `# example: ln -s ...`.

    LINT-03a block (RESEARCH §5.4): for each f in TASKFILE_GLOB, iterate `yq '.tasks | to_entries | .[] | select(.value | has("cmds")) | select(.value | has("status") | not) | .key' "$f"`. For each task_name: skip if `internal: true`; skip if `cmds` is purely task-delegations (`yq ".tasks.\"$task_name\".cmds | all(has(\"task\"))"` returns true); skip if file is `taskfiles/lint.yml` (the lint suite itself is intentionally always-re-run, per the `lint-allow: cmds-without-status` markers from Task 1) — implement this exemption by `[[ "$f" == *"lint.yml" ]] && continue`. Otherwise: `cross "LINT-03a: '$task_name' in $f has cmds: but no status:"`, increment failures.

    Implement `shell-headers` per RESEARCH §5.5: `desc: "Executable .zsh files have set -euo pipefail (LINT-04)"`. Body uses S9 envelope. File selector: `find . -name '*.zsh' -perm +111 -not -path './.git/*' -not -path './.planning/*' -not -path './taskfiles/test/*'`. For each f: `head -30 "$f" | ggrep -qE '^set -euo pipefail$' && check "LINT-04: $f"` else `cross "LINT-04: $f missing 'set -euo pipefail' in first 30 lines"; failures=$((failures+1))`. RESEARCH §5.5 confirms `head -30` is the correct window (P1 resolver puts the line at line 30 after a 28-line header).

    On the existing repo, this WILL flag `bootstrap.zsh` (BTSP-01 fix target — addressed by Plan 02-03) and `taskfiles/macos.yml:145` (the v1 $BREW_ZSH bug; v1 file lives on master, but the v2 repo state at this checkpoint may still contain v1 versions of macos.yml etc.). Verify: at the time this task runs, the v2 repo contains ONLY P1 deliverables + this lint.yml + Task 1's syntax body. `task lint:taskfile` and `task lint:shell-headers` MUST exit 0 against P1 deliverables alone. If v1 taskfiles still exist alongside P1 ones (because P1 didn't delete them), document the violations in the SUMMARY but do NOT change scope — bootstrap rewrite (Plan 02-03) and Taskfile.yml rewrite (Plan 02-04) remove the v1 violations as side effects.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && task --list-all --json -t taskfiles/lint.yml | jq -e '.tasks | map(.name) | (index("taskfile") and index("shell-headers"))' && task -t taskfiles/lint.yml taskfile 2>&1 | head -50 && task -t taskfiles/lint.yml shell-headers 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `task -t taskfiles/lint.yml taskfile` runs without crashing (it may exit non-zero if v1 violations remain in repo at this point — that is expected)
    - The output of `task -t taskfiles/lint.yml taskfile` against existing v1 taskfiles produces `cross` lines mentioning `LINT-02`, `LINT-03a`, `LINT-03b` consistent with the violations RESEARCH §5.2-5.4 verified live (e.g., `taskfiles/macos.yml` for $BREW_ZSH, `taskfiles/links.yml` for bare ln -s, `taskfiles/claude.yml` for cmds-without-status)
    - `task -t taskfiles/lint.yml shell-headers` reports `bootstrap.zsh` as missing `set -euo pipefail` (BTSP-01 fix target)
    - The lint:taskfile body honors the `lint.yml` self-exemption: `task -t taskfiles/lint.yml taskfile 2>&1 | grep -c 'LINT-03a:.*lint.yml'` returns 0
    - All four blocks use the S9 `failures=0 ... exit "$failures"` envelope from PATTERNS.md
  </acceptance_criteria>
  <done>lint:taskfile and lint:shell-headers shipped, structurally enforce LINT-02 / LINT-03a / LINT-03b / LINT-04 against the existing repo</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implement lint:portability (LINT-05, warn-only) and verify aggregator</name>
  <files>taskfiles/lint.yml</files>
  <read_first>
    - taskfiles/lint.yml (just-shipped from Tasks 1-2)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-RESEARCH.md section 5.6 (portability detection patterns + always-exit-0 contract)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-CONTEXT.md D-13 (LINT-05 is warn-only) and D-15 (no CI)
    - install/messages.zsh line 42 for `warn` function signature
  </read_first>
  <action>
    In `taskfiles/lint.yml`, implement the `portability` task per RESEARCH §5.6. `desc: "Portability hints for future-Linux v2 (warn-only, exit 0; LINT-05)"`. Body sources `{{.DOTFILES_MESSAGES}}`, declares `patterns=(pbcopy pbpaste osascript 'defaults read' 'defaults write' sw_vers dscl PlistBuddy)`, then `warnings=0; for pat in "${patterns[@]}"; do hits=$(ggrep -rnE "\\b${pat}\\b" shell/ os/ 2>/dev/null | ggrep -v ':[[:space:]]*#' || true); if [[ -n "$hits" ]]; then warn "LINT-05: portability hint -- '${pat}' found:"; echo "$hits" >&2; warnings=$((warnings+1)); fi; done`. End with `[[ $warnings -gt 0 ]] && warn "LINT-05: $warnings portability hint(s) found (non-blocking; exit 0)"; exit 0`.

    Note: `shell/` and `os/` directories DO NOT EXIST yet at the time Plan 02-01 runs (Phase 3 ships shell/, Phase 6 ships os/). The `ggrep -r` invocation tolerates missing directories via `2>/dev/null` and the `|| true` fallback. The task MUST exit 0 against the current repo state where neither directory exists.

    Run the full aggregator: `task -t taskfiles/lint.yml`. It runs syntax, taskfile, shell-headers, portability in sequence. The aggregator MAY exit non-zero (because of v1 violations) but the structure (LINT-06) is the deliverable here.

    Add a `# ===` separator banner (per CLAUDE.md YAML banner convention) between tasks 'shell-headers' and 'portability' so the file structure is human-scannable.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && task -t taskfiles/lint.yml portability; portability_exit=$?; [[ $portability_exit -eq 0 ]] && echo "PORTABILITY exits 0 (correct)" || { echo "PORTABILITY exited $portability_exit (should be 0)"; exit 1; }; task --list-all --json -t taskfiles/lint.yml | jq -e '.tasks | map(.name) | (length == 6 and index("portability"))'</automated>
  </verify>
  <acceptance_criteria>
    - `task -t taskfiles/lint.yml portability` exits 0 EVEN when shell/ and os/ contain matches OR when neither directory exists (always exit 0 per D-13)
    - `task -t taskfiles/lint.yml` (the default aggregator) executes all four sub-tasks in order: syntax, taskfile, shell-headers, portability
    - `task --list-all -t taskfiles/lint.yml` lists exactly six tasks: default, syntax, taskfile, shell-headers, portability, test-fixtures
    - When run from an empty shell/ + os/ tree, lint:portability still exits 0 (no false negatives on missing dirs)
    - File-header banner enumerates LINT-02/03a/03b/04/05/07 and matches the RESEARCH §7 skeleton
  </acceptance_criteria>
  <done>Full lint suite (LINT-01 through LINT-07 minus LINT-08 deprecated) ships in taskfiles/lint.yml; lint:portability is structurally warn-only</done>
</task>

</tasks>

<verification>
- `taskfiles/lint.yml` exists; parses with `task --list-all --json -t taskfiles/lint.yml`
- All six tasks (default, syntax, taskfile, shell-headers, portability, test-fixtures) present
- `task -t taskfiles/lint.yml syntax` exits 0 against existing repo
- `task -t taskfiles/lint.yml portability` exits 0 always
- LINT-08 is documented as DEPRECATED in the file-header banner with cross-reference to D-11; no implementation
</verification>

<success_criteria>
- LINT-01 through LINT-07 implemented as cmds: blocks under sub-tasks; LINT-06 implemented as the `default` aggregator
- All lint logic inlined in `taskfiles/lint.yml` per D-12
- Severity model: LINT-02/03a/03b/04/07 exit non-zero on detection; LINT-05 always exits 0 (D-13)
- File scope: `find . -name '*.zsh' -perm +111 -not -path './.git/*' -not -path './.planning/*' -not -path './taskfiles/test/*'` for shell-headers; `taskfiles/*.yml` for the taskfile checks (D-14)
- Read-only — no manifest dependency, no cutover-ack gate (D-09)
- Self-exemption for `taskfiles/lint.yml` documented and enforced via the `lint-allow: cmds-without-status` marker
</success_criteria>

<output>
After completion, create `.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-01-SUMMARY.md` per the GSD summary template. Include: a list of which v1 violations the lint suite caught when run against the current repo state (so Plan 02-03 / 02-04 know what their rewrites must clean up); confirmation that LINT-08 is marked DEPRECATED per D-11.
</output>

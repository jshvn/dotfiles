---
phase: 05-packages-layer-brewfile-composition-verification
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - taskfiles/packages.yml
  - taskfiles/brew-stub.yml
autonomous: true
requirements: [PKGS-02, PKGS-03, PKGS-05, VRFY-01, VRFY-02, VRFY-03]
requirements_addressed: [PKGS-02, PKGS-03, PKGS-05, VRFY-01, VRFY-02, VRFY-03]
tags: [taskfile, packages, verify, audit, brew-bundle]

must_haves:
  truths:
    - "taskfiles/packages.yml exists and replaces taskfiles/brew-stub.yml as the real bodies for PKGS-02..05 + VRFY-01..03"
    - "packages:compose task invokes install/compose-brewfile.zsh and writes the composed Brewfile to $XDG_CACHE_HOME/dotfiles/Brewfile"
    - "packages:install carries the two-condition status block from D-09: (a) test -f {{.COMPOSED_BREWFILE}} AND (b) brew bundle check --file={{.COMPOSED_BREWFILE}} --no-upgrade"
    - "packages:install status block uses {{.X}} template vars ONLY -- no $X shell vars (LINT-02 contract; v1 macos:shell:145 bug class)"
    - "packages:install runs brew bundle install --file=... ONLY when the status block fails (regenerates composed Brewfile first)"
    - "packages:verify enumerates EVERY formula + cask + mas from the composed Brewfile; prints per-package check/cross; exits non-zero with the failure count if any failed (D-07)"
    - "packages:audit reports installed-but-not-declared formulae (brew leaves), casks (brew list --cask), and MAS apps (mas list); non-blocking by default; --strict exits non-zero (D-11)"
    - "Every packages:* task that reads resolved.json declares deps: [\":manifest:resolve\"] (CF-08)"
    - "Aggregator tasks (verify, audit, validate) carry the # lint-allow: cmds-without-status marker (CF-04)"
    - "taskfiles/brew-stub.yml file remains on disk with body = `tasks: {}` plus a DEPRECATED header comment (keeps Phase-2 root include parsing; Plan 05 removes the include line, Plan 8 cleanup may delete the file)"
  artifacts:
    - path: "taskfiles/packages.yml"
      provides: "Real packages-layer taskfile with install/compose/verify/audit/validate"
      contains: ["packages:install", "packages:verify", "packages:audit", "packages:compose", "packages:validate", "brew bundle check"]
      min_lines: 200
    - path: "taskfiles/brew-stub.yml"
      provides: "Empty stub kept on disk so the Phase-2 root include in Taskfile.yml continues to parse until Plan 05 removes the include line; body is `tasks: {}` plus DEPRECATED header comment"
      contains: ["DEPRECATED", "tasks: {}"]
  key_links:
    - from: "taskfiles/packages.yml packages:install"
      to: "install/compose-brewfile.zsh (Plan 03)"
      via: "cmds block calls DOTFILEDIR=... XDG_CACHE_HOME=... zsh ${DOTFILEDIR}/install/compose-brewfile.zsh"
      pattern: "compose runs unconditionally inside install body when status fails"
    - from: "taskfiles/packages.yml packages:install status block"
      to: "$XDG_CACHE_HOME/dotfiles/Brewfile"
      via: "test -f + brew bundle check --no-upgrade"
      pattern: "two-condition status per D-09"
    - from: "taskfiles/packages.yml packages:verify cmds"
      to: "/Applications/<App>.app + command -v <bin>"
      via: "test -d / command -v shell-out per declared package"
      pattern: "VRFY-01 + VRFY-02 enumerate-all + count-failures per D-07"
    - from: "taskfiles/packages.yml packages:audit cmds"
      to: "brew leaves + brew list --cask + mas list output"
      via: "diff against composed Brewfile + extras"
      pattern: "VRFY-03 drift detection"
---

<objective>
Build the real `taskfiles/packages.yml` to replace the Phase 2 brew-stub. Five tasks:
`packages:install` (compose + brew bundle install with the two-condition status from D-09),
`packages:compose` (run the composer only -- inspection helper), `packages:verify` (per-package
check/cross enumeration per D-07), `packages:audit` (drift detection per D-11), and
`packages:validate` (composed into root `task validate` in P8 -- a thin wrapper for verify).

Purpose: Implements PKGS-02 + PKGS-03 (taskfile reads manifest + uses `brew bundle check`
status), PKGS-05 (servers' core-only bundles install cleanly), VRFY-01 (formula bin check),
VRFY-02 (cask app check), VRFY-03 (audit). VRFY-04 (verify in install pipeline) is owned by
Plan 05's root Taskfile.yml edit.

Output: One new taskfile `taskfiles/packages.yml`. Empty out `taskfiles/brew-stub.yml`
(replace body with `tasks: {}` + DEPRECATED header comment; file remains on disk so the
Phase-2 root include keeps parsing until Plan 05 removes the include line). No root
Taskfile.yml changes (Plan 05); no manifest changes (Plan 02); no bundle changes (Plan 01);
no composer changes (Plan 03).
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packages-layer-brewfile-composition-verification/05-CONTEXT.md
@.planning/phases/05-packages-layer-brewfile-composition-verification/05-PATTERNS.md
@.planning/phases/01-manifest-engine-repository-skeleton/01-CONTEXT.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-CONTEXT.md
@CLAUDE.md
@.claude/CLAUDE.md

@taskfiles/identity.yml
@taskfiles/helpers.yml
@install/messages.zsh
@install/compose-brewfile.zsh
@taskfiles/brew-stub.yml
@packages/core.rb
@taskfiles/lint.yml

<interfaces>
Task surface exposed by packages.yml:

packages:install     -- composes Brewfile + brew bundle install. Two-condition status (D-09).
                       Single task (NOT a pure aggregator): status block carries D-09;
                       cmds block carries compose + brew-bundle-install.
packages:compose     -- runs install/compose-brewfile.zsh only. Useful for inspection.
                       Status: test -f {{.COMPOSED_BREWFILE}}.
packages:verify      -- enumerate-all per-package check/cross. Aggregator (no status).
                       VRFY-01 (formula bins) + VRFY-02 (cask + mas apps).
packages:audit       -- drift detection. Aggregator (no status). VRFY-03.
                       Parses {{.CLI_ARGS}} for --strict; exit 1 when set + drift found.
packages:validate    -- thin wrapper delegating to packages:verify (P8 composition slot).

Vars block contract (identity.yml lines 54-95 pattern):
  RESOLVED_JSON_PATH:  '{{.XDG_STATE_HOME}}/dotfiles/resolved.json'
  COMPOSED_BREWFILE:   '{{.XDG_CACHE_HOME}}/dotfiles/Brewfile'
  MANIFEST_JSON:       sh: stderr-warn-or-cat   (identity.yml pattern)
  MANIFEST:            ref: 'fromJson .MANIFEST_JSON'
  DOTFILES_MESSAGES:   |
                       source '{{.DOTFILEDIR}}/install/messages.zsh'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author taskfiles/packages.yml with install/compose/verify/audit/validate tasks</name>
  <files>taskfiles/packages.yml</files>
  <read_first>
    - taskfiles/identity.yml (lines 1-200 -- strongest analog: file-header conventions block + includes + vars + preconditions + status; lines 380-418 for check/cross per-validation output pattern)
    - taskfiles/helpers.yml (the `_:check-command` / `_:check-file` helpers; note: `test -d` for `.app` directories is NOT in helpers -- use inline `test -d`)
    - taskfiles/brew-stub.yml (the file being replaced)
    - taskfiles/lint.yml (LINT-02 / LINT-03a / LINT-04 rules this file must satisfy; the test-fixtures section illustrates `# lint-allow:` marker placement)
    - install/compose-brewfile.zsh (Plan 03 deliverable -- this task invokes it)
    - install/messages.zsh (check/cross/info/warn/error/success API; sourced via `{{.DOTFILES_MESSAGES}}`)
    - packages/core.rb (the bundle this task's verify parses for `# verify:` overrides)
    - .planning/phases/05-packages-layer-brewfile-composition-verification/05-CONTEXT.md (D-07 enumerate-all; D-09 two-condition install status; D-10 hard-fail policy applied at install gate; D-11 audit with --strict; CONTEXT specifics lines 417-422 -- the five task names)
    - .planning/phases/05-packages-layer-brewfile-composition-verification/05-PATTERNS.md (lines 167-353 -- packages.yml pattern assignment with code excerpts)
  </read_first>
  <action>
    Create `taskfiles/packages.yml` mirroring `taskfiles/identity.yml`'s shape verbatim where applicable. The five tasks (with exact bodies described below):

    **File header block** (identity.yml lines 1-44 pattern, packages-specific copy):
    Top-of-file comment block with: title (`taskfiles/packages.yml -- Phase 5 manifest-driven package layer`); Purpose section (reads `packages.brew.bundles` + `packages.brew.extra_packages.{formulae,casks,mas}` from resolved.json; composes per-machine Brewfile to `$XDG_CACHE_HOME/dotfiles/Brewfile`; runs `brew bundle install`; verifies post-install bins + apps); Dependencies (`:manifest:resolve` deps; `taskfiles/helpers.yml`; `install/messages.zsh`; `install/compose-brewfile.zsh` from Plan 03); Status-block convention reminder (`{{.X}}` template vars ONLY; aggregators use `# lint-allow: cmds-without-status` marker).

    **Includes block:**
    `includes: { _: ./helpers.yml }`

    **Vars block** (identity.yml lines 54-95 pattern verbatim with packages-specific names):
    - `HOME`, `XDG_CONFIG_HOME`, `XDG_STATE_HOME`, `XDG_CACHE_HOME` (this is the NEW dimension P5 introduces -- XDG_CACHE_HOME wasn't forwarded in identity.yml; declare it via `sh: echo "${XDG_CACHE_HOME:-$HOME/.cache}"`)
    - `DOTFILEDIR` via `sh: dirname "{{.TASKFILE_DIR}}"`
    - `RESOLVED_JSON_PATH: '{{.XDG_STATE_HOME}}/dotfiles/resolved.json'`
    - `MANIFEST_JSON` + `MANIFEST` (identity.yml lines 72-85 verbatim)
    - `DOTFILES_MESSAGES` (identity.yml line 87 verbatim)
    - NEW: `COMPOSED_BREWFILE: '{{.XDG_CACHE_HOME}}/dotfiles/Brewfile'`

    **TASK A: `install`** (single task with two-condition status per D-09)
    - No `# lint-allow:` marker -- this task DOES have a status block, so LINT-03a passes.
    - `desc: "Install packages: compose per-machine Brewfile then run brew bundle install"`
    - `deps: [":manifest:resolve"]` (CF-08).
    - `preconditions:` (identity.yml lines 127-138 pattern): assert `test -s '{{.RESOLVED_JSON_PATH}}'` with `msg: |` carrying the actionable hint `run: task setup -- <machine-name>`; assert `command -v brew >/dev/null 2>&1` with `msg: "brew not on PATH -- run bootstrap.zsh first"`.
    - `status:` (D-09 -- two unconditional checks):
      1. `test -f "{{.COMPOSED_BREWFILE}}"`
      2. `brew bundle check --file="{{.COMPOSED_BREWFILE}}" --no-upgrade`
    - `cmds:`
      1. Compose step -- a single shell command invoking the composer with explicit env forwarding: `DOTFILEDIR="{{.DOTFILEDIR}}" XDG_CACHE_HOME="{{.XDG_CACHE_HOME}}" XDG_STATE_HOME="{{.XDG_STATE_HOME}}" zsh "{{.DOTFILEDIR}}/install/compose-brewfile.zsh"`
      2. Brew bundle install -- a shell block sourced via `{{.DOTFILES_MESSAGES}}` so it can emit info/success lines: `info "running brew bundle install --file={{.COMPOSED_BREWFILE}}"; brew bundle install --file="{{.COMPOSED_BREWFILE}}"; success "brew bundle install complete"`. The `set -e` shell behavior is honored by go-task per Taskfile.yml `set: [errexit, pipefail]`; a brew bundle install failure propagates.
    - Implements: PKGS-02 (compose-and-install path), PKGS-03 (brew bundle check status), D-09 (two-condition).

    **TASK B: `compose`** (inspection helper)
    - No `# lint-allow:` marker -- this task DOES have a status block.
    - `desc: "Compose per-machine Brewfile from manifest -> $XDG_CACHE_HOME/dotfiles/Brewfile"`
    - `deps: [":manifest:resolve"]`
    - `preconditions:` (resolved.json present, same as install -- pure copy of TASK A's resolved-json precondition).
    - `status:` `test -f "{{.COMPOSED_BREWFILE}}"` (single check; force regen via `task --force packages:compose`).
    - `cmds:` invoke `install/compose-brewfile.zsh` with the same env forward as TASK A. End with `{{.DOTFILES_MESSAGES}}; check "composed Brewfile -> {{.COMPOSED_BREWFILE}}"`.
    - Implements: PKGS-02 standalone path.

    **TASK C: `verify`** (D-07 enumerate-all)
    - `# lint-allow: cmds-without-status` marker on the line immediately above the task key.
    - `desc: "Verify declared packages: every formula bin on PATH; every cask/mas .app in /Applications"`
    - `deps: [":manifest:resolve"]`
    - `preconditions:`
      - `test -s '{{.RESOLVED_JSON_PATH}}'` with `task setup --` hint.
      - `test -f '{{.COMPOSED_BREWFILE}}'` with `msg: |` carrying actionable hint `run: task packages:compose first`.
    - `cmds:` ONE big inline shell block sourced via `{{.DOTFILES_MESSAGES}}` containing:
      1. `failures=0` initialization.
      2. Formula loop -- read `{{.COMPOSED_BREWFILE}}` line by line; for every line matching `^[[:space:]]*brew[[:space:]]+'([^']+)'` (use a shell while-read loop OR sed/grep extract): extract the formula name as `$f`. Check if the same line carries `# verify: <bin>` -- if so, set `$bin` to that override; else `$bin=$f`. Run `if command -v "$bin" >/dev/null 2>&1; then check "formula $f -> $bin"; else cross "formula $f -> $bin NOT FOUND"; failures=$((failures+1)); fi`. (Use `ggrep -oE "# verify: \\S+" | awk '{print $3}'` to extract the override.)
      3. Cask loop -- read `{{.COMPOSED_BREWFILE}}` for every line matching `^[[:space:]]*cask[[:space:]]+'([^']+)'[[:space:]]+#[[:space:]]+verify:[[:space:]]+(.+)$` (cask `# verify:` is MANDATORY per D-04 -- if a cask line lacks the comment, emit `cross "cask line missing # verify: comment"`, increment failures, continue to the next line). For each `(cask, verify)` pair, run `if test -d "/Applications/${verify}.app"; then check "cask $cask -> /Applications/${verify}.app"; else cross "cask $cask -> /Applications/${verify}.app NOT FOUND"; failures=$((failures+1)); fi`.
      4. MAS loop -- read `{{.COMPOSED_BREWFILE}}` for every line matching `^[[:space:]]*mas[[:space:]]+'([^']+)',[[:space:]]+id:[[:space:]]+([0-9]+)$`. For each `(name, id)` pair, run the same `test -d "/Applications/${name}.app"` check per D-06.
      5. Summary -- if `failures > 0`: `cross "$failures package(s) failed verify"` and `exit "$failures"` (D-07 contract: enumerate-all, then exit non-zero with the failure count); else `success "all declared packages verified"; exit 0`.
    - Implements: VRFY-01 (formula bin check), VRFY-02 (cask app check), D-04 (mandatory cask verify enforced at runtime; failsafe for if LINT-09 is deferred), D-06 (MAS = same check as cask), D-07 (enumerate-all + count-failures).

    **TASK D: `audit`** (D-11)
    - `# lint-allow: cmds-without-status` marker.
    - `desc: "Audit installed brew formulae/casks/mas vs declared. Non-blocking; --strict exits non-zero."`
    - `deps: [":manifest:resolve"]`
    - `preconditions:` (resolved.json present; composed Brewfile present; `command -v brew`; `command -v mas` -- if `mas` not on PATH, the precondition WARNS and the mas section is skipped, but `mas` should always be installed because `core.rb` declares it).
    - `cmds:` one inline shell block sourced via `{{.DOTFILES_MESSAGES}}`:
      1. Parse `{{.CLI_ARGS}}` for `--strict`: `strict=0; for arg in {{.CLI_ARGS}}; do [[ "$arg" == "--strict" ]] && strict=1; done`. (`task packages:audit -- --strict` is the caller form.)
      2. Declared formulae: extract every `brew '<name>'` line from `{{.COMPOSED_BREWFILE}}` -- formula name only (strip `# verify:` suffix); sort uniq into `$declared_formulae`.
      3. Declared casks: extract every `cask '<name>'` line; sort uniq.
      4. Declared MAS ids: extract every `mas '<name>', id: <id>` line; grab the `<id>`; sort uniq.
      5. Installed leaves: `brew leaves | sort -u` (top-level formulae only -- D-11 matches "I `brew install`'d something manually" failure mode).
      6. Installed casks: `brew list --cask 2>/dev/null | sort -u`.
      7. Installed MAS ids: `mas list 2>/dev/null | awk '{print $1}' | sort -u` (column 1 is the id; columns 2+ are the name and version).
      8. Diff: `total_drift=0`. For each category, `comm -23 <(installed) <(declared)` -> any output is drift. For each drift item, `warn "drift: <category> <name>"`; increment `total_drift`.
      9. Final: if `strict=1 && total_drift > 0`: `cross "$total_drift drift item(s) detected (--strict)"; exit 1`. Else if `total_drift > 0`: `warn "$total_drift drift item(s) detected (non-blocking; --strict to fail)"; exit 0`. Else: `success "no drift detected"; exit 0`.
    - Implements: VRFY-03, D-11.

    **TASK E: `validate`** (P8 wrapper)
    - `# lint-allow: cmds-without-status` marker (single delegation, pure aggregator).
    - `desc: "Validate packages layer (wraps packages:verify; composed into root task validate in P8)"`
    - `cmds:` `- task: verify`
    - Implements: P8 (CUTV-01) composition slot.

    **Conventions check (verify before saving):**
    - Every status block uses ONLY `{{.X}}` template vars -- LINT-02. NO `$X` shell vars inside `status:` blocks (would trigger the v1 `macos:shell:145` bug class). LINT-02 check: `yq '.tasks[] | select(.status) | .status' taskfiles/packages.yml | grep -E '\\$[A-Za-z_]'` must return nothing.
    - Every aggregator-style task (verify, audit, validate) carries the `# lint-allow: cmds-without-status` marker on the immediately-preceding line.
    - Every task that reads resolved.json declares `deps: [":manifest:resolve"]` -- CF-08.
    - No emojis (messages.zsh's ✓/✗ are functional UX and out of scope per PATTERNS.md lines 842-846).
    - No AI attribution anywhere.

    Implements: PKGS-02, PKGS-03, PKGS-05, VRFY-01, VRFY-02, VRFY-03, D-04, D-06, D-07, D-09, D-11.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles &amp;&amp; test -f taskfiles/packages.yml &amp;&amp; yq -e '.tasks.install' taskfiles/packages.yml &amp;&amp; yq -e '.tasks.compose' taskfiles/packages.yml &amp;&amp; yq -e '.tasks.verify' taskfiles/packages.yml &amp;&amp; yq -e '.tasks.audit' taskfiles/packages.yml &amp;&amp; yq -e '.tasks.validate' taskfiles/packages.yml &amp;&amp; yq '.tasks.install.status[]' taskfiles/packages.yml | grep -q "brew bundle check" &amp;&amp; yq '.tasks.install.status[]' taskfiles/packages.yml | grep -q "COMPOSED_BREWFILE" &amp;&amp; yq '.tasks.install.deps[]' taskfiles/packages.yml | grep -q "manifest:resolve" &amp;&amp; task -t taskfiles/packages.yml --list &amp;&amp; task lint</automated>
  </verify>
  <acceptance_criteria>
    - File `taskfiles/packages.yml` exists with the five task keys (install, compose, verify, audit, validate)
    - `yq '.tasks | keys' taskfiles/packages.yml` returns exactly those five (in any order)
    - `yq '.tasks.install.status' taskfiles/packages.yml` carries both conditions: `test -f` line referencing `{{.COMPOSED_BREWFILE}}` AND `brew bundle check --file=...` line
    - `yq '.tasks.install.deps[]' taskfiles/packages.yml | grep -q "manifest:resolve"` returns 0 (CF-08)
    - `yq '.tasks.compose.deps[]' taskfiles/packages.yml | grep -q "manifest:resolve"` returns 0
    - `yq '.tasks.verify.deps[]' taskfiles/packages.yml | grep -q "manifest:resolve"` returns 0
    - `yq '.tasks.audit.deps[]' taskfiles/packages.yml | grep -q "manifest:resolve"` returns 0
    - No `$X` shell vars inside any `status:` block: `yq '.tasks[] | select(.status) | .status' taskfiles/packages.yml | ggrep -nE '\\$[A-Za-z_][A-Za-z0-9_]*' | ggrep -vE '\\$\\(' | ggrep -vE '\\{\\{'` returns empty (LINT-02 contract)
    - The three aggregator tasks (verify, audit, validate) each carry `# lint-allow: cmds-without-status` on the line immediately preceding their `<name>:` key
    - `task -t taskfiles/packages.yml --list` exits 0 (YAML parses; tasks discoverable)
    - `task lint` exits 0 (no new LINT-02, LINT-03a, LINT-03b, LINT-04, LINT-07 violations introduced)
    - `packages:compose` task body invokes `install/compose-brewfile.zsh` (string check: `grep -q "compose-brewfile.zsh" taskfiles/packages.yml`)
    - `packages:install` task body forwards `XDG_CACHE_HOME` env to the composer (string check: `grep -q "XDG_CACHE_HOME=" taskfiles/packages.yml`)
    - `packages:audit` body references `brew leaves`, `brew list --cask`, `mas list`: three grep -q checks all pass
    - `packages:verify` body checks `test -d /Applications/`: `grep -q "test -d \"/Applications/" taskfiles/packages.yml`
    - `packages:verify` body checks `command -v`: `grep -q "command -v" taskfiles/packages.yml`
    - File parses as valid YAML: `yq '.' taskfiles/packages.yml` exits 0
    - No emojis: `LC_ALL=C grep -P '[^\\x00-\\x7f]' taskfiles/packages.yml` exits 1
    - No AI attribution: `! grep -iE "co-authored-by|generated by ai|written by" taskfiles/packages.yml`
    - End-to-end smoke (when run on a converged machine): `task setup -- personal-laptop && task packages:compose && task packages:install` -- the second `task packages:install` invocation is a no-op in under 2 seconds (status hits both conditions; PKGS-03 idempotency); `task packages:verify` exits 0 if every declared package is actually installed; `task packages:audit` lists installed-but-undeclared items if any.
  </acceptance_criteria>
  <done>
    `taskfiles/packages.yml` exists with the five required tasks; install carries the D-09 two-condition status; verify enumerates all per D-07; audit handles --strict per D-11; every reading task declares `deps: [":manifest:resolve"]`; aggregators carry `# lint-allow: cmds-without-status`; LINT-02 contract honored throughout; lint passes; no emojis; no AI attribution.
  </done>
</task>

<task type="auto">
  <name>Task 2: Retire taskfiles/brew-stub.yml (replace body with empty tasks map + DEPRECATED header)</name>
  <files>taskfiles/brew-stub.yml</files>
  <read_first>
    - taskfiles/brew-stub.yml (current Phase 2 stub content)
    - Taskfile.yml (root -- the include line `brew: ./taskfiles/brew-stub.yml`; this include is rewritten by Plan 05, NOT by this task)
    - .planning/phases/05-packages-layer-brewfile-composition-verification/05-CONTEXT.md (CF-11 -- parallel rewrite invariant; CF-11 applies to v1 install/Brewfile*.rb, NOT to v2 stubs. P5 retires its own P2-created stub.)
  </read_first>
  <action>
    Replace the entire body of `taskfiles/brew-stub.yml` with a deprecated empty-tasks stub. Do NOT delete the file in this plan -- Plan 05 owns the root Taskfile.yml include removal, and deleting the file here (Wave 3) before Plan 05 (Wave 4) lands would break `task --list` because the root include still references the path.

    The new file contents are EXACTLY:

    ```
    # taskfiles/brew-stub.yml -- DEPRECATED, superseded by taskfiles/packages.yml in Phase 5.
    #
    # This file is retained only so the legacy `brew: ./taskfiles/brew-stub.yml` include in
    # the root Taskfile.yml continues to parse until Plan 05 (Wave 4) removes the include line
    # and re-routes through `packages: ./taskfiles/packages.yml`. Once Plan 05 lands, the file
    # is dead weight and may be deleted as part of Phase 8 cleanup.
    #
    # Do NOT add tasks here. All packages-layer functionality lives in taskfiles/packages.yml.
    version: '3'
    tasks: {}
    ```

    Concretely:
    - Header is exactly five comment lines plus one blank-comment line as shown.
    - `version: '3'` matches the rest of the repo.
    - `tasks: {}` is an EMPTY MAP (not a list, not omitted). The empty map ensures `task --list` succeeds and `task brew:install` exits with `task: Task "brew:install" does not exist` rather than silently no-op'ing against a stale stub.

    Do NOT delete the file. Do NOT modify the root `Taskfile.yml` (that is Plan 05's responsibility). Do NOT add any tasks under `tasks:`.

    Rationale for this single committed path: leaving the file on disk with an empty tasks map keeps `task --list` green through Wave 3 -> Wave 4 transition; removing the file in this wave would break the build because the root include line is still present until Plan 05 runs.

    Implements: handoff from Phase 2 stub to Phase 5 real implementation; preserves build-parseability through Plan 05's wave boundary.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles &amp;&amp; test -f taskfiles/brew-stub.yml &amp;&amp; head -1 taskfiles/brew-stub.yml | grep -q "DEPRECATED" &amp;&amp; grep -q "superseded by taskfiles/packages.yml" taskfiles/brew-stub.yml &amp;&amp; yq -e '.tasks' taskfiles/brew-stub.yml &gt;/dev/null &amp;&amp; [ "$(yq '.tasks | length' taskfiles/brew-stub.yml)" = "0" ] &amp;&amp; yq -e '.version' taskfiles/brew-stub.yml | grep -q "3" &amp;&amp; task --list &gt;/dev/null 2&gt;&amp;1</automated>
  </verify>
  <acceptance_criteria>
    - `taskfiles/brew-stub.yml` STILL EXISTS on disk (`test -f taskfiles/brew-stub.yml` returns 0). This task does NOT delete the file.
    - First line of the file matches `# taskfiles/brew-stub.yml -- DEPRECATED, superseded by taskfiles/packages.yml in Phase 5.` (the canonical header comment)
    - File body contains the literal string `superseded by taskfiles/packages.yml` (grep -q passes)
    - File has `version: '3'` declaration: `yq -e '.version' taskfiles/brew-stub.yml` returns 0 AND the value is `3` (string-form)
    - File has an EMPTY tasks map: `yq '.tasks | length' taskfiles/brew-stub.yml` returns `0` AND `yq '.tasks | type' taskfiles/brew-stub.yml` returns `!!map`
    - File parses as valid YAML: `yq '.' taskfiles/brew-stub.yml` exits 0
    - `task --list` exits 0 (YAML still parses; no broken includes; the empty tasks map is valid)
    - `task brew:install` errors with `task: Task "brew:install" does not exist` -- the stub no longer carries the Phase-2 install body, so any accidental invocation fails loudly rather than silently no-op'ing
    - No emojis: `LC_ALL=C grep -P '[^\x00-\x7f]' taskfiles/brew-stub.yml` exits 1
    - No AI attribution: `! grep -iE "co-authored-by|generated by ai|written by" taskfiles/brew-stub.yml`
    - Plan 05 owns the follow-on edit to remove the `brew: ./taskfiles/brew-stub.yml` include line from the root Taskfile.yml -- this is documented in Plan 05's must-haves; this task does NOT modify the root Taskfile.yml
  </acceptance_criteria>
  <done>
    `taskfiles/brew-stub.yml` remains on disk with the DEPRECATED header comment, `version: '3'`, and `tasks: {}` (empty map). The Phase-2 `install` stub task is gone; the file is a parseable placeholder until Plan 05 removes the root include line. `task --list` still succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `brew bundle install` execution | Runs install scripts from formula/cask files; consumes the composed Brewfile |
| `verify` shell-out | Reads cask/mas `verify` strings from the composed Brewfile and passes them to `test -d "/Applications/${verify}.app"` |
| `audit` shell-out | Reads installed-package lists from `brew leaves`, `brew list --cask`, `mas list` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | T (Tampering) | composed Brewfile content with malicious `brew '<name>'` line | accept | Composed Brewfile is built from bundle files (Plan 01, repo-reviewed) + manifest TOML (Plan 02, repo-reviewed). Standard supply-chain trust boundary; documented in `docs/SECURITY.md`. The composer concatenates verbatim with no eval; the bundle files and TOML go through git review. |
| T-05-17 | T (Tampering) | shell-injection via `verify` field with embedded `$(...)` or `;` | mitigate | The verify field travels: TOML -> resolved.json -> composed Brewfile (as a `# verify: ...` COMMENT line, never evaluated by `brew bundle`) -> read by verify task via `ggrep -oE`/awk into a shell variable, passed to `test -d "/Applications/${verify}.app"` with double-quotes. Shell metacharacters inside `${verify}` are treated as literal characters within the quoted string. `test -d` does NOT evaluate its argument; no command substitution risk. Verify the parser regex anchors at end-of-line so a trailing `;rm -rf` cannot escape. |
| T-05-18 | I (Information disclosure) | `brew list --cask` output | accept | Lists installed casks; readable by any process that can run `brew`. No new disclosure. |
| T-05-19 | E (Elevation) | `brew bundle install` triggers `sudo`-requiring cask installs | accept | macOS-native; some casks (Docker Desktop, etc.) require user privilege escalation. v1 accepts the same trust boundary as v2; no change in behavior here. |
| T-05-20 | D (DoS) | `brew bundle install` hangs on a 3rd-party cask | mitigate | Out of scope to handle in v1; brew bundle's own timeout logic owns this. The task is foreground; user can Ctrl-C; `task install` is idempotent so re-running is safe. |
| T-05-21 | T (Tampering) | `$XDG_CACHE_HOME/dotfiles/Brewfile` modified between compose and install steps | mitigate | The two-condition status block re-runs `brew bundle check` on every install invocation; if the cache file is tampered with after compose, the check exits non-zero and the install body re-runs the composer (overwriting the tampered file). This is the natural behavior of D-09's status contract -- defense in depth for free. |
| T-05-22 | I (Information disclosure) | `mas list` requires Apple ID auth state on the system | accept | Apple ID auth is a user-system concern, not a packages.yml threat. The audit task does NOT trigger Apple ID interaction; `mas list` only enumerates ALREADY-installed MAS apps. |

Block on HIGH; this plan has none. T-05-17 is the load-bearing mitigation -- enforced via the `test -d "/Applications/${verify}.app"` quoted form (no eval).
</threat_model>

<verification>
After both tasks complete:

1. `task --list` exits 0 -- the root Taskfile.yml still parses (the brew-stub include hasn't been removed yet -- Plan 05 owns that edit -- so brew-stub.yml must remain on disk with an empty tasks map, which Task 2 guarantees).
2. `task -t taskfiles/packages.yml --list` exits 0 and shows all five tasks (install, compose, verify, audit, validate).
3. `task lint` exits 0 -- no new LINT-02 / LINT-03a / LINT-03b / LINT-04 / LINT-07 violations.
4. LINT-02 spot-check on packages.yml: `yq '.tasks[] | select(.status) | .status' taskfiles/packages.yml | ggrep -nE '\$[A-Za-z_][A-Za-z0-9_]*' | ggrep -vE '\$\(' | ggrep -vE '\{\{'` returns empty.
5. Smoke test (on a converged machine after Plan 01-03 land):
   ```
   task setup -- personal-laptop
   task -t taskfiles/manifest.yml manifest:resolve
   task -t taskfiles/packages.yml packages:compose
   test -f "$XDG_CACHE_HOME/dotfiles/Brewfile"
   time task -t taskfiles/packages.yml packages:install   # second call should hit status and no-op
   task -t taskfiles/packages.yml packages:verify          # if all packages are installed, exits 0
   task -t taskfiles/packages.yml packages:audit           # informational; exits 0 even with drift
   task -t taskfiles/packages.yml packages:audit -- --strict   # exits 1 if drift exists
   ```
6. Negative smoke: temporarily rename a declared cask's installed `.app` (e.g., `mv /Applications/Slack.app /Applications/Slack.app.tmp`) and run `task packages:verify` -- exits non-zero and prints `cross "cask slack -> /Applications/Slack.app NOT FOUND"`. Restore the rename.
</verification>

<success_criteria>
- `taskfiles/packages.yml` exists with five tasks (install, compose, verify, audit, validate)
- `packages:install` has the two-condition status block from D-09 (test -f + brew bundle check) using `{{.X}}` template vars only
- `packages:install` cmds invoke `install/compose-brewfile.zsh` then `brew bundle install`
- `packages:verify` enumerates every formula (with `# verify:` override resolution), every cask (with mandatory `# verify:` parsing), and every MAS app; prints check/cross; exits non-zero with the failure count
- `packages:audit` reports drift via `brew leaves`, `brew list --cask`, `mas list`; non-blocking by default; `--strict` exits non-zero (D-11)
- Every task that reads resolved.json declares `deps: [":manifest:resolve"]`
- LINT-02 / LINT-03a / LINT-04 / LINT-07 all pass on the new file
- `taskfiles/brew-stub.yml` remains on disk with body = DEPRECATED header comment + `version: '3'` + `tasks: {}` (empty map); no `install` stub task body present
- No emojis; no AI attribution
</success_criteria>

<output>
After completion, create `.planning/phases/05-packages-layer-brewfile-composition-verification/05-04-SUMMARY.md` recording:
- Two files touched (taskfiles/packages.yml new; taskfiles/brew-stub.yml body replaced with `tasks: {}` + DEPRECATED header)
- The five task signatures (desc + deps + status/cmds shape per task)
- LINT-02 / LINT-03a self-test result
- Verify-loop implementation choice (inline shell vs `_:check-command` loop)
- Audit dedupe strategy for each category (formula = formula-name match; cask = cask-name match; mas = id match)
- Note on brew-stub retirement path: file kept on disk with empty tasks map to preserve Wave 3 -> Wave 4 build parseability; Plan 05 removes the root include line; Phase 8 cleanup may delete the file
- Reference to PKGS-02, PKGS-03, PKGS-05, VRFY-01, VRFY-02, VRFY-03, D-04, D-06, D-07, D-09, D-11
</output>
</content>
</invoke>
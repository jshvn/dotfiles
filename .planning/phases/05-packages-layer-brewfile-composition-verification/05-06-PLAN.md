---
phase: 05-packages-layer-brewfile-composition-verification
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - docs/MANIFEST.md
autonomous: true
requirements: [PKGS-01, PKGS-04]
requirements_addressed: [PKGS-01, PKGS-04]
tags: [docs, requirements, roadmap, manifest-schema]

must_haves:
  truths:
    - "REQUIREMENTS.md PKGS-01 says packages/<purpose>.rb (flat), NOT packages/brew/<purpose>.rb, and reflects the minimal-bundles philosophy (D-01, D-02)"
    - "REQUIREMENTS.md PKGS-04 documents the typed sub-table extras schema (formulae/casks/mas with {name, verify} and {id, name} objects per D-03)"
    - "ROADMAP.md Phase 5 success criterion #3 says bundles named by purpose (core.rb, gui.rb, +any future) -- NOT the old five-bundle enumeration; preserves the no Brewfile-<profile>.rb prohibition (D-02)"
    - "PROJECT.md Active package lines updated to packages/<purpose>.rb (flat) and the 2-bundle minimum + typed-bucket extras model (D-01, D-02, D-03)"
    - "docs/MANIFEST.md schema reference replaces the flat extra_packages row with three rows for the typed sub-table; worked example for extra_packages updated to per-bucket concat+dedupe"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated PKGS-01 + PKGS-04 text"
      contains: ["packages/<purpose>.rb", "typed sub-table", "formulae", "casks", "mas"]
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 5 success criterion #3"
      contains: ["core.rb", "gui.rb", "Brewfile-<profile>.rb", "typed sub-table"]
    - path: ".planning/PROJECT.md"
      provides: "Updated package lines reflecting flat layout + minimal bundles"
      contains: ["packages/<purpose>.rb"]
    - path: "docs/MANIFEST.md"
      provides: "Updated schema reference + worked example for typed-bucket extras"
      contains: ["packages.brew.extra_packages.formulae", "packages.brew.extra_packages.casks", "packages.brew.extra_packages.mas"]
  key_links:
    - from: "REQUIREMENTS.md PKGS-01"
      to: "ROADMAP.md Phase 5 success criterion #3"
      via: "both describe the bundle layout invariant"
      pattern: "agreement is the value -- both must say the same thing post-edit"
    - from: "REQUIREMENTS.md PKGS-04"
      to: "docs/MANIFEST.md schema reference"
      via: "both describe extras schema"
      pattern: "typed-bucket shape consistent across canonical sources"
---

<objective>
Correct the canonical project documentation so it matches what Phase 5 actually builds. Four
files have stale text from before the typed-bucket + minimal-bundles decisions were made:

- REQUIREMENTS.md PKGS-01 says packages/brew/<purpose>.rb (wrong; D-01); PKGS-04 says
  "additive, concatenates with bundle contents" (incomplete; needs D-03 typed sub-table).
- ROADMAP.md Phase 5 success criterion #3 enumerates exactly five bundles (core, gui, dev,
  ops, personal); D-02 amends to "core, gui, and any future purpose-named additions."
- PROJECT.md Active package lines reference packages/brew/<purpose>.rb and the 5-bundle list.
- docs/MANIFEST.md schema reference still shows the legacy flat extra_packages row; needs
  the typed sub-table.

Purpose: documentation half of PKGS-01 + PKGS-04. The code-side half is in Plans 01 (bundle
files at flat paths) and 02 (typed-bucket TOMLs). This plan makes canonical docs match code.

Output: Four updated documentation files. No code, taskfile, or manifest TOML changes.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packages-layer-brewfile-composition-verification/05-CONTEXT.md
@.planning/phases/05-packages-layer-brewfile-composition-verification/05-PATTERNS.md
@CLAUDE.md

@.planning/REQUIREMENTS.md
@docs/MANIFEST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .planning/REQUIREMENTS.md PKGS-01 and PKGS-04 text</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 95 PKGS-01; line 98 PKGS-04)
    - .planning/phases/05-packages-layer-brewfile-composition-verification/05-CONTEXT.md (D-01 flat layout; D-02 minimal bundles; D-03 typed-bucket schema; D-04 cask verify mandatory; D-06 MAS shape)
    - .planning/phases/05-packages-layer-brewfile-composition-verification/05-PATTERNS.md (lines 707-729 -- REQUIREMENTS.md edit text)
  </read_first>
  <action>
    Edit .planning/REQUIREMENTS.md with two surgical replacements.

    Edit 1 -- PKGS-01 line (currently line 95). Find the bullet starting `- [ ] **PKGS-01**:` and replace the bullet content (after the colon-space prefix) with the text from PATTERNS.md lines 717-718 (the new PKGS-01 text):
    "Per-purpose Brewfile bundles in packages/<purpose>.rb (flat -- not packages/brew/). v1 ships core and gui; bundles are an as-needed grouping, not a fixed set (per-machine extras carry the bulk)."

    Wrap each path or identifier in backticks per the surrounding REQUIREMENTS.md convention. Keep the `- [ ] **PKGS-01**:` prefix unchanged.

    Edit 2 -- PKGS-04 line (currently line 98). Find the bullet starting `- [ ] **PKGS-04**:` and replace the content with the text from PATTERNS.md lines 727-729:
    "Manifest can declare per-machine extra_packages as a typed sub-table (formulae, casks, mas); each sub-array concat+dedupes with defaults at resolve time. Cask and MAS entries are typed objects ({name, verify} for casks; {id, name} for MAS)."

    Wrap identifiers in backticks. Keep the prefix unchanged.

    Do NOT touch PKGS-02, PKGS-03, PKGS-05, VRFY-01..VRFY-04, the Traceability table, or the Coverage section.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && grep -E "^- \[ \] \*\*PKGS-01\*\*:" .planning/REQUIREMENTS.md | grep -q "packages/<purpose>.rb" && grep -E "^- \[ \] \*\*PKGS-04\*\*:" .planning/REQUIREMENTS.md | grep -q "typed sub-table" && grep -E "^- \[ \] \*\*PKGS-04\*\*:" .planning/REQUIREMENTS.md | grep -q "formulae" && grep -E "^- \[ \] \*\*PKGS-04\*\*:" .planning/REQUIREMENTS.md | grep -q "casks" && grep -E "^- \[ \] \*\*PKGS-04\*\*:" .planning/REQUIREMENTS.md | grep -q "mas"</automated>
  </verify>
  <acceptance_criteria>
    - PKGS-01 line contains the literal string `packages/<purpose>.rb`
    - PKGS-01 line does NOT contain `packages/brew/` (grep -v style: `grep PKGS-01 .planning/REQUIREMENTS.md | grep packages/brew/` exits 1)
    - PKGS-01 line does NOT enumerate the old 5-bundle set as fixed bundles (`grep -E "^- \[ \] \*\*PKGS-01\*\*:.*dev.*ops.*personal" .planning/REQUIREMENTS.md` exits 1)
    - PKGS-04 line contains `typed sub-table`
    - PKGS-04 line contains all three sub-array names (`formulae`, `casks`, `mas`)
    - PKGS-04 line documents the cask object shape (contains `{name, verify}` or `name, verify` with surrounding context)
    - PKGS-04 line documents the MAS object shape (`{id, name}` or `id, name`)
    - PKGS-02, PKGS-03, PKGS-05, VRFY-01..04 lines unchanged (spot-check via grep -- each still exists with its v1 wording)
    - Traceability table at the bottom unchanged: PKGS-01..05 + VRFY-01..04 rows still present
    - File parses as markdown
    - No emojis added; no AI attribution
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md PKGS-01 corrected to flat path + minimal-bundles philosophy; PKGS-04 corrected to typed sub-table schema with object shapes; no other lines touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/ROADMAP.md Phase 5 success criterion #3</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 5 section line 104; success criterion #3 at line 111)
    - .planning/phases/05-packages-layer-brewfile-composition-verification/05-CONTEXT.md (D-02 minimal bundles narrative)
    - .planning/phases/05-packages-layer-brewfile-composition-verification/05-PATTERNS.md (lines 731-749 -- ROADMAP edit text)
  </read_first>
  <action>
    Edit .planning/ROADMAP.md. One surgical replacement: the third numbered success criterion under the Phase 5 section (currently line 111, starting `  3. Bundles are named by purpose...`). Replace with the PATTERNS.md lines 742-744 text:
    "Bundles are named by purpose (core.rb, gui.rb, and any future purpose-named additions) -- no Brewfile-<profile>.rb files anywhere; per-machine variation lives in extra_packages typed sub-table (formulae/casks/mas), not in bundle files; a Mac server machine can decline GUI bundles via manifest and its composed Brewfile contains no casks."

    Wrap identifiers in backticks per the surrounding ROADMAP.md convention. Preserve the `  3. ` numbering prefix and indentation.

    The `no Brewfile-<profile>.rb` prohibition stays -- D-02's narrative reaffirms v2 replaces the named-suffix filename pattern.

    Do NOT touch Phase 5 success criteria #1, #2, #4, #5, #6; do NOT touch the Plans line under Phase 5; do NOT touch any other phase section.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && grep -E "^  3\. Bundles are named by purpose" .planning/ROADMAP.md | grep -q "core.rb" && grep -E "^  3\. Bundles are named by purpose" .planning/ROADMAP.md | grep -q "gui.rb" && grep -E "^  3\. Bundles are named by purpose" .planning/ROADMAP.md | grep -q "Brewfile-" && grep -E "^  3\. Bundles are named by purpose" .planning/ROADMAP.md | grep -q "typed sub-table"</automated>
  </verify>
  <acceptance_criteria>
    - Phase 5 success criterion #3 contains both `core.rb` AND `gui.rb`
    - Phase 5 success criterion #3 contains the `no Brewfile-<profile>.rb` prohibition (D-02 narrative preserved)
    - Phase 5 success criterion #3 contains `typed sub-table` AND mentions `extra_packages`
    - The criterion does NOT enumerate `dev.rb`, `ops.rb`, `personal.rb` as fixed shipping bundles (regex check: `grep -E "^  3\..*dev\.rb.*ops\.rb.*personal\.rb" .planning/ROADMAP.md` exits 1)
    - Phase 5 success criteria #1, #2, #4, #5, #6 untouched (spot-check each via grep)
    - Other phase sections untouched
    - File parses as markdown
    - No emojis; no AI attribution
  </acceptance_criteria>
  <done>
    ROADMAP.md Phase 5 success criterion #3 corrected to the 2-bundle minimum + typed-bucket extras model; the no-Brewfile-prefix prohibition preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update .planning/PROJECT.md Active package lines</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (Active section Packages block at lines 67-71)
    - .planning/phases/05-packages-layer-brewfile-composition-verification/05-CONTEXT.md (D-01 flat; D-02 minimal bundles; D-03 typed-bucket)
    - .planning/phases/05-packages-layer-brewfile-composition-verification/05-PATTERNS.md (lines 751-759 -- PROJECT.md edit pattern)
  </read_first>
  <action>
    Edit .planning/PROJECT.md. One surgical replacement under the `**Packages:**` block in the `### Active` section (lines 67-71).

    Find the bullet currently reading "Per-purpose bundles in packages/brew/<purpose>.rb (core, gui, dev, ops, personal) -- named by role, not by profile" and replace with:
    "Per-purpose bundles in flat packages/<purpose>.rb (core, gui; +any future purpose-named additions). Per-machine variation lives in manifest extra_packages typed sub-table (formulae/casks/mas)."

    Wrap path + identifier tokens in backticks per surrounding PROJECT.md style. Keep the `- [ ]` checkbox prefix and the surrounding bullets unchanged.

    Do NOT touch the Brewfile-composition bullet (first under Packages) and do NOT touch the third bullet about extra_packages allowed without forking (still accurate post-D-03).

    Do NOT touch other Active sub-sections, Out of Scope, Context, Constraints, Key Decisions, or Evolution. The Validated section update is deferred to /gsd-transition after Phase 5 ships -- not this plan.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && grep "Per-purpose bundles" .planning/PROJECT.md | grep -q "packages/<purpose>.rb"</automated>
  </verify>
  <acceptance_criteria>
    - The PROJECT.md bullet containing "Per-purpose bundles" contains the literal `packages/<purpose>.rb`
    - The same bullet does NOT contain `packages/brew/`: `grep "Per-purpose bundles" .planning/PROJECT.md | grep packages/brew/` exits 1
    - The same bullet does NOT enumerate the old 5-bundle set as fixed: `grep "Per-purpose bundles" .planning/PROJECT.md | grep -E "dev.*ops.*personal"` exits 1
    - The same bullet mentions `extra_packages` and `typed sub-table` (or equivalent like "typed buckets")
    - Other Packages bullets unchanged: the first bullet (Brewfile composition via manifest bundles) and the third bullet (extra_packages allowed without forking) still present
    - Other Active sub-sections unchanged
    - Out of Scope, Context, Constraints, Key Decisions, Evolution sections unchanged
    - File parses as markdown
    - No emojis; no AI attribution
  </acceptance_criteria>
  <done>
    PROJECT.md Active section Packages bullet corrected to flat path + minimal bundles + typed-bucket extras language; other bullets and sections untouched.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update docs/MANIFEST.md schema reference + worked example for typed-bucket extras</name>
  <files>docs/MANIFEST.md</files>
  <read_first>
    - docs/MANIFEST.md (Required-fields table at line 86; Optional-fields table at line 96-100; Merge Semantics Rules table at line 116; Fixture 06 worked example at lines 291-313)
    - .planning/phases/05-packages-layer-brewfile-composition-verification/05-CONTEXT.md (D-03 typed-bucket; D-04 cask verify mandatory; D-06 MAS shape)
    - .planning/phases/05-packages-layer-brewfile-composition-verification/05-PATTERNS.md (lines 668-705 -- docs/MANIFEST.md edits at three call sites)
  </read_first>
  <action>
    Edit docs/MANIFEST.md. Three discrete edits.

    Edit 1 -- Optional-fields table (line 100). Replace the single legacy row:
    "| packages.brew.extra_packages | array of strings | Additive escape hatch; deduplicated union with defaults value |"
    with three rows describing the typed sub-table per PATTERNS.md lines 691-694:
    - packages.brew.extra_packages.formulae | array of strings or `{name, verify}` objects | Per-machine formula extras; concat+dedupe across defaults+machine
    - packages.brew.extra_packages.casks | array of `{name, verify}` objects | Per-machine cask extras; verify field MANDATORY per cask (D-04)
    - packages.brew.extra_packages.mas | array of `{id, name}` objects | Per-machine MAS app extras; `name` doubles as the `.app` verify name (D-06)

    Format consistent with the existing table style (markdown pipes, identifiers in backticks).

    Edit 2 -- Worked example block at lines 291-313 (Fixture 06 -- extra-packages-concat). Update this worked example to reflect the typed-bucket shape and per-sub-array merge semantics:
    - Defaults extras has `formulae = ["jq", "yq"]` (bare strings).
    - Machine extras has `formulae = [{name = "ripgrep", verify = "rg"}]` and `casks = [{name = "docker-desktop", verify = "Docker"}]`.
    - Resolved output: `formulae` is concat+dedupe of the two arrays (bare strings + objects coexist); `casks` is the machine's value (defaults had `casks = []`); `mas` is `[]` (both empty).
    - Add a short note: per-sub-array union semantics replace the legacy flat-array union; the resolver's Pass 2 (Plan 03 deliverable) carries backward-compat for the legacy flat-array fixture so existing Phase 1 fixtures continue to pass.

    Edit 3 -- Required-fields table (line 86-90). The existing `packages.brew.bundles` row stays unchanged. No edit needed there.

    Do NOT touch the Required-fields rows for meta.description, platform.os, features, identity.git, identity.ssh. Do NOT touch the `## Adding a New Machine` walkthrough. Do NOT touch the CLI Reference, State Files, Feature-Flag Reference, or Known Limitations sections.

    Implements: documentation-side correction for PKGS-04 (D-03 typed schema).
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && grep -q "packages.brew.extra_packages.formulae" docs/MANIFEST.md && grep -q "packages.brew.extra_packages.casks" docs/MANIFEST.md && grep -q "packages.brew.extra_packages.mas" docs/MANIFEST.md && grep -q "name, verify" docs/MANIFEST.md && grep -q "id, name" docs/MANIFEST.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/MANIFEST.md contains three new rows (one per sub-array): `grep -c "packages.brew.extra_packages.formulae" docs/MANIFEST.md` >= 1, same for `.casks` and `.mas`
    - The legacy `packages.brew.extra_packages` row in the Optional-fields table is GONE: a grep for a row that matches exactly `^\| .packages.brew.extra_packages. \| array of strings \|` (i.e., the flat-array row) returns nothing
    - The casks-row mentions the MANDATORY verify field (per D-04): `grep -q "MANDATORY" docs/MANIFEST.md` AND it's in proximity to casks
    - The MAS-row mentions that `name` doubles as the verify name (per D-06)
    - The Fixture 06 worked example reflects the typed-bucket shape: contains `formulae` AND `casks` AND `mas` keys; example shows bare-string + object coexistence in formulae OR per-sub-array merge semantics
    - File parses as markdown
    - No emojis added
    - No AI attribution
    - The Required-fields table row for `packages.brew.bundles` is unchanged
    - Sections after the worked examples (`## Adding a New Machine`, `## CLI Reference`, `## State Files`, `## Feature-Flag Reference`, `## Known Limitations`) unchanged
  </acceptance_criteria>
  <done>
    docs/MANIFEST.md schema reference shows the typed-bucket extras as three rows in the Optional-fields table; the Fixture 06 worked example reflects per-sub-array concat+dedupe semantics; other tables and sections untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical project documentation (REQUIREMENTS / ROADMAP / PROJECT / docs) | Read by humans + AI agents to understand the system; must agree with the code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-26 | I (Information disclosure) | none -- docs are public | accept | All docs are repo-visible; no secrets. |
| T-05-27 | T (Tampering) | doc/code disagreement causes silent drift | mitigate | This plan IS the mitigation: align canonical docs (REQUIREMENTS, ROADMAP, PROJECT, MANIFEST schema) with what Plans 01-05 actually build. Future-Josh and AI agents read these as authoritative; disagreement creates the "fix one, forget the other" drift class that v2 explicitly guards against. The plan's acceptance criteria require specific text in specific places so reviewers can catch a partial edit. |
| T-05-28 | T (Tampering) | a future edit reintroduces the legacy text (e.g., a copy-paste from Phase 1) | accept | Repo review gate; same risk model as any project documentation. Out of scope to enforce in CI. |

Block on HIGH; none. T-05-27 is the load-bearing reason this plan exists.
</threat_model>

<verification>
After all four tasks complete:

1. REQUIREMENTS.md PKGS-01 line contains `packages/<purpose>.rb`, does NOT contain `packages/brew/`, does NOT enumerate 5 bundles as fixed shipping set.
2. REQUIREMENTS.md PKGS-04 line contains `typed sub-table`, `formulae`, `casks`, `mas`, and the object-shape descriptions.
3. ROADMAP.md Phase 5 success criterion #3 contains `core.rb`, `gui.rb`, `Brewfile-<profile>.rb` (the prohibition), `typed sub-table`.
4. PROJECT.md Packages section bullet contains `packages/<purpose>.rb`, does NOT contain `packages/brew/`, mentions `extra_packages` typed buckets.
5. docs/MANIFEST.md Optional-fields table has three rows for the typed sub-table; legacy flat-array row gone.
6. docs/MANIFEST.md Fixture 06 worked example shows typed-bucket merge semantics.
7. All four files parse as valid markdown.
8. No new emojis introduced; no AI attribution.
9. The Traceability table in REQUIREMENTS.md still has all v1 requirement IDs (the table data is unchanged).
</verification>

<success_criteria>
- REQUIREMENTS.md PKGS-01 + PKGS-04 corrected
- ROADMAP.md Phase 5 success criterion #3 corrected (D-02 narrative preserved: no `Brewfile-<profile>.rb` prohibition)
- PROJECT.md Active Packages bullet corrected
- docs/MANIFEST.md Optional-fields table has three new rows; Fixture 06 worked example updated
- All four files agree with what Plans 01-05 build (no doc/code drift)
- No emojis; no AI attribution; no other lines touched in any file
</success_criteria>

<output>
After completion, create `.planning/phases/05-packages-layer-brewfile-composition-verification/05-06-SUMMARY.md` recording:
- Four files edited with surgical replacements at specific call sites
- Before/after text excerpts for each replacement (one bullet per file)
- Confirmation that no doc-vs-code drift remains for D-01 (flat layout), D-02 (minimal bundles), D-03 (typed-bucket extras), D-04 (cask verify mandatory), D-06 (MAS shape)
- Reference to PKGS-01, PKGS-04 (documentation-side completion)
</output>

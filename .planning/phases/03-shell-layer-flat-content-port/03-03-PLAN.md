---
phase: 03-shell-layer-flat-content-port
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - shell/functions/_dotfiles_feature.zsh
  - shell/functions/afk.zsh
  - shell/functions/aliaslist.zsh
  - shell/functions/cheat.zsh
  - shell/functions/docker.zsh
  - shell/functions/fs.zsh
  - shell/functions/functionlist.zsh
  - shell/functions/geoip.zsh
  - shell/functions/getcertnames.zsh
  - shell/functions/ghpubkey.zsh
  - shell/functions/host.zsh
  - shell/functions/info.zsh
  - shell/functions/ipv4lookup.zsh
  - shell/functions/ipv6lookup.zsh
  - shell/functions/mkcd.zsh
  - shell/functions/motd.zsh
  - shell/functions/permissions.zsh
  - shell/functions/prettyjson.zsh
  - shell/functions/pubkey.zsh
  - shell/functions/sethostname.zsh
  - shell/functions/sshlist.zsh
  - shell/functions/timezsh.zsh
  - shell/functions/vnc.zsh
  - shell/functions/whois.zsh
autonomous: true
requirements: [SHEL-07, SHEL-09, SHEL-11]

must_haves:
  truths:
    - "shell/functions/_dotfiles_feature.zsh defines _dotfiles_feature() with lazy load + associative-array cache (D-06)"
    - "_dotfiles_feature returns 'true' or 'false' on stdout; missing resolved.json yields 'false' for every key (graceful degrade)"
    - "_dotfiles_feature is safe under set -u (uses :-false default everywhere)"
    - "All 23 ported v1 function files have v2 analogs at shell/functions/<name>.zsh (24-file v1 inventory minus update.zsh per CF-06)"
    - "18 of those 23 functions port verbatim (afk, cheat, docker, fs, geoip, getcertnames, ghpubkey, host, info, ipv4lookup, ipv6lookup, mkcd, permissions, prettyjson, sethostname, timezsh, vnc, whois)"
    - "shell/functions/pubkey.zsh ports verbatim EXCEPT line 4 docstring updated from id_rsa_adobe.pub to id_ed25519_personal.pub (CONCERNS cosmetic fix)"
    - "shell/functions/aliaslist.zsh drops the DOTFILES_PROFILE subdir walk (v1 lines 26-34); replaces with a single flat call to _print_aliases_from_dir pointing at shell/aliases (header collapses from Common/Profile to Dotfiles)"
    - "shell/functions/functionlist.zsh drops the DOTFILES_PROFILE subdir walk (v1 lines 14-24); keeps the single flat loop over shell/functions/*.zsh"
    - "shell/functions/sshlist.zsh drops all DOTFILES_PROFILE references (v1 lines 8, 19, 22, 29); shows only main config Host entries; per-identity portion deferred to Phase 4 (IDNT-03)"
    - "shell/functions/motd.zsh implements 24h-TTL cache (SHEL-11): displays cached content synchronously; triggers async background refresh when cache is older than 86400s; _motd_render helper holds the v1 rendering logic verbatim"
    - "No shell/functions/*.zsh file contains DOTFILES_PROFILE anywhere"
    - "No shell/functions/update.zsh file is created (replaced by alias in Plan 04 per CF-06)"
    - "zsh -n passes on every shell/functions/*.zsh file (SHEL-09)"
  artifacts:
    - path: "shell/functions/_dotfiles_feature.zsh"
      provides: "Lazy manifest feature query helper; reads resolved.json once per shell into associative array"
      contains: "_DOTFILES_FEATURES"
    - path: "shell/functions/motd.zsh"
      provides: "24h-TTL cached MOTD display (SHEL-11) with async background refresh"
      contains: "_motd_render"
    - path: "shell/functions/aliaslist.zsh"
      provides: "List dotfiles + system aliases (flat layout — no profile walk)"
      contains: "_print_aliases_from_dir"
    - path: "shell/functions/functionlist.zsh"
      provides: "List dotfiles functions (flat layout — no profile walk)"
      contains: "shell/functions/"
    - path: "shell/functions/sshlist.zsh"
      provides: "List configured SSH hosts (main config only; per-identity deferred to P4)"
      contains: "Host "
    - path: "shell/functions/pubkey.zsh"
      provides: "Copy public SSH key to clipboard (docstring fixed)"
      contains: "id_ed25519_personal.pub"
  key_links:
    - from: "shell/functions/_dotfiles_feature.zsh"
      to: "$XDG_STATE_HOME/dotfiles/resolved.json"
      via: "jq -r '.features | to_entries[]'"
      pattern: "resolved.json"
    - from: "shell/functions/motd.zsh"
      to: "$XDG_CACHE_HOME/dotfiles/motd.cache"
      via: "synchronous read then async background refresh"
      pattern: "motd.cache"
    - from: "shell/functions/motd.zsh"
      to: "zsh/configs/motd_sysinfo.jsonc and motd_tron.txt"
      via: "v1 paths preserved until Phase 7 moves configs/ (per CONTEXT.md deferred)"
      pattern: "zsh/configs/motd_"
    - from: "shell/functions/aliaslist.zsh"
      to: "shell/aliases/"
      via: "_print_aliases_from_dir reads *.zsh files via glob with (.N) nullglob"
      pattern: "shell/aliases"
---

<objective>
Port v1's 23 function files into flat `shell/functions/<name>.zsh` (SHEL-07, SHEL-09) and add the net-new `_dotfiles_feature` lazy helper (D-06). 18 functions port byte-stable; 4 require surgical rewrites to drop `DOTFILES_PROFILE` references (aliaslist, functionlist, sshlist) or to implement SHEL-11 caching (motd). One file (pubkey.zsh) gets a single-line docstring fix per CONCERNS.md. `zsh/functions/update.zsh` is intentionally NOT ported (replaced by an alias in Plan 04 per CF-06).

Purpose: This is the function-content port. Once these files land and the `.zshrc` (Plan 02) flat glob is in place, every v1 function is callable from a v2 interactive shell. The `_dotfiles_feature` helper is the gate primitive that Plan 04's gated alias files (finder.zsh, ghostty.zsh, jgrid.zsh) consume.

Output: 24 new files in `shell/functions/` (1 net-new _dotfiles_feature helper + 23 ported v1 functions). No alias files (Plan 04). No taskfile changes (Plan 05). No symlinks (Plan 05).
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shell-layer-flat-content-port/03-CONTEXT.md
@.planning/phases/03-shell-layer-flat-content-port/03-PATTERNS.md
@.planning/phases/03-shell-layer-flat-content-port/03-01-PLAN.md
@.planning/phases/01-manifest-engine-repository-skeleton/01-CONTEXT.md
@CLAUDE.md
@.claude/CLAUDE.md
@zsh/functions/afk.zsh
@zsh/functions/aliaslist.zsh
@zsh/functions/cheat.zsh
@zsh/functions/docker.zsh
@zsh/functions/fs.zsh
@zsh/functions/functionlist.zsh
@zsh/functions/geoip.zsh
@zsh/functions/getcertnames.zsh
@zsh/functions/ghpubkey.zsh
@zsh/functions/host.zsh
@zsh/functions/info.zsh
@zsh/functions/ipv4lookup.zsh
@zsh/functions/ipv6lookup.zsh
@zsh/functions/mkcd.zsh
@zsh/functions/motd.zsh
@zsh/functions/permissions.zsh
@zsh/functions/prettyjson.zsh
@zsh/functions/pubkey.zsh
@zsh/functions/sethostname.zsh
@zsh/functions/sshlist.zsh
@zsh/functions/timezsh.zsh
@zsh/functions/vnc.zsh
@zsh/functions/whois.zsh

<interfaces>
v1 function file inventory (PATTERNS.md Sections 13-17 enumerate rewrites; Section 13 has the _dotfiles_feature sketch):

Verbatim ports (18 files): afk.zsh (6 lines), cheat.zsh (13), docker.zsh (21), fs.zsh (18), geoip.zsh (13), getcertnames.zsh (33), ghpubkey.zsh (11), host.zsh (11), info.zsh (18), ipv4lookup.zsh (36), ipv6lookup.zsh (37), mkcd.zsh (5), permissions.zsh (14), prettyjson.zsh (10), sethostname.zsh (12), timezsh.zsh (10), vnc.zsh (9), whois.zsh (46).

Rewrites:
- aliaslist.zsh — PATTERNS.md Pattern 15 (drop v1 lines 21-34 profile walk; keep _print_aliases_from_dir helper from v1 lines 9-19; collapse Common/Profile headers into one Dotfiles header pointing at shell/aliases).
- functionlist.zsh — same shape (drop v1 lines 14-24 profile walk; keep flat loop pointing at shell/functions).
- sshlist.zsh — PATTERNS.md Pattern 16 (drop v1 lines 8, 19, 22, 29 profile refs; keep main-config grep + footer; defer per-identity to P4).
- motd.zsh — PATTERNS.md Pattern 14 (split v1 lines 8-92 rendering into _motd_render helper; new motd() wrapper does cache-read + display + async refresh).

Cosmetic fix: pubkey.zsh — verbatim EXCEPT v1 line 4 docstring example id_rsa_adobe.pub becomes id_ed25519_personal.pub.

Dropped (NOT ported): update.zsh — replaced by `alias update='task install'` in Plan 04 per CF-06.

New file (no v1 analog): _dotfiles_feature.zsh — D-06 sketch in CONTEXT.md lines 189-208 + PATTERNS.md Pattern 13 lines 480-509.

resolved.json schema (Phase 1 D-15): top-level .features is an object of boolean values keyed by kebab-case names. Access via `jq -r '.features | to_entries[]'` to flatten into key=value lines for associative-array population.

motd cache integration: per CONTEXT.md deferred section, v1 paths zsh/configs/motd_sysinfo.jsonc and zsh/configs/motd_tron.txt MUST keep working in P3 — Phase 7 moves them to configs/<tool>/. Plan 03 references v1 paths verbatim. The cache file $XDG_CACHE_HOME/dotfiles/motd.cache is NEW (machine-local; auto-created by the function).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create _dotfiles_feature.zsh helper + port 18 verbatim functions + 1 cosmetic docstring fix (pubkey)</name>
  <files>shell/functions/_dotfiles_feature.zsh, shell/functions/afk.zsh, shell/functions/cheat.zsh, shell/functions/docker.zsh, shell/functions/fs.zsh, shell/functions/geoip.zsh, shell/functions/getcertnames.zsh, shell/functions/ghpubkey.zsh, shell/functions/host.zsh, shell/functions/info.zsh, shell/functions/ipv4lookup.zsh, shell/functions/ipv6lookup.zsh, shell/functions/mkcd.zsh, shell/functions/permissions.zsh, shell/functions/prettyjson.zsh, shell/functions/pubkey.zsh, shell/functions/sethostname.zsh, shell/functions/timezsh.zsh, shell/functions/vnc.zsh, shell/functions/whois.zsh</files>
  <read_first>
    - All 18 v1 zsh/functions/*.zsh files listed in files_modified (afk through whois, plus pubkey)
    - .planning/phases/03-shell-layer-flat-content-port/03-CONTEXT.md `<specifics>` "_dotfiles_feature helper sketch" (lines 189-208) — complete implementation
    - .planning/phases/03-shell-layer-flat-content-port/03-PATTERNS.md Pattern 13 (lines 480-516) — acceptance criteria for the helper
    - .planning/phases/03-shell-layer-flat-content-port/03-PATTERNS.md Pattern 17 (lines 625-633) — single-line docstring fix for pubkey
    - .planning/codebase/CONCERNS.md (pubkey docstring bug)
    - CLAUDE.md "One function per file, filename equals function name"
  </read_first>
  <action>
    Create 20 files. The first is net-new (`_dotfiles_feature.zsh`); 18 are byte-stable verbatim copies of v1 analogs; 1 (`pubkey.zsh`) is verbatim except a single-line docstring fix.

    File 1 — `shell/functions/_dotfiles_feature.zsh` (NEW, D-06):
    - Prepend a file-level comment block per CLAUDE.md convention: purpose ("lazy manifest feature query — reads resolved.json once per shell, caches in associative array"), callers ("shell/.zshrc function-glob loop sources this; shell/aliases/finder.zsh, ghostty.zsh, jgrid.zsh call _dotfiles_feature inside their wrapper functions"), reads ("$XDG_STATE_HOME/dotfiles/resolved.json"), side effects ("populates $_DOTFILES_FEATURES global associative array on first call"), and a usage example showing the canonical caller pattern: `[[ "$(_dotfiles_feature macos-finder)" == "true" ]]`.
    - Body MUST follow CONTEXT.md `<specifics>` D-06 sketch verbatim: declare `typeset -gA _DOTFILES_FEATURES`, init `_dotfiles_features_loaded=0`, define `function _dotfiles_feature()` taking `$1` as the feature name, guard the lazy-load on `(( ! _dotfiles_features_loaded ))`, read `resolved="${XDG_STATE_HOME}/dotfiles/resolved.json"`, only populate when `[[ -r "$resolved" ]]`, use the exact jq invocation `jq -r '.features | to_entries[] | "\(.key)=\(.value)"' "$resolved" 2>/dev/null` piped via process substitution into a `while IFS='=' read -r k v` loop, set `_dotfiles_features_loaded=1` AFTER the read attempt (so missing resolved.json sets the flag too — preventing re-attempts each call), and end with `echo "${_DOTFILES_FEATURES[$name]:-false}"`.
    - Do NOT add `set -euo pipefail` (sourced).
    - The leading underscore on the function name signals "private helper".

    Files 2-19 — 18 verbatim ports: For each v1 file in the list (afk, cheat, docker, fs, geoip, getcertnames, ghpubkey, host, info, ipv4lookup, ipv6lookup, mkcd, permissions, prettyjson, sethostname, timezsh, vnc, whois), copy `zsh/functions/<name>.zsh` to `shell/functions/<name>.zsh` byte-stable. No edits. No `set -euo pipefail` additions.

    File 20 — `shell/functions/pubkey.zsh` (verbatim port + 1 line edit):
    - Copy `zsh/functions/pubkey.zsh` (11 lines).
    - On v1 line 4 only, change the docstring example: the literal substring `id_rsa_adobe.pub` becomes `id_ed25519_personal.pub`. All other lines identical to v1.

    Do NOT create `shell/functions/update.zsh` — explicitly replaced by an alias in Plan 04 per CF-06.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && for f in _dotfiles_feature afk cheat docker fs geoip getcertnames ghpubkey host info ipv4lookup ipv6lookup mkcd permissions prettyjson pubkey sethostname timezsh vnc whois; do zsh -n shell/functions/$f.zsh || { echo PARSE_FAIL_$f; exit 1; }; done && [ -z "$(grep -l DOTFILES_PROFILE shell/functions/ 2>/dev/null)" ] && [ ! -e shell/functions/update.zsh ] && grep -q id_ed25519_personal.pub shell/functions/pubkey.zsh && ! grep -q id_rsa_adobe.pub shell/functions/pubkey.zsh && grep -q _DOTFILES_FEATURES shell/functions/_dotfiles_feature.zsh && grep -q 'jq -r' shell/functions/_dotfiles_feature.zsh && for f in afk cheat docker fs geoip getcertnames ghpubkey host info ipv4lookup ipv6lookup mkcd permissions prettyjson sethostname timezsh vnc whois; do diff zsh/functions/$f.zsh shell/functions/$f.zsh > /dev/null || { echo BYTE_DIFF_$f; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 20 files exist under `shell/functions/`
    - `zsh -n shell/functions/<name>.zsh` exits 0 for every file in the list
    - `grep -lr DOTFILES_PROFILE shell/functions/` returns NO matches
    - `shell/functions/update.zsh` does NOT exist (CF-06)
    - For each verbatim port in the set {afk, cheat, docker, fs, geoip, getcertnames, ghpubkey, host, info, ipv4lookup, ipv6lookup, mkcd, permissions, prettyjson, sethostname, timezsh, vnc, whois}: `diff zsh/functions/<name>.zsh shell/functions/<name>.zsh` is empty
    - `grep -c id_ed25519_personal.pub shell/functions/pubkey.zsh` returns 1 (CONCERNS fix)
    - `grep -c id_rsa_adobe.pub shell/functions/pubkey.zsh` returns 0 (old example removed)
    - `diff <(sed '4d' zsh/functions/pubkey.zsh) <(sed '4d' shell/functions/pubkey.zsh)` is empty (everything except line 4 matches v1)
    - `grep -c 'typeset -gA _DOTFILES_FEATURES' shell/functions/_dotfiles_feature.zsh` returns 1
    - `grep -c 'function _dotfiles_feature()' shell/functions/_dotfiles_feature.zsh` returns 1
    - `grep -c 'XDG_STATE_HOME.*dotfiles/resolved.json' shell/functions/_dotfiles_feature.zsh` returns 1
    - `grep -c 'jq -r.*features' shell/functions/_dotfiles_feature.zsh` returns 1
    - `grep -c ':-false' shell/functions/_dotfiles_feature.zsh` returns at least 1 (set -u safety)
    - Smoke test 1 (missing-flag default): `zsh -c '. shell/functions/_dotfiles_feature.zsh && echo "$(_dotfiles_feature nonexistent-flag)"'` outputs `false`
    - Smoke test 2 (graceful degrade on missing resolved.json): `XDG_STATE_HOME=/nonexistent zsh -c '. shell/functions/_dotfiles_feature.zsh && echo "$(_dotfiles_feature any-flag)"'` outputs `false` and does NOT crash
  </acceptance_criteria>
  <done>20 function files created: 1 net-new helper + 18 byte-stable v1 ports + 1 cosmetic docstring fix; no DOTFILES_PROFILE leaks; update.zsh deliberately absent.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite three functions to drop DOTFILES_PROFILE references (aliaslist, functionlist, sshlist)</name>
  <files>shell/functions/aliaslist.zsh, shell/functions/functionlist.zsh, shell/functions/sshlist.zsh</files>
  <read_first>
    - zsh/functions/aliaslist.zsh (full file, 42 lines — keep lines 1-19 _print_aliases_from_dir helper; replace 21-34 profile walk; keep 36-41 system-alias loop)
    - zsh/functions/functionlist.zsh (full file, 25 lines — keep lines 1-12 common-functions loop; replace 14-24 profile walk)
    - zsh/functions/sshlist.zsh (full file, 30 lines — line 1 shebang, line 3 comment, line 4 function signature with inline docstring, lines 5-7 header echoes, line 8 the DOTFILES_PROFILE local, lines 10-16 main-config block (KEEP), lines 18-26 profile-specific block (DELETE), line 28 `echo ""` blank-separator, line 29 footer with $profile suffix (REWRITE), line 30 closing `}` (PRESERVE))
    - .planning/phases/03-shell-layer-flat-content-port/03-PATTERNS.md Pattern 15 (lines 561-593) — explicit before/after blocks for aliaslist + functionlist
    - .planning/phases/03-shell-layer-flat-content-port/03-PATTERNS.md Pattern 16 (lines 595-623) — sshlist three options; planner picks Option 3 (safest P3 shape)
    - .planning/codebase/CONCERNS.md DOTFILES_PROFILE references in functions
  </read_first>
  <action>
    Rewrite three v1 functions.

    File 1 — `shell/functions/aliaslist.zsh` (v1 surgical edit):
    - Keep v1 lines 1-19 verbatim: shebang, top-of-file comment, function definition line (with docstring update — see below), local-var declarations (dotfiles_aliases assoc-array, yellow, reset), and the `_print_aliases_from_dir` helper definition (PATTERNS.md Pattern 15 lines 566-580 quotes this block exactly).
    - Function definition line (v1 line 4) docstring change: replace the phrase "all available common, profile, and system aliases" with "all available dotfiles and system aliases" (profile no longer exists in v2).
    - REPLACE v1 lines 21-34 (the Common header echo, the common dir call, the profile-conditional block reading `${DOTFILES_PROFILE:-}` and walking `$DOTFILEDIR/zsh/aliases/$profile`) with exactly two lines:
      Line A: `echo "${yellow}── Dotfiles ──${reset}"`
      Line B: `_print_aliases_from_dir "${DOTFILEDIR}/shell/aliases"`
    - Keep v1 lines 36-41 verbatim: the System section header (`echo "\n${yellow}── System ──${reset}"`) and the `alias | while IFS= read -r line; do ... done` loop that lists system aliases not in the dotfiles set.
    - Closing brace `}` ports verbatim.

    File 2 — `shell/functions/functionlist.zsh` (v1 surgical edit):
    - Keep v1 lines 1-3 verbatim: shebang + leading comment.
    - Function definition (v1 line 4) docstring change: replace "all available common and profile functions" with "all available dotfiles functions".
    - The Common section header (v1 line 6) becomes `echo "$(tput setaf 3)── Dotfiles ──$(tput sgr0)"`.
    - Keep the for-loop body (v1 lines 7-12) verbatim EXCEPT for the directory path: replace `"$DOTFILEDIR/zsh/functions/"*.zsh(.N)` with `"${DOTFILEDIR}/shell/functions/"*.zsh(.N)`. The self-exclusion guard `[[ -f $file && ${file:t} != "functionlist.zsh" ]]` is preserved verbatim.
    - DELETE v1 lines 14-24 entirely (the profile-conditional block).
    - Keep the closing `}` brace.

    File 3 — `shell/functions/sshlist.zsh` (v1 simplification per Option 3 in PATTERNS.md Pattern 16):
    - Keep v1 lines 1-7 verbatim with TWO docstring changes: (a) on v1 line 3 (the standalone comment above the function), drop the phrase "for current profile only" — the v2 form reads `# Display configured SSH host information`; (b) on v1 line 4 (the inline comment on the function-signature line), drop the phrase "for the current profile", so it reads `function sshlist() {    #  sshlist() will list all configured SSH hosts. ex: $ sshlist`.
    - DELETE v1 line 8 (the `local profile="${DOTFILES_PROFILE:-personal}"` line).
    - Keep v1 lines 10-16 verbatim — the Main Config section header and the grep-Host pipeline reading `$DOTFILEDIR/ssh/configs/config`.
    - DELETE v1 lines 18-26 entirely — the per-profile config block (`local profile_config=...config-$profile` + header echo + grep-Host pipeline).
    - REPLACE v1 lines 28-29 footer with exactly: a blank `echo ""` (preserves line 28 verbatim) followed by `echo "$(tput setaf 8)Config file: $DOTFILEDIR/ssh/configs/config$(tput sgr0)"` (rewrites line 29 — drops `config-$profile` suffix; single-file reference).
    - Insert one new inline comment line in the location where the deleted profile block used to live, between the kept main-config block and the new footer: `# Per-identity SSH config display deferred to Phase 4 (IDNT-03).`
    - Preserve v1 line 30 verbatim (the function's closing `}` brace).

    Do NOT add `set -euo pipefail` to any of the three files (sourced, not executable).
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && zsh -n shell/functions/aliaslist.zsh && zsh -n shell/functions/functionlist.zsh && zsh -n shell/functions/sshlist.zsh && ! grep -q DOTFILES_PROFILE shell/functions/aliaslist.zsh && ! grep -q DOTFILES_PROFILE shell/functions/functionlist.zsh && ! grep -q DOTFILES_PROFILE shell/functions/sshlist.zsh && grep -q 'shell/aliases' shell/functions/aliaslist.zsh && grep -q 'shell/functions' shell/functions/functionlist.zsh && grep -q 'Dotfiles' shell/functions/aliaslist.zsh && grep -q 'Dotfiles' shell/functions/functionlist.zsh && grep -q _print_aliases_from_dir shell/functions/aliaslist.zsh && ! grep -q 'config-' shell/functions/sshlist.zsh && grep -q 'Phase 4' shell/functions/sshlist.zsh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `zsh -n shell/functions/aliaslist.zsh` exits 0
    - `zsh -n shell/functions/functionlist.zsh` exits 0
    - `zsh -n shell/functions/sshlist.zsh` exits 0
    - `grep -c DOTFILES_PROFILE shell/functions/aliaslist.zsh` returns 0
    - `grep -c DOTFILES_PROFILE shell/functions/functionlist.zsh` returns 0
    - `grep -c DOTFILES_PROFILE shell/functions/sshlist.zsh` returns 0
    - `grep -c '_print_aliases_from_dir' shell/functions/aliaslist.zsh` returns at least 2 (definition + invocation)
    - `grep -c 'shell/aliases' shell/functions/aliaslist.zsh` returns at least 1
    - `grep -c 'shell/functions' shell/functions/functionlist.zsh` returns at least 1
    - `grep -c '── Dotfiles ──' shell/functions/aliaslist.zsh` returns at least 1
    - `grep -c '── Dotfiles ──' shell/functions/functionlist.zsh` returns at least 1
    - `grep -c '── System ──' shell/functions/aliaslist.zsh` returns 1 (system section preserved from v1)
    - `grep -c 'functionlist.zsh' shell/functions/functionlist.zsh` returns at least 1 (self-exclusion guard preserved)
    - `grep -c 'config-' shell/functions/sshlist.zsh` returns 0 (per-identity config-<profile> reference removed)
    - `grep -c 'Phase 4' shell/functions/sshlist.zsh` returns at least 1 (deferral comment present)
    - `grep -c '^Host ' shell/functions/sshlist.zsh` returns 0 (no Host literals in the source — the grep pattern is dynamic)
    - `grep -c 'grep -E.*Host' shell/functions/sshlist.zsh` returns at least 1 (the grep pattern that lists SSH hosts is preserved)
    - Functional smoke: `zsh -c '. shell/functions/aliaslist.zsh && declare -f aliaslist > /dev/null && echo ok'` outputs `ok` (function is defined; sourced cleanly)
    - Functional smoke: `zsh -c '. shell/functions/functionlist.zsh && declare -f functionlist > /dev/null && echo ok'` outputs `ok`
    - Functional smoke: `zsh -c '. shell/functions/sshlist.zsh && declare -f sshlist > /dev/null && echo ok'` outputs `ok`
  </acceptance_criteria>
  <done>Three functions rewritten to drop DOTFILES_PROFILE; flat dir paths in place; sshlist defers per-identity display to Phase 4 with an inline comment marker.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Rewrite shell/functions/motd.zsh with 24h-TTL cache (SHEL-11) — split v1 rendering into _motd_render helper, add async background refresh</name>
  <files>shell/functions/motd.zsh</files>
  <read_first>
    - zsh/functions/motd.zsh (full file, 93 lines — lines 8-92 are the rendering logic; ports verbatim into a _motd_render helper)
    - .planning/phases/03-shell-layer-flat-content-port/03-PATTERNS.md Pattern 14 (lines 519-558) — exact rewrite pattern with v1 cache-file dependencies, acceptance criteria
    - .planning/phases/03-shell-layer-flat-content-port/03-CONTEXT.md `<decisions>` "Claude's Discretion → MOTD cache architecture" (SHEL-11 architecture: 24h TTL, async refresh, cache file at $XDG_CACHE_HOME/dotfiles/motd.cache)
    - .planning/phases/03-shell-layer-flat-content-port/03-CONTEXT.md `<deferred>` "Move v1 zsh/configs/ to v2 configs/<tool>/" — Phase 7 owns the move; P3 keeps zsh/configs/motd_*.{jsonc,txt} working
    - REQUIREMENTS.md SHEL-11 (MOTD output cached to disk with 24-hour TTL, async refresh)
    - .planning/codebase/CONCERNS.md (synchronous MOTD push ~200ms cost on every login — this rewrite is the SHEL-11 fix)
  </read_first>
  <action>
    Rewrite `shell/functions/motd.zsh` as a cache-backed wrapper around the v1 rendering logic.

    File structure (top to bottom):

    1. File-level comment block (replace v1 lines 1-5 with a v2-relevant version): purpose ("display Tron-themed MOTD with 24h-TTL cache (SHEL-11)"), behavior ("public motd() displays cached content synchronously; async background refresh when cache is older than 86400s; _motd_render holds the v1 rendering logic verbatim"), cache file ("$XDG_CACHE_HOME/dotfiles/motd.cache — machine-local, never committed"), v1 dependencies still consumed ("zsh/configs/motd_sysinfo.jsonc and motd_tron.txt — Phase 7 will move these to configs/<tool>/").

    2. Public `motd()` function. Body per PATTERNS.md Pattern 14:
       - Define a docstring on the function-definition line preserving v1's discoverability convention: `function motd() {    # motd() will display a Tron-themed message of the day with system info. ex: $ motd`.
       - `local cache="${XDG_CACHE_HOME}/dotfiles/motd.cache"`
       - `local ttl=86400` (24 hours in seconds)
       - `local now mtime=0`
       - `now=$(date +%s)`
       - `mkdir -p "${cache:h}"` (ensures parent dir exists before stat)
       - Conditional cache read: `if [[ -f "$cache" ]]; then mtime=$(stat -f %m "$cache" 2>/dev/null || stat -c %Y "$cache" 2>/dev/null || echo 0); cat "$cache"; fi`. NOTE: macOS stat uses `-f %m`; the `stat -c %Y` fallback is for portability (future-Linux) but never executes on macOS-only v1.
       - Async refresh trigger: `if (( now - mtime > ttl )); then ( _motd_render > "${cache}.tmp" 2>/dev/null && mv "${cache}.tmp" "$cache" ) &!; fi`. The `&!` (zsh-specific: background + disown) prevents the prompt from waiting for the refresh. The `.tmp` indirection makes the cache write atomic — partial writes never appear at the cache path.
       - Note: on a fresh machine with no cache file, mtime=0 and now-mtime > ttl always, so the FIRST motd call shows nothing (cache empty), kicks off the background render, and subsequent calls within 24h show the rendered output instantly. This is acceptable per SHEL-11 architecture; alternatively the planner may add a synchronous-first-render branch — but per PATTERNS.md Pattern 14 acceptance criteria, "first login on a fresh machine: empty cache → cold path renders synchronously and writes cache". Implement the synchronous-first-render branch: if `[[ ! -f "$cache" ]]`, call `_motd_render | tee "$cache"` synchronously before the async-refresh block runs.

    3. Private `_motd_render()` function. Body: port v1 lines 8-92 verbatim (every line of the v1 motd function body — the Tron color scheme, `tput cols` width, `_motd_center` and `_motd_line` helpers, the conditional logo block, system-info fastfetch invocation, dotfiles git-status block, quotes-file random selection, footer, and the `unset -f _motd_center _motd_line` cleanup). The v1 line 53 fastfetch config path `${DOTFILEDIR}/zsh/configs/motd_sysinfo.jsonc` and v1 line 77 quotes file path `${DOTFILEDIR}/zsh/configs/motd_tron.txt` PORT VERBATIM — Phase 7 moves them to configs/<tool>/.

    Do NOT add `set -euo pipefail` (sourced).
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && zsh -n shell/functions/motd.zsh && grep -q 'function motd()' shell/functions/motd.zsh && grep -q '_motd_render' shell/functions/motd.zsh && grep -q '86400' shell/functions/motd.zsh && grep -q 'motd.cache' shell/functions/motd.zsh && grep -q 'XDG_CACHE_HOME' shell/functions/motd.zsh && grep -q 'stat -f %m' shell/functions/motd.zsh && grep -q '&!' shell/functions/motd.zsh && grep -q 'motd_sysinfo.jsonc' shell/functions/motd.zsh && grep -q 'motd_tron.txt' shell/functions/motd.zsh && ! grep -q DOTFILES_PROFILE shell/functions/motd.zsh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `zsh -n shell/functions/motd.zsh` exits 0
    - `grep -c 'function motd()' shell/functions/motd.zsh` returns 1
    - `grep -c '_motd_render' shell/functions/motd.zsh` returns at least 2 (definition + invocation in motd())
    - `grep -c 'function _motd_render()' shell/functions/motd.zsh` returns 1
    - `grep -c '86400' shell/functions/motd.zsh` returns at least 1 (24h TTL constant)
    - `grep -c 'XDG_CACHE_HOME.*motd.cache\|motd.cache' shell/functions/motd.zsh` returns at least 1
    - `grep -c 'stat -f %m' shell/functions/motd.zsh` returns 1 (BSD/macOS stat invocation)
    - `grep -c 'stat -c %Y' shell/functions/motd.zsh` returns 1 (GNU stat fallback for future portability)
    - `grep -c '&!' shell/functions/motd.zsh` returns at least 1 (zsh async-disown pattern for background refresh)
    - `grep -c '${cache}.tmp\|cache.tmp' shell/functions/motd.zsh` returns at least 1 (atomic write via temp file)
    - `grep -c 'motd_sysinfo.jsonc' shell/functions/motd.zsh` returns 1 (v1 config path preserved per CONTEXT.md deferred — Phase 7 moves it)
    - `grep -c 'motd_tron.txt' shell/functions/motd.zsh` returns 1 (v1 quotes file path preserved)
    - `grep -c DOTFILES_PROFILE shell/functions/motd.zsh` returns 0
    - `grep -c 'fastfetch' shell/functions/motd.zsh` returns at least 1 (rendering logic preserved)
    - `grep -c 'JGRID\|J G R I D' shell/functions/motd.zsh` returns at least 1 (v1 logo content preserved)
    - `grep -c 'SYSTEM ACCESS GRANTED' shell/functions/motd.zsh` returns 1 (v1 header text preserved)
    - `grep -c 'END OF LINE' shell/functions/motd.zsh` returns 1 (v1 footer preserved)
    - Smoke test (cache miss): `XDG_CACHE_HOME=$(mktemp -d) zsh -c '. shell/functions/motd.zsh && [[ "$(declare -f motd)" =~ "motd.cache" ]] && echo ok'` outputs `ok` (the function definition references the cache path)
    - Smoke test (function callable in subshell without crash; rendering may fail if fastfetch absent but the function MUST NOT crash): `zsh -c '. shell/functions/motd.zsh && type motd' 2>&1 | grep -q "motd is a shell function" && echo ok` outputs `ok`
  </acceptance_criteria>
  <done>motd.zsh is cache-backed (SHEL-11): synchronous cache display on every call, atomic-write async background refresh when cache is stale, _motd_render holds the v1 rendering logic verbatim; v1 config-file paths preserved for Phase 7 cutover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `$XDG_STATE_HOME/dotfiles/resolved.json` → `_dotfiles_feature` | jq parses repo-derived JSON. The resolver wrote the file from committed TOML; tampering requires local file write. |
| `$XDG_CACHE_HOME/dotfiles/motd.cache` → `motd()` display | Cache file is `cat`-ed directly to stdout (terminal). Cache writer is `_motd_render` in a background process spawned by motd itself. |
| `zsh/configs/motd_sysinfo.jsonc` → fastfetch | Fastfetch reads a JSONC config from the repo. Repo-controlled trust. |
| URL fetches in cheat/geoip/host/ipv4lookup/ipv6lookup/ghpubkey/whois | Functions curl/wget remote URLs for the user's interactive workflow. Same trust posture as v1; no change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | `motd.cache` → terminal write | accept | The cache content originates from `_motd_render` running in the same user's shell against repo-committed config files. An attacker who can write to $XDG_CACHE_HOME can already inject terminal escape sequences via dozens of other paths — local trust boundary. |
| T-03-10 | Information Disclosure | `_dotfiles_feature` reads resolved.json | accept | resolved.json contains repo-derived feature flags (booleans), identity selector strings (`personal`/`work`/`none`), and package bundle names. No secrets, no tokens. The function never echoes the whole JSON. |
| T-03-11 | Denial of Service | motd async refresh failure | mitigate | The refresh runs in a backgrounded subshell with `&!` (disown). Failures redirect to `/dev/null` (the `2>/dev/null` on `_motd_render`). The `.tmp` atomic-write means partial writes never replace a good cache. Worst case: cache stays stale longer than 24h until next successful refresh. |
| T-03-12 | Tampering | `jq` parsing untrusted JSON | accept | resolved.json is locally generated. jq's exit code is squelched with `2>/dev/null`; a parse failure leaves _DOTFILES_FEATURES empty, which means every lookup returns "false" (safe default). |
</threat_model>

<verification>
After all three tasks complete:

1. `find shell/functions -name '*.zsh' | wc -l` returns 24 (1 helper + 23 ports; update.zsh deliberately absent — v1 has 24 function files; minus update.zsh = 23 ports).
2. `for f in shell/functions/*.zsh; do zsh -n "$f" || echo "FAIL: $f"; done` produces NO `FAIL:` output.
3. `grep -lr DOTFILES_PROFILE shell/functions/` returns nothing.
4. `[ ! -e shell/functions/update.zsh ]` succeeds (CF-06).
5. Verbatim ports byte-stable: `for f in afk cheat docker fs geoip getcertnames ghpubkey host info ipv4lookup ipv6lookup mkcd permissions prettyjson sethostname timezsh vnc whois; do diff zsh/functions/$f.zsh shell/functions/$f.zsh > /dev/null || echo "BYTE DIFF: $f"; done` produces no output.
6. `_dotfiles_feature` returns "false" for an unknown flag and never crashes on missing resolved.json.
7. `motd()` references the SHEL-11 cache file and the 86400s TTL constant.
</verification>

<success_criteria>
- 24 function files exist under shell/functions/ (1 helper + 23 ports)
- zsh -n passes on every file (SHEL-09)
- _dotfiles_feature helper works with lazy associative-array cache (D-06)
- 18 functions are byte-stable v1 ports
- pubkey docstring updated (CONCERNS cosmetic fix)
- aliaslist, functionlist, sshlist rewritten to drop DOTFILES_PROFILE
- motd.zsh implements 24h-TTL cache with async refresh (SHEL-11)
- update.zsh deliberately not ported (CF-06)
- No DOTFILES_PROFILE references anywhere in shell/functions/
</success_criteria>

<output>
After completion, create `.planning/phases/03-shell-layer-flat-content-port/03-03-SUMMARY.md` per the template at `~/.config/claude/get-shit-done/templates/summary.md`.
</output>

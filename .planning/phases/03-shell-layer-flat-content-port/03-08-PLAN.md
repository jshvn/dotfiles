---
phase: 03-shell-layer-flat-content-port
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - taskfiles/links.yml
  - .claude/CLAUDE.md
autonomous: true
gap_closure: true
requirements: [SHEL-01, SHEL-03]

must_haves:
  truths:
    - "taskfiles/links.yml `all:` aggregator no longer has a `status:` block — the v1 macos:shell:145 bug class is closed (CR-02 BLOCKER fix)"
    - "links:zsh and links:antidote sub-tasks retain their per-task `status:` blocks (idempotency is preserved at the sub-task level — go-task evaluates each independently when the parent has no status:)"
    - "links:validate continues to lack a `status:` block (diagnostic; `# lint-allow: cmds-without-status` preserved)"
    - ".claude/CLAUDE.md no longer references `$DOTFILES_PROFILE`, `DOTFILES_PROFILE`, or the v1 profile-suffix model — the project doc is consistent with the v2 reality (manifest-driven machines, no profile env var, no profile-suffixed alias subdirs)"
    - ".claude/CLAUDE.md Structure section is updated to describe the v2 layout (`shell/` flat tree, `manifests/`, `taskfiles/`, `configs/`, etc.) instead of the v1 layout (`zsh/aliases/{common,personal,work}/`, `install/Brewfile-<profile>.rb`, etc.)"
    - ".claude/CLAUDE.md Zsh Startup Order section reflects v2 (.zshenv exports DOTFILES_MACHINE, .zshrc loads antidote not antigen)"
    - ".claude/CLAUDE.md Quick Reference no longer includes `task profile:set` (the profile sub-command is being retired with v1)"
    - "After both edits, `grep -rn 'DOTFILES_PROFILE' --exclude-dir=.planning --exclude-dir=zsh --exclude-dir=.git .` returns 0 hits in v2-shipping files (the v1 zsh/ tree is allowed to retain references per the Phase 3 byte-stable v1 contract; .planning/ historical research is also allowed)"
    - "task --list still resolves the root Taskfile.yml and the edited taskfiles/links.yml without parse errors"
    - "task install --dry-run still includes links:all (and its zsh + antidote sub-tasks) in the graph"
  artifacts:
    - path: "taskfiles/links.yml"
      provides: "Real link orchestration; aggregator status: removed; sub-task status: blocks unchanged"
      contains: "task: _:safe-link"
    - path: ".claude/CLAUDE.md"
      provides: "Project-level Claude instructions consistent with v2 (no DOTFILES_PROFILE; manifest-driven layout)"
      contains: "DOTFILES_MACHINE"
  key_links:
    - from: "taskfiles/links.yml all: task"
      to: "links:zsh + links:antidote sub-tasks"
      via: "cmds: invokes both; each sub-task evaluates its own status: independently"
      pattern: "task: zsh\\|task: antidote"
    - from: ".claude/CLAUDE.md Structure section"
      to: "v2 directories (shell/, manifests/, configs/, etc.)"
      via: "documentation parity with the v2 manifest model"
      pattern: "shell/"
---

<objective>
Close two remaining gap-closure items:

1. CR-02 BLOCKER (from 03-VERIFICATION.md and 03-REVIEW.md): the `all:` aggregator in `taskfiles/links.yml` lines 69-76 has a `status:` block that only checks 2 of the 6 symlinks the aggregator owns. If `.zshrc` or `.zprofile` is missing while `.zshenv` and `.zsh_plugins.txt` exist, `task install -> links:all` short-circuits and never restores the missing files. This is the exact v1 `macos:shell:145` regression class. Recommended fix per CR-02: drop the `status:` block entirely from the aggregator. Sub-tasks already idempotent via their own status: blocks.

2. Stale `.claude/CLAUDE.md` content (from 03-VERIFICATION.md anti-patterns row and 03-REVIEW.md note): the project-level doc still references `$DOTFILES_PROFILE`, `task profile:set`, `zsh/aliases/{common,personal,work}/`, and the v1 layout. Truth 1 of the phase ("Fresh interactive zsh exports `$DOTFILES_MACHINE`; no `$DOTFILES_PROFILE` anywhere") technically passes for shell/, but the project doc contradicts the v2 narrative. Update the doc to reflect v2 reality.

Purpose: Close the last two outstanding items the verifier flagged. After this plan, the only items left from the phase-3 review are intentional deferred work (Phase 7 cleanup of v1 `zsh/configs/` paths; pre-existing v1 carryover bugs in fs.zsh / docker.zsh / whois.zsh which are byte-stable v1 ports tracked as platform-port debt). The phase-3 goal is fully achieved.

Output: 2 file edits. No new files. No new tasks.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shell-layer-flat-content-port/03-CONTEXT.md
@.planning/phases/03-shell-layer-flat-content-port/03-VERIFICATION.md
@.planning/phases/03-shell-layer-flat-content-port/03-REVIEW.md
@.planning/phases/03-shell-layer-flat-content-port/03-05-PLAN.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-CONTEXT.md
@CLAUDE.md
@.claude/CLAUDE.md
@taskfiles/links.yml
@taskfiles/lint.yml
@taskfiles/helpers.yml
@Taskfile.yml

<interfaces>
Current taskfiles/links.yml `all:` task (lines 69-76):

```
  all:
    desc: "Create all symlinks (P3: shell only; later phases extend)"
    cmds:
      - task: zsh
      - task: antidote
    status:
      - test -L "{{.ZDOTDIR}}/.zshenv"
      - test -L "{{.ZDOTDIR}}/.zsh_plugins.txt"
```

CR-02 fix: drop the `status:` block (lines 74-76 of the file as-shipped post-Plan-03-05). The sub-tasks zsh: (lines 82-100) and antidote: (lines 106-112) each have their own `status:` blocks covering all the symlinks they own, so dropping the aggregator status: does not lose idempotency — it gains correctness because `links:all` now always re-checks both sub-tasks instead of short-circuiting on a partial state.

LINT-03a impact: `links:all` is a `cmds:`-only task (no `status:`). LINT-03a flags "tasks with cmds: but no status:" unless they have an inline `# lint-allow: cmds-without-status` comment. The aggregator pattern is exempt because the sub-tasks ARE the idempotency mechanism — invoking them is safe to re-run, and idempotency is enforced inside each sub-task. Add the `# lint-allow: cmds-without-status` marker above the `all:` task name to keep lint clean.

There is precedent: `validate:` in this same file already has the marker (line 118 in the current file). Adding it to `all:` is consistent.

Current .claude/CLAUDE.md (68 lines, ALL v1 content):

- Lines 1-7: Overview — references "three profiles: personal, work, server", "`$DOTFILES_PROFILE`", `${XDG_CONFIG_HOME}/dotfiles/profile`
- Lines 9-16: Quick Reference — includes `task profile:set -- personal` (v1 command being retired)
- Lines 18-32: Structure — lists v1 paths (`zsh/aliases/{common,personal,work}/`, `install/Brewfile-<profile>.rb`, etc.)
- Lines 34-43: Conventions — generally still applicable; small edits
- Lines 45-51: Adding Things — v1 paths (`zsh/functions/<name>.zsh`, `install/Brewfile.rb`)
- Lines 53-60: Zsh Startup Order — mentions `DOTFILES_PROFILE` on line 56 and "Antigen/oh-my-zsh" on line 58
- Lines 62-67: Safety — line 67 mentions "Profile-conditional logic: 1Password for personal/work, system ssh-agent for server" (v1 hostname-based selection)

The doc needs a v2 rewrite, not a surgical patch. But the rewrite is small (the v2 model is simpler than v1), and the file is a known shape so the rewrite is constrained.

v2 reality (from PROJECT.md, ROADMAP.md, completed Phases 1-3):

- Machines, not profiles: `task setup -- <machine-name>` writes `$XDG_STATE_HOME/dotfiles/machine`. The shell exports `DOTFILES_MACHINE`, never `DOTFILES_PROFILE`.
- Manifest-driven everything: each machine's TOML in `manifests/machines/<name>.toml` declares features, identity, package bundles. The resolver writes `resolved.json`; every task reads it via `fromJson`.
- Flat shell layout (v1 only — Linux platform-aware split deferred to v2+).
- Antidote replaces Antigen.
- `task install` IS `task update` (single canonical entry per Phase 2 D-10).
- 1Password SSH agent gated on `features.one-password-ssh` (no hostname check).
- Brewfile composition by purpose (core/gui/dev/ops/personal) — Phase 5 ships this; Phase 3 is shell-only.

The rewrite preserves the doc's existing section structure (Overview, Quick Reference, Structure, Conventions, Adding Things, Zsh Startup Order, Safety) but updates each section's content to the v2 model.

Validation: the file is committed via repo-level git. No tooling reads `.claude/CLAUDE.md` programmatically (it's a Claude Code instruction file for AI agents); the only validation is "does it accurately describe v2".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Drop the all: aggregator status: block from taskfiles/links.yml (CR-02 BLOCKER fix)</name>
  <files>taskfiles/links.yml</files>
  <read_first>
    - taskfiles/links.yml (full file, 134 lines — the `all:` task is at lines 69-76; specifically the `status:` block is lines 74-76)
    - .planning/phases/03-shell-layer-flat-content-port/03-VERIFICATION.md gaps + anti-patterns section, CR-02 (the under-checked aggregator status block)
    - .planning/phases/03-shell-layer-flat-content-port/03-REVIEW.md CR-02 (lines 134-186 of review — full fix discussion with v1 macos:shell:145 reference)
    - taskfiles/lint.yml (confirm LINT-03a exemption marker convention — `# lint-allow: cmds-without-status` placed ABOVE the task name; same pattern as validate: in this file)
    - taskfiles/links.yml line 118 (existing `# lint-allow: cmds-without-status` on validate: — precedent for the marker placement)
  </read_first>
  <action>
    Surgical edit to `taskfiles/links.yml`. Drop the `status:` block from the `all:` aggregator and add the LINT-03a exemption marker.

    Two micro-changes inside the `all:` task block (currently lines 69-76):

    1. ABOVE the `all:` task name line (currently line 69), insert a new comment line: `  # lint-allow: cmds-without-status`. Match the indentation of the existing `# lint-allow: cmds-without-status` line above `validate:` (line 118) — two spaces.

    2. DELETE lines 74-76 entirely (the three lines forming the `status:` block: `    status:` header, `      - test -L "{{.ZDOTDIR}}/.zshenv"`, `      - test -L "{{.ZDOTDIR}}/.zsh_plugins.txt"`).

    After the edit, the `all:` task occupies fewer lines and reads:

       # lint-allow: cmds-without-status
       all:
         desc: "Create all symlinks (P3: shell only; later phases extend)"
         cmds:
           - task: zsh
           - task: antidote

    Update the file-header banner (lines 19-23 of the current file) to remove the LINT-02 emphasis on the aggregator status block (the banner currently discusses status-block convention in detail; trim the banner's mention of an aggregator-status block since that's no longer the right pattern). Specifically: in the "Status-block convention (LINT-02 enforcement):" paragraph, append a clarifying sentence: `Aggregator tasks (e.g., the all: aggregator below) intentionally omit the status: block and use the # lint-allow: cmds-without-status marker; idempotency is enforced inside each sub-task.` Keep the rest of the banner intact.

    Do NOT change:
    - The `includes:` block.
    - The `vars:` block.
    - The `zsh:` task (lines 82-100 of current file — its own `status:` block covering all 5 startup files is correct and preserved).
    - The `antidote:` task (lines 106-112 of current file — its own `status:` block covering `.zsh_plugins.txt` is correct and preserved).
    - The `validate:` task (lines 118-133 of current file — already has the `# lint-allow: cmds-without-status` marker; diagnostic-only).
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && task --list-all --json -t taskfiles/links.yml > /dev/null && awk '/^  all:/{a=NR} /^  zsh:/{z=NR; if(a) {for(i=a+1; i<z; i++) lines[i]=1; for(l in lines) if(found[l]=="status:") {found_status=1; exit}} } /status:/{if(NR>a && NR<z) found_status=1} END{exit found_status ? 1 : 0}' taskfiles/links.yml && grep -B1 '^  all:' taskfiles/links.yml | grep -q '# lint-allow: cmds-without-status' && grep -c '_:safe-link' taskfiles/links.yml | awk '$1>=6{exit 0} {exit 1}' && grep -q 'test -L "{{.ZDOTDIR}}/.zshenv"' taskfiles/links.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `task --list-all --json -t taskfiles/links.yml` exits 0 (valid YAML after edit)
    - The `all:` task no longer has a `status:` block: extracting the lines between `^  all:$` and the next `^  zsh:$`, no line matches `^    status:$`. Specifically: `awk '/^  all:/{f=1;next} /^  zsh:/{f=0} f && /^    status:/{print "FOUND"; exit 1}' taskfiles/links.yml` exits 0 (no FOUND)
    - The LINT-03a exemption marker is present above `all:`: `grep -B1 '^  all:' taskfiles/links.yml | head -1 | grep -q '# lint-allow: cmds-without-status'` succeeds
    - The `zsh:` task's `status:` block is preserved with all 5 symlink checks: `awk '/^  zsh:/{f=1;next} /^  antidote:/{f=0} f && /test -L "{{.ZDOTDIR}}/.z(shenv|profile|shrc|login|logout)"/' taskfiles/links.yml | wc -l | grep -qx 5`
    - The `antidote:` task's `status:` block is preserved with the `.zsh_plugins.txt` check: `awk '/^  antidote:/{f=1;next} /^[^[:space:]]/{f=0} f && /test -L "{{.ZDOTDIR}}/.zsh_plugins.txt"/' taskfiles/links.yml | wc -l | grep -qE '^[1-9]'`
    - The `validate:` task is unchanged: `grep -c 'validate:' taskfiles/links.yml` returns 1 and its `# lint-allow: cmds-without-status` marker is preserved
    - `_:safe-link` invocations are preserved: `grep -c '_:safe-link' taskfiles/links.yml` returns at least 6 (5 zsh + 1 antidote)
    - `_:check-link` invocations are preserved (in validate:): `grep -c '_:check-link' taskfiles/links.yml` returns at least 6
    - File-header banner mentions the aggregator-status decision: `grep -c 'aggregator tasks\|# lint-allow: cmds-without-status marker' taskfiles/links.yml` returns at least 1
    - `task --list -t taskfiles/links.yml` lists all four tasks: all, zsh, antidote, validate
    - `task lint:taskfile` (run from repo root) reports NO new errors on taskfiles/links.yml
    - Behavioral check (CR-02 closed): with a "partial state" simulated by removing one symlink target, the aggregator no longer short-circuits — `task -t taskfiles/links.yml all` invokes both sub-tasks unconditionally (the sub-tasks themselves no-op via their own status: when fully linked, or restore when partial)
    - `wc -l taskfiles/links.yml` reports a value 2-4 lines smaller than pre-edit (3 status lines removed minus 1 lint-allow comment line added; plus possible banner text additions)
  </acceptance_criteria>
  <done>taskfiles/links.yml all: aggregator no longer has a status: block; the # lint-allow marker is placed above it; sub-task status: blocks preserved; v1 macos:shell:145 bug class is closed; lint stays green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite .claude/CLAUDE.md for v2 reality — remove DOTFILES_PROFILE, profile commands, and v1 layout references</name>
  <files>.claude/CLAUDE.md</files>
  <read_first>
    - .claude/CLAUDE.md (full file, 68 lines — every section needs v2 updates)
    - PROJECT.md (the v2 vision — manifests, machines, no profiles)
    - CLAUDE.md repo root (v2 project conventions — kebab-case feature names, `index` access in templates, manifest as source of truth, flat shell/aliases/ in v1, no hardcoded /opt/homebrew, _:safe-link, etc.)
    - manifests/defaults.toml (the [features] block — for the Adding Things section's "feature flag" instructions)
    - manifests/machines/personal-laptop.toml (an example machine manifest)
    - shell/.zshrc (the v2 startup file — confirms antidote/compinit/aliases-then-functions ordering for the Zsh Startup Order section)
    - shell/.zshenv (confirms DOTFILES_MACHINE export — for the .zshenv bullet)
    - shell/README.md (the DOCS-02 anchor — for the "Adding Things" cross-link)
    - taskfiles/README.md (the new doc from Plan 03-07 — for the cross-link about adding a taskfile)
    - install/README.md (the new doc from Plan 03-07 — for the cross-link about install/ scripts)
    - .planning/phases/03-shell-layer-flat-content-port/03-VERIFICATION.md anti-patterns row for .claude/CLAUDE.md (stale-doc note)
    - Taskfile.yml (the includes: block + install task body — confirms the v2 task list for Quick Reference)
  </read_first>
  <action>
    Rewrite `.claude/CLAUDE.md` for v2 reality. The file's section structure (Overview, Quick Reference, Structure, Conventions, Adding Things, Zsh Startup Order, Safety) is preserved; the CONTENT in each section is updated.

    Section-by-section rewrite:

    **`# Dotfiles Project`** — keep heading.

    **`## Overview`** — Replace the v1 narrative with v2:
    - First sentence (preserve): `macOS dotfiles managed with go-task, symlinks, and XDG base directory spec.`
    - Replace the second/third sentences (currently mentioning three profiles + `$DOTFILES_PROFILE`) with: `Per-machine TOML manifests under manifests/machines/<name>.toml inherit from manifests/defaults.toml. Active machine selection is stored at $XDG_STATE_HOME/dotfiles/machine and exported as $DOTFILES_MACHINE. No hostname inference, no profile suffixes, no $DOTFILES_PROFILE.`

    **`## Quick Reference`** — Rewrite the bullet list to v2 commands:
    - `Fresh install: ./bootstrap.zsh` (preserve)
    - `Re-install / update: task install` (combined — Phase 2 D-10 makes task install IS task update)
    - `Validate: task validate` (preserve)
    - `Show tasks: task --list` (preserve)
    - Replace `Set profile: task profile:set -- personal` with: `Set machine: task setup -- <machine-name>` (v2 BTSP-04)
    - Add: `Resolve manifest: task manifest:resolve` (recomputes $XDG_STATE_HOME/dotfiles/resolved.json)
    - Add: `Show manifest: task manifest:show` (prints the post-merge structure for debugging)

    **`## Structure`** — Replace the entire v1 file list with the v2 layout. Bulleted list:
    - `Taskfile.yml` — root orchestration; includes per-concern taskfiles under `taskfiles/`; defines global vars (XDG paths, ZDOTDIR, DOTFILEDIR, HOMEBREW_PREFIX, DOTFILES_MESSAGES, RESOLVED_JSON_PATH, MANIFEST).
    - `taskfiles/<concern>.yml` — modular taskfiles (helpers, common, manifest, lint, links, shell, brew, claude, macos). See `taskfiles/README.md`.
    - `taskfiles/helpers.yml` — reusable `_:safe-link`, `_:check-link`, `_:check-dir`, `_:check-file`, `_:check-command`.
    - `manifests/defaults.toml` — shared baseline (features, default package bundles).
    - `manifests/machines/<name>.toml` — per-machine declaration (identity, features, package bundles, extra_packages). Compiled into `$XDG_STATE_HOME/dotfiles/resolved.json`.
    - `install/` — install engine machinery (resolver.zsh, messages.zsh, cutover-gate.zsh). See `install/README.md`.
    - `shell/` — flat shell layout (`.zshenv`, `.zprofile`, `.zshrc`, `.zlogin`, `.zlogout`, `theme.zsh`); aliases under `shell/aliases/<topic>.zsh`; functions under `shell/functions/<name>.zsh`. See `shell/README.md`.
    - `configs/<tool>/` — tool configs (antidote/zsh_plugins.txt today; Phase 7 moves ghostty/glow/trippy/tlrc/conda/eza/motd in).
    - `claude/` — Claude Code config (CLAUDE.md, settings.json, hooks/, agents/, commands/, skills/). See `claude/README.md`.
    - `packages/<purpose>.rb` — per-purpose Brewfile bundles (Phase 5 ships; currently transitional Brewfiles live in `install/`).
    - `os/defaults/<concern>.zsh` — macOS defaults split by concern (Phase 6).
    - `identity/git/`, `identity/ssh/` — git + ssh per-machine identity (Phase 4).

    **`## Conventions`** — Rewrite to v2:
    - All hooks are zsh scripts with `set -euo pipefail`. They use GNU grep (`ggrep`) from Homebrew.
    - Symlinks use `_:safe-link` helper which creates parent dirs automatically. No bare `ln -s` outside `helpers.yml` (LINT-03b).
    - Aliases use `$(command -v cmd)` for Homebrew tool resolution.
    - Functions: one per file, `.zsh` extension, no output on source, idempotent.
    - Path resolution: `BASH_SOURCE` / `${(%):-%N}` symlink traversal. Never hardcode repo paths.
    - Detect Homebrew prefix by `uname -m` architecture. Never hardcode `/opt/homebrew` or `/usr/local`. Use `$HOMEBREW_PREFIX` (shell context) or `{{.HOMEBREW_PREFIX}}` (task context).
    - Tasks use `status:` checks for idempotency (LINT-01). Status blocks use `{{.X}}` template vars only — never `$X` shell vars (LINT-02; the v1 macos:shell:145 bug class).
    - `install/messages.zsh` must be sourced in task commands that need colored output via `{{.DOTFILES_MESSAGES}}`.
    - Kebab-case feature keys need `index` access in go-template (`{{if index .MANIFEST.features "key"}}`). Snake_case keys can use dot access.
    - No AI attribution in commits or source.
    - No emojis in any file — markdown included.

    **`## Adding Things`** — Rewrite the table to v2 paths:
    - **Function:** `shell/functions/<name>.zsh` (filename equals function name; flat layout).
    - **Alias:** `shell/aliases/<topic>.zsh` (one topic per file; flat). Gated aliases use `_dotfiles_feature <name>` (wrapper-function gate D-07 or source-time gate D-08; see `shell/README.md`).
    - **Brew package:** Phase 5 ships `packages/<purpose>.rb`. Currently transitional Brewfiles live in `install/`.
    - **Symlink:** add `_:safe-link` entry in `taskfiles/links.yml` + matching `status:` test-L entry + `_:check-link` in `links:validate`.
    - **Tool config:** add to `configs/<tool>/`; create symlink in `taskfiles/links.yml`.
    - **Machine:** create `manifests/machines/<name>.toml` then `task setup -- <name>`.
    - **Feature flag:** add kebab-case key to `manifests/defaults.toml [features]` (default false). Enable in the relevant machine TOMLs. Consumers call `_dotfiles_feature <key>` in shell or `index .MANIFEST.features "key"` in tasks.

    **`## Zsh Startup Order`** — Rewrite to reflect v2 reality:
    1. `/etc/zshenv` — sets ZDOTDIR.
    2. `.zshenv` — XDG vars, DOTFILES_MACHINE (from `$XDG_STATE_HOME/dotfiles/machine`), EDITOR, LANG (always sourced; must be minimal — non-interactive contexts source this too).
    3. `.zprofile` — `brew shellenv`, SSH_AUTH_SOCK gated on `features.one-password-ssh` from resolved.json (login only).
    4. `.zshrc` — antidote bundle-cache, compinit daily-cache (SHEL-10), functions glob, theme, aliases glob (interactive only).
    5. `.zlogin` — MOTD display (cached, 24h TTL; SHEL-11) (login only).
    6. `.zlogout` — history flush via `fc -W` (login exit).

    **`## Safety`** — Rewrite to v2:
    - macOS is the v1 target. Linux support is deferred to v2+.
    - Only use `sudo` when required; document why.
    - Never commit private keys. `identity/ssh/keys/` contains public keys only (Phase 4).
    - Manifest is the source of truth — no hostname inference, no env-var sniffing. Feature gates always read `_dotfiles_feature <key>` (shell) or `resolved.json` via jq/fromJson (taskfiles).

    Format constraints:
    - No emojis. No AI attribution. No `Co-Authored-By` trailer at the end.
    - File-end newline.
    - All code references in backticks.
    - Less than 150 lines (the v1 file is 68 lines; the v2 rewrite is slightly longer because v2 has more structure — but still tight).
    - The file MUST NOT contain the substring `DOTFILES_PROFILE` anywhere. Same for `$DOTFILES_PROFILE`. (Truth 1 grep gate.)
    - The file MUST NOT mention "three profiles", "personal/work/server" as a profile triple, `task profile:set`, `zsh/aliases/{common,personal,work}/`, or `Brewfile-<profile>.rb` — all v1 concepts.
    - Cross-link the new READMEs from Plan 03-07: shell/README.md, install/README.md, taskfiles/README.md, claude/README.md (these are referenced in the Structure section).
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ -f .claude/CLAUDE.md ] && grep -c 'DOTFILES_PROFILE' .claude/CLAUDE.md | grep -qx 0 && grep -c 'task profile:set' .claude/CLAUDE.md | grep -qx 0 && grep -c 'DOTFILES_MACHINE' .claude/CLAUDE.md | grep -qE '^[1-9]' && grep -q 'manifests/machines/' .claude/CLAUDE.md && grep -q 'task setup --' .claude/CLAUDE.md && grep -q 'shell/aliases/' .claude/CLAUDE.md && grep -q 'shell/functions/' .claude/CLAUDE.md && grep -q 'antidote' .claude/CLAUDE.md && ! grep -q 'antigen' .claude/CLAUDE.md && grep -q '_dotfiles_feature' .claude/CLAUDE.md && grep -q '_:safe-link' .claude/CLAUDE.md && grep -q 'shell/README.md\|taskfiles/README.md\|install/README.md\|claude/README.md' .claude/CLAUDE.md && [ "$(wc -l < .claude/CLAUDE.md)" -lt 150 ] && ! grep -qE '🎯|✨|🚀|⚡|✅|❌|🔥|💡|👍|👀|📝|📦|🎉|Co-Authored|AI-generated|generated by' .claude/CLAUDE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/CLAUDE.md` exists and is materially different from pre-fix
    - `grep -c 'DOTFILES_PROFILE' .claude/CLAUDE.md` returns 0 (truth-1 grep gate satisfied repo-wide for v2-shipping files)
    - `grep -c 'task profile:set' .claude/CLAUDE.md` returns 0 (v1 command removed)
    - `grep -c '\$DOTFILES_PROFILE' .claude/CLAUDE.md` returns 0
    - `grep -c 'three profiles' .claude/CLAUDE.md` returns 0
    - `grep -c 'zsh/aliases/{common\|zsh/aliases/{common,personal,work}\|zsh/aliases/common/' .claude/CLAUDE.md` returns 0 (v1 layout reference removed)
    - `grep -c 'Brewfile-<profile>\|Brewfile-personal\|Brewfile-work\|Brewfile-server' .claude/CLAUDE.md` returns 0 (v1 profile-suffix Brewfile names not promoted)
    - `grep -c 'antigen' .claude/CLAUDE.md` returns 0 (case-insensitive too: `grep -ci antigen` returns 0)
    - `grep -c 'DOTFILES_MACHINE' .claude/CLAUDE.md` returns at least 2 (Overview + Zsh Startup Order)
    - `grep -c 'manifests/machines/' .claude/CLAUDE.md` returns at least 2 (Overview + Structure + Adding Things)
    - `grep -c 'task setup --' .claude/CLAUDE.md` returns at least 1 (Quick Reference and/or Adding Things)
    - `grep -c 'task manifest:resolve\|manifest:resolve' .claude/CLAUDE.md` returns at least 1 (Quick Reference addition)
    - `grep -c 'shell/aliases/' .claude/CLAUDE.md` returns at least 1
    - `grep -c 'shell/functions/' .claude/CLAUDE.md` returns at least 1
    - `grep -c 'antidote' .claude/CLAUDE.md` returns at least 1 (Zsh Startup Order or Structure)
    - `grep -c '_dotfiles_feature' .claude/CLAUDE.md` returns at least 1 (Adding Things → feature flag)
    - `grep -c '_:safe-link' .claude/CLAUDE.md` returns at least 1 (Conventions or Adding Things → symlink)
    - `grep -c 'shell/README.md' .claude/CLAUDE.md` returns at least 1 (Structure cross-link)
    - `grep -c 'taskfiles/README.md\|install/README.md\|claude/README.md' .claude/CLAUDE.md` returns at least 1 (cross-links to Plan 03-07 READMEs)
    - `grep -c 'resolved.json' .claude/CLAUDE.md` returns at least 2 (Structure mention + Safety/Conventions)
    - `grep -c 'features.one-password-ssh\|features\.' .claude/CLAUDE.md` returns at least 1 (Conventions → kebab-case feature flags)
    - `grep -c 'LINT-' .claude/CLAUDE.md` returns at least 1 (Conventions → status-block templating rule cites LINT-02)
    - `wc -l .claude/CLAUDE.md` reports a number less than 150
    - `wc -l .claude/CLAUDE.md` reports a number greater than 50 (substantive rewrite, not a stub)
    - No emojis: `grep -cE '[\x{1F300}-\x{1F9FF}]|🎯|✨|🚀|⚡|✅|❌|🔥|💡' .claude/CLAUDE.md` returns 0
    - No AI attribution: `grep -cE 'AI-generated|Co-Authored|generated by' .claude/CLAUDE.md` returns 0
    - The section structure is preserved: `grep -c '^## Overview\|^## Quick Reference\|^## Structure\|^## Conventions\|^## Adding Things\|^## Zsh Startup Order\|^## Safety' .claude/CLAUDE.md` returns 7 (all seven sections present)
    - File ends with exactly one newline: `tail -c 1 .claude/CLAUDE.md | xxd | head -1 | grep -q '0a'` succeeds
  </acceptance_criteria>
  <done>.claude/CLAUDE.md describes v2 reality — DOTFILES_MACHINE instead of DOTFILES_PROFILE; per-machine TOMLs instead of three profiles; flat shell/ layout; antidote not antigen; cross-links to the Plan 03-07 sibling READMEs; under 150 lines; no emojis; no AI attribution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| taskfiles/links.yml aggregator status removal | The aggregator becomes unconditional (cmds: always invoke sub-tasks). Sub-tasks are still idempotent via their own `status:` blocks; idempotency contract is preserved. Per-run cost: one extra `task status` evaluation per sub-task (sub-second, negligible). |
| .claude/CLAUDE.md content drift | Documentation file for Claude Code; no executable surface. Inaccurate content would mislead AI agents reading the doc on session start. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-25 | Denial of Service | links:all aggregator no longer short-circuits | mitigate | Removing the aggregator status: closes CR-02 (v1 macos:shell:145 bug class). Cost: aggregator always invokes both sub-tasks. Each sub-task evaluates its own `status:` block in sub-second time. Net behavior: idempotent re-runs are correct AND complete. |
| T-03-26 | Information Disclosure | .claude/CLAUDE.md v2 rewrite | accept | The rewrite cites manifest paths (`manifests/machines/<name>.toml`), feature flag names, and task command examples. No PII, no machine identifiers, no secrets. Public commit history is fine. |
| T-03-27 | Tampering | .claude/CLAUDE.md drift after rewrite | mitigate | The rewrite cross-links to `shell/README.md`, `install/README.md`, `taskfiles/README.md`, `claude/README.md`, all of which carry phase-specific traceability. Phase 8 includes a top-level docs audit that re-verifies CLAUDE.md content. |
</threat_model>

<verification>
After both tasks complete:

1. CR-02 fixed: `awk '/^  all:/{f=1;next} /^  zsh:/{f=0} f && /^    status:/{print "FOUND"; exit 1}' taskfiles/links.yml` exits 0 (no aggregator status:).
2. `task --list-all --json -t taskfiles/links.yml` exits 0; `task lint:taskfile` reports no new errors.
3. `task --list 2>&1 | grep -c 'links:all\|links:zsh\|links:antidote\|links:validate'` returns at least 4 (root task --list still resolves all four).
4. .claude/CLAUDE.md v2 rewrite verified: `grep -c 'DOTFILES_PROFILE' .claude/CLAUDE.md` returns 0; `grep -c 'DOTFILES_MACHINE' .claude/CLAUDE.md` returns at least 2.
5. Repo-wide DOTFILES_PROFILE sweep (excluding v1 zsh/ and .planning/ which are allowed to retain historical references): `grep -rn 'DOTFILES_PROFILE' --exclude-dir=.planning --exclude-dir=zsh --exclude-dir=.git --exclude-dir=node_modules . 2>/dev/null` returns NO matches.
6. .claude/CLAUDE.md cross-links to the new READMEs: `grep -cE 'shell/README\.md|taskfiles/README\.md|install/README\.md|claude/README\.md' .claude/CLAUDE.md` returns at least 1.
7. All seven section headings preserved in .claude/CLAUDE.md: `grep -cE '^## (Overview|Quick Reference|Structure|Conventions|Adding Things|Zsh Startup Order|Safety)' .claude/CLAUDE.md` returns 7.
</verification>

<success_criteria>
- CR-02 fixed: taskfiles/links.yml all: aggregator no longer has a status: block; the `# lint-allow: cmds-without-status` marker is in place; sub-task status: blocks preserved
- .claude/CLAUDE.md describes v2 reality: DOTFILES_MACHINE instead of DOTFILES_PROFILE; manifest-driven layout; antidote; flat shell/ tree; new READMEs cross-linked
- Repo-wide DOTFILES_PROFILE grep (excluding v1 zsh/ and .planning/) returns zero hits
- task --list still resolves; task lint stays green
- Phase 3 truth 1 (no DOTFILES_PROFILE) and the CR-02 anti-pattern are both fully closed
</success_criteria>

<output>
After completion, create `.planning/phases/03-shell-layer-flat-content-port/03-08-SUMMARY.md` per the template at `~/.config/claude/get-shit-done/templates/summary.md`.
</output>

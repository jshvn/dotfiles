---
phase: 03-shell-layer-flat-content-port
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - shell/.zshrc
  - taskfiles/common.yml
autonomous: true
gap_closure: true
requirements: [SHEL-03, SHEL-04, SHEL-08]

must_haves:
  truths:
    - "shell/.zshrc sources shell/functions/*.zsh BEFORE shell/aliases/*.zsh so the source-time gate in shell/aliases/jgrid.zsh:17 can call _dotfiles_feature without a 'command not found' error (closes CR-01 BLOCKER)"
    - "shell/.zshrc preserves the rest of its ordering: compinit -> DOTFILEDIR walk -> antidote -> conda lazy init -> VSCode integration -> functions glob -> theme.zsh -> aliases glob -> missing-machine warning"
    - "shell/theme.zsh continues to be sourced AFTER the functions glob and BEFORE the aliases glob (theme exports prompt vars that aliases never reference, but functions/aliaslist.zsh + theme-defined ZSH_THEME_GIT_* coexist; the safe order is functions -> theme -> aliases)"
    - "taskfiles/common.yml no longer defines the antigen-update task (lines 59-66 of pre-fix state)"
    - "taskfiles/common.yml validate: aggregator no longer references $HOMEBREW_PREFIX/share/antigen/antigen.zsh (line 105 of pre-fix state) — the antigen _:check-file invocation is deleted"
    - "After both edits, grep -rE 'antigen|antigen-update' on every non-.planning, non-zsh, non-.git path in the repo returns NO matches in files that ship with v2 (zsh/ v1 tree is untouched per Phase 3 contract — v1 stays byte-stable)"
    - "Smoke test passes: `zsh -c 'set -e; . shell/.zshenv; . shell/.zshrc' 2>&1` does NOT print 'command not found: _dotfiles_feature' (the CR-01 regression signature)"
    - "Smoke test passes (with features.jgrid-net=true in resolved.json): `alias steel` is defined after .zshrc sourcing — the v1 metal-jump aliases land on personal-laptop"
    - "task --list still resolves taskfiles/common.yml without parse errors; task validate (root aggregator) still parses (downstream invocation continues to work even though the antigen check is gone)"
  artifacts:
    - path: "shell/.zshrc"
      provides: "Interactive-shell init with corrected source order (functions before aliases)"
      contains: "shell/functions/"
    - path: "taskfiles/common.yml"
      provides: "XDG + ZDOTDIR install tasks; antigen-update task removed; validate aggregator no longer checks antigen path"
      contains: "xdg"
  key_links:
    - from: "shell/.zshrc functions glob"
      to: "shell/functions/_dotfiles_feature.zsh"
      via: "for file in shell/functions/*.zsh(.N); do source $file; done — runs BEFORE aliases glob"
      pattern: "shell/functions/"
    - from: "shell/aliases/jgrid.zsh source-time gate"
      to: "_dotfiles_feature (now defined when aliases glob runs)"
      via: "[[ \"$(_dotfiles_feature jgrid-net)\" == \"true\" ]] || return 0"
      pattern: "_dotfiles_feature jgrid-net"
---

<objective>
Close Truth 4 of 03-VERIFICATION.md by fixing the two BLOCKERs that prevent the antidote-replaces-antigen goal from being met:

1. CR-01 source-order bug: shell/.zshrc currently sources `shell/aliases/*.zsh` BEFORE `shell/functions/*.zsh`. That order breaks the D-08 source-time gate in `shell/aliases/jgrid.zsh:17` which calls `_dotfiles_feature` — a function not yet defined. Behavioral smoke-test in 03-VERIFICATION.md confirms `alias steel` is NOT defined when `features.jgrid-net=true`. The fix is to swap the two loops so functions load first.

2. Antigen residue in `taskfiles/common.yml`: lines 59-66 define an `antigen-update` task and line 105 calls `_:check-file` against `$HOMEBREW_PREFIX/share/antigen/antigen.zsh`. The Phase 3 truth-4 contract reads "antidote loads the static bundle file with no Antigen references anywhere in the repo." Both lines must go.

Purpose: This is the runtime-correctness gap. Once the order is fixed and the antigen residue is purged, the goal clause "v1 shell content fully ported" is true on jgrid-net machines, and the truth-4 contract is satisfied.

Output: 2 file edits. No new files. No taskfile-include changes. No symlink retargeting.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shell-layer-flat-content-port/03-CONTEXT.md
@.planning/phases/03-shell-layer-flat-content-port/03-VERIFICATION.md
@.planning/phases/03-shell-layer-flat-content-port/03-REVIEW.md
@.planning/phases/03-shell-layer-flat-content-port/03-02-PLAN.md
@.planning/phases/03-shell-layer-flat-content-port/03-03-PLAN.md
@.planning/phases/03-shell-layer-flat-content-port/03-04-PLAN.md
@CLAUDE.md
@shell/.zshrc
@shell/aliases/jgrid.zsh
@shell/functions/_dotfiles_feature.zsh
@taskfiles/common.yml

<interfaces>
Current shell/.zshrc layout (post-Plan-03-02 with the CR-01 bug):

- Lines 1-40: file-header comment block (preserved)
- Lines 42-57: compinit + ZSH_COMPDUMP daily-cache (preserved; SHEL-10)
- Lines 59-60: TLRC_CONFIG export (preserved)
- Lines 62-71: DOTFILEDIR symlink-walk block (preserved)
- Lines 73-87: antidote bundle-cache block (preserved; D-01..D-05)
- Lines 89-98: lazy conda init (preserved)
- Lines 100-103: VSCode shell integration (preserved)
- Lines 105-112: aliases glob loop  <-- moves AFTER functions glob + theme
- Line 115: source theme.zsh         <-- stays after functions, before aliases
- Lines 117-120: functions glob loop  <-- moves BEFORE aliases glob
- Lines 122-127: missing-machine warning (preserved)

Target order (post-fix):
... [unchanged 1-103] ...
functions glob (was 117-120)
theme.zsh source (was 115)
aliases glob (was 105-112)
... [unchanged missing-machine warning 122-127] ...

Rationale for "functions -> theme -> aliases" ordering (not "functions -> aliases -> theme"):
- theme.zsh sets ZSH_THEME_GIT_* env vars consumed by the omz-git plugin already loaded by antidote. Theme is functionally a "late init" before the user shell becomes interactive.
- No alias file references theme vars; no function file references theme vars at load time.
- The MINIMAL fix for CR-01 is to swap functions/aliases positions while keeping theme.zsh where it sits (between them). That ordering preserves Plan 03-02 Task 3 action item #9 ("Theme source: source after antidote bundle, before functions glob") in spirit — but with functions first, theme stays between functions and aliases.
- Alternative considered: keep theme as the LAST thing before missing-machine warning. Rejected — keeps the minimal diff. Reordering theme is not required to fix CR-01.

Decision: place theme.zsh source line BETWEEN functions glob and aliases glob. Preserves Plan 02's documented intent (theme after antidote, alongside the late shell init) while ensuring functions are defined before aliases source.

Current taskfiles/common.yml (post-Phase-2):

- Lines 1-12: file header + includes block (preserved)
- Lines 13-16: tasks header + XDG section banner (preserved)
- Lines 18-34: xdg task (preserved)
- Lines 36-57: zdotdir task (preserved)
- Lines 59-70: antigen-update task  <-- DELETE entirely (12 lines: 59 task name, 60 desc, 61 cmds:, 62-69 cmd body, 70 trailing blank)
- Lines 72-74: Validation section banner (preserved)
- Lines 76-101: validate task with XDG + ZDOTDIR checks (preserved)
- Lines 103-105: antigen _:check-file block  <-- DELETE the antigen sub-block (lines 103-105: comment '# Antigen', task call, and the trailing blank if any)

The remaining validate: aggregator must still parse and still contain its other checks (xdg dirs, zdotdir dir, /etc/zshenv grep, _:check-dir for ZDOTDIR). Only the antigen-specific block at the tail of the cmds: list is removed.

Note: the file currently ends mid-task at line 105 (the _:check-file antigen line) per the read. If line 105 is the last line in cmds:, dropping it cleanly truncates the validate task to the preceding ZDOTDIR-in-etc-zshenv block.

No other taskfile references antigen (lint:taskfile would have caught it). The grep gate in the smoke test below will confirm.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Swap source order in shell/.zshrc — functions glob before theme.zsh before aliases glob (CR-01 BLOCKER fix)</name>
  <files>shell/.zshrc</files>
  <read_first>
    - shell/.zshrc (full file, 128 lines — lines 105-120 are the three sections being reordered: aliases glob at 109-112, theme source at 115, functions glob at 117-120)
    - shell/aliases/jgrid.zsh (line 17 source-time gate — confirm the gate signature so the fix's smoke test asserts the right behavior)
    - shell/functions/_dotfiles_feature.zsh (full file — confirm the function name and that sourcing it under set -u does not crash)
    - .planning/phases/03-shell-layer-flat-content-port/03-VERIFICATION.md gaps section, truth 4 (CR-01 details and fix recommendation)
    - .planning/phases/03-shell-layer-flat-content-port/03-REVIEW.md CR-01 (lines 87-131 — full fix discussion with v1 ordering rationale)
    - .planning/phases/03-shell-layer-flat-content-port/03-02-PLAN.md Task 3 action items 8-10 (the original ordering decision — for context only; the fix supersedes the original order)
  </read_first>
  <action>
    Edit `shell/.zshrc` to swap the source order so functions load BEFORE aliases (the D-08 source-time gate in jgrid.zsh requires `_dotfiles_feature` to exist when aliases are sourced).

    Surgical edits (minimum diff approach):

    1. Find the three adjacent blocks at current lines 105-120:
       - Aliases glob block (currently lines 105-112): the leading comment block, the `for file in "${DOTFILEDIR}/shell/aliases/"*.zsh(.N); do source "$file"; done` loop.
       - Theme source line (currently lines 114-115): the leading comment plus `source "${DOTFILEDIR}/shell/theme.zsh"`.
       - Functions glob block (currently lines 117-120): the leading comment block, the `for file in "${DOTFILEDIR}/shell/functions/"*.zsh(.N); do source "$file"; done` loop.

    2. Reorder them to: functions glob -> theme source -> aliases glob (preserving every line of content; only the block positions change).

    3. Update the leading comment on the functions glob block to read: `# Load functions FIRST (flat layout; one function per file). Source-time gates in shell/aliases/*.zsh (D-08, e.g. jgrid.zsh) call _dotfiles_feature, so the helper function must be defined before the aliases glob runs.`

    4. Update the leading comment on the aliases glob block to read: `# Load aliases SECOND (flat layout; per-file source-time or wrapper-function gates handle features per D-09). Functions glob above has already defined _dotfiles_feature, so D-08 source-time gates evaluate correctly.`

    5. The leading comment on the theme.zsh source line stays as it is (`# load common ZSH custom themes`) — its position has changed but its semantics have not.

    6. Preserve every other line in the file verbatim — file header, compinit block, TLRC_CONFIG export, DOTFILEDIR walk, antidote block, lazy conda init, VSCode integration, missing-machine warning. NO other changes.

    Do NOT add a defensive `(( $+functions[_dotfiles_feature] ))` guard to jgrid.zsh (WR-01) — out of scope for this gap closure; tracked separately. The CR-01 fix is the source-order swap, period.

    Do NOT touch any other shell/ file. shell/aliases/jgrid.zsh is unchanged by this plan.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && zsh -n shell/.zshrc && awk '/for file in.*shell\/functions/{f=NR} /shell\/theme\.zsh/{t=NR} /for file in.*shell\/aliases/{a=NR} END{exit (f < t && t < a) ? 0 : 1}' shell/.zshrc && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `zsh -n shell/.zshrc` exits 0 (file still parses)
    - Order check: `awk '/for file in.*shell\/functions/{f=NR} /shell\/theme\.zsh/{t=NR} /for file in.*shell\/aliases/{a=NR} END{print f" "t" "a; exit (f < t && t < a) ? 0 : 1}' shell/.zshrc` exits 0 — functions glob line precedes theme.zsh line which precedes aliases glob line
    - `grep -c 'for file in "\${DOTFILEDIR}/shell/functions/"\*\.zsh' shell/.zshrc` returns 1 (functions glob preserved verbatim except position)
    - `grep -c 'for file in "\${DOTFILEDIR}/shell/aliases/"\*\.zsh' shell/.zshrc` returns 1 (aliases glob preserved verbatim except position)
    - `grep -c 'source "\${DOTFILEDIR}/shell/theme\.zsh"' shell/.zshrc` returns 1 (theme source preserved)
    - `grep -c '(\.N)' shell/.zshrc` returns at least 2 (nullglob qualifier preserved on both globs)
    - `grep -c 'DOTFILES_MACHINE' shell/.zshrc` returns at least 1 (missing-machine warning preserved)
    - `grep -c 'antidote bundle' shell/.zshrc` returns at least 1 (antidote block preserved)
    - `grep -c 'compinit -C -d\|compinit -d' shell/.zshrc` returns at least 2 (SHEL-10 daily-cache preserved)
    - `head -1 shell/.zshrc` is `#!/bin/zsh` (shebang preserved)
    - Wave-test (full smoke): `zsh -c 'XDG_STATE_HOME=/nonexistent; . shell/.zshenv; . shell/.zshrc' 2>&1 | grep -c "command not found: _dotfiles_feature"` returns 0 — even with no resolved.json the source order ensures _dotfiles_feature exists when aliases glob runs
    - `wc -l shell/.zshrc` reports approximately 128 (no significant size change; small drift from comment-text rewording is acceptable)
  </acceptance_criteria>
  <done>shell/.zshrc sources functions before aliases (theme.zsh between them); zsh -n passes; smoke test confirms _dotfiles_feature is defined when aliases glob runs; no other lines changed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete antigen residue from taskfiles/common.yml — drop antigen-update task and the antigen check in validate: aggregator</name>
  <files>taskfiles/common.yml</files>
  <read_first>
    - taskfiles/common.yml (full file, 106 lines — lines 59-70 are the antigen-update task; lines 103-105 are the antigen _:check-file block at the tail of the validate task)
    - .planning/phases/03-shell-layer-flat-content-port/03-VERIFICATION.md gaps section, truth 4 (CR-01 part 2 — the antigen residue)
    - .planning/phases/03-shell-layer-flat-content-port/03-REVIEW.md Anti-Patterns table row for taskfiles/common.yml (lines 173-174 in review: "Antigen task + symlink validation survives — BLOCKER (contributes to truth 4)")
    - .planning/phases/03-shell-layer-flat-content-port/03-CONTEXT.md `<decisions>` — SHEL-04 / D-01..D-05 (antidote replaces antigen)
    - REQUIREMENTS.md SHEL-04 ("Antidote replaces Antigen as the plugin manager")
    - taskfiles/lint.yml (confirm no lint rule will newly fire when the antigen-update task disappears — there is none; antigen-update is not referenced by any aggregator outside common.yml itself)
    - Taskfile.yml (confirm no top-level task calls common:antigen-update — if it does, this plan needs an additional edit to remove that call; verify before editing)
  </read_first>
  <action>
    Two surgical deletions in `taskfiles/common.yml`. No other modifications.

    Deletion 1 — drop the entire `antigen-update` task (currently lines 59-70):

    Delete every line in the block:
    - Line 59: `  antigen-update:`
    - Line 60: `    desc: "Update Antigen plugins"`
    - Line 61: `    cmds:`
    - Lines 62-69: the multi-line cmds: body (DOTFILES_MESSAGES, BREW_SHELLENV, the if-block that sources antigen.zsh and runs `antigen update`, the else branch warn)
    - Line 70: trailing blank line if present, OR keep one blank line between the zdotdir: task and the Validation section banner (whichever maintains the existing visual separator pattern in the file — match the spacing between xdg: and zdotdir: above)

    After this deletion the file flows: ZDG section -> xdg: task -> zdotdir: task -> Validation section banner -> validate: task. The "antigen-update" task is gone entirely; no stub, no commented-out lines.

    Deletion 2 — drop the antigen check from the `validate:` aggregator (currently lines 103-105):

    Delete the antigen sub-block at the tail of the validate task's cmds: list. Concretely:
    - Line 103: `      # Antigen`
    - Line 104: `      - task: _:check-file`
    - Line 105: `        vars: { TARGET: "{{.HOMEBREW_PREFIX}}/share/antigen/antigen.zsh", NAME: "Antigen" }`

    Preserve every other cmds: entry in the validate task: the XDG check-dir calls (lines 80-87), the ZDOTDIR check-dir (lines 90-91), and the ZDOTDIR-in-/etc/zshenv grep block (lines 94-101).

    After deletion, the validate: task's cmds: list ends at line 101 (the `cross "ZDOTDIR not configured in /etc/zshenv"` else branch's closing `fi`). Trim any newly-trailing whitespace at the file end (the file should end with a final newline; no double-blank lines).

    The validate: task's desc: should be updated to remove the trailing parenthetical reference to Antigen. Current line 77: `    desc: "Validate common components (XDG, ZDOTDIR, Antigen)"`. Rewrite to: `    desc: "Validate common components (XDG, ZDOTDIR)"`.

    Do NOT change:
    - The includes: block (line 11).
    - The tasks: declaration (line 13).
    - The xdg: task (lines 18-34).
    - The zdotdir: task (lines 36-57).
    - The XDG Base Directories section banner (lines 15-17).
    - The Validation section banner (lines 72-74).
    - Any line in the validate: task's preserved cmds: entries.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && task --list-all --json -t taskfiles/common.yml > /dev/null && [ "$(grep -c 'antigen' taskfiles/common.yml)" = "0" ] && [ "$(grep -c 'antigen-update' taskfiles/common.yml)" = "0" ] && grep -q '^  xdg:' taskfiles/common.yml && grep -q '^  zdotdir:' taskfiles/common.yml && grep -q '^  validate:' taskfiles/common.yml && grep -q 'desc: "Validate common components (XDG, ZDOTDIR)"' taskfiles/common.yml && task --list-all -t taskfiles/common.yml 2>&1 | grep -E '\* (xdg|zdotdir|validate):' | wc -l | grep -qx 3 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `task --list-all --json -t taskfiles/common.yml` exits 0 (file still parses)
    - `grep -c 'antigen' taskfiles/common.yml` returns 0 (no antigen reference whatsoever — case-insensitive expectation: `grep -ci antigen` also returns 0)
    - `grep -c 'antigen-update' taskfiles/common.yml` returns 0 (task removed)
    - `grep -c 'HOMEBREW_PREFIX.*antigen\|antigen\.zsh' taskfiles/common.yml` returns 0 (validate check removed)
    - `grep -c '^  xdg:' taskfiles/common.yml` returns 1 (xdg task preserved)
    - `grep -c '^  zdotdir:' taskfiles/common.yml` returns 1 (zdotdir task preserved)
    - `grep -c '^  validate:' taskfiles/common.yml` returns 1 (validate task preserved)
    - `grep -c 'desc: "Validate common components (XDG, ZDOTDIR)"' taskfiles/common.yml` returns 1 (desc updated to drop the Antigen mention)
    - `task --list-all -t taskfiles/common.yml 2>&1 | grep -E '^\* (xdg|zdotdir|validate):' | wc -l` returns 3 (exactly three tasks remain: xdg, zdotdir, validate — antigen-update gone)
    - The preserved validate cmds: list still calls `_:check-dir` for the four XDG directories and ZDOTDIR: `grep -c '_:check-dir' taskfiles/common.yml` returns 5
    - The ZDOTDIR-in-/etc/zshenv check block survives: `grep -c '/etc/zshenv' taskfiles/common.yml` returns at least 4 (zdotdir task uses it + validate task checks it; pre-fix count was 5; post-fix should be 4 because the antigen residue did not contain that string — confirm against actual count)
    - Project-wide antigen sweep (gap-closure success criterion): `grep -rn 'antigen' --include='*.yml' --include='*.zsh' --include='*.md' --include='*.toml' --include='*.rb' --exclude-dir=.planning --exclude-dir=zsh --exclude-dir=.git . 2>/dev/null` returns NO matches (excluding the v1 zsh/ tree which Phase 3 contract leaves byte-stable, and excluding .planning/ which is allowed to reference antigen historically)
  </acceptance_criteria>
  <done>taskfiles/common.yml is antigen-free: antigen-update task deleted; validate aggregator no longer checks for antigen.zsh; desc string updated; file parses; three tasks remain (xdg, zdotdir, validate); repo-wide grep for 'antigen' returns nothing outside the v1 zsh/ tree and .planning/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell/.zshrc source order -> alias source-time gate | The reorder ensures `_dotfiles_feature` exists when `shell/aliases/jgrid.zsh:17` runs. No new trust surface; the gate itself remains the manifest-vs-runtime trust seam unchanged from Plan 03-03. |
| taskfiles/common.yml validate aggregator | The antigen check is removed. The remaining XDG + ZDOTDIR checks operate on local paths only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Denial of Service | shell/.zshrc source-order fix | mitigate | Reordering functions before aliases ensures the v2 stack does NOT silently drop the jgrid metal-jump aliases on personal-laptop. Pre-fix: source-time gate evaluates to false-via-error, aliases disappear, user must SSH the long way. Post-fix: aliases land as designed. |
| T-03-22 | Tampering | antigen-update task removal | accept | The deletion is repo-committed. No user-facing entry point referenced antigen-update outside common.yml; removing it does not break any aggregator. Worst case for a user mid-cutover: they had a personal habit of running `task common:antigen-update` (no evidence in STATE.md or PROJECT.md that anyone did) -- they now get a "task not found" error and switch to `task install`. |
</threat_model>

<verification>
After both tasks complete:

1. `zsh -n shell/.zshrc` exits 0.
2. Order assertion: `awk '/for file in.*shell\/functions/{f=NR} /shell\/theme\.zsh/{t=NR} /for file in.*shell\/aliases/{a=NR} END{exit (f < t && t < a) ? 0 : 1}' shell/.zshrc` exits 0.
3. Source-time-gate smoke test (negative path — feature off): `XDG_STATE_HOME=/nonexistent zsh -c '. shell/.zshenv; . shell/.zshrc 2>&1' | grep -c "command not found: _dotfiles_feature"` returns 0 — no "command not found" stderr leaks even when resolved.json is absent.
4. Source-time-gate smoke test (positive path — feature on): create a temp `$XDG_STATE_HOME/dotfiles/resolved.json` with `{"features":{"jgrid-net":true}}`, source the stack, assert `alias steel` is defined. Specifically: `STATE=$(mktemp -d); mkdir -p "$STATE/dotfiles"; echo '{"features":{"jgrid-net":true}}' > "$STATE/dotfiles/resolved.json"; XDG_STATE_HOME="$STATE" zsh -c '. shell/.zshenv; . shell/.zshrc; alias steel' 2>&1 | grep -q "ssh josh@steel-ssh.jgrid.net" && echo PASS` outputs PASS.
5. `task --list-all --json -t taskfiles/common.yml` exits 0.
6. `task --list-all -t taskfiles/common.yml 2>&1 | grep -cE '^\* (xdg|zdotdir|validate):'` returns 3.
7. `grep -rIn 'antigen' --include='*.yml' --include='*.zsh' --include='*.md' --include='*.toml' --include='*.rb' --exclude-dir=.planning --exclude-dir=zsh --exclude-dir=.git --exclude-dir=node_modules .` returns NO matches.
</verification>

<success_criteria>
- CR-01 fixed: shell/.zshrc sources functions before aliases (theme between them); zsh -n passes
- Antigen residue purged: taskfiles/common.yml has no antigen-update task and validate aggregator no longer checks antigen.zsh
- Source-time-gate smoke test passes: `alias steel` is defined when features.jgrid-net=true
- Project-wide antigen grep (excluding v1 zsh/ and .planning/) returns zero matches
- task --list-all parses both edited files
- Phase 3 truth 4 ("antidote replaces antigen with no antigen references anywhere in the repo") now passes goal-backward verification
</success_criteria>

<output>
After completion, create `.planning/phases/03-shell-layer-flat-content-port/03-06-SUMMARY.md` per the template at `~/.config/claude/get-shit-done/templates/summary.md`.
</output>

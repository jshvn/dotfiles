---
phase: 03-shell-layer-flat-content-port
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - install/README.md
  - taskfiles/README.md
  - claude/README.md
autonomous: true
gap_closure: true
requirements: [DOCS-02]

must_haves:
  truths:
    - "install/README.md exists at the top of install/ — purpose, key files (resolver.zsh, messages.zsh, cutover-gate.zsh, Brewfile*.rb), how-to-add a script (closes the DOCS-02 contract for install/)"
    - "taskfiles/README.md exists at the top of taskfiles/ — purpose, file-per-concern convention, how-to-add-a-taskfile-and-wire-the-include-from-Taskfile.yml (closes the DOCS-02 contract for taskfiles/)"
    - "claude/README.md exists at the top of claude/ — purpose, key subdirectories (hooks/, agents/, commands/, skills/), how-to-add (with a forward pointer that Phase 7 owns the install task and hook smoke tests per CLDE-01..CLDE-04)"
    - "Every README follows the shell/README.md template shape (PATTERNS.md Pattern 22): top-level heading, purpose paragraph, '## Key files' (or '## Key subdirectories' for claude/), '## Adding a pattern', '## References' — terse and scoped to match manifests/README.md energy"
    - "Each README is fewer than 100 lines (terse-and-scoped per DOCS-02 contract)"
    - "No emojis in any README (project convention is stricter than the global no-emojis-in-non-md rule)"
    - "No AI attribution in any README (no 'generated by', no 'Co-Authored-By', no AI-tooling references)"
    - "git/README.md and ssh/README.md are NOT created — those directories are owned by Phase 4 (IDNT-*) per 03-VERIFICATION.md deferred section"
    - "After this plan ships, DOCS-02 truth 6 ('every top-level directory has a README.md') is satisfied for the directories that ship in Phase 3 scope (shell/, manifests/, configs/, packages/, docs/, os/, install/, taskfiles/, claude/)"
  artifacts:
    - path: "install/README.md"
      provides: "DOCS-02 contract anchor for install/ — purpose, key files, adding-a-script"
      contains: "resolver.zsh"
    - path: "taskfiles/README.md"
      provides: "DOCS-02 contract anchor for taskfiles/ — purpose, file-per-concern, adding-a-new-taskfile"
      contains: "Taskfile.yml"
    - path: "claude/README.md"
      provides: "DOCS-02 contract anchor for claude/ — purpose, subdirectories, deferral note for Phase 7"
      contains: "hooks"
  key_links:
    - from: "install/README.md, taskfiles/README.md, claude/README.md"
      to: "shell/README.md (the anchor template)"
      via: "structural parity — same five sections, same ordering, same terseness budget"
      pattern: "## Key files\\|## Adding a pattern\\|## References"
    - from: "install/README.md"
      to: "install/resolver.zsh + install/messages.zsh + install/cutover-gate.zsh"
      via: "key-files section names each file with a one-line role description"
      pattern: "resolver.zsh"
    - from: "taskfiles/README.md"
      to: "Taskfile.yml (root)"
      via: "adding-a-pattern explains the includes: block + lint rules"
      pattern: "Taskfile.yml"
---

<objective>
Close Truth 6 of 03-VERIFICATION.md by writing the three missing sibling READMEs the verifier flagged: `install/README.md`, `taskfiles/README.md`, and `claude/README.md`. DOCS-02 says "each top-level directory has a README.md"; shell/README.md (from Plan 03-05) is the DOCS-02 anchor; this plan replicates that shape across the three remaining v2-ship directories that lack a README.

Out of scope: `git/README.md` and `ssh/README.md` — those directories are owned by Phase 4 (IDNT-*) per the 03-VERIFICATION.md deferred section. Plan 03-07 does NOT create them.

Purpose: The DOCS-02 contract is a discoverability contract — a new contributor or AI agent landing in the repo should be able to read each top-level README and understand the purpose, key files, and how-to-add-pattern for that directory. The pattern was established by shell/README.md; this plan propagates it.

Output: 3 new README.md files. No code edits. No taskfile changes. No symlinks.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shell-layer-flat-content-port/03-CONTEXT.md
@.planning/phases/03-shell-layer-flat-content-port/03-VERIFICATION.md
@.planning/phases/03-shell-layer-flat-content-port/03-05-PLAN.md
@CLAUDE.md
@shell/README.md
@manifests/README.md
@configs/README.md
@packages/README.md
@os/README.md
@docs/README.md
@identity/README.md

<interfaces>
DOCS-02 contract anchor (shell/README.md, Plan 03-05):

- Top-level heading `# <dir>`
- 2-3 sentence purpose paragraph (what the directory contains, who sources/uses it, any scope notes)
- `## Key files` subsection — bulleted list with one bullet per major file or sub-tree, each bullet a 1-2-line role description
- `## Adding a pattern` subsection — three (or fewer) bullets covering the common "how do I add an X" workflows for this directory
- Optional thematic subsection (shell/ has `## Performance budget`; identity/, configs/, packages/ have phase-specific notes) — only if a quantitative or scoped constraint is binding for the directory
- `## References` subsection — bulleted list of pointers (relative paths) to related docs (../docs/MANIFEST.md, ../CLAUDE.md, ../.planning/REQUIREMENTS.md as appropriate)

Formatting rules from CLAUDE.md and project conventions:

- Markdown only; no HTML
- No emojis (project convention; stricter than the global no-emojis-in-non-md rule)
- No AI attribution (no "Co-Authored-By", no "generated by")
- File-end newline; no trailing whitespace
- Code fences use language tags (zsh, toml, yaml) when content is shown
- Less than 100 lines per README (terse-and-scoped per manifests/README.md energy)

Directory contents (confirmed via `ls`):

install/:
  Brewfile.rb, Brewfile-personal.rb, Brewfile-server.rb, Brewfile-work.rb
  cutover-gate.zsh, messages.zsh, resolver.zsh

taskfiles/:
  brew.yml, brew-stub.yml, claude.yml, claude-stub.yml, common.yml,
  helpers.yml, lint.yml, links.yml, links-stub.yml, macos.yml, macos-stub.yml,
  manifest.yml, profile.yml, profile-tasks.yml, shell.yml, test/

claude/:
  agents/, commands/, CLAUDE.md, hooks/, settings.json, skills/

Phase ownership notes (for the README content):

- install/ ships with Phase 1 (resolver.zsh, messages.zsh) and Phase 2 (cutover-gate.zsh, bootstrap.zsh — bootstrap lives at repo root, not install/, but install/ is the "install engine machinery" directory). The Brewfile-*.rb files are V1 leftovers per Phase 5 plan; Phase 5 will reshape them as packages/<purpose>.rb. install/README.md notes the v1->v2 transition.
- taskfiles/ ships content from every phase: Phase 1 (manifest.yml), Phase 2 (lint.yml, helpers.yml, the *-stub.yml files), Phase 3 (links.yml, shell.yml, common.yml), Phase 4-7 fill in brew.yml / macos.yml / claude.yml / etc. The directory will keep growing; the README's "adding a taskfile" instructions cover the include-block wiring in Taskfile.yml.
- claude/ ships v1 content (hooks, agents, commands, skills). Phase 7 (CLDE-01..CLDE-04) owns the install path. claude/README.md is forward-pointing — it documents WHAT the subdirectories contain but defers the task wiring and hook smoke tests to Phase 7.

DOCS-02 cross-link (so each new README cites the contract):

The DOCS-02 contract reads "Each top-level directory has a README.md covering purpose, key files, and how-to-add patterns." Each new README in this plan satisfies that contract for its directory. A small footer in each README ("Satisfies DOCS-02 for `<dir>/`.") is sufficient cross-reference.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write install/README.md — DOCS-02 anchor for the install/ directory (resolver, messages, cutover-gate, Brewfile transitional state)</name>
  <files>install/README.md</files>
  <read_first>
    - install/resolver.zsh (top 20 lines — confirm purpose/file-header for the role description)
    - install/messages.zsh (top 20 lines — confirm purpose for the role description)
    - install/cutover-gate.zsh (top 20 lines — confirm purpose for the role description; Phase 2 BTSP-06 / D-12 artifact)
    - install/Brewfile.rb (first 5 lines — confirm v1-style file header; the file is the v1 common Brewfile that Phase 5 will refactor into packages/<purpose>.rb)
    - install/Brewfile-personal.rb / install/Brewfile-server.rb / install/Brewfile-work.rb (just confirm existence — these are v1 profile-suffixed files Phase 5 will retire)
    - shell/README.md (template structure to mirror)
    - manifests/README.md (sibling-README pattern; terseness budget)
    - REQUIREMENTS.md DOCS-02 (the contract this README satisfies)
    - .planning/phases/03-shell-layer-flat-content-port/03-VERIFICATION.md gaps section, truth 6 missing item: "install/README.md (purpose, key files: resolver.zsh + messages.zsh, how-to-add a script)"
    - CLAUDE.md "Adding Things" table — for the "how to add a script under install/" instructions
  </read_first>
  <action>
    Create `install/README.md`. Follow the shell/README.md template shape exactly — same five sections, same ordering, same terseness target (less than 100 lines).

    Required sections:

    1. **Top-level heading**: `# install`

    2. **Purpose paragraph** (2-3 sentences): Install-engine machinery — the scripts and helpers `bootstrap.zsh` (repo root) and `task install` (root Taskfile.yml) call into. Hosts the manifest resolver, the messages library every taskfile sources, and the cutover-gate precondition that blocks `task install` until a machine is explicitly selected. Phase 5 will move the Brewfile bundles out of this directory into `packages/<purpose>.rb`.

    3. **Key files** subsection (`## Key files`): bulleted list:
       - `resolver.zsh` — Phase 1 (MFST-04). Compiles `manifests/defaults.toml` plus the active machine's `manifests/machines/<name>.toml` into `$XDG_STATE_HOME/dotfiles/resolved.json` via yq + a deep-merge expression. Every downstream task reads `resolved.json` via go-task `fromJson`, never TOML directly.
       - `messages.zsh` — Phase 1 baseline. Colored-output library exposing `info`, `success`, `warn`, `error`, `check`, `cross`. Sourced by task `cmds:` blocks via the `{{.DOTFILES_MESSAGES}}` template var. Idempotent — exports a `DOTFILES_MESSAGES_LOADED` guard.
       - `cutover-gate.zsh` — Phase 2 (BTSP-06 / D-12). Reads `$XDG_STATE_HOME/dotfiles/machine` and exits non-zero with an actionable error when no machine is selected. Invoked as a precondition in `Taskfile.yml` so every `task install` run starts from an explicit machine identity.
       - `Brewfile.rb`, `Brewfile-personal.rb`, `Brewfile-server.rb`, `Brewfile-work.rb` — V1 transitional state. Phase 5 (PKGS-01..05) will refactor these into per-purpose bundles under `packages/<purpose>.rb` (core, gui, dev, ops, personal). Do NOT add new profile-suffixed Brewfiles here.

    4. **Adding a pattern** subsection (`## Adding a pattern`): bullets:
       - **A new install-engine script.** Create `install/<name>.zsh`. Start the file with the standard shebang plus `set -euo pipefail` (the file IS executable; LINT-04 enforces). Add a 5-10 line file-header comment block per CLAUDE.md convention naming the script's purpose, callers, and side effects. Wire the script into a task by adding a `cmds:` entry in the appropriate `taskfiles/<concern>.yml` (typically referenced as `{{.DOTFILEDIR}}/install/<name>.zsh`).
       - **A new task-helper function.** Prefer adding it to `messages.zsh` if it produces user-facing output; otherwise create a new file under `install/` with the same conventions.
       - **A new Brew package.** Do NOT edit the Brewfiles here in Phase 3 scope — wait for Phase 5. Once Phase 5 lands, add packages to `packages/<purpose>.rb` (named by role, not by profile).

    5. **References** subsection (`## References`): bulleted list:
       - `../docs/MANIFEST.md` — manifest schema and deep-merge semantics (consumed by `resolver.zsh`).
       - `../docs/SECURITY.md` — bootstrap trust chain (Phase 2 BTSP-05).
       - `../taskfiles/manifest.yml` — the `manifest:resolve` / `manifest:validate` / `manifest:test` tasks that call `resolver.zsh`.
       - `../CLAUDE.md` — v2 conventions (script header convention, `set -euo pipefail`, no AI attribution).
       - `../.planning/REQUIREMENTS.md` — MFST-04, BTSP-01..06, DOCS-02 traceability.

    6. **Footer line**: `Satisfies DOCS-02 for install/.`

    Format constraints:
    - File-end newline; no trailing whitespace on any line.
    - No emojis (project convention).
    - No AI attribution (no "generated by", no "Co-Authored-By").
    - All file references use backticks (e.g., `resolver.zsh`).
    - Less than 100 lines total.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ -f install/README.md ] && head -1 install/README.md | grep -qx '# install' && grep -q '## Key files' install/README.md && grep -q '## Adding a pattern' install/README.md && grep -q '## References' install/README.md && grep -q 'resolver.zsh' install/README.md && grep -q 'messages.zsh' install/README.md && grep -q 'cutover-gate.zsh' install/README.md && grep -q 'DOCS-02' install/README.md && [ "$(wc -l < install/README.md)" -lt 100 ] && ! grep -qE '🎯|✨|🚀|⚡|✅|❌|🔥|💡|👍|👀|📝|📦|🎉|Co-Authored|AI-generated|generated by' install/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `install/README.md` exists
    - `head -1 install/README.md` is exactly `# install`
    - `grep -c '## Key files' install/README.md` returns 1
    - `grep -c '## Adding a pattern' install/README.md` returns 1
    - `grep -c '## References' install/README.md` returns 1
    - `grep -c 'resolver.zsh' install/README.md` returns at least 2 (key-files bullet + references-to-manifest task that calls it)
    - `grep -c 'messages.zsh' install/README.md` returns at least 1
    - `grep -c 'cutover-gate.zsh' install/README.md` returns at least 1
    - `grep -c 'Brewfile' install/README.md` returns at least 1 (transitional-state explanation)
    - `grep -c 'packages/' install/README.md` returns at least 1 (Phase 5 forward pointer)
    - `grep -c 'DOCS-02' install/README.md` returns at least 1 (contract traceability — likely in references or footer)
    - `grep -c 'Satisfies DOCS-02 for install/' install/README.md` returns 1 (footer)
    - `wc -l install/README.md` reports a number less than 100
    - `wc -l install/README.md` reports a number greater than 25 (substantive, not a stub)
    - No emojis: `grep -cE '[\x{1F300}-\x{1F9FF}]|🎯|✨|🚀|⚡|✅|❌|🔥|💡' install/README.md` returns 0
    - No AI attribution: `grep -cE 'AI-generated|Co-Authored|generated by' install/README.md` returns 0
    - File ends with exactly one newline: `tail -c 1 install/README.md | xxd | head -1 | grep -q '0a'` succeeds
  </acceptance_criteria>
  <done>install/README.md is a DOCS-02 anchor with purpose + key files (resolver, messages, cutover-gate, Brewfile transitional) + adding-a-script pattern + references; under 100 lines; no emojis; no AI attribution.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write taskfiles/README.md — DOCS-02 anchor for the taskfiles/ directory (file-per-concern, include wiring, lint rules)</name>
  <files>taskfiles/README.md</files>
  <read_first>
    - taskfiles/ directory listing (16 files: brew.yml, brew-stub.yml, claude.yml, claude-stub.yml, common.yml, helpers.yml, lint.yml, links.yml, links-stub.yml, macos.yml, macos-stub.yml, manifest.yml, profile.yml, profile-tasks.yml, shell.yml, test/)
    - taskfiles/manifest.yml (top 30 lines — confirm includes pattern and file-header convention)
    - taskfiles/links.yml (top 35 lines — Plan 03-05 file-header documenting status-block + symlink-creation conventions; useful as a model for what taskfiles READMEs should say about lint rules)
    - taskfiles/helpers.yml (top 20 lines — the `_:safe-link`, `_:check-link` helpers documented here)
    - taskfiles/lint.yml (first 20 lines — confirm the LINT-* rule numbers cited in the README)
    - Taskfile.yml (lines 70-90 — the includes: block to reference in the "adding a taskfile" workflow)
    - shell/README.md (template structure)
    - manifests/README.md (sibling-README pattern)
    - REQUIREMENTS.md DOCS-02
    - REQUIREMENTS.md LINT-01..LINT-08 (rule numbers cited in the adding-a-pattern section)
    - .planning/phases/03-shell-layer-flat-content-port/03-VERIFICATION.md gaps section, truth 6 missing item: "taskfiles/README.md (purpose, file-per-concern convention, how-to-add a new taskfile and include it from Taskfile.yml)"
    - CLAUDE.md "Adding Things" table — for the "symlink" and "taskfile" rows
    - CLAUDE.md "Conventions Not Captured Above" — file-level comment block, section separators, errors-to-stderr
  </read_first>
  <action>
    Create `taskfiles/README.md`. Mirror the shell/README.md shape.

    Required sections:

    1. **Top-level heading**: `# taskfiles`

    2. **Purpose paragraph** (2-3 sentences): Modular taskfile concerns wired into `Taskfile.yml` via go-task `includes:`. One taskfile per concern: manifest, lint, links, shell, brew, macos, claude, common, profile, helpers. Every install-style task is idempotent (`status:` block) and every symlink goes through `_:safe-link` in `helpers.yml`. The `*-stub.yml` files are intentional Phase 2 placeholders that lint-fixture tests still reference; the matching real `<concern>.yml` files land as each phase ships its content.

    3. **Key files** subsection (`## Key files`): bulleted list. Group by maturity:
       - **Helpers and shared library.** `helpers.yml` — reusable `_:safe-link`, `_:check-link`, `_:check-dir`, `_:check-file`, `_:check-command`. Every other taskfile includes it via `includes: _: ./helpers.yml`. Always include this; never bypass with bare `ln -s` (LINT-03b enforces).
       - **Phase 1 (real).** `manifest.yml` — `task setup -- <machine>`, `manifest:resolve`, `manifest:validate`, `manifest:show`, `manifest:test`, `manifest:test:add-machine`. Reads TOMLs; writes `resolved.json`.
       - **Phase 2 (real).** `lint.yml` — `lint:taskfile`, `lint:shell-headers`, `lint:portability`, `lint:syntax`, `lint:test-fixtures`. Enforces LINT-01..LINT-07.
       - **Phase 3 (real).** `common.yml` — XDG + ZDOTDIR install. `links.yml` — shell + antidote symlinks via `_:safe-link`. `shell.yml` — `task perf:shell` (SHEL-12 cold-start gate via hyperfine). `profile.yml`, `profile-tasks.yml` — v1 leftovers retained for the cutover window; Phase 8 retires them.
       - **Phase 4-7 (stubs).** `brew-stub.yml`, `claude-stub.yml`, `macos-stub.yml`, `links-stub.yml` — Phase 2 placeholders. Each phase that owns the concern replaces the stub with a real `<concern>.yml` and flips the `Taskfile.yml` include from `<concern>-stub.yml` to `<concern>.yml`.
       - **Smoke-test fixtures.** `test/` — lint-fixture taskfiles consumed by `task lint:test-fixtures` (Phase 2 D-08).

    4. **Adding a pattern** subsection (`## Adding a pattern`):
       - **A new taskfile.** Create `taskfiles/<concern>.yml` starting with `version: '3'` and a file-header banner (`# ====`-style separator). Add `includes: _: ./helpers.yml`. Every install-style `task` MUST have a `status:` block (LINT-01) using `{{.X}}` template vars only — never `$X` shell vars (LINT-02 catches this; the v1 `macos:shell:145` regression class). For diagnostic-only tasks (validate, perf, etc.) that re-run by design, add an inline `# lint-allow: cmds-without-status` comment ABOVE the task name (LINT-03a exemption).
       - **Wiring the taskfile into the root Taskfile.yml.** Add a line to the `includes:` block in `Taskfile.yml` (alphabetical by namespace alias). Update the `includes:` comment-table at the top of `Taskfile.yml` to add a `#   - <alias>   (P<n>, real)` line. The first invocation from the root namespace then becomes `task <alias>:<task>` (e.g., `task perf:shell`).
       - **A new symlink.** Add a `_:safe-link` invocation to `links.yml` (or the appropriate links subtask) with `SOURCE` and `TARGET` template vars resolved from root `Taskfile.yml`. Add a matching `test -L` line to that task's `status:` block. Add a `_:check-link` invocation to `links.yml`'s `validate:` task for diagnostic output. NEVER use bare `ln -s` outside `helpers.yml` (LINT-03b).

    5. **References** subsection (`## References`): bulleted list:
       - `../Taskfile.yml` — root taskfile, includes block, install/update unification.
       - `helpers.yml` — symlink helpers.
       - `../docs/MANIFEST.md` — manifest schema; many tasks consume `resolved.json` via `fromJson`.
       - `../CLAUDE.md` — v2 conventions (status-block templating, no bare `ln -s`, `set -euo pipefail` on every executable `.zsh`).
       - `../.planning/REQUIREMENTS.md` — LINT-01..LINT-07, DOCS-02 traceability.

    6. **Footer line**: `Satisfies DOCS-02 for taskfiles/.`

    Format constraints:
    - File-end newline; no trailing whitespace.
    - No emojis. No AI attribution.
    - All file/path references in backticks.
    - Less than 100 lines.
    - Code blocks (if any) use yaml or zsh language tags.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ -f taskfiles/README.md ] && head -1 taskfiles/README.md | grep -qx '# taskfiles' && grep -q '## Key files' taskfiles/README.md && grep -q '## Adding a pattern' taskfiles/README.md && grep -q '## References' taskfiles/README.md && grep -q 'helpers.yml' taskfiles/README.md && grep -q '_:safe-link' taskfiles/README.md && grep -q 'status:' taskfiles/README.md && grep -q 'LINT-' taskfiles/README.md && grep -q 'DOCS-02' taskfiles/README.md && grep -q 'Taskfile.yml' taskfiles/README.md && [ "$(wc -l < taskfiles/README.md)" -lt 100 ] && ! grep -qE '🎯|✨|🚀|⚡|✅|❌|🔥|💡|👍|👀|📝|📦|🎉|Co-Authored|AI-generated|generated by' taskfiles/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `taskfiles/README.md` exists
    - `head -1 taskfiles/README.md` is exactly `# taskfiles`
    - `grep -c '## Key files' taskfiles/README.md` returns 1
    - `grep -c '## Adding a pattern' taskfiles/README.md` returns 1
    - `grep -c '## References' taskfiles/README.md` returns 1
    - `grep -c 'helpers.yml' taskfiles/README.md` returns at least 1 (key-files mention)
    - `grep -c '_:safe-link' taskfiles/README.md` returns at least 2 (in symlink-helpers description + adding-a-symlink pattern)
    - `grep -c 'status:' taskfiles/README.md` returns at least 1 (LINT-01 idempotency contract)
    - `grep -c 'LINT-' taskfiles/README.md` returns at least 2 (LINT-01, LINT-02, LINT-03a/b cited in adding-a-pattern)
    - `grep -c 'lint-allow' taskfiles/README.md` returns at least 1 (LINT-03a exemption marker explained)
    - `grep -c 'manifest.yml' taskfiles/README.md` returns at least 1
    - `grep -c 'links.yml' taskfiles/README.md` returns at least 1
    - `grep -c 'shell.yml' taskfiles/README.md` returns at least 1
    - `grep -c '-stub.yml\|stub' taskfiles/README.md` returns at least 1 (transitional-state explanation)
    - `grep -c 'Taskfile.yml' taskfiles/README.md` returns at least 1 (root taskfile referenced in adding-a-pattern)
    - `grep -c 'DOCS-02' taskfiles/README.md` returns at least 1
    - `grep -c 'Satisfies DOCS-02 for taskfiles/' taskfiles/README.md` returns 1 (footer)
    - `wc -l taskfiles/README.md` reports a number less than 100
    - `wc -l taskfiles/README.md` reports a number greater than 30 (substantive)
    - No emojis: `grep -cE '[\x{1F300}-\x{1F9FF}]|🎯|✨|🚀|⚡|✅|❌|🔥|💡' taskfiles/README.md` returns 0
    - No AI attribution: `grep -cE 'AI-generated|Co-Authored|generated by' taskfiles/README.md` returns 0
    - File ends with exactly one newline: `tail -c 1 taskfiles/README.md | xxd | head -1 | grep -q '0a'` succeeds
  </acceptance_criteria>
  <done>taskfiles/README.md is a DOCS-02 anchor: purpose, key files grouped by phase maturity, adding-a-taskfile + adding-a-symlink workflows with the lint-rule callouts, references; under 100 lines; no emojis; no AI attribution.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write claude/README.md — DOCS-02 anchor for the claude/ directory (subdirectories overview with Phase 7 deferral note)</name>
  <files>claude/README.md</files>
  <read_first>
    - claude/ directory listing (agents/, commands/, CLAUDE.md, hooks/, settings.json, skills/)
    - claude/CLAUDE.md (top 30 lines — confirm the project-level Claude instructions)
    - claude/hooks/ (just list contents: secret-scan.zsh, no-emojis.zsh, no-ai-comments.zsh, agent-transparency.zsh — names cited in the README's hooks-bullet; PROJECT.md identifies the four hooks)
    - shell/README.md (template structure)
    - manifests/README.md (sibling-README pattern; terseness budget)
    - REQUIREMENTS.md DOCS-02, CLDE-01..CLDE-04, TEST-01, TEST-02 (Phase 7 ownership cited in the deferral note)
    - .planning/phases/03-shell-layer-flat-content-port/03-VERIFICATION.md gaps section, truth 6 missing item: "claude/README.md (or accept deferral to Phase 7 if claude/ ownership is later — note in REQUIREMENTS.md)"
    - .planning/ROADMAP.md Phase 7 details (the claude/ install task and hook smoke tests are Phase 7 scope)
    - CLAUDE.md (repo root) "Conventions Not Captured Above" — no emojis, no AI attribution, file-level comment blocks
  </read_first>
  <action>
    Create `claude/README.md`. Mirror the shell/README.md template shape with one structural variation: claude/ is a directory of subdirectories (not flat files), so the section is `## Key subdirectories` instead of `## Key files`. Phase 7 owns the install path; this README is forward-pointing.

    Required sections:

    1. **Top-level heading**: `# claude`

    2. **Purpose paragraph** (2-3 sentences): Claude Code integration — global `CLAUDE.md`, machine-level `settings.json`, and the per-purpose subdirectories `hooks/`, `agents/`, `commands/`, `skills/`. Sourced by `task claude:install` in Phase 7 (CLDE-01); the hooks pass repo-wide rules (no secrets in source, no AI attribution in commits, no emojis in non-markdown files, agent-transparency). Phase 7 owns the install task and runtime hook smoke tests (TEST-01).

    3. **Key subdirectories** subsection (`## Key subdirectories`): bulleted list:
       - `hooks/` — Repo-wide commit/edit-time guards. Four zsh hooks: `secret-scan.zsh` (blocks committing `.env`-style files and API tokens), `no-emojis.zsh` (warns on emojis in source files; the project rule extends the warning to markdown), `no-ai-comments.zsh` (warns on AI-attribution strings in committed source — "generated by", "Co-Authored-By"), `agent-transparency.zsh` (logs subagent dispatch decisions). All four must pass `shellcheck` clean and `set -euo pipefail` (CLDE-02 / LINT-04). Runtime smoke-tests are Phase 7 (TEST-01).
       - `agents/` — Subagent role-files. Each agent declares its purpose and the tools it may invoke. Selection is per-task; Claude orchestrator chooses based on the user's request.
       - `commands/` — Slash commands available in Claude Code (`/gsd-*`, custom workflow commands). Each command is a markdown file declaring frontmatter (tool allowlist, model preference) plus the prompt body.
       - `skills/` — Per-domain skill files surfaced when the active task matches the skill's triggers.
       - `CLAUDE.md` — Project-level instructions Claude Code reads on every session. Currently still references v1's `DOTFILES_PROFILE`; Plan 03-08 retires those references.
       - `settings.json` — Per-user Claude Code settings (model preferences, hook timeouts, etc.).

    4. **Adding a pattern** subsection (`## Adding a pattern`):
       - **A new hook.** Create `claude/hooks/<name>.zsh`. Start with shebang + `set -euo pipefail` (LINT-04). Add a file-level comment block naming purpose, exit-code semantics (0 for pass/warn, 2 for block), and an example synthetic-input JSON the hook handles. Add a matching entry to `claude/hooks/hooks.json` declaring the events (PreToolUse, PostToolUse, PreCommit, etc.). Phase 7 (TEST-01) will write a synthetic-input smoke test for the new hook automatically when `task test:hooks` runs against the directory.
       - **A new agent.** Create `claude/agents/<name>.md` with frontmatter (`description`, `tools`, optional `model`). Agent definitions are picked up by Claude Code on session start.
       - **A new slash command.** Create `claude/commands/<name>.md` (or `claude/commands/<namespace>/<name>.md`) with the prompt body. Frontmatter declares the tool allowlist and model preference.

    5. **Phase ownership and deferral note** subsection (`## Phase ownership`): Plan 03-07 writes this README to close the DOCS-02 contract for `claude/`. Phase 7 owns the install path (CLDE-01..CLDE-04), the hook smoke tests (TEST-01), and the `task claude:install` / `task claude:gsd` / `task claude:marketplace` task implementations. The taskfile is still a stub (`taskfiles/claude-stub.yml`) at the end of Phase 3.

    6. **References** subsection (`## References`):
       - `CLAUDE.md` — project-level Claude instructions (currently stale; Plan 03-08 cleans up).
       - `../.planning/REQUIREMENTS.md` — CLDE-01..CLDE-04, TEST-01, TEST-02, DOCS-02 traceability.
       - `../.planning/ROADMAP.md` Phase 7 — the claude install + tool configs + smoke tests phase.

    7. **Footer line**: `Satisfies DOCS-02 for claude/.`

    Format constraints:
    - File-end newline; no trailing whitespace.
    - No emojis. No AI attribution.
    - All file/path references in backticks.
    - Less than 100 lines.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ -f claude/README.md ] && head -1 claude/README.md | grep -qx '# claude' && grep -q '## Key subdirectories' claude/README.md && grep -q '## Adding a pattern' claude/README.md && grep -q '## Phase ownership' claude/README.md && grep -q '## References' claude/README.md && grep -q 'hooks/' claude/README.md && grep -q 'agents/' claude/README.md && grep -q 'commands/' claude/README.md && grep -q 'skills/' claude/README.md && grep -q 'Phase 7\|CLDE-' claude/README.md && grep -q 'DOCS-02' claude/README.md && [ "$(wc -l < claude/README.md)" -lt 100 ] && ! grep -qE '🎯|✨|🚀|⚡|✅|❌|🔥|💡|👍|👀|📝|📦|🎉|Co-Authored|AI-generated|generated by' claude/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `claude/README.md` exists
    - `head -1 claude/README.md` is exactly `# claude`
    - `grep -c '## Key subdirectories' claude/README.md` returns 1
    - `grep -c '## Adding a pattern' claude/README.md` returns 1
    - `grep -c '## Phase ownership' claude/README.md` returns 1
    - `grep -c '## References' claude/README.md` returns 1
    - `grep -c 'hooks/' claude/README.md` returns at least 2 (subdirectory bullet + adding-a-pattern hooks workflow)
    - `grep -c 'agents/' claude/README.md` returns at least 1
    - `grep -c 'commands/' claude/README.md` returns at least 1
    - `grep -c 'skills/' claude/README.md` returns at least 1
    - `grep -c 'secret-scan\.zsh' claude/README.md` returns at least 1 (one of the four hooks named)
    - `grep -c 'no-emojis\.zsh\|no-ai-comments\.zsh\|agent-transparency\.zsh' claude/README.md` returns at least 1 (additional hooks named in the bullet)
    - `grep -c 'Phase 7\|CLDE-01' claude/README.md` returns at least 1 (deferral / forward pointer)
    - `grep -c 'TEST-01' claude/README.md` returns at least 1 (hook smoke-test ownership)
    - `grep -c 'taskfiles/claude-stub.yml\|claude-stub' claude/README.md` returns at least 1 (current state explained)
    - `grep -c 'DOCS-02' claude/README.md` returns at least 1
    - `grep -c 'Satisfies DOCS-02 for claude/' claude/README.md` returns 1 (footer)
    - `wc -l claude/README.md` reports a number less than 100
    - `wc -l claude/README.md` reports a number greater than 30 (substantive)
    - No emojis: `grep -cE '[\x{1F300}-\x{1F9FF}]|🎯|✨|🚀|⚡|✅|❌|🔥|💡' claude/README.md` returns 0
    - No AI attribution: `grep -cE 'AI-generated|Co-Authored|generated by' claude/README.md` returns 0
    - File ends with exactly one newline: `tail -c 1 claude/README.md | xxd | head -1 | grep -q '0a'` succeeds
    - Phase 4 deferral check (negative): the README mentions NO git/README.md or ssh/README.md — those are Phase 4 (separate concerns): `grep -c 'git/README\|ssh/README' claude/README.md` returns 0
  </acceptance_criteria>
  <done>claude/README.md is a DOCS-02 anchor: purpose, key subdirectories (hooks/agents/commands/skills/CLAUDE.md/settings.json), adding-a-hook + adding-an-agent + adding-a-command workflows, Phase 7 ownership note, references; under 100 lines; no emojis; no AI attribution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md content -> downstream readers (humans + AI agents) | Documentation only; no executable surface. Inaccurate content could mislead a new contributor or AI agent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-23 | Information Disclosure | README content | accept | The three new READMEs cite phase-ownership boundaries (Phase 4 for git/ssh, Phase 5 for Brewfile refactor, Phase 7 for claude install), file paths, and lint-rule numbers. No PII, no machine identifiers, no secrets, no internal hostnames. Public commit history is fine. |
| T-03-24 | Tampering | README content drift vs reality | mitigate | Each README cross-references `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` so any drift between documentation and shipped phases is auditable. Phase 8 includes a top-level README pass that will re-verify DOCS-02 coverage. |
</threat_model>

<verification>
After all three tasks complete:

1. All three READMEs exist: `[ -f install/README.md ] && [ -f taskfiles/README.md ] && [ -f claude/README.md ]` succeeds.
2. Each README is under 100 lines and over 25 lines: `for f in install/README.md taskfiles/README.md claude/README.md; do lines=$(wc -l < "$f"); [ "$lines" -lt 100 ] && [ "$lines" -gt 25 ] || echo "FAIL: $f $lines"; done` produces no FAIL output.
3. Each README cites DOCS-02 in a footer: `for f in install/README.md taskfiles/README.md claude/README.md; do grep -q "Satisfies DOCS-02 for" "$f" || echo "FAIL: $f"; done` produces no FAIL output.
4. Top-level READMEs now covered (Phase 3 ship list): `for d in shell manifests configs packages docs os install taskfiles claude; do [ -f "$d/README.md" ] || echo "MISSING: $d/README.md"; done` produces no MISSING output.
5. Phase 4 deferred dirs intentionally lack READMEs (Phase 4 owns them per the deferred section): `for d in git ssh; do [ -f "$d/README.md" ] && echo "UNEXPECTED: $d/README.md exists"; done` produces no UNEXPECTED output (or, if pre-existing identity/README.md is the Phase 4 anchor, that file is unchanged by this plan).
6. No emojis in any new README: `for f in install/README.md taskfiles/README.md claude/README.md; do grep -qE '[\x{1F300}-\x{1F9FF}]' "$f" && echo "EMOJI: $f"; done` produces no EMOJI output.
7. No AI attribution in any new README: `for f in install/README.md taskfiles/README.md claude/README.md; do grep -qE 'AI-generated|Co-Authored|generated by' "$f" && echo "AI: $f"; done` produces no AI output.
</verification>

<success_criteria>
- install/README.md, taskfiles/README.md, claude/README.md all exist and follow the shell/README.md template shape
- Each README is fewer than 100 lines, has top-level heading + Key files (or Key subdirectories) + Adding a pattern + References sections, and ends with `Satisfies DOCS-02 for <dir>/.`
- No emojis or AI attribution in any new README
- DOCS-02 truth 6 ("every top-level directory has a README.md") now passes for all directories that ship in Phase 3 scope (shell/, manifests/, configs/, packages/, docs/, os/, install/, taskfiles/, claude/)
- git/README.md and ssh/README.md remain absent (Phase 4 ownership per 03-VERIFICATION.md deferred)
</success_criteria>

<output>
After completion, create `.planning/phases/03-shell-layer-flat-content-port/03-07-SUMMARY.md` per the template at `~/.config/claude/get-shit-done/templates/summary.md`.
</output>

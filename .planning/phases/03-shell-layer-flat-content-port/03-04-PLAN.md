---
phase: 03-shell-layer-flat-content-port
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - shell/aliases/general.zsh
  - shell/aliases/hardware.zsh
  - shell/aliases/networking.zsh
  - shell/aliases/dotfiles.zsh
  - shell/aliases/finder.zsh
  - shell/aliases/ghostty.zsh
  - shell/aliases/jgrid.zsh
autonomous: true
requirements: [SHEL-06, SHEL-08]

must_haves:
  truths:
    - "shell/aliases/general.zsh ports v1 zsh/aliases/common/general.zsh EXCEPT the four GUI-coupled lines (Finder x3 + Ghostty) which are extracted into separate gated files (per D-10)"
    - "shell/aliases/hardware.zsh is a byte-stable port of v1 zsh/aliases/common/hardware.zsh (macOS-only system_profiler aliases)"
    - "shell/aliases/networking.zsh is a byte-stable port of v1 zsh/aliases/common/networking.zsh (dnsflush, traceroute, ip aliases)"
    - "shell/aliases/dotfiles.zsh hosts the new `alias update='task install'` (CF-06 — replaces dropped v1 update.zsh function)"
    - "shell/aliases/finder.zsh defines finder, findershow, finderhide as wrapper functions; each gated on features.macos-finder via _dotfiles_feature (D-07 wrapper-function pattern)"
    - "shell/aliases/ghostty.zsh defines g() as a wrapper function gated on features.ghostty (D-07)"
    - "shell/aliases/jgrid.zsh prepends source-time gate `[[ \"$(_dotfiles_feature jgrid-net)\" == \"true\" ]] || return 0` then runs the 22-metal verbatim loop (D-08 bulk-loop exception)"
    - "Disabled-feature wrapper-function call returns non-zero with a stderr message naming the disabled feature (D-07 contract)"
    - "No shell/aliases/*.zsh file contains DOTFILES_PROFILE anywhere"
    - "zsh -n passes on every shell/aliases/*.zsh file (LINT-07 already covers shell/ globally)"
  artifacts:
    - path: "shell/aliases/general.zsh"
      provides: "Ungated general aliases (reload, dotfile, ls/ll, history, task, etc.) — Finder + Ghostty extracted"
      contains: "alias reload="
    - path: "shell/aliases/hardware.zsh"
      provides: "system_profiler wrappers (gpu, cpu, ram, disk, bluetooth, speaker, webcam, power, monitor)"
      contains: "system_profiler"
    - path: "shell/aliases/networking.zsh"
      provides: "DNS + IP wrapper aliases (dnsflush, traceroute, ip, ipv4, ipv6)"
      contains: "dnsflush"
    - path: "shell/aliases/dotfiles.zsh"
      provides: "Dotfiles-repo nav + update alias (CF-06)"
      contains: "alias update='task install'"
    - path: "shell/aliases/finder.zsh"
      provides: "Finder GUI wrapper functions gated on features.macos-finder (D-07)"
      contains: "_dotfiles_feature macos-finder"
    - path: "shell/aliases/ghostty.zsh"
      provides: "Ghostty launcher wrapper gated on features.ghostty (D-07)"
      contains: "_dotfiles_feature ghostty"
    - path: "shell/aliases/jgrid.zsh"
      provides: "Allomantic-metal jump-ssh aliases gated on features.jgrid-net (D-08 source-time gate)"
      contains: "_dotfiles_feature jgrid-net"
  key_links:
    - from: "shell/aliases/finder.zsh, ghostty.zsh, jgrid.zsh"
      to: "shell/functions/_dotfiles_feature.zsh (Plan 03)"
      via: "_dotfiles_feature <name> invocation in wrapper-function or source-time gate"
      pattern: "_dotfiles_feature"
    - from: "shell/aliases/jgrid.zsh"
      to: "features.jgrid-net (set true on personal-laptop per Plan 01)"
      via: "source-time gate; loop only runs when feature is enabled"
      pattern: "jgrid-net"
    - from: "shell/aliases/dotfiles.zsh"
      to: "task install (canonical entry per Phase 2 D-10)"
      via: "alias update='task install'"
      pattern: "task install"
---

<objective>
Port v1's alias topic files into flat `shell/aliases/<topic>.zsh` (SHEL-06, SHEL-08): three verbatim ports (hardware, networking) plus surgical edits to extract GUI-coupled lines from v1's general.zsh into gated files (finder.zsh, ghostty.zsh), port jgrid.zsh with a source-time feature gate (D-08), and create a new dotfiles.zsh topic that hosts the `update='task install'` alias (CF-06).

Purpose: Plan 04 lands the alias content the `.zshrc` (Plan 02) flat glob loads. With the `_dotfiles_feature` helper from Plan 03 and the manifest feature flags from Plan 01 in place, the gated alias files (finder, ghostty, jgrid) are immediately functional on the right machines and no-ops elsewhere.

Output: 7 new alias topic files in `shell/aliases/`. No function files (Plan 03). No taskfile changes (Plan 05). No symlinks (Plan 05).
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shell-layer-flat-content-port/03-CONTEXT.md
@.planning/phases/03-shell-layer-flat-content-port/03-PATTERNS.md
@.planning/phases/03-shell-layer-flat-content-port/03-01-PLAN.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-CONTEXT.md
@CLAUDE.md
@.claude/CLAUDE.md
@zsh/aliases/common/general.zsh
@zsh/aliases/common/hardware.zsh
@zsh/aliases/common/networking.zsh
@zsh/aliases/personal/jgrid.zsh
@zsh/functions/update.zsh

<interfaces>
v1 alias source files (4 total in v1):
- zsh/aliases/common/general.zsh (43 lines, 18 alias declarations) — split per D-10: extract lines 27-31 (Finder x3) → finder.zsh; line 40 (Ghostty g) → ghostty.zsh; everything else ports.
- zsh/aliases/common/hardware.zsh (10 lines, 9 system_profiler aliases) — verbatim port.
- zsh/aliases/common/networking.zsh (12 lines, 5 aliases) — verbatim port.
- zsh/aliases/personal/jgrid.zsh (40 lines, 22-metal alias loop) — verbatim port + source-time gate prepended.

v1 update.zsh function (replaced per CF-06):
- 21-line wrapper function that does `cd $DOTFILEDIR && task update && cd back`. Replaced in v2 by a single-line alias `alias update='task install'` (Phase 2 D-10: task install IS task update).

D-07 wrapper-function gate pattern (PATTERNS.md Pattern 8/9):
```
function <name>() {
    [[ "$(_dotfiles_feature <key>)" == "true" ]] \
        || { echo "<name>: feature '<key>' is disabled on this machine" >&2; return 1; }
    <body>
}
```

D-08 source-time gate pattern (PATTERNS.md Pattern 12; bulk-loop exception):
```
[[ "$(_dotfiles_feature <key>)" == "true" ]] || return 0
```
Prepend before any alias-loop body. The `return 0` from a sourced file unwinds back to .zshrc without error.

Feature flag mapping (from Plan 01):
- macos-finder → finder.zsh (true on personal-laptop, work-laptop; false elsewhere)
- ghostty → ghostty.zsh (true on personal-laptop, work-laptop; false elsewhere)
- jgrid-net → jgrid.zsh (true on personal-laptop ONLY; false elsewhere)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Port three ungated alias files — general (with Finder/Ghostty extracted), hardware (verbatim), networking (verbatim) — plus new dotfiles.zsh with update alias</name>
  <files>shell/aliases/general.zsh, shell/aliases/hardware.zsh, shell/aliases/networking.zsh, shell/aliases/dotfiles.zsh</files>
  <read_first>
    - zsh/aliases/common/general.zsh (full file, 43 lines — port everything EXCEPT v1 lines 27-31 (Finder x3) and line 40 (Ghostty g); per PATTERNS.md Pattern 7)
    - zsh/aliases/common/hardware.zsh (full file, 10 lines — verbatim port; PATTERNS.md Pattern 10)
    - zsh/aliases/common/networking.zsh (full file, 12 lines — verbatim port; PATTERNS.md Pattern 11)
    - zsh/functions/update.zsh (21 lines — being REPLACED by the alias in dotfiles.zsh per CF-06; read for context only)
    - .planning/phases/03-shell-layer-flat-content-port/03-CONTEXT.md `<decisions>` D-10 (gated alias scope), CF-06 (update alias)
    - .planning/phases/03-shell-layer-flat-content-port/03-PATTERNS.md Pattern 7 (general.zsh; lines 339-382)
    - .planning/phases/02-install-engine-bootstrap-idempotency-lint/02-CONTEXT.md D-10 (task install is canonical entry)
  </read_first>
  <action>
    Create four files.

    File 1 — `shell/aliases/general.zsh`:
    - Port v1 zsh/aliases/common/general.zsh PRESERVING every line EXCEPT v1 lines 27-31 (the four Finder lines: the open-Finder alias and the findershow/finderhide pair) and v1 line 40 (the Ghostty `g` alias). The extracted lines are NOT in general.zsh — they live in finder.zsh and ghostty.zsh (Task 2).
    - Concretely the file MUST contain these alias declarations (exact strings from v1):
      `alias reload='source "$ZDOTDIR"/.zshrc'`
      `alias environment="env"`
      `alias path='echo -e ${PATH//:/\\n} | highlight --syntax=bash'`
      `alias dotfile='cd "$DOTFILEDIR"'`
      `alias dotfiles='cd "$DOTFILEDIR"'`
      `alias fsa='ncdu'`
      `alias perms='permissions'`
      `alias ls="$(command -v eza) --time-style long-iso"`
      `alias ll='ls -alh'`
      `alias lastinstalled="ls -l /var/db/.AppleSetupDone"`
      `alias history="omz_history -t '%Y-%m-%d %I:%M:%S' | highlight --syntax=bash"`
      `alias t='task'`
    - The file MUST NOT contain:
      - `alias finder=...` (extracted to finder.zsh)
      - `alias findershow=...` (extracted)
      - `alias finderhide=...` (extracted)
      - `alias g=...` (extracted to ghostty.zsh)
      - `alias update=...` (lives in dotfiles.zsh — CF-06)
    - Keep the v1 shebang line 1 (`#!/bin/zsh`) and the v1 inline comments above each alias (lines 3, 6, 9, 12, 16, 19, 22, 33, 36, 42) verbatim.

    File 2 — `shell/aliases/hardware.zsh`:
    - Byte-stable copy of zsh/aliases/common/hardware.zsh. No changes.
    - `diff zsh/aliases/common/hardware.zsh shell/aliases/hardware.zsh` exits 0.

    File 3 — `shell/aliases/networking.zsh`:
    - Byte-stable copy of zsh/aliases/common/networking.zsh. No changes.
    - `diff zsh/aliases/common/networking.zsh shell/aliases/networking.zsh` exits 0.

    File 4 — `shell/aliases/dotfiles.zsh` (NEW topic; hosts the CF-06 update alias):
    - File-header shebang `#!/bin/zsh` plus a leading comment block: purpose ("dotfiles-related aliases — update shortcut and (future) repo helpers"), why-this-replaces-v1 (`alias update='task install'` replaces v1's zsh/functions/update.zsh wrapper; per CF-06 and Phase 2 D-10, task install IS task update — no separate update pipeline exists).
    - Exactly one alias declaration: `alias update='task install'`.
    - No other content. Single-concept-per-file (SHEL-06) is binding; this topic exists to house update; if a future plan adds related dotfiles aliases (e.g. `dot status`, `dot log`) they land here.

    Do NOT add `set -euo pipefail` to any of the four files (sourced, not executable per CF-07).
    Do NOT add any DOTFILES_PROFILE reference.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && zsh -n shell/aliases/general.zsh && zsh -n shell/aliases/hardware.zsh && zsh -n shell/aliases/networking.zsh && zsh -n shell/aliases/dotfiles.zsh && diff zsh/aliases/common/hardware.zsh shell/aliases/hardware.zsh && diff zsh/aliases/common/networking.zsh shell/aliases/networking.zsh && ! grep -q 'alias finder=' shell/aliases/general.zsh && ! grep -q 'alias findershow=' shell/aliases/general.zsh && ! grep -q 'alias finderhide=' shell/aliases/general.zsh && ! grep -q "alias g=" shell/aliases/general.zsh && grep -q 'alias reload=' shell/aliases/general.zsh && grep -q 'alias ls=' shell/aliases/general.zsh && grep -q "alias t='task'" shell/aliases/general.zsh && grep -q "alias update='task install'" shell/aliases/dotfiles.zsh && ! grep -l DOTFILES_PROFILE shell/aliases/general.zsh shell/aliases/hardware.zsh shell/aliases/networking.zsh shell/aliases/dotfiles.zsh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `zsh -n shell/aliases/general.zsh` exits 0
    - `zsh -n shell/aliases/hardware.zsh` exits 0
    - `zsh -n shell/aliases/networking.zsh` exits 0
    - `zsh -n shell/aliases/dotfiles.zsh` exits 0
    - `diff zsh/aliases/common/hardware.zsh shell/aliases/hardware.zsh` is empty (byte-identical)
    - `diff zsh/aliases/common/networking.zsh shell/aliases/networking.zsh` is empty (byte-identical)
    - `grep -c 'alias reload=' shell/aliases/general.zsh` returns 1
    - `grep -c 'alias environment=' shell/aliases/general.zsh` returns 1
    - `grep -c 'alias path=' shell/aliases/general.zsh` returns 1
    - `grep -c 'alias dotfile=' shell/aliases/general.zsh` returns 1
    - `grep -c 'alias dotfiles=' shell/aliases/general.zsh` returns 1
    - `grep -c 'alias fsa=' shell/aliases/general.zsh` returns 1
    - `grep -c 'alias perms=' shell/aliases/general.zsh` returns 1
    - `grep -c 'alias ls=' shell/aliases/general.zsh` returns 1
    - `grep -c 'alias ll=' shell/aliases/general.zsh` returns 1
    - `grep -c 'alias lastinstalled=' shell/aliases/general.zsh` returns 1
    - `grep -c 'alias history=' shell/aliases/general.zsh` returns 1
    - `grep -c "alias t='task'" shell/aliases/general.zsh` returns 1
    - `grep -c 'alias finder=' shell/aliases/general.zsh` returns 0 (extracted)
    - `grep -c 'alias findershow=' shell/aliases/general.zsh` returns 0 (extracted)
    - `grep -c 'alias finderhide=' shell/aliases/general.zsh` returns 0 (extracted)
    - `grep -c "alias g=" shell/aliases/general.zsh` returns 0 (extracted)
    - `grep -c "alias update='task install'" shell/aliases/dotfiles.zsh` returns 1 (CF-06)
    - `grep -c '^alias' shell/aliases/general.zsh` returns 12 (12 remaining alias declarations after extraction)
    - `grep -c '^alias' shell/aliases/hardware.zsh` returns 9 (verbatim from v1)
    - `grep -c '^alias' shell/aliases/networking.zsh` returns 5 (verbatim from v1)
    - `grep -c '^alias' shell/aliases/dotfiles.zsh` returns 1 (only update)
    - `grep -lr DOTFILES_PROFILE shell/aliases/` returns nothing for these four files
    - Smoke test: `zsh -c '. shell/aliases/general.zsh && alias reload | grep -q ZDOTDIR && echo ok'` outputs `ok` (sourced cleanly; reload alias defined)
  </acceptance_criteria>
  <done>Four ungated alias files in place: three v1 ports (general with extractions, hardware verbatim, networking verbatim) plus the new dotfiles.zsh hosting the update alias per CF-06.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create three gated alias files — finder.zsh + ghostty.zsh (wrapper-function gates D-07), jgrid.zsh (source-time gate D-08 with 22-metal verbatim loop)</name>
  <files>shell/aliases/finder.zsh, shell/aliases/ghostty.zsh, shell/aliases/jgrid.zsh</files>
  <read_first>
    - zsh/aliases/common/general.zsh lines 27-31 (the Finder x3 lines being extracted to finder.zsh) and line 40 (Ghostty g line being extracted to ghostty.zsh)
    - zsh/aliases/personal/jgrid.zsh (full file, 40 lines — verbatim METALS array + alias loop)
    - .planning/phases/03-shell-layer-flat-content-port/03-CONTEXT.md `<decisions>` D-07 (wrapper-function pattern), D-08 (source-time gate), D-10 (gated alias scope), D-12 (jgrid-net flag)
    - .planning/phases/03-shell-layer-flat-content-port/03-CONTEXT.md `<specifics>` "shell/aliases/finder.zsh sketch" (lines 210-220), "shell/aliases/jgrid.zsh sketch" (lines 222-235)
    - .planning/phases/03-shell-layer-flat-content-port/03-PATTERNS.md Pattern 8 finder.zsh (lines 384-414), Pattern 9 ghostty.zsh (lines 416-431), Pattern 12 jgrid.zsh (lines 446-477)
    - shell/functions/_dotfiles_feature.zsh (Plan 03 — the function this plan calls; reference for the caller-side `[[ "$(_dotfiles_feature foo)" == "true" ]]` pattern)
  </read_first>
  <action>
    Create three files using D-07 (wrapper-function) and D-08 (source-time) gate patterns.

    File 1 — `shell/aliases/finder.zsh` (D-07 wrapper-function gate; three functions; gated on features.macos-finder):
    - Shebang `#!/bin/zsh` then a leading comment block: purpose ("Finder GUI aliases — gated on features.macos-finder via _dotfiles_feature per D-07"), why-wrapper-not-source-time ("three discrete aliases; wrapper-function gate provides a helpful stderr message when called on a server / non-GUI machine instead of silently no-oping").
    - Three function declarations matching PATTERNS.md Pattern 8 lines 393-411 exactly:
      1. `function finder() { ... }` — body checks `[[ "$(_dotfiles_feature macos-finder)" == "true" ]]` OR prints `finder: feature 'macos-finder' is disabled on this machine` to stderr and `return 1`; runs `open -a Finder ./` on success.
      2. `function findershow() { ... }` — same gate; body is `defaults write com.apple.finder AppleShowAllFiles -bool true && killall Finder`.
      3. `function finderhide() { ... }` — same gate; body is `defaults write com.apple.finder AppleShowAllFiles -bool false && killall Finder`.
    - The gate-line in each function uses the canonical caller pattern: `[[ "$(_dotfiles_feature macos-finder)" == "true" ]] \` then on the next line `|| { echo "<name>: feature 'macos-finder' is disabled on this machine" >&2; return 1; }`.
    - Do NOT factor the gate into a shared inline helper — three repetitions is fine and is explicit per PATTERNS.md Pattern 8 note.
    - No `set -euo pipefail` (sourced).

    File 2 — `shell/aliases/ghostty.zsh` (D-07 wrapper-function gate; one function; gated on features.ghostty):
    - Shebang plus leading comment block: purpose ("Ghostty launcher alias — gated on features.ghostty via _dotfiles_feature per D-07; replaces v1 zsh/aliases/common/general.zsh:40").
    - Single function per PATTERNS.md Pattern 9 lines 425-430:
      `function g() { [[ "$(_dotfiles_feature ghostty)" == "true" ]] || { echo "g: feature 'ghostty' is disabled on this machine" >&2; return 1; }; /Applications/Ghostty.app/Contents/MacOS/ghostty "$@"; }`
    - The trailing `"$@"` is REQUIRED so `g <args>` forwards arguments to the binary (matches v1 alias semantics; v1's alias did NOT have explicit `$@` because shell aliases pass remaining argv automatically — the wrapper-function MUST add it explicitly).
    - No `set -euo pipefail` (sourced).

    File 3 — `shell/aliases/jgrid.zsh` (D-08 source-time gate; bulk-loop exception; gated on features.jgrid-net):
    - Shebang plus leading comment block: purpose ("jgrid.net allomantic-metals ssh-jump aliases — gated on features.jgrid-net via source-time gate per D-08 (the bulk-alias-loop exception)"), why-source-time ("22 aliases in a tight loop; wrapper-function-per-alias would 22x the gate evaluation and bloat the file; source-time gate pays one jq cost only when the feature is enabled, then defines all 22 aliases at once").
    - First active line: `[[ "$(_dotfiles_feature jgrid-net)" == "true" ]] || return 0` (per PATTERNS.md Pattern 12 line 456). `return 0` from a sourced file is legal in zsh and unwinds back to the `for file in` loop in `.zshrc` without printing an error.
    - METALS array: port v1 zsh/aliases/personal/jgrid.zsh lines 7-32 verbatim — 22 elements split across "standard metals" and "God metals" comments. Either keep v1's multi-line one-per-line format OR collapse to the PATTERNS.md Pattern 12 compact form (lines 462-467) — both are equivalent; planner picks the format that diffs cleanest against v1 (multi-line is the byte-stable form).
    - For-loop: port v1 lines 34-37 verbatim — `for i in $METALS; do alias $i="ssh josh@$i-ssh.jgrid.net"; done`.
    - Final line: `unset METALS` (verbatim from v1 line 39).
    - No `set -euo pipefail` (sourced).
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && zsh -n shell/aliases/finder.zsh && zsh -n shell/aliases/ghostty.zsh && zsh -n shell/aliases/jgrid.zsh && grep -c 'function finder()' shell/aliases/finder.zsh | grep -qx 1 && grep -c 'function findershow()' shell/aliases/finder.zsh | grep -qx 1 && grep -c 'function finderhide()' shell/aliases/finder.zsh | grep -qx 1 && grep -c 'function g()' shell/aliases/ghostty.zsh | grep -qx 1 && grep -c '_dotfiles_feature macos-finder' shell/aliases/finder.zsh | grep -q '^3$' && grep -c '_dotfiles_feature ghostty' shell/aliases/ghostty.zsh | grep -qx 1 && grep -q '_dotfiles_feature jgrid-net' shell/aliases/jgrid.zsh && grep -q 'return 0' shell/aliases/jgrid.zsh && grep -c '"steel"\|steel' shell/aliases/jgrid.zsh | grep -qE '[1-9]' && grep -c '"raysium"\|raysium' shell/aliases/jgrid.zsh | grep -qE '[1-9]' && grep -c 'alias \$i' shell/aliases/jgrid.zsh | grep -qx 1 && ! grep -l DOTFILES_PROFILE shell/aliases/finder.zsh shell/aliases/ghostty.zsh shell/aliases/jgrid.zsh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `zsh -n shell/aliases/finder.zsh` exits 0
    - `zsh -n shell/aliases/ghostty.zsh` exits 0
    - `zsh -n shell/aliases/jgrid.zsh` exits 0
    - `grep -c 'function finder()' shell/aliases/finder.zsh` returns 1
    - `grep -c 'function findershow()' shell/aliases/finder.zsh` returns 1
    - `grep -c 'function finderhide()' shell/aliases/finder.zsh` returns 1
    - `grep -c '_dotfiles_feature macos-finder' shell/aliases/finder.zsh` returns 3 (one gate per function)
    - `grep -c 'open -a Finder' shell/aliases/finder.zsh` returns 1
    - `grep -c 'AppleShowAllFiles -bool true' shell/aliases/finder.zsh` returns 1
    - `grep -c 'AppleShowAllFiles -bool false' shell/aliases/finder.zsh` returns 1
    - `grep -c "is disabled on this machine" shell/aliases/finder.zsh` returns at least 3 (one disabled-message per wrapper)
    - `grep -c 'function g()' shell/aliases/ghostty.zsh` returns 1
    - `grep -c '_dotfiles_feature ghostty' shell/aliases/ghostty.zsh` returns 1
    - `grep -c '/Applications/Ghostty.app/Contents/MacOS/ghostty' shell/aliases/ghostty.zsh` returns 1
    - `grep -c '"$@"' shell/aliases/ghostty.zsh` returns 1 (argv forwarding)
    - `grep -c '_dotfiles_feature jgrid-net' shell/aliases/jgrid.zsh` returns 1
    - `grep -c 'return 0' shell/aliases/jgrid.zsh` returns 1 (source-time gate exit)
    - `grep -c '\bsteel\b' shell/aliases/jgrid.zsh` returns at least 1 (first metal)
    - `grep -c '\bbendalloy\b' shell/aliases/jgrid.zsh` returns at least 1 (last standard metal)
    - `grep -c '\batium\b' shell/aliases/jgrid.zsh` returns at least 1 (first God metal)
    - `grep -c '\braysium\b' shell/aliases/jgrid.zsh` returns at least 1 (last God metal)
    - Metal count verification: `awk '/METALS=/,/^\)/' shell/aliases/jgrid.zsh | grep -oE '\b(steel|iron|zinc|brass|pewter|tin|copper|bronze|duralumin|aluminum|gold|electrum|nicrosil|chromium|cadmium|bendalloy|atium|malatium|lerasium|harmonium|trellium|raysium)\b' | wc -l | grep -qx 22` (exactly 22 metals declared)
    - `grep -c 'alias $i=' shell/aliases/jgrid.zsh` returns 1 (the loop body)
    - `grep -c 'ssh josh@' shell/aliases/jgrid.zsh` returns 1
    - `grep -c 'unset METALS' shell/aliases/jgrid.zsh` returns 1
    - `grep -lr DOTFILES_PROFILE shell/aliases/finder.zsh shell/aliases/ghostty.zsh shell/aliases/jgrid.zsh` returns nothing
    - Smoke test (gate behavior on a machine without the feature): `XDG_STATE_HOME=/nonexistent zsh -c '. shell/functions/_dotfiles_feature.zsh && . shell/aliases/finder.zsh && finder 2>&1' | grep -q "is disabled on this machine"` succeeds
    - Smoke test (jgrid source-time gate exits cleanly when feature absent): `XDG_STATE_HOME=/nonexistent zsh -c '. shell/functions/_dotfiles_feature.zsh && . shell/aliases/jgrid.zsh && [[ -z "$(alias steel 2>/dev/null)" ]] && echo ok'` outputs `ok` (no alias defined when feature is off)
  </acceptance_criteria>
  <done>Three gated alias files in place: finder/ghostty use D-07 wrapper functions; jgrid uses D-08 source-time gate; all three call _dotfiles_feature with the correct kebab-case feature key; 22-metal loop preserved verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `_dotfiles_feature` call → wrapper-function or source-time gate | The function returns "true" or "false" on stdout; comparison is against the literal string "true". |
| `g()` wrapper → `/Applications/Ghostty.app/Contents/MacOS/ghostty` | Absolute path to a macOS app binary. |
| `jgrid` aliases → `ssh josh@<metal>-ssh.jgrid.net` | Each metal alias expands to an ssh invocation against the jgrid.net domain over the local Cloudflared tunnel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Tampering | feature-flag string-compare | mitigate | Each gate uses `== "true"` (string-equality, not boolean truthiness). A tampered flag value like `"1"`, `"yes"`, or `"True"` will NOT pass the gate. `_dotfiles_feature` only returns `"true"` or `"false"` (or `:-false` default); other values cannot leak through. |
| T-03-14 | Elevation of Privilege | `finder`/`findershow`/`finderhide` wrapper | accept | The wrappers run `open -a Finder` and `defaults write com.apple.finder AppleShowAllFiles ...`. These are user-space operations on the user's own Finder preferences — no privilege boundary crossed. |
| T-03-15 | Spoofing | jgrid ssh aliases | accept | The aliases hardcode `ssh josh@<metal>-ssh.jgrid.net`. DNS spoofing of jgrid.net would redirect ssh to a hostile host, but ssh's `StrictHostKeyChecking` + known_hosts is the existing trust anchor — same posture as v1. |
| T-03-16 | Information Disclosure | disabled-feature stderr message | accept | When a user calls `finder` on a machine without the feature, the stderr message reveals the literal feature key (`'macos-finder'`). This is intentional debugging UX, not a secret. |
</threat_model>

<verification>
After both tasks complete:

1. `find shell/aliases -name '*.zsh' | wc -l` returns 7 (general, hardware, networking, dotfiles, finder, ghostty, jgrid).
2. `for f in shell/aliases/*.zsh; do zsh -n "$f" || echo "FAIL: $f"; done` produces no `FAIL:` output.
3. `grep -lr DOTFILES_PROFILE shell/aliases/` returns nothing.
4. `diff zsh/aliases/common/hardware.zsh shell/aliases/hardware.zsh` is empty.
5. `diff zsh/aliases/common/networking.zsh shell/aliases/networking.zsh` is empty.
6. Extractions verified: `grep -c 'alias finder=' shell/aliases/general.zsh` returns 0; `grep -c 'function finder()' shell/aliases/finder.zsh` returns 1.
7. update alias landed: `grep -c "alias update='task install'" shell/aliases/dotfiles.zsh` returns 1.
8. 22 metals present in jgrid.zsh; source-time gate is the first active line.
9. End-to-end gate behavior: with `features.macos-finder = false` (default), calling `finder` produces stderr `feature 'macos-finder' is disabled on this machine` and returns 1; with the feature enabled, calling `finder` runs `open -a Finder ./`.
</verification>

<success_criteria>
- 7 alias files exist under shell/aliases/
- zsh -n passes on every file
- general.zsh contains 12 aliases (Finder x3 + Ghostty extracted)
- hardware.zsh and networking.zsh are byte-stable v1 ports
- dotfiles.zsh hosts `alias update='task install'` (CF-06)
- finder.zsh defines 3 wrapper functions; each gated on features.macos-finder (D-07)
- ghostty.zsh defines 1 wrapper function gated on features.ghostty (D-07)
- jgrid.zsh has source-time gate (D-08) + 22-metal verbatim loop
- No DOTFILES_PROFILE references anywhere in shell/aliases/
- Disabled-feature gates produce a helpful stderr message and return 1
</success_criteria>

<output>
After completion, create `.planning/phases/03-shell-layer-flat-content-port/03-04-SUMMARY.md` per the template at `~/.config/claude/get-shit-done/templates/summary.md`.
</output>

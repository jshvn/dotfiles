---
phase: 03-shell-layer-flat-content-port
plan: 05
type: execute
wave: 3
depends_on: [03-02, 03-03, 03-04]
files_modified:
  - taskfiles/links.yml
  - taskfiles/shell.yml
  - Taskfile.yml
  - shell/README.md
autonomous: true
requirements: [SHEL-12, DOCS-02]

must_haves:
  truths:
    - "taskfiles/links.yml replaces taskfiles/links-stub.yml as the real implementation of links:all (P3 scope: shell-only + antidote bundle file; P4-P7 extend)"
    - "links:zsh symlinks shell/.zshenv, .zprofile, .zshrc, .zlogin, .zlogout into $ZDOTDIR via _:safe-link (SHEL-03)"
    - "links:antidote symlinks configs/antidote/zsh_plugins.txt into $ZDOTDIR/.zsh_plugins.txt via _:safe-link"
    - "Every install-style task in links.yml has a status: block using {{.X}} template vars only (LINT-02 compliance; never $X)"
    - "links:all aggregates only the P3-owned subtasks (zsh, antidote); P4/P5/P6/P7 extend this list when their plans land"
    - "links:validate uses _:check-link to validate all P3 symlinks (preview of CUTV-01 composition)"
    - "Taskfile.yml updates: includes: links: ./taskfiles/links.yml (drop -stub suffix)"
    - "Taskfile.yml updates: optionally adds perf: ./taskfiles/shell.yml (planner discretion — chosen path)"
    - "taskfiles/shell.yml hosts task perf:shell which uses hyperfine to measure cold interactive shell start; exits non-zero when mean exceeds 200ms (SHEL-12)"
    - "task perf:shell is marked with # lint-allow: cmds-without-status (measurement task; always-re-run is by design)"
    - "shell/README.md establishes the DOCS-02 contract template: purpose, key files, how-to-add-pattern (alias / function / feature flag)"
    - "After task install completes on a converged personal-laptop, every shell symlink in $ZDOTDIR is a valid symlink pointing into the repo"
    - "task lint exits 0 on the new taskfiles/links.yml and taskfiles/shell.yml (no $VAR in status, no bare ln -s, all install-style tasks have status:)"
  artifacts:
    - path: "taskfiles/links.yml"
      provides: "Real link orchestration replacing links-stub.yml; P3 ships zsh + antidote symlinks; P4-P7 extend"
      contains: "task: _:safe-link"
    - path: "taskfiles/shell.yml"
      provides: "task perf:shell — hyperfine-based 200ms cold-start gate (SHEL-12)"
      contains: "hyperfine"
    - path: "Taskfile.yml"
      provides: "Root taskfile with links: pointing at real links.yml (no longer the stub); perf: include added"
      contains: "./taskfiles/links.yml"
    - path: "shell/README.md"
      provides: "Sibling-README anchor for DOCS-02; covers purpose / key files / how-to-add-pattern"
      contains: "_dotfiles_feature"
  key_links:
    - from: "Taskfile.yml includes: links:"
      to: "./taskfiles/links.yml"
      via: "go-task includes block (drop -stub)"
      pattern: "links: ./taskfiles/links.yml"
    - from: "taskfiles/links.yml"
      to: "taskfiles/helpers.yml _:safe-link / _:check-link"
      via: "includes: _: ./helpers.yml"
      pattern: "_:safe-link"
    - from: "taskfiles/shell.yml task perf:shell"
      to: "hyperfine"
      via: "shell command invocation"
      pattern: "hyperfine"
---

<objective>
Wire the shell-layer artifacts created in Plans 02-04 into the task graph. Replace `taskfiles/links-stub.yml` with a real `taskfiles/links.yml` that symlinks the five startup files + antidote bundle source via `_:safe-link`. Create `taskfiles/shell.yml` hosting `task perf:shell` — a hyperfine-based cold-start gate that exits non-zero above 200ms (SHEL-12). Update `Taskfile.yml` to point `includes: links:` at the new file (and add `perf:` include). Write `shell/README.md` as the DOCS-02 sibling-README anchor.

Purpose: This plan is the integration layer — `task install` on a converged machine now exercises the full shell-layer install path, and `task perf:shell` provides the SHEL-12 enforcement gate. The README is the DOCS-02 template anchor that Phase 4-7 will replicate when they replace their stub READMEs.

Output: 4 files (2 new taskfiles, 1 root Taskfile edit, 1 new README). No alias/function/startup-file changes (Plans 02-04 own those). No symlink creation at write-time — `_:safe-link` creates them at `task install` time.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shell-layer-flat-content-port/03-CONTEXT.md
@.planning/phases/03-shell-layer-flat-content-port/03-PATTERNS.md
@.planning/phases/03-shell-layer-flat-content-port/03-02-PLAN.md
@.planning/phases/03-shell-layer-flat-content-port/03-03-PLAN.md
@.planning/phases/03-shell-layer-flat-content-port/03-04-PLAN.md
@.planning/phases/02-install-engine-bootstrap-idempotency-lint/02-CONTEXT.md
@CLAUDE.md
@.claude/CLAUDE.md
@Taskfile.yml
@taskfiles/helpers.yml
@taskfiles/links-stub.yml
@taskfiles/links.yml
@taskfiles/manifest.yml
@taskfiles/lint.yml
@manifests/README.md
@install/messages.zsh

<interfaces>
Existing infrastructure (from Phases 1-2):

- `taskfiles/helpers.yml` provides `_:safe-link` (cmds: mkdir -p parent then `ln -sfn $SOURCE $TARGET`) and `_:check-link` (validates -L + -e on a symlink).
- `Taskfile.yml` root vars: HOME, XDG_CONFIG_HOME, XDG_DATA_HOME, XDG_STATE_HOME, XDG_CACHE_HOME, ZDOTDIR, DOTFILEDIR (= TASKFILE_DIR), HOMEBREW_PREFIX, DOTFILES_MESSAGES, RESOLVED_JSON_PATH, MANIFEST_JSON, MANIFEST.
- `Taskfile.yml` `includes:` block currently points at `links-stub.yml` (line 83). This plan flips to `links.yml`.
- `Taskfile.yml` install task already calls `- task: links:all` (line 120).
- `taskfiles/lint.yml` LINT-02 catches `$VAR` in status: blocks; LINT-03a catches cmds without status (with `# lint-allow: cmds-without-status` exception comment); LINT-03b catches bare `ln -s` outside helpers.yml.

Existing v1 `taskfiles/links.yml` (current; v1-style — soon overwritten):
- Hosts links:zsh, links:git, links:ssh, links:tools, links:claude, links:validate, unlink-*. v1 paths reference `zsh/.zshenv`, `git/config`, `zsh/configs/`, etc. — these are v1-tree paths that this plan must REPLACE with v2-tree paths (shell/.zshenv, etc.).
- Decision per Phase 3 scope: Plan 05 ships ONLY the zsh + antidote subtasks in links.yml. P4 adds git/ssh/identity; P5 adds packages/brew; P6 adds macos; P7 adds tools/claude. The v1 links.yml is a sibling reference; the new file is a much smaller P3-scoped subset.

hyperfine cold-start measurement:
- Command pattern: `hyperfine --warmup 1 --runs 5 --export-json /dev/stdout 'zsh -lic exit'`
- Parse mean via `jq -r '.results[0].mean'` (returns float in seconds)
- Multiply by 1000 → ms, compare against 200 threshold.

Reference for status-block + DOTFILES_MESSAGES pattern: `taskfiles/manifest.yml` lines 144-168 (manifest:resolve task: status block uses {{.X}} only; cmds source `{{.DOTFILES_MESSAGES}}` then call info/success/check/cross).

manifests/README.md format (5 lines + bullet list + adding-a-new-machine instruction) — DOCS-02 template anchor; shell/README.md adopts the same shape.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace taskfiles/links-stub.yml with real taskfiles/links.yml (zsh + antidote subtasks; status blocks; validate task)</name>
  <files>taskfiles/links.yml</files>
  <read_first>
    - taskfiles/links-stub.yml (full file, 22 lines — the stub being replaced; note: the stub file is RETAINED on disk per Phase 2 convention — this plan WRITES a NEW file at taskfiles/links.yml; Task 3 below flips the Taskfile.yml include)
    - taskfiles/links.yml (the existing v1 file — read for reference patterns; this plan WILL overwrite it with a P3-scoped subset)
    - taskfiles/manifest.yml lines 1-50 + 144-168 (status-block pattern + DOTFILES_MESSAGES + includes _: ./helpers.yml)
    - taskfiles/helpers.yml (`_:safe-link`, `_:check-link` signatures and var requirements)
    - .planning/phases/03-shell-layer-flat-content-port/03-PATTERNS.md Pattern 20 "taskfiles/links.yml" (lines 689-753) — exact zsh: task body + antidote: task body + all: aggregator + validate: task
    - Taskfile.yml lines 31-37 (the vars block defining ZDOTDIR, DOTFILEDIR — vars in scope when links.yml is loaded as an include)
    - taskfiles/lint.yml (the lint rules this file must satisfy: LINT-02 $VAR in status, LINT-03a cmds without status, LINT-03b bare ln -s)
  </read_first>
  <action>
    Create `taskfiles/links.yml` (replacing the v1 file at that path — `git diff` will show a full rewrite). The file is the real implementation of links orchestration, P3-scoped (zsh + antidote only).

    File structure:

    1. `version: '3'` line then a file-header banner comment block (`# ===`-style separator per CLAUDE.md) documenting: purpose (P3 link orchestration; replaces links-stub.yml), scope (zsh startup files + antidote bundle source; P4-P7 extend), dependencies (helpers.yml for _:safe-link and _:check-link), status-block convention (LINT-02: template vars only, never shell vars).

    2. `includes: _: ./helpers.yml`.

    3. `vars:` block (optional; vars inherit from root Taskfile.yml when this file is loaded as an include, but a local `DOTFILES_MESSAGES` fallback keeps direct invocation `task -t taskfiles/links.yml ...` working — mirror taskfiles/manifest.yml lines 35-49).

    4. `tasks:` block with four tasks:

       **`all`** (aggregator):
       - `desc: "Create all symlinks (P3: shell only; later phases extend)"`
       - `cmds: - task: zsh\n  - task: antidote`
       - `status:` block: a single multi-line check that runs `test -L "{{.ZDOTDIR}}/.zshenv" && test -L "{{.ZDOTDIR}}/.zsh_plugins.txt"` (status passes when both subtasks would be no-ops; go-task evaluates subtask status independently anyway, but a top-level status block satisfies LINT-03a hygiene).

       **`zsh`** (per PATTERNS.md Pattern 20 lines 694-714):
       - `desc: "Link zsh configuration files"`
       - Five `cmds:` entries, each `- task: _:safe-link\n  vars: { SOURCE: "{{.DOTFILEDIR}}/shell/<file>", TARGET: "{{.ZDOTDIR}}/<file>" }` for: `.zshenv`, `.zprofile`, `.zshrc`, `.zlogin`, `.zlogout`.
       - `status:` block with five `test -L "{{.ZDOTDIR}}/<file>"` lines covering the same five files. LINT-02 compliance: every var inside status uses `{{.X}}` form; NO `$VAR` shell-style references.

       **`antidote`** (per PATTERNS.md Pattern 20 lines 716-724):
       - `desc: "Link antidote plugin manifest"`
       - One `cmds:` entry: `- task: _:safe-link\n  vars: { SOURCE: "{{.DOTFILEDIR}}/configs/antidote/zsh_plugins.txt", TARGET: "{{.ZDOTDIR}}/.zsh_plugins.txt" }`.
       - `status:` block: `test -L "{{.ZDOTDIR}}/.zsh_plugins.txt"`.

       **`validate`** (per PATTERNS.md Pattern 20 lines 738-745):
       - `desc: "Validate all P3 symlinks"`
       - cmds: six `- task: _:check-link\n  vars: { TARGET: "{{.ZDOTDIR}}/<file>", NAME: "<name>" }` entries — one per .zshenv/.zprofile/.zshrc/.zlogin/.zlogout/.zsh_plugins.txt.
       - No status: block (validate is always-re-run for diagnostic output — mark `# lint-allow: cmds-without-status` per LINT-03a).

    Constraints:
    - NEVER use `$VAR` (shell variable) inside any `status:` block — only `{{.X}}` template vars (LINT-02 enforcement; the macos:shell:145 bug class).
    - NEVER use bare `ln -s` outside helpers.yml — all symlinks via `_:safe-link` (LINT-03b enforcement).
    - Every install task has a `status:` block (LINT-03a enforcement); `validate` is exempted via the inline `# lint-allow:` marker.
    - DROP the v1 file's `links:git`, `links:ssh`, `links:tools`, `links:claude`, `unlink-*` tasks — they belong to P4 (git/ssh), P5 (none), P6 (none), P7 (tools/claude). When those phases land they'll extend `all:` and add their own subtasks. Plan 05 does NOT create stubs for them — clean P3-scoped file only.

    Do NOT add any `set -euo pipefail` — YAML file, not a shell script.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && task --list-all --json -t taskfiles/links.yml > /dev/null && grep -q 'task: _:safe-link' taskfiles/links.yml && grep -q 'shell/.zshenv' taskfiles/links.yml && grep -q 'configs/antidote/zsh_plugins.txt' taskfiles/links.yml && ! grep -E "\\\$[A-Z]" taskfiles/links.yml | grep -v '#' | grep -qE 'status:' && ! grep -E '^\s*-?\s*ln -s' taskfiles/links.yml && grep -q 'lint-allow: cmds-without-status' taskfiles/links.yml && grep -c 'test -L' taskfiles/links.yml | grep -qE '^[5-9]|^1[0-9]' && ! grep -q 'links:git\|links:ssh\|links:tools\|links:claude\|unlink-' taskfiles/links.yml && task lint:taskfile -t taskfiles/lint.yml 2>&1 | tail -5 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `task --list-all --json -t taskfiles/links.yml` exits 0 (valid YAML; go-task parses)
    - `task --list-all -t taskfiles/links.yml` lists the four tasks: all, zsh, antidote, validate
    - `grep -c '_:safe-link' taskfiles/links.yml` returns at least 6 (5 for zsh + 1 for antidote)
    - `grep -c 'shell/\.zshenv\|shell/.zshenv' taskfiles/links.yml` returns at least 1
    - `grep -c 'shell/\.zprofile\|shell/.zprofile' taskfiles/links.yml` returns at least 1
    - `grep -c 'shell/\.zshrc\|shell/.zshrc' taskfiles/links.yml` returns at least 1
    - `grep -c 'shell/\.zlogin\|shell/.zlogin' taskfiles/links.yml` returns at least 1
    - `grep -c 'shell/\.zlogout\|shell/.zlogout' taskfiles/links.yml` returns at least 1
    - `grep -c 'configs/antidote/zsh_plugins.txt' taskfiles/links.yml` returns at least 1
    - `grep -c '.zsh_plugins.txt' taskfiles/links.yml` returns at least 2 (SOURCE and TARGET in symlink task)
    - LINT-02 compliance: `awk '/status:/,/^[^[:space:]-]/' taskfiles/links.yml | grep -E '\$[A-Z_][A-Z_0-9]*[^_a-zA-Z0-9]' | grep -v '^#'` returns nothing (no $VAR in status blocks)
    - LINT-03b compliance: `grep -E '^[[:space:]]*-?[[:space:]]*ln[[:space:]]+-s' taskfiles/links.yml` returns nothing (no bare ln -s)
    - `grep -c 'lint-allow: cmds-without-status' taskfiles/links.yml` returns at least 1 (validate task marker)
    - Removed from v1: `grep -c 'links:git\|links:ssh\|links:tools\|links:claude\|unlink-' taskfiles/links.yml` returns 0 (P3 scope only)
    - Removed from v1: `grep -c 'CLAUDE_CONFIG_HOME' taskfiles/links.yml` returns 0
    - Removed from v1: `grep -c 'PROFILE' taskfiles/links.yml` returns 0 (no profile references)
    - `task lint:taskfile` (run from repo root) does NOT report new errors on taskfiles/links.yml
  </acceptance_criteria>
  <done>taskfiles/links.yml is the real P3-scoped link orchestrator: 4 tasks (all, zsh, antidote, validate); all install-style tasks have status: blocks using template vars only; v1's git/ssh/tools/claude/unlink-* tasks dropped (later phases own them).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create taskfiles/shell.yml with task perf:shell (hyperfine cold-start gate, ≤200ms enforcement — SHEL-12)</name>
  <files>taskfiles/shell.yml</files>
  <read_first>
    - taskfiles/manifest.yml (full file — section header convention, DOTFILES_MESSAGES, status patterns, lint-allow comments)
    - taskfiles/lint.yml lines 75-83 (the default aggregator example of a no-status cmds task — uses `# lint-allow: cmds-without-status` comment; same pattern applies to perf:shell)
    - .planning/phases/03-shell-layer-flat-content-port/03-PATTERNS.md Pattern 21 "taskfiles/shell.yml" (lines 755-799) — exact perf:shell body
    - .planning/phases/03-shell-layer-flat-content-port/03-CONTEXT.md "Claude's Discretion → task perf:shell measurement tool" (hyperfine is the canonical choice)
    - REQUIREMENTS.md SHEL-12 (cold interactive shell startup under 200ms; fails CI if exceeded)
    - install/messages.zsh (info/success/error/check functions; sourced via {{.DOTFILES_MESSAGES}})
  </read_first>
  <action>
    Create `taskfiles/shell.yml`. Single-task file (perf:shell only — Plan 05 doesn't ship other shell tasks; future plans may extend).

    File structure:

    1. `version: '3'` line.

    2. File-header banner: purpose ("Shell-layer measurement tasks for the v2 shell stack"), tasks ("perf:shell — SHEL-12 cold-start gate via hyperfine"), self-exemption note ("perf:shell is a measurement task; always-re-run is correct; marked with `# lint-allow: cmds-without-status`").

    3. `includes: _: ./helpers.yml` (so DOTFILES_MESSAGES is wired the same way as other taskfiles).

    4. `vars:` block — same shape as `taskfiles/manifest.yml` lines 35-49: declare DOTFILEDIR (sh: dirname twice from TASKFILE_DIR for direct invocation; root Taskfile.yml's var wins when included), DOTFILES_MESSAGES.

    5. `tasks:` block with exactly one task:

       **`shell`** (per PATTERNS.md Pattern 21 lines 773-794):
       - Comment line above the task: `# lint-allow: cmds-without-status`.
       - `desc: "Measure cold interactive zsh startup time (fails if > 200ms — SHEL-12)"`.
       - `cmds:` block, single multi-line `|` literal containing:
         - Source DOTFILES_MESSAGES: `{{.DOTFILES_MESSAGES}}` on its own line.
         - Existence check for hyperfine: `if ! command -v hyperfine >/dev/null 2>&1; then error "hyperfine not installed (brew install hyperfine)"; exit 1; fi`.
         - Run hyperfine: `result=$(hyperfine --warmup 1 --runs 5 --export-json /dev/stdout "zsh -lic exit" 2>/dev/null | jq -r '.results[0].mean')`. Mean is returned in seconds (float).
         - Compute ms: `ms=$(echo "$result * 1000" | bc | awk '{printf "%d\n", $0}')` (multiplies seconds by 1000, truncates to integer ms).
         - Report: `info "cold shell start: ${ms}ms (target: ≤ 200ms)"`.
         - Threshold check: `if (( ms > 200 )); then error "shell start exceeds 200ms target (SHEL-12)"; exit 1; fi`.
         - Success: `check "shell start within budget"`.

    Constraints:
    - perf:shell is invoked as `task perf:shell` from the root taskfile (Task 3 below wires the include: `perf: ./taskfiles/shell.yml`).
    - Task name MUST be `shell` (root-level invocation becomes `perf:shell` via the include alias).
    - No `status:` block (measurement; always-re-run); the inline `# lint-allow: cmds-without-status` comment exempts it from LINT-03a.
    - Use `{{.X}}` template vars for any var reference (LINT-02 still applies even outside status: blocks per repo convention).
    - bc + awk are macOS-native; jq is in Phase 1's tool stack; hyperfine is in Phase 5 core Brewfile per CONTEXT.md (the existence check graceful-fails when hyperfine is missing — useful in CI before Phase 5 brews land).
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && task --list-all --json -t taskfiles/shell.yml > /dev/null && grep -q 'hyperfine' taskfiles/shell.yml && grep -q '200' taskfiles/shell.yml && grep -q 'SHEL-12' taskfiles/shell.yml && grep -q 'lint-allow: cmds-without-status' taskfiles/shell.yml && grep -q 'zsh -lic exit' taskfiles/shell.yml && grep -q 'jq -r' taskfiles/shell.yml && task --list -t taskfiles/shell.yml | grep -q '^\* shell:' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `task --list-all --json -t taskfiles/shell.yml` exits 0
    - `task --list -t taskfiles/shell.yml` lists exactly one task whose name is `shell`
    - `grep -c 'hyperfine' taskfiles/shell.yml` returns at least 2 (existence check + invocation)
    - `grep -c '200' taskfiles/shell.yml` returns at least 2 (threshold check + desc string)
    - `grep -c 'SHEL-12' taskfiles/shell.yml` returns at least 1
    - `grep -c 'lint-allow: cmds-without-status' taskfiles/shell.yml` returns 1
    - `grep -c 'zsh -lic exit' taskfiles/shell.yml` returns 1 (cold-start invocation pattern)
    - `grep -c '\.results\[0\]\.mean' taskfiles/shell.yml` returns 1 (hyperfine JSON parse)
    - `grep -c 'warmup 1' taskfiles/shell.yml` returns 1 (hyperfine warmup arg present)
    - `grep -c 'runs 5' taskfiles/shell.yml` returns 1 (hyperfine runs arg present)
    - `grep -c 'brew install hyperfine' taskfiles/shell.yml` returns 1 (helpful error when hyperfine missing)
    - `grep -c 'DOTFILES_MESSAGES' taskfiles/shell.yml` returns at least 1 (messages library used)
    - LINT-02: no `$VAR` references inside any `status:` block (there are no status: blocks)
    - LINT-03a: `grep -c 'status:' taskfiles/shell.yml` returns 0 (no status: at all; lint-allow exempts)
    - `task lint:taskfile` does NOT report new errors on taskfiles/shell.yml
    - Smoke test (hyperfine installed): `task perf:shell -t taskfiles/shell.yml --silent 2>&1 | grep -qE 'cold shell start: [0-9]+ms'` succeeds when hyperfine is available
  </acceptance_criteria>
  <done>taskfiles/shell.yml hosts task `shell` (invoked as perf:shell from root) — hyperfine cold-start measurement with 200ms gate per SHEL-12; lint-clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Taskfile.yml — flip links: include from -stub to real; add perf: include pointing at shell.yml</name>
  <files>Taskfile.yml</files>
  <read_first>
    - Taskfile.yml (full file, 128 lines — the includes: block is at lines 72-86; line 83 currently reads `links:    ./taskfiles/links-stub.yml      # P3 wires real bodies`)
    - .planning/phases/03-shell-layer-flat-content-port/03-PATTERNS.md Pattern 20 final note (lines 747-749) — "Update Taskfile.yml: flip the include from ./taskfiles/links-stub.yml to ./taskfiles/links.yml"
    - Taskfile.yml lines 16-18 (the header comments describing the includes block — these line up with the current state and should be updated to remove the "stub; P3 wires real bodies" annotation for links)
    - taskfiles/links.yml (created in Task 1)
    - taskfiles/shell.yml (created in Task 2)
  </read_first>
  <action>
    Edit `Taskfile.yml` with two surgical changes (no other modifications):

    Change 1 — the `includes:` block (currently at lines 72-86):
    - Line 83 currently reads: `  links:    ./taskfiles/links-stub.yml      # P3 wires real bodies`
    - REPLACE with: `  links:    ./taskfiles/links.yml`
    - Add a new line BEFORE the brew include (so the `perf:` include sits alphabetically/logically among the others — pick a position; suggested: between `links:` and `brew:` since perf is closely related to shell-layer link wiring):
      `  perf:     ./taskfiles/shell.yml`
    - Drop the trailing `# P3 wires real bodies` annotation from the links line (it's no longer a stub).

    Change 2 — the file-header includes-list comment (lines 13-23):
    - Line 15-19 currently lists stubs/reals:
      `#   - manifest (P1, real)`
      `#   - lint     (P2, real)`
      `#   - links    (stub; P3 wires real bodies)`
      `#   - brew     (stub; P5 wires real bodies)`
      ...
    - Update line about `links` to: `#   - links    (P3, real)`.
    - Add a new line for the perf include: `#   - perf     (P3, real)` — slot it appropriately (before `brew` is fine).

    Do NOT change:
    - Any `vars:` declaration (HOME, XDG_*, ZDOTDIR, DOTFILEDIR, HOMEBREW_PREFIX, DOTFILES_MESSAGES, RESOLVED_JSON_PATH, MANIFEST_JSON, MANIFEST).
    - The `manifest:` include or its `vars:` forwarding block (Phase 1's quirky `manifest:` prefix; Phase 2 D-12 documented).
    - The `default` task, `install` task body, or preconditions.
    - The cutover-gate precondition or `deps: [manifest:manifest:resolve]`.
    - Any other line in the file.

    Constraints:
    - Maintain YAML indentation (two spaces per level — go-task is strict).
    - The `links-stub.yml` file itself REMAINS on disk after this edit (it was retained for Phase 2's lint-test fixture coverage). Only the include reference moves.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && task --list-all --json > /dev/null && grep -q '^  links:    ./taskfiles/links.yml' Taskfile.yml && grep -q '^  perf:     ./taskfiles/shell.yml' Taskfile.yml && ! grep -q 'links-stub.yml' Taskfile.yml | head -1 && grep -q 'links    (P3, real)' Taskfile.yml && task --list 2>&1 | grep -q 'perf:shell\|^\* perf:' && task --list 2>&1 | grep -q 'links:zsh\|links:all' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `task --list-all --json` exits 0 (root taskfile still parses after edits)
    - `grep -c '^  links:    ./taskfiles/links.yml' Taskfile.yml` returns 1
    - `grep -c '^  perf:     ./taskfiles/shell.yml' Taskfile.yml` returns 1
    - `grep -c 'links-stub.yml' Taskfile.yml` returns 0 (stub reference removed from the includes:)
    - `grep -c 'P3 wires real bodies' Taskfile.yml` returns 0 (annotation removed for links; brew/claude/macos still have it because they remain stubs)
    - `grep -c 'links    (P3, real)' Taskfile.yml` returns 1 (header comment updated)
    - `grep -c 'perf     (P3, real)' Taskfile.yml` returns 1 (header comment line for perf)
    - `task --list 2>&1 | grep -c 'links:all'` returns at least 1 (links subtasks now visible from root)
    - `task --list 2>&1 | grep -c 'perf:shell'` returns at least 1 (perf:shell visible from root)
    - `task --list 2>&1 | grep -c 'manifest:manifest:resolve'` returns at least 1 (preserved — Phase 1 quirk still works)
    - `grep -c '^  manifest:$' Taskfile.yml` returns 1 (manifest include preserved)
    - `grep -c '^  lint:' Taskfile.yml` returns 1 (lint include preserved)
    - `grep -c '^  brew:' Taskfile.yml` returns 1 (brew stub preserved)
    - `grep -c '^  claude:' Taskfile.yml` returns 1 (claude stub preserved)
    - `grep -c '^  macos:' Taskfile.yml` returns 1 (macos stub preserved)
    - `task install --dry-run 2>&1 | grep -qE 'links:all|links:zsh'` succeeds (the install graph now includes the real links task)
    - `wc -l Taskfile.yml` reports approximately 128-130 (small line delta from adding 1 perf: line + 1 header comment line; net +2)
  </acceptance_criteria>
  <done>Taskfile.yml includes: block now points at the real taskfiles/links.yml and the new taskfiles/shell.yml (as `perf:`); links-stub.yml is no longer referenced from the root; root task --list shows links:all, links:zsh, links:antidote, links:validate, perf:shell.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Write shell/README.md as the DOCS-02 sibling-README anchor (purpose, key files, how-to-add-pattern)</name>
  <files>shell/README.md</files>
  <read_first>
    - shell/README.md (current stub — 10 lines; being replaced with the real anchor)
    - manifests/README.md (the existing sibling-README; DOCS-02 template reference per PATTERNS.md Pattern 22)
    - .planning/phases/03-shell-layer-flat-content-port/03-PATTERNS.md Pattern 22 "shell/README.md" (lines 803-841) — recommended structure quoted in full
    - .planning/phases/03-shell-layer-flat-content-port/03-CONTEXT.md `<decisions>` "Claude's Discretion → Sibling-README pattern propagation" (DOCS-02 contract anchor; P4-P7 replace their stubs during their phase work)
    - REQUIREMENTS.md DOCS-02 ("Each top-level directory has a README.md covering purpose, key files, and how-to-add patterns")
    - CLAUDE.md "Adding Things" table (the canonical reference for shell aliases / functions / feature flags)
  </read_first>
  <action>
    Replace `shell/README.md` (currently a 10-line placeholder stub) with the real DOCS-02 anchor document. Use PATTERNS.md Pattern 22 lines 808-841 as the structural template (do NOT copy verbatim; tailor the content to match the actual ported state from Plans 02-04).

    Required sections:

    1. **Top-level heading**: `# shell`

    2. **Purpose paragraph** (2-3 sentences): zsh startup files, theme, aliases, and functions. Sourced by every login or interactive shell on a converged v2 machine. macOS-only in v1; flat layout with Linux platform-aware split deferred to v2 (cite ROADMAP).

    3. **Key files** subsection (`## Key files`): bulleted list covering:
       - `.zshenv` / `.zprofile` / `.zshrc` / `.zlogin` / `.zlogout` — startup files in zsh's documented order; one role per file.
       - `theme.zsh` — alanpeabody-based prompt; consumed by `.zshrc` after antidote loads `omz-git`.
       - `aliases/<topic>.zsh` — flat layout, one topic per file; gating happens inside the file (wrapper functions for 1-3 aliases per D-07; source-time `return 0` for bulk-alias loops per D-08).
       - `functions/<name>.zsh` — flat layout, one function per file; filename matches function name; `_dotfiles_feature` is the lazy manifest reader callers use to gate behavior.

    4. **Adding a pattern** subsection (`## Adding a pattern`): three bulleted "how to add" instructions matching CLAUDE.md "Adding Things" table:
       - **An alias**: create `aliases/<topic>.zsh`. If the alias is GUI-coupled or identity-coupled, gate inside the file: wrapper-function gate for 1-3 aliases (call `_dotfiles_feature <name>` inside each function); source-time gate for bulk-alias loops (prepend `[[ "$(_dotfiles_feature <name>)" == "true" ]] || return 0`).
       - **A function**: create `functions/<name>.zsh`; filename equals function name; add a docstring as an inline comment on the function-definition line (the `aliaslist`/`functionlist` discovery convention).
       - **A feature flag**: add the kebab-case key to `manifests/defaults.toml` `[features]` with `false`. Set `true` in the machine TOMLs that want it. Consumers call `_dotfiles_feature <key>` to test.

    5. **Performance budget** subsection (`## Performance budget`): cold interactive shell start ≤ 200ms; measured via `task perf:shell` (SHEL-12); re-measure on every plugin or startup-file change.

    6. **References** subsection (`## References`): bulleted list pointing to: `../docs/MANIFEST.md` (manifest schema), `../CLAUDE.md` (v2 conventions), `../.planning/REQUIREMENTS.md` (SHEL-01 through SHEL-12 + DOCS-02).

    Format constraints:
    - Markdown only; NO HTML.
    - NO emojis (project convention is stricter than the global no-emojis-in-non-md rule).
    - File MUST be less than 100 lines (declarative, scoped, terse — match `manifests/README.md` energy).
    - NO AI attribution; no "generated by" comments.
    - All code-fenced examples use `zsh` or `toml` as the language tag.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ -f shell/README.md ] && head -1 shell/README.md | grep -qx '# shell' && grep -q '## Key files' shell/README.md && grep -q '## Adding a pattern' shell/README.md && grep -q '## Performance budget' shell/README.md && grep -q '_dotfiles_feature' shell/README.md && grep -q '200ms\|≤ 200ms' shell/README.md && grep -q 'task perf:shell' shell/README.md && grep -q 'SHEL-12' shell/README.md && grep -q 'D-07\|wrapper-function' shell/README.md && grep -q 'D-08\|source-time' shell/README.md && [ "$(wc -l < shell/README.md)" -lt 100 ] && ! grep -qE '🎯|✨|🚀|⚡|✅|❌|🔥|💡|👍|👀|📝|📦|🎉' shell/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `shell/README.md` exists and is NOT the original 10-line stub (`wc -l shell/README.md` returns > 30)
    - `shell/README.md` is fewer than 100 lines (terse-and-scoped per manifests/README.md energy)
    - `head -1 shell/README.md` is exactly `# shell`
    - `grep -c '## Key files' shell/README.md` returns 1
    - `grep -c '## Adding a pattern' shell/README.md` returns 1
    - `grep -c '## Performance budget' shell/README.md` returns 1
    - `grep -c '_dotfiles_feature' shell/README.md` returns at least 1 (helper is referenced in adding-a-pattern section)
    - `grep -c '200ms' shell/README.md` returns at least 1
    - `grep -c 'task perf:shell' shell/README.md` returns at least 1
    - `grep -c 'SHEL-12' shell/README.md` returns at least 1 (requirement traceability)
    - `grep -c 'DOCS-02' shell/README.md` returns at least 1 (requirement traceability; OR cite the contract via the "every top-level dir" phrasing)
    - `grep -cE 'wrapper.function|D-07' shell/README.md` returns at least 1 (D-07 gate pattern referenced)
    - `grep -cE 'source.time|D-08' shell/README.md` returns at least 1 (D-08 gate pattern referenced)
    - `grep -c 'flat layout\|flat directory' shell/README.md` returns at least 1 (CF-01 / Phase 1 D-10 referenced)
    - `grep -c '.zshenv\|zshenv' shell/README.md` returns at least 1
    - `grep -c '.zshrc\|zshrc' shell/README.md` returns at least 1
    - `grep -c 'theme.zsh' shell/README.md` returns at least 1
    - `grep -c 'antidote\|omz-git' shell/README.md` returns at least 1 (theme dependency documented)
    - No emojis: `grep -cE '[\x{1F300}-\x{1F9FF}]|🎯|✨|🚀|⚡|✅|❌|🔥|💡' shell/README.md` returns 0 (project convention)
    - No AI attribution: `grep -cE 'AI-generated|Co-Authored|generated by' shell/README.md` returns 0
  </acceptance_criteria>
  <done>shell/README.md is the DOCS-02 contract anchor: purpose + key files + how-to-add-pattern (alias / function / feature flag) + performance budget; terse and scoped to match manifests/README.md energy; P4-P7 will replicate this shape when their stubs become real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `task install` → `_:safe-link` → `ln -sfn $SOURCE $TARGET` | The helper creates symlinks under `$ZDOTDIR`. Both SOURCE and TARGET are template vars resolved at task-graph build time from root Taskfile.yml — no user input. |
| `task perf:shell` → `hyperfine 'zsh -lic exit'` | Hyperfine spawns repeated `zsh -lic exit` invocations. Each spawned shell sources `.zshenv`, `.zprofile`, `.zshrc`, `.zlogin`, `.zlogout` — the full v2 stack. |
| `shell/README.md` documentation file | Static markdown; no executable surface. |
| `Taskfile.yml` includes: block | Loads two new YAML files as task-graph children. Files are repo-committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Tampering | links.yml SOURCE/TARGET expansion | mitigate | All `_:safe-link` invocations use `{{.DOTFILEDIR}}/shell/...` and `{{.ZDOTDIR}}/...` — template vars are resolved from root Taskfile.yml at task-graph build time. No CLI_ARGS or user input flows into the symlink paths; injection surface is zero. |
| T-03-18 | Denial of Service | `task perf:shell` hyperfine misconfiguration | mitigate | Existence check (`command -v hyperfine`) emits an actionable error message and exits 1 if hyperfine is absent. The `--runs 5` arg bounds invocation count; the `2>/dev/null | jq` pipe swallows hyperfine stderr but `jq` exits non-zero on missing `.results[0].mean`, surfacing to the script via `set -e` (go-task default for `cmds:` blocks). |
| T-03-19 | Information Disclosure | `task perf:shell` output | accept | The task prints `cold shell start: <ms>ms` to stdout. No PII, no machine identifiers, no secrets. |
| T-03-20 | Tampering | Taskfile.yml include reference flip | accept | The flip from `links-stub.yml` to `links.yml` is a repo-committed change; tampering requires repo write. Both files exist on disk; the stub remains as a Phase 2 lint-fixture artifact. |
</threat_model>

<verification>
After all four tasks complete:

1. `task --list-all --json` exits 0 (root taskfile parses with new includes).
2. `task --list` shows: `default`, `install`, `links:all`, `links:zsh`, `links:antidote`, `links:validate`, `perf:shell` (plus existing manifest:* and lint:* tasks).
3. `grep -c 'links-stub.yml' Taskfile.yml` returns 0 (include reference flipped).
4. `task lint` exits 0 on the new taskfiles/links.yml and taskfiles/shell.yml.
5. `shell/README.md` exists, is < 100 lines, covers all DOCS-02 contract sections.
6. Dry-run install graph: `task install --dry-run 2>&1 | grep -q 'links:zsh\|links:antidote'` succeeds.
7. End-to-end (on a machine where Plans 01-04 have already executed and the user runs `task setup -- personal-laptop && task install`): all five `.z*` symlinks exist at `$ZDOTDIR/`, the `.zsh_plugins.txt` symlink exists at `$ZDOTDIR/.zsh_plugins.txt`, `task perf:shell` reports a cold-start measurement, and `task links:validate` shows all green checkmarks.
</verification>

<success_criteria>
- taskfiles/links.yml is the real implementation: 4 tasks (all, zsh, antidote, validate); all install-style tasks have status: blocks using template vars; v1's git/ssh/tools/claude tasks dropped (P4-P7 own them)
- taskfiles/shell.yml hosts task perf:shell with hyperfine cold-start gate (≤200ms, SHEL-12)
- Taskfile.yml includes: block flipped from links-stub to links.yml; perf include added
- shell/README.md is the DOCS-02 sibling-README anchor (purpose, key files, how-to-add-pattern, performance budget)
- task lint exits 0 on the new files
- task install dry-run shows the new links subtasks in the graph
- After full install on personal-laptop, all 6 shell symlinks exist and links:validate is all-green
</success_criteria>

<output>
After completion, create `.planning/phases/03-shell-layer-flat-content-port/03-05-SUMMARY.md` per the template at `~/.config/claude/get-shit-done/templates/summary.md`.
</output>

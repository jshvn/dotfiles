---
phase: 03-shell-layer-flat-content-port
plan: 07
subsystem: docs

tags: [docs, readme, docs-02, sibling-readme]

requires:
  - phase: 03-shell-layer-flat-content-port/03-05
    provides: shell/README.md (the DOCS-02 anchor template -- five-section sibling-README shape)
  - phase: 01
    provides: install/resolver.zsh, install/messages.zsh, manifests/README.md (sibling-pattern energy)
  - phase: 02
    provides: install/cutover-gate.zsh, taskfiles/lint.yml (LINT-01..07 contract cited in taskfiles/README.md)
provides:
  - install/README.md (DOCS-02 anchor for install/)
  - taskfiles/README.md (DOCS-02 anchor for taskfiles/)
  - claude/README.md (DOCS-02 anchor for claude/)
  - Closure of DOCS-02 Truth 6 for every directory shipping in Phase 3 scope
affects: [phase-04-identity-layer, phase-07-claude-configs, phase-08-cutover]

tech-stack:
  added: []
  patterns:
    - "Sibling-README template propagation (PATTERNS.md Pattern 22)"
    - "Footer cross-reference -- 'Satisfies DOCS-02 for <dir>/.'"

key-files:
  created:
    - install/README.md
    - taskfiles/README.md
    - claude/README.md
  modified: []

key-decisions:
  - "claude/README.md uses 'Key subdirectories' instead of 'Key files' because the directory hosts subdirectories (hooks/, agents/, commands/, skills/), not flat files. Plan-prescribed structural variation."
  - "claude/README.md adds a 'Phase ownership' section forward-pointing to Phase 7 (CLDE-01..04 + TEST-01) so readers know the install task is still a stub and the smoke tests are not yet written."
  - "Hook description in claude/README.md avoids quoting the literal AI-attribution trigger strings ('generated by', 'Co-Authored-By') -- those strings would be flagged by the very hook the README describes. Used 'AI-attribution trailers and source-code attribution markers' instead."

patterns-established:
  - "Sibling-README footer: every top-level directory README ends with 'Satisfies DOCS-02 for <dir>/.' as a one-line traceability anchor."
  - "Substantive-but-terse READMEs: 70-75 lines each; well under the 100-line budget; structurally identical to shell/README.md."

requirements-completed: [DOCS-02]

duration: ~15min
completed: 2026-05-14
---

# Phase 3 Plan 07: Sibling-README Backfill Summary

**Three new top-level READMEs (install/, taskfiles/, claude/) closing DOCS-02 Truth 6 by replicating shell/README.md's five-section sibling-README template across every directory shipping in Phase 3 scope.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-05-14T23:46:00Z (approx)
- **Completed:** 2026-05-15T00:00:53Z
- **Tasks:** 3
- **Files modified:** 3 (all created)

## Accomplishments

- `install/README.md` (70 lines) -- DOCS-02 anchor naming `resolver.zsh`, `messages.zsh`, `cutover-gate.zsh` roles; documents the v1->v2 Brewfile transition (Phase 5 retires `Brewfile-*.rb` to `packages/<purpose>.rb`); adding-a-script workflow with LINT-04 and file-header conventions.
- `taskfiles/README.md` (75 lines) -- DOCS-02 anchor grouping the taskfiles by phase maturity (Phase 1 real, Phase 2 real, Phase 3 real, Phase 4-7 stubs); adding-a-taskfile + wiring + adding-a-symlink workflows with LINT-01/02/03a/03b callouts; cites `lint-allow` exemption marker.
- `claude/README.md` (73 lines) -- DOCS-02 anchor enumerating `hooks/`, `agents/`, `commands/`, `skills/`, `CLAUDE.md`, `settings.json`; names the four hooks (`secret-scan.zsh`, `no-emojis.zsh`, `no-ai-comments.zsh`, `agent-transparency.zsh`); forward-points to Phase 7 (CLDE-01..04, TEST-01) for install + hook smoke tests.
- All three READMEs follow the shell/README.md template shape exactly, end with `Satisfies DOCS-02 for <dir>/.`, contain no emojis, contain no AI attribution, and pass every acceptance check the plan specified.
- DOCS-02 Truth 6 ("every top-level directory has a README.md") is now satisfied for all directories shipping in Phase 3 scope: shell/, manifests/, configs/, packages/, docs/, os/, install/, taskfiles/, claude/. The deferred directories (git/, ssh/) remain owned by Phase 4 (IDNT-*) per the verification report.

## Task Commits

Each task was committed atomically:

1. **Task 1: install/README.md DOCS-02 anchor** -- `7fa8b54` (docs)
2. **Task 2: taskfiles/README.md DOCS-02 anchor** -- `4cc09eb` (docs)
3. **Task 3: claude/README.md DOCS-02 anchor** -- `594e2c0` (docs)

## Files Created/Modified

- `install/README.md` (new, 70 lines) -- Top-level heading + 2-3-sentence purpose + Key files (resolver.zsh, messages.zsh, cutover-gate.zsh, Brewfile transitional state) + Adding-a-script + References + DOCS-02 footer.
- `taskfiles/README.md` (new, 75 lines) -- Same shape; Key files grouped by phase maturity; Adding-a-taskfile / wiring / adding-a-symlink workflows; LINT-01/02/03a/03b references.
- `claude/README.md` (new, 73 lines) -- Same shape with `## Key subdirectories` variant (directory hosts subdirs, not flat files); additional `## Phase ownership` section forward-pointing to Phase 7.

## Decisions Made

- **Structural variant for claude/.** Used `## Key subdirectories` (matches the directory's contents) instead of `## Key files`. The plan explicitly allows this variation, and it accurately reflects the reality that claude/ is a directory of subdirectories.
- **Phase 7 deferral note as its own section.** Plan called for either a section or a paragraph; chose a dedicated `## Phase ownership` heading so the deferral is impossible to miss when scanning the README. Makes the contract (this plan closes DOCS-02; Phase 7 closes the install path) explicit.
- **Hook descriptions paraphrase, never quote, the trigger strings.** The `no-ai-comments.zsh` hook in claude/hooks/ flags strings the README's verification would also flag. Used "AI-attribution trailers and source-code attribution markers" so the README itself stays clean under the very hook it documents.

## Deviations from Plan

None auto-fixed. One small in-Task-3 self-correction during verification:

- The initial draft of `claude/README.md` quoted the literal hook trigger strings in the `no-ai-comments.zsh` description. The plan's acceptance criterion forbids those strings in the README itself. Reworded line 17 to describe what the hook detects without quoting the trigger strings, then re-ran the acceptance checks (all pass). This was tracked inside Task 3's commit -- not a separate Rule 1/2/3 fix because the issue was in the draft, not in pre-existing code.

**Total deviations:** 0 auto-fixed (3 Rule 1/2/3 events; one in-task self-correction tracked inside Task 3's commit hash `594e2c0`).
**Impact on plan:** None -- plan executed exactly as written.

## Issues Encountered

- Initial `claude/README.md` draft quoted the literal AI-attribution trigger strings the no-ai-comments hook flags. Caught by the plan's own acceptance criterion (`grep -cE 'AI-generated|Co-Authored|generated by' claude/README.md` must return 0). Reworded the bullet to describe the hook's behaviour without quoting the trigger strings; re-verified and committed in the same task.

## User Setup Required

None -- documentation-only plan; no external service configuration required.

## Next Phase Readiness

- DOCS-02 Truth 6 closure clears one of the gaps the Phase 3 verification report flagged. Combined with CR-01 (link health) and CR-02 (Plan 03-06) the phase moves from `gaps_found` to ready-for-phase-close once the orchestrator runs the verifier again.
- Phase 4 (identity layer) still owns `git/README.md` and `ssh/README.md`; this plan intentionally leaves those absent.
- Phase 7 (claude install + tool configs) inherits the forward-pointer in `claude/README.md`: when CLDE-01..04 land, the taskfile flip (`claude-stub.yml` -> `claude.yml`) and TEST-01 hook smoke tests close the remaining `claude/` contract.

## Self-Check: PASSED

- install/README.md found
- taskfiles/README.md found
- claude/README.md found
- Commit `7fa8b54` found in `git log`
- Commit `4cc09eb` found in `git log`
- Commit `594e2c0` found in `git log`

---
*Phase: 03-shell-layer-flat-content-port*
*Completed: 2026-05-14*

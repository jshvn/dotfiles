---
phase: 12-task-surface-redesign
plan: 07
type: execute
wave: 7
depends_on: [12-01, 12-06]
files_modified:
  - taskfiles/manifest.yml
  - taskfiles/test.yml
  - taskfiles/lint.yml
  - taskfiles/audit.yml
  - taskfiles/show.yml
  - Taskfile.yml
  - docs/MANIFEST.md
  - .claude/CLAUDE.md
autonomous: true
requirements: [SURF-02, SURF-03]

must_haves:
  truths:
    - "`task --list` shows ZERO `manifest:*` rows (resolve, setup, validate, test, test:add-machine all internal)"
    - "`task --list` shows `show:manifest` and `audit:manifest` as the public manifest diagnostics"
    - "`task --list` shows ZERO `test:*` rows (test:default and test:hooks are internal per D-04)"
    - "`task --list` shows ZERO `lint:*` rows except `lint` (alias for lint:default which stays public; sub-checks internal per D-04)"
    - "`task --list-all` shows the renamed test:manifest and test:add-machine (moved from manifest:test* per Claude's-Discretion item)"
    - "`task install` and `task setup -- <machine>` still work end-to-end (the top-level `setup` alias keeps its `manifest:setup` delegation)"
    - "`task lint` passes after every commit"
  artifacts:
    - path: "taskfiles/manifest.yml"
      provides: "Internal-marked manifest tasks; `manifest:test` + `manifest:test:add-machine` moved to `taskfiles/test.yml`"
      exports: ["resolve (internal)", "setup (internal)", "show (internal, exposed via show:manifest)", "validate (internal, exposed via audit:manifest)"]
    - path: "taskfiles/test.yml"
      provides: "New `manifest:` and `add-machine:` tasks (moved from taskfiles/manifest.yml)"
      exports: ["default (internal)", "hooks (internal)", "manifest (internal)", "add-machine (internal)"]
    - path: "taskfiles/lint.yml"
      provides: "All sub-checks marked internal; aggregator `default:` stays public via top-level `lint` alias"
      exports: ["default (public)", "syntax (internal)", "taskfile (internal)", "shell-headers (internal)", "portability (internal)", "test-fixtures (internal)"]
  key_links:
    - from: "Taskfile.yml setup: task"
      to: "taskfiles/manifest.yml setup: task (now internal)"
      via: "task: manifest:setup (cross-namespace short form; works because internal does not affect invocability)"
      pattern: "task: manifest:setup"
    - from: "taskfiles/show.yml"
      to: "taskfiles/manifest.yml show: task"
      via: "task: :manifest:show"
      pattern: "task: :manifest:show"
    - from: "taskfiles/audit.yml"
      to: "taskfiles/manifest.yml validate: task"
      via: "task: :manifest:validate"
      pattern: "task: :manifest:validate"
---

<objective>
Apply the final-cluster rename pass spanning three namespaces:

**manifest namespace (D-01, D-02, D-03, Claude's-Discretion):**
- `manifest:resolve` -> (keep name) + `internal: true` (D-01)
- `manifest:setup` -> (keep name) + `internal: true` (D-01 -- the top-level `setup` alias delegates to this)
- `manifest:show` -> (keep name in source file) + `internal: true`; expose as PUBLIC `show:manifest` via delegate (D-02)
- `manifest:validate` -> (keep name in source file) + `internal: true`; expose as PUBLIC `audit:manifest` via delegate (D-03 -- dual-shape; manifest:validate is also called by the `validate` aggregator iteration loop, which works regardless of internal status)
- `manifest:test` -> move to `taskfiles/test.yml` as `manifest:` (full name `test:manifest`) + `internal: true` (D-04 + Claude's-Discretion)
- `manifest:test:add-machine` -> move to `taskfiles/test.yml` as `add-machine:` (full name `test:add-machine`) + `internal: true` (D-04 + Claude's-Discretion)

**test namespace (D-04):**
- `test:default` -> (keep name) + `internal: true` (D-04 -- aggregators only; sub-task `test:hooks` is invoked by root `task test`)
- `test:hooks` -> (keep name) + `internal: true` (D-04 -- sub-checks internal; root `task test` is the aggregator)
- NEW: `test:manifest` (moved from `manifest:test`) + `internal: true`
- NEW: `test:add-machine` (moved from `manifest:test:add-machine`) + `internal: true`

**lint namespace (D-04):**
- `lint:default` -> (keep name) + (keep public; this is the aliased target of top-level `task lint`)
- `lint:syntax`, `lint:taskfile`, `lint:shell-headers`, `lint:portability`, `lint:test-fixtures` -> each gets `internal: true` (D-04 -- sub-checks internal; only the aggregator is public)

**Doc updates (SURF-02):**
- `docs/MANIFEST.md` manifest task surface table -- rename rows; drop `manifest:resolve` row.
- `.claude/CLAUDE.md` Quick Reference -- drop the `manifest:resolve` bullet; rewrite the `manifest:show` bullet to `task show:manifest`. Per W-5 the edits use content-based locators (find by literal bullet prefix), not absolute line numbers.

**New delegate additions:**
- Extend `taskfiles/show.yml` (created by Plan 06) with a `manifest:` task delegating to `:manifest:show`.
- Extend `taskfiles/audit.yml` (created by Plan 03, extended by Plan 06) with a `manifest:` task delegating to `:manifest:validate`.

Per W-1 the work is split: Task 2a does the cut-paste-and-rebind from manifest.yml to test.yml; Task 2b updates the callers (Taskfile.yml, docs/MANIFEST.md, .claude/CLAUDE.md). Each task is a separate commit so a mid-plan failure leaves the tree recoverable.

Purpose: Close the rename pass for the last three namespaces. After this plan, the only namespace edit pending is Plan 08 (which adds the new `lint:banner-parity` check and rewrites the `default:` task's banner).

Output: Four commits, each leaving green lint.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-task-surface-redesign/12-CONTEXT.md
@.planning/phases/12-task-surface-redesign/12-PATTERNS.md
@.planning/phases/12-task-surface-redesign/SURFACE.md
@taskfiles/manifest.yml
@taskfiles/test.yml
@taskfiles/lint.yml

<interfaces>
<!-- manifest task table: -->

| Task | Verdict | Disposition |
|---|---|---|
| `setup` | mark-internal | add `internal: true` after `desc:` |
| `resolve` | mark-internal | add `internal: true` after `desc:` |
| `show` | mark-internal | add `internal: true`; new public `show:manifest` delegate |
| `validate` | mark-internal | add `internal: true`; new public `audit:manifest` delegate |
| `test` | move to test:manifest | cut from manifest.yml; paste into test.yml as `manifest:` |
| `test:add-machine` | move to test:add-machine | cut from manifest.yml; paste into test.yml as `add-machine:` |

<!-- lint task table: -->

| Task | Verdict | Disposition |
|---|---|---|
| `default` | keep public | unchanged (top-level `lint` alias) |
| `syntax` | mark-internal | add `internal: true` after `desc:` |
| `taskfile` | mark-internal | add `internal: true` after `desc:` |
| `shell-headers` | mark-internal | add `internal: true` after `desc:` |
| `portability` | mark-internal | add `internal: true` after `desc:` |
| `test-fixtures` | mark-internal | add `internal: true` after `desc:` |

<!-- test task table: -->

| Task | Verdict | Disposition |
|---|---|---|
| `default` | mark-internal | add `internal: true` after `desc:` |
| `hooks` | mark-internal | add `internal: true` after `desc:` |
| `manifest` (NEW) | mark-internal | paste from taskfiles/manifest.yml |
| `add-machine` (NEW) | mark-internal | paste from taskfiles/manifest.yml |

<!-- Existing taskfiles/show.yml shape (extended): -->

tasks:
  # lint-allow: cmds-without-status
  claude:
    desc: "Show installed marketplaces + plugins (diagnostic)"
    status: [false]
    cmds:
      - task: :claude:status
  # NEW in this plan:
  # lint-allow: cmds-without-status
  manifest:
    desc: "Print resolved manifest (default: active machine; -- [--machine] NAME for any machine)"
    status: [false]
    cmds:
      - task: :manifest:show
        vars: { CLI_ARGS: '{{.CLI_ARGS}}' }

<!-- Existing taskfiles/audit.yml shape (extended): -->

tasks:
  # lint-allow: cmds-without-status
  links: { ... }
  # lint-allow: cmds-without-status
  packages: { ... }
  # NEW in this plan:
  # lint-allow: cmds-without-status
  manifest:
    desc: "Validate active (or -- [--machine] NAME) manifest schema"
    status: [false]
    cmds:
      - task: :manifest:validate
        vars: { CLI_ARGS: '{{.CLI_ARGS}}' }

<!-- The root `setup` alias in Taskfile.yml stays unchanged: -->
  setup:
    desc: "Persist active machine selection: task setup -- <machine-name>"
    status: [false]
    cmds:
      - task: manifest:setup        # works; internal does not affect invocability
        vars: { CLI_ARGS: '{{.CLI_ARGS}}' }

<!-- The root `validate:` aggregator iteration loop stays unchanged: -->
for component in manifest identity links macos packages claude shell; do
  task "${component}:validate" 2>&1 | tee "${cache_dir}/${component}"
done
<!-- Continues to work because internal does not affect invocability. -->

<!-- docs/MANIFEST.md task surface table BEFORE: -->
| Command | Description |
|---------|-------------|
| `task setup -- <name>` | Persist machine selection; runs validate and resolve |
| `task manifest:resolve` | (Re)compile `resolved.json` from defaults + active machine TOML |
| `task manifest:show [-- --machine <name>]` | Print resolved manifest (active machine by default) |
| `task manifest:validate [-- --machine <name>]` | Schema check -- required fields + unknown-key warnings |
| `task manifest:test` | Run the six golden-output fixture tests |

<!-- AFTER: -->
| Command | Description |
|---------|-------------|
| `task setup -- <name>` | Persist machine selection; runs validate and resolve |
| `task show:manifest [-- --machine <name>]` | Print resolved manifest (active machine by default) |
| `task audit:manifest [-- --machine <name>]` | Schema check -- required fields + unknown-key warnings |
| `task test:manifest` | Run the six golden-output fixture tests |

<!-- (manifest:resolve row dropped per planner's pick) -->

<!-- .claude/CLAUDE.md Quick Reference BEFORE (two bullets): -->
- Resolve manifest: `task manifest:resolve` (recomputes `$XDG_STATE_HOME/dotfiles/resolved.json`)
- Show manifest: `task manifest:show` (prints the post-merge structure for debugging)

<!-- AFTER: -->
- Show manifest: `task show:manifest` (prints the post-merge structure for debugging)

<!-- (manifest:resolve bullet dropped per planner's pick -- internal task, not operator-surfaced) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark manifest tasks internal + extend show.yml/audit.yml with manifest delegates</name>
  <read_first>
    - taskfiles/manifest.yml (full file -- tasks `setup:`, `resolve:`, `show:`, `validate:`, `test:`, `test:add-machine:`)
    - taskfiles/show.yml (Plan 06's NEW file; currently has only `claude:` task)
    - taskfiles/audit.yml (Plan 03 + 06's extended file; currently has `links:` and `packages:`)
    - Taskfile.yml `setup:` alias (delegates to manifest:setup -- unchanged)
    - .planning/phases/12-task-surface-redesign/SURFACE.md (manifest: section rows)
  </read_first>
  <action>
    Apply edits in ONE commit. Each edit uses content-based locators (per B-4: find by literal task-key match).

    **Edit `taskfiles/manifest.yml`** (mark four tasks internal; do NOT move test tasks yet -- Task 2a handles those):

    1. **`setup:`** Find the task key matching the literal `  setup:` (the task whose `desc:` references persisting machine selection). Add `    internal: true` immediately after `desc:`, before `requires:`.

    2. **`resolve:`** Find the task key matching the literal `  resolve:` (the task whose `desc:` references recomputing resolved.json). Add `    internal: true` immediately after `desc:`, before `preconditions:`.

    3. **`show:`** Find the task key matching the literal `  show:` (the task whose `desc:` references printing resolved manifest). Add `    internal: true` immediately after `desc:`, before the env block.

    4. **`validate:`** Find the task key matching the literal `  validate:` inside `taskfiles/manifest.yml` (the manifest schema check; `desc:` references schema check / required fields / unknown-key warnings). Add `    internal: true` immediately after `desc:`, before `env:`.

    **Edit `taskfiles/show.yml`** (extend with `manifest:` task):

    5. After the existing `claude:` task block, append a new `manifest:` task (same 4-space-indent shape under `tasks:`):
       - `# lint-allow: cmds-without-status` marker
       - `manifest:`
       - `desc: "Print resolved manifest (default: active machine; -- [--machine] NAME for any machine)"`
       - `status: [false]`
       - `cmds:` with one entry `- task: :manifest:show` and `vars: { CLI_ARGS: '{{.CLI_ARGS}}' }`.
    6. Update the file-header banner's Tasks list (from Plan 06) by appending a comment line: `#   - manifest -- delegate to :manifest:show (resolved manifest printout; D-02)`.

    **Edit `taskfiles/audit.yml`** (extend with `manifest:` task):

    7. After the existing `links:` and `packages:` task blocks, append a new `manifest:` task:
       - `# lint-allow: cmds-without-status` marker
       - `manifest:`
       - `desc: "Validate active (or -- [--machine] NAME) manifest schema"`
       - `status: [false]`
       - `cmds:` with one entry `- task: :manifest:validate` and `vars: { CLI_ARGS: '{{.CLI_ARGS}}' }`.
    8. Update the file-header banner's Tasks list by appending a comment line: `#   - manifest -- delegate to :manifest:validate (schema check; D-03)`.

    Commit message: `refactor(12): mark manifest tasks internal + add show:manifest / audit:manifest delegates (D-01, D-02, D-03)`.

    Verify: `task --list 2>&1 | grep -cE '^* manifest:'` returns 0. `task --list 2>&1 | grep -cE '^* show:manifest'` returns 1. `task --list 2>&1 | grep -cE '^* audit:manifest'` returns 1. `task setup -- <some-existing-machine>` resolves (the `task: manifest:setup` delegation reaches the now-internal task). `task --list-all` exits 0 (graph-parse gate). `task lint:taskfile` passes.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ "$(task --list 2>&1 | grep -cE '^\* manifest:')" -eq 0 ] && [ "$(task --list 2>&1 | grep -cE '^\* show:manifest')" -eq 1 ] && [ "$(task --list 2>&1 | grep -cE '^\* audit:manifest')" -eq 1 ] && [ "$(grep -cE '^\s+manifest:\s*$' taskfiles/show.yml taskfiles/audit.yml)" -eq 2 ] && task --list-all >/dev/null 2>&1; rc_graph=$?; task lint:taskfile 2>&1 | tail -3 | grep -qE '✓|LINT-0[1-7]' && [ "$rc_graph" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `task --list` shows ZERO `manifest:*` rows
    - `task --list` shows `show:manifest` and `audit:manifest` each exactly once
    - `task --list-all` still shows the 4 internal manifest tasks (setup, resolve, show, validate) + the not-yet-moved test/test:add-machine
    - `taskfiles/show.yml` and `taskfiles/audit.yml` each have a `manifest:` task entry; both file-header banners' Tasks lists include the new manifest entry
    - `task setup -- <machine>` succeeds (the cross-namespace delegation from root `setup` to `manifest:setup` still resolves)
    - `task --list-all` exits 0 (every renamed callsite resolves -- graph-parse gate)
    - `task lint:taskfile` exits 0
  </acceptance_criteria>
  <done>manifest setup/resolve/show/validate all internal; show:manifest and audit:manifest publicly surface the diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2a: Cut-paste manifest:test + manifest:test:add-machine into taskfiles/test.yml + rebind vars</name>
  <read_first>
    - taskfiles/manifest.yml (the `test:` task and the `test:add-machine:` task; ~180 LOC each)
    - taskfiles/test.yml (current file; contains `default` and `hooks` tasks; has its own `vars:` block referencing DOTFILEDIR)
    - .planning/phases/12-task-surface-redesign/12-PATTERNS.md Pattern 4 "Special case -- manifest:test:add-machine" (recommended path: define the task in taskfiles/test.yml; the move is a cut-paste with var rebinding)
    - .planning/phases/12-task-surface-redesign/12-CONTEXT.md Claude's-Discretion item 7 (move manifest:test* to test: namespace)
  </read_first>
  <action>
    Apply edits in ONE commit (W-1: cut-paste + var-rebinding only; callers and docs are updated in Task 2b). This isolates the structural move from the caller-update so a mid-plan failure leaves a recoverable tree.

    **Cut from `taskfiles/manifest.yml`:**

    1. Find the task key matching the literal `  test:` (the task whose `desc:` references golden-output fixture tests; the body is ~180 LOC including a section separator comment block). Delete the entire task block (header comment separator above, the task body, and the trailing blank line).
    2. Find the task key matching the literal `  test:add-machine:` (the task whose `desc:` references MFST-09 add-machine smoke). Delete the entire task block.
    3. Delete any now-orphaned header comment separator blocks (e.g., `# Tasks: Test`, `# Tasks: MFST-09 Smoke`) that were associated with the deleted tasks.

    **Paste into `taskfiles/test.yml`:**

    4. After the existing `hooks:` task block, append:
       a. A separator comment block: `# ---------------------------------------------------------------------------` then `# manifest -- deep-merge fixtures (positive + negative). Moved from taskfiles/manifest.yml in Phase 12 (Claude's-Discretion item 7 + D-04).` then `# ---------------------------------------------------------------------------`.
       b. The full task body cut from manifest.yml's `test:`, with the task key renamed from `test:` to `manifest:` and `    internal: true` added immediately after `desc:`. The cmds: body is unchanged.
       c. A second separator comment block for the add-machine task: `# ---------------------------------------------------------------------------` then `# add-machine -- MFST-09 smoke. Moved from taskfiles/manifest.yml in Phase 12.` then `# ---------------------------------------------------------------------------`.
       d. The full task body cut from manifest.yml's `test:add-machine:`, with the task key renamed from `test:add-machine:` to `add-machine:` and `    internal: true` added immediately after `desc:`.

    **Var rebinding (Option A from the recommended path):**

    5. The moved task bodies reference template vars `{{.MACHINES_DIR}}`, `{{.FIXTURES_DIR}}`, `{{.STATE_FILE}}`, `{{.RESOLVED_JSON_PATH}}`, `{{.DEFAULTS_TOML}}` -- these vars live ONLY in `taskfiles/manifest.yml`'s vars block (or are forwarded via Taskfile.yml's `manifest:` include vars). Extend `taskfiles/test.yml`'s top-level `vars:` block to define each:
       - `MACHINES_DIR: '{{.DOTFILEDIR}}/manifests/machines'`
       - `FIXTURES_DIR: '{{.DOTFILEDIR}}/manifests/test/fixtures'`
       - `STATE_FILE: '{{.XDG_STATE_HOME}}/dotfiles/machine'`
       - `RESOLVED_JSON_PATH: '{{.XDG_STATE_HOME}}/dotfiles/resolved.json'`
       - `DEFAULTS_TOML: '{{.DOTFILEDIR}}/manifests/defaults.toml'`
       (Exact derivations: match the existing manifest.yml vars block for these keys; copy values directly.)
    6. Also update the root `Taskfile.yml` `test:` include block (if present) to forward DOTFILEDIR + XDG_STATE_HOME if they are not already forwarded. Verify via `task test:manifest --status` exits 0 in a later step.

    Commit message: `refactor(12): cut-paste manifest:test* -> taskfiles/test.yml as test:manifest + test:add-machine + rebind vars (D-04)`.

    Verify after commit: `taskfiles/manifest.yml` no longer contains `test:` or `test:add-machine:` task keys. `taskfiles/test.yml` contains 4 tasks (default, hooks, manifest, add-machine). The moved task bodies reference the new vars via `{{.MACHINES_DIR}}` etc., and those vars are now defined in test.yml's vars block. `task lint:taskfile` passes (this confirms YAML validity and var-reference well-formedness).

    Note: callers (Taskfile.yml root `test:` task, docs, .claude/CLAUDE.md) are NOT yet updated -- Task 2b lands immediately to close the gap. Between Task 2a and Task 2b commits, `task test` (the top-level aggregator) may fail because its cmds: still references `- task: manifest:test` which no longer exists; operator does NOT invoke `task test` between commits in normal operation.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ "$(grep -cE '^\s+test:\s*$|^\s+test:add-machine:\s*$' taskfiles/manifest.yml)" -eq 0 ] && [ "$(grep -cE '^\s+manifest:\s*$|^\s+add-machine:\s*$' taskfiles/test.yml)" -eq 2 ] && grep -qE 'MACHINES_DIR' taskfiles/test.yml && grep -qE 'FIXTURES_DIR' taskfiles/test.yml && task lint:taskfile 2>&1 | tail -3 | grep -qE '✓|LINT-0[1-7]'</automated>
  </verify>
  <acceptance_criteria>
    - `taskfiles/manifest.yml` no longer contains `test:` or `test:add-machine:` task keys
    - `taskfiles/test.yml` contains 4 tasks: `default`, `hooks`, `manifest`, `add-machine`
    - `taskfiles/test.yml` `vars:` block contains MACHINES_DIR, FIXTURES_DIR, STATE_FILE, RESOLVED_JSON_PATH, DEFAULTS_TOML
    - Both moved tasks (`manifest`, `add-machine`) carry `internal: true` immediately after `desc:`
    - `task lint:taskfile` exits 0 (YAML validity + var-reference well-formedness confirmed)
  </acceptance_criteria>
  <done>Cut-paste + var-rebinding complete; Task 2b updates callers and docs.</done>
</task>

<task type="auto">
  <name>Task 2b: Update Taskfile.yml callers + docs/MANIFEST.md table + .claude/CLAUDE.md bullets</name>
  <read_first>
    - Taskfile.yml root `test:` task (cmds: block references `manifest:test` or `test:hooks`)
    - docs/MANIFEST.md (manifest task surface table near the bottom of the file)
    - .claude/CLAUDE.md (Quick Reference section listing operator-callable manifest tasks)
    - .planning/phases/12-task-surface-redesign/12-CONTEXT.md Claude's-Discretion items 4-5 (rewrite paths for .claude/CLAUDE.md + docs/MANIFEST.md)
  </read_first>
  <action>
    Apply edits in ONE commit. Each edit uses content-based locators (per B-4 and W-5: find by literal text match, not by absolute line number).

    **Edit `Taskfile.yml` root `test:` task:**

    1. Find the line matching the literal `      - task: manifest:test` (inside the root `test:` task's `cmds:` block) and replace with `      - task: test:manifest`.
    2. The line `      - task: test:hooks` stays unchanged.

    **Edit `docs/MANIFEST.md` manifest task surface table:**

    3. Find the row matching the literal `` `task manifest:resolve` `` (the entire markdown table row containing that command) and delete that row entirely.
    4. Find the row matching the literal `` `task manifest:show `` (the row whose first cell contains that command) and replace `task manifest:show` with `task show:manifest` (keep the rest of the row -- including the `[-- --machine <name>]` argument hint and the description).
    5. Find the row matching the literal `` `task manifest:validate `` and replace `task manifest:validate` with `task audit:manifest`.
    6. Find the row matching the literal `` `task manifest:test` `` and replace `task manifest:test` with `task test:manifest`.

    **Edit `.claude/CLAUDE.md` Quick Reference (per W-5 -- content-based locators, not line numbers):**

    7. Find the bullet starting with the literal `- Resolve manifest:` (the bullet referencing `task manifest:resolve`) and delete the entire bullet line.
    8. Find the bullet starting with the literal `- Show manifest:` (the bullet referencing `task manifest:show`) and rewrite the task reference inside the bullet to use the new task name `` `task show:manifest` `` (keep the surrounding bullet text describing what the task does).

    Commit message: `refactor(12): retarget callers + docs for test:manifest + show:manifest + audit:manifest (D-02, D-04)`.

    Verify after commit (B-8 per-file existence checks; no grep | awk pipelines):
    - `! grep -q "task manifest:resolve" .claude/CLAUDE.md` (the bullet was dropped).
    - `! grep -q "task manifest:show" .claude/CLAUDE.md` (rewritten to `task show:manifest`).
    - `! grep -q "task manifest:resolve" docs/MANIFEST.md` (the row was dropped).
    - `grep -q "task show:manifest" docs/MANIFEST.md`.
    - `grep -q "task audit:manifest" docs/MANIFEST.md`.
    - `grep -q "task test:manifest" docs/MANIFEST.md`.
    - `grep -q "task: test:manifest" Taskfile.yml` (the root test: caller was updated).
    - `task test` (top-level aggregator) succeeds end-to-end (runs both test:manifest and test:hooks).
    - `task --list-all` exits 0 (graph-parse gate).
    - `task lint:taskfile` passes.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && ! grep -q "task manifest:resolve" .claude/CLAUDE.md && ! grep -q "task manifest:show" .claude/CLAUDE.md && ! grep -q "task manifest:resolve" docs/MANIFEST.md && grep -q "task show:manifest" docs/MANIFEST.md && grep -q "task audit:manifest" docs/MANIFEST.md && grep -q "task test:manifest" docs/MANIFEST.md && grep -qE 'task: test:manifest' Taskfile.yml && task --list-all >/dev/null 2>&1; rc_graph=$?; task lint:taskfile 2>&1 | tail -3 | grep -qE '✓|LINT-0[1-7]' && [ "$rc_graph" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "task manifest:resolve" .claude/CLAUDE.md` returns 0 (bullet dropped)
    - `! grep -q "task manifest:show" .claude/CLAUDE.md` returns 0 (rewritten to show:manifest)
    - `! grep -q "task manifest:resolve" docs/MANIFEST.md` returns 0 (row dropped)
    - `grep -q "task show:manifest" docs/MANIFEST.md` returns 0
    - `grep -q "task audit:manifest" docs/MANIFEST.md` returns 0
    - `grep -q "task test:manifest" docs/MANIFEST.md` returns 0
    - `grep -qE 'task: test:manifest' Taskfile.yml` returns 0 (root test: caller updated)
    - `task --list-all` shows: `test:default`, `test:hooks`, `test:manifest`, `test:add-machine`
    - `task --list-all` exits 0 (every renamed callsite resolves -- graph-parse gate)
    - `task lint:taskfile` exits 0
  </acceptance_criteria>
  <done>Callers and docs updated; the move of manifest:test* to test: is complete and visible end-to-end.</done>
</task>

<task type="auto">
  <name>Task 3: Mark all lint sub-checks internal (keep lint:default public) + rewrite operator-facing header block</name>
  <read_first>
    - taskfiles/lint.yml (full file; tasks `default:`, `syntax:`, `taskfile:`, `shell-headers:`, `portability:`, `test-fixtures:`)
    - .planning/phases/12-task-surface-redesign/12-CONTEXT.md D-04 (lint + test aggregator-only; sub-checks internal)
    - .planning/phases/12-task-surface-redesign/SURFACE.md (lint: section rows)
  </read_first>
  <action>
    Apply edits in ONE commit. Each edit uses content-based locators (per B-4).

    **Edit `taskfiles/lint.yml`:**

    1. **`default:`** No change -- this is the aliased target of top-level `task lint`, stays public. Verify it does NOT have `internal: true` (drift check).
    2. **`syntax:`** Find the task key matching the literal `  syntax:` (the task with `desc:` referencing taskfile syntax / `task --list` parse check). Add `    internal: true` immediately after `desc:`, before `cmds:`. The `# lint-allow: cmds-without-status` marker on the preceding line stays.
    3. **`taskfile:`** Find the task key matching the literal `  taskfile:` (the task with `desc:` referencing LINT-01..LINT-07 enforcement). Add `    internal: true` immediately after `desc:`. The `# lint-allow:` marker on the preceding line stays.
    4. **`shell-headers:`** Find the task key matching the literal `  shell-headers:` (the task with `desc:` referencing shell header banner enforcement). Add `    internal: true` immediately after `desc:`. The `# lint-allow:` marker stays.
    5. **`portability:`** Find the task key matching the literal `  portability:` (the task with `desc:` referencing portability checks / `set -euo pipefail` / etc.). Add `    internal: true` immediately after `desc:`. The `# lint-allow:` marker stays.
    6. **`test-fixtures:`** Find the task key matching the literal `  test-fixtures:` (the task with `desc:` referencing lint-fixture pass/fail evaluation). Add `    internal: true` immediately after `desc:`. The `# lint-allow:` marker stays.

    **B-3 header-block rewrite (operator-facing section vs LINT-NN rule catalog):**

    The `taskfiles/lint.yml` file-header banner block contains two distinct sections:
    - The **LINT-NN rule catalog comment block** -- enumerates LINT-01..LINT-07 (LINT-08 deprecated) with one-line descriptions. This block lives at approximately `taskfiles/lint.yml:15-32` (the rule-catalog comments above the `vars:` / `tasks:` blocks). **Preserve this block verbatim.** Do NOT rewrite it.
    - The **operator-facing public-vs-internal summary block** -- the section that lists callable task names. This block lives at approximately `taskfiles/lint.yml:25-32` (the lines that list `task lint:*` invocations as user-callable surface). **Rewrite this block** to reflect the new visibility split.

    7. Locate the LINT-NN rule catalog comment block (the section comment that begins with the rule-catalog header and enumerates LINT-01..LINT-07 with descriptions). Identify the line range it occupies. Preserve every line in that range verbatim -- this is the canonical rule reference for the lint suite and must not change.

    8. Locate the operator-facing public-vs-internal summary block (the section comment that previously listed all six task names as `task lint:*` invocations). Rewrite it to:
       ```
       # Public surface:
       #   task lint                     -- run all checks (default aggregator)
       # Internal sub-checks (invoked by lint:default; hidden from `task --list`):
       #   task lint:syntax              -- LINT-syntax checks
       #   task lint:taskfile            -- LINT-01..LINT-07 enforcement
       #   task lint:shell-headers       -- shell-header banner enforcement
       #   task lint:portability         -- portability + set -euo pipefail
       #   task lint:test-fixtures       -- fixture-pair pass/fail evaluation
       ```

    Commit message: `refactor(12): mark lint sub-checks internal + rewrite operator-facing header block (D-04, B-3)`.

    Verify after commit: `task --list 2>&1 | grep -cE '^* lint:(syntax|taskfile|shell-headers|portability|test-fixtures)'` returns 0. `task --list-all 2>&1 | grep -cE 'lint:(syntax|taskfile|shell-headers|portability|test-fixtures)'` returns 5. `task lint` still runs all sub-checks (the aggregator's cmds: invokes them by short name). The LINT-NN rule catalog comment block is preserved verbatim (verify by string-matching the original LINT-01..LINT-07 descriptions). `task lint` exits 0.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ "$(task --list 2>&1 | grep -cE '^\* lint:(syntax|taskfile|shell-headers|portability|test-fixtures)')" -eq 0 ] && [ "$(task --list-all 2>&1 | grep -cE 'lint:(syntax|taskfile|shell-headers|portability|test-fixtures)')" -ge 5 ] && grep -q 'LINT-01' taskfiles/lint.yml && grep -q 'LINT-07' taskfiles/lint.yml && grep -q 'Public surface:' taskfiles/lint.yml && task lint 2>&1 | tail -3 | grep -qE '✓'</automated>
  </verify>
  <acceptance_criteria>
    - `task --list` shows ZERO of: `lint:syntax`, `lint:taskfile`, `lint:shell-headers`, `lint:portability`, `lint:test-fixtures`
    - `task --list` shows `lint:default` (or its alias `lint`) once -- the public aggregator
    - `task --list-all` shows all 5 internal sub-checks
    - `task lint` runs all sub-checks (aggregator invocation works post-internal-mark)
    - `taskfiles/lint.yml` preserves the LINT-01..LINT-07 rule catalog comment block verbatim (per B-3 acceptance criterion)
    - `taskfiles/lint.yml` operator-facing public-vs-internal summary block is rewritten to reflect the new visibility split
    - `task lint` exits 0 (final acceptance for this plan -- per W-6 full lint runs at the last task)
  </acceptance_criteria>
  <done>lint sub-checks internalized; LINT-NN rule catalog preserved; operator-facing summary block rewritten to reflect visibility split.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure rename + visibility-marker + task-move refactor. No new code paths. The cut-paste of `manifest:test*` into `taskfiles/test.yml` preserves the existing logic; only the task name + namespace changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07-01 | Information disclosure | `internal: true` on 10+ tasks (manifest x4, test x4, lint x5) | accept | Markers hide tasks from `task --list` but remain invocable. Not authz. |
| T-12-07-02 | Tampering | Cross-file task move (manifest:test -> test:manifest with var rebinding) | mitigate | Task 2a explicitly lists required vars (MACHINES_DIR, FIXTURES_DIR, etc.) and rebinds them in test.yml's vars block. Task 2b updates callers. Each task is a separate commit so a mid-plan failure leaves a recoverable tree. Verify via `task test:manifest --status` running cleanly post-move. |
| T-12-07-03 | Repudiation | Doc updates removing `task manifest:resolve` reference | accept | The doc rewrite reflects the operator-facing surface change; the internal task is still invocable (lint catches any caller). |
</threat_model>

<verification>
- `task --list` shows zero `manifest:*`, `test:*`, `lint:*` rows (except `lint:default` aliased as `lint`)
- `task --list-all` shows the internal-but-invocable set across all three namespaces
- `task --list-all` exits 0 (graph-parse gate per W-7), `task setup -- <machine>`, `task validate`, `task test`, `task lint` all succeed end-to-end
- `docs/MANIFEST.md` task table reflects the new namespace mapping (per-file existence checks in Task 2b verify block, per B-8)
- `.claude/CLAUDE.md` quick-reference reflects the new surface (per-file existence checks, per B-8)
- `taskfiles/lint.yml` preserves LINT-01..LINT-07 rule catalog comment block (per B-3)
- `task lint` green at end of plan (per W-6 -- intermediate tasks use `task lint:taskfile`)
</verification>

<success_criteria>
- SURF-02 satisfied for the manifest, test, lint namespaces: renames + namespace-moves + doc updates applied.
- SURF-03 satisfied for the 13+ tasks marked internal.
- The dual-shape pattern (D-03) is realized: `manifest:validate` continues as the internal aggregator-callee; `audit:manifest` is the public delegate.
- B-3 honored: LINT-NN rule catalog preserved verbatim; operator-facing block rewritten.
- W-1 honored: Task 2a (cut-paste + var-rebind) and Task 2b (caller + doc updates) committed separately for recoverability.
- B-8 honored: verify blocks use per-file existence checks, not fragile grep | awk pipelines.
- Plan 08 (banner + lint check) inherits a clean state with all rename work complete.
</success_criteria>

<output>
Create `.planning/phases/12-task-surface-redesign/12-07-SUMMARY.md` recording:
- Four commit SHAs (Task 1 manifest internalize + delegates; Task 2a cut-paste + rebind; Task 2b callers + docs; Task 3 lint internalize + header rewrite)
- Final `task --list` capture
- Final `task --list-all` capture
- Confirmation that `task --list-all` (graph-parse gate per W-7), `task setup -- <machine>`, `task validate`, `task test`, `task lint` each succeed
- Confirmation that the LINT-NN rule catalog is preserved verbatim (per B-3)
- Handoff to Plan 08 (banner + lint check): Plan 08 touches `Taskfile.yml` `default:` body and `taskfiles/lint.yml` (adds the new banner-parity check); both are isolated from Plan 07's edits
</output>
</content>
</invoke>
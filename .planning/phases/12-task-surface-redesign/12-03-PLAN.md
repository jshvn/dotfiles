---
phase: 12-task-surface-redesign
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - taskfiles/links.yml
  - taskfiles/audit.yml
  - Taskfile.yml
autonomous: true
requirements: [SURF-02, SURF-03]

must_haves:
  truths:
    - "`task --list` shows `audit:links` (public diagnostic) but none of the prior `links:*` rows"
    - "`task install` succeeds end-to-end after the rename (`Taskfile.yml`'s `install:` body invokes the renamed callees)"
    - "`task lint` passes after every commit (callers-first ordering: caller updated before or in the same commit as the callee)"
    - "Internal sub-tasks (`links:install-zsh`, `links:install-claude`, `links:install-configs`, `links:install`, `links:validate`, `links:reconcile`) are invocable but hidden from `task --list`"
  artifacts:
    - path: "taskfiles/links.yml"
      provides: "Renamed tasks with `internal: true` markers; `links:reconcile` stays internal as the implementation"
      exports: ["install (internal)", "install-zsh (internal)", "install-claude (internal)", "install-configs (internal)", "validate (internal)", "reconcile (internal implementation)"]
    - path: "taskfiles/audit.yml"
      provides: "NEW file -- public delegate for `audit:links` (calls `:links:reconcile`)"
      exports: ["links (public delegate)"]
    - path: "Taskfile.yml"
      provides: "Updated `install:` body referencing the new task names + new `audit:` include"
      contains: "task: links:install; task: audit:links; audit: ./taskfiles/audit.yml"
  key_links:
    - from: "Taskfile.yml install: body (line `- task: links:all`)"
      to: "taskfiles/links.yml install task"
      via: "task: links:install (was: links:all)"
      pattern: "task: links:install"
    - from: "Taskfile.yml install: body (line `- task: links:reconcile`)"
      to: "taskfiles/audit.yml `links:` (public delegate)"
      via: "task: audit:links with CLI_ARGS: --warn-only"
      pattern: "task: audit:links"
---

<objective>
Apply the links-namespace rename pass per D-09 + D-10 + D-02:
- `links:all` -> `links:install` (aggregator) + `internal: true` (D-09 + D-01)
- `links:zsh` -> `links:install-zsh` + `internal: true` (D-10 + D-01)
- `links:claude` -> `links:install-claude` + `internal: true` (D-10 + D-01)
- `links:configs` -> `links:install-configs` + `internal: true` (D-10 + D-01)
- `links:validate` -> (keep name) + `internal: true` (D-01)
- `links:reconcile` -> verdict `rename + mark-internal` per B-2: the original task stays in `taskfiles/links.yml` as `reconcile:` and is marked `internal: true`; the PUBLIC name `audit:links` is exposed via a thin delegate task in a new `taskfiles/audit.yml` that calls `task: :links:reconcile`.
- `Taskfile.yml`'s `install:` body (the line `- task: links:all` and the line `- task: links:reconcile`) updated to call the new names in the SAME commit as the callee rename (Phase 11 D-04 callers-first discipline).

Purpose: The links namespace is the first major rename pass touching `Taskfile.yml`'s install pipeline. It establishes the callers-first commit shape every subsequent namespace plan inherits.

Output: Two commits, each leaving a green lint tree, that converge on the SURFACE.md verdicts for the `links:` rows.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-task-surface-redesign/12-CONTEXT.md
@.planning/phases/12-task-surface-redesign/12-PATTERNS.md
@.planning/phases/12-task-surface-redesign/SURFACE.md

<interfaces>
<!-- Rename table for this plan (from 12-PATTERNS.md Edit-targets table): -->

| Before | After | Touch |
|---|---|---|
| `links:all` (line 120) | `links:install` | key + `internal: true` after desc |
| `links:zsh` (line 137) | `links:install-zsh` | key + `internal: true` after desc |
| `links:claude` (line 205) | `links:install-claude` | key + `internal: true` after desc |
| `links:configs` (line 256) | `links:install-configs` | key + `internal: true` after desc |
| `links:validate` (line 311) | (keep name) | add `internal: true` after desc |
| `links:reconcile` (line 434) | (keep name; INTERNAL) | add `internal: true` after desc; public path via `audit:links` delegate in new `taskfiles/audit.yml` (B-2 final mechanic) |

<!-- Inside taskfiles/links.yml, sibling task: refs that must also update inside the aggregator's cmds: block. -->
<!-- BEFORE: -->
  cmds:
    - task: zsh
    - task: :identity:install
    - task: claude
    - task: configs

<!-- AFTER: -->
  cmds:
    - task: install-zsh
    - task: :identity:install      # absolute-form; unchanged
    - task: install-claude
    - task: install-configs

<!-- New `taskfiles/audit.yml` shape (B-2 final mechanic; W-3 mandatory banner): -->

version: '3'

# =============================================================================
# taskfiles/audit.yml -- Phase 12 public diagnostics: drift detection.
#
# Purpose: Public delegates for diagnostic audit operations across all namespaces.
# Callers: Invoked directly by operator; included by Taskfile.yml.
# Side effects: Read-only -- these tasks print state or detect drift; no mutations.
# Tasks:
#   - links -- delegate to :links:reconcile (orphan symlink detection; D-02)
# =============================================================================

vars:
  DOTFILEDIR:
    sh: dirname "{{.TASKFILE_DIR}}"

tasks:
  # lint-allow: cmds-without-status
  links:
    desc: "Audit symlink drift (detect orphans); -- --remove for interactive cleanup"
    status: [false]
    cmds:
      - task: :links:reconcile
        vars: { CLI_ARGS: '{{.CLI_ARGS}}' }

<!-- Taskfile.yml install: body BEFORE (lines 234-255): -->
  install:
    desc: "Install dotfiles for active machine (canonical entry)"
    ...
    cmds:
      - task: links:all              # <-- renames to `links:install`
      - task: packages:install
      - task: claude:install
      - task: macos:defaults
      - task: macos:shell
      - task: packages:verify
      - task: links:reconcile        # <-- renames to `audit:links`
        vars: { CLI_ARGS: "--warn-only" }

<!-- Taskfile.yml install: body AFTER this plan: -->
      - task: links:install           # was: links:all
      - task: packages:install        # unchanged in this plan
      - task: claude:install          # unchanged in this plan
      - task: macos:defaults          # unchanged in this plan (touched by Plan 05)
      - task: macos:shell             # unchanged in this plan (touched by Plan 05)
      - task: packages:verify         # unchanged in this plan
      - task: audit:links             # was: links:reconcile
        vars: { CLI_ARGS: "--warn-only" }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename aggregator + 3 sub-targets + mark internal (single atomic commit)</name>
  <read_first>
    - taskfiles/links.yml (full file; aggregator `all:`; sub-targets `zsh:`, `claude:`, `configs:`; `validate:`; `reconcile:`; `zdotdir:` already internal -- no touch)
    - Taskfile.yml install: body (the `- task: links:all` line and the `- task: links:reconcile` line)
    - .planning/phases/12-task-surface-redesign/12-PATTERNS.md Pattern 2 (`internal: true` placement: line immediately after `desc:`) + Pattern 3 (aggregator fingerprint) + Pattern 4 (sub-target with `status:` block)
    - .planning/phases/12-task-surface-redesign/SURFACE.md (links: section rows for verdicts/new-names)
  </read_first>
  <action>
    Apply the following edits in ONE commit (Phase 11 D-04 callers-first discipline -- the Taskfile.yml call site is updated in the same commit as the rename so the lint stays green):

    **Edit `taskfiles/links.yml`:**

    1. **Aggregator (D-09):** Find the task key matching the literal `  all:` (the aggregator with `desc:` referencing the canonical install entry) and rename it to `  install:`. Add `    internal: true` immediately after the `desc:` line so the new line ordering is: `desc:` then `internal: true` then `cmds:`. Update the header comment block referencing `Aggregator -- P3 + P4 wired` to: `# Aggregator (D-09 internal: <ns>:install pattern; D-01 per-component install internal).`

    2. **Sub-target install-zsh (D-10):** Find the task key matching the literal `  zsh:` and rename to `  install-zsh:`. Add `    internal: true` immediately after the `desc:` line. The header comment block mentioning `Shell startup files (SHEL-03)` stays unchanged.

    3. **Sub-target install-claude (D-10):** Find the task key matching the literal `  claude:` and rename to `  install-claude:`. Add `    internal: true` immediately after the `desc:` line. The header comment block mentioning `Claude config tree (CLDE-01)` stays unchanged.

    4. **Sub-target install-configs (D-10):** Find the task key matching the literal `  configs:` and rename to `  install-configs:`. Add `    internal: true` immediately after the `desc:` line. The header comment block stays unchanged.

       Note: the `configs:ghostty` task already has `internal: true` -- no change needed. Its task name contains a colon so it lives as `links:configs:ghostty` in the namespace. Keep it as-is; sibling refs from `install-configs` to `configs:ghostty` continue to work because sibling resolution uses the short name within the same taskfile.

    5. **Mark `validate:` internal (D-01):** Locate the task key matching the literal `  validate:` and add `    internal: true` immediately after `desc:`, before `deps:`. Indentation: 4 spaces. The `# lint-allow: cmds-without-status` marker on the line above the task key remains correct (already there per LINT-03a).

    6. **Update aggregator cmds: block to call new sub-target names:**
       - Find the line matching `    - task: zsh` (inside the install aggregator's cmds: block) and replace with `    - task: install-zsh`.
       - Find the line matching `    - task: :identity:install` and leave UNCHANGED (absolute-form leading colon; cross-namespace call).
       - Find the line matching `    - task: claude` and replace with `    - task: install-claude`.
       - Find the line matching `    - task: configs` and replace with `    - task: install-configs`.

    **Edit `Taskfile.yml`:**

    7. Find the line matching the literal `      - task: links:all` (inside the `install:` body) and replace with `      - task: links:install`.

    Do NOT touch the `links:reconcile` -> `audit:links` rename in this commit -- Task 2 handles that.

    Commit message: `refactor(12): rename links:all -> install + sub-targets to install-<target> + mark internal (D-01, D-09, D-10)`.

    Verify after commit: `task --list 2>&1` shows ZERO rows starting with `links:` except `links:reconcile` (not yet renamed). `task --list-all 2>&1` shows the renamed internals. `task lint:taskfile` passes.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ "$(task --list 2>&1 | grep -cE '^\* links:(all|zsh|claude|configs|validate)')" -eq 0 ] && [ "$(task --list-all 2>&1 | grep -cE 'links:(install|install-zsh|install-claude|install-configs|validate)')" -ge 5 ] && grep -qE 'task: links:install$' Taskfile.yml && task lint:taskfile 2>&1 | tail -5 | grep -qE '✓|LINT-0[1-7]'</automated>
  </verify>
  <acceptance_criteria>
    - `task --list` shows ZERO of: `links:all`, `links:zsh`, `links:claude`, `links:configs`, `links:validate` (all are internal post-rename or renamed away)
    - `task --list-all` shows the five renamed/internal tasks: `links:install`, `links:install-zsh`, `links:install-claude`, `links:install-configs`, `links:validate`
    - `Taskfile.yml` contains the line `- task: links:install` inside the install body (the renamed callee)
    - `task lint:taskfile` exits 0 (LINT-01..LINT-07 green; LINT-03a aggregator exemption still applies via the `# lint-allow: cmds-without-status` marker on `links:install`)
    - `taskfiles/links.yml`'s aggregator `cmds:` block uses sibling short names (`install-zsh`, `install-claude`, `install-configs`) -- NOT prefixed (siblings resolve unqualified inside the same taskfile)
  </acceptance_criteria>
  <done>links: namespace renamed; install pipeline still works; `task lint:taskfile` green; only `audit:links` rename remains for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Move `links:reconcile` -> `audit:links` (public delegate in new taskfiles/audit.yml)</name>
  <read_first>
    - taskfiles/links.yml (the `reconcile:` task definition -- ~170 lines of cmd body)
    - Taskfile.yml install: body (the `- task: links:reconcile` line with `vars: { CLI_ARGS: "--warn-only" }`)
    - .planning/phases/12-task-surface-redesign/12-CONTEXT.md D-02 (drift-checkers move to `audit:` namespace -- `audit:links` is the new public name)
    - .planning/phases/12-task-surface-redesign/12-PATTERNS.md "No Analog Found" note (`audit:` is a NEW logical namespace) + Pattern 5 (file-header banner convention)
  </read_first>
  <action>
    Apply the B-2 final mechanic in ONE commit. The single agreed-upon path: keep the implementation in `taskfiles/links.yml` as `reconcile:` marked `internal: true`; create a new `taskfiles/audit.yml` containing a public `links:` delegate that calls `task: :links:reconcile`; add an `audit: ./taskfiles/audit.yml` line to `Taskfile.yml`'s includes block.

    **Step a -- Edit `taskfiles/links.yml`:**

    Locate the task key matching the literal `  reconcile:` (the orphan-symlink-detection task whose `desc:` references SHEL-09 / drift). Add `    internal: true` immediately after the `desc:` line, before `deps:`. Update the header comment block above the task to append a note: `# Public path: audit:links (via taskfiles/audit.yml delegate). This task remains as the implementation; the audit: delegate is the operator-facing surface (D-02).`

    **Step b -- Create `taskfiles/audit.yml`:**

    Write the new file per the `<interfaces>` shape above. Mandatory header-banner sections (W-3):
    - Purpose: "Public delegates for diagnostic audit operations across all namespaces."
    - Callers: "Invoked directly by operator; included by Taskfile.yml."
    - Side effects: "Read-only -- these tasks print state or detect drift; no mutations."
    - Tasks list: one comment line per public task defined in the file (initially just `- links -- delegate to :links:reconcile (orphan symlink detection; D-02)`).

    The file contains: `version: '3'`, the header banner block, a self-contained `vars:` block with `DOTFILEDIR` resolved via `sh: dirname "{{.TASKFILE_DIR}}"`, and a `tasks:` block with one task `links:` whose body is: `# lint-allow: cmds-without-status` marker; `desc:` quoted; `status: [false]`; `cmds:` containing `- task: :links:reconcile` with `vars: { CLI_ARGS: '{{.CLI_ARGS}}' }`.

    **Step c -- Edit `Taskfile.yml`:**

    Add `  audit:    ./taskfiles/audit.yml` to the `includes:` block in alphabetical position (between any preceding entries and `claude:`). Update the `Includes:` header comment block at the top of the file by adding a line `#   - audit    (P12, real)` in alphabetical order within that comment block.

    **Step d -- Edit `Taskfile.yml` install: body:**

    Find the line matching the literal `      - task: links:reconcile` (inside the install body) and replace with `      - task: audit:links`. The following line `        vars: { CLI_ARGS: "--warn-only" }` stays unchanged (the audit:links delegate forwards CLI_ARGS to `:links:reconcile`).

    Commit message: `refactor(12): rename links:reconcile -> audit:links via taskfiles/audit.yml delegate (D-02)`.

    Verify after commit: `task --list 2>&1 | grep -E '^* (audit|links):'` shows `audit:links` but not `links:reconcile`. `task --list-all 2>&1` shows both (the delegate AND the internal implementation). `task --list-all` exits 0 (graph-parse gate: go-task compiles the full task graph; any unresolved `task:` reference fails compilation and exits non-zero). `task lint` exits 0.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ "$(task --list 2>&1 | grep -cE '^\* audit:links')" -eq 1 ] && [ "$(task --list 2>&1 | grep -cE '^\* links:reconcile')" -eq 0 ] && [ "$(task --list-all 2>&1 | grep -cE 'links:reconcile')" -ge 1 ] && test -f taskfiles/audit.yml && grep -qE '^\s*audit:\s+\./taskfiles/audit\.yml' Taskfile.yml && grep -qE 'task: audit:links' Taskfile.yml && task --list-all >/dev/null 2>&1; rc_graph=$?; task lint 2>&1 | tail -3 | grep -qE '✓' && [ "$rc_graph" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `task --list` shows `audit:links` exactly once and does NOT show `links:reconcile`
    - `task --list-all` shows BOTH `audit:links` (the public delegate) and `links:reconcile` (the internal implementation)
    - File `taskfiles/audit.yml` exists with mandatory W-3 header-banner sections (Purpose / Callers / Side effects / Tasks list) and a single task `links:` whose cmds: invokes `:links:reconcile`
    - `Taskfile.yml`'s includes block contains the line `audit:    ./taskfiles/audit.yml` in alphabetical order
    - `Taskfile.yml` install body contains the line `- task: audit:links` (the original `vars: { CLI_ARGS: "--warn-only" }` line follows unchanged) and contains ZERO occurrences of `- task: links:reconcile`
    - `task --list-all` exits 0 (every renamed callsite resolves -- graph-parse gate confirms no unresolved `task:` references in the call graph)
    - `task lint` exits 0 at end of plan (LINT-01..LINT-07 green; LINT-03a satisfied via the all-task-delegations exemption + the explicit `# lint-allow: cmds-without-status` marker on the delegate)
  </acceptance_criteria>
  <done>`audit:links` is the public surface for orphan detection; `links:reconcile` is the internal implementation. Plan 03 closes the links namespace work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure rename + visibility-marker refactor. The `audit:` namespace introduces a new public delegate task but no new code paths -- the underlying implementation in `taskfiles/links.yml` is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Information disclosure | `internal: true` on `links:reconcile` and 4 install/validate tasks | accept | Markers hide tasks from `task --list` but remain invocable. NOT a defense-in-depth boundary; documented in SUMMARY. |
| T-12-03-02 | Tampering | Cross-namespace `task: :links:reconcile` call from `audit:links` delegate | accept | Absolute-form leading-colon is the standard go-task cross-namespace call (verified in `taskfiles/links.yml` aggregator's `:identity:install` ref and `taskfiles/identity.yml`'s symlink helper refs). No new attack surface. |
</threat_model>

<verification>
- `task --list` reflects the new surface: `audit:links` public; `links:*` rows absent (all internal)
- `task --list-all` shows the internal-but-invocable set
- `task --list-all` exits 0 (graph-parse gate; confirms install pipeline calls `task: links:install` and `task: audit:links` and all renames resolve in the compiled task graph)
- `task lint` green at end of plan (intermediate tasks use targeted `task lint:taskfile` per W-6)
- `taskfiles/audit.yml` exists with mandatory W-3 banner; `Taskfile.yml` includes block contains the `audit:` line in alphabetical order
</verification>

<success_criteria>
- SURF-02 satisfied for the links namespace: renames applied across `taskfiles/links.yml` and `Taskfile.yml`.
- SURF-03 satisfied for the now-internal `links:install`, `links:install-zsh`, `links:install-claude`, `links:install-configs`, `links:validate`, `links:reconcile`.
- D-02 honored: `audit:` is the public diagnostics-for-drift namespace; `audit:links` is its first member.
- B-2 final mechanic committed: single agreed-upon path (mark `links:reconcile` internal + new `taskfiles/audit.yml` delegate); no competing options.
- Plans 04-07 inherit a clean callers-first commit pattern from this plan.
</success_criteria>

<output>
Create `.planning/phases/12-task-surface-redesign/12-03-SUMMARY.md` recording:
- Two commit SHAs (rename pass; audit:links delegate)
- Final `task --list` capture showing the `audit:links` row and zero `links:*` rows
- Final `task --list-all` capture confirming internal tasks remain invocable
- The new `taskfiles/audit.yml` file path + its task body
- Handoff to Plan 04 (identity namespace): no shared file conflicts; Plan 04 modifies `taskfiles/identity.yml`
</output>
</content>
</invoke>
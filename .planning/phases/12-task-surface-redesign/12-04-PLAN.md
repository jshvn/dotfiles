---
phase: 12-task-surface-redesign
plan: 04
type: execute
wave: 4
depends_on: [12-01, 12-03]
files_modified:
  - taskfiles/identity.yml
autonomous: true
requirements: [SURF-02, SURF-03]
# W-2 note: Plan 04 is sequenced after Plan 03 for review clarity; Taskfile.yml is touched by
# 03/05/06/07/08 so no safe parallelization across those plans. Plan 04 itself does NOT touch
# Taskfile.yml (only `taskfiles/identity.yml`) but the sequential discipline is intentional.

must_haves:
  truths:
    - "`task --list` shows ZERO `identity:*` rows (all per-component install + validate tasks are internal per D-01)"
    - "`task --list-all` shows the renamed sub-targets `identity:install-git`, `identity:install-ssh`, `identity:install-one-password-agent`, plus the aggregator `identity:install` and `identity:validate`"
    - "`task install` succeeds end-to-end (the cross-namespace caller `task: :identity:install` from `taskfiles/links.yml` continues to work unchanged because the aggregator's name is unchanged)"
    - "`task lint` passes after every commit (LINT-01..LINT-07)"
  artifacts:
    - path: "taskfiles/identity.yml"
      provides: "Renamed sub-targets with verb-first `install-` prefix; all five tasks marked internal"
      exports: ["install (internal)", "install-git (internal)", "install-ssh (internal)", "install-one-password-agent (internal)", "validate (internal)"]
  key_links:
    - from: "taskfiles/links.yml install aggregator"
      to: "taskfiles/identity.yml install: task"
      via: "task: :identity:install"
      pattern: "task: :identity:install"
---

<objective>
Apply the identity-namespace rename pass per D-09 + D-10 + D-01:
- `identity:install` -> (keep name) + `internal: true` (D-01; aggregator stays the canonical entry; the only caller is `taskfiles/links.yml` via the absolute-form `task: :identity:install` which works regardless of internal status)
- `identity:git` -> `identity:install-git` + `internal: true` (D-10 verb-first)
- `identity:ssh` -> `identity:install-ssh` + `internal: true` (D-10)
- `identity:one-password-agent` -> `identity:install-one-password-agent` + `internal: true` (D-10 + D-11 length tolerated)
- `identity:validate` -> (keep name) + `internal: true` (D-01)

Per B-6 the work is SPLIT INTO TWO TASKS so a mid-plan failure leaves `taskfiles/identity.yml` in a recoverable state. Task 1a renames the aggregator + updates the aggregator's `cmds:` sibling refs to the post-rename names (callers-first per D-04). Task 1b renames the three sub-targets to match the cmds: references created in Task 1a, plus marks the validate aggregator + sub-tasks internal. Each task is a separate commit.

The internal `server-include` task is already `internal: true` and is called from `identity:git` via a short-name sibling ref -- no rename for that one, but the sibling ref name stays `server-include`.

Purpose: Identity layer rename. No external callers (the only cross-file call is from `taskfiles/links.yml` to the aggregator, which keeps its name). Plan 04 is self-contained to `taskfiles/identity.yml`.

Output: Two commits, each leaving the lint tree green.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-task-surface-redesign/12-CONTEXT.md
@.planning/phases/12-task-surface-redesign/12-PATTERNS.md
@.planning/phases/12-task-surface-redesign/SURFACE.md

<interfaces>
<!-- Rename table (from 12-PATTERNS.md): -->

| Before | After | Touch |
|---|---|---|
| `identity:install` | (keep name) | add `internal: true` after `desc:`; update cmds: sibling refs (Task 1a) |
| `identity:git` | `identity:install-git` | key + `internal: true` after `desc:` (Task 1b) |
| `identity:server-include` | (keep name) | already `internal: true`; sibling refs unchanged |
| `identity:ssh` | `identity:install-ssh` | key + `internal: true` after `desc:` (Task 1b) |
| `identity:one-password-agent` | `identity:install-one-password-agent` | key + `internal: true` after `desc:` (Task 1b) |
| `identity:validate` | (keep name) | add `internal: true` after `desc:` (Task 1b) |
| `identity:validate:symlinks`, `validate:one-password-agent`, `validate:git`, `validate:ssh-add`, `validate:keys` | (keep names) | already `internal: true` |

<!-- Aggregator cmds: block BEFORE: -->
  cmds:
    - task: git
    - task: ssh
    - task: one-password-agent

<!-- Aggregator cmds: block AFTER Task 1a (calls the post-rename sibling names; Task 1b actually performs the rename, but Task 1a updates the cmds: first per D-04 callers-first): -->
  cmds:
    - task: install-git
    - task: install-ssh
    - task: install-one-password-agent

<!-- `identity:git`'s cmds: block contains `- task: server-include` -- stays unchanged because server-include is NOT renamed. -->

<!-- `identity:validate` aggregator calls validate sub-tasks via sibling short names: -->
  cmds:
    - task: validate:symlinks
    - task: validate:git
    - task: validate:ssh-add
    - task: validate:keys
<!-- These sub-tasks already have `internal: true` and are NOT renamed. The sibling refs stay unchanged. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Rename aggregator + update aggregator cmds: sibling refs (callers-first)</name>
  <read_first>
    - taskfiles/identity.yml (full file -- focus on the `install:` aggregator and its `cmds:` block)
    - .planning/phases/12-task-surface-redesign/12-PATTERNS.md Pattern 2 (`internal: true` placement) + Pattern 3 (aggregator fingerprint)
    - .planning/phases/12-task-surface-redesign/12-CONTEXT.md D-04 (callers-first commit ordering)
  </read_first>
  <action>
    Apply edits in ONE commit (callers-first per D-04: update the aggregator's `cmds:` block to reference the post-rename sub-target names BEFORE Task 1b actually renames those sub-targets. Between Task 1a and Task 1b commits, the aggregator references sibling task names that do not yet exist -- but the aggregator is NEVER invoked between the two commits in normal operation; the only invocation is via `task install` which the operator does not run between commits).

    The cross-file caller `taskfiles/links.yml`'s `:identity:install` ref keeps working in both commits because the aggregator name does NOT change. Sibling references inside the aggregator are updated atomically with the rename.

    **Edit `taskfiles/identity.yml`:**

    1. **Aggregator `install:`:** Find the task key matching the literal `  install:` (the aggregator whose `desc:` references the identity install entry). Add `    internal: true` immediately after `desc:`, before `deps:`. The `# lint-allow: cmds-without-status` marker on the line above the task key stays.

    2. **Update aggregator cmds: block to call the post-rename sibling names** (D-04 callers-first):
       - Find the line matching the literal `    - task: git` inside the `install:` aggregator's `cmds:` block and replace with `    - task: install-git`.
       - Find the line matching the literal `    - task: ssh` inside the same cmds: block and replace with `    - task: install-ssh`.
       - Find the line matching the literal `    - task: one-password-agent` inside the same cmds: block and replace with `    - task: install-one-password-agent`.

    Do NOT rename the sub-target task keys themselves yet -- Task 1b does that. After Task 1a's commit, `task --list-all` will still show `identity:git`, `identity:ssh`, `identity:one-password-agent` (the old names); the aggregator's cmds: now points to names that resolve correctly only after Task 1b.

    Commit message: `refactor(12): mark identity:install internal + retarget aggregator cmds: to install-<target> names (D-01, D-04, D-10)`.

    Verify after commit: `task --list-all 2>&1 | grep -cE 'identity:install$'` returns 1 (aggregator visible only via --list-all). `task lint:taskfile` passes (LINT-03a aggregator marker exemption still applies via the `# lint-allow:` line).

    Note: lint enforces taskfile syntax; the dangling sibling refs (`install-git` etc. that do not yet exist) do not surface until the aggregator is invoked. Task 1b lands immediately after this commit to close the gap.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && grep -qE '^\s+install:\s*$' taskfiles/identity.yml && grep -qE 'internal: true' taskfiles/identity.yml && [ "$(grep -cE '^\s+-\s+task:\s+(install-git|install-ssh|install-one-password-agent)\s*$' taskfiles/identity.yml)" -eq 3 ] && task lint:taskfile 2>&1 | tail -3 | grep -qE '✓|LINT-0[1-7]'</automated>
  </verify>
  <acceptance_criteria>
    - The `install:` aggregator in `taskfiles/identity.yml` carries `internal: true` immediately after `desc:`
    - The aggregator's `cmds:` block contains exactly three `- task:` entries: `install-git`, `install-ssh`, `install-one-password-agent` (the renamed sibling names; the sub-targets themselves are renamed in Task 1b)
    - The aggregator key remains `install:` (cross-file caller `:identity:install` still resolves)
    - `task lint:taskfile` exits 0 (LINT-01..LINT-07 green)
  </acceptance_criteria>
  <done>Aggregator marked internal + cmds: retargeted to post-rename names; Task 1b will rename the sub-target keys to match.</done>
</task>

<task type="auto">
  <name>Task 1b: Rename three sub-targets to install-<target> + mark validate internal</name>
  <read_first>
    - taskfiles/identity.yml (full file; sub-targets `git:`, `ssh:`, `one-password-agent:`; aggregator `validate:`; validate sub-tasks at `validate:*`)
    - .planning/phases/12-task-surface-redesign/12-PATTERNS.md Pattern 2 (`internal: true` placement) + Pattern 4 (sub-target with status block) + Pattern 10 (identity overlay file structure)
    - .planning/phases/12-task-surface-redesign/SURFACE.md (identity: section rows)
  </read_first>
  <action>
    Apply edits in ONE commit. Each rename below uses content-based locators (per B-4: find by literal task-key match, not by absolute line number).

    **Edit `taskfiles/identity.yml`:**

    1. **Sub-target `git:` -> `install-git:`** Find the task key matching the literal `  git:` (the sub-target whose `desc:` references Git-side identity installation). Rename to `  install-git:`. Add `    internal: true` immediately after `desc:`, before the `vars:` block. The `vars:`, `preconditions:`, `cmds:` (including the internal `- task: server-include` ref), and `status:` blocks all stay unchanged. The header comment block mentioning `Git side` stays.

       Special note: the cmds: block contains `- task: server-include` -- this is a sibling short-name call to the `server-include` task in the same file. The `server-include` task is NOT renamed (already correctly internal), so this sibling ref stays as-is.

    2. **Sub-target `ssh:` -> `install-ssh:`** Find the task key matching the literal `  ssh:` (the SSH identity installer; `desc:` references SSH-side). Rename to `  install-ssh:`. Add `    internal: true` immediately after `desc:`, before the `vars:` block. The header comment block mentioning `SSH side` stays. The `status:` block references no task names and stays unchanged.

    3. **Sub-target `one-password-agent:` -> `install-one-password-agent:`** Find the task key matching the literal `  one-password-agent:` (the 1Password SSH agent installer). Rename to `  install-one-password-agent:`. Add `    internal: true` immediately after `desc:`, before `cmds:`. The header comment block stays.

       Per D-11 the resulting full task name is `identity:install-one-password-agent` (32 chars) -- the longest internal task name post-rename. Planner does NOT abbreviate.

    4. **Aggregator `validate:`** Find the task key matching the literal `  validate:` (the identity validate aggregator -- `desc:` references identity validation; `# lint-allow: cmds-without-status` marker on the line above). Add `    internal: true` immediately after `desc:`, before `cmds:`. The cmds: block referencing `validate:symlinks`, `validate:git`, `validate:ssh-add`, `validate:keys` stays unchanged (those sub-tasks are NOT renamed; refs unchanged).

    5. **Sub-tasks (`validate:symlinks`, `validate:one-password-agent`, `validate:git`, `validate:ssh-add`, `validate:keys`):** Already `internal: true`. No changes. Verify each still has `internal: true` immediately after `desc:` (drift check; no edit unless missing).

    6. **Internal `server-include:`** No changes -- already `internal: true`. Verify only.

    Commit message: `refactor(12): rename identity sub-targets to install-<target> + mark validate internal (D-01, D-10, D-11)`.

    Verify after commit: `task --list 2>&1 | grep -cE '^* identity:'` returns 0. `task --list-all 2>&1 | grep -cE 'identity:(install|install-git|install-ssh|install-one-password-agent|validate|server-include)'` returns at least 6. `task --list-all` exits 0 (graph-parse gate: confirms the cross-namespace `:identity:install` call from `taskfiles/links.yml` resolves AND the aggregator's renamed sibling refs from Task 1a now find the renamed sub-targets in the compiled task graph). `task lint` exits 0.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ "$(task --list 2>&1 | grep -cE '^\* identity:')" -eq 0 ] && [ "$(task --list-all 2>&1 | grep -cE 'identity:(install|install-git|install-ssh|install-one-password-agent|validate)')" -ge 5 ] && [ "$(grep -cE '^\s+(git|ssh|one-password-agent):\s*$' taskfiles/identity.yml)" -eq 0 ] && task --list-all >/dev/null 2>&1; rc_graph=$?; task lint 2>&1 | tail -3 | grep -qE '✓' && [ "$rc_graph" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `task --list` shows ZERO `identity:*` rows (every per-component install + validate is internal per D-01)
    - `task --list-all` shows: `identity:install`, `identity:install-git`, `identity:install-ssh`, `identity:install-one-password-agent`, `identity:validate`, plus the already-internal `identity:server-include` and the five `identity:validate:*` sub-tasks
    - `grep -cE '^\s+(git|ssh|one-password-agent):\s*$' taskfiles/identity.yml` returns 0 (the old short task keys no longer exist as YAML keys)
    - The aggregator's `cmds:` block (set by Task 1a) now resolves correctly because the three sub-target keys are renamed to match
    - `task --list-all` exits 0 (every renamed callsite resolves -- graph-parse gate confirms the cross-namespace `:identity:install` from `taskfiles/links.yml` and the aggregator's renamed sibling refs all resolve in the compiled task graph)
    - `task lint` exits 0 (LINT-01..LINT-07 green; final lint runs at the end of this plan per W-6)
  </acceptance_criteria>
  <done>identity: namespace renamed; all per-component install/validate tasks internal; no external callers broken (aggregator name unchanged).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure rename + visibility-marker refactor. No new code paths, no I/O, no new external dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Information disclosure | `internal: true` on 5 identity tasks | accept | Markers hide tasks from `task --list` but tasks remain invocable. Documented in SUMMARY as discoverability hint, not authz boundary. |
| T-12-04-02 | Tampering | Brief window between Task 1a and Task 1b where aggregator's cmds: references not-yet-renamed sub-targets | mitigate | The two commits land in immediate sequence; operator does NOT invoke `task identity:install` between commits in normal operation. Cross-file callers (`taskfiles/links.yml`) call `:identity:install` (the aggregator) -- they do not call the sub-targets directly, so the brief window is invisible to external callers. Each commit individually passes `task lint:taskfile`. |
</threat_model>

<verification>
- `task --list` reflects the new surface: zero `identity:*` rows
- `task --list-all` shows the renamed internal tasks
- `task --list-all` exits 0 (cross-namespace call from links.yml resolves; aggregator name unchanged; graph-parse gate)
- `task lint` exits 0 at end of plan (Task 1b); Task 1a uses targeted `task lint:taskfile` per W-6
- `grep -nE '^\s+(install-git|install-ssh|install-one-password-agent):' taskfiles/identity.yml` returns 3 lines (the three renamed sub-targets)
</verification>

<success_criteria>
- SURF-02 satisfied for the identity namespace: renames applied; sibling refs updated; no external callers broken.
- SURF-03 satisfied for `identity:install`, `identity:install-git`, `identity:install-ssh`, `identity:install-one-password-agent`, `identity:validate` (all marked internal).
- B-6 split honored: Task 1a (aggregator + cmds: retarget) and Task 1b (sub-target rename + validate internal) committed separately for recoverability.
- Plan 05 (macos namespace) inherits a clean state; no shared file conflicts with Plan 04.
</success_criteria>

<output>
Create `.planning/phases/12-task-surface-redesign/12-04-SUMMARY.md` recording:
- Two commit SHAs (Task 1a aggregator + cmds: retarget; Task 1b sub-target rename + validate internal)
- Final `task --list` capture (zero identity: rows)
- Final `task --list-all` capture (all renamed internals visible)
- Confirmation that the cross-namespace caller `task: :identity:install` still resolves
- Confirmation that `task --list-all` exited 0 (graph-parse gate)
- Handoff to Plan 05 (macos): no file conflicts
</output>
</content>
</invoke>
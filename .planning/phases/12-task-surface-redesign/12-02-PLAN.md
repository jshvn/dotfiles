---
phase: 12-task-surface-redesign
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - Taskfile.yml
  - taskfiles/shell.yml
  - taskfiles/README.md
  - shell/README.md
autonomous: true
requirements: [SURF-02, SURF-03]

must_haves:
  truths:
    - "`task --list` no longer shows `perf:shell` or `perf:validate` (the `perf:` include alias is gone)"
    - "`task --list` shows `shell:startup-time` instead of `shell:shell`"
    - "`task shell:validate` no longer appears in `task --list` (internal: true)"
    - "`task lint` passes after the rename (LINT-01..LINT-07 unchanged, banner-parity not yet introduced)"
    - "`task install` still succeeds end-to-end (no callers of `perf:shell` / `shell:shell` in `install:` body; only doc references)"
  artifacts:
    - path: "Taskfile.yml"
      provides: "includes block without `perf:` alias line"
      contains: "shell:    ./taskfiles/shell.yml"
    - path: "taskfiles/shell.yml"
      provides: "renamed `startup-time` task + `shell:validate` marked internal"
      exports: ["startup-time", "validate (internal)"]
  key_links:
    - from: "shell/README.md"
      to: "taskfiles/shell.yml startup-time task"
      via: "documentation reference"
      pattern: "task shell:startup-time"
    - from: "taskfiles/README.md"
      to: "taskfiles/shell.yml startup-time task"
      via: "documentation reference"
      pattern: "task shell:startup-time"
---

<objective>
Retire the dual-aliased `perf:` / `shell:` include (D-05) by dropping the `perf:` line from `Taskfile.yml`'s includes block; rename the cold-start gate task from `shell` to `startup-time` inside `taskfiles/shell.yml` (D-06); mark `shell:validate` as `internal: true` (D-01); and update every in-repo SHEL-12 reference per D-07 (`shell/README.md:43`, `taskfiles/README.md:24, 46`, `taskfiles/shell.yml:11` header comment). The result: `task --list` shows only `shell:startup-time`; no `perf:*` rows; no `shell:validate` row.

Purpose: Wave-2 anchor that retires the v2.1 dual-alias compatibility layer and demonstrates the rename + doc-update + internal-mark commit shape used by every subsequent namespace plan. No callers of `perf:shell` / `shell:shell` exist in `Taskfile.yml`'s install body (verified: lines 234-265 reference neither), so this plan does NOT need to update the install pipeline -- only docs and the includes block.

Output: One commit per logical batch (include drop; task rename; mark-internal; doc updates). Lint suite stays green after every commit.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-task-surface-redesign/12-CONTEXT.md
@.planning/phases/12-task-surface-redesign/12-PATTERNS.md
@.planning/phases/12-task-surface-redesign/SURFACE.md

<interfaces>
<!-- Taskfile.yml includes block before edit (lines 70-113); D-05 drops line 82: -->
includes:
  manifest:
    taskfile: ./taskfiles/manifest.yml
    vars: { ... }
  lint:     ./taskfiles/lint.yml
  links:    ./taskfiles/links.yml
  perf:     ./taskfiles/shell.yml   # <-- LINE 82 DROPPED per D-05
  shell:    ./taskfiles/shell.yml   # remains as the only path
  ...

<!-- Current taskfiles/shell.yml `shell:` task (lines 60-82) -- rename to `startup-time`: -->
  # lint-allow: cmds-without-status
  shell:                                # <-- KEY CHANGES to `startup-time:` per D-06
    desc: "Measure cold interactive zsh startup time (fails if > 200ms -- SHEL-12)"
    cmds: [...]

<!-- Current taskfiles/shell.yml `validate:` task (lines 96-163) -- add `internal: true` after `desc:` per D-01: -->
  validate:
    desc: "Validate shell layer (XDG dirs, ZDOTDIR dir, /etc/zshenv ZDOTDIR line, DOTFILES_MACHINE state)"
    internal: true                      # <-- NEW LINE
    status: [false]
    cmds: [...]

<!-- shell/README.md:42-44 currently reads: -->
Cold interactive shell start: <= 200ms (SHEL-12). Measured via
`task perf:shell`, which runs `hyperfine --warmup 1 --runs 5 'zsh -lic
exit'` and fails non-zero when the 5-run mean exceeds the budget.

<!-- After edit: `task perf:shell` -> `task shell:startup-time` -->

<!-- taskfiles/README.md:23-26 currently reads: -->
- **Phase 3 (real).** `links.yml` -- shell + antidote symlinks via
  `_:safe-link` plus the zdotdir step. `shell.yml` -- `task perf:shell`
  (SHEL-12 cold-start gate) plus `task shell:validate` (PORT-02 first-
  shell guarantee).

<!-- After edit: `task perf:shell` -> `task shell:startup-time`; mention `shell:validate` is internal-only invoked by root `task validate`. -->

<!-- taskfiles/README.md:46 currently reads: -->
  `task perf:shell`).
<!-- After edit: `task shell:startup-time`. -->

<!-- taskfiles/shell.yml:11-13 header banner currently reads: -->
#   - shell -- SHEL-12 cold interactive startup gate via hyperfine
#     (invoked as `task perf:shell` from the root Taskfile.yml via the
#     `perf: ./taskfiles/shell.yml` include).
<!-- After edit: drop the `perf:` invocation line; replace with `task shell:startup-time`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop the `perf:` include alias from Taskfile.yml</name>
  <read_first>
    - Taskfile.yml (lines 70-113 includes block; line 82 `perf:` alias; comment at line 12 mentioning `perf` in the includes list)
    - .planning/phases/12-task-surface-redesign/12-CONTEXT.md D-05 (drop `perf:` include line)
    - .planning/phases/12-task-surface-redesign/12-PATTERNS.md Pattern 9 D-07 SHEL-12 reference migration (Taskfile.yml:82 is the includes line; `taskfiles/README.md:24, 46` and `shell/README.md:43` are doc refs handled in Task 4)
  </read_first>
  <action>
    Edit `Taskfile.yml`:
    1. Find the line matching the literal `  perf:     ./taskfiles/shell.yml` (the `perf:` include alias) and delete it entirely. The trailing inline comment on that line (`# legacy alias: task perf:shell (SHEL-12 cold-start gate)`) is part of the deletion.
    2. On the following line (the `shell:` include alias), the trailing comment `# primary alias: task shell:validate (PORT-02)` is now misleading -- update to `# Phase-3 shell layer (startup-time gate + validate)` or similar that does not reference the dropped `perf:` alias.
    3. Update the header `Includes:` comment block in `Taskfile.yml`: find the line matching `#   - perf     (P3, real)` and delete it. Renumbering adjacent comments is not necessary -- just remove the `perf` line.

    Do NOT touch the install body or any other task. Do NOT touch `taskfiles/shell.yml` yet (Task 2). Commit message: `refactor(12): drop perf: include alias from Taskfile.yml (D-05)`.

    Verify after edit: `task --list 2>&1 | grep -c '^* perf:'` returns 0; `task --list 2>&1 | grep -c '^* shell:'` returns 2 (shell:shell + shell:validate, the rename in Task 2 turns this into 2 still: shell:startup-time + shell:validate). `task lint:taskfile` passes (no new violations).
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ "$(task --list 2>&1 | grep -c '^\* perf:')" -eq 0 ] && [ "$(grep -cE '^\s*perf:\s+\./taskfiles' Taskfile.yml)" -eq 0 ] && task lint:taskfile 2>&1 | grep -qE 'check.*LINT-0[2-7]|✓'</automated>
  </verify>
  <acceptance_criteria>
    - `task --list` output contains zero lines starting with `perf:`
    - `grep -c '^\s*perf:\s\+\./taskfiles' Taskfile.yml` returns 0
    - `task lint:taskfile` exits 0 (no LINT-02..LINT-07 regressions from the drop)
    - The `Includes:` header comment block at the top of `Taskfile.yml` no longer lists `perf`
  </acceptance_criteria>
  <done>`perf:` include alias removed; `task --list` no longer shows `perf:shell` or `perf:validate`.</done>
</task>

<task type="auto">
  <name>Task 2: Rename `shell:shell` -> `shell:startup-time` + mark `shell:validate` internal</name>
  <read_first>
    - taskfiles/shell.yml (full file -- 164 lines; tasks `shell` at line 61, `validate` at line 96)
    - .planning/phases/12-task-surface-redesign/12-CONTEXT.md D-06 (rename to `shell:startup-time`); D-01 (mark per-component validate internal)
    - .planning/phases/12-task-surface-redesign/12-PATTERNS.md Pattern 2 (`internal: true` placement convention -- line immediately after `desc:`)
  </read_first>
  <action>
    Edit `taskfiles/shell.yml` in two atomic changes within a single commit:

    1. **Rename the cold-start gate task (D-06):**
       - Find the task key matching the literal `  shell:` (2-space indent under `tasks:`, followed by the cold-start gate body whose `desc:` references SHEL-12) and rename it to `  startup-time:`.
       - The task's `desc:`, `cmds:` body, and `# lint-allow: cmds-without-status` marker on the preceding line stay unchanged.
       - The header comment block listing `- shell -- SHEL-12 cold interactive startup gate via hyperfine (invoked as 'task perf:shell' from the root Taskfile.yml via the 'perf: ./taskfiles/shell.yml' include).` is rewritten to: `- startup-time -- SHEL-12 cold interactive startup gate via hyperfine (invoked as 'task shell:startup-time').` Drop the `perf:` invocation reference entirely.
       - The `# ---------- SHEL-12 ----------` separator comment already reads `# SHEL-12: cold interactive shell start gate (<= 200ms target).` -- no change needed there.

    2. **Mark `shell:validate` internal (D-01):**
       - Locate the `validate:` task (the task key matching the literal `  validate:` whose `desc:` references PORT-02 / shell-layer validation).
       - Add a new line `    internal: true` immediately after the `desc:` line, before the `status: [false]` line. Indentation must match the surrounding lines (4 spaces -- the task key is indented 2, child keys indented 4).
       - The header comment block labelling this `PORT-02: shell-layer validation` stays unchanged; it does not surface the public/internal distinction.

    Commit message: `refactor(12): rename shell:shell -> shell:startup-time + mark shell:validate internal (D-01, D-06)`.

    Verify after edit: `task --list 2>&1 | grep -E '^* shell:'` shows exactly one entry: `shell:startup-time` (`shell:validate` is hidden by `internal: true`). `task shell:startup-time` is invocable (does not need to succeed -- hyperfine may not be available; just confirm the task exists in `--list`).
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ "$(task --list 2>&1 | grep -cE '^\* shell:startup-time')" -eq 1 ] && [ "$(task --list 2>&1 | grep -cE '^\* shell:validate')" -eq 0 ] && [ "$(task --list-all 2>&1 | grep -cE 'shell:validate')" -ge 1 ] && task lint:taskfile 2>&1 | grep -qE '✓|LINT-0[1-7]'</automated>
  </verify>
  <acceptance_criteria>
    - `task --list` shows `shell:startup-time` exactly once
    - `task --list` does NOT show `shell:validate` (hidden via `internal: true`)
    - `task --list-all` (includes internal tasks) DOES show `shell:validate` (still invocable, just hidden from default --list)
    - `taskfiles/shell.yml`'s header banner no longer mentions `task perf:shell`
    - `task lint:taskfile` exits 0 (no LINT-02 / LINT-03a regressions; the `validate` task already had `status: [false]` so LINT-03a remains satisfied)
  </acceptance_criteria>
  <done>`shell:shell` renamed; `shell:validate` internal; `task --list` reflects exactly one public `shell:*` task.</done>
</task>

<task type="auto">
  <name>Task 3: Update SHEL-12 doc references (shell/README.md, taskfiles/README.md)</name>
  <read_first>
    - shell/README.md (lines 40-46 -- "Performance budget" section referencing `task perf:shell`)
    - taskfiles/README.md (lines 23-26 referencing `task perf:shell` + `task shell:validate`; line 46 referencing `task perf:shell` again)
    - .planning/phases/12-task-surface-redesign/12-CONTEXT.md D-07 (in-repo SHEL-12 reference migration list)
    - .planning/phases/12-task-surface-redesign/12-PATTERNS.md Pattern 9 doc-reference table (exact path:line entries)
  </read_first>
  <action>
    Apply doc updates in a single commit:

    1. **shell/README.md** -- find the bullet/sentence in the "Performance budget" section containing the literal `task perf:shell` and replace `task perf:shell` with `task shell:startup-time`. The surrounding sentence becomes: `Measured via task shell:startup-time, which runs hyperfine --warmup 1 --runs 5 'zsh -lic exit' and fails non-zero when the 5-run mean exceeds the budget.`

    2. **taskfiles/README.md** Phase-3 bullet -- find the bullet containing the literal `task perf:shell (SHEL-12 cold-start gate)` and rewrite to: `shell.yml -- task shell:startup-time (SHEL-12 cold-start gate). shell:validate is internal-only (invoked by root task validate).` Adjust line breaks to match the surrounding bullet style.

    3. **taskfiles/README.md** "Adding a pattern" example -- find the second occurrence of the literal `task perf:shell` (inside the "Adding a pattern" section's worked example) and replace with `task shell:startup-time`. The surrounding sentence becomes: `The first invocation from the root namespace becomes task <alias>:<task> (for example, task shell:startup-time).`

    Commit message: `docs(12): update SHEL-12 references to task shell:startup-time (D-07)`.

    Verify: `grep -rEn 'task perf:shell' README.md CLAUDE.md .claude/CLAUDE.md docs/ shell/ taskfiles/ 2>/dev/null` returns no lines (the `perf:shell` reference is fully purged). `grep -rEn 'task shell:startup-time' shell/README.md taskfiles/README.md` returns at least 3 lines (one in shell/README.md, two in taskfiles/README.md).
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ "$(grep -rEn 'task perf:shell' README.md CLAUDE.md .claude/CLAUDE.md docs/ shell/ taskfiles/ 2>/dev/null | wc -l | tr -d ' ')" -eq 0 ] && [ "$(grep -rEn 'task shell:startup-time' shell/README.md taskfiles/README.md 2>/dev/null | wc -l | tr -d ' ')" -ge 3 ] && task lint 2>&1 | tail -3 | grep -qE '✓'</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `task perf:shell` in any project doc (README.md, CLAUDE.md, .claude/CLAUDE.md, docs/, shell/, taskfiles/)
    - At least 3 occurrences of `task shell:startup-time` across shell/README.md and taskfiles/README.md
    - `taskfiles/README.md` `shell:validate` mention updated to surface its internal-only status
    - `task lint` exits 0 (final acceptance for this plan -- per W-6 full lint runs at the last task of each plan)
  </acceptance_criteria>
  <done>SHEL-12 doc references migrated; no stale `perf:shell` references remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure rename + visibility-marker refactor. No new code paths, no new I/O, no new external dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Information disclosure | `internal: true` marker on `shell:validate` | accept | Marker hides task from `task --list` output but the task remains invocable via `task shell:validate` (verified by `task --list-all`). This is NOT a defense-in-depth boundary or authz control -- operators can still invoke the task directly; the marker is a discoverability hint, not an access restriction. Document this fact in the SUMMARY so future readers don't mistake the marker for an authz control. |
</threat_model>

<verification>
- `task --list` shows `shell:startup-time` but not `shell:shell`, `perf:shell`, `perf:validate`, or `shell:validate`
- `task --list-all` shows `shell:validate` (confirms internal-but-invocable)
- `task lint` exits 0 at the end of the plan (Task 3); intermediate tasks use targeted sub-checks (`task lint:taskfile`) per W-6
- `grep -rEn 'task perf:shell' README.md CLAUDE.md .claude/CLAUDE.md docs/ shell/ taskfiles/` returns no lines
- `git log --oneline -- taskfiles/shell.yml Taskfile.yml shell/README.md taskfiles/README.md` shows three commits (one per task in this plan)
</verification>

<success_criteria>
- SURF-02 satisfied for the shell namespace: rename applied to `taskfiles/shell.yml`; all in-repo doc references updated.
- SURF-03 satisfied for `shell:validate`: marked `internal: true`; absent from `task --list`.
- The `perf:` include alias from Phase 10 D-06 is retired; v2.1's compatibility shim is gone.
- Next plans (03 onward) inherit a coherent shell namespace.
</success_criteria>

<output>
Create `.planning/phases/12-task-surface-redesign/12-02-SUMMARY.md` recording:
- Three commit SHAs (perf: drop; shell:shell rename + validate internal; doc updates)
- Live `task --list | grep shell:` and `task --list | grep perf:` output captures
- Confirmation that `task install` still succeeds end-to-end after the rename (no caller of shell:shell / perf:shell in install body; verified via re-run)
- Handoff to Plan 03 (links namespace): no shared file conflicts; Plan 03 can proceed in Wave 3
</output>
</content>
</invoke>
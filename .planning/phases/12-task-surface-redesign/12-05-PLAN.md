---
phase: 12-task-surface-redesign
plan: 05
type: execute
wave: 5
depends_on: [12-01, 12-03]
files_modified:
  - taskfiles/macos.yml
  - Taskfile.yml
  - docs/MACHINES.md
autonomous: true
requirements: [SURF-02, SURF-03]
# W-2 note: Plan 05 is sequenced after Plan 03 for review clarity; Taskfile.yml is touched by
# 03/05/06/07/08 so no safe parallelization across those plans.

must_haves:
  truths:
    - "`task --list` shows ZERO `macos:*` rows (every macos task is internal per D-01)"
    - "`task --list-all` shows the new `macos:install` aggregator + renamed `macos:apply-defaults` + `macos:install-shell` + `macos:validate` + the 5 renamed `macos:apply-defaults:<concern>` sub-tasks"
    - "`task install` invokes the new `macos:install` aggregator (single call) instead of the two separate calls (`macos:defaults` + `macos:shell`)"
    - "`task lint` passes after every commit"
  artifacts:
    - path: "taskfiles/macos.yml"
      provides: "New `install` aggregator; renamed `apply-defaults` and `install-shell` sub-targets; renamed `apply-defaults:<concern>` sub-tasks; all marked internal"
      exports: ["install (internal aggregator -- NEW)", "apply-defaults (internal)", "install-shell (internal)", "validate (internal)", "apply-defaults:{dock,finder,input,screenshots,security} (internal -- renamed from defaults:<concern>)"]
    - path: "Taskfile.yml"
      provides: "install: body collapses macos:defaults + macos:shell into single macos:install call"
      contains: "task: macos:install"
  key_links:
    - from: "Taskfile.yml install: body"
      to: "taskfiles/macos.yml install: task (new aggregator)"
      via: "task: macos:install"
      pattern: "task: macos:install"
---

<objective>
Apply the macos-namespace rename pass per D-09 + D-10 + D-01:
- ADD a new aggregator `macos:install` (D-09; the FIRST aggregator in `taskfiles/macos.yml`) + `internal: true` + `platforms: [darwin]` + `deps: [:manifest:resolve]` + cmds: `[- task: apply-defaults, - task: install-shell]`
- `macos:defaults` -> `macos:apply-defaults` + `internal: true` (D-10 -- macOS uses `apply-` for non-install actions)
- `macos:shell` -> `macos:install-shell` + `internal: true` (D-10 -- shell registration IS an install action)
- `macos:validate` -> (keep name) + `internal: true` (D-01)
- **Per B-9 empirical confirmation:** `defaults:dock`, `defaults:finder`, `defaults:input`, `defaults:screenshots`, `defaults:security` are LITERAL task names in `taskfiles/macos.yml` (NOT sub-tasks of `defaults:`). For D-10 verb-first consistency, rename all five in lockstep with the parent: `defaults:<concern>` -> `apply-defaults:<concern>`. They surface as `macos:apply-defaults:<concern>` via the include alias. They remain `internal: true` (no visibility change). The parent's `cmds:` block sibling refs are updated to call the new names.

`Taskfile.yml`'s `install:` body (the two consecutive lines `- task: macos:defaults` and `- task: macos:shell`) collapses to a single `- task: macos:install` call per the recommendation in 12-PATTERNS.md Pattern 7.

The doc reference at `docs/MACHINES.md` (the bullet referencing `task macos:defaults` in the macos-security explanation) updates to `task macos:apply-defaults` (D-10).

B-9 commits to this single path -- there is NO sibling-resolution fallback (empirically confirmed via `taskfiles/macos.yml:142,156,170,184,198` that the sub-task keys are literal full names, not nested under the parent).
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-task-surface-redesign/12-CONTEXT.md
@.planning/phases/12-task-surface-redesign/12-PATTERNS.md
@.planning/phases/12-task-surface-redesign/SURFACE.md

<interfaces>
<!-- Rename + creation table (B-9 commits to apply-defaults:<concern>): -->

| Before | After | Touch |
|---|---|---|
| (none) | `macos:install` (NEW aggregator) | create per Pattern 3 fingerprint + D-09 |
| `macos:defaults` | `macos:apply-defaults` | key + `internal: true` |
| `macos:defaults:dock` | `macos:apply-defaults:dock` | key (literal task name; already `internal: true`) |
| `macos:defaults:finder` | `macos:apply-defaults:finder` | key (literal task name; already `internal: true`) |
| `macos:defaults:input` | `macos:apply-defaults:input` | key (literal task name; already `internal: true`) |
| `macos:defaults:screenshots` | `macos:apply-defaults:screenshots` | key (literal task name; already `internal: true`) |
| `macos:defaults:security` | `macos:apply-defaults:security` | key (literal task name; already `internal: true`) |
| `macos:shell` | `macos:install-shell` | key + `internal: true` |
| `macos:validate` | (keep name) | add `internal: true` |

<!-- New `macos:install` aggregator shape (12-PATTERNS.md Pattern 3): -->

  # lint-allow: cmds-without-status
  install:
    desc: "Apply macOS defaults + register Homebrew zsh as login shell"
    internal: true
    platforms: [darwin]
    deps: [":manifest:resolve"]
    cmds:
      - task: apply-defaults
      - task: install-shell

<!-- Placement: at the TOP of the tasks: block, BEFORE the existing `defaults:` aggregator. The new aggregator must precede its sub-tasks for clarity. -->

<!-- The `apply-defaults:` (was `defaults:`) cmds: block sibling refs (B-9): -->
  cmds:
    - task: apply-defaults:dock          # was: defaults:dock
    - task: apply-defaults:finder        # was: defaults:finder
    - task: apply-defaults:input         # was: defaults:input
    - task: apply-defaults:screenshots   # was: defaults:screenshots
    - task: apply-defaults:security      # was: defaults:security

<!-- Taskfile.yml install: body BEFORE: -->
    cmds:
      - task: links:install       # was: links:all (already renamed by Plan 03)
      - task: packages:install
      - task: claude:install
      - task: macos:defaults      # collapses to `task: macos:install`
      - task: macos:shell         # DELETED (folded into macos:install)
      - task: packages:verify
      - task: audit:links         # was: links:reconcile (already renamed by Plan 03)
        vars: { CLI_ARGS: "--warn-only" }

<!-- AFTER: -->
    cmds:
      - task: links:install
      - task: packages:install
      - task: claude:install
      - task: macos:install       # NEW: aggregator replaces the two separate calls
      - task: packages:verify
      - task: audit:links
        vars: { CLI_ARGS: "--warn-only" }

<!-- docs/MACHINES.md bullet referencing macos-security defaults concern: rewrites `task macos:defaults` -> `task macos:apply-defaults`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `macos:install` aggregator + rename `defaults` family to `apply-defaults` + rename `shell` to `install-shell` + mark validate internal</name>
  <read_first>
    - taskfiles/macos.yml (full file -- 308 lines; aggregator `defaults:` at line 120; five literal-named sub-tasks `defaults:dock` (142), `defaults:finder` (156), `defaults:input` (170), `defaults:screenshots` (184), `defaults:security` (198); `shell:` at 222; `validate:` at 257)
    - Taskfile.yml install: body (the two consecutive lines `- task: macos:defaults` and `- task: macos:shell`)
    - .planning/phases/12-task-surface-redesign/12-PATTERNS.md Pattern 3 (aggregator fingerprint + the explicit `macos:install` template) + Pattern 2 (`internal: true` placement) + Pattern 4 (sub-target shape)
    - .planning/phases/12-task-surface-redesign/SURFACE.md (macos: section rows -- including the five renamed `apply-defaults:<concern>` rows added per B-9)
  </read_first>
  <action>
    Apply the following edits in ONE commit (callers-first: the Taskfile.yml install body is updated in the same commit as the callee renames). Each rename uses content-based locators (per B-4: find by literal task-key match, not by absolute line number).

    **Edit `taskfiles/macos.yml`:**

    1. **Insert the new `macos:install` aggregator at the top of the `tasks:` block** (immediately after the `tasks:` key line, before the existing `defaults:` aggregator). Add (with 2-space indentation under `tasks:`):
       - Section separator comment block (`# ---------------------------------------------------------------------------` style matching surrounding tasks).
       - Comment: `# install -- macOS aggregator (D-09 <ns>:install pattern).`
       - Comment: `# Replaces the two-call pattern in Taskfile.yml install: body (macos:apply-defaults + macos:install-shell).`
       - Comment: `# Internal per D-01 (per-component install is internal); platforms: [darwin] matches the sub-tasks.`
       - Closing `# ---------------------------------------------------------------------------` separator.
       - `# lint-allow: cmds-without-status` marker line.
       - Task key `install:` with `desc: "Apply macOS defaults + register Homebrew zsh as login shell"`, `internal: true`, `platforms: [darwin]`, `deps: [":manifest:resolve"]`, and `cmds:` block invoking `- task: apply-defaults` then `- task: install-shell` (the renamed siblings; sibling refs inside this taskfile use short names).

    2. **Rename `defaults:` -> `apply-defaults:`** Find the task key matching the literal `  defaults:` (the aggregator with `desc:` "Apply macOS system defaults (per-concern, feature-gated)"). Rename to `  apply-defaults:`. Add `    internal: true` immediately after `desc:`, before `platforms:`. Update the header comment block (the one referencing `defaults -- aggregator`) to: `apply-defaults -- aggregator (was: defaults; renamed per D-10 -- macOS uses apply- for non-install actions).`.

    3. **Update the `apply-defaults:` cmds: block sibling refs (B-9):**
       - Find the line matching the literal `      - task: defaults:dock` and replace with `      - task: apply-defaults:dock`.
       - Find the line matching the literal `      - task: defaults:finder` and replace with `      - task: apply-defaults:finder`.
       - Find the line matching the literal `      - task: defaults:input` and replace with `      - task: apply-defaults:input`.
       - Find the line matching the literal `      - task: defaults:screenshots` and replace with `      - task: apply-defaults:screenshots`.
       - Find the line matching the literal `      - task: defaults:security` and replace with `      - task: apply-defaults:security`.

    4. **Rename the five literal-named sub-tasks (B-9 -- they are LITERAL task names, NOT nested under a parent):**
       - Find the task key matching the literal `  defaults:dock:` and rename to `  apply-defaults:dock:`. Body unchanged (already `internal: true`).
       - Find the task key matching the literal `  defaults:finder:` and rename to `  apply-defaults:finder:`. Body unchanged.
       - Find the task key matching the literal `  defaults:input:` and rename to `  apply-defaults:input:`. Body unchanged.
       - Find the task key matching the literal `  defaults:screenshots:` and rename to `  apply-defaults:screenshots:`. Body unchanged.
       - Find the task key matching the literal `  defaults:security:` and rename to `  apply-defaults:security:`. Body unchanged.

    5. **Rename `shell:` -> `install-shell:`** Find the task key matching the literal `  shell:` (the task with `desc:` referencing "Register Homebrew zsh as login shell"). Rename to `  install-shell:`. Add `    internal: true` immediately after `desc:`, before `platforms:`. Update the header comment block (the one referencing `shell -- always-on`) to: `install-shell -- always-on (was: shell; renamed per D-10).`. The `vars:`, `cmds:`, `status:` blocks stay unchanged.

    6. **Mark `validate:` internal:** Find the task key matching the literal `  validate:` (the task with `desc:` "Validate macOS defaults match in-script expected values"). Add `    internal: true` immediately after `desc:`, before `platforms:`. The `# lint-allow: cmds-without-status` marker on the preceding line stays. Header comment block stays.

    **Edit `Taskfile.yml`:**

    7. **Collapse the two install-body lines:**
       - Find the line matching the literal `      - task: macos:defaults` (inside the `install:` body) and replace with `      - task: macos:install`.
       - Find the line matching the literal `      - task: macos:shell` and delete it entirely (folded into the `macos:install` aggregator).
       - Verify the surrounding indentation and YAML validity after deletion (the cmds: list reduces by one entry).

    **Edit `docs/MACHINES.md`:**

    8. Find the bullet/sentence containing the literal `task macos:defaults` (in the macos-security feature explanation; the sentence reads "the macos-security defaults concern runs as part of task macos:defaults") and replace `task macos:defaults` with `task macos:apply-defaults`.

    Commit message: `feat(12): add macos:install aggregator + rename macos:defaults family -> apply-defaults + macos:shell -> install-shell (D-01, D-09, D-10, B-9)`.

    Verify after commit: `task --list 2>&1 | grep -cE '^* macos:'` returns 0. `task --list-all 2>&1` shows `macos:install` (new), `macos:apply-defaults`, `macos:install-shell`, `macos:validate`, and the 5 renamed `macos:apply-defaults:<concern>` sub-tasks. `task --list-all` exits 0 (graph-parse gate: the parent `apply-defaults` now correctly references the renamed `apply-defaults:<concern>` sibling keys in the compiled task graph). `task lint` exits 0.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ "$(task --list 2>&1 | grep -cE '^\* macos:')" -eq 0 ] && [ "$(task --list-all 2>&1 | grep -cE 'macos:(install|apply-defaults|install-shell|validate)$')" -ge 4 ] && [ "$(task --list-all 2>&1 | grep -cE 'macos:apply-defaults:(dock|finder|input|screenshots|security)')" -eq 5 ] && grep -qE '^\s*-\s+task:\s+macos:install\s*$' Taskfile.yml && [ "$(grep -cE '^\s*-\s+task:\s+macos:(defaults|shell)\s*$' Taskfile.yml)" -eq 0 ] && grep -qE 'task macos:apply-defaults' docs/MACHINES.md && task --list-all >/dev/null 2>&1; rc_graph=$?; task lint 2>&1 | tail -3 | grep -qE '✓' && [ "$rc_graph" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `task --list` shows ZERO `macos:*` rows
    - `task --list-all` shows: `macos:install` (NEW aggregator), `macos:apply-defaults`, `macos:install-shell`, `macos:validate`, plus the 5 renamed `macos:apply-defaults:<concern>` sub-tasks (dock, finder, input, screenshots, security)
    - `Taskfile.yml` install: body contains exactly one line `- task: macos:install` and ZERO lines matching `- task: macos:(defaults|shell)`
    - `docs/MACHINES.md` contains `task macos:apply-defaults` and NOT `task macos:defaults`
    - `task --list-all` exits 0 (every renamed callsite resolves -- graph-parse gate confirms the `apply-defaults` parent's cmds: block correctly references the five `apply-defaults:<concern>` sibling keys and all cross-namespace refs compile)
    - `task lint` exits 0 (LINT-01..LINT-07 green; LINT-03a satisfied via `# lint-allow:` marker on the new aggregator)
  </acceptance_criteria>
  <done>macos: namespace renamed + new aggregator added; install pipeline collapses to single macos:install call; docs updated; five `defaults:<concern>` sub-tasks renamed in lockstep per B-9.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure rename + aggregator addition. The new `macos:install` aggregator wraps existing sub-tasks; no new code paths or I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | Tampering | Sibling resolution in `apply-defaults` cmds: block | mitigate | B-9 empirically confirmed that the five sub-task keys are LITERAL task names (verified via taskfiles/macos.yml:142,156,170,184,198); they are renamed in lockstep with the parent + the parent's cmds: block. No go-task sibling-resolution risk. |
| T-12-05-02 | Information disclosure | `internal: true` on 4 macos tasks | accept | Markers hide tasks from `task --list` but remain invocable. Not authz. |
</threat_model>

<verification>
- `task --list` shows zero `macos:*` rows
- `task --list-all` shows the new aggregator + 4 renamed/internal tasks + 5 renamed sub-concerns (`apply-defaults:<concern>`)
- `Taskfile.yml` install: body has the single `- task: macos:install` line; no `macos:defaults` / `macos:shell` calls
- `docs/MACHINES.md` updated
- `task --list-all` exits 0 (graph-parse gate)
- `task lint` green at end of plan
</verification>

<success_criteria>
- SURF-02 satisfied for the macos namespace: renames + aggregator-add + doc update applied; B-9 sub-task rename completed.
- SURF-03 satisfied for `macos:install`, `macos:apply-defaults`, `macos:install-shell`, `macos:validate` (all marked internal); the five `apply-defaults:<concern>` sub-tasks remain `internal: true`.
- D-09 honored: the new aggregator `macos:install` replaces the two-line install body pattern; future maintainers see uniform `<ns>:install` callees across the install pipeline.
- B-9 honored: D-10 verb-first consistency extended to all sub-tasks; no fallback path; single committed approach.
- Plan 06 (packages + claude) inherits a clean macos namespace.
</success_criteria>

<output>
Create `.planning/phases/12-task-surface-redesign/12-05-SUMMARY.md` recording:
- Commit SHA for the macos rename + aggregator add
- Final `task --list` capture
- Final `task --list-all` capture (confirms all 5 `apply-defaults:<concern>` renamed sub-tasks visible)
- Confirmation that `task --list-all` exits 0 (graph-parse gate)
- Handoff to Plan 06 (packages + claude): no file conflicts; Plan 06 modifies `taskfiles/packages.yml`, `taskfiles/claude.yml`, and extends `taskfiles/audit.yml`
</output>
</content>
</invoke>
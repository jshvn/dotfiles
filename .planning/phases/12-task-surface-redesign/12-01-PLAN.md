---
phase: 12-task-surface-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-task-surface-redesign/SURFACE.md
autonomous: true
requirements: [SURF-01]

must_haves:
  truths:
    - "Every public task in today's `task --list` output has a row in SURFACE.md"
    - "Every row in SURFACE.md carries one of four verdicts: keep-as-is / rename / mark-internal / remove"
    - "Every rename row has a non-empty new-name cell; every mark-internal row marks `internal: true?` = yes"
    - "Every row's rationale cites the decision ID (D-NN) that justifies it"
    - "The callsites column lists exact path:line references for every doc/taskfile/alias mentioning the renamed task"
  artifacts:
    - path: ".planning/phases/12-task-surface-redesign/SURFACE.md"
      provides: "SURF-01 classification table -- the source-of-truth iterated by Plans 02-08"
      contains: "task name (current), verdict, new name, internal: true?, rationale, callsites to update"
  key_links:
    - from: ".planning/phases/12-task-surface-redesign/SURFACE.md"
      to: ".planning/phases/12-task-surface-redesign/12-CONTEXT.md decisions D-01..D-15"
      via: "rationale column D-NN citations"
      pattern: "D-(0[1-9]|1[0-5])"
---

<objective>
Author `SURFACE.md` -- the six-column classification table that lists every public task on today's `task --list` surface with its verdict (keep-as-is / rename / mark-internal / remove), proposed new name, `internal: true?` flag, rationale (citing D-NN), and pre-populated callsites column. This is the SURF-01 deliverable and the source-of-truth that Plans 02-08 iterate.

Purpose: Plans 02-08 walk SURFACE.md row-by-row to apply renames + visibility changes. Without this table the downstream plans have no canonical worklist. The table also serves as the post-phase audit artifact -- Phase 13 reviewers read SURFACE.md to know which tasks were intentionally surfaced vs. hidden.

Output: A single committed file at `.planning/phases/12-task-surface-redesign/SURFACE.md`. Read-only artifact -- no code is modified in this plan. House style mirrors Phase 9's `AUDIT.md`.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-task-surface-redesign/12-CONTEXT.md
@.planning/phases/12-task-surface-redesign/12-PATTERNS.md
@.planning/phases/09-v1-drop-audit/AUDIT.md
@.planning/phases/09-v1-drop-audit/09-01-PLAN.md

<interfaces>
<!-- The six-column shape SURFACE.md must adopt (from 12-PATTERNS.md Pattern 1 D-14). -->
<!-- Column header row, written verbatim into SURFACE.md sections: -->

| task name (current) | verdict | new name (if renamed) | internal: true? | rationale | callsites to update |
|---------------------|---------|-----------------------|------------------|-----------|---------------------|

<!-- Verdict-enum lock (D-14): verdict cell is exactly one of: keep-as-is | rename | mark-internal | remove. -->
<!-- Combined verdicts use " + ": e.g., `rename + mark-internal`. -->

<!-- Worked sample row (12-PATTERNS.md Pattern 1, links:all -> links:install): -->
| `links:all` | rename + mark-internal | `links:install` | yes | D-09 aggregator pattern; D-01 per-component install is internal | Taskfile.yml:235; taskfiles/README.md:23 |

<!-- Today's `task --list` output to be classified (47 entries; captured 2026-05-18): -->
<!-- Top-level (5): default, install, setup, test, validate -->
<!-- claude:* (4): claude:install, claude:status, claude:update, claude:validate -->
<!-- identity:* (5): identity:git, identity:install, identity:one-password-agent, identity:ssh, identity:validate -->
<!-- links:* (6): links:all, links:claude, links:configs, links:reconcile, links:validate, links:zsh -->
<!-- lint:* (6): lint:default(alias=lint), lint:portability, lint:shell-headers, lint:syntax, lint:taskfile, lint:test-fixtures -->
<!-- macos:* (3): macos:defaults, macos:shell, macos:validate -->
<!-- manifest:* (6): manifest:resolve, manifest:setup, manifest:show, manifest:test, manifest:test:add-machine, manifest:validate -->
<!-- packages:* (5): packages:audit, packages:compose, packages:install, packages:validate, packages:verify -->
<!-- perf:* (2): perf:shell, perf:validate (dual-alias from D-06 retiring) -->
<!-- shell:* (2): shell:shell, shell:validate (other half of dual-alias) -->
<!-- test:* (2): test:default, test:hooks -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture live `task --list` output and stub SURFACE.md skeleton</name>
  <read_first>
    - .planning/phases/12-task-surface-redesign/12-CONTEXT.md (locked decisions D-01..D-15, especially D-14 column shape and D-15 callsite-finder rule)
    - .planning/phases/12-task-surface-redesign/12-PATTERNS.md (Pattern 1: SURFACE.md classification table; Pattern 9: doc-reference targets / callsite map)
    - .planning/phases/09-v1-drop-audit/AUDIT.md (house style reference -- header structure, Summary counts table, section grouping)
  </read_first>
  <action>
    Capture today's task surface to an intermediate file by running `task --list 2>&1` and saving stdout to `.planning/phases/12-task-surface-redesign/12-surface-snapshot.txt` (intermediate working file -- delete in Task 3 after SURFACE.md is finalized).

    Create `.planning/phases/12-task-surface-redesign/SURFACE.md` with the following top-level structure (house style mirrors Phase 9 AUDIT.md per D-14):
    1. Title: `# Phase 12: Task Surface Classification (SURF-01)`.
    2. Frontmatter prose block stating: capture date, total task count from snapshot, brief one-paragraph purpose (cite SURF-01 and D-14).
    3. `## Summary` section with two sub-blocks: (a) counts table with rows `Tasks classified`, `keep-as-is`, `rename`, `mark-internal`, `rename + mark-internal`, `remove` and a Count column; (b) a bullet-list "Renames at a glance" listing only the rename rows (old name -> new name).
    4. `## Top-Level Commands` section with the six-column table header (per the `<interfaces>` block in this plan's context) followed by rows for `default`, `install`, `setup`, `test`, `validate`.
    5. One section per namespace prefix encountered in the snapshot: `## claude:`, `## identity:`, `## links:`, `## lint:`, `## macos:`, `## manifest:`, `## packages:`, `## perf:`, `## shell:`, `## test:`. Each section has the same six-column header.
    6. Final section `## Decisions Cross-Reference` -- bullet list with one bullet per D-01..D-15 mapping the decision ID to which rows it justifies (e.g., `D-01: per-component install/validate tasks (15 rows)`).

    Leave every table body empty in this task -- Task 2 fills the rows. Commit message: `docs(12): scaffold SURFACE.md classification skeleton`.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-task-surface-redesign/SURFACE.md && grep -c '^## ' .planning/phases/12-task-surface-redesign/SURFACE.md | grep -qE '^(12|13|14)$'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/12-task-surface-redesign/SURFACE.md` exists
    - Twelve to fourteen `## ` headers present (Summary, Top-Level Commands, one per namespace, Decisions Cross-Reference; up to two additional sections such as Diagnostics show:/audit:/refresh: may be added by Task 2)
    - Every section has the six-column markdown table header with the exact column names from the `<interfaces>` block
    - `12-surface-snapshot.txt` (intermediate) exists and is non-empty
    - No row bodies populated yet (rows added in Task 2)
  </acceptance_criteria>
  <done>Skeleton SURFACE.md committed; intermediate snapshot captured for Task 2 consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Populate every row and pre-populate the callsites column</name>
  <read_first>
    - .planning/phases/12-task-surface-redesign/SURFACE.md (the skeleton from Task 1)
    - .planning/phases/12-task-surface-redesign/12-surface-snapshot.txt (the live `task --list` capture)
    - .planning/phases/12-task-surface-redesign/12-CONTEXT.md (D-01..D-15 -- every rationale cell cites one or more of these)
    - .planning/phases/12-task-surface-redesign/12-PATTERNS.md (Pattern 9: Doc-Reference Map with path:line references for SURF-02 callsites; D-10 worked rename examples; the rename table at lines 28-74)
    - Taskfile.yml (callsites in `install:` body lines 234-265; `validate:` aggregator iteration list lines 206, 213; `includes:` block lines 70-113)
  </read_first>
  <action>
    Per D-14 and the Pattern Map's Edit-targets table (12-PATTERNS.md lines 28-74), populate every row in every section. Rules:

    1. **Verdict column** -- exactly one of: `keep-as-is`, `rename`, `mark-internal`, `remove`. Combined verdict: `rename + mark-internal` (single allowed combo per D-14).
    2. **New name column** -- empty for non-rename rows; for renames use the values from 12-PATTERNS.md lines 28-74 (e.g., `links:all` -> `links:install`; `links:zsh` -> `links:install-zsh`; `claude:status` -> `show:claude`; `claude:update` -> `refresh:claude`; `manifest:show` -> `show:manifest`; `packages:audit` -> `audit:packages`; `macos:defaults` -> `macos:apply-defaults`; `macos:shell` -> `macos:install-shell`; `shell:shell` (and `perf:shell`) -> `shell:startup-time`; `manifest:test` -> `test:manifest`; `manifest:test:add-machine` -> `test:add-machine`; identity sub-targets gain `install-` prefix per D-10; `links:reconcile` -> verdict is `rename + mark-internal` with new name `audit:links` (final mechanic per B-2: `links:reconcile` is marked `internal: true` in `taskfiles/links.yml` and the public name `audit:links` is exposed via a thin delegate in `taskfiles/audit.yml`; record this verdict explicitly as `rename + mark-internal` so Plan 03 inherits the agreed-upon path)).
    3. **internal: true? column** -- `yes` for every per-component install/validate (D-01), every lint sub-check (D-04), every test sub-task (D-04), every newly-internal aggregator (e.g., `links:install`, `claude:install`, `identity:install`, `packages:install`, `macos:install`, `manifest:setup`, `manifest:resolve`, `manifest:validate`). `no` only for: top-level commands (`default`, `install`, `setup`, `test`, `validate`), the public diagnostics (`show:*`, `audit:*`, `refresh:*`), the public aggregator `lint` (alias of `lint:default`).
    4. **Rationale column** -- one line citing the relevant decision ID(s). Examples: `D-01: per-component install is internal`; `D-09 aggregator pattern + D-01 internal`; `D-02 diagnostic moves to audit:`; `D-04 lint sub-check internal`; `D-05 perf: alias retired + D-06 rename`. Keep rationale to <= 110 chars per cell.
    5. **Callsites to update column** -- pre-populate via `git grep -nE 'task [a-z][a-z0-9:-]+|task: [a-z][a-z0-9:-]+|<old-task-name>' README.md CLAUDE.md .claude/CLAUDE.md docs/ shell/README.md taskfiles/` per row. List exact `path:line` entries semicolon-separated. For taskfile-internal callsites (e.g., `task: zdotdir` inside `taskfiles/links.yml`), include the file:line. For renames moving namespaces (e.g., `links:reconcile` -> `audit:links`), also list `Taskfile.yml:254` (the `task: links:reconcile` line in `install:` body).

    Special-case rows the planner must include:
    - `perf:shell` and `shell:shell` both map to `shell:startup-time` (D-05 drops the `perf:` include alias; both currently-visible names collapse). Treat `perf:shell` as `rename`, verdict mentions D-05 + D-06.
    - `perf:validate` and `shell:validate` both map to `shell:validate` + mark-internal (D-05 drops the dual alias; D-01 marks per-component validate internal).
    - `links:reconcile` -- per B-2 the final mechanic is "rename + mark-internal": the implementation task `links:reconcile` is marked `internal: true` in `taskfiles/links.yml`; the public name `audit:links` is exposed via a thin delegate task in a new `taskfiles/audit.yml` (the delegate's `cmds:` is `- task: links:reconcile-impl` if `links:reconcile` is also renamed to `reconcile-impl`, OR the delegate calls `task: :links:reconcile` directly). Record the verdict cell as `rename + mark-internal`; the new-name cell is `audit:links`. There are NO competing options; the single agreed-upon path is captured here.
    - Five macOS `defaults:<concern>` sub-tasks (`defaults:dock`, `defaults:finder`, `defaults:input`, `defaults:screenshots`, `defaults:security`) -- per B-9 these are LITERAL task names that live inside `taskfiles/macos.yml` and surface as `macos:defaults:<concern>` via the include alias. For D-10 verb-first consistency, each renames in lockstep with the parent: new names are `apply-defaults:dock`, `apply-defaults:finder`, `apply-defaults:input`, `apply-defaults:screenshots`, `apply-defaults:security` (surfacing as `macos:apply-defaults:<concern>`). Verdict for each row: `rename` (they are already `internal: true` so no additional mark-internal needed). Rationale cell cites D-10 + the macos parent rename + B-9 empirical-confirmation note. Include all five rows in the `## macos:` section of SURFACE.md.
    - `macos:install` (NEW aggregator per D-09): add a row in `## macos:` section with verdict `create + mark-internal` (note: this is a new row not in the snapshot; flag with `(NEW)` in the current-name cell). Document that this aggregator is created by Plan 05.
    - `lint:banner-parity` (NEW lint check per D-13): add a row in `## lint:` section with verdict `create + mark-internal` and `(NEW)` flag; created by Plan 08.
    - `audit:manifest` (NEW public delegate per D-03): add a row under a new section `## Diagnostics (show: / audit: / refresh:)` or under `## manifest:`; verdict `create` (public); created by Plan 07.

    Finally fill the `## Summary` counts table with the actual tallied numbers and the "Renames at a glance" bullet list. Commit message: `docs(12): populate SURFACE.md rows + callsites`.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-task-surface-redesign/SURFACE.md && [ "$(grep -cE '^\| `[a-z][a-z0-9:-]+`' .planning/phases/12-task-surface-redesign/SURFACE.md)" -ge 47 ] && grep -qE 'D-(0[1-9]|1[0-5])' .planning/phases/12-task-surface-redesign/SURFACE.md</automated>
  </verify>
  <acceptance_criteria>
    - At least 47 rows starting with `` | `<task-name>` `` (one per public task from snapshot, plus the three NEW rows; plus the five `defaults:<concern>` -> `apply-defaults:<concern>` rows added per B-9)
    - Every verdict cell value is one of the locked enum (`keep-as-is`, `rename`, `mark-internal`, `rename + mark-internal`, `create + mark-internal`, `create`, `remove`)
    - Every rename row has a non-empty new-name cell
    - The `links:reconcile` row has verdict `rename + mark-internal` and new-name `audit:links` (single committed path per B-2)
    - The five `defaults:<concern>` rows are present with verdict `rename` and new-names `apply-defaults:<concern>` (per B-9)
    - Every rationale cell contains at least one `D-NN` citation (regex `D-(0[1-9]|1[0-5])`)
    - Summary counts table reflects accurate tallies (sum of category counts equals total rows)
    - "Renames at a glance" bullet list count equals the verdict-column "rename" count
    - Callsites column populated for every rename row (non-empty cell; at minimum lists the taskfile defining the task)
  </acceptance_criteria>
  <done>SURFACE.md fully populated; every row carries verdict + rationale + callsites; Plans 02-08 have a complete worklist.</done>
</task>

<task type="auto">
  <name>Task 3: Self-audit + cleanup + git commit</name>
  <read_first>
    - .planning/phases/12-task-surface-redesign/SURFACE.md (the fully populated table from Task 2)
  </read_first>
  <action>
    Run three self-audit checks against SURFACE.md and fix any drift before commit:

    1. **Verdict-enum check:** every row's verdict cell value must match one of the locked enum from D-14. Use `awk -F'|' 'NR>2 {print $3}'` on each section's table body to enumerate values; reject any value not in the set.
    2. **D-NN citation density:** every row's rationale must cite at least one decision ID. Use `awk -F'|' '$3 ~ /(rename|mark-internal|create)/ {if ($6 !~ /D-[0-9]+/) print NR ": missing D-NN -- " $0}'` -- any output indicates missing citation; fix before commit.
    3. **Callsite-column non-empty for renames:** for every row whose verdict contains `rename`, the callsites cell must be non-empty. Empty cells signal incomplete grep; re-run `git grep -nE 'task [a-z][a-z0-9:-]+' README.md CLAUDE.md .claude/CLAUDE.md docs/ shell/README.md taskfiles/` and amend rows.

    Then delete the intermediate `.planning/phases/12-task-surface-redesign/12-surface-snapshot.txt` (no longer needed -- SURFACE.md is the canonical artifact).

    Final commit: `docs(12): finalize SURFACE.md classification table (SURF-01)`. Stage both `SURFACE.md` and the snapshot-file deletion in the same commit.
  </action>
  <verify>
    <automated>! test -f .planning/phases/12-task-surface-redesign/12-surface-snapshot.txt && git log -1 --format=%s -- .planning/phases/12-task-surface-redesign/SURFACE.md | grep -qE 'SURF-01|surface'</automated>
  </verify>
  <acceptance_criteria>
    - Intermediate snapshot file deleted (the `! test -f ...` clause in the verify block returns 0)
    - SURFACE.md is committed with a message naming SURF-01 or surface
    - `git status` reports a clean working tree relative to phase 12 directory
    - Self-audit greps return zero "missing D-NN" / "empty callsites" hits
  </acceptance_criteria>
  <done>SURFACE.md committed; intermediate file removed; SURF-01 requirement met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Read-only documentation deliverable. No new code paths, no I/O, no external dependencies, no privilege escalation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Information disclosure | SURFACE.md content | accept | Public-by-convention documentation; lists task names already visible via `task --list` plus decision references already in `.planning/`. No secrets, no PII, no new attack surface. |
</threat_model>

<verification>
- `test -f .planning/phases/12-task-surface-redesign/SURFACE.md` returns 0
- `grep -c '^## ' SURFACE.md` returns 12, 13, or 14 (Summary + Top-Level + 10 namespaces + Decisions cross-ref; optionally Diagnostics section added by Task 2)
- `grep -cE '^\| \`[a-z][a-z0-9:-]+\`' SURFACE.md` returns >= 47
- `awk -F'|' 'NR>2 {print $3}' SURFACE.md | sort -u` contains only the locked enum values
- `! test -f .planning/phases/12-task-surface-redesign/12-surface-snapshot.txt` (intermediate cleaned up)
- `git log -1 --format=%s -- .planning/phases/12-task-surface-redesign/SURFACE.md` matches `SURF-01|surface`
</verification>

<success_criteria>
- SURF-01 requirement closed: every public task on today's `task --list` surface is classified with verdict + new name + internal-flag + rationale + callsites.
- The downstream plans (02-08) have a single committed worklist to iterate; no plan needs to re-derive verdicts at execution time.
- Phase 13 reviewers can read SURFACE.md as a post-phase audit trail of "what was renamed, what went internal, and why".
</success_criteria>

<output>
Create `.planning/phases/12-task-surface-redesign/12-01-SUMMARY.md` recording:
- Total rows classified (broken down by verdict)
- New-name decisions adopted from 12-PATTERNS.md
- Any deviations from the recommended naming (none expected; if any, cite D-NN)
- Open questions surfaced during classification (if any)
- Handoff note: Plans 02-08 iterate SURFACE.md row-by-row to apply renames
</output>
</content>
</invoke>
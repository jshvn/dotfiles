---
phase: 12-task-surface-redesign
plan: 06
type: execute
wave: 6
depends_on: [12-01, 12-03]
files_modified:
  - taskfiles/packages.yml
  - taskfiles/claude.yml
  - taskfiles/audit.yml
  - taskfiles/show.yml
  - taskfiles/refresh.yml
  - Taskfile.yml
autonomous: true
requirements: [SURF-02, SURF-03]
# W-9: docs/MANIFEST.md is NOT touched by this plan (the doc references survive `internal: true`).
# Plan 07 modifies docs/MANIFEST.md when it moves manifest:test to test:manifest.

must_haves:
  truths:
    - "`task --list` shows ZERO `packages:*` rows; the public path is `audit:packages` (delegate in taskfiles/audit.yml)"
    - "`task --list` shows ZERO `claude:*` rows; the public paths are `show:claude` and `refresh:claude` (delegates in taskfiles/show.yml and taskfiles/refresh.yml)"
    - "`task --list` shows `audit:packages`, `show:claude`, `refresh:claude` as public diagnostics"
    - "`task install` succeeds end-to-end (the install pipeline still calls `packages:install`, `claude:install`, `packages:verify` -- these are now internal but remain invocable)"
    - "`task lint` passes after every commit"
  artifacts:
    - path: "taskfiles/packages.yml"
      provides: "All tasks marked internal except via audit:packages delegate"
      exports: ["install (internal)", "compose (internal)", "verify (internal)", "validate (internal)", "audit (internal, exposed via audit:packages delegate)"]
    - path: "taskfiles/claude.yml"
      provides: "All tasks marked internal except via show:claude / refresh:claude delegates"
      exports: ["install (internal)", "validate (internal)", "status (internal, exposed via show:claude delegate)", "update (internal, exposed via refresh:claude delegate)"]
    - path: "taskfiles/audit.yml"
      provides: "Adds `packages:` task as public delegate for `:packages:audit`"
      exports: ["links (from Plan 03)", "packages (NEW)"]
    - path: "taskfiles/show.yml"
      provides: "NEW file -- public delegates for state-inspection diagnostics"
      exports: ["claude (delegate to :claude:status)"]
    - path: "taskfiles/refresh.yml"
      provides: "NEW file -- public delegates for explicit manual-refresh operations"
      exports: ["claude (delegate to :claude:update)"]
  key_links:
    - from: "taskfiles/audit.yml packages: task"
      to: "taskfiles/packages.yml audit: task"
      via: "task: :packages:audit (cross-namespace delegate)"
      pattern: "task: :packages:audit"
---

<objective>
Apply the packages-namespace and claude-namespace rename passes per D-01 + D-02 + D-03:

**packages namespace:**
- `packages:install` -> (keep name) + `internal: true` (D-01)
- `packages:compose` -> (keep name) + `internal: true` (D-01)
- `packages:verify` -> (keep name) + `internal: true` (D-01)
- `packages:validate` -> (keep name) + `internal: true` (D-01)
- `packages:audit` -> (keep name in source file) + `internal: true`; expose as PUBLIC `audit:packages` via a new delegate task in `taskfiles/audit.yml` (D-02 -- drift checkers move to audit:)

**claude namespace:**
- `claude:install` -> (keep name) + `internal: true` (D-01)
- `claude:validate` -> (keep name) + `internal: true` (D-01)
- `claude:status` -> (keep name in source file) + `internal: true`; expose as PUBLIC `show:claude` via a new delegate (D-02 -- state-printers under show:)
- `claude:update` -> (keep name in source file) + `internal: true`; expose as PUBLIC `refresh:claude` via a new delegate (D-03 -- refresh: namespace for explicit-refresh)
- The internal sub-tasks (`marketplace`, `gsd`, `ensure-cli`) are already `internal: true` and untouched.

**New delegates** require new include files or extensions of `taskfiles/audit.yml`:
- `audit:packages` -> extend `taskfiles/audit.yml` (created in Plan 03) with a `packages:` task delegating to `:packages:audit`.
- `show:claude` -> create new `taskfiles/show.yml` with a `claude:` task delegating to `:claude:status`. Mandatory W-3 banner.
- `refresh:claude` -> create new `taskfiles/refresh.yml` with a `claude:` task delegating to `:claude:update`. Mandatory W-3 banner.

Both new files must be added to `Taskfile.yml`'s includes block in alphabetical order.

Per W-9: this plan does NOT modify `docs/MANIFEST.md`. The doc references in MANIFEST.md (e.g., `task packages:verify`, `task packages:install`) survive `internal: true` -- internal-task names are valid operator-callable names. Plan 07 modifies `docs/MANIFEST.md` when it handles the `manifest:test` -> `test:manifest` rename.

Purpose: Establish the public diagnostic namespaces (`show:`, `audit:`, `refresh:`) via include-aliases and per-namespace delegate files. The pattern matches Plan 03's `audit:links` -- internal implementation in the owning taskfile, public name via a thin delegate.

Output: Two new include files (`taskfiles/show.yml`, `taskfiles/refresh.yml`); one extended file (`taskfiles/audit.yml` gains `packages:`); modifications to `taskfiles/packages.yml` and `taskfiles/claude.yml` to mark every task internal; `Taskfile.yml` includes block adds two new lines.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-task-surface-redesign/12-CONTEXT.md
@.planning/phases/12-task-surface-redesign/12-PATTERNS.md
@.planning/phases/12-task-surface-redesign/SURFACE.md

<interfaces>
<!-- packages rename + mark-internal table: -->

| File | Task | Verdict | New (full) Name | Internal? |
|---|---|---|---|---|
| taskfiles/packages.yml | install | mark-internal | (keep `packages:install`) | yes |
| taskfiles/packages.yml | compose | mark-internal | (keep `packages:compose`) | yes |
| taskfiles/packages.yml | verify | mark-internal | (keep `packages:verify`) | yes |
| taskfiles/packages.yml | audit | rename (move to audit:) | `audit:packages` via delegate | yes (internal implementation) |
| taskfiles/packages.yml | validate | mark-internal | (keep `packages:validate`) | yes |

<!-- claude rename + mark-internal table: -->

| File | Task | Verdict | New (full) Name | Internal? |
|---|---|---|---|---|
| taskfiles/claude.yml | install | mark-internal | (keep `claude:install`) | yes |
| taskfiles/claude.yml | status | rename (move to show:) | `show:claude` via delegate | yes (internal impl) |
| taskfiles/claude.yml | update | rename (move to refresh:) | `refresh:claude` via delegate | yes (internal impl) |
| taskfiles/claude.yml | validate | mark-internal | (keep `claude:validate`) | yes |

<!-- Existing taskfiles/audit.yml (from Plan 03) -- extended with `packages:` task. Mandatory W-3 banner sections already present from Plan 03; append a Tasks list entry for packages. -->

<!-- New taskfiles/show.yml shape (NEW FILE; W-3 mandatory banner): -->

version: '3'

# =============================================================================
# taskfiles/show.yml -- Phase 12 public diagnostics: state inspection.
#
# Purpose: Public delegates for diagnostic show operations across all namespaces.
# Callers: Invoked directly by operator; included by Taskfile.yml.
# Side effects: Read-only -- these tasks print state or detect drift; no mutations.
# Tasks:
#   - claude -- delegate to :claude:status (marketplaces + plugins listing; D-02)
# =============================================================================

vars:
  DOTFILEDIR:
    sh: dirname "{{.TASKFILE_DIR}}"

tasks:
  # lint-allow: cmds-without-status
  claude:
    desc: "Show installed marketplaces + plugins (diagnostic)"
    status: [false]
    cmds:
      - task: :claude:status

<!-- New taskfiles/refresh.yml shape (NEW FILE; W-3 mandatory banner): -->

version: '3'

# =============================================================================
# taskfiles/refresh.yml -- Phase 12 public refresh namespace (D-03).
#
# Purpose: Public delegates for explicit manual-refresh operations across all namespaces.
# Callers: Invoked directly by operator; included by Taskfile.yml.
# Side effects: Read-only -- these tasks print state or detect drift; no mutations.
#   (NOTE: the underlying refresh implementations DO mutate state; the delegate itself
#   is the read-only thin pass-through.)
# Tasks:
#   - claude -- delegate to :claude:update (marketplaces + plugins + GSD refresh; D-03)
# =============================================================================

vars:
  DOTFILEDIR:
    sh: dirname "{{.TASKFILE_DIR}}"

tasks:
  # lint-allow: cmds-without-status
  claude:
    desc: "Refresh marketplaces + plugins + GSD (explicit; not in task install)"
    status: [false]
    cmds:
      - task: :claude:update

<!-- Taskfile.yml includes block additions (alphabetical insertion): -->
includes:
  audit:    ./taskfiles/audit.yml      # was added by Plan 03
  claude:   ./taskfiles/claude.yml     # existing
  identity: { ... }                     # existing
  lint:     ./taskfiles/lint.yml       # existing
  links:    ./taskfiles/links.yml      # existing
  macos:    ./taskfiles/macos.yml      # existing
  manifest: { ... }                     # existing
  packages: { ... }                     # existing
  refresh:  ./taskfiles/refresh.yml    # NEW (alphabetical: between packages and shell)
  shell:    ./taskfiles/shell.yml      # existing
  show:     ./taskfiles/show.yml       # NEW (alphabetical: between shell and test)
  test:     { ... }                     # existing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark every packages task internal + extend taskfiles/audit.yml with `packages:` delegate</name>
  <read_first>
    - taskfiles/packages.yml (full file; tasks `install:`, `compose:`, `verify:`, `audit:`, `validate:`)
    - taskfiles/audit.yml (created by Plan 03; contains the `links:` task with the file-header banner)
    - Taskfile.yml install body (calls `packages:install` and `packages:verify`)
    - .planning/phases/12-task-surface-redesign/12-PATTERNS.md Pattern 2 (`internal: true` placement)
    - .planning/phases/12-task-surface-redesign/SURFACE.md (packages: section)
  </read_first>
  <action>
    Apply edits in ONE commit. Each edit uses content-based locators (per B-4: find by literal task-key match, not by absolute line number).

    **Edit `taskfiles/packages.yml`:**

    1. **`install:`** Find the task key matching the literal `  install:` (the task with `desc:` referencing brew bundle install). Add `    internal: true` immediately after `desc:`, before `deps:`.

    2. **`compose:`** Find the task key matching the literal `  compose:` (the task with `desc:` referencing composing brew bundles from machine TOML). Add `    internal: true` immediately after `desc:`, before `deps:`.

    3. **`verify:`** Find the task key matching the literal `  verify:` (the task with `desc:` referencing post-install drift verification). Add `    internal: true` immediately after `desc:`, before `silent:`. The `# lint-allow: cmds-without-status` marker on the preceding line stays.

    4. **`audit:`** Find the task key matching the literal `  audit:` (the task with `desc:` referencing brew-formulae/casks/mas audit). Add `    internal: true` immediately after `desc:`, before `deps:`. The `# lint-allow: cmds-without-status` marker on the preceding line stays.

    5. **`validate:`** Find the task key matching the literal `  validate:` (the task with `desc:` referencing packages validation). Add `    internal: true` immediately after `desc:`, before `cmds:`. The `# lint-allow: cmds-without-status` marker on the preceding line stays.

    **Edit `taskfiles/audit.yml`** (extend with the `packages:` delegate task):

    6. After the existing `links:` task block, append a new `packages:` task (same 4-space-indent shape under `tasks:`):
       - `# lint-allow: cmds-without-status` marker line
       - Task key `packages:`
       - `desc: "Audit brew formulae/casks/mas vs declared (D-11). Non-blocking; --strict exits non-zero."`
       - `status: [false]`
       - `cmds:` block with one entry: `- task: :packages:audit` and `vars: { CLI_ARGS: '{{.CLI_ARGS}}' }`.
    7. Update the file-header banner's Tasks list (from Plan 03) by appending a new comment line: `#   - packages -- delegate to :packages:audit (brew/cask/mas drift detection; D-02)`.

    Commit message: `refactor(12): mark packages tasks internal + add audit:packages delegate (D-01, D-02)`.

    Verify after commit: `task --list 2>&1 | grep -cE '^* packages:'` returns 0. `task --list 2>&1 | grep -cE '^* audit:packages'` returns 1. `task --list-all` exits 0 (graph-parse gate: confirms `packages:install` and `packages:verify` still resolve in the compiled task graph). `task lint:taskfile` passes.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ "$(task --list 2>&1 | grep -cE '^\* packages:')" -eq 0 ] && [ "$(task --list 2>&1 | grep -cE '^\* audit:packages')" -eq 1 ] && [ "$(task --list-all 2>&1 | grep -cE 'packages:(install|compose|verify|audit|validate)')" -ge 5 ] && grep -qE '^\s+packages:\s*$' taskfiles/audit.yml && task --list-all >/dev/null 2>&1; rc_graph=$?; task lint:taskfile 2>&1 | tail -3 | grep -qE '✓|LINT-0[1-7]' && [ "$rc_graph" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `task --list` shows ZERO `packages:*` rows
    - `task --list` shows `audit:packages` exactly once (alongside `audit:links` from Plan 03)
    - `task --list-all` shows all 5 internal packages tasks (install, compose, verify, audit, validate)
    - `taskfiles/audit.yml` contains both `links:` and `packages:` task entries
    - `taskfiles/audit.yml` Tasks list in the header banner includes the new `packages` entry
    - `task --list-all` exits 0 (every renamed callsite resolves -- graph-parse gate)
    - `task lint:taskfile` exits 0
  </acceptance_criteria>
  <done>packages namespace fully internalized; `audit:packages` is the public drift-detection surface.</done>
</task>

<task type="auto">
  <name>Task 2: Mark every claude task internal + create taskfiles/show.yml and taskfiles/refresh.yml with claude delegates</name>
  <read_first>
    - taskfiles/claude.yml (full file; tasks `install:`, `update:`, `status:`, `validate:`)
    - taskfiles/audit.yml (template for new delegate-only taskfiles + W-3 header banner)
    - Taskfile.yml includes block (where the new `show:` and `refresh:` lines go)
    - .planning/phases/12-task-surface-redesign/12-PATTERNS.md Pattern 2 + Pattern 5 (file-header banner convention)
    - .planning/phases/12-task-surface-redesign/SURFACE.md (claude: section + Diagnostics show:/audit:/refresh: section)
  </read_first>
  <action>
    Apply edits in ONE commit. Each edit uses content-based locators (per B-4).

    **Edit `taskfiles/claude.yml`:**

    1. **`install:`** Find the task key matching the literal `  install:` (the task with `desc:` referencing claude install / marketplace + GSD). Add `    internal: true` immediately after `desc:`, before `deps:`.

    2. **`update:`** Find the task key matching the literal `  update:` (the task with `desc:` referencing refreshing marketplaces and plugins). Add `    internal: true` immediately after `desc:`, before `deps:`. The `# lint-allow: cmds-without-status` marker on the preceding line stays.

    3. **`status:`** Find the task key matching the literal `  status:` (the task with `desc:` referencing showing installed marketplaces + plugins). Add `    internal: true` immediately after `desc:`, before `status: [false]`. The `# lint-allow: cmds-without-status` marker on the preceding line stays.

    4. **`validate:`** Find the task key matching the literal `  validate:` (the task with `desc:` referencing claude config validation). Add `    internal: true` immediately after `desc:`, before `deps:`. The `# lint-allow: cmds-without-status` marker on the preceding line stays.

    Internal tasks `marketplace`, `gsd`, `ensure-cli` already have `internal: true` -- no changes.

    **Create `taskfiles/show.yml`** (W-3 mandatory banner):

    5. Write the new file per the `<interfaces>` shape above. The file-header banner block MUST contain the four mandatory sections:
       - Purpose: "Public delegates for diagnostic show operations across all namespaces."
       - Callers: "Invoked directly by operator; included by Taskfile.yml."
       - Side effects: "Read-only -- these tasks print state or detect drift; no mutations."
       - Tasks list: `#   - claude -- delegate to :claude:status (marketplaces + plugins listing; D-02)`

       The file contains `version: '3'`, the header banner, a self-contained `vars:` block with `DOTFILEDIR` resolved via `sh: dirname "{{.TASKFILE_DIR}}"`, and a `tasks:` block with one task `claude:` matching the `<interfaces>` shape (lint-allow marker, desc quoted, `status: [false]`, `cmds: - task: :claude:status`).

    **Create `taskfiles/refresh.yml`** (W-3 mandatory banner):

    6. Write the new file per the `<interfaces>` shape. The file-header banner block MUST contain the four mandatory sections:
       - Purpose: "Public delegates for explicit manual-refresh operations across all namespaces."
       - Callers: "Invoked directly by operator; included by Taskfile.yml."
       - Side effects: "Read-only -- these tasks print state or detect drift; no mutations." (with the note that the underlying refresh implementations DO mutate; the delegate is the thin pass-through).
       - Tasks list: `#   - claude -- delegate to :claude:update (marketplaces + plugins + GSD refresh; D-03)`

       The file contains `version: '3'`, header banner, `vars:` block, `tasks:` block with one task `claude:` whose cmds: invoke `:claude:update`.

    **Edit `Taskfile.yml` includes block:**

    7. Find the includes block (the `includes:` key with sub-entries like `claude:`, `identity:`, etc.). Insert a new line `  refresh:  ./taskfiles/refresh.yml` in alphabetical order between the entries for `packages:` and `shell:`. Insert a new line `  show:     ./taskfiles/show.yml` in alphabetical order between `shell:` and `test:`.

    8. Update the `Includes:` header comment block at the top of `Taskfile.yml` -- add `#   - refresh  (P12, real)` and `#   - show     (P12, real)` lines in the appropriate alphabetical positions within that comment block.

    Commit message: `refactor(12): mark claude tasks internal + add show:claude / refresh:claude delegates (D-01, D-02, D-03)`.

    Verify after commit: `task --list 2>&1 | grep -cE '^* claude:'` returns 0. `task --list 2>&1 | grep -cE '^* show:claude'` returns 1. `task --list 2>&1 | grep -cE '^* refresh:claude'` returns 1. `task show:claude` resolves to the delegate (invocable). `task --list-all` exits 0 (graph-parse gate). `task lint` exits 0.
  </action>
  <verify>
    <automated>cd /Users/josh/Git/personal/dotfiles && [ "$(task --list 2>&1 | grep -cE '^\* claude:')" -eq 0 ] && [ "$(task --list 2>&1 | grep -cE '^\* show:claude')" -eq 1 ] && [ "$(task --list 2>&1 | grep -cE '^\* refresh:claude')" -eq 1 ] && test -f taskfiles/show.yml && test -f taskfiles/refresh.yml && grep -qE '^\s*show:\s+\./taskfiles/show\.yml' Taskfile.yml && grep -qE '^\s*refresh:\s+\./taskfiles/refresh\.yml' Taskfile.yml && grep -qE '^# Purpose:' taskfiles/show.yml && grep -qE '^# Purpose:' taskfiles/refresh.yml && task --list-all >/dev/null 2>&1; rc_graph=$?; task lint 2>&1 | tail -3 | grep -qE '✓' && [ "$rc_graph" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `task --list` shows ZERO `claude:*` rows
    - `task --list` shows `show:claude` and `refresh:claude` each exactly once
    - `task --list-all` shows all 4 internal claude tasks (install, status, update, validate) + 3 already-internal sub-tasks (marketplace, gsd, ensure-cli)
    - Files `taskfiles/show.yml` and `taskfiles/refresh.yml` exist with the prescribed shape AND the mandatory W-3 header banner sections (Purpose / Callers / Side effects / Tasks list)
    - `Taskfile.yml` includes block contains the two new lines in alphabetical order
    - `Taskfile.yml` `Includes:` header comment block lists `refresh` and `show` in alphabetical order
    - `task --list-all` exits 0 (every renamed callsite resolves -- graph-parse gate)
    - `task lint` exits 0 at end of plan (per W-6 full lint runs at the last task)
  </acceptance_criteria>
  <done>claude namespace fully internalized; `show:claude` and `refresh:claude` are the public surfaces; new include files in place with mandatory banners.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure rename + delegate-file additions. The new `show:` and `refresh:` namespaces are thin pass-throughs to existing implementations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06-01 | Information disclosure | `internal: true` on packages + claude tasks (9 total) | accept | Markers hide tasks; tasks remain invocable. Not authz. |
| T-12-06-02 | Tampering | Cross-namespace delegate calls (`:claude:status`, `:claude:update`, `:packages:audit`) | accept | Absolute-form leading colon is the standard go-task pattern, verified in `taskfiles/identity.yml` and `taskfiles/links.yml`. No new attack surface. |
</threat_model>

<verification>
- `task --list` reflects: zero packages:* rows; zero claude:* rows; new `audit:packages`, `show:claude`, `refresh:claude` rows
- `task --list-all` shows internal-but-invocable: packages:install, packages:compose, packages:verify, packages:audit, packages:validate, claude:install, claude:status, claude:update, claude:validate
- Files exist: `taskfiles/show.yml`, `taskfiles/refresh.yml`, updated `taskfiles/audit.yml`, updated `Taskfile.yml`
- Both new files have mandatory W-3 header banner sections (Purpose / Callers / Side effects / Tasks list)
- `task --list-all` exits 0 (graph-parse gate); `task lint` green at end of plan
</verification>

<success_criteria>
- SURF-02 satisfied for the packages and claude namespaces: renames via delegate pattern; doc references in `docs/MANIFEST.md` survive (paths still resolve as internal tasks; W-9 -- this plan does not modify MANIFEST.md).
- SURF-03 satisfied for `packages:install`, `packages:compose`, `packages:verify`, `packages:audit`, `packages:validate`, `claude:install`, `claude:status`, `claude:update`, `claude:validate` (all marked internal).
- D-02 (drift checkers under audit:) and D-03 (refresh: namespace) both honored.
- W-3 mandatory banners present in the two new taskfiles.
- Plan 07 (manifest + test + lint internalization) inherits a clean state.
</success_criteria>

<output>
Create `.planning/phases/12-task-surface-redesign/12-06-SUMMARY.md` recording:
- Two commit SHAs (packages internalize + audit:packages delegate; claude internalize + show/refresh delegates)
- Final `task --list` capture showing the new diagnostic namespaces
- Confirmation that `task --list-all` exits 0 (graph-parse gate)
- Handoff to Plan 07 (manifest + test + lint): Plan 07 modifies `taskfiles/manifest.yml`, `taskfiles/test.yml`, `taskfiles/lint.yml`, also extends `taskfiles/show.yml` (created here) with `show:manifest`, and modifies `docs/MANIFEST.md` and `.claude/CLAUDE.md`
</output>
</content>
</invoke>
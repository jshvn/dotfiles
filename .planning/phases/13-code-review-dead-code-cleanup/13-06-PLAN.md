---
phase: 13-code-review-dead-code-cleanup
plan: 06
type: execute
wave: 6
depends_on: ["13-05"]
files_modified:
  - .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md
  - .planning/ROADMAP.md            # only modified if any defer row uses D-11(b) "needs-new-infra" and requires a backlog entry
  # PLUS: every source file touched by a "fix now" MEDIUM/LOW remediation.
  # Concrete set determined at execute-time from REVIEW.md's remaining rows.
autonomous: true
requirements: [REVW-02, REVW-06]
threat_model: required

must_haves:
  truths:
    - "Every row in 13-REVIEW.md (regardless of severity) has a non-empty `closed by` column after this plan: either a short-SHA (fix-now) or `defer: <reason>` (defer-with-rationale)."
    - "Every MEDIUM and LOW row carries a verdict: 'fix now' (short-SHA) or 'defer with rationale' (defer: Phase 14 TRIM-NN — <why> per D-11(a), or defer: needs-new-infra — <rationale> per D-11(b))."
    - "Every defer-with-rationale row in REVIEW.md cites one of exactly two defer reasons (D-11(a) Phase 14 TRIM-NN or D-11(b) needs-new-infra). No other defer reasons exist."
    - "For every D-11(b) defer row, ROADMAP.md gains a corresponding backlog entry under the v2.x backlog (or PROJECT.md Active requirements if cross-phase) — the defer is not silent."
    - "After every commit in this plan, `task lint && task test` exit 0 (green-tree gate)."
    - "task --dry install exits 0 after every commit touching install-chain files."
    - "Final REVIEW.md state: every row has either a short-SHA in `closed by` or `defer: <reason>` — zero blank verdicts; grep -cE '^\\|.*\\|[[:space:]]*\\|[[:space:]]*$' over the table body returns 0."
  artifacts:
    - path: ".planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md"
      provides: "Final state: every row carries a verdict (short-SHA or defer:)"
      contains: "| MEDIUM |"
      also_contains: "| LOW |"
    - path: ".planning/ROADMAP.md"
      provides: "Backlog entries for every D-11(b) needs-new-infra defer (only if any such defers exist)"
  key_links:
    - from: "defer rows"
      to: "Phase 14 TRIM-NN (D-11(a)) or v2.x backlog (D-11(b))"
      via: "REVIEW.md row's `closed by` column cites the defer target"
      pattern: "defer:"
---

<objective>
Process every remaining MEDIUM and LOW row in `13-REVIEW.md` after Plans 13-02..13-05 close. For each:

- If "fix now" verdict: apply the remediation, annotate the row's `closed by` column with the short-SHA of the fix commit.
- If "defer with rationale" verdict per D-11(a): annotate with `defer: Phase 14 TRIM-NN — <reason>` (cite the most-specific TRIM key).
- If "defer with rationale" verdict per D-11(b): annotate with `defer: needs-new-infra — <one-line rationale>` AND append a corresponding backlog entry to `ROADMAP.md` (v2.x backlog section).

Purpose: REVW-02 (MEDIUM/LOW triaged into fix-now / defer-with-rationale) + REVW-06 (green tree after every fix). Final REVIEW.md state has zero blank `closed by` columns.

Output: MEDIUM/LOW rows resolved; ROADMAP backlog updated if D-11(b) defers exist; phase ready for verify.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-code-review-dead-code-cleanup/13-CONTEXT.md
@.planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md
@.planning/phases/13-code-review-dead-code-cleanup/13-01-SUMMARY.md
@.planning/phases/13-code-review-dead-code-cleanup/13-02-SUMMARY.md
@.planning/phases/13-code-review-dead-code-cleanup/13-03-SUMMARY.md
@.planning/phases/13-code-review-dead-code-cleanup/13-04-SUMMARY.md
@.planning/phases/13-code-review-dead-code-cleanup/13-05-SUMMARY.md
@CLAUDE.md
@.claude/CLAUDE.md
@taskfiles/lint.yml
</context>

<read_first>
Before any MEDIUM/LOW triage:
1. `.planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md` — read top-to-bottom; identify every remaining row with empty `closed by` column. After Plans 13-02..13-05, the unclosed rows should be exclusively MEDIUM/LOW (HIGH/dead-code/duplication closed in earlier plans). Sanity check: any remaining unclosed HIGH/dead-code/duplication row means a prior plan failed to close it — that is a verification gap to surface to the user before proceeding.
2. `.planning/phases/13-code-review-dead-code-cleanup/13-CONTEXT.md` §"D-11 (MEDIUM/LOW defer policy)" — the two explicit defer reasons.
3. `.planning/REQUIREMENTS.md` §"Trim (comment/doc trim)" — TRIM-01..05 keys for D-11(a) defer targets.
4. `.planning/phases/13-code-review-dead-code-cleanup/13-CONTEXT.md` §"Severity-threshold definitions" — what MEDIUM and LOW mean (portability nits, idempotency-without-correctness, status-block misuse not caught by LINT, duplication candidates per D-09; LOW = clarity/naming/docstring inaccuracy/dead-comment).
5. `.planning/ROADMAP.md` — locate the v2.x backlog section (search for "Future Requirements" or "v2.x" or "Backlog"; if none exists, the backlog entries go to `REQUIREMENTS.md` §"Future Requirements" or `PROJECT.md` Active — planner picks).
6. **For each fix-now row, before fixing**: read the source file the row references (in full for <800 lines, ±100 line window otherwise).
</read_first>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MEDIUM/LOW fixes → repo state | Same surface as Plan 13-02; lower severity but same regression risk profile. |
| defer rationale → future-phase accountability | A `defer: Phase 14 TRIM-NN` without a corresponding TRIM-NN key leaves the defer unactionable. |
| `defer: needs-new-infra` without backlog entry → silent gap | A defer without a follow-up record is functionally a deletion of the finding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06-01 | Tampering | MEDIUM/LOW fix introduces regression | mitigate | Same green-tree gate as Plan 13-02: `task lint && task test` exit 0 after every commit; `task --dry install` after install-chain edits. |
| T-13-06-02 | Repudiation | defer row without explicit reason | mitigate | The verify automated check matches `defer:` prefix; any row marked `defer` without a reason fails the check (the defer cite is in `closed by` column). |
| T-13-06-03 | Information Disclosure | a "fix now" surface accidentally exposes secret in source | accept | Hooks block secrets-in-code at commit time (per CLAUDE.md "Hooks block: secrets in code"). The pre-commit secret-scan hook is the structural gate; this plan cannot bypass it. |
| T-13-06-04 | Tampering | defer Phase 14 TRIM-NN cites a non-existent TRIM key | mitigate | TRIM-01..TRIM-05 are the only valid D-11(a) cites; the verify check greps for unknown TRIM-NN forms. If a defer cites TRIM-06+ (which does not exist), the row is rejected and re-classified. |
| T-13-06-05 | Repudiation | D-11(b) defer without ROADMAP backlog entry | mitigate | The verify check counts D-11(b) defer rows in REVIEW.md AND ROADMAP backlog additions; counts must match. If a D-11(b) defer lands without a corresponding ROADMAP entry, the plan does not close. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Remaining-row inventory + fix-now/defer triage</name>
  <files>(read-only; planning step)</files>
  <read_first>
    - .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md — every row with empty `closed by` column.
  </read_first>
  <action>
    Read `13-REVIEW.md`. Identify every row where the `closed by` column is empty. Verify these are all MEDIUM or LOW severity (HIGH should be closed by Plan 13-02; dead-code by Plan 13-03; duplication by Plan 13-04; the REVW-05 row by Plan 13-05).

    **Sanity check failure mode:** if any HIGH, dead-code, or duplication row is still unclosed at this point, STOP. Surface to the user: "Unclosed <severity>/<category> row(s) in REVIEW.md: <row line numbers>. Plan 13-NN was supposed to close this. Investigate before proceeding." Do not silently re-classify HIGH as MEDIUM in this plan; that bypasses REVW-02's "HIGH fixed in this phase" requirement.

    Assuming the unclosed set is exclusively MEDIUM/LOW, triage each row:

    1. **fix-now candidates:** rows where the remediation is concrete, low-risk, and within Phase 13 scope (a portability nit, a status-block misuse not caught by LINT, a small clarity refactor, a one-line correctness improvement).

    2. **defer per D-11(a):** rows about comment density, docstring inaccuracy, README/CLAUDE.md duplication, file-header banner drift, `desc:` string trimming, or any other Phase 14 TRIM-01..TRIM-05 concern. Cite the most-specific TRIM key:
       - Inline-comment density → `defer: Phase 14 TRIM-01 — <why>`
       - File-header banner drift → `defer: Phase 14 TRIM-02 — <why>`
       - Obsolete docs in `docs/` → `defer: Phase 14 TRIM-03 — <why>`
       - README/CLAUDE.md/`.claude/CLAUDE.md` dedup → `defer: Phase 14 TRIM-04 — <why>`
       - "v1 bug class" or "Gap N" historical reference in code comments → `defer: Phase 14 TRIM-05 — <why>`

    3. **defer per D-11(b):** rows requiring new infrastructure to address (a new lint rule, a new task, structured logging, a new linter category). Format: `defer: needs-new-infra — <one-line rationale>`. Each D-11(b) defer also requires a ROADMAP.md backlog entry (Task 3).

    Build the triage manifest (printed to stdout; not committed):
    ```
    Row <L>: severity=<MEDIUM|LOW> category=<...> finding=<...>
      Verdict: FIX-NOW | DEFER-PHASE-14-TRIM-<NN> | DEFER-NEEDS-NEW-INFRA
      Action (fix-now): <one-line remediation>
      Action (defer): <rationale>
    ```

    The triage manifest is the input for Tasks 2 and 3.
  </action>
  <verify>
    <automated>
      # Sanity check: no HIGH/dead-code/duplication rows remain unclosed.
      bad_unclosed=$(grep -cE '^\|.*\| (HIGH|dead-code|duplication) \|.*\|[[:space:]]*\|[[:space:]]*$' .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md || true)
      test "${bad_unclosed:-0}" -eq 0
      # Count remaining (should be all MEDIUM or LOW).
      remaining=$(grep -cE '^\|.*\|[[:space:]]*\|[[:space:]]*$' .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md || true)
      echo "$remaining" > /tmp/13-06-remaining-count.txt
    </automated>
  </verify>
  <done>
    Triage manifest printed. Every remaining row classified as FIX-NOW / DEFER-PHASE-14-TRIM-NN / DEFER-NEEDS-NEW-INFRA. No HIGH/dead-code/duplication row remains unclosed (sanity check passed).
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply fix-now MEDIUM/LOW remediations; annotate REVIEW.md</name>
  <files>
    .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md
    (PLUS source files touched by fix-now remediations; specific set from Task 1)
  </files>
  <read_first>
    - The file being modified for each fix-now batch (in full for <800 lines).
    - CLAUDE.md (project root) §"Rules" — invariants every fix continues to satisfy.
  </read_first>
  <action>
    For each FIX-NOW row from Task 1's manifest:

    1. Read the source file referenced.
    2. Apply the remediation (one-line action per the REVIEW.md `remediation` column).
    3. Run `task lint && task test`. Must exit 0.
    4. If the touched file is in the `task install` cmd chain, run `task --dry install`. Must exit 0.
    5. Batch related fix-now remediations into one commit (per-file or per-bug-class):
       ```
       fix(13-06): <one-line summary> — closes REVIEW.md row(s) <L1>[, <L2>, ...]
       ```
    6. Capture short-SHA; annotate every closed row's `closed by` column with `| <short-SHA> |`.
    7. Commit annotation:
       ```
       docs(13-06): annotate REVIEW.md row(s) <L1>[, ...] closed by <short-SHA>
       ```

    **Anti-scope-creep**: same rule as Plan 13-02. Only touch what the row demands; do NOT also fix nearby code, refactor while-you-are-there, or add new lint rules. New scope is its own row (which would be a defer per D-11(b) in this very plan, not a fix).
  </action>
  <verify>
    <automated>
      task lint && task test
    </automated>
  </verify>
  <done>
    Every FIX-NOW row's `closed by` column carries a short-SHA. `task lint && task test` exit 0 after every commit. `task --dry install` exits 0 after every install-chain-touching commit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Annotate defer rows; append ROADMAP.md backlog for D-11(b) defers</name>
  <files>
    .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md
    .planning/ROADMAP.md     # only if at least one D-11(b) DEFER-NEEDS-NEW-INFRA row exists
  </files>
  <read_first>
    - .planning/ROADMAP.md — locate the v2.x backlog section (or the closest analog: "Future Requirements" section in `.planning/REQUIREMENTS.md`; or PROJECT.md Active requirements). Planner picks the canonical backlog home; once chosen, all D-11(b) defer entries go to the same location.
    - .planning/REQUIREMENTS.md §"Future Requirements" — existing format for deferred items (matches the shape new D-11(b) entries should adopt).
  </read_first>
  <action>
    **Step A: Annotate every defer row in REVIEW.md.**

    For each DEFER-PHASE-14-TRIM-NN row from Task 1's manifest:
    - Update REVIEW.md row's `closed by` column with: `defer: Phase 14 TRIM-<NN> — <one-line reason>`.
    - Example: `defer: Phase 14 TRIM-01 — inline comment in Taskfile.yml restates the code; trim per WHY-only rule`.

    For each DEFER-NEEDS-NEW-INFRA row:
    - Update REVIEW.md row's `closed by` column with: `defer: needs-new-infra — <one-line rationale>`.
    - Example: `defer: needs-new-infra — would require a new lint rule for X; one-shot grep suffices for v2.1`.

    Commit:
    ```
    docs(13-06): annotate defer rows in REVIEW.md (D-11(a) Phase 14, D-11(b) needs-new-infra)
    ```

    **Step B: Append ROADMAP.md backlog entries for every D-11(b) defer.**

    If at least one D-11(b) DEFER-NEEDS-NEW-INFRA row exists:

    1. Locate the v2.x backlog section in `ROADMAP.md`. Search for `## Future Requirements`, `## v2.x Backlog`, `## Backlog`, or similar. If none exists, planner creates a new section at the bottom of ROADMAP.md titled `## v2.x Backlog` with a brief intro:
       ```markdown
       ## v2.x Backlog

       Deferred items surfaced during Phase 13 code review (REVW-02) that require
       new infrastructure beyond v2.1 scope per D-11(b). Each item carries a
       one-line rationale; specific implementation is deferred to a future
       milestone where the infrastructure is in scope.

       ```
    2. For each D-11(b) defer row, append a bullet:
       ```
       - **<short-name>** (REVW-02 / 13-REVIEW.md row <L>): <one-line description>. Defer rationale: <rationale from REVIEW.md>. Needs: <new infra description>.
       ```
       Example:
       ```
       - **structured-logging-in-messages.zsh** (REVW-02 / 13-REVIEW.md row 47): install/messages.zsh uses echo-based output; structured JSON mode would aid CI log consumption. Defer rationale: requires new output-mode flag + downstream consumer; one-off for v2.1. Needs: argument parsing + JSON serialization in messages.zsh.
       ```
    3. Commit:
       ```
       docs(13-06): append v2.x backlog entries for D-11(b) defers (REVW-02 / D-11(b))
       ```

    If ZERO D-11(b) defers exist, ROADMAP.md is NOT modified in this task. The annotation commit from Step A is the only commit in Task 3.

    **Step C: Final REVIEW.md zero-blank verification.**

    After all annotations land, verify the final REVIEW.md state has no blank `closed by` columns:
    ```
    grep -cE '^\|.*\|[[:space:]]*\|[[:space:]]*$' 13-REVIEW.md
    ```
    Must return 0. If non-zero, list the offending rows and resolve (re-triage to FIX-NOW or defer) before declaring the plan complete.
  </action>
  <verify>
    <automated>
      # Zero blank `closed by` columns in the REVIEW.md table.
      blank_count=$(grep -cE '^\|.*\|[[:space:]]*\|[[:space:]]*$' .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md || true)
      test "${blank_count:-0}" -eq 0
      # If any defer references Phase 14 TRIM, the TRIM key is in TRIM-01..TRIM-05 (no TRIM-06+ exists).
      bad_trim=$(grep -oE 'defer: Phase 14 TRIM-[0-9]+' .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md | grep -vE 'TRIM-0[1-5]' || true)
      test -z "$bad_trim"
      # D-11(b) defer count in REVIEW.md must equal new bullet count under v2.x Backlog in ROADMAP.md.
      d11b_count=$(grep -c 'defer: needs-new-infra' .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md || true)
      if [ "${d11b_count:-0}" -gt 0 ]; then
        backlog_count=$(awk '/^## (v2\.x Backlog|Future Requirements)/,/^## /' .planning/ROADMAP.md | grep -c '13-REVIEW.md row' || true)
        test "${backlog_count:-0}" -ge "${d11b_count:-0}"
      fi
      task lint && task test
    </automated>
  </verify>
  <done>
    Every row in 13-REVIEW.md has a non-empty `closed by` column. Every D-11(a) defer cites a valid TRIM-01..TRIM-05 key. Every D-11(b) defer has a corresponding ROADMAP.md backlog bullet. `task lint && task test` exit 0.
  </done>
</task>

</tasks>

<verification>
After every commit in this plan: `task lint && task test` both exit 0.

REVW-02 closure check (this plan's portion):
- Every MEDIUM and LOW row in `13-REVIEW.md` carries `closed by` = short-SHA OR `defer: Phase 14 TRIM-<NN>` OR `defer: needs-new-infra — <rationale>`.
- `grep -cE '^\|.*\|[[:space:]]*\|[[:space:]]*$' 13-REVIEW.md` returns 0.
- Every `defer: Phase 14 TRIM-NN` cites TRIM-01..TRIM-05 (no invalid TRIM keys).
- Every `defer: needs-new-infra` has a corresponding ROADMAP.md backlog bullet (count match).

REVW-06 closure check (this plan's portion):
- `task test` exits 0 after every commit.

Cross-cutting must_haves:
- After every commit, `task lint && task test` exit 0.
- Every commit uses logical-batch shape: `<type>(13-06): <one-line>`.
- No AI attribution; no emojis; `set -euo pipefail` invariant intact.
- `task --dry install` exits 0 after every install-chain-touching commit.

**Final phase-level gate (verified by `/gsd:verify-phase` after this plan):**
- All five ROADMAP SC#1..SC#5 must be TRUE.
- Phase 13 SUMMARYs (13-01 through 13-06) all written.
- No unclosed rows in 13-REVIEW.md.
</verification>

<success_criteria>
- Zero rows in 13-REVIEW.md with empty `closed by` column.
- Every `defer:` row cites D-11(a) (Phase 14 TRIM-NN) or D-11(b) (needs-new-infra) — no other defer reasons.
- D-11(b) defer count = ROADMAP backlog bullet count.
- `task lint && task test` exit 0 after every commit.
- `task --dry install` exits 0.
</success_criteria>

<output>
After Tasks 1–3, write `.planning/phases/13-code-review-dead-code-cleanup/13-06-SUMMARY.md`:
- Count of FIX-NOW rows resolved (MEDIUM + LOW)
- Count of DEFER-PHASE-14-TRIM-NN rows (with TRIM-NN distribution)
- Count of DEFER-NEEDS-NEW-INFRA rows (with summary of backlog entries added)
- Per-commit list (SHA → REVIEW.md rows closed / annotated)
- Confirmation final REVIEW.md state has zero blank `closed by` columns
- Files modified count for ROADMAP completion
- Phase-13-complete signal: every REVW-NN requirement satisfied across the six plans
</output>

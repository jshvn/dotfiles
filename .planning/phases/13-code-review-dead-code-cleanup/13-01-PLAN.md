---
phase: 13-code-review-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [REVW-01]
threat_model: required

must_haves:
  truths:
    - "A single classified review report exists at .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md enumerating every finding from the zsh, YAML, TOML, and aux surfaces."
    - "Every finding row in 13-REVIEW.md carries six columns: file:line | severity (HIGH/MEDIUM/LOW) | category (correctness / portability / security / clarity / dead-code / duplication) | finding | remediation | closed by."
    - "Rows are sorted severity descending (HIGH > MEDIUM > LOW), then category alphabetical, then file:line ascending."
    - "ROADMAP.md SC#1 cites the actual REVIEW.md path .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md (not the stale .planning/phases/13-code-review/REVIEW.md)."
    - "Marketplace symlinked content under claude/agents/, claude/commands/, claude/skills/ is NOT reviewed and produces zero findings."
    - "Findings that duplicate existing lint enforcement (LINT-01..08, manifest:validate schema, task lint:syntax shellcheck) are NOT included — those are already covered."
    - "task lint && task test exit 0 after the REVIEW.md commit (green-tree gate)."
  artifacts:
    - path: ".planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md"
      provides: "Six-column classified finding list with header row and at least one finding row"
      contains: "| file:line | severity | category | finding | remediation | closed by |"
    - path: ".planning/ROADMAP.md"
      provides: "Updated SC#1 path citation for Phase 13"
      contains: "13-code-review-dead-code-cleanup/13-REVIEW.md"
  key_links:
    - from: "13-REVIEW.md"
      to: "ROADMAP.md SC#1"
      via: "filepath citation"
      pattern: "13-code-review-dead-code-cleanup/13-REVIEW\\.md"
    - from: "13-REVIEW.md rows"
      to: "later plans 13-02..13-06"
      via: "row line numbers cited in fix commit messages"
      pattern: "closes REVIEW.md row"
---

<objective>
Run a repo-wide code review of the post-Phase-12 v2 surface using four parallel `ecc:code-reviewer` agent invocations partitioned by surface (zsh / YAML / TOML / aux), merge the outputs into a single six-column classified finding list at `.planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md`, and amend `ROADMAP.md` SC#1 so the cited path matches the actual filename.

Purpose: REVW-01 — produce the spine that the next five plans cite. Every later plan annotates rows in this file by line number when closing findings. The six-column house style mirrors Phase 9 / Phase 12 conventions (D-05, D-14 in 12-CONTEXT.md).

Output:
- `13-REVIEW.md` (new) — header table + finding rows, sorted severity desc then category
- `ROADMAP.md` (amended) — SC#1 path correction
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-code-review-dead-code-cleanup/13-CONTEXT.md
@.planning/phases/12-task-surface-redesign/12-CONTEXT.md
@CLAUDE.md
@.claude/CLAUDE.md
@taskfiles/lint.yml
@taskfiles/helpers.yml
</context>

<read_first>
Before any review-pass work begins, the executor MUST read:
1. `.planning/phases/13-code-review-dead-code-cleanup/13-CONTEXT.md` — D-01 through D-11 set every reviewer-mix, plan-breakdown, dead-code, duplication, smoke-test, and defer-policy decision this plan honors.
2. `.planning/REQUIREMENTS.md` §"Review" — REVW-01 exact text; this plan addresses REVW-01.
3. `.planning/ROADMAP.md` §"Phase 13: Code Review + Dead-Code Cleanup" — five success criteria; SC#1 is the artifact this plan produces.
4. `.planning/phases/12-task-surface-redesign/12-CONTEXT.md` §D-14 — the six-column table house style this plan adopts verbatim.
5. `CLAUDE.md` (project root) §"Rules" and §"Conventions Not Captured Above" — lint rules + no-emojis + no-AI-attribution + `set -euo pipefail` invariants every fix must satisfy.
6. `taskfiles/lint.yml` — LINT-02 / LINT-03a / LINT-03b / LINT-04 / LINT-07 / LINT-08 enforcement bodies; reviewers must NOT re-report findings already caught by these rules.
7. `taskfiles/helpers.yml` — existing `_:safe-link`, `_:check-link`, `_:check-dir`, `_:check-file`, `_:check-command` family; the established `internal: true` + `requires.vars` pattern shared helpers must follow.
</read_first>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewer-agent → repo files | Reviewer agents read every authored file under the review surface and may surface its contents (file paths, line excerpts) in the report. |
| 13-REVIEW.md → planning record | Findings recorded here may be cited in commit messages and downstream phases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Information Disclosure | reviewer-agent over identity/ssh/keys/ | accept | identity/ssh/keys/ only contains `.pub` files per Phase 4 SC#4 (no private keys committed). Reviewer reads public material only. |
| T-13-01-02 | Information Disclosure | reviewer-agent over manifests/machines/*.toml | accept | Machine TOMLs contain identity placeholders (name, email) — already in git, already public to anyone with repo access. No new exposure. |
| T-13-01-03 | Tampering | reviewer output → REVIEW.md merge step | mitigate | Executor manually reviews and normalizes each reviewer's output before writing 13-REVIEW.md; no auto-paste of agent output. |
| T-13-01-04 | Repudiation | finding rows without provenance | mitigate | Every row carries `file:line` so the source is auditable; later closure rows carry short-SHA commit references. |

Out-of-scope surfaces (NOT read by any reviewer in this plan): `claude/agents/`, `claude/commands/`, `claude/skills/` (marketplace symlinks, not repo-authored), `.planning/` (planning artifacts), `.git/`.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Pre-review structural gate baseline</name>
  <files>(read-only; no files modified in this task)</files>
  <read_first>
    - taskfiles/lint.yml — LINT-01..08 enforcement bodies; the reviewer must know what is already enforced so it does not re-report it.
    - taskfiles/manifest.yml — manifest:validate schema gate; reviewers must not re-report schema violations.
    - taskfiles/test.yml — test:default aggregator; the green-tree gate at the end of this plan.
  </read_first>
  <action>
    Run the structural gates first to establish baseline state the reviewers must not re-report:

    1. `task lint:syntax` — captures shellcheck zsh-syntax findings already covered.
    2. `task lint:taskfile` — captures LINT-02 ($VAR in status), LINT-03a (cmds without status), LINT-03b (bare ln -s) baselines.
    3. `task lint:shell-headers` — captures LINT-04 missing `set -euo pipefail` baselines.
    4. `task lint:banner-parity` — captures LINT-08 banner drift baseline.
    5. `task manifest:resolve` then `task audit:manifest` — captures TOML schema baseline (resolver invariants + schema validity).
    6. `task test` — captures current passing fixture set.

    Save the output of each command to scratch text variables (or a temp file under `/tmp/13-01-baselines/`) so the reviewer-agent prompts can include "do NOT report findings already enforced by these rules:" sections. The baseline output is NOT committed; it exists only to scope the reviewer prompts. NO source files are modified in this task.

    If any of `task lint` / `task lint:taskfile` / `task lint:banner-parity` / `task audit:manifest` / `task test` exit non-zero on the baseline, surface the failure to the user as a pre-existing-bug finding (it becomes the first HIGH row in 13-REVIEW.md, attributed to the surface where the failure lives). Do NOT attempt to fix in this plan — fixes happen in 13-02 / 13-06.
  </action>
  <verify>
    <automated>
      task lint 2>&1 | tee /tmp/13-01-baseline-lint.log > /dev/null
      task test 2>&1 | tee /tmp/13-01-baseline-test.log > /dev/null
      test -s /tmp/13-01-baseline-lint.log && test -s /tmp/13-01-baseline-test.log
    </automated>
  </verify>
  <done>
    Baseline output captured for lint (LINT-02/03a/03b/04/08), manifest schema, and test suite. Pre-existing failures (if any) noted and slated for inclusion as HIGH rows in Task 2's REVIEW.md authorship.
  </done>
</task>

<task type="auto">
  <name>Task 2: Four parallel reviewer-agent spawns; merge into 13-REVIEW.md</name>
  <files>.planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md</files>
  <read_first>
    - .planning/phases/13-code-review-dead-code-cleanup/13-CONTEXT.md §"Reviewer mix and review surface" (D-01..D-04) — defines the four surface partitions verbatim.
    - .planning/phases/12-task-surface-redesign/12-CONTEXT.md §D-14 — the six-column table house style.
    - .planning/phases/13-code-review-dead-code-cleanup/13-CONTEXT.md §"Severity-threshold definitions" (Claude's Discretion) — HIGH/MEDIUM/LOW criteria.
    - Every file in each surface partition listed below (the reviewer agents do the actual file reads; the executor reads only enough to construct the reviewer prompts and merge outputs).
  </read_first>
  <action>
    Spawn FOUR `ecc:code-reviewer` agents IN PARALLEL (planner's discretion recommendation per D-Discretion item "Reviewer execution order"). Each operates on a disjoint surface partition — no cross-dependency, parallel-safe.

    **Reviewer 1 — zsh surface (D-01):**
    - In scope: `bootstrap.zsh`; `install/bootstrap.zsh`, `install/messages.zsh`, `install/resolver.zsh`, `install/compose-brewfile.zsh`, `install/test-hooks.zsh`; every file matching `shell/functions/*.zsh`, `shell/aliases/*.zsh`, `shell/theme.zsh`, `shell/.zshenv`, `shell/.zprofile`, `shell/.zshrc`, `shell/.zlogin`, `shell/.zlogout`; `os/shell-registration.zsh` and every `os/defaults/*.zsh`; `identity/ssh/cloudflared.zsh` and any other `identity/**/*.zsh`; `claude/hooks/lib.zsh` and every `claude/hooks/*.zsh` script.
    - Do NOT report findings already enforced by `task lint:syntax` (shellcheck), `task lint:shell-headers` (LINT-04 `set -euo pipefail`), or `task lint:portability` (LINT-05 macOS-only patterns).
    - DO report: dead branches, unreachable code, unsafe patterns (unquoted expansions, command-injection risk, missing input validation at boundaries), idiom violations, duplication candidates (functions/snippets appearing 3+ times), helpers with zero callsites, fragile error handling that swallows exit codes silently.
    - Output format: six-column markdown table — `| file:line | severity | category | finding | remediation | closed by |` — one row per finding; severity ∈ {HIGH, MEDIUM, LOW}; category ∈ {correctness, portability, security, clarity, dead-code, duplication}; `closed by` column initially blank (`|  |`).

    **Reviewer 2 — YAML surface (D-02):**
    - In scope: `Taskfile.yml`, every `taskfiles/*.yml` (audit, claude, helpers, identity, links, lint, macos, manifest, packages, refresh, shell, show, test).
    - Do NOT report findings already enforced by `task lint:taskfile` (LINT-02/03a/03b) or `task lint:banner-parity` (LINT-08).
    - DO report: near-duplicate status blocks across taskfiles (the duplication candidates Plan 13-04 will consolidate), redundant cmd patterns, orphan internal tasks with zero `task:` callers, drift between taskfiles, unreferenced `deps:` / `task:` references, dead branches in shell heredocs inside `cmds:`, status blocks that only check existence when they should check target match (REVW-05 / Plan 13-05 fix surface — flag `taskfiles/links.yml` `install-zsh` / `install-claude` / `install-configs` / `configs:ghostty` status blocks here so Plan 13-05 has the inventory).
    - Output format: same six-column shape as Reviewer 1.

    **Reviewer 3 — TOML surface (D-03):**
    - In scope: `manifests/defaults.toml`, every `manifests/machines/*.toml`.
    - Do NOT report findings already enforced by `task audit:manifest` (schema gate) or `install/resolver.zsh` (deep-merge invariants).
    - DO report: features declared in `defaults.toml` with zero consumers (search `*.yml`, `*.zsh` for the feature key — zero hits = dead feature flag = REVW-03 candidate); inconsistent `extra_packages` shapes between machines; kebab-case adherence in feature keys; identity placeholder drift; comment-vs-key mismatch.
    - Output format: same six-column shape as Reviewer 1.

    **Reviewer 4 — aux surface (D-04):**
    - In scope: every file under `configs/*` (conda, eza, ghostty, glow, motd, tlrc, trippy); `packages/core.rb`, `packages/gui.rb`; README files inside each top-level dir (`shell/README.md`, `packages/README.md`, `configs/README.md`, `identity/README.md`, `os/README.md`, `taskfiles/README.md`, `claude/README.md`, `install/README.md`).
    - Out of scope (do NOT review): `claude/agents/`, `claude/commands/`, `claude/skills/` (marketplace symlinks per D-04).
    - DO report: dead config blocks, obsolete settings, removed-formula references in brewfiles, README inaccuracies that name nonexistent tasks/files (post-Phase-12 rename drift). Doc-text density and comment-trimming concerns are Phase 14 — do NOT report those (route to `defer: Phase 14 TRIM-01` if surfaced).
    - Output format: same six-column shape as Reviewer 1.

    After all four reviewers return, the executor:
    1. Merges the four six-column tables into ONE table in `13-REVIEW.md`.
    2. Sorts rows by severity descending (HIGH > MEDIUM > LOW), then by category alphabetical, then by file:line ascending.
    3. De-duplicates rows that surfaced from overlapping coverage (e.g., a duplication candidate that both Reviewer 1 zsh-side and Reviewer 2 YAML-side report — keep one canonical row).
    4. Adds the file header:
       ```
       # Phase 13 Code Review

       Generated: <date>
       Surfaces reviewed: zsh (D-01), YAML (D-02), TOML (D-03), aux (D-04)
       Reviewer: ecc:code-reviewer (four parallel spawns)
       Out of scope: claude/agents/, claude/commands/, claude/skills/ (marketplace), .planning/, .git/

       Severity gate: HIGH fixed in this phase (Plan 13-02); MEDIUM/LOW triaged (Plan 13-06)
       Closed-by format: short-SHA (e.g., abc1234) or `defer: <reason>` per D-11
       ```
    5. Writes the merged table with header row:
       ```
       | file:line | severity | category | finding | remediation | closed by |
       |-----------|----------|----------|---------|------------|-----------|
       ```
    6. Commits with message `docs(13-01): add 13-REVIEW.md with HIGH/MEDIUM/LOW findings (REVW-01)`.

    Do NOT inline reviewer raw output — every row is normalized to the six-column shape. If a reviewer surfaces a finding with no clear remediation, the row gets `remediation: investigate in Plan 13-02` (HIGH) or `remediation: investigate in Plan 13-06` (MEDIUM/LOW) — never blank.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md
      grep -c '^| file:line' .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md | grep -qx '1'
      grep -cE '^\|.*\| (HIGH|MEDIUM|LOW) \|' .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md | awk '$1 >= 1 { exit 0 } { exit 1 }'
      grep -q 'Out of scope: claude/agents/' .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md
    </automated>
  </verify>
  <done>
    `13-REVIEW.md` exists with exactly one header row, at least one finding row of recognized severity, the out-of-scope declaration line present, and the file committed with the message shape `docs(13-01): add 13-REVIEW.md ...`. `task lint && task test` exit 0 (the file is markdown; no code changes; no lint regression).
  </done>
</task>

<task type="auto">
  <name>Task 3: Amend ROADMAP.md SC#1 path citation</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 13: Code Review + Dead-Code Cleanup" — locate SC#1 (Success Criteria #1) which currently cites `.planning/phases/13-code-review/REVIEW.md` (the near-miss from before the slug was finalized per D-06).
    - .planning/phases/13-code-review-dead-code-cleanup/13-CONTEXT.md §D-06 — the path-correction rationale.
  </read_first>
  <action>
    In `.planning/ROADMAP.md`, locate the Phase 13 Success Criteria block (under `### Phase 13: Code Review + Dead-Code Cleanup` → `**Success Criteria**`). In SC#1, replace the exact string `.planning/phases/13-code-review/REVIEW.md` with `.planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md`. The replacement uses the `{padded_phase}-REVIEW.md` naming convention consistent with `13-CONTEXT.md`, `13-SUMMARY.md`, `13-VERIFICATION.md`.

    Also in SC#4 of the same Phase 13 block, locate the citation `.planning/phases/13-code-review/13-SMOKE.md` (if present) and replace with `.planning/phases/13-code-review-dead-code-cleanup/13-SMOKE.md`. (SC#4 may already use the correct path; if so, no change — verify by grep.)

    No other ROADMAP.md edits. Commit with message `docs(13-01): amend ROADMAP.md SC#1 (and SC#4 if needed) to actual 13-REVIEW.md / 13-SMOKE.md path (D-06)`.
  </action>
  <verify>
    <automated>
      grep -q '13-code-review-dead-code-cleanup/13-REVIEW.md' .planning/ROADMAP.md
      ! grep -q '13-code-review/REVIEW.md' .planning/ROADMAP.md
      ! grep -q '13-code-review/13-SMOKE.md' .planning/ROADMAP.md
      task lint && task test
    </automated>
  </verify>
  <done>
    ROADMAP.md SC#1 cites the actual `13-REVIEW.md` path; the stale `13-code-review/REVIEW.md` and `13-code-review/13-SMOKE.md` paths are absent; commit lands with the prescribed message; `task lint && task test` exit 0.
  </done>
</task>

</tasks>

<verification>
After every commit in this plan: `task lint && task test` both exit 0 (green-tree gate per D-Discretion).

REVW-01 closure check:
- `test -f .planning/phases/13-code-review-dead-code-cleanup/13-REVIEW.md` returns 0
- The file's first non-header line (after the markdown frontmatter) opens the six-column table
- At least one finding row of recognized severity exists
- `grep -q '13-code-review-dead-code-cleanup/13-REVIEW.md' .planning/ROADMAP.md` returns 0
- `! grep -q '13-code-review/REVIEW.md' .planning/ROADMAP.md` returns 0

Commit message shape: every commit in this plan uses `docs(13-01): <one-line>` (REVW-01 is the doc/artifact deliverable, not a code fix — `docs(` is the correct type per the global git convention).

Cross-cutting must_haves (apply to every plan in this phase):
- After every commit, `task lint && task test` exit 0.
- Every commit uses logical-batch shape: `<type>(13-NN): <one-line>` (this plan uses `docs(` for both commits).
- No AI attribution; no emojis anywhere; `set -euo pipefail` invariant remains intact (no .zsh files edited in this plan).
</verification>

<success_criteria>
- `13-REVIEW.md` is committed at the canonical path with a normalized six-column table covering all four reviewer surfaces.
- ROADMAP.md SC#1 cites the actual `13-REVIEW.md` path (D-06 correction landed).
- Marketplace surfaces produced zero findings (verified by inspection — `claude/agents/`, `claude/commands/`, `claude/skills/` do not appear in any `file:line` column).
- Findings already enforced by `task lint*` rules or `task audit:manifest` are NOT in the report (no double-coverage).
- `task lint && task test` exit 0 after both commits.
</success_criteria>

<output>
After all three tasks complete, write the per-plan summary at `.planning/phases/13-code-review-dead-code-cleanup/13-01-SUMMARY.md` using the standard template:
- Findings count by severity (HIGH N, MEDIUM N, LOW N)
- Findings count by category
- Surface partition coverage (zsh / YAML / TOML / aux file counts read)
- Any baseline lint/test failures that became HIGH rows (with the row line number for downstream plans to reference)
- ROADMAP.md SC#1 path correction confirmed
</output>

# Phase 13 Code Review

Generated: 2026-05-18
Surfaces reviewed: zsh (D-01), YAML (D-02), TOML (D-03), aux (D-04)
Reviewer: ecc:code-reviewer surrogate (four serial surface-partition passes; planner-executor authored under executor constraints since the spawn primitive is unavailable in this runtime; each finding is normalized to the six-column house style D-14 from 12-CONTEXT.md verbatim)
Out of scope: claude/agents/, claude/commands/, claude/skills/ (marketplace symlinks not authored here), .planning/, .git/

Severity gate: HIGH fixed in this phase (Plan 13-02); MEDIUM/LOW triaged (Plan 13-06)
Closed-by format: short-SHA (e.g., abc1234) or `defer: <reason>` per D-11

| file:line | severity | category | finding | remediation | closed by |
|-----------|----------|----------|---------|------------|-----------|
| shell/aliases/general.zsh:24 | HIGH | correctness | `alias ls="$(command -v eza) --time-style long-iso"` evaluates `command -v eza` at source time; if eza is uninstalled (or PATH is unprimed), expansion yields ` --time-style long-iso` which masks the system `ls` entirely (every directory listing fails). Same class at networking.zsh:7 for `trip`. | Replace with `alias ls='eza --time-style long-iso'` (lazy expansion) gated on `command -v eza >/dev/null 2>&1 \|\| return 0` source-time guard; apply identical fix to `networking.zsh:7`. Fix in Plan 13-02. | d5b21a0 |
| taskfiles/links.yml:155-160 | HIGH | correctness | `install-zsh` status block uses `test -L` (existence-only) for 5 symlinks; per REVW-05 + SC#4 the contract requires `readlink -f <link> == <expected source>` so a corrupted symlink does not skip re-link. Same defect at install-claude:240-252 (13 entries), install-configs:285-290 (8 entries), configs:ghostty:309 (1 entry) — 27 entries total, matches REVW-05. | Plan 13-05: extend `helpers.yml _:check-link` target-match into a `_:check-link-target` callable from status: blocks, OR rewrite each entry inline with `[[ "$(readlink -f <link>)" == "<expected>" ]]`. Planner-recommendation: helper-based (D-Discretion). | defer: Plan 13-05 (REVW-05) |
| install/README.md:5-7,23-28,29-34,42,51 | MEDIUM | clarity | README describes `cutover-gate.zsh` and `Brewfile.rb` / `Brewfile-personal.rb` / `Brewfile-server.rb` / `Brewfile-work.rb` as live files; all five files were deleted in Phase 11. Also "Phase 5 will move the Brewfile bundles..." (Phase 5 closed). Stale Phase-2/Phase-3-tense phrasing throughout. | Plan 13-06: rewrite Key files block to list only `resolver.zsh`, `messages.zsh`, `compose-brewfile.zsh`, `test-hooks.zsh`; drop cutover-gate + Brewfile blocks; correct tense; remove the Adding-a-pattern Brewfile reference. (Defer to Phase 14 TRIM-05 if scope creeps.) |  |
| configs/README.md:12 | MEDIUM | clarity | README table row `antidote \| antidote/zsh_plugins.txt \| ~/.config/antidote/zsh_plugins.txt \| always on` references nonexistent `configs/antidote/` directory; antidote was reverted to antigen in `.zshrc:75`. Operator following this doc would look for a nonexistent file. | Plan 13-06: delete the antidote row from the configs table (and any sibling antidote references in shell/README.md:16, taskfiles/README.md:23, packages/README.md:17). |  |
| install/compose-brewfile.zsh:33-35,142 | MEDIUM | clarity | Docstring `Lines shape` comment (33-35) still lists `brew '<name>' # verify: <verify>` as the formula-override form, and the jq emit at line 142 still produces those per-line comments. Plan 05-07 stripped per-line `# verify:` from bundle authoring, but the composer emit-side and its docstring were not updated; result: composed Brewfile may carry vestigial verify comments only the bundle file author would write. | Plan 13-02 or 13-06: if formula-override `# verify:` is dead per the Gap-2 pivot, remove the doc lines AND simplify the jq branch (`if type == "string"` becomes a pass-through). If still live for some edge case, update the docstring to reference exactly which post-Gap-2 path uses it. |  |
| install/messages.zsh:52 | MEDIUM | correctness | `[[ "$DOTFILES_DEBUG" == "true" ]]` references the var bare; any caller running with `set -u` (every install/`*.zsh` and every os/defaults/*.zsh) crashes when `DOTFILES_DEBUG` is unset and `debug` is invoked. | Plan 13-02: change to `[[ "${DOTFILES_DEBUG:-}" == "true" ]]`. One-line fix matches the existing `${DOTFILES_MESSAGES_LOADED:-}` guard pattern earlier in the file. |  |
| install/test-hooks.zsh:13,28,181 | MEDIUM | correctness | File-header comment claims `Exit codes: 0 if all eight fixtures pass; non-zero (count of failures) otherwise.` but the runner assigns `failed=1` (binary) on any failure rather than incrementing — so the exit code is at most 1, never the count described. Doc-vs-code drift. | Plan 13-02: change `failed=1` to `failed=$((failed + 1))` in all four `test_*` functions (4 sites) so the documented contract is honored. |  |
| install/compose-brewfile.zsh:43-45 + install/resolver.zsh:36-40 + os/shell-registration.zsh:55-59 + os/defaults/dock.zsh:35-39 + os/defaults/finder.zsh + os/defaults/input.zsh + os/defaults/security.zsh + os/defaults/screenshots.zsh | MEDIUM | duplication | 8 .zsh scripts carry an identical 9-line "Source messages.zsh, but only if not already loaded ... pre-initialize the guard variable" block (`: "${DOTFILEDIR:?...}"` + `: "${DOTFILES_MESSAGES_LOADED:=}"` + `if [[ -z ... ]]; then source ...; fi`). 8 sites > rule-of-three threshold. | Plan 13-04: extract to `install/load-messages.zsh` sourced via a single line `source "${0:A:h}/load-messages.zsh"` or extend `install/messages.zsh` with a self-bootstrapping guard so callers can `source` it under `set -u` without the manual pre-initialize incantation. | 852a763 |
| os/defaults/dock.zsh:55-90 + os/defaults/finder.zsh:55-92 + os/defaults/input.zsh + os/defaults/screenshots.zsh + os/defaults/security.zsh | MEDIUM | duplication | 5 files implement near-identical `apply_X` (tuple-stride-4 loop calling `defaults write`) and `verify_X` (tuple loop + `defaults read` + bool-normalize + check/cross) — only the array name (`DOCK_DEFAULTS` / `FINDER_DEFAULTS` / ...) and the `killall <Process>` target differ between files. | Plan 13-04: extract `os/defaults/_apply_verify.zsh` (or new `_:apply-defaults` / `_:verify-defaults` helpers in helpers.yml) accepting `domain_array_name=` and `killall_target=`. Each per-concern file then declares just its tuple array + invokes the shared apply/verify. | 5462d78 |
| taskfiles/links.yml:240-252 | MEDIUM | duplication | 13 identical `'{{if not (index .MANIFEST.features "claude-marketplace")}}true{{else}}test -L "..."{{end}}'` lines in `install-claude.status`. Same template-gate-around-test pattern; only the path differs per line. | Plan 13-05 (or 13-04): merge with the REVW-05 fix — switch to a single `for path in ...; do [[ readlink -f match expected ]]; done` shell block inside one status entry, gated once on the feature flag, eliminating both the duplication AND closing the target-match gap simultaneously. | defer: Plan 13-05 (REVW-05) — same fix as row 14: the 13-entry status block is the duplication side of the test -L target-match bug; per Phase 13 plan-breakdown table (13-CONTEXT.md §D-05) REVW-05 closure routes to Plan 13-05 which will collapse both row 14 and row 22 simultaneously with the readlink -f fix |
| taskfiles/manifest.yml:202-235 + 247-294 | MEDIUM | duplication | `manifest:show` and `manifest:validate` each contain identical 17-line "Parse --machine NAME from CLI_ARGS" blocks (sed extraction + bare-name fallback + state-file fallback + missing-machine error). Two complete copies of the same logic. | Plan 13-04: extract to either an inline helper script (`install/parse-machine-arg.zsh` returning the resolved name on stdout) OR a shared shell-function snippet sourced from a single location. (Borderline rule-of-three; only 2 sites, but the logic is non-trivial.) | keep: 2 occurrences — rule-of-three (D-09); the row itself flags "Borderline rule-of-three; only 2 sites" and the D-09 rule keeps 2-occurrence patterns inline |
| Taskfile.yml:40-45 + bootstrap.zsh:70-74 + shell/.zprofile:35-44 + identity/ssh/cloudflared.zsh:19-27 | MEDIUM | duplication | The "detect Homebrew prefix from `uname -m` arch" dispatch is implemented 4 separate ways: shell-block `if ... elif ... else echo /opt/homebrew/...` (Taskfile.yml), `if [[ arm64 ]] ... else /usr/local ...` (bootstrap.zsh, .zprofile), and `case "$(uname -m)" in arm64) ... x86_64) ...` (cloudflared.zsh). 4 sites with subtly different shapes; first error in any one would not be caught by the others. | Plan 13-04: consolidate into a single sourced helper or document in CLAUDE.md as a sanctioned literal pattern (acceptable since the four sites all bootstrap before `brew shellenv` and HOMEBREW_PREFIX is consequently unavailable). If consolidation is chosen, prefer `install/detect-brew-prefix.zsh` echoing the path; each site does `prefix=$(source detect-brew-prefix.zsh)`. | keep: sanctioned literal pattern per CLAUDE.md "Detect the Homebrew prefix by uname -m" — consolidation architecturally infeasible (.zshenv does not export DOTFILEDIR, so .zprofile + cloudflared.zsh + bootstrap.zsh-pre-DOTFILEDIR + Taskfile.yml-vars cannot source a shared helper); each site bootstraps before brew shellenv is available; the 4 shapes return subtly different artifacts (brew-binary path vs prefix) on purpose; remediation column's second option (CLAUDE.md sanctioned literal) is the accepted resolution |
| taskfiles/test.yml:173-265 | MEDIUM | duplication | Five negative-fixture validation blocks (`_invalid-missing-desc`, `_invalid-bad-os`, `_invalid-identity-without-opssh`, `_invalid-identity-without-opsign`, `_invalid-bad-identity`) repeat the identical 10-line `neg_dir=... neg_copies+=... cp ... neg_stderr=$(...resolver.zsh --validate-only --machine ... 2>&1) rm -f ... if grep -q PATTERN; then check; else cross; fi` shape; only the fixture name and the expected stderr substring differ. | Plan 13-04: extract a `_run_negative_fixture <name> <expected_stderr_substring>` shell-function inside the cmds block (or a `taskfiles/test-helpers.yml` snippet) so the four copies collapse into 5 one-liner calls. | 12af5b9 |
| shell/aliases/finder.zsh:13-17,19-23,25-29 + shell/aliases/ghostty.zsh:15-19 | MEDIUM | duplication | 4 wrapper-function gates carry the identical `[[ "$(_dotfiles_feature X)" == "true" ]] \|\| { echo "Y: feature 'X' is disabled on this machine" >&2; return 1; }` two-line guard. The repeated stderr-message-then-return-1 pattern is rule-of-three-positive. | Plan 13-04 candidate (low-priority): extract a `_dotfiles_require_feature <feature> <function-name>` helper in `shell/functions/`; each wrapper becomes a single line. Note: interactive surface is preserved per D-08 Class A, but the helper itself is internal and falls under Class B (extract is safe). | be4d90a |
| taskfiles/shell.yml:107-133 | MEDIUM | duplication | `validate` task body inlines `[[ -d "<dir>" ]] && check / cross + failures+=1` four times for the four XDG dirs. 4 sites of an identical 6-line pattern within one task body. | Plan 13-04 (or 13-06): pull into a tiny loop `for pair in "{{.XDG_CONFIG_HOME}}:XDG config home" ...; do dir="${pair%%:*}"; name="${pair#*:}"; ...; done`. Cuts ~24 lines to ~6. | 784b812 |
| Taskfile.yml:131-148 + 240-248 | MEDIUM | clarity | Two long inline comment blocks justify the "Source messages.zsh via TASKFILE_DIR (NOT DOTFILES_MESSAGES)" workaround pointing at `lint.yml:59 + links.yml:61` DOTFILEDIR pollution. The root cause (`sh: dirname "{{.TASKFILE_DIR}}"` in 8 included taskfiles leaking back to root scope) survives in lint.yml, links.yml, shell.yml, test.yml, manifest.yml, audit.yml, show.yml, refresh.yml. Each new include re-introduces the leak. | Plan 13-04: standardize the per-taskfile DOTFILEDIR-fallback pattern so it does NOT leak back to root scope (e.g., use a different var name like `_LOCAL_DOTFILEDIR` for the fallback, or unify on root's vars-merge contract). Removes the need for the TASKFILE_DIR workaround in Taskfile.yml. | keep: 2 occurrences (comment blocks) — rule-of-three (D-09); the duplication surface is two comment blocks, not duplicated executable logic. The underlying DOTFILEDIR-leak fix would require renaming the fallback var across 8 taskfile includes — architectural (Rule 4) work that defers to Phase 14 TRIM-04 (doc/comment refresh) or a dedicated future-phase entry per D-11(b) (needs-new-infra-grade rename touching the vars-merge contract) |
| shell/functions/motd.zsh:52,64,80 | MEDIUM | correctness | Three literal emoji codepoints (⚡ U+26A1, 📦 U+1F4E6, 💭 U+1F4AD) in display strings. Project CLAUDE.md is explicit: "No emojis in any file — markdown included. Project convention is stricter than the global rule." `claude/hooks/no-emojis.zsh` would block any future write here; the file predates the hook. | Plan 13-02: replace with ASCII labels per the no-emojis hook recommendation (e.g., `[ SYSTEM ]`, `[ DOTFILES ]`, `[ TRANSMISSION ]`). Box-drawing/dingbat unicode in `theme.zsh` and `install/messages.zsh:65,69` (✓ ✗) are intentional UI semantics and remain in scope of `defer: theme locked-keep per PROJECT.md`. |  |
| manifests/defaults.toml:28 + manifests/machines/personal-laptop.toml:23 + work-laptop.toml:22 + atium.toml:16 | MEDIUM | dead-code | Feature flag `motd = true` declared in defaults + all three machine TOMLs; zero consumers in the repo. `shell/.zlogin:17` invokes `motd` unconditionally via `(( $+functions[motd] ))`; no `_dotfiles_feature motd` call exists anywhere in shell/, os/, taskfiles/, or claude/. The flag controls nothing. | Plan 13-03 (REVW-03): per D-08 Class B with grep evidence (`git grep -nE 'motd' -- '*.zsh' '*.yml' shows zero `_dotfiles_feature motd` or `index .MANIFEST.features "motd"` hits), remove the `motd` key from defaults.toml and the three machine TOMLs in one commit; or wire `.zlogin` to gate on `_dotfiles_feature motd` if motd-opt-out is desired. | edbbabd |
| taskfiles/lint.yml:198 | MEDIUM | dead-code | `[[ "$f" == *"commit-task"* ]] && continue` exempts a `commit-task1.yml` file from LINT-03a enforcement; the file does not exist anywhere in the repo (`find . -name 'commit-task*'` returns nothing). | Plan 13-03 (REVW-03 / D-08 Class B): remove the line (and the preceding comment `# Also skip commit-task1.yml if present (temporary executor helper)`). Zero grep hits across the repo. | cdbab32 |
| shell/.zprofile:41-44 | MEDIUM | dead-code | Linux branch `DIRECTORY="/home/linuxbrew/.linuxbrew/bin/brew"` in a v1 macOS-only repo. PROJECT.md "Out of Scope" explicitly defers Linux to v2+; the `else` arm is unreachable in v1 (every machine TOML enforces `platform.os = "darwin"`). | Plan 13-03 (D-08 Class B): drop the `else` branch and the `# Linux` comment; collapse to the arm64/x86_64 dispatch (matches bootstrap.zsh:70-74 + cloudflared.zsh:20-27 — neither of which carries a Linux arm). Re-introduce a Linux branch the day a Linux machine TOML is added (Phase 14+). | ebccf47 |
| install/resolver.zsh:209 | LOW | clarity | `valid=$(ls "$ident_dir" 2>/dev/null \| tr '\n' '\|' \| sed 's/\|$//')` parses `ls` output to build the error-message "available" list; `ls` parsing is an anti-pattern even though kebab-case kept it safe in practice. | Plan 13-06: replace with zsh glob `valid="${(j:|:)${(@):-$(print -l "$ident_dir"/*(N:t))}}"` or the simpler `valid=$(print -l "$ident_dir"/*(N:t) \| tr '\n' '\|' \| sed 's/\|$//')`. Same fix applicable to `taskfiles/manifest.yml:116 AVAILABLE_MACHINES` (`xargs basename` chain). |  |
| shell/.zshenv:37,38 | LOW | correctness | `export XDG_DATA_DIRS="/usr/local/share:/usr/share"` and `export XDG_CONFIG_DIRS="/etc/xdg"` unconditionally OVERWRITE any value the launching context already set. `.zshenv` is sourced for non-interactive contexts too; a system-administered XDG_DATA_DIRS would be silently clobbered. | Plan 13-06: switch to the same `${VAR:-default}` pattern the surrounding XDG_*_HOME lines use (line 31-34). One-line fix. |  |
| shell/.zshenv:64 | LOW | clarity | `export BROWSER="/Applications/Firefox.app/Contents/MacOS/firefox"` hardcodes Firefox path; tools reading `$BROWSER` get a path-to-nothing on machines without Firefox installed. | Plan 13-06: guard with `[[ -x "/Applications/Firefox.app/Contents/MacOS/firefox" ]] && export BROWSER=...` or move to a per-machine identity overlay since this is operator preference, not a manifest feature. |  |
| shell/.zshrc:50 | LOW | correctness | `local _zcomp_age=0` at script scope (line 50, outside any function). zsh accepts `local` at top-level when sourced but shellcheck flags it; same class as the `local`-at-script-scope shellcheck error that Plan 07-02 fixed in `agent-transparency.zsh`. | Plan 13-06: drop `local`; the variable is already unset at line 59. |  |
| shell/.zshrc:113-115 | LOW | correctness | `source "$(code --locate-shell-integration-path zsh)"` runs unconditionally when `$TERM_PROGRAM == "vscode"`; if VSCode terminal is active but the `code` CLI is unavailable (or not installed via "Shell Command: Install 'code' command in PATH"), `source ""` fails noisily. | Plan 13-06: wrap with `command -v code >/dev/null 2>&1 && source "$(code --locate-shell-integration-path zsh 2>/dev/null || true)"`. |  |
| shell/functions/pubkey.zsh:7,11 | LOW | clarity | Line 11 `more ~/.ssh/"${1}" | pbcopy` uses `more` to read a file before piping (more does pagination, then exits when stdin is consumed); `pbcopy < ~/.ssh/"$1"` is the canonical form. Line 7 `local keylist=$(ls ~/.ssh/*.pub)` parses `ls` output. Line 11 also lacks a `[[ -f ~/.ssh/"$1" ]]` existence guard before reading. | Plan 13-06: `pbcopy < ~/.ssh/"$1"` for line 11; `local keylist=$(print -l ~/.ssh/*.pub(N))` for line 7; add the existence guard. |  |
| shell/functions/cheat.zsh:8 | LOW | correctness | `echo $result` (unquoted) word-splits multi-line `curl` output, mangling whitespace-significant cheatsheet content. | Plan 13-06: quote: `echo "$result"`. One-character fix. |  |
| shell/functions/prettyjson.zsh:9 | LOW | correctness | `jq '.' "${1}"` only checks empty arg via `-z`; passes through to jq which fails noisily on a missing file. A `[[ -f "${1}" ]]` precondition (matching `getcertnames.zsh`'s validation pattern) would surface a clean error. | Plan 13-06: add `[[ -f "${1}" ]] \|\| { echo "ERROR: file not found: ${1}" >&2; return 1; }` after the empty-arg check. |  |
| shell/functions/whois.zsh + getcertnames.zsh + permissions.zsh + cheat.zsh + host.zsh + ghpubkey.zsh + geoip.zsh + pubkey.zsh + vnc.zsh + prettyjson.zsh + sethostname.zsh | LOW | duplication | 11 functions use the identical `if [[ -z "${1}" ]]; then echo "ERROR: No <thing> specified"; return 1; fi` arg-validation pattern. | `defer: Phase 14 TRIM-02` — these are interactive D-08 Class A functions where per-call messages give better UX than a shared helper. Phase 14 may revisit if comment density makes them harder to scan. | defer: Phase 14 TRIM-02 — interactive D-08 Class A functions; per-call stderr messages give better UX than a shared helper. Phase 14 may revisit if comment density makes them harder to scan |
| taskfiles/refresh.yml:8-11 | LOW | clarity | Header comment first asserts tasks are "Read-only" then immediately notes "the underlying refresh implementations DO mutate state". Self-contradictory two sentences side-by-side. | Plan 13-06: rewrite to one sentence: "Tasks here are thin pass-through delegates; the underlying implementations may mutate state (e.g., claude:update re-fetches plugins)." |  |
| taskfiles/manifest.yml:9-15 | LOW | clarity | Comments still read "NOT yet included from the root Taskfile.yml (Phase 1 constraint)" and "Phase 2 will add `manifest: ./taskfiles/manifest.yml` to the root includes block." Root Taskfile.yml:77-82 already includes it. Stale tense. | Plan 13-06: rewrite the header to past-tense ("Included by the root Taskfile.yml") and drop the Phase 1 / Phase 2 forward-looking phrasing. |  |
| manifests/machines/personal-laptop.toml:67 + work-laptop.toml:56 | LOW | clarity | `mas` entry App Store ID 904280696 is named `"Things3"` on personal-laptop and `"Things"` on work-laptop. Same Mac App Store ID resolves to "Things 3" in the store; brewfile/mas tolerates either, but inconsistency between machines makes the diff confusing. | Plan 13-06: standardize to `"Things 3"` (the official Apple App Store name) in both machine TOMLs. |  |
| shell/.zlogout:2-51 | LOW | clarity | 51-line comment block over a 2-line active body (`fc -W 2>/dev/null \|\| true`). Heavy "examples / typical contents / safety notes" prose. | `defer: Phase 14 TRIM-01` — inline-comment density audit. Phase 14 owns the comment-trim pass per D-11(a). |  |
| shell/theme.zsh + multiple | LOW | clarity | Decoration banner comments `##############################` / `###### Section` in `shell/theme.zsh`, `shell/.zshenv`, `shell/.zprofile` headers; verbose banner blocks across `os/defaults/*.zsh`. | `defer: Phase 14 TRIM-02` — per-file header banner slim. Phase 14 owns the banner pass per D-11(a). |  |
| taskfiles/README.md:23 + shell/README.md:16 + packages/README.md:17 | LOW | clarity | All three READMEs reference `antidote` as if it is the live plugin manager. `.zshrc:75` reverted to antigen; the README narrative is stale. Lower-priority sibling of the configs/README.md:12 finding (MEDIUM there because the table row points at a nonexistent FILE). | Plan 13-06 (or roll into the same commit as the configs/README.md fix): replace each antidote mention with antigen, with a one-line "antidote was evaluated in Phase 3 D-01 and reverted; see .zshrc:75 comment for rationale." |  |
| install/resolver.zsh + taskfiles/manifest.yml + others | LOW | duplication | The `head -n1 "$state_file" \| sed -e 's/^[[:space:]]*//' -e 's/[[:space:]]*$//'` "read first line + trim edges" idiom appears in install/resolver.zsh:598, taskfiles/manifest.yml:82,228,271,315 and taskfiles/test.yml:315. 6 sites. | `defer: Phase 14 TRIM-02 \|\| Plan 13-04` — single helper `read_machine_state_file <path>` could collapse all six sites. Marginal value; defer unless Plan 13-04 has spare cycles. | defer: Phase 14 TRIM-02 — LOW-severity per REVIEW; re-grepped actual count is 4 (manifest.yml:82,228,271 + test.yml:315; original "6 sites" overcounted); per-site contexts mixed (1 YAML sh: var-resolution block + 3 cmd heredocs); shared-helper extraction is the row's own opt-in routing (`defer ... unless Plan 13-04 has spare cycles`); Plan 13-04 prioritized MEDIUM-severity duplication rows |
| (general) | LOW | clarity | `# Phase N` / `# D-NN` / `# CR-NN` / `# WR-NN` / `# RESEARCH §X.Y` planning-history annotations are scattered across nearly every taskfile and zsh script; cumulatively significant noise for a new contributor with zero v2-history context. | `defer: Phase 14 TRIM-01 + TRIM-05` — exactly the "no v1 (bug \| finding \| leftover) \| Gap [0-9]+ \| D-[0-9]+ \| UAT [Gg]ap returns zero matches in code" gate from Phase 14 SC#5. |  |

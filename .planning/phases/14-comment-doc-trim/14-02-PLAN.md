---
phase: 14-comment-doc-trim
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - .planning/phases/14-comment-doc-trim/14-METRICS.md
  - Taskfile.yml
  - taskfiles/links.yml
  - taskfiles/manifest.yml
  - taskfiles/packages.yml
  - taskfiles/identity.yml
  - taskfiles/lint.yml
  - taskfiles/claude.yml
  - taskfiles/macos.yml
  - taskfiles/test.yml
  - taskfiles/shell.yml
  - taskfiles/helpers.yml
  - taskfiles/audit.yml
  - taskfiles/show.yml
  - taskfiles/refresh.yml
  - install/resolver.zsh
  - install/messages.zsh
  - install/bootstrap.zsh
  - install/compose-brewfile.zsh
  - install/test-hooks.zsh
  - os/defaults/dock.zsh
  - os/defaults/finder.zsh
  - os/defaults/input.zsh
  - os/defaults/screenshots.zsh
  - os/defaults/security.zsh
  - os/shell-registration.zsh
  - identity/ssh/cloudflared.zsh
  - shell/.zlogout
  - shell/.zshenv
  - shell/.zprofile
  - shell/.zshrc
  - shell/.zlogin
  - shell/theme.zsh
  - shell/functions/_dotfiles_feature.zsh
  - shell/functions/_dotfiles_require_feature.zsh
  - shell/aliases/jgrid.zsh
  - shell/aliases/ghostty.zsh
  - shell/aliases/finder.zsh
  - shell/aliases/dotfiles.zsh
  - claude/hooks/lib.zsh
  - claude/hooks/secret-scan.zsh
  - claude/hooks/no-emojis.zsh
  - claude/hooks/no-ai-comments.zsh
  - claude/hooks/agent-transparency.zsh
autonomous: true
requirements: [TRIM-01, TRIM-02]
requirements_addressed: [TRIM-01, TRIM-02]
tags: [comment-trim, banner-trim, taskfile, zsh, metrics]

must_haves:
  truths:
    - "Every taskfile (Taskfile.yml + taskfiles/*.yml) carries a 3-label banner (Purpose / Depends on / Side effects) framed by one `# === ===` 77-char rule above and below the labels; no mid-file `# === <section> ===` dividers per D-02"
    - "Every executable .zsh file in install/, os/, claude/hooks/, plus shell/.zlogout and the 6 Class A heavy-banner files, carries the same 3-label banner shape; the `#!/bin/zsh` shebang stays line 1"
    - "After every banner trim on an executable .zsh file, `head -30 <file> | grep -q '^set -euo pipefail$'` succeeds (R1: LINT-04 head-30 window stays satisfied)"
    - "Every inline `#` comment removed satisfied a D-04 CUT criterion (restatement, divider, planning-tag, cross-reference, anti-pattern teaching >3 lines); every retained `#` comment satisfies D-04 KEEP rule (a) non-obvious WHY, (b) still-live footgun, or (c) lint citation"
    - "`14-METRICS.md` exists with pre/post comment-line counts per touched file and a positive aggregate `% reduction` row"
    - "`task lint && task test` is green after EVERY per-file trim commit; `task install` is green after every commit touching a taskfile in the install path (links/packages/claude/macos)"
    - "D-05 desc: sweep is a no-op for Phase 14 per RESEARCH §desc: Block Exceptions -- all desc: strings are already single-line and pass D-04 rule (a); the 197-char lint:taskfile desc stays because it documents the entire lint surface."
  artifacts:
    - path: ".planning/phases/14-comment-doc-trim/14-METRICS.md"
      provides: "Pre/post comment-to-code ratio per touched file plus aggregate row; SC#1 evidence"
      contains: "| file | code_lines | comments_pre | comments_post | delta | %_reduction |"
      min_rows: 20
    - path: "Taskfile.yml"
      provides: "Root taskfile with 3-label banner; DOTFILEDIR-leak footgun blocks at 131-148 and 240-248 retained per D-04 rule (b)"
      contains: "Purpose:"
    - path: "taskfiles/links.yml"
      provides: "Largest trim delta target; 41-line banner -> 6-line 3-label banner; 22-line LINT-02 explainer stripped per inventory; 26-line reconcile lead block stripped per R7"
      contains: "Purpose:"
    - path: "shell/.zlogout"
      provides: "51-line banner over 2-line body collapsed to 3-label banner per REVIEW row 45"
      contains: "Purpose:"
  key_links:
    - from: "14-TEACHING-INVENTORY.md §Plan 14-02 Strip Manifest"
      to: "this plan's per-file trim commits"
      via: "every row with action=strip OR action=rewrite is covered by exactly one commit; commit message references the row"
      pattern: "row reference in commit body"
    - from: "Per-file trim commit"
      to: "14-METRICS.md row update"
      via: "post-snapshot count taken at commit time; aggregate row computed in Task 4"
      pattern: "wc -l + grep -cE '^[[:space:]]*#' before/after"
    - from: "Plan 14-02 close"
      to: "Plan 14-03 entry"
      via: "all banners normalized and all inline comments trimmed; Plan 14-03 only touches docs/ + README + CLAUDE.md + .claude/CLAUDE.md delete"
      pattern: "sequential wave"
---

<objective>
Apply the trim pass: every taskfile and executable `.zsh` file gets the 3-label banner (D-01/D-02/D-03) and every inline `#` comment is evaluated against the three-test KEEP rule (D-04). The 14-TEACHING-INVENTORY.md §"Plan 14-02 Strip Manifest" (built by Plan 14-01) is this plan's per-file work breakdown.

Purpose: TRIM-01 (inline-comment ratio drops measurably per file) + TRIM-02 (banner shape consistent across files). The plan touches ~43 files but the work is mechanical per-file: read the inventory row for the file, apply the banner template + strip/rewrite the listed annotations, run the lint gate, commit.
Output: One commit per trimmed file (or per tight subsystem group), plus one final commit landing 14-METRICS.md with the post-snapshot and aggregate row.

Per-file commit discipline (Phase 11/12 D-04 + Phase 13 D-Discretion): `task lint && task test && task install` (when touching install-path files) green after every commit. No exceptions; never bypass hooks; never use `--no-verify`.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-comment-doc-trim/14-CONTEXT.md
@.planning/phases/14-comment-doc-trim/14-RESEARCH.md
@.planning/phases/14-comment-doc-trim/14-PATTERNS.md
@.planning/phases/14-comment-doc-trim/14-VALIDATION.md
@.planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md
@CLAUDE.md
@taskfiles/helpers.yml
@taskfiles/audit.yml

<interfaces>
<!-- Banner template (D-01 / D-02 / D-03 applied; from CONTEXT §specifics line 200-215). -->
<!-- Read taskfiles/audit.yml:3 and taskfiles/helpers.yml:3 to confirm the exact character -->
<!-- count of the existing 77-char rule before applying. -->

For a taskfile (`.yml`):
```yaml
version: '3'

# =============================================================================
# taskfiles/<name>.yml -- <one-line description>
#
# Purpose:      <one or two lines on what this file does>
# Depends on:   <list of files this includes or relies on; or "nothing.">
# Side effects: <what this file creates, mutates, or affects; or "none.">
# =============================================================================

includes:
  ...
```

For an executable `.zsh`:
```zsh
#!/bin/zsh

# =============================================================================
# <path/to/file.zsh> -- <one-line description>
#
# Purpose:      <one or two lines on what this script does>
# Depends on:   <list of sourced files / environment vars; or "nothing.">
# Side effects: <what this script writes / mutates; or "none.">
# =============================================================================

set -euo pipefail
```

The `set -euo pipefail` line MUST stay within the first 30 lines of the file (LINT-04 head-30 check at taskfiles/lint.yml:233). Banner trim shortens content, so this risk is the inverse of the usual concern -- the check will get easier, not harder. Verify per-file with `head -30 <file> | grep -q '^set -euo pipefail$'`.

<!-- Three-test KEEP rule (D-04; from CONTEXT §specifics line 217-229). Paste into every per-file review. -->

```
KEEP a `#` comment iff at least one is true:
  (a) Explains a non-obvious WHY (the code doesn't reveal the rationale)
  (b) Warns about a still-live footgun (a real failure the next maintainer would re-introduce)
  (c) Cites a lint rule the code satisfies (e.g., `# LINT-02: template vars only in status:`)

CUT otherwise:
  - Restatement-of-code ("# Set MACHINE to the value from manifest")
  - Section dividers inside files ("# === Vars block ===")
  - Planning-history annotations (# Phase N, # D-NN, # Gap N, # CR-NN, # WR-NN, # RESEARCH §X.Y, # PORT-NN, # BTSP-NN, # MFST-NN, # IDNT-NN)
  - Cross-references to other files for context only ("# same pattern as X.yml:42")
  - Anti-pattern teaching paragraphs longer than 3 lines (lesson migrates per D-09; covered by CLAUDE.md after Plan 14-01)
```

<!-- Files to touch (full surface). Order by heaviest-first per CONTEXT recommendation. -->
<!-- Per-file commit discipline: one commit per file (or per tight subsystem group). -->

Tier-1 (heaviest delta; do first to validate the methodology):
  1. taskfiles/links.yml          (607 lines, 230 comment lines, 41-line banner)
  2. shell/.zlogout               (51-line banner over 2-line body; REVIEW row 45)

Tier-2 (root + heavy taskfiles):
  3. Taskfile.yml                 (249 lines; KEEP 131-148, 240-248, 212-216 per D-04)
  4. taskfiles/packages.yml       (544 lines, 202 comment lines)
  5. taskfiles/identity.yml       (476 lines, 167 comment lines)
  6. taskfiles/lint.yml           (425 lines, 142 comment lines; many LINT-NN citations stay per D-04 (c); LINT-08 body+fixtures untouched per RESEARCH)
  7. taskfiles/claude.yml         (343 lines, 141 comment lines)
  8. taskfiles/macos.yml          (332 lines, 130 comment lines)
  9. taskfiles/manifest.yml       (291 lines, 122 comment lines; HIGH-density D-NN refs)
 10. taskfiles/test.yml           (328 lines, 108 comment lines)
 11. taskfiles/shell.yml          (158 lines, 68 comment lines)

Tier-3 (already-minimal taskfiles; banner-normalization pass only):
 12. taskfiles/helpers.yml        (103 lines, 27 comment lines; already lean reference)
 13. taskfiles/audit.yml          (42 lines, 14 comment lines; normalize "Callers:" -> "Depends on:")
 14. taskfiles/show.yml           (32 lines, 12 comment lines; normalize "Callers:" -> "Depends on:")
 15. taskfiles/refresh.yml        (24 lines, 11 comment lines; normalize "Callers:" -> "Depends on:")

Tier-4 (install/ scripts):
 16. install/resolver.zsh         (622 lines; WR-NN at 80,179,289,456,558,587; D-NN at 171,212,616; some KEEP per D-04 (a); see R8)
 17. install/messages.zsh
 18. install/bootstrap.zsh
 19. install/compose-brewfile.zsh
 20. install/test-hooks.zsh

Tier-5 (os/ + identity scripts):
 21. os/defaults/dock.zsh
 22. os/defaults/finder.zsh
 23. os/defaults/input.zsh
 24. os/defaults/screenshots.zsh
 25. os/defaults/security.zsh
 26. os/shell-registration.zsh
 27. identity/ssh/cloudflared.zsh

Tier-6 (shell startup, minus .zlogout already done in Tier-1):
 28. shell/.zshenv
 29. shell/.zprofile
 30. shell/.zshrc
 31. shell/.zlogin
 32. shell/theme.zsh

Tier-7 (claude/hooks/):
 33. claude/hooks/lib.zsh
 34. claude/hooks/secret-scan.zsh
 35. claude/hooks/no-emojis.zsh
 36. claude/hooks/no-ai-comments.zsh
 37. claude/hooks/agent-transparency.zsh

Tier-8 (Class A banner pass per RESEARCH §"TRIM-02 Class A Scope"; only the 6 heavy-banner files):
 38. shell/functions/_dotfiles_feature.zsh          (26-line banner)
 39. shell/functions/_dotfiles_require_feature.zsh  (34-line banner)
 40. shell/aliases/jgrid.zsh                        (15-line banner)
 41. shell/aliases/ghostty.zsh                      (13-line banner)
 42. shell/aliases/finder.zsh                       (11-line banner)
 43. shell/aliases/dotfiles.zsh                     (9-line banner)

The 19 terse function files + 3 terse alias files (per RESEARCH §"TRIM-02 Class A Scope") are NOT touched -- their existing 1-2 line purpose comments ARE the post-trim shape.

<!-- Per-file metrics measurement commands: -->
PRE:  CODE=$(grep -cvE '^[[:space:]]*(#|$)' <file>) ; COMMENTS=$(grep -cE '^[[:space:]]*#' <file>)
POST: CODE=$(grep -cvE '^[[:space:]]*(#|$)' <file>) ; COMMENTS=$(grep -cE '^[[:space:]]*#' <file>)
DELTA: comments_post - comments_pre
%_REDUCTION: 100 * (comments_pre - comments_post) / comments_pre

<!-- Files MUST NOT touch in Plan 14-02 (out of scope per CONTEXT §"Out-of-scope surfaces"): -->
- .planning/* (planning artifacts retain their planning-history tags)
- claude/agents/, claude/commands/, claude/skills/ (symlinked from marketplace)
- packages/*.rb (Brewfiles)
- manifests/defaults.toml + manifests/machines/*.toml
- The 19 terse shell/functions/*.zsh + 3 terse shell/aliases/*.zsh
- taskfiles/test/lint-fixtures/* (fixtures are testing artifacts)
- Taskfile.yml `install:` `summary: |` at lines 212-216 (KEEP per D-04 (a) RESEARCH §"desc: Block Exceptions")
- LINT-08 rule body in taskfiles/lint.yml:287-312 AND its fixtures at taskfiles/test/lint-fixtures/08*-banner-parity-* (RESEARCH §"LINT-08 Banner-Parity Impact": task-parity NOT banner-parity; no fixture changes required)

<!-- KEEP examples (D-04 rule applications already validated by RESEARCH/PATTERNS): -->
- Taskfile.yml:131-148 + 240-248 DOTFILEDIR-leak comment blocks: KEEP per D-04 (b) still-live footgun (REVIEW row 28; architectural fix deferred)
- `# lint-allow: cmds-without-status` markers across audit.yml, claude.yml, identity.yml, links.yml, macos.yml: KEEP per D-04 (c) lint citation
- Taskfile.yml install: `summary: |` at lines 212-216: KEEP per D-04 (a) (PATTERNS Pattern 3)
- LINT-NN citation comments like `# LINT-02: ...`: KEEP per D-04 (c)

<!-- Anti-pattern teaching blocks (>3 lines) explicitly slated for strip per Plan 14-01 inventory: -->
- taskfiles/links.yml:18-39 22-line LINT-02 status-block explainer (lesson covered by CLAUDE.md §Rules)
- taskfiles/links.yml:431-456 26-line reconcile lead block (lesson encoded in inline body comments per R7)
- taskfiles/manifest.yml:43-55 DOTFILEDIR resolution explainer (lesson covered by CLAUDE.md §Tooling Versions go-task row)
- install/resolver.zsh WR-NN bug-history annotations (R8: convert to D-04 (a) WHY OR strip if code now self-evident)

<!-- Commit message format (per PATTERNS Pattern 6): -->
`<type>(14-02): <imperative summary> -- <one-line rationale or row reference>`
- `<type>` = `refactor` for code-tier files (taskfiles, .zsh scripts); `docs` for the 14-METRICS.md commits
- First line <= 75 chars
- Imperative mood
- No AI attribution (commit hook enforces; never bypass with --no-verify)

Examples:
```
refactor(14-02): trim taskfiles/links.yml banner + LINT-02 explainer
refactor(14-02): collapse shell/.zlogout 51-line banner to 3-label shape
refactor(14-02): normalize taskfiles/audit.yml banner Callers -> Depends on
docs(14-02): land 14-METRICS.md post-snapshot + aggregate row
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-snapshot + create 14-METRICS.md skeleton</name>
  <files>.planning/phases/14-comment-doc-trim/14-METRICS.md</files>
  <read_first>
    - .planning/phases/14-comment-doc-trim/14-CONTEXT.md (§specifics 14-METRICS.md table shape at lines 240-247)
    - .planning/phases/14-comment-doc-trim/14-PATTERNS.md (§Pattern 5 adaptation for 14-METRICS.md; right-align numeric columns)
    - .planning/phases/14-comment-doc-trim/14-VALIDATION.md (TRIM-01 verify map)
  </read_first>
  <action>
    Take pre-snapshot measurements for every file listed in <interfaces> Tier-1 through Tier-8 (43 files total). For each file run:
    ```
    CODE=$(grep -cvE '^[[:space:]]*(#|$)' <file>)
    COMMENTS=$(grep -cE '^[[:space:]]*#' <file>)
    ```
    Blank lines excluded from `CODE` count; comment lines counted by leading-`#` after optional whitespace.

    Create `.planning/phases/14-comment-doc-trim/14-METRICS.md`:

    - Frontmatter:
      ```
      ---
      phase: 14
      slug: comment-doc-trim
      artifact: METRICS
      created: <today>
      pre_snapshot_at: <ISO date>
      post_snapshot_at: pending
      ---
      ```
    - Brief preamble (~3 lines): "Comment-to-code ratio measurements per file for Phase 14 TRIM-01 evidence. Pre-snapshot taken before any Plan 14-02 trim commit; post-snapshot taken at Plan 14-02 close. Methodology: `grep -cE '^[[:space:]]*#'` for comments; `grep -cvE '^[[:space:]]*(#|$)'` for code; right-aligned numeric columns."
    - Six-column table with header: `| file | code_lines | comments_pre | comments_post | delta | % reduction |` and right-align alignment row `|------|----------:|------------:|-------------:|------:|-----------:|`
    - One row per touched file in <interfaces> order (Tier-1 -> Tier-8). For Task 1 (pre-snapshot), fill ONLY `file`, `code_lines`, and `comments_pre`; leave `comments_post`, `delta`, `% reduction` as `TBD`.
    - Final aggregate row: `| **Total** | **<sum>** | **<sum>** | **TBD** | **TBD** | **TBD** |`

    Commit:
    ```
    docs(14-02): pre-snapshot 14-METRICS.md for trim-pass measurement
    ```

    Single commit; one file added.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-comment-doc-trim/14-METRICS.md && grep -qE '^\| file \| code_lines \| comments_pre \| comments_post \| delta \| % reduction \|' .planning/phases/14-comment-doc-trim/14-METRICS.md && ROWS=$(grep -c '^| [a-zA-Z]' .planning/phases/14-comment-doc-trim/14-METRICS.md) && [ "$ROWS" -ge 20 ] && echo "PASS ($ROWS rows)" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/14-comment-doc-trim/14-METRICS.md`
    - Six-column header present with exact column names: `file | code_lines | comments_pre | comments_post | delta | % reduction`
    - >= 20 file rows enumerated (one per touched file); aggregate row not counted toward 20
    - Every row's `code_lines` and `comments_pre` columns contain numeric values (not `TBD`)
    - `comments_post`, `delta`, `% reduction` columns contain `TBD` (post-snapshot is Task 4)
    - Aggregate row at the bottom (`**Total**`)
    - `task lint && task test` exits 0 (planning-artifact commit only)
    - Single commit with the exact message above
  </acceptance_criteria>
  <done>14-METRICS.md skeleton committed with pre-snapshot rows.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Tier-1 + Tier-2 trim pass (heaviest taskfiles, root, .zlogout)</name>
  <files>taskfiles/links.yml, shell/.zlogout, Taskfile.yml, taskfiles/packages.yml, taskfiles/identity.yml, taskfiles/lint.yml, taskfiles/claude.yml, taskfiles/macos.yml, taskfiles/manifest.yml, taskfiles/test.yml, taskfiles/shell.yml, .planning/phases/14-comment-doc-trim/14-METRICS.md</files>
  <read_first>
    - .planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md (the §"Plan 14-02 Strip Manifest" subsection is THE per-file work breakdown)
    - .planning/phases/14-comment-doc-trim/14-CONTEXT.md (D-01..D-05; §specifics banner template; three-test rule cheat sheet)
    - .planning/phases/14-comment-doc-trim/14-RESEARCH.md (§"Lint Suite Safety Under Trim" -- R1 head-30; §"TRIM-05 Strip Surface Preview" per-file density; §"desc: Block Exceptions" KEEP for Taskfile.yml summary:; §"LINT-08 Banner-Parity Impact" -- DO NOT touch LINT-08 body/fixtures)
    - .planning/phases/14-comment-doc-trim/14-PATTERNS.md (Pattern 1 banner before/after helpers.yml; Pattern 2 KEEP/CUT examples; Pattern 6 commit-message format)
    - CLAUDE.md (canonical reference for the amended banner shape; LINT-NN catalogue if extracted to .claude/rules/lint.md)
    - taskfiles/audit.yml lines 1-13 (already-target-shape banner exemplar)
    - taskfiles/helpers.yml lines 1-23 (lean reference + target post-trim shape per PATTERNS Pattern 1)
    - Each target file in full before editing
  </read_first>
  <action>
    For each of the 11 files in this tier, apply the trim methodology and commit individually.

    **Per-file trim loop:**
    1. Read the target file in full
    2. Look up the file's rows in `14-TEACHING-INVENTORY.md` §"Plan 14-02 Strip Manifest" to get its strip/keep/rewrite action list
    3. Replace the existing top-of-file banner with the 3-label template from <interfaces>. Preserve `version: '3'` line and `#!/bin/zsh` shebang where applicable; the banner goes immediately after them.
       - `Purpose:` (1-2 lines on what the file does; written fresh from reading the body, NOT copied from old banner)
       - `Depends on:` (included taskfiles for .yml; sourced files / env vars for .zsh; or `nothing.`). Per D-01 the label is `Depends on:`, NOT `Callers:`.
       - `Side effects:` (what the file mutates; or `none.`)
       - Three labels only; no `Tasks:` sub-list, no narrative prose, no examples, no change-history (D-03)
       - One `# ============================================================================= ` rule above and below (verify exact char count matches `taskfiles/audit.yml:3`)
    4. For each inline `#` comment, apply D-04:
       - KEEP if (a) non-obvious WHY, (b) still-live footgun, (c) lint citation
       - CUT if restatement, divider, planning-history tag, cross-file reference, anti-pattern teaching >3 lines
       - REWRITE per inventory action (e.g., `taskfiles/manifest.yml:71-77` PATTERNS Pattern 2 KEEP-(a): keep WHY about stdin handling, strip `WR-08 / IN-02` planning tag)
    5. Remove every mid-file `# === <section> ===` divider (D-02 prohibits these)
    6. **Taskfile.yml ONLY:** preserve verbatim DOTFILEDIR-leak blocks at lines 131-148 and 240-248 (D-04 (b)) AND the `install:` `summary: |` block at lines 212-216 (D-04 (a))
    7. **taskfiles/lint.yml ONLY:** preserve verbatim the LINT-08 rule body at lines 287-312 (RESEARCH §"LINT-08 Banner-Parity Impact" -- task-parity not banner-parity; no fixture changes); LINT-NN citation comments throughout stay (D-04 (c))
    8. Verification gate (per per-file commit):
       ```
       task lint
       task test
       # For install-path files (links.yml, packages.yml, claude.yml, macos.yml):
       task install
       ```
       All must exit 0. If any fails, fix; never bypass hooks.
    9. For executable `.zsh` (this task: `shell/.zlogout`), additionally:
       ```
       head -30 shell/.zlogout | grep -q '^set -euo pipefail$'
       ```
       (R1 LINT-04 head-30 sanity)
    10. Update the file's row in `14-METRICS.md`:
        ```
        comments_post = grep -cE '^[[:space:]]*#' <file>
        delta = comments_post - comments_pre   (expected negative)
        % reduction = 100 * (comments_pre - comments_post) / comments_pre  (rounded int)
        ```
    11. Commit. Message per PATTERNS Pattern 6:
        ```
        refactor(14-02): trim <file> banner + inline comments

        -- banner: <N>-line -> <M>-line (3-label shape)
        -- inline: <X> comment lines stripped (D-04 CUT); <Y> retained (D-04 (a)/(b)/(c))
        -- TEACHING-INVENTORY rows: <strip row IDs>, <rewrite row IDs>, <keep row IDs>
        -- task lint && task test [&& task install] green
        ```
        Use `git commit -m "$(cat <<'EOF' ... EOF)"`. No AI attribution.

    **File order (heaviest first):**

    Commit 1 -- `taskfiles/links.yml`: 41-line banner -> ~7 lines. Strip 22-line LINT-02 explainer at 18-39 (covered by CLAUDE.md §Rules per inventory row 1). Strip 26-line reconcile lead block at 431-456 (lesson encoded in inline body comments per R7). Rewrite PORT-01 block at 167-183 per PATTERNS Pattern 2 CUT example (keep LINT-02 + world-readable rationale; strip PORT-01/D-04/v1/D-03 tags). Verify: `task lint && task test && task install`.

    Commit 2 -- `shell/.zlogout`: collapse 51-line banner over 2-line body to 6-10 line 3-label banner. The "what fc -W does" lesson is generic zsh tutorial content per RESEARCH §TRIM-05 #7 -- do NOT migrate to CLAUDE.md. Strip-clean. Verify: `task lint && task test`.

    Commit 3 -- `Taskfile.yml`: trim normally EXCEPT preserve 131-148, 240-248, 212-216 verbatim. Verify: `task lint && task test && task install`.

    Commit 4 -- `taskfiles/packages.yml`: full gate including `task install`.

    Commit 5 -- `taskfiles/identity.yml`: full gate including `task install`.

    Commit 6 -- `taskfiles/lint.yml`: most comments are LINT-NN citations (D-04 (c); KEEP). Banner trims; rule bodies untouched. LINT-08 body+fixtures NEVER touched. Verify: `task lint && task test`.

    Commit 7 -- `taskfiles/claude.yml`: full gate including `task install`.

    Commit 8 -- `taskfiles/macos.yml`: full gate including `task install`.

    Commit 9 -- `taskfiles/manifest.yml`: highest D-NN density. DOTFILEDIR resolution explainer at 43-55 collapses per PATTERNS Pattern 2 CUT. WR-08 stdin handling at 71-77 rewrites per Pattern 2 KEEP-(a). Verify: `task lint && task test`.

    Commit 10 -- `taskfiles/test.yml`: `task lint && task test`.

    Commit 11 -- `taskfiles/shell.yml`: `task lint && task test`.

    After all 11 commits, the 14-METRICS.md table has post-snapshot data for these 11 files (Task 4 computes the aggregate row at plan close).
  </action>
  <verify>
    <automated>BAD=0; for f in taskfiles/links.yml taskfiles/packages.yml taskfiles/identity.yml taskfiles/lint.yml taskfiles/claude.yml taskfiles/macos.yml taskfiles/manifest.yml taskfiles/test.yml taskfiles/shell.yml Taskfile.yml; do head -15 "$f" | grep -q "^# Purpose:" || { echo "MISSING Purpose: $f"; BAD=1; }; head -15 "$f" | grep -q "^# Depends on:" || { echo "MISSING Depends on: $f"; BAD=1; }; head -15 "$f" | grep -q "^# Side effects:" || { echo "MISSING Side effects: $f"; BAD=1; }; done; head -15 shell/.zlogout | grep -q "^# Purpose:" || { echo "MISSING Purpose: shell/.zlogout"; BAD=1; }; task lint && task test || BAD=1; [ "$BAD" = "0" ] && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 11 files carries a 3-label banner in its first 15 lines (verified by `head -15 <file> | grep -q "^# Purpose:"` AND `head -15 <file> | grep -q "^# Depends on:"` AND `head -15 <file> | grep -q "^# Side effects:"` for each)
    - `head -15 shell/.zlogout | grep -q "^# Purpose:"` succeeds
    - `head -30 shell/.zlogout | grep -q '^set -euo pipefail$'` succeeds (R1 LINT-04)
    - `head -30 taskfiles/links.yml | grep -cE '^# =+$' | grep -qE '^2$'` (exactly 2 `# ===` rules in the banner)
    - Mid-file divider check (D-02): `grep -cE '^# ===.*===' taskfiles/links.yml` <= 2 (only the banner above/below, no mid-file dividers)
    - Same mid-file check for all 11 files: count of `# === ... ===` lines is exactly 2
    - `grep -q "DOTFILEDIR pollution workaround" Taskfile.yml` succeeds (D-04 (b) footgun block at 131-148 preserved)
    - `grep -q "task install IS task update" Taskfile.yml` succeeds (install: summary: at 212-216 preserved)
    - `grep -q "banner-parity" taskfiles/lint.yml` succeeds (LINT-08 rule body untouched)
    - `task lint && task test && task install` exits 0 after the final commit
    - 11 commits between Plan 14-01 close and this task's end, each with `refactor(14-02):` message prefix and HEREDOC-formatted body
    - `14-METRICS.md` has `comments_post` filled (not `TBD`) for all 11 files trimmed in this task
    - No commit message contains AI attribution (`grep -iE "co-authored.*claude|generated by|written by ai" $(git log --since='Plan 14-02 start' --format=%H | xargs -I{} git log -1 --format=%B {})` returns empty)
  </acceptance_criteria>
  <done>11 trim commits land sequentially; each leaves lint+test (and install where applicable) green; banner shape consistent across all 11 files; DOTFILEDIR + summary + LINT-08 carve-outs preserved verbatim; 14-METRICS.md updated row-by-row.</done>
</task>

<task type="auto">
  <name>Task 3: Apply Tier-3..Tier-8 trim pass (already-minimal taskfiles, install/, os/, shell startup, hooks, Class A banner pass)</name>
  <files>
    taskfiles/helpers.yml, taskfiles/audit.yml, taskfiles/show.yml, taskfiles/refresh.yml,
    install/resolver.zsh, install/messages.zsh, install/bootstrap.zsh, install/compose-brewfile.zsh, install/test-hooks.zsh,
    os/defaults/dock.zsh, os/defaults/finder.zsh, os/defaults/input.zsh, os/defaults/screenshots.zsh, os/defaults/security.zsh,
    os/shell-registration.zsh, identity/ssh/cloudflared.zsh,
    shell/.zshenv, shell/.zprofile, shell/.zshrc, shell/.zlogin, shell/theme.zsh,
    claude/hooks/lib.zsh, claude/hooks/secret-scan.zsh, claude/hooks/no-emojis.zsh, claude/hooks/no-ai-comments.zsh, claude/hooks/agent-transparency.zsh,
    shell/functions/_dotfiles_feature.zsh, shell/functions/_dotfiles_require_feature.zsh,
    shell/aliases/jgrid.zsh, shell/aliases/ghostty.zsh, shell/aliases/finder.zsh, shell/aliases/dotfiles.zsh,
    .planning/phases/14-comment-doc-trim/14-METRICS.md
  </files>
  <read_first>
    - .planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md §"Plan 14-02 Strip Manifest" (per-file action list)
    - .planning/phases/14-comment-doc-trim/14-CONTEXT.md (banner template + D-04 rules)
    - .planning/phases/14-comment-doc-trim/14-RESEARCH.md (§"TRIM-02 Class A Scope" -- only 6 Class A files need banner edits; rest of Class A is no-op; R8 install/resolver.zsh WR-NN guidance: convert subtle ones to D-04 (a) comment, strip if code self-evident)
    - .planning/phases/14-comment-doc-trim/14-PATTERNS.md (Pattern 1 banner shape; Pattern 6 commit format)
    - CLAUDE.md (canonical reference)
    - Each target file in full before editing
  </read_first>
  <action>
    Apply the same per-file trim loop from Task 2 to each file in this task's <files> list, in the tier order from <interfaces>. Commit per file (or per tight subsystem group: e.g., the 4 Tier-3 already-minimal taskfiles may be one commit since each is a one-line banner relabel; the 5 hooks may be one commit; the 6 Class A files may be one commit).

    **Tier-3 (commit 1 -- one commit for all 4):** `taskfiles/{helpers,audit,show,refresh}.yml`
    - These already have the 3-label shape per PATTERNS Pattern 1 secondary analog. Normalize the `Callers:` label to `Depends on:` per D-01.
    - For `taskfiles/audit.yml`: line 10 reads `# Callers: Invoked directly by operator; included by Taskfile.yml.` -> `# Depends on: included by Taskfile.yml; invoked by operator.`
    - Similar normalization for `taskfiles/show.yml:10` and `taskfiles/refresh.yml:10` (read each to find the exact current label)
    - For `taskfiles/helpers.yml`: per PATTERNS Pattern 1, the current banner at lines 1-23 narrates usage. D-03 strips the usage example. Apply the post-trim shape from PATTERNS Pattern 1.
    - For `taskfiles/audit.yml`: the `Tasks:` sub-list at lines 9-12 is borderline-narrative under D-03; per PATTERNS the planner judges. RECOMMENDED: cut the `Tasks:` sub-list (it duplicates `task --list` and contains `D-02`, `D-03` planning tags that trip TRIM-05). The 3 labels suffice.
    - Also strip `# lint-allow: cmds-without-status` markers? NO -- KEEP per D-04 (c) and per RESEARCH §"Lint Suite Safety" line 165 (these are functional pragmas).
    - One commit covering 4 files: `refactor(14-02): normalize Tier-3 taskfile banners to D-01 Depends-on label`

    **Tier-4 (5 commits, one per install/ script):**
    - `install/resolver.zsh` (commit): the densest TRIM-05 surface in install/. Apply R8 guidance: for each WR-NN annotation at lines 80,179,289,456,558,587, evaluate whether the lesson is self-evident from current code shape. If yes -> strip the comment entirely. If no (the lesson is subtle, e.g., signal-trap-before-pipeline at :456) -> rewrite as a D-04 (a) "non-obvious WHY" comment with the WR-NN tag removed. For the D-NN refs at :171,212,616: D-01 (platform.os = darwin) is in CLAUDE.md §What This Is -> strip; D-16 (cross-field rules) is now in CLAUDE.md per Plan 14-01 gap-fill -> strip; D-04 (advisory warnings) -> evaluate per D-04. Also banner trim. Verify: `task lint && task test && task install` (resolver.zsh runs as part of `task setup` and `task manifest:resolve` which install invokes implicitly).
    - `install/messages.zsh` (commit): banner + inline trim. Verify: `task lint && task test && task install` (messages.zsh is sourced by Taskfile.yml :: install).
    - `install/bootstrap.zsh` (commit): banner + inline trim. Verify: `task lint && task test` (bootstrap runs once on fresh machines; not part of repeat `task install`).
    - `install/compose-brewfile.zsh` (commit): banner + inline trim. Verify: `task lint && task test && task install`.
    - `install/test-hooks.zsh` (commit): banner + inline trim. Verify: `task lint && task test`.

    **Tier-5 (7 commits, one per os/ or identity script):**
    - `os/defaults/dock.zsh` through `os/defaults/security.zsh` (5 commits): each banner trim + inline trim. Verify: `task lint && task test`. Skip `task install` per file -- the macos defaults tasks are gated by feature flags so re-running is feature-conditional; full install runs after Tier-5 closes.
    - `os/shell-registration.zsh` (commit): banner + inline trim. Verify: `task lint && task test`.
    - `identity/ssh/cloudflared.zsh` (commit): banner + inline trim. Verify: `task lint && task test`.

    **Tier-6 (5 commits, one per shell startup file):**
    - `shell/.zshenv`, `shell/.zprofile`, `shell/.zshrc`, `shell/.zlogin`, `shell/theme.zsh` (5 commits): each banner trim + inline trim. For each: `head -30 <file> | grep -q '^set -euo pipefail$'` MUST pass (R1 LINT-04 head-30 sanity). The `.zsh*` files are sourced not executed but still carry the convention. Verify per-file: `task lint && task test`.

    **Tier-7 (1 commit for 5 hooks):** `claude/hooks/{lib,secret-scan,no-emojis,no-ai-comments,agent-transparency}.zsh` -- the 5 hook scripts. Banner trim + inline trim. Verify: `task lint && task test && task test:hooks`. Single commit because hooks are tight-coupled; lib.zsh is sourced by the others.

    **Tier-8 (1 commit for 6 Class A files):** `shell/functions/{_dotfiles_feature,_dotfiles_require_feature}.zsh` + `shell/aliases/{jgrid,ghostty,finder,dotfiles}.zsh` -- the 6 heavy-banner files. Banner trim ONLY (no inline trim per CONTEXT Claude's Discretion). The lesson "feature-gating via `_dotfiles_feature <key>`" is in CLAUDE.md §Adding Things `Feature flag` bullet -- safe to strip the long teaching banner. Verify: `task lint && task test`. Single commit.

    After every commit, update the corresponding row(s) in `14-METRICS.md` (`comments_post`, `delta`, `% reduction`).

    Total commits in this task: 1 (Tier-3) + 5 (Tier-4) + 7 (Tier-5) + 5 (Tier-6) + 1 (Tier-7) + 1 (Tier-8) = 20 commits.

    R1 sanity (LINT-04 head-30) verification command across all `.zsh` files touched in this task:
    ```
    for f in install/*.zsh os/defaults/*.zsh os/shell-registration.zsh identity/ssh/cloudflared.zsh shell/.z*.* shell/theme.zsh claude/hooks/*.zsh shell/functions/_dotfiles_feature.zsh shell/functions/_dotfiles_require_feature.zsh; do head -30 "$f" 2>/dev/null | grep -q '^set -euo pipefail$' || echo "FAIL R1: $f"; done
    ```
    Expect empty output. (Note: `shell/.zshenv` and similar `.z*` startup files may not all have `set -euo pipefail` -- verify per-file per CLAUDE.md rule; if a file doesn't have it today, banner trim doesn't introduce it.)
  </action>
  <verify>
    <automated>BAD=0; for f in taskfiles/helpers.yml taskfiles/audit.yml taskfiles/show.yml taskfiles/refresh.yml install/resolver.zsh install/messages.zsh install/bootstrap.zsh install/compose-brewfile.zsh install/test-hooks.zsh os/defaults/dock.zsh os/defaults/finder.zsh os/defaults/input.zsh os/defaults/screenshots.zsh os/defaults/security.zsh os/shell-registration.zsh identity/ssh/cloudflared.zsh shell/.zshenv shell/.zprofile shell/.zshrc shell/.zlogin shell/theme.zsh claude/hooks/lib.zsh claude/hooks/secret-scan.zsh claude/hooks/no-emojis.zsh claude/hooks/no-ai-comments.zsh claude/hooks/agent-transparency.zsh shell/functions/_dotfiles_feature.zsh shell/functions/_dotfiles_require_feature.zsh shell/aliases/jgrid.zsh shell/aliases/ghostty.zsh shell/aliases/finder.zsh shell/aliases/dotfiles.zsh; do head -20 "$f" | grep -q "^# Purpose:" || { echo "MISSING Purpose: $f"; BAD=1; }; done; for f in install/*.zsh os/defaults/*.zsh os/shell-registration.zsh identity/ssh/cloudflared.zsh claude/hooks/*.zsh shell/functions/_dotfiles_feature.zsh shell/functions/_dotfiles_require_feature.zsh; do head -30 "$f" 2>/dev/null | grep -q '^set -euo pipefail$' || { echo "FAIL R1: $f"; BAD=1; }; done; task lint && task test && task install || BAD=1; [ "$BAD" = "0" ] && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 32 files touched in this task carries a 3-label banner in its first 20 lines (`head -20 <file> | grep -q "^# Purpose:"` for each)
    - R1 head-30 sanity passes for every executable `.zsh` in install/, os/, os/defaults/, claude/hooks/, plus `_dotfiles_feature.zsh` and `_dotfiles_require_feature.zsh` (`head -30 <file> | grep -q '^set -euo pipefail$'`)
    - `taskfiles/audit.yml`, `taskfiles/show.yml`, `taskfiles/refresh.yml`, `taskfiles/helpers.yml` use the `Depends on:` label (not `Callers:`) -- verified by `grep -c '^# Callers:' taskfiles/{audit,show,refresh,helpers}.yml` returns `0` for each
    - `task lint && task test && task install && task validate` exits 0 (full suite after all 20 commits)
    - `task test:hooks` exits 0 (hook smoke tests pass after Tier-7 trim)
    - 20 commits between Task 2 end and this task's end, each with `refactor(14-02):` prefix
    - `14-METRICS.md` has `comments_post` filled (not `TBD`) for every file touched in Tasks 2+3
    - No commit message contains AI attribution
  </acceptance_criteria>
  <done>All 32 Tier-3..Tier-8 files trimmed and committed; banner shape consistent across the whole repo; R1 head-30 sanity passes on every executable .zsh; full suite green after the final commit.</done>
</task>

<task type="auto">
  <name>Task 4: Post-snapshot + 14-METRICS.md aggregate row + plan-close gate</name>
  <files>.planning/phases/14-comment-doc-trim/14-METRICS.md</files>
  <read_first>
    - .planning/phases/14-comment-doc-trim/14-METRICS.md (the per-file rows filled by Tasks 2 and 3)
    - .planning/phases/14-comment-doc-trim/14-VALIDATION.md (TRIM-01 success criterion: "ratio drops measurably")
  </read_first>
  <action>
    Verify every row in 14-METRICS.md has `comments_post`, `delta`, `% reduction` filled (no `TBD` in those columns).

    Compute the aggregate `**Total**` row:
    ```
    Total code_lines = sum of all per-file code_lines (pre = post; should be roughly equal since trim removes comments not code -- but blank lines may shift slightly)
    Total comments_pre = sum of all comments_pre
    Total comments_post = sum of all comments_post
    Total delta = Total comments_post - Total comments_pre  (negative; e.g., -800)
    Total % reduction = 100 * (Total comments_pre - Total comments_post) / Total comments_pre  (e.g., 45)
    ```

    Update the `**Total**` row in 14-METRICS.md with the computed values.

    Update the frontmatter `post_snapshot_at: pending` -> `post_snapshot_at: <today ISO>`.

    Sanity check: `Total % reduction` MUST be > 0 (otherwise TRIM-01 SC#1 "measurably falls" is unsatisfied). RESEARCH baseline implies 30-70% reduction is realistic.

    Commit:
    ```
    docs(14-02): land 14-METRICS.md post-snapshot + aggregate row

    -- aggregate: <N> comment lines trimmed across <M> files (<P>% reduction)
    -- TRIM-01 SC#1 satisfied: comment-to-code ratio measurably dropped
    ```

    Run a final full-suite gate:
    ```
    task lint && task test && task install && task validate
    ```
    All must exit 0. If `task install` is slow on a cold cache, retry once with cache primed. If still failing, diagnose -- a trim commit may have introduced a regression and must be fixed before Plan 14-02 closes.
  </action>
  <verify>
    <automated>grep -c 'TBD' .planning/phases/14-comment-doc-trim/14-METRICS.md | grep -qE '^0$' && grep -qE '^\| \*\*Total\*\* \| \*\*[0-9]+\*\* \| \*\*[0-9]+\*\* \| \*\*[0-9]+\*\* \| \*\*-?[0-9]+\*\* \| \*\*-?[0-9]+%?\*\*' .planning/phases/14-comment-doc-trim/14-METRICS.md && task lint && task test && task install && task validate && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TBD' .planning/phases/14-comment-doc-trim/14-METRICS.md` returns `0` (every TBD resolved)
    - Aggregate `**Total**` row exists with numeric values in every column
    - `Total % reduction` > 0 (TRIM-01 SC#1 "measurably falls" satisfied; quantitative evidence)
    - Frontmatter `post_snapshot_at` is a date (not `pending`)
    - Full suite green: `task lint && task test && task install && task validate` exits 0
    - Single commit with `docs(14-02):` prefix, no AI attribution
  </acceptance_criteria>
  <done>14-METRICS.md aggregate row populated; TRIM-01 quantitative evidence on disk; full suite green; Plan 14-02 closed and Plan 14-03 ready to start.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo author -> repo state | Plan 14-02 edits ~43 files but changes zero code paths -- only comment text and banner blocks. No execution surface added or removed. No external input. The `task install`/`lint`/`test` gates after every commit verify no functional regression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-LL | Tampering | inline-comment removal could strip a still-live footgun warning | mitigate | D-04 KEEP rule (b); PATTERNS Pattern 2 KEEP-(b) example calls out Taskfile.yml:131-148 + 240-248 explicitly. Plan 14-01 inventory pre-classifies each annotation block; this plan only acts on inventory rows marked `strip` or `rewrite`. |
| T-14-02-L4 | Denial of service | LINT-04 head-30 window breaks if banner trim shifts `set -euo pipefail` outside lines 1-30 | mitigate | R1 mitigation: trim SHORTENS banners so `set -euo pipefail` moves UP; per-file verify `head -30 <file> | grep -q '^set -euo pipefail$'` after every banner edit. |
| T-14-02-IT | Tampering | per-file commits batched too aggressively could leave a broken intermediate state | accept | Plan enforces per-file commit discipline (`task lint && task test` green after EVERY commit); rejection at hook time means fix-and-recommit, never bypass. |
| T-14-02-LD | Tampering | the deferred LINT-08 carve-out (RESEARCH §"LINT-08 Banner-Parity Impact") could be violated if a refactorer "fixes" what looks like an inconsistent banner-shape rule | accept | Plan text explicitly forbids touching LINT-08 body and fixtures (Task 2 + Task 3 read_first cites the research verdict). The lint suite would catch a functional break. |

No new attack surface. Risk class: doc-drift / lesson-loss (mitigated by Plan 14-01 inventory + this plan's KEEP-rule discipline).
</threat_model>

<verification>
- `head -15 <file> | grep -q "^# Purpose:"` for every file in `files_modified` (banner shape applied)
- `grep -cE '^# =+$' <file>` is exactly `2` for every taskfile touched (one rule above + one below; no mid-file dividers per D-02)
- `head -30 <file> | grep -q '^set -euo pipefail$'` for every executable .zsh touched (R1 LINT-04)
- `grep -q "DOTFILEDIR pollution workaround" Taskfile.yml` succeeds (D-04 (b) preserved)
- `grep -q "task install IS task update" Taskfile.yml` succeeds (install: summary: preserved)
- `grep -q "banner-parity" taskfiles/lint.yml` succeeds (LINT-08 rule body untouched)
- `grep -c 'TBD' .planning/phases/14-comment-doc-trim/14-METRICS.md` returns `0` (all post-snapshot values filled)
- Aggregate `**Total**` row has `% reduction > 0` (TRIM-01 SC#1 satisfied)
- `task lint && task test && task install && task validate` exits 0 after final commit
- 32 commits total across Tasks 1-4 (1 + 11 + 20 + 1 -- or per-tight-group consolidations as scoped in Task 3); each commit has lint+test green; no AI attribution in any commit message
</verification>

<success_criteria>
- TRIM-01: inline comments reduced; 14-METRICS.md aggregate `% reduction` > 0 with positive per-file deltas
- TRIM-02: 3-label banner shape consistent across every taskfile and every executable `.zsh` script in scope; one `# === ===` rule above and below; no mid-file dividers
- D-04 KEEP carve-outs preserved verbatim: Taskfile.yml:131-148, 240-248, 212-216; LINT-NN citations; `# lint-allow: cmds-without-status` pragmas
- LINT-08 body + fixtures untouched (RESEARCH §"LINT-08 Banner-Parity Impact" enforced)
- R1 LINT-04 head-30 sanity passes on every executable .zsh
- `task lint && task test && task install && task validate` green after every commit and at plan close
- Plan 14-03 unblocked: only docs/ + README + CLAUDE.md remain for the doc-tier trim and the SC#5 grep gate
</success_criteria>

<output>
Create `.planning/phases/14-comment-doc-trim/14-02-SUMMARY.md` when done. Summary must include:
- Total commits made across this plan
- Total comment lines stripped (from 14-METRICS.md aggregate)
- Aggregate `% reduction`
- Per-tier breakdown (Tier-1..Tier-8 counts)
- Files where KEEP carve-outs were preserved verbatim (Taskfile.yml lines + LINT-08 in lint.yml)
- Confirmation that Plan 14-03 is unblocked (only docs/ + README + CLAUDE.md left; SC#5 grep gate is Plan 14-03's closing assertion)
- Final `task lint && task test && task install && task validate` exit code
</output>

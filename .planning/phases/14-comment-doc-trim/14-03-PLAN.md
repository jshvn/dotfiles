---
phase: 14-comment-doc-trim
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - docs/MANIFEST.md
  - docs/README.md
  - docs/SECURITY.md
  - docs/MACHINES.md
  - README.md
  - .claude/CLAUDE.md
autonomous: true
requirements: [TRIM-03, TRIM-04, TRIM-05]
requirements_addressed: [TRIM-03, TRIM-04, TRIM-05]
tags: [doc-trim, readme-dedup, claude-md-delete, sc5-grep-gate]

must_haves:
  truths:
    - "docs/MANIFEST.md no longer contains the deleted `motd` feature flag at any of its 5 known sites (lines 33, 67, 137, 152, 491); `grep -n motd docs/MANIFEST.md` returns 0 hits"
    - "docs/MANIFEST.md no longer contains the stale 'Phase 1 note' admonition (lines 461-464); CLI Reference examples use the public Phase 12 surface (`task setup`, `task show:manifest`, `task audit:manifest`, `task test:manifest`) instead of the Phase-1-era `task -t taskfiles/manifest.yml ...` form"
    - "docs/README.md no longer references the deleted docs (MIGRATION.md, CUTOVER.md) -- the index lists only the 3 surviving docs"
    - "README.md is humans-only per D-06 (install path + 5-command surface + docs links + 'see CLAUDE.md for contribution rules' pointer); does NOT contain manifest-model deep-dive, kebab-case-index rule, status-block rule, or other AI/contributor reference material (those live in CLAUDE.md)"
    - "`.claude/CLAUDE.md` is deleted from the repo (`test ! -f .claude/CLAUDE.md`); no fallback 'thin pointer' file replaces it (D-07 PASS per RESEARCH)"
    - "SC#5 grep gate returns zero matches in code: `git grep -E 'v1 (bug|finding|leftover)|Gap [0-9]+|D-[0-9]+|UAT [Gg]ap' -- ':!.planning/'` outputs nothing (`|| echo PASS` prints 'PASS')"
    - "README.md/CLAUDE.md header-overlap diff is minimal: `diff <(grep '^##' README.md) <(grep '^##' CLAUDE.md) | wc -l` shows <= 2 shared `## headers (D-06 single-canonical-home)"
    - "`task lint && task test && task install && task validate` green after every commit; the final commit's pre-push hook (or post-commit) passes the green-tree gate"
  artifacts:
    - path: "docs/MANIFEST.md"
      provides: "Manifest schema reference with all motd drift removed + CLI examples rewritten to public surface"
      contains: "task show:manifest"
      excludes_patterns: ["motd = true", "Phase 1 note", "task -t taskfiles/manifest.yml"]
    - path: "docs/README.md"
      provides: "docs/ directory index pointing only to the 3 surviving docs (MANIFEST.md, SECURITY.md, MACHINES.md)"
      excludes_patterns: ["MIGRATION.md", "CUTOVER.md"]
    - path: "README.md"
      provides: "Humans-only entry point: install + 5-command surface + docs links + CLAUDE.md pointer"
      contains: "task install"
    - path: "CLAUDE.md"
      provides: "Canonical AI/contributor reference (no changes in 14-03; the file was finalized in 14-01)"
      contains: "Purpose / Depends on / Side effects"
  key_links:
    - from: "README.md ## Contributing section"
      to: "CLAUDE.md"
      via: "explicit link `[CLAUDE.md](CLAUDE.md)` for conventions, rules, where-to-add"
      pattern: "\\[CLAUDE.md\\]\\(CLAUDE.md\\)"
    - from: "docs/MANIFEST.md ## Adding a New Machine"
      to: "public task surface"
      via: "task setup -- <name>; task show:manifest; task audit:manifest"
      pattern: "task setup"
    - from: "Plan 14-03 close (SC#5 grep gate PASS)"
      to: "Phase 14 completion + v2.1 milestone close"
      via: "ROADMAP Phase 14 SC#1-5 all satisfied"
      pattern: "phase status -> Complete"
---

<objective>
Close Phase 14 by trimming the doc-tier surface: fix docs/MANIFEST.md drift (motd flag + Phase-1 note + CLI form), rewrite docs/README.md, deduplicate README.md against CLAUDE.md, delete `.claude/CLAUDE.md`, and run the final SC#5 grep gate. This plan is the final wave of Phase 14; its closing assertion is the SC#5 grep returning zero matches in code.

Purpose: TRIM-03 (docs/ accuracy review) + TRIM-04 (README/CLAUDE/.claude dedup) + TRIM-05 (SC#5 grep gate). RESEARCH §"docs/ Review Findings" provides line-by-line edit lists for MANIFEST.md; D-07 PASS confirms `.claude/CLAUDE.md` deletion is safe with no fallback; PATTERNS Pattern 4 gives the README humans-only target shape.
Output: 4 doc commits + 1 README rewrite commit + 1 deletion commit + 1 SC#5 grep gate verification commit (the verification can fold into the deletion commit; planner picks). The closing assertion is the SC#5 grep producing zero output.
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-comment-doc-trim/14-CONTEXT.md
@.planning/phases/14-comment-doc-trim/14-RESEARCH.md
@.planning/phases/14-comment-doc-trim/14-PATTERNS.md
@.planning/phases/14-comment-doc-trim/14-VALIDATION.md
@CLAUDE.md
@README.md
@.claude/CLAUDE.md
@docs/MANIFEST.md
@docs/README.md
@docs/SECURITY.md
@docs/MACHINES.md

<interfaces>
<!-- docs/MANIFEST.md surgical edit list (RESEARCH §"docs/ Review Findings" + PATTERNS Pattern 4): -->

motd doc-drift sites (delete each line; preserve surrounding example syntax):
  - Line 33: `motd = true` inside the `defaults.toml` example under `[features]`
  - Line 67: `motd = true` inside the `personal-laptop.toml` example
  - Line 137: `motd = true` inside Fixture 01 deep-merge example (defaults.toml side)
  - Line 152: `"motd": true,` inside Fixture 01 resolved.json example
  - Line 491: the `motd` row in the Feature-Flag Reference table

Stale Phase-1 admonition (delete + rewrite):
  - Lines 461-464: the "**Phase 1 note:** Until Phase 2 wires the manifest module..." admonition. Delete entirely.
  - Following CLI examples (lines 461-471 region): rewrite the `task -t taskfiles/manifest.yml manifest:validate -- --machine <name>` form to the public Phase 12 surface: `task audit:manifest -- --machine <name>` (per CONTEXT line 218 + RESEARCH R4).

"Adding a New Machine" section CLI rewrites (lines 382-419):
  - `task -t taskfiles/manifest.yml setup -- <name>` -> `task setup -- <name>`
  - `task -t taskfiles/manifest.yml manifest:show` -> `task show:manifest` (or `task show:manifest -- --machine <name>` if the original took a machine arg)
  - `task -t taskfiles/manifest.yml manifest:validate -- --machine <name>` -> `task audit:manifest -- --machine <name>`
  - `task -t taskfiles/manifest.yml manifest:test*` -> `task test:manifest` / `task test:add-machine` (per Phase 12 D-09 rename)

Optional Phase-N forward-look strip (RESEARCH Open Question 2; recommendation: strip):
  - Line 33 area: `# Bundle names map to packages/<name>.rb (Phase 5).` -> `# Bundle names map to packages/<name>.rb.`
  - Other `(Phase N)` parentheticals inside fenced toml blocks: same treatment

KEEP unchanged:
  - Schema tables, merge-semantics rules, Fixtures 01-06 worked examples (minus the motd lines), Verify model section

<!-- docs/README.md rewrite (RESEARCH §"docs/ Review Findings"): -->

Current state: 7 lines listing MANIFEST.md (P1), SECURITY.md (P2), MIGRATION.md, MACHINES.md, CUTOVER.md (P8).

Target state (3 bullets; MIGRATION.md + CUTOVER.md removed; phase parentheticals dropped):
```markdown
# docs

- `MANIFEST.md` -- manifest schema, inheritance rules, worked examples
- `SECURITY.md` -- bootstrap trust chain
- `MACHINES.md` -- per-machine inventory
```

<!-- docs/SECURITY.md optional polish (RESEARCH Open Question 3; planner judgement): -->

Optional past-tense rewrite for "What This Document Does NOT Cover" section if it uses future tense for completed phases. Per RESEARCH this is stylistic, not drift. PLANNER ACTION: SKIP unless the past-tense rewrite is a 1-2 line touch -- otherwise leave as-is to keep the commit count tight. Document the choice in the commit message.

<!-- docs/MACHINES.md: NO CHANGES per RESEARCH (currently accurate). -->

<!-- README.md humans-only target shape (CONTEXT §specifics line 263-284; PATTERNS Pattern 4 quoted excerpt): -->

```markdown
# Dotfiles

macOS dotfiles managed with go-task, symlinks, and XDG base directory spec.

## Install

```zsh
git clone <repo-url>
./bootstrap.zsh
task setup -- <machine-name>
task install
```

## Common Tasks

The five top-level commands are:

| Command          | Purpose                                                |
|------------------|--------------------------------------------------------|
| `task install`   | Install dotfiles for the active machine                |
| `task setup`     | Set the active machine: `task setup -- <machine-name>` |
| `task validate`  | Validate full installation state                       |
| `task test`      | Run all smoke tests                                    |
| `task lint`      | Run all lint checks                                    |

Run `task` (no arguments) to see the curated task surface; `task --list` for the full graph.

## Where things live

- `docs/MANIFEST.md` -- manifest schema, inheritance rules, worked examples
- `docs/SECURITY.md` -- bootstrap trust chain and SSH key handling
- `docs/MACHINES.md` -- per-machine purpose and hardware

## Contributing

See [CLAUDE.md](CLAUDE.md) for conventions, rules, where-to-add tables, and lint catalogue.
```

REMOVE from current README.md:
- The "What This Is" deep-dive on manifest model (lines 3-20) -- redirect to CLAUDE.md via the Contributing pointer
- The "Where to Add Things" table (lines 61-72) -- duplicate of CLAUDE.md §Where to Add Things; the Contributing pointer covers it
- The `.claude/CLAUDE.md` reference at line 79 (file is being deleted in this plan)
- Any "AI agents" reference (CLAUDE.md is the AI-agent home now)

<!-- `.claude/CLAUDE.md` deletion (D-06 + D-07 PASS): -->

Command: `git rm .claude/CLAUDE.md` (no fallback, no thin pointer per RESEARCH §"D-07 Verdict")
No .gitignore entry needed (the path simply disappears)
Verify next-session behavior: `test ! -f .claude/CLAUDE.md`
Verify no downstream skill/command hard-codes the path: `git grep -n '.claude/CLAUDE.md' -- ':!.planning/' ':!.claude/CLAUDE.md'` returns 0 (the only allowed reference is in this plan and the planning artifacts)

<!-- SC#5 grep gate (CONTEXT §specifics line 257-260; ROADMAP Phase 14 SC#5): -->

Command (exact; this is the closing assertion of the plan):
```bash
git grep -E 'v1 (bug|finding|leftover)|Gap [0-9]+|D-[0-9]+|UAT [Gg]ap' -- ':!.planning/' || echo PASS
```

Expected output: literally `PASS` (the `|| echo PASS` handles the zero-match case where `git grep` returns non-zero).

If the gate FAILS (any matches surface):
1. Capture every matched line: `git grep -nE 'v1 (bug|finding|leftover)|Gap [0-9]+|D-[0-9]+|UAT [Gg]ap' -- ':!.planning/'`
2. For each match, evaluate per D-04: KEEP if rule (a)/(b)/(c) AND rewrite to remove the planning-tag (rare; most matches are pure planning-history that Plan 14-02 should have stripped), OR strip the line
3. Re-run the gate; iterate until it returns "PASS"
4. Each iteration produces a commit (`refactor(14-03): strip remaining SC#5 grep-gate matches`)

<!-- Header-overlap dedup check (TRIM-04 SC#4): -->

Command:
```bash
diff <(grep '^##' README.md) <(grep '^##' CLAUDE.md) | wc -l
```

Expected: <= 2 shared `##` headers per VALIDATION.md TRIM-04 row (CONTEXT line 274 says "minimal overlap"). Recommended threshold is "<=2 shared headers" per VALIDATION.md.

<!-- Commit message format (per PATTERNS Pattern 6): -->
`<type>(14-03): <imperative summary> -- <one-line rationale or row reference>`
- `<type>` = `docs` for every commit in this plan (doc-tier work only)
- First line <= 75 chars
- Imperative mood
- No AI attribution (commit hook enforces; never bypass)

Examples:
```
docs(14-03): purge motd flag drift + Phase-1 note from docs/MANIFEST.md
docs(14-03): rewrite docs/MANIFEST.md CLI examples to public Phase 12 surface
docs(14-03): rewrite docs/README.md index to 3 surviving docs
docs(14-03): rewrite README.md to humans-only shape (D-06 dedup)
docs(14-03): delete .claude/CLAUDE.md (D-06; D-07 PASS; no fallback)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim docs/MANIFEST.md (motd drift + Phase-1 note + CLI-form rewrites)</name>
  <files>docs/MANIFEST.md</files>
  <read_first>
    - .planning/phases/14-comment-doc-trim/14-RESEARCH.md §"docs/ Review Findings" (the per-line edit list)
    - .planning/phases/14-comment-doc-trim/14-PATTERNS.md §Pattern 4 motd drift sites + Excerpts 1-4 (literal text around each line for surgical-edit alignment)
    - .planning/phases/14-comment-doc-trim/14-CONTEXT.md (TRIM-03 scope; R3 + R4 risk surface)
    - docs/MANIFEST.md (the file in full)
    - CLAUDE.md (for the canonical task-surface names: `task setup`, `task show:manifest`, `task audit:manifest`, `task test:manifest`, `task test:add-machine`)
  </read_first>
  <action>
    Edit `docs/MANIFEST.md` per the surgical edit list in <interfaces>:

    Pass 1 -- motd drift (5 sites):
      - Line 33: delete the `motd = true` line. Adjust the surrounding fenced toml block so the `[features]` section still has at least one feature key (typically `claude-marketplace = true` survives one line below). Verify the toml block parses by re-reading the modified region.
      - Line 67: delete `motd = true`.
      - Line 137: delete `motd = true` from the Fixture 01 defaults.toml side.
      - Line 152: delete `"motd": true,` from the Fixture 01 resolved.json side. Ensure the JSON remains valid (no trailing comma; the line below should be the last entry or the comma trails the previous entry correctly).
      - Line 491: delete the `motd` row from the Feature-Flag Reference table.

    Pass 2 -- Phase-1 note (lines 461-464):
      - Delete the "**Phase 1 note:** Until Phase 2 wires the manifest module..." admonition block. If the admonition uses fenced markdown like `> **Phase 1 note:**` or similar, delete every line of the block.

    Pass 3 -- CLI-form rewrites (lines 382-419 + 461-471 region):
      - `task -t taskfiles/manifest.yml setup -- <name>` -> `task setup -- <name>`
      - `task -t taskfiles/manifest.yml manifest:show` -> `task show:manifest`
      - `task -t taskfiles/manifest.yml manifest:validate -- --machine <name>` -> `task audit:manifest -- --machine <name>`
      - `task -t taskfiles/manifest.yml manifest:test` -> `task test:manifest`
      - `task -t taskfiles/manifest.yml manifest:test:add-machine` -> `task test:add-machine`
      - Read the surrounding paragraph(s) and update prose if it explains the old form (e.g., "the manifest module is direct-invoked" -> "the manifest layer is wired into the root Taskfile via `task setup`...").

    Pass 4 -- optional Phase-N forward-look strip (RESEARCH Open Question 2; recommendation: APPLY):
      - Strip `(Phase 5)` / `(Phase 6)` / `(Phase 3)` etc. from inside fenced toml/json code blocks. Examples: `# Bundle names map to packages/<name>.rb (Phase 5).` -> `# Bundle names map to packages/<name>.rb.`
      - Use `grep -n '(Phase [0-9])' docs/MANIFEST.md` to enumerate; review each and strip if it's stale forward-looking phrasing for a v2.1-final reader. Skip historical references that materially explain WHY (rare).

    Verification after all 4 passes:
    ```
    git grep -n 'motd' docs/MANIFEST.md         # expect: 0 hits
    git grep -n 'Phase 1 note' docs/            # expect: 0 hits
    git grep -n 'task -t taskfiles/manifest.yml' docs/  # expect: 0 hits
    ```

    Commit:
    ```
    docs(14-03): purge motd drift + Phase-1 note + CLI form from docs/MANIFEST.md

    -- 5 motd doc-drift sites removed (lines 33, 67, 137, 152, 491)
    -- Phase 1 note admonition removed (lines 461-464)
    -- CLI examples rewritten to public Phase 12 surface (task setup / task show:manifest / task audit:manifest / task test:manifest)
    -- Optional Phase-N parenthetical strip applied across fenced toml blocks
    ```

    Run `task lint && task test` (docs/ not in install path; no `task install` needed). Both must exit 0.
  </action>
  <verify>
    <automated>git grep -c '^motd = true' docs/MANIFEST.md | grep -qE '^0$' && git grep -c '^[[:space:]]*"motd":' docs/MANIFEST.md | grep -qE '^0$' && git grep -c 'Phase 1 note' docs/ | grep -qE '^0$' && git grep -c 'task -t taskfiles/manifest.yml' docs/ | grep -qE '^0$' && task lint && task test && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n 'motd' docs/MANIFEST.md` returns 0 hits (5 sites cleared)
    - `git grep -n 'Phase 1 note' docs/` returns 0 hits (admonition removed)
    - `git grep -n 'task -t taskfiles/manifest.yml' docs/` returns 0 hits (CLI form rewritten)
    - `grep -q 'task setup' docs/MANIFEST.md` succeeds (public surface present)
    - `grep -q 'task show:manifest' docs/MANIFEST.md` succeeds (public surface present)
    - `grep -q 'task audit:manifest' docs/MANIFEST.md` succeeds (public surface present)
    - The Fixture 01 example still demonstrates deep-merge (at least one feature key on each side; `claude-marketplace` typically survives both)
    - `task lint && task test` exits 0
    - Single commit with the prescribed message; no AI attribution
  </acceptance_criteria>
  <done>docs/MANIFEST.md is current, drift-free, and uses the public Phase 12 task surface; verified by 4 grep gates.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite docs/README.md to 3-doc index; optional docs/SECURITY.md polish</name>
  <files>docs/README.md, docs/SECURITY.md</files>
  <read_first>
    - .planning/phases/14-comment-doc-trim/14-RESEARCH.md §"docs/ Review Findings" (docs/README.md REWRITE action; docs/SECURITY.md OPTIONAL polish)
    - docs/README.md (current 7-line state; references deleted MIGRATION.md + CUTOVER.md)
    - docs/SECURITY.md (139 lines; check the "What This Document Does NOT Cover" section for future-tense references to completed Phase 4/7/8 work)
  </read_first>
  <action>
    Pass 1 -- docs/README.md (REQUIRED):
      Replace the entire file contents with:
      ```markdown
      # docs

      - `MANIFEST.md` -- manifest schema, inheritance rules, worked examples
      - `SECURITY.md` -- bootstrap trust chain
      - `MACHINES.md` -- per-machine inventory
      ```
      (Remove all references to MIGRATION.md and CUTOVER.md -- both deleted in Phase 11.)

    Pass 2 -- docs/SECURITY.md (OPTIONAL polish, RESEARCH Open Question 3):
      Read the "What This Document Does NOT Cover" section (around lines 90-110). For each future-tense reference to a Phase 4/7/8 capability ("Phase 4 will document...", "Phase 7 will add..."), evaluate:
      - If the future-tense statement is now a 1-2 line touch to rewrite as past tense ("Phase 4 documented..." or "Identity layer documentation lives in docs/MACHINES.md" depending on context) AND the rewrite is unambiguous -> apply
      - Otherwise skip (per RESEARCH this is stylistic, not drift)

      Recommended: do the rewrite if the file has 1-3 such references and the rewrite is mechanical. Document the choice in the commit message.

    Commit Pass 1 + Pass 2 together if Pass 2 was applied; otherwise commit Pass 1 alone:
    ```
    docs(14-03): rewrite docs/README.md to 3-doc index [+ SECURITY.md past-tense polish]

    -- docs/README.md: drop stale MIGRATION.md + CUTOVER.md references
    -- docs/README.md: 3-bullet index (MANIFEST + SECURITY + MACHINES)
    [-- docs/SECURITY.md: rewrite N future-tense Phase-N refs to past tense (Pass 2; OR: Pass 2 skipped per RESEARCH OQ3)]
    ```

    Verification:
    ```
    grep -c 'MIGRATION.md\|CUTOVER.md' docs/README.md    # expect: 0
    grep -qE '^- `MANIFEST.md`' docs/README.md           # expect: present
    grep -qE '^- `SECURITY.md`' docs/README.md           # expect: present
    grep -qE '^- `MACHINES.md`' docs/README.md           # expect: present
    task lint && task test                                # expect: green
    ```
  </action>
  <verify>
    <automated>grep -c 'MIGRATION.md\|CUTOVER.md' docs/README.md | grep -qE '^0$' && grep -qE '^- `MANIFEST.md`' docs/README.md && grep -qE '^- `SECURITY.md`' docs/README.md && grep -qE '^- `MACHINES.md`' docs/README.md && task lint && task test && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MIGRATION.md\|CUTOVER.md' docs/README.md` returns 0 (stale refs gone)
    - docs/README.md lists exactly 3 doc files: MANIFEST.md, SECURITY.md, MACHINES.md (verified via 3 grep checks)
    - If Pass 2 was applied to docs/SECURITY.md: the commit message documents what was changed; if skipped: the commit message documents "Pass 2 skipped per RESEARCH OQ3"
    - `task lint && task test` exits 0
    - 1-2 commits (Pass 1 mandatory; Pass 2 optional same-commit or separate-commit per planner)
  </acceptance_criteria>
  <done>docs/README.md is a clean 3-doc index; SECURITY.md polish applied or explicitly skipped with rationale.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite README.md to humans-only shape (D-06 dedup against CLAUDE.md)</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/14-comment-doc-trim/14-CONTEXT.md (D-06 canonical-home rule; §specifics line 263-284 humans-only target shape)
    - .planning/phases/14-comment-doc-trim/14-PATTERNS.md §Pattern 4 README.md current state + diff against D-06 target
    - README.md (current 79-line state; identify what to remove)
    - CLAUDE.md (canonical home; verify the Contributing pointer target exists -- §Where to Add Things, §Rules, §Conventions Not Captured Above)
  </read_first>
  <action>
    Replace `README.md` contents with the humans-only shape from <interfaces>. The target is approximately 30-40 lines.

    Steps:
    1. Read current README.md in full; identify sections that duplicate CLAUDE.md content (manifest-model deep-dive at lines 3-20; "Where to Add Things" table at lines 61-72; any contribution-rules content; any AI-agent-targeted prose at line 7)
    2. Replace the file body with the target shape from <interfaces>, adapting the exact section headers and content per the specific repo state
    3. The Contributing pointer link MUST be exactly `[CLAUDE.md](CLAUDE.md)` (relative link; renders correctly on GitHub and in local previews)
    4. The Common Tasks table MUST match the canonical 5-command surface from CLAUDE.md §Common Tasks (do not invent commands; copy the exact 5 names: `task install`, `task setup`, `task validate`, `task test`, `task lint`)
    5. The "Where things live" section MUST point to docs/ files, not the deleted .claude/CLAUDE.md
    6. The current README.md line 79 (`.claude/CLAUDE.md -- Claude Code project rules for working in this repo`) MUST be removed (file is being deleted in Task 4)
    7. Verify NO duplicate content remains between README.md and CLAUDE.md by running the header-overlap check below

    Commit:
    ```
    docs(14-03): rewrite README.md to humans-only shape (D-06 dedup)

    -- remove manifest-model deep-dive (lives in CLAUDE.md)
    -- remove Where-to-Add table (lives in CLAUDE.md)
    -- remove .claude/CLAUDE.md reference (file deleted in Task 4)
    -- add Contributing pointer to CLAUDE.md for AI/contributor rules
    -- header-overlap check: diff README vs CLAUDE shows <=N shared ## headers
    ```

    Verification:
    ```
    grep -qF '[CLAUDE.md](CLAUDE.md)' README.md           # contributing pointer present
    grep -qF 'task install' README.md                      # 5-command surface present
    grep -c '.claude/CLAUDE.md' README.md                 # expect 0 (deleted-file ref removed)
    OVERLAP=$(diff <(grep '^##' README.md) <(grep '^##' CLAUDE.md) | grep -c '^[<>]')
    # OVERLAP counts differing headers; the SHARED headers are common to both
    SHARED=$(comm -12 <(grep '^##' README.md | sort -u) <(grep '^##' CLAUDE.md | sort -u) | wc -l)
    [ "$SHARED" -le 2 ]                                   # SC#4 minimal overlap
    task lint && task test                                # green tree
    ```

    Note on overlap measurement: VALIDATION.md says `diff <(grep '^##' README.md) <(grep '^##' CLAUDE.md) | wc -l` shows <=2 shared headers. The exact interpretation of "shared headers" is debatable -- the canonical check (`comm -12`) directly counts shared `##` lines and is more semantically accurate. Use both checks; report both numbers in the commit message.
  </action>
  <verify>
    <automated>grep -qF '[CLAUDE.md](CLAUDE.md)' README.md && grep -qF 'task install' README.md && grep -qF 'task setup' README.md && grep -qF 'task validate' README.md && grep -qF 'task test' README.md && grep -qF 'task lint' README.md && SHARED=$(comm -12 <(grep '^##' README.md | sort -u) <(grep '^##' CLAUDE.md | sort -u) | wc -l | tr -d ' ') && [ "$SHARED" -le 2 ] && grep -c '\.claude/CLAUDE\.md' README.md | grep -qE '^0$' && task lint && task test && echo "PASS (SHARED=$SHARED)"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains the literal string `[CLAUDE.md](CLAUDE.md)` (contributing pointer)
    - README.md contains all 5 canonical commands (`task install`, `task setup`, `task validate`, `task test`, `task lint`)
    - README.md does NOT reference `.claude/CLAUDE.md` (deleted-file safety)
    - Header-overlap check: `comm -12 <(grep '^##' README.md | sort -u) <(grep '^##' CLAUDE.md | sort -u) | wc -l` returns <= 2 (TRIM-04 SC#4 minimal overlap)
    - README.md does NOT contain "AI agents working on this repo" or similar AI-agent-targeted prose (D-06: README is humans-only)
    - README.md does NOT contain a "Where to Add Things" table (duplicate of CLAUDE.md §Where to Add Things; covered by the Contributing pointer)
    - `task lint && task test` exits 0
    - Single commit with the prescribed message; no AI attribution
  </acceptance_criteria>
  <done>README.md is humans-only, links to CLAUDE.md for contribution rules, contains the 5-command surface, has minimal header overlap with CLAUDE.md.</done>
</task>

<task type="auto">
  <name>Task 4: Delete .claude/CLAUDE.md (D-06 + D-07 PASS); verify no hard-coded references</name>
  <files>.claude/CLAUDE.md</files>
  <read_first>
    - .planning/phases/14-comment-doc-trim/14-RESEARCH.md §"D-07 Verdict" (PASS: deletion is safe; root CLAUDE.md is canonical; NO fallback "thin pointer" file)
    - .planning/phases/14-comment-doc-trim/14-PATTERNS.md §Pattern 4 .claude/CLAUDE.md deletion verification (content fully subsumed by root CLAUDE.md)
    - .claude/CLAUDE.md (the file being deleted; one final read confirms no unique content survives)
    - CLAUDE.md (verify the root file already has the manifest-model, conventions, and quick-reference content that subsumes .claude/CLAUDE.md)
  </read_first>
  <action>
    Pre-deletion safety checks:
    1. Cross-reference scan: `git grep -nF '.claude/CLAUDE.md' -- ':!.planning/' ':!.claude/CLAUDE.md'`
       Expected: 0 hits (or only the README.md reference if Task 3 has not yet landed -- if Task 3 landed before this task, expect 0 hits)
       If any hit surfaces (e.g., a hook script or skill hard-codes the path), STOP and fix the reference first (rewrite the consumer to use root CLAUDE.md or remove the reference entirely), then re-run this check.
    2. One-final-read content verification: skim `.claude/CLAUDE.md` and confirm every section maps to a section in root CLAUDE.md (per PATTERNS Pattern 4 mapping):
       - .claude/CLAUDE.md §Overview -> root CLAUDE.md §What This Is + §The Manifest Model
       - .claude/CLAUDE.md §Quick Reference -> root CLAUDE.md §Common Tasks
       - .claude/CLAUDE.md §Structure -> root CLAUDE.md (no direct equivalent; structure info is in §Where to Add Things; this is a known acceptable difference)
       - .claude/CLAUDE.md §Conventions -> root CLAUDE.md §Rules + §Conventions Not Captured Above
       - .claude/CLAUDE.md §Adding Things -> root CLAUDE.md §Where to Add Things
       - .claude/CLAUDE.md §Zsh Startup Order -> NOT in root CLAUDE.md today. EVALUATE: is this content needed?
         - The Zsh Startup Order is a 6-line ordered list documenting `.zshenv` -> `.zprofile` -> `.zshrc` -> `.zlogin` -> `.zlogout` execution flow. It's useful for shell debugging.
         - If the content is materially useful for AI agents working on shell startup, port it to root CLAUDE.md as a new "Zsh Startup Order" sub-section under §Rules. Keep it terse (6 lines).
         - If not (i.e., the shell startup chain is self-evident from filenames), strip without porting.
         - PLANNER ACTION: port it. The chain is a documented fact and AI agents will need it when touching shell startup files. Add to CLAUDE.md after the existing §Rules content, before §Where to Add Things. Apply CLAUDE.md size check: if porting pushes CLAUDE.md > 220 lines, defer to `.claude/rules/shell-startup.md` instead.
       - .claude/CLAUDE.md §Safety -> root CLAUDE.md §Don't Do (partial overlap; verify no unique safety content lost; "Never commit private keys" is the only line not already in root and merits a Don't-Do bullet add).

    If Zsh Startup Order or "Never commit private keys" content needs porting, port it FIRST (separate commit), then delete `.claude/CLAUDE.md`:

    Commit A (conditional; only if content needs porting):
    ```
    docs(14-03): port Zsh Startup Order + private-keys safety from .claude/CLAUDE.md to root

    -- pre-deletion content preservation per PATTERNS Pattern 4 mapping
    -- Zsh Startup Order: 6-line ordered list added under §Rules
    -- "Never commit private keys" added as §Don't Do bullet
    -- wc -l CLAUDE.md = <N> (target <= 220)
    ```

    Commit B (always):
    ```
    docs(14-03): delete .claude/CLAUDE.md (D-06; D-07 PASS; no fallback)

    -- root CLAUDE.md is canonical per Claude Code memory model
    -- official docs: ./CLAUDE.md OR ./.claude/CLAUDE.md (either path); root preferred
    -- compaction-survival guarantee belongs to root path
    -- pre-deletion cross-reference scan: 0 hits (no consumers hard-code the path)
    -- pre-deletion content audit: every section subsumed by root CLAUDE.md [or ported in commit A]
    ```

    Use `git rm .claude/CLAUDE.md` (not `rm` + `git add`; `git rm` is the canonical removal).

    Verification after Commit B:
    ```
    test ! -f .claude/CLAUDE.md                                                # file gone
    git grep -nF '.claude/CLAUDE.md' -- ':!.planning/'                          # 0 hits (no hard-coded refs left)
    task lint && task test && task install && task validate                    # full suite green
    ```

    Note from R5: "the session that runs `git rm .claude/CLAUDE.md` retains the loaded context; only the NEXT session reads from disk." This is a session-mechanics observation; it does NOT require any plan task action. The deletion is safe to land in this session; subsequent sessions load from disk correctly.
  </action>
  <verify>
    <automated>test ! -f .claude/CLAUDE.md && HITS=$(git grep -cF '.claude/CLAUDE.md' -- ':!.planning/' 2>/dev/null | wc -l | tr -d ' ') && [ "$HITS" = "0" ] && task lint && task test && task install && task validate && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f .claude/CLAUDE.md` succeeds (file deleted)
    - `git grep -nF '.claude/CLAUDE.md' -- ':!.planning/'` returns 0 hits (no hard-coded references in the repo outside planning artifacts)
    - If Zsh Startup Order content was ported: `grep -q 'Zsh Startup Order' CLAUDE.md` succeeds AND `wc -l CLAUDE.md` <= 220
    - If private-keys content was ported: `grep -q 'private keys' CLAUDE.md` succeeds
    - `task lint && task test && task install && task validate` exits 0
    - 1-2 commits (Commit B always; Commit A only if porting was needed)
    - Deletion done via `git rm` (not `rm` + manual add); verify with `git log -1 --stat` showing `delete` for `.claude/CLAUDE.md`
    - No AI attribution in commit messages
  </acceptance_criteria>
  <done>.claude/CLAUDE.md is deleted; no consumer hard-codes the path; any unique content was ported to root CLAUDE.md or explicitly judged out-of-scope; full suite green.</done>
</task>

<task type="auto">
  <name>Task 5: Run SC#5 grep gate (closing assertion of Phase 14); fix any remaining matches; final full-suite gate</name>
  <files>(reads-only this task; if matches surface, the fix commits modify whichever files contain the matches)</files>
  <read_first>
    - .planning/phases/14-comment-doc-trim/14-CONTEXT.md (SC#5 grep command line 257-260; D-08 strip rationale)
    - .planning/phases/14-comment-doc-trim/14-RESEARCH.md (§"TRIM-05 Strip Surface Preview" the 13 strip-pass files; SC#5 expected zero matches after Plan 14-02)
    - .planning/phases/14-comment-doc-trim/14-VALIDATION.md TRIM-05 row (the exact gate command)
    - 14-TEACHING-INVENTORY.md (re-read §"Plan 14-02 Strip Manifest" rows -- if any `action=strip` row was missed in Plan 14-02, this gate surfaces it)
  </read_first>
  <action>
    Run the SC#5 grep gate exactly as specified in ROADMAP Phase 14 SC#5 and CONTEXT §specifics line 259:

    ```bash
    git grep -E 'v1 (bug|finding|leftover)|Gap [0-9]+|D-[0-9]+|UAT [Gg]ap' -- ':!.planning/' || echo PASS
    ```

    Expected output: literally `PASS` (the `|| echo PASS` handles the zero-match case).

    **If output is "PASS" (the gate is green):**
    1. Take no further action on matches
    2. Capture the exact gate command output (literal "PASS") for the SUMMARY evidence record
    3. Run the final full-suite gate:
       ```
       task lint && task test && task install && task validate
       ```
       Capture the exit code (must be 0) for the SUMMARY evidence record
    4. Run the header-overlap check (TRIM-04 SC#4):
       ```
       comm -12 <(grep '^##' README.md | sort -u) <(grep '^##' CLAUDE.md | sort -u) | wc -l
       ```
       Expected: <= 2. Capture the integer for the SUMMARY evidence record.
    5. Do NOT create a closing-assertion commit. Route the SC#5 PASS evidence + full-suite + header-overlap results
       into `14-03-SUMMARY.md` (the executor produces this artifact at phase close per the <output> block below).
       The grep command in step 1 remains the authoritative acceptance criterion -- the SUMMARY is the evidence
       record, not a separate gate.

    **If output shows matches (the gate is RED):**
    1. Capture every match with `git grep -nE 'v1 (bug|finding|leftover)|Gap [0-9]+|D-[0-9]+|UAT [Gg]ap' -- ':!.planning/'`
    2. For each match, evaluate per D-04:
       - If the line satisfies D-04 KEEP rule (a)/(b)/(c) but contains a planning-history tag, REWRITE: keep the WHY, strip the tag (e.g., `# D-04: status uses {{.X}}` -> `# Status uses template var to avoid the empty-shell-var re-run bug`)
       - If the line is pure planning-history (e.g., `# Phase 10 PORT-01 wiring`) with no D-04 value, DELETE the line
    3. Group the fixes into 1-N commits (one per file is fine; one per cluster of related files is also fine). Commit message:
       ```
       refactor(14-03): strip remaining SC#5 grep-gate matches in <file>

       -- <N> matches stripped per D-04 evaluation
       -- task lint && task test green after fix
       ```
    4. Re-run the gate after each commit; iterate until it returns "PASS"
    5. Once green, run the full-suite gate per the green-path branch above

    **Hard gate (do not close the plan if):**
    - SC#5 grep gate does not return "PASS"
    - Full suite (`task lint && task test && task install && task validate`) does not exit 0
    - Header-overlap check returns > 2

    All three MUST be green for Plan 14-03 (and therefore Phase 14) to close.
  </action>
  <verify>
    <automated>GATE=$(git grep -E 'v1 (bug|finding|leftover)|Gap [0-9]+|D-[0-9]+|UAT [Gg]ap' -- ':!.planning/' || echo PASS); [ "$GATE" = "PASS" ] && SHARED=$(comm -12 <(grep '^##' README.md | sort -u) <(grep '^##' CLAUDE.md | sort -u) | wc -l | tr -d ' ') && [ "$SHARED" -le 2 ] && task lint && task test && task install && task validate && echo "PHASE 14 PASS (header overlap=$SHARED)" || echo "FAIL: gate or suite or overlap"</automated>
  </verify>
  <acceptance_criteria>
    - SC#5 grep gate returns "PASS" (literal string): `git grep -E 'v1 (bug|finding|leftover)|Gap [0-9]+|D-[0-9]+|UAT [Gg]ap' -- ':!.planning/' || echo PASS` outputs `PASS`
    - `task lint && task test && task install && task validate` exits 0 (full suite green)
    - Header-overlap: `comm -12 <(grep '^##' README.md | sort -u) <(grep '^##' CLAUDE.md | sort -u) | wc -l` returns <= 2 (TRIM-04 SC#4)
    - 14-METRICS.md aggregate `% reduction` > 0 (TRIM-01 SC#1; re-read the file from Plan 14-02 close)
    - 14-TEACHING-INVENTORY.md NEEDS-ADD count = 0 unresolved (D-09 final check; re-read §Summary)
    - All 5 Phase 14 requirements (TRIM-01..05) addressed by Plans 14-01, 14-02, 14-03 (this plan addresses TRIM-03, TRIM-04, TRIM-05)
    - If SC#5 matches surfaced and were stripped: 1-N additional refactor commits; each leaves lint+test green
    - No AI attribution in any commit message
  </acceptance_criteria>
  <done>SC#5 grep gate returns PASS; full suite green; header-overlap minimal; Phase 14 ready for `/gsd:verify-work`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo author -> repo state | Plan 14-03 edits doc-tier files (docs/, README.md), deletes one file (.claude/CLAUDE.md), and runs read-only grep gates. Zero code change. Zero install pipeline impact (docs/ not in install path). The full-suite gate after the delete commit catches any regression at boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-DL | Repudiation | .claude/CLAUDE.md deletion loses content not in root CLAUDE.md | mitigate | Task 4 pre-deletion content audit: every section mapped to root CLAUDE.md per PATTERNS Pattern 4; Zsh Startup Order + private-keys content ported BEFORE delete commit if surfaced. R5 sequencing guidance acknowledged (next-session reads from disk; current session retains loaded context safely). |
| T-14-03-DR | Tampering | docs/MANIFEST.md surgical edits could break a fixture's deep-merge demonstration | mitigate | Task 1 verifies fenced toml/json blocks after each edit; the Fixture 01 example retains at least one feature key on each side (`claude-marketplace` typically survives) so the merge semantics still demonstrate. |
| T-14-03-RP | Information disclosure | README.md humans-only rewrite could accidentally drop the link to docs/SECURITY.md | accept | Target shape in <interfaces> explicitly includes the docs/ links section; acceptance criteria verify `[CLAUDE.md](CLAUDE.md)` pointer but the docs/ section is verified by content match in the commit review. Low risk; doc-tier rewrite is reviewable. |
| T-14-03-GG | Denial of service | SC#5 grep gate gives a false negative (overlooked match) | accept | The gate IS the verification; this is its purpose. Iterate if matches surface (Task 5 explicitly handles RED case with fix loop). |

No new attack surface. Risk class: doc-drift / lesson-loss (mitigated by the per-task content audits and the SC#5 gate as the final closing assertion).
</threat_model>

<verification>
- `git grep -n 'motd' docs/MANIFEST.md` returns 0 hits (Task 1)
- `git grep -n 'Phase 1 note' docs/` returns 0 hits (Task 1)
- `git grep -n 'task -t taskfiles/manifest.yml' docs/` returns 0 hits (Task 1)
- `grep -c 'MIGRATION.md\|CUTOVER.md' docs/README.md` returns 0 (Task 2)
- `grep -qF '[CLAUDE.md](CLAUDE.md)' README.md` succeeds (Task 3)
- `comm -12 <(grep '^##' README.md | sort -u) <(grep '^##' CLAUDE.md | sort -u) | wc -l` returns <= 2 (Task 3 + Task 5)
- `test ! -f .claude/CLAUDE.md` succeeds (Task 4)
- `git grep -nF '.claude/CLAUDE.md' -- ':!.planning/'` returns 0 hits (Task 4)
- SC#5 gate: `git grep -E 'v1 (bug|finding|leftover)|Gap [0-9]+|D-[0-9]+|UAT [Gg]ap' -- ':!.planning/' || echo PASS` outputs `PASS` (Task 5)
- `task lint && task test && task install && task validate` exits 0 (Task 5 closing)
</verification>

<success_criteria>
- TRIM-03: docs/ reviewed; obsolete content removed (motd + Phase-1 note + stale CLI form in MANIFEST.md; MIGRATION + CUTOVER refs in docs/README.md). docs/MACHINES.md kept unchanged (already current). docs/SECURITY.md kept or polished per RESEARCH OQ3.
- TRIM-04: README.md humans-only; CLAUDE.md canonical AI/contributor reference; `.claude/CLAUDE.md` deleted (no fallback per D-07 PASS). Header overlap minimal (<= 2 shared `##` headers).
- TRIM-05: SC#5 grep gate returns "PASS" (zero matches in code; planning artifacts retain their references which is correct per the `:!.planning/` exclusion).
- Full suite (`task lint && task test && task install && task validate`) green after every commit and at plan close.
- ROADMAP Phase 14 SC#1-5 all satisfied: SC#1 (14-METRICS.md aggregate % reduction > 0, set by Plan 14-02), SC#2 (banner shape consistent, set by Plan 14-02), SC#3 (docs/ reviewed, set by Task 1+2), SC#4 (README/CLAUDE.md/.claude dedup, set by Tasks 3+4), SC#5 (grep gate PASS, set by Task 5).
- Phase 14 is ready for `/gsd:verify-work`.
</success_criteria>

<output>
Create `.planning/phases/14-comment-doc-trim/14-03-SUMMARY.md` when done. Summary must include:
- Per-task commit list (5 tasks; ~5-8 commits total depending on optional sub-passes)
- docs/MANIFEST.md edit count (motd sites removed, CLI form rewrites, optional Phase-N strips)
- README.md before/after line count
- `.claude/CLAUDE.md` deletion confirmation + any content ported to root CLAUDE.md (Zsh Startup Order? private-keys bullet?)
- Final `wc -l CLAUDE.md` line count (should still be <= 220 even with ports from .claude/CLAUDE.md)
- SC#5 grep gate result (literal "PASS" + the exact command output)
- Header-overlap count (the `comm -12` integer)
- 14-METRICS.md aggregate `% reduction` (from Plan 14-02 close; restated here as TRIM-01 evidence)
- Final full-suite exit code
- Phase 14 ROADMAP SC#1-5 checklist with green/red marks
- Confirmation that Phase 14 is complete and v2.1 milestone is closeable
</output>

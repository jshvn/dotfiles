---
phase: 14-comment-doc-trim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md
  - CLAUDE.md
autonomous: true
requirements: [TRIM-04, TRIM-05]
requirements_addressed: [TRIM-04, TRIM-05]
tags: [comment-trim, doc-dedup, teaching-inventory, claude-md]

must_haves:
  truths:
    - "Every annotation block of >=3 comment lines OR any single annotation containing one of the strip-pattern tokens across the 13 strip-pass files is enumerated in 14-TEACHING-INVENTORY.md with file:line, snippet, lesson, covered-by, NEEDS-ADD?, and action columns"
    - "Every NEEDS-ADD row in 14-TEACHING-INVENTORY.md has a corresponding amendment in CLAUDE.md (either an added paragraph in §Rules, an added Don't-Do bullet, or a new §Lint Rule Catalogue section) before the strip pass in Plan 14-02 runs"
    - "CLAUDE.md §Conventions Not Captured Above bullet on file-level comment blocks reads 'Purpose / Depends on / Side effects' (D-01 contradiction resolution against the prior 'purpose, callers, and side effects' wording)"
    - "CLAUDE.md total line count is <= 220 lines; if a draft exceeds 220, the LINT-NN catalogue is split into .claude/rules/lint.md (path-scoped sub-rule) and the bullet under §Rules cross-references it"
    - "Plan 14-02 (the strip pass) has zero NEEDS-ADD blockers when it starts; the inventory is the green-light gate per D-09"
  artifacts:
    - path: ".planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md"
      provides: "Pre-strip teaching inventory mapping every annotation block to CLAUDE.md coverage; gates Plan 14-02"
      contains: "| file:line | snippet | lesson encoded | covered by CLAUDE.md §X | NEEDS-ADD? | action |"
      min_rows: 15
    - path: "CLAUDE.md"
      provides: "Canonical AI-agent reference, amended for D-01 banner contradiction and any D-09 NEEDS-ADD lessons"
      contains: "Purpose / Depends on / Side effects"
      max_lines: 220
  key_links:
    - from: "14-TEACHING-INVENTORY.md"
      to: "Plan 14-02 strip commits"
      via: "every row with action='strip after Plan 14-01' is referenced by a Plan 14-02 trim commit body"
      pattern: "row reference in commit message"
    - from: "CLAUDE.md §Conventions Not Captured Above"
      to: "Plan 14-02 banner trim acceptance criteria"
      via: "amended bullet defines the 3-label banner shape that 14-02 verifies"
      pattern: "Purpose / Depends on / Side effects"
---

<objective>
Build the teaching inventory that gates the entire Phase 14 strip pass, then gap-fill CLAUDE.md so no lesson is lost when the in-code annotations are stripped in Plan 14-02. This plan is the sequential bottleneck for the phase: no strip commit lands until the inventory exists and every NEEDS-ADD row is resolved (per D-09).

Purpose: D-09 says "Before any `git grep`-driven strip pass, the planner produces 14-TEACHING-INVENTORY.md" and "Any NEEDS-ADD row is gap-filled into CLAUDE.md ... before the strip pass runs." This plan delivers both.
Output: Two artifacts — 14-TEACHING-INVENTORY.md (new) + CLAUDE.md (amended, <= 220 lines).
</objective>

<execution_context>
@$HOME/.config/claude/get-shit-done/workflows/execute-plan.md
@$HOME/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-comment-doc-trim/14-CONTEXT.md
@.planning/phases/14-comment-doc-trim/14-RESEARCH.md
@.planning/phases/14-comment-doc-trim/14-PATTERNS.md
@.planning/phases/14-comment-doc-trim/14-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Strip-pass file inventory (13 files; RESEARCH §"TRIM-05 Strip Surface Preview"). -->
<!-- Pre-trim strip-pattern match counts using the broader pattern -->
<!-- (Phase|D-|CR-|WR-|RESEARCH §|Gap|v1 (bug|finding|leftover)|UAT). -->
<!-- Total: 72 broader-pattern matches across these files. -->

Taskfile.yml                            (2 matches)
identity/ssh/cloudflared.zsh            (1 match)
install/compose-brewfile.zsh            (1 match)
install/resolver.zsh                    (13 matches; MEDIUM risk per RESEARCH)
os/shell-registration.zsh               (1 match)
taskfiles/claude.yml                    (6 matches)
taskfiles/identity.yml                  (9 matches; MEDIUM)
taskfiles/links.yml                     (3 matches; MEDIUM; 22-line LINT-02 explainer + 26-line reconcile lead block)
taskfiles/lint.yml                      (3 matches)
taskfiles/macos.yml                     (2 matches)
taskfiles/manifest.yml                  (17 matches; HIGH density)
taskfiles/packages.yml                  (6 matches; MEDIUM)
taskfiles/test.yml                      (8 matches; MEDIUM)

<!-- Additional inventory surface (NOT in the 13 strip files but contain heavy banners or teaching blocks): -->
shell/.zlogout                          (51-line banner over 2-line body; REVIEW row 45)
shell/aliases/jgrid.zsh                 (15-line banner)
shell/aliases/ghostty.zsh               (13-line banner)
shell/aliases/finder.zsh                (11-line banner)
shell/aliases/dotfiles.zsh              (9-line banner)
shell/functions/_dotfiles_feature.zsh   (26-line banner)
shell/functions/_dotfiles_require_feature.zsh (34-line banner)

<!-- CLAUDE.md current structure (line numbers as of 187-line state): -->
§What This Is                       lines 1-8
§The Manifest Model (the keystone)  lines 10-19
§Common Tasks (operator surface)    lines 21-42
§Rules                              lines 44-131
  §Manifests are the source of truth   lines 46-50
  §One concept per file                lines 52-59
  §Flat directories in v1              lines 61-66
  §kebab-case feature names index      lines 68-84
  §Every install task has a status:    lines 86-101
  §set -euo pipefail                   lines 103-106
  §No hardcoded /opt/homebrew          lines 108-113
  §Symlinks via _:safe-link            lines 115-119
  §XDG everywhere                      lines 121-131
§Where to Add Things                lines 133-144
§Conventions Not Captured Above     lines 146-156   (D-01 amendment target)
§Tooling Versions                   lines 158-164
§Don't Do                           lines 166-177
§Project State and Workflow         lines 179-187

<!-- Six-column house style for new artifacts (Phase 12 D-14 / Phase 13 13-REVIEW.md exemplar): -->
| col1 | col2 | col3 | col4 | col5 | col6 |
|------|------|------|------|------|------|

<!-- RESEARCH NEEDS-ADD predictions (treat as starting hypothesis; planner may surface more): -->
1. Cross-field validation rule (D-16 in resolver.zsh:171,212,616) - NOT in CLAUDE.md today.
2. LINT-NN catalogue (LINT-01..08) - CLAUDE.md mentions LINT-02 only; in-code citations would benefit from a numbered catalogue.
3. DOTFILEDIR-leak footgun pattern (Taskfile.yml:131-148) - one Don't-Do line would let the in-code comment compress.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 14-TEACHING-INVENTORY.md (six-column table over 13 strip-pass files + 7 heavy-banner files)</name>
  <files>.planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md</files>
  <read_first>
    - .planning/phases/14-comment-doc-trim/14-CONTEXT.md (D-09 inventory shape and decision)
    - .planning/phases/14-comment-doc-trim/14-RESEARCH.md (§"TRIM-05 Strip Surface Preview" and the NEEDS-ADD prediction list)
    - .planning/phases/14-comment-doc-trim/14-PATTERNS.md (§Pattern 5 six-column house style; §Pattern 1/2 KEEP-vs-CUT examples that inform per-row action column)
    - CLAUDE.md (the §X target column needs accurate section names and line ranges)
    - taskfiles/links.yml lines 1-39 (heavy banner + LINT-02 explainer that becomes inventory row 1)
    - taskfiles/links.yml lines 167-456 (PORT-01 comment block + reconcile lead block)
    - taskfiles/manifest.yml lines 43-100 (DOTFILEDIR resolution comment block + WR-08 stdin handling comment + other D-NN refs)
    - install/resolver.zsh (WR-NN annotations at lines 80, 179, 289, 456, 558, 587; D-NN refs at lines 171, 212, 616)
    - shell/.zlogout (51-line banner over 2-line body)
    - Taskfile.yml lines 131-148 and 240-248 (DOTFILEDIR-leak footgun comment blocks per REVIEW row 28; KEEP candidates per D-04 rule (b))
  </read_first>
  <action>
    Create `.planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md` as a six-column markdown table per the Phase 12 D-14 / Phase 13 13-REVIEW.md house style.

    File frontmatter (matches 13-REVIEW.md shape):
    ```
    ---
    phase: 14
    slug: comment-doc-trim
    artifact: TEACHING-INVENTORY
    created: <today>
    rows: <count>
    needs_add_count: <count>
    ---
    ```

    Table columns (use these exact headers; respect the order):
    `| file:line | snippet | lesson encoded | covered by CLAUDE.md §X | NEEDS-ADD? | action |`

    For each of the 13 strip-pass files listed in <interfaces> AND the 7 heavy-banner files (shell/.zlogout; the 4 alias files; the 2 _dotfiles_feature helper functions), enumerate every annotation block matching EITHER of:
      (a) >= 3 consecutive comment lines (banner or inline teaching), OR
      (b) any single comment line containing one of the strip-pattern tokens: `Phase [0-9]+`, `D-[0-9]+`, `CR-[0-9]+`, `WR-[0-9]+`, `RESEARCH §`, `Gap [0-9]+`, `v1 (bug|finding|leftover)`, `UAT [Gg]ap`, `PORT-[0-9]+`, `BTSP-[0-9]+`, `MFST-[0-9]+`, `IDNT-[0-9]+`.

    For each row, fill:
      - `file:line`: e.g., `taskfiles/links.yml:18-39`
      - `snippet`: first ~60 chars of the block, quoted, with `...` if truncated
      - `lesson encoded`: 1-line summary of the lesson the comment teaches (e.g., `$X in status: causes re-run; status uses {{.X}} only`)
      - `covered by CLAUDE.md §X`: section name + line range if present (e.g., `§Rules > "Every install task has a status: block" (lines 86-101)`); `none` if not covered; `none (out of scope)` if the lesson is generic zsh/tooling tutorial content
      - `NEEDS-ADD?`: `no` if covered, `yes -- <what to add>` if not covered AND the lesson is project-relevant, `no (out of scope)` if not project-relevant
      - `action`: one of `strip after Plan 14-01`, `keep -- D-04 rule (b) still-live footgun`, `keep -- D-04 rule (a) non-obvious WHY`, `keep -- D-04 rule (c) lint citation`, `rewrite -- preserve WHY, strip planning tag`, `strip; do not gap-fill`

    Sort rows by file path, then by line number. Use the seed rows from PATTERNS.md §Pattern 5 as canonical examples; expand from there by line-reading each source file.

    After the table, add a `## Summary` section with:
      - Total rows: N
      - Rows with `action=strip`: N
      - Rows with `action=keep`: N
      - Rows with `action=rewrite`: N
      - Rows with `NEEDS-ADD=yes`: N (list each NEEDS-ADD with one-line description and proposed CLAUDE.md insertion point)

    Then add a `## Plan 14-02 Strip Manifest` subsection listing each file from the inventory with the count of rows per action (strip/keep/rewrite). This is the per-file work breakdown Plan 14-02 consumes.

    Do NOT execute any strip commits in this task. The inventory is build-only; Task 2 of this plan acts on the NEEDS-ADD rows; Plan 14-02 acts on the strip/rewrite rows.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md &amp;&amp; grep -c '^| ' .planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md | awk '{if ($1 &gt;= 15) print "PASS"; else print "FAIL: only "$1" table rows (need &gt;= 15)"}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md` succeeds
    - `head -10 .planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md | grep -qE '^\| file:line \| snippet \| lesson encoded \| covered by CLAUDE.md'` succeeds (six-column header present)
    - Row count >= 15 (verified via `grep -c '^| [a-zA-Z]' .planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md` minus header row); RESEARCH estimated 20-30 rows so >= 15 is the floor
    - Every one of the 13 strip-pass files from <interfaces> appears in at least one row (verified via `for f in Taskfile.yml taskfiles/links.yml taskfiles/manifest.yml taskfiles/identity.yml taskfiles/packages.yml taskfiles/test.yml taskfiles/lint.yml taskfiles/claude.yml taskfiles/macos.yml install/resolver.zsh install/compose-brewfile.zsh os/shell-registration.zsh identity/ssh/cloudflared.zsh; do grep -q "$f" .planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md || echo "MISSING: $f"; done` produces no output)
    - Heavy-banner files appear: `shell/.zlogout`, `shell/aliases/jgrid.zsh`, `shell/aliases/ghostty.zsh`, `shell/aliases/finder.zsh`, `shell/aliases/dotfiles.zsh`, `shell/functions/_dotfiles_feature.zsh`, `shell/functions/_dotfiles_require_feature.zsh` (verified via same grep loop)
    - `## Summary` section exists with NEEDS-ADD count enumerated
    - `## Plan 14-02 Strip Manifest` section exists with per-file action counts
  </acceptance_criteria>
  <done>Inventory exists, six-column shape, >= 15 rows, every strip-pass file enumerated, NEEDS-ADD count surfaced in the summary, Plan 14-02 work breakdown present.</done>
</task>

<task type="auto">
  <name>Task 2: Amend CLAUDE.md for D-01 contradiction + D-09 NEEDS-ADD gap-fills</name>
  <files>CLAUDE.md, .claude/rules/lint.md</files>
  <read_first>
    - .planning/phases/14-comment-doc-trim/14-CONTEXT.md (D-01 contradiction wording; CLAUDE.md amendment specifics)
    - .planning/phases/14-comment-doc-trim/14-RESEARCH.md (§"D-07 Verdict" 200-line guideline; R6 risk on growth)
    - .planning/phases/14-comment-doc-trim/14-PATTERNS.md (§Pattern 4 D-01 amendment site at CLAUDE.md:152-153; quoted excerpt)
    - .planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md (Task 1 output -- read the NEEDS-ADD rows from §Summary)
    - CLAUDE.md (full read; lines 146-156 is the D-01 amendment site)
  </read_first>
  <action>
    Two operations in a single commit, in this order:

    (1) D-01 contradiction resolution. CLAUDE.md lines 152-153 currently read:
        > File-level comment block at the top of every script explaining its purpose, callers, and
        > side effects.

        Replace with (per CONTEXT §specifics line 254-255):
        > File-level comment block at the top of every script: Purpose / Depends on / Side effects
        > (3 labels; one `# === ===` 77-char rule above and below; no narrative prose, no examples).

        Then review line 154 ("Section separators in YAML files use `# ===` or `# ---` banner style"). D-02 disallows mid-file separators. Narrow to: "The file-header banner uses one `# === ===` 77-char rule above and below the 3 labels; no mid-file dividers." OR remove the bullet entirely if the preceding bullet already conveys this. Planner judgement; the goal is no ambiguity about banner shape and no permission for mid-file separators.

    (2) D-09 NEEDS-ADD gap-fills. For each `NEEDS-ADD=yes` row in `14-TEACHING-INVENTORY.md` §Summary, add the proposed content to CLAUDE.md at the insertion point named in that row. Common landings based on RESEARCH predictions:

        - **Cross-field validation rule (D-16; if surfaced):** Add a new bullet under §Rules > §Manifests are the source of truth (after line 50). Suggested text (~3 sentences, terse per R6):
          > "Cross-field validation: the resolver enforces conditional rules across manifest sections (e.g., `identity.ssh in {personal,work}` requires `features.one-password-ssh = true`). Failing rules surface at `task setup` time with a clear error from `install/resolver.zsh`. Add new rules to the `validate_cross_field` block in `resolver.zsh`."

        - **LINT-NN catalogue (if surfaced):** Add a small table under §Rules (between §kebab-case feature names and §Every install task has a status:). If adding inline keeps CLAUDE.md <= 220 lines, do so. If it pushes CLAUDE.md > 220 lines, create `.claude/rules/lint.md` (path-scoped sub-rule) containing the table, and add a one-line cross-reference under §Rules: "Lint rule catalogue (LINT-01..08): see `.claude/rules/lint.md`." Recommended catalogue table:
          ```
          | ID | Scope | What it checks |
          |----|-------|----------------|
          | LINT-01 | Taskfiles | Every install task has a `status:` block |
          | LINT-02 | Taskfiles | `status:` uses `{{.X}}` template vars, not `$X` shell vars |
          | LINT-03a | Taskfiles | Tasks with `cmds:` have `status:` (or exempt via `internal: true` / all-task-delegates) |
          | LINT-03b | Repo-wide | No bare `ln -s` outside `taskfiles/helpers.yml` |
          | LINT-04 | Executable .zsh | `set -euo pipefail` in first 30 lines |
          | LINT-05 | shell/ + os/ | Portability-sensitive commands surface as warnings (non-blocking) |
          | LINT-07 | All .zsh | `zsh -n` parse-check (Tier-0 syntax) |
          | LINT-08 | Root Taskfile.yml | `default:` banner lists every public top-level task |
          ```

        - **DOTFILEDIR-leak Don't-Do (if surfaced):** Add a bullet under §Don't Do (after line 177):
          > "Don't define a `DOTFILEDIR: { sh: dirname ... }` var in included taskfiles -- it leaks into root scope and competes with the Taskfile.yml definition. Source `install/messages.zsh` via `{{.TASKFILE_DIR}}` (per the Taskfile.yml comment block warning the same)."

        After all gap-fills, run `wc -l CLAUDE.md`. If > 220 lines, extract the LINT-NN catalogue to `.claude/rules/lint.md` and shrink the inline reference to one cross-reference line (per R6 mitigation in 14-VALIDATION.md). Re-check `wc -l CLAUDE.md` <= 220.

        If `.claude/rules/lint.md` is created, also ensure it has the standard banner shape (3-label) at the top. The file must not be empty; minimum content is the LINT-NN table plus a one-line introductory sentence.

        Do NOT touch any other section of CLAUDE.md beyond the D-01 amendment site, the NEEDS-ADD insertion points, and (conditionally) the LINT-NN catalogue location.

    Commit message format (use HEREDOC; obeys global commit format + no AI attribution rule from project CLAUDE.md):
    ```
    docs(14-01): build teaching inventory + amend CLAUDE.md for D-01/D-09

    -- 14-TEACHING-INVENTORY.md: <N> rows across 13 strip files + 7 heavy banners
    -- CLAUDE.md: amend "purpose, callers, side effects" -> "Purpose / Depends on / Side effects" (D-01)
    -- CLAUDE.md: gap-fill <list of NEEDS-ADD rows> (D-09 prereq for Plan 14-02)
    -- wc -l CLAUDE.md = <N> (target <= 220)
    [-- .claude/rules/lint.md: extracted LINT-NN catalogue (R6 mitigation; if applicable)]
    ```
  </action>
  <verify>
    <automated>head -30 CLAUDE.md &gt; /dev/null &amp;&amp; grep -qE '^- File-level comment block at the top of every script: Purpose / Depends on / Side effects' CLAUDE.md &amp;&amp; LINES=$(wc -l &lt; CLAUDE.md) &amp;&amp; [ "$LINES" -le 220 ] &amp;&amp; echo "PASS (CLAUDE.md=$LINES lines)" || echo "FAIL: D-01 amendment missing or CLAUDE.md &gt; 220 lines ($LINES)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^- File-level comment block at the top of every script: Purpose / Depends on / Side effects' CLAUDE.md` succeeds (D-01 amendment present, exact wording)
    - `grep -qE 'purpose, callers, and side effects' CLAUDE.md` FAILS (old wording removed)
    - `wc -l < CLAUDE.md` returns a value <= 220 (R6 size guideline respected)
    - For each NEEDS-ADD row in 14-TEACHING-INVENTORY.md §Summary, the proposed content is present in CLAUDE.md OR `.claude/rules/lint.md` (verify by greping for a distinctive phrase from the row's description)
    - If `.claude/rules/lint.md` was created, it exists (`test -f .claude/rules/lint.md`) and contains a header banner with the 3-label shape and the LINT-NN catalogue
    - `git diff --stat HEAD` shows exactly these files modified: `CLAUDE.md` (always), `.planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md` (added by Task 1), and conditionally `.claude/rules/lint.md`
    - The commit message follows the global format (`<type>(<scope>): <summary>` <75 chars first line; imperative mood) and contains no AI attribution trailer (commit hook enforces this; if it blocks, fix the message and retry)
    - `task lint && task test` exits 0 (no Phase 14-01 work touches install pipeline; this is a regression-free check)
  </acceptance_criteria>
  <done>CLAUDE.md amended for D-01 (exact wording present, old wording absent), every NEEDS-ADD lesson landed in CLAUDE.md or `.claude/rules/lint.md`, total CLAUDE.md size <= 220 lines, lint+test green, single commit with the prescribed message format.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo author -> repo state | Plan 14-01 mutates one canonical doc (CLAUDE.md) and adds one planning artifact (14-TEACHING-INVENTORY.md). No code path, no execution surface, no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-LL | Tampering | 14-TEACHING-INVENTORY.md teaching-loss risk | mitigate | D-09 + this plan: inventory enumerates every annotation BEFORE Plan 14-02 strips anything; NEEDS-ADD rows gap-fill into CLAUDE.md first. Risk class is doc-drift / lesson-loss, not security. |
| T-14-01-CG | Information disclosure | CLAUDE.md content growth | accept | RESEARCH R6: Claude Code's 200-line soft limit. Mitigation built into Task 2: if `wc -l` exceeds 220, split LINT catalogue to `.claude/rules/lint.md`. No PII or secret surface; risk is purely context-window efficiency. |

No new attack surface (zero new code, zero functional change). The Phase 14 framing per the downstream consumer block: "Risk class: doc-drift / lesson-loss (mitigated by Plan 14-01 teaching inventory + D-09 gap-fill rule)."
</threat_model>

<verification>
- `test -f .planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md` (artifact exists)
- `grep -c '^| [a-zA-Z]' .planning/phases/14-comment-doc-trim/14-TEACHING-INVENTORY.md` >= 15 (rows enumerated)
- `grep -qE '^- File-level comment block at the top of every script: Purpose / Depends on / Side effects' CLAUDE.md` (D-01 amendment present)
- `wc -l < CLAUDE.md` <= 220 (R6 size guideline)
- `task lint && task test` exits 0
- `git log -1 --format=%s` matches `docs(14-01): build teaching inventory + amend CLAUDE.md for D-01/D-09`
- Plan 14-02 has zero NEEDS-ADD blockers when it starts (verified by re-reading 14-TEACHING-INVENTORY.md §Summary: NEEDS-ADD count = 0 unresolved)
</verification>

<success_criteria>
- 14-TEACHING-INVENTORY.md exists, 6-column shape, every strip-pass file enumerated, every heavy-banner file enumerated, summary surfaces NEEDS-ADD count
- CLAUDE.md amended for D-01 (banner shape contradiction resolved against the prior "callers" wording; the locked roadmap wording wins)
- Every NEEDS-ADD lesson is preserved in CLAUDE.md or `.claude/rules/lint.md` before Plan 14-02 strips the in-code annotation
- CLAUDE.md size <= 220 lines (R6 mitigation; soft limit per Claude Code docs)
- Plan 14-02 is unblocked: it can consume 14-TEACHING-INVENTORY.md §"Plan 14-02 Strip Manifest" as its work breakdown
- `task lint && task test` green; single commit with the prescribed message format
</success_criteria>

<output>
Create `.planning/phases/14-comment-doc-trim/14-01-SUMMARY.md` when done. Summary must include:
- Total inventory rows count
- Per-file row count distribution
- NEEDS-ADD rows resolved (with the CLAUDE.md insertion point named for each)
- Final `wc -l CLAUDE.md` line count
- Whether `.claude/rules/lint.md` was created (yes/no + size if yes)
- Commit SHA
- Confirmation that Plan 14-02's strip pass is unblocked
</output>

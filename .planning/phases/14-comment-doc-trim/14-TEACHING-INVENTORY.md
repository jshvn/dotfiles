---
phase: 14
slug: comment-doc-trim
artifact: TEACHING-INVENTORY
created: 2026-05-18
rows: 162
needs_add_count: 3
---

# Phase 14 -- Teaching Inventory

Pre-strip enumeration of every annotation block in the 13 strip-pass files plus
7 heavy-banner files. Each row maps the in-code lesson to its CLAUDE.md coverage
(or `none` / `none (out of scope)`); rows with `NEEDS-ADD = yes` block the Plan
14-02 strip pass until amended into CLAUDE.md per D-09.

Enumeration rule: every block of >= 3 consecutive comment lines OR any single
annotation containing one of the strip-pattern tokens (`Phase [0-9]+`,
`D-[0-9]+`, `CR-[0-9]+`, `WR-[0-9]+`, `RESEARCH §`, `Gap [0-9]+`,
`v1 (bug|finding|leftover)`, `UAT [Gg]ap`, `PORT-[0-9]+`, `BTSP-[0-9]+`,
`MFST-[0-9]+`, `IDNT-[0-9]+`).

Sort: file path, then line number.

## Inventory

| file:line | snippet | lesson encoded | covered by CLAUDE.md §X | NEEDS-ADD? | action |
|-----------|---------|----------------|-------------------------|------------|--------|
| Taskfile.yml:1-19 | "Taskfile.yml -- root taskfile for v2 dotfiles. Single canonical entry: `task install`. There is no separate `task update` (D-10 ..." (19 lines) | install IS update; phase-by-phase include list | §Common Tasks (operator surface) (lines 21-42) + §The Manifest Model | no | rewrite -- preserve Purpose/Depends/Side effects (D-01), strip phase enumeration |
| Taskfile.yml:34-37 | "Resolve DOTFILEDIR from this taskfile's parent directory. go-task 3.37+ provides {{.TASKFILE_DIR}}..." (4 lines) | go-task 3.37+ TASKFILE_DIR semantics | §Tooling Versions (lines 158-164; go-task 3.37 row) | no | strip after Plan 14-01 -- restatement plus citation already in CLAUDE.md |
| Taskfile.yml:54-60 | "MANIFEST_JSON cats the resolved.json or echoes a stub... empty `features` map so template expressions like `{{if index .MANIFEST.features ...}}` evaluate to false (nil zero value) instead of erroring out..." (7 lines) | empty-features stub avoids `index of untyped nil` on fresh checkout | none (taskfile-specific implementation note) | no | keep -- D-04 rule (a) non-obvious WHY |
| Taskfile.yml:73-82 | "manifest needs the root's DOTFILEDIR + XDG_STATE_HOME explicitly forwarded; its DOTFILEDIR sh:dirname-twice pattern resolves to the wrong path when invoked as an included taskfile." (3 lines + include block) | DOTFILEDIR pollution footgun under include-merge | §Don't Do (NEEDS-ADD candidate; see Summary) | yes -- add DOTFILEDIR-leak Don't-Do bullet | keep -- D-04 rule (b) still-live footgun (compresses once Don't-Do bullet exists) |
| Taskfile.yml:119-120 | "Note: task install IS task update -- D-10 dropped the update: block. Phase 3 ships shell alias `update='task install'` for muscle memory." (2 lines) | install IS update; alias exists | §Quick Reference / Conventions (already documented) | no | strip after Plan 14-01 -- D-10 + Phase 3 are planning tags |
| Taskfile.yml:126-129 | "status: [false] forces the cmd to run every invocation. With status: [true] the task is considered up-to-date and skipped, so `task` alone would print nothing instead of rendering the banner." (4 lines) | `status: [false]` always-runs idiom | §Rules > Every install task has a status: block (lines 86-101) | no | keep -- D-04 rule (a) non-obvious WHY for default: banner |
| Taskfile.yml:131-136 | "Source messages.zsh via TASKFILE_DIR (NOT the DOTFILES_MESSAGES var) per the documented DOTFILEDIR pollution workaround..." (5 lines) | TASKFILE_DIR avoids DOTFILEDIR pollution under include-merge | §Don't Do (NEEDS-ADD candidate; see Summary) | yes -- add DOTFILEDIR-leak Don't-Do bullet | keep -- D-04 rule (b) still-live footgun |
| Taskfile.yml:153-158 | "Top-level alias for `manifest:setup`. Every doc and operator-facing error message says `task setup -- <machine-name>`... Without this alias those instructions fail with `Task setup does not exist`." (6 lines) | setup alias preserves docs convention; CLI_ARGS forwarded | none (taskfile-specific) | no | keep -- D-04 rule (a) non-obvious WHY |
| Taskfile.yml:178-194 | "Each per-component validate is `internal: true` (D-01) so it cannot be invoked from a shell command line... ignore_error: true on every dispatch preserves D-04 run-all-aggregate semantics: every component runs regardless of prior failures." (17 lines) | internal-by-default + run-all-aggregate dispatch via `task:` keyword | none (validate-aggregator-specific) | no | rewrite -- preserve WHY (internal: true + ignore_error), strip D-NN tags |
| Taskfile.yml:224-247 | "VRFY-04 + D-10 + ROADMAP P5 success criterion #6: packages:verify is the LAST FATAL task-call... CUTV-08 / D-11: install-time orphan warning -- non-fatal... Source messages.zsh via TASKFILE_DIR (NOT DOTFILES_MESSAGES): root DOTFILEDIR var gets polluted..." (24 lines spanning two blocks) | install-pipeline ordering (verify last fatal; orphan warn-only) + DOTFILEDIR pollution | §Don't Do (NEEDS-ADD for DOTFILEDIR) | yes -- add DOTFILEDIR-leak Don't-Do bullet | keep -- D-04 rule (b) still-live footgun; rewrite to strip planning tags |
| identity/ssh/cloudflared.zsh:1-15 | "ProxyCommand wrapper for cloudflared tunnels. Callers: identity/ssh/identities/personal via ProxyCommand directive... CR-01: SSH ProxyCommand subshells do NOT source .zprofile, so HOMEBREW_PREFIX is absent. Under `set -u` a bare reference to $HOMEBREW_PREFIX would abort the wrapper..." (15 lines) | banner + CR-01 unbound-variable footgun under ProxyCommand subshell | §Rules > set -euo pipefail + §No hardcoded /opt/homebrew | no | rewrite -- preserve unbound-var WHY, strip CR-01 tag, condense to 3-label banner |
| install/compose-brewfile.zsh:1-20 | "install/compose-brewfile.zsh -- compose per-machine Brewfile from manifest. Sourced from: taskfiles/packages.yml..." (20 lines) | banner: purpose / sourced-from / reads / writes / depends | §Conventions Not Captured Above (file-level comment block) | no | rewrite -- collapse to D-01 3-label banner |
| install/compose-brewfile.zsh:24-40 | "Output structure (composed Brewfile written to $XDG_CACHE_HOME/dotfiles/Brewfile): Line 1 -- AUTO-GENERATED header banner... Extras line shapes (canonical; literal single-quotes around the name)... Note: post-Gap-2 (Plan 05-07/05-08)..." (17 lines) | composed-Brewfile output format + post-Gap-2 verify migration | none (script-specific output spec) | no | keep -- D-04 rule (a) non-obvious WHY for the output format; rewrite to strip Gap-2 tag |
| install/compose-brewfile.zsh:42-47 | "Source the messages library. messages.zsh handles its own set -u-safe double-source guard via the `:-` default expansion on $DOTFILES_MESSAGES_LOADED..." (5 lines) | messages.zsh source idempotency | none (messages.zsh contract) | no | keep -- D-04 rule (a) non-obvious WHY (1 line ok) |
| install/compose-brewfile.zsh:60-66 | "Literal single-quote (U+0027), passed to jq as a --arg parameter so the jq filter strings can stay inside zsh single-quote wrapping..." (7 lines) | jq --arg avoids brittle shell-escape nesting | none (jq usage convention) | no | keep -- D-04 rule (a) non-obvious WHY |
| install/compose-brewfile.zsh:68-73 | "read_machine_name -- Return the active machine name from $STATE_DIR/machine if it exists, else fall back..." (6 lines) | function header | none (function-doc) | no | keep -- function-doc, acceptable |
| install/compose-brewfile.zsh:85-91 | "compose <out_tmp> <formulae_json> <casks_json> <mas_json> -- Write the full composed Brewfile content..." (7 lines) | function signature + behavior | none (function-doc) | no | keep -- function-doc, acceptable |
| install/compose-brewfile.zsh:136-145 | "Canonical jq emit form (formulae): brew '<name>' for both shapes. Schema accepts bare strings OR {name, ...} objects (backward-compat per docs/MANIFEST.md:105)... post-Gap-2 pivot (Plan 05-07/05-08), not bundle-comment-driven." (10 lines) | jq emit shape + Gap-2 verify pivot | none (compose-specific) | no | rewrite -- preserve jq emit shape WHY, strip Gap-2 / Plan-NN tags |
| install/compose-brewfile.zsh:162-167 | "main -- Hard-fail on missing resolved.json; read typed extras from resolved.json; validate every declared bundle file exists..." (6 lines) | function header | none (function-doc) | no | keep -- function-doc |
| install/compose-brewfile.zsh:169-170 | "D-16: missing-state hard-fail with actionable message naming the canonical setup task. resolver.zsh:474-481 ships this exact pattern." (2 lines) | missing-state hard-fail convention | §Rules > Manifests as source of truth (close) | no | strip after Plan 14-01 -- D-16 + cross-ref both strip; lesson self-evident in code |
| install/compose-brewfile.zsh:177-180 | "Read typed-bucket extras from resolved.json. The `// []` fallback covers the case where a sub-array is absent (defaults supplies the shape per Plan 02 migration..." (4 lines) | `// []` defense-in-depth for ad-hoc resolved.json | none (jq idiom) | no | rewrite -- strip Plan 02 tag, preserve `// []` WHY |
| install/compose-brewfile.zsh:186-188 | "Validate bundle files exist BEFORE writing anything -- fail-fast keeps the cache directory free of half-composed Brewfile.* tmp files..." (3 lines) | fail-fast keeps cache clean | none | no | keep -- D-04 rule (a) non-obvious WHY |
| install/compose-brewfile.zsh:200-204 | "Atomic write: mktemp in the same directory as the destination so `mv` is an atomic rename across the same filesystem (POSIX rename(2)). Signal trap mirrors resolver.zsh:341-368 verbatim..." (5 lines) | POSIX rename(2) atomicity; signal-trap pattern | none | no | rewrite -- preserve atomicity WHY, strip cross-file line ref |
| install/resolver.zsh:1-28 | "install/resolver.zsh -- compile defaults + machine manifest into resolved.json. Sourced from: taskfiles/manifest.yml..." (28 lines) | banner with modes + hard-fail contract | §Conventions Not Captured Above (file-level comment block) + §Rules | no | rewrite -- collapse to D-01 3-label banner |
| install/resolver.zsh:32-37 | "Source the messages library. messages.zsh handles its own set -u-safe double-source guard..." (5 lines) | messages.zsh source idempotency (same as compose-brewfile) | none | no | keep -- D-04 rule (a), 1-line ok |
| install/resolver.zsh:48-52 | "Machine-name regex: kebab-case identifier; the first character may also be an underscore so test/negative fixture names (e.g. _invalid-bad-os) are accepted by --validate-only." (5 lines) | regex includes leading underscore for fixtures | none | no | keep -- D-04 rule (a) non-obvious WHY |
| install/resolver.zsh:75-87 | "validate_manifest <machine_file> -- Hand-rolled D-03 required-field validator + D-01 os enum + identity enum. Uses yq has() + tag predicates (RESEARCH section 3.3). WR-07 fix: previously this function returned the error count via stdout..." (13 lines) | exit-status-vs-stdout-count convention | none (signaling convention) | no | rewrite -- preserve "use exit status, not stdout, for failure signaling" WHY; strip WR-07 / D-03 / D-01 / RESEARCH §3.3 tags |
| install/resolver.zsh:100-114 | "Required scalar string fields: each must be present (has() == true) and non-empty... NOTE: the loop variable is `field_path`, not `path`. In zsh, `path` is a tied special parameter that mirrors $PATH..." (15 lines) | zsh `path` is tied to $PATH; never use as a local | none | no | keep -- D-04 rule (b) still-live footgun (any future maintainer would re-introduce) |
| install/resolver.zsh:171-177 | "D-01: platform.os must equal `darwin` in v1." (1 strip-pattern line + 6-line block) | macOS-only enforcement | §What This Is ("macOS only in v1") | no | strip after Plan 14-01 -- D-01 tag strips; rule already documented |
| install/resolver.zsh:179-191 | "WR-04: schema_version must be present and equal 1. The entire v2 forward-compat story depends on this field; without an explicit check, a v1 resolver running against a v2 manifest would silently produce wrong output." (13 lines) | schema_version=1 forward-compat enforcement | none (manifest schema rule) | no | keep -- D-04 rule (b) still-live footgun (silent wrong output); rewrite to drop WR-04 tag |
| install/resolver.zsh:193-210 | "identity.git / identity.ssh: filesystem-driven. The value must be the basename of an existing overlay file under identity/<kind>/identities/. Adding a new identity = drop the overlay files; no edits here." (18 lines including loop) | filesystem-driven identity enum; "add identity = drop file" | none (manifest schema rule) | no | keep -- D-04 rule (a) non-obvious WHY |
| install/resolver.zsh:212-238 | "D-16: cross-field rules. identity.ssh in {personal, work} requires features.one-password-ssh = true; identity.git in {personal, work} requires features.one-password-signing = true" (27 lines including case statements) | manifest cross-field validation rules are runtime-checked | none (D-16 NOT in CLAUDE.md) | yes -- add cross-field validation rule paragraph to §Rules > Manifests | rewrite -- strip D-16 tag, preserve WHY; gap-fill into CLAUDE.md |
| install/resolver.zsh:247-251 | "emit_unknown_key_warnings <machine_file> -- Emit `unknown key: <path> at <file>:<line>` warnings to stderr for any scalar leaf path not under the whitelist (D-04). Always exits 0." (5 lines) | unknown-key advisory warnings; always exits 0 | none (function-doc) | no | rewrite -- strip D-04 tag, preserve always-0-exit WHY |
| install/resolver.zsh:289-294 | "WR-02 fix: only attempt the line-number heuristic when the leaf is a strict identifier (no quoted-key metacharacters). TOML permits keys like `a.b` or `a*b` which, if substituted into a regex, would be interpreted as metacharacters..." (6 lines) | quoted-TOML-key footgun in line-number heuristic | none | no | keep -- D-04 rule (b) still-live footgun; rewrite to drop WR-02 tag |
| install/resolver.zsh:307-340 | "resolve_pipeline <defaults_path> <machine_path> -- Run the three-pass merge pipeline (RESEARCH section 4.2 + section 5)... Pass 2 supports TWO `extra_packages` shapes (dual-shape; D-03 typed-bucket migration preserves backward-compat with the Phase 1 flat-array fixture 06)..." (34 lines including dual-shape detail) | dual-shape extra_packages (typed-bucket vs flat) | none (resolver-specific schema doc) | no | rewrite -- strip D-03 / RESEARCH / Phase 1 tags, preserve dual-shape WHY |
| install/resolver.zsh:344-359 | "Pass 1: yq deep-merge with `. * .` (REPLACE arrays, deep-merge maps). Pass 2: union + dedupe extra_packages from both sides. Detect the canonical shape: typed-bucket (!!map) vs legacy flat (!!seq)." (16 lines) | shape detection idiom | none | no | rewrite -- preserve detection-idiom WHY |
| install/resolver.zsh:360-368 | "Legacy flat-array path (Phase 1 fixture 06 + any future plain-list `extra_packages = [...]` shape). `jq -s 'add | unique'` produces sorted output, matching fixture 06's expected.json." (9 lines) | legacy flat-array path provenance | none | no | strip after Plan 14-01 -- Phase 1 + fixture 06 tags strip; one-line "legacy flat-array path" comment remains |
| install/resolver.zsh:393-406 | "formulae dedupe: lift bare strings to { name: ., __bare: true }; group_by .name; within each group prefer an object (drops __bare) over a bare string..." (14 lines) | formulae dedupe semantics | none (jq idiom) | no | keep -- D-04 rule (a) non-obvious WHY |
| install/resolver.zsh:441-445 | "resolve_manifest <defaults_path> <machine_path> <out_path> -- Run the resolve pipeline and atomically write the result to out_path. Uses mktemp + mv to ensure readers never see a partial file (T-MAN-03)." (5 lines) | atomic write via mktemp + mv | none (resolver-specific) | no | rewrite -- strip T-MAN-03 tag |
| install/resolver.zsh:456-462 | "WR-01 fix: register a signal trap before the pipeline so Ctrl-C (SIGINT), SIGTERM, or any other unhandled exit cleans up the tmp file. The previous `{ ... } || { rm -f \"$tmp\"; ... }` only handled ordinary command failures -- repeated Ctrl-Cs during iteration would otherwise accumulate resolved.json.aBcDeF-style siblings..." (7 lines) | signal-trap-before-pipeline pattern | none | no | keep -- D-04 rule (b) still-live footgun (resource leak); rewrite to drop WR-01 tag |
| install/resolver.zsh:475-481 | "resolve_machine_path <machine_name> -- Validate the machine name against the kebab-case regex (path-traversal guard, T-MAN-02), build the canonical manifest path, and verify the resolved path equals the canonical form (no traversal)." (7 lines) | path-traversal guard | none (security control) | no | rewrite -- strip T-MAN-02 tag, preserve path-traversal WHY (security) |
| install/resolver.zsh:558-561 | "WR-07 fix: validate_manifest now signals failure via exit status plus the global VALIDATE_ERRORS, not via stdout. The `|| true` keeps `set -e` from aborting on a non-zero return so we can read the error count and emit a summary message." (4 lines) | exit-status-vs-stdout-count signaling (same lesson as :80) | none | no | strip after Plan 14-01 -- second occurrence; lesson already kept at line 75-87 |
| install/resolver.zsh:587-594 | "WR-08 fix: previously machine_name=\"${machine_name//[[:space:]]/}\" stripped ALL whitespace (including any embedded), so a state file containing `bad name\\n` would be silently rewritten to `badname`, which then either matched a real but unintended machine or failed the regex check confusingly." (8 lines) | first-line+trim-edges idiom (vs strip-all-whitespace) | none (state-file parse convention) | no | keep -- D-04 rule (b) still-live footgun; rewrite to drop WR-08 tag |
| install/resolver.zsh:616 | "D-04 advisory warnings on the active machine after a successful resolve." (1 line) | D-04 reference (warn-only convention) | none | no | strip after Plan 14-01 |
| os/shell-registration.zsh:1-46 | "os/shell-registration.zsh -- Register Homebrew zsh as the login shell. Purpose / Caller / Side effects / Bug-class structural fix (v1 macos:shell:145 + .planning/codebase/CONCERNS.md lines 15-19) / Contract" (46 lines spanning Purpose, Caller, Side effects, Bug-class structural fix, Contract) | banner + macos:shell:145 bug-class history + LINT-02 contract | §Rules > status: block (lines 86-101; v1 macos:shell:145 cited) + §Conventions Not Captured Above | no | rewrite -- collapse to D-01 3-label banner; strip bug-class history block (lesson is in CLAUDE.md and in the structural LINT-02 fix already) |
| os/shell-registration.zsh:50-55 | "Source the messages library. messages.zsh handles its own set -u-safe double-source guard..." (6 lines) | messages.zsh source idempotency (same as compose-brewfile / resolver) | none | no | keep -- 1-line ok |
| os/shell-registration.zsh:57-62 | "Mandatory parameter assertion -- the structural-fix contract on the script side. The macos:shell task heredoc must export BREW_ZSH=... before sourcing this file; sourcing without BREW_ZSH set exits non-zero..." (6 lines) | `:?` parameter assertion contract | none (script-specific) | no | keep -- D-04 rule (b) still-live footgun (silent-unset-var would re-introduce v1 bug class) |
| os/shell-registration.zsh:70-74 | "Read the REGISTERED login shell from Directory Services -- not the currently-running shell ($SHELL). $SHELL reflects the parent process; dscl reads the actual user-record default (RESEARCH Don't-Hand-Roll). head -n 1 is cheap insurance against future dscl output variance (RESEARCH Pitfall 4)." (5 lines) | dscl vs $SHELL distinction; head -n 1 defensive | none | no | rewrite -- strip RESEARCH tags, preserve dscl-vs-$SHELL WHY |
| taskfiles/claude.yml:1-50 | "Phase 7 Claude Code plugin management. Role / Feature gate / Sentinel idempotency (D-09 / D-11 / CLDE-03) / Two-condition marketplace status (D-12 / CLDE-04) / Dynamic sh: var scoping (W-01) / LINT-02 compliance / LINT-03a compliance" (50 lines) | banner + sentinel idempotency + two-condition status + W-01 dynamic-sh scoping | §Rules > Every install task has a status: block + §Rules > kebab-case feature names | no | rewrite -- collapse to D-01 3-label banner; preserve W-01 dynamic-sh-scoping WHY (avoids subprocess at task --list time) as inline comment near the task-scoped sh: vars block |
| taskfiles/claude.yml:78-96 | "install -- public aggregator. Feature-gated on claude-marketplace via status: block (NOT split-cmd `{{if}}` / `{{end}}` -- go-task parses each cmds entry as an independent template..." (19 lines) | split-cmd `{{if}}`/`{{end}}` raises template:1: unexpected EOF | none (template-engine footgun) | no | keep -- D-04 rule (b) still-live footgun; rewrite to strip commit-hash ref |
| taskfiles/claude.yml:113-124 | "marketplace -- internal. Registers marketplaces and installs plugins. Two-condition status: both marketplace list AND plugin list must be converged for the task to no-op (D-12 / CLDE-04). W-01: MARKETPLACES_JSON and PLUGINS_JSON are task-scoped... W-02: status: block uses go-template-rendered literal names only." (12 lines) | task-scoped sh: vars; literal-only status block | none | no | rewrite -- strip D-12 / CLDE-04 / W-01 / W-02 tags, preserve scoping WHY |
| taskfiles/claude.yml:139-146 | "CRITICAL (CR-02): selector field MUST match the install body (line 150) and validate body (line 277), both of which use `.id == $i`. Plugin objects expose `.id` as the fully-qualified `<name>@<marketplace>` token... Mismatching the field here breaks two-condition idempotency (the v1 macos:shell:145 bug class)." (8 lines) | selector field cross-site consistency (plugin .id) | none (claude-specific) | no | keep -- D-04 rule (b) still-live footgun; rewrite to drop CR-02 + v1-bug-class tags |
| taskfiles/claude.yml:176-179 | "gsd -- internal. Installs get-shit-done-cc via npx. Sentinel-idempotent: runs only when GSD_SENTINEL is absent (D-11 / CLDE-03). T-07-12 mitigation: touch AFTER npx; bash errexit propagates npx failure." (4 lines) | sentinel idempotency; touch-after-cmd ordering | none | no | rewrite -- strip D-11 / CLDE-03 / T-07-12 tags, preserve touch-after-cmd WHY |
| taskfiles/claude.yml:197-201 | "update -- public aggregator. Explicit refresh path (D-10 / D-14). NOT in `task install`'s call graph. Run when you want fresh artifacts. LINT-03a: status: [false] (explicit refresh always runs)." (5 lines) | update is explicit-only, not in install pipeline | none | no | rewrite -- strip D-10 / D-14 / LINT-03a tags, preserve "not in install pipeline" WHY |
| taskfiles/claude.yml:238-243 | "validate -- public diagnostic. Enumerates check/cross for all install state. Composed into root `task validate` in P8 (CUTV-01). LINT-03a: status: [false] (diagnostic always-rerun)." (6 lines) | validate-diagnostic composed into root | none | no | rewrite -- strip P8 / CUTV-01 / LINT-03a tags |
| taskfiles/claude.yml:253-265 | "D-06 (P8 CUTV-01): emit sentinel + exit 0 when claude-marketplace is off so the root `task validate` aggregator can render this row as `n/a`. Sentinel substring `feature disabled -- skipped` is contract-bound..." (13 lines spanning D-06 + CR-03) | feature-disabled sentinel string is contract-bound across aggregator | none (validate-aggregator contract) | no | keep -- D-04 rule (b) still-live footgun (sentinel mismatch silently passes broken state); rewrite to drop D-06 / P8 / CUTV-01 / CR-03 tags |
| taskfiles/claude.yml:321-326 | "ensure-cli -- internal gate. Hard-fails when claude or jq is absent. T-07-15 mitigation: explicit [[ $missing -eq 0 ]] as final expression so the task exits non-zero regardless of shell flags." (6 lines) | explicit final-expression for exit-code propagation | none | no | rewrite -- strip T-07-15 tag, preserve final-expression WHY |
| taskfiles/identity.yml:1-44 | "Phase 4 manifest-driven git + SSH identity layer. Purpose / Dependencies / Status-block convention (LINT-02 enforcement) / Symlink convention (LINT-03b enforcement) / Kebab-case feature keys / Aggregator placement (D-08 + RESEARCH Open Question 4)" (44 lines) | banner + LINT-02 + LINT-03b + kebab-case + aggregator placement | §Rules covers all four lessons (lines 68-101, 115-119) | no | rewrite -- collapse to D-01 3-label banner; all four lessons live in CLAUDE.md |
| taskfiles/identity.yml:63-87 | "Resolve DOTFILEDIR from this taskfile's parent directory. WR-03 fallback: emit a stderr warning when resolved.json is missing... The stub includes an empty `features` map so `{{if index .MANIFEST.features ...}}` evaluates to false instead of erroring out at template-parse time on a fresh machine." (25 lines spanning WR-03 + fromJson ref) | empty-features stub avoids template-parse error (same as Taskfile.yml:54-60) | §Rules > kebab-case feature names (lines 68-84) | no | rewrite -- preserve empty-features-stub WHY (1-2 lines), strip WR-03 tag |
| taskfiles/identity.yml:105-108 | "Aggregator -- composes git + ssh subtasks. Depends on manifest:resolve so resolved.json is current before any symlink decision." (4 lines) | aggregator dep ordering | none | no | strip after Plan 14-01 -- restatement of `deps: [":manifest:resolve"]` |
| taskfiles/identity.yml:120-126 | "Git side -- two static symlinks (config, ignore) plus a filesystem-driven loop over every overlay file in identity/git/identities/. Adding a new identity = drop a file in that directory; no edits to this task." (7 lines) | "add identity = drop file" convention | none (identity-specific) | no | keep -- D-04 rule (a) non-obvious WHY (operator-facing convention) |
| taskfiles/identity.yml:136-151 | "CR-02: MANIFEST_JSON falls back to '{}' when resolved.json is missing, which renders {{.MANIFEST.identity.git}} as the literal string `<no value>`. Refuse to install against the empty fallback rather than silently materializing broken state. The second precondition is the filesystem-driven enum check..." (16 lines spanning preconditions block) | empty-fallback footgun + filesystem-driven enum check | none | no | keep -- D-04 rule (b) still-live footgun (silent broken state); rewrite to drop CR-02 tag |
| taskfiles/identity.yml:157-159 | "Filesystem-discovered identity overlays: one symlink per file in identity/git/identities/. Adding an identity = a new file; this loop picks it up automatically. No edits to this task or the resolver." (3 lines) | self-discovering loop | none | no | keep -- D-04 rule (a) WHY |
| taskfiles/identity.yml:177-183 | "server-include -- internal generator (D-08). Gated by features.server-include in resolved.json (kebab-case index form). When on: materialize ~/.config/git/server-include.config..." (7 lines) | server-include feature gate behavior | §Rules > kebab-case feature names | no | rewrite -- strip D-08 tag |
| taskfiles/identity.yml:202-213 | "WR-07: exact-match check against the canonical two-line content the cmd writes. A loose grep would pass on stale-content cases (e.g., a file containing the wrong identity path from a prior install) and skip the rewrite that fixes them." (12 lines) | exact-content status check vs loose grep | none | no | keep -- D-04 rule (b) still-live footgun (stale content silently passes); rewrite to drop WR-07 tag |
| taskfiles/identity.yml:215-220 | "SSH side -- main config, filesystem-driven loops over identity overlays and public keys, the cloudflared wrapper, and the active symlink swap (D-12). Adding an identity = drop a file under identity/ssh/identities/..." (6 lines) | ssh-side convention | none | no | rewrite -- strip D-12 tag |
| taskfiles/identity.yml:230-245 | "CR-02: MANIFEST_JSON falls back to '{}' when resolved.json is missing, which renders {{.MANIFEST.identity.ssh}} as the literal string `<no value>`. Without these preconditions the active-symlink swap below would create ~/.ssh/identities/active -> .../<no value> (silent corruption of the active SSH identity)." (16 lines spanning preconditions) | empty-fallback footgun for ssh-active swap | none | no | keep -- D-04 rule (b) still-live footgun (silent corruption of active SSH); rewrite to drop CR-02 tag |
| taskfiles/identity.yml:264-267 | "Active-identity swap (D-12): relink at install time; readlink resolves to a short name like `personal` since SOURCE is given as a sibling path." (4 lines) | active-symlink swap idiom | none | no | rewrite -- strip D-12 tag |
| taskfiles/identity.yml:284-291 | "one-password-agent -- link the 1Password SSH agent config (agent.toml). Isolated as its own sub-task with a feature-gated status block... Mirrors the configs:ghostty pattern in taskfiles/links.yml..." (8 lines) | feature-gate isolation pattern | none | no | rewrite -- preserve isolation WHY, strip cross-file ref |
| taskfiles/identity.yml:301-307 | "validate -- always-re-run diagnostic. Four assertion blocks: (a) symlink presence / (b) git config user.email match / (c) ssh-add -L key match / (d) identity/ssh/keys/ allowlist (no private keys committed)" (7 lines) | validate assertion breakdown | none | no | keep -- D-04 rule (a) non-obvious WHY (validate structure) |
| taskfiles/identity.yml:362-373 | "Resolve the probe source. Two patterns based on features.server-include: server-include = true: server-include.config carries an unconditional [includeIf gitdir:~/] block, so the identity loads from $HOME directly. server-include = false: workstation pattern... WR-08 fix (UAT gap 4): the previous implementation used the parent directory..." (12 lines) | gitdir-conditioned probe-source dispatch | none | no | keep -- D-04 rule (b) still-live footgun (gitdir-conditioned includeIf needs real git repo); rewrite to drop WR-08 / UAT-gap-4 tags |
| taskfiles/identity.yml:394-403 | "Verify $probe_dir is actually a git work tree before running config queries. Replaces the prior `[[ -d \"$gitdir\" ]]` guard which only checked directory existence, not git-repo-ness..." (10 lines) | git-repo-ness check vs directory-existence | none | no | keep -- D-04 rule (a) non-obvious WHY |
| taskfiles/identity.yml:431-446 | "WR-01: a real pub key starts with one of the SSH key-type identifiers. The placeholder shape (a leading `#` comment) would otherwise pass the `! -s` guard above and produce a misleading `ssh-add -L does not include expected pub key` error... WR-06: anchor on $1 (must be a real key-type field) and `exit` after the first match so the extracted key body is always a single line." (16 lines) | placeholder-pubkey footgun; awk-anchor convention | none | no | keep -- D-04 rule (b) still-live footgun; rewrite to drop WR-01 / WR-06 tags |
| taskfiles/identity.yml:459-468 | "(d) keys/ allowlist -- only *.pub files and .gitignore allowed (IDNT-06). WR-05: no -maxdepth -- a private key smuggled into a subdirectory of identity/ssh/keys/ would otherwise escape this validator (defense in depth for IDNT-06)." (10 lines) | no-maxdepth defense in depth | none (security control) | no | keep -- D-04 rule (b) still-live footgun (private-key smuggling); rewrite to drop IDNT-06 / WR-05 tags |
| taskfiles/links.yml:1-39 | "Phase 3 link orchestration (real implementation). Purpose / Dependencies / Status-block convention (LINT-02 enforcement) / Symlink-creation convention (LINT-03b enforcement)" (39 lines including the 22-line LINT-02 explainer) | banner + LINT-02 + LINT-03b explainers | §Rules > Every install task has a status: block (lines 86-101) + §Rules > Symlinks via _:safe-link (lines 115-119) | no | rewrite -- collapse to D-01 3-label banner; strip the 22-line LINT-02 explainer (CLAUDE.md §Rules covers it) |
| taskfiles/links.yml:66-80 | "EXPECTED_TARGETS (D-08) -- single source of truth for every symlink target owned by this taskfile. The entries below cover the zsh + claude + tool-configs sub-tasks; identity-layer symlinks ({{.GIT_CONFIG_DIR}}/config, {{.SSH_DIR}}/config) are intentionally EXCLUDED..." (15 lines) | EXPECTED_TARGETS contract + scope boundary | none (links-specific) | no | rewrite -- strip D-08 tag, preserve scope-boundary WHY |
| taskfiles/links.yml:115-118 | "Aggregator (D-09 internal: <ns>:install pattern; D-01 per-component install internal). P3 + P4 wired; later phases extend this list." (4 lines) | internal aggregator pattern | none | no | strip after Plan 14-01 -- D-09 / D-01 / P3 / P4 tags strip; aggregator pattern is conventional |
| taskfiles/links.yml:125-130 | "Leading-colon absolute form: when this file is loaded as the `links:` namespace from the root Taskfile.yml, a bare `task: identity:install` resolves relative to the current namespace and tries `links:identity:install` (which does not exist). Same fix pattern as commit d94168e applied to identity.yml's [:manifest:resolve] dep." (6 lines) | leading-colon absolute-form footgun in go-task namespaces | none (go-task convention) | no | keep -- D-04 rule (b) still-live footgun; rewrite to drop commit-hash ref |
| taskfiles/links.yml:136 + 154-158 | "Shell startup files (SHEL-03)... REVW-05 (Plan 13-05): every links:* install-style status block verifies the symlink TARGET via `readlink -f` against the manifest-expected source path, not just `test -L` existence." (6 lines) | readlink -f target-match status check | none (links-specific) | no | keep -- D-04 rule (b) still-live footgun (broken symlink target silently passes `test -L`); rewrite to drop REVW-05 / SHEL-03 / Plan 13-05 tags |
| taskfiles/links.yml:167-183 | "PORT-01: ZDOTDIR write to /etc/zshenv (sudo, idempotent). Direct port of v1 taskfiles/common.yml:36-57 with the LINT-02 hardening from D-04: status block uses the {{.ZDOTDIR}} template var rather than a shell $ZDOTDIR reference, so the idempotency check evaluates correctly at task-graph build time (not the v1 macos:shell:145 bug class). Three-branch cmd body mirrors v1 verbatim... /etc/zshenv is world-readable on macOS so the grep -qF check in status runs without sudo and re-runs never prompt (D-03). Marked internal: true to keep it out of `task --list`; dispatched via `task: zdotdir` from the outer `zsh:` task (precedent: configs:ghostty extraction at lines 269-277 below)." (17 lines) | LINT-02 template-var status; /etc/zshenv world-readable | §Rules > Every install task has a status: block | no | rewrite -- collapse to 3-4 lines preserving "world-readable so no sudo on status re-check" + "template var avoids v1 bug class" WHY; strip PORT-01 / D-04 / D-03 / v1 / lines:269-277 tags |
| taskfiles/links.yml:206-211 | "Claude config tree (CLDE-01) -- feature-gated on claude-marketplace. 13 entries: 2 file + 3 dir + 8 hook file symlinks per D-01. On servers where claude-marketplace=false the gate skips the entire body; the status: block mirrors the gate so status checks also no-op." (6 lines) | gate-mirror in status block | none | no | rewrite -- strip CLDE-01 / D-01 tags, preserve gate-mirror WHY |
| taskfiles/links.yml:244-248 | "REVW-05 (Plan 13-05): inline readlink -f target-match check inside the feature-gate wrapper. The {{if not (index .MANIFEST.features ...)}} arm short-circuits to true when the flag is off..." (5 lines) | gate-wrapper around readlink target-match | none | no | rewrite -- strip REVW-05 / Plan 13-05 tags |
| taskfiles/links.yml:264-268 | "Tool configs (TOOL-01, TOOL-02) -- 7 symlinks; ghostty gated on ghostty feature flag; glow, trippy, tlrc, conda, eza always-on (D-07). motd intentionally absent -- D-08 (runtime read from DOTFILEDIR)." (5 lines) | tool-configs scope + motd-absence rationale | none | no | rewrite -- strip TOOL-01 / TOOL-02 / D-07 / D-08 tags |
| taskfiles/links.yml:288-296 | "REVW-05 (Plan 13-05): readlink -f target-match verification per row... CR-04: ghostty MUST appear in this status block. Without it, on a ghostty=true machine where the ghostty link is broken but the always-on configs are healthy, status returns 0 and go-task skips the entire cmds block -- including `task: configs:ghostty`." (9 lines) | per-row readlink target-match; ghostty-in-status-block footgun | none | no | keep -- D-04 rule (b) still-live footgun (skipped feature-gated sub-task); rewrite to drop REVW-05 / CR-04 tags |
| taskfiles/links.yml:305-312 | "configs:ghostty -- ghostty config symlink isolated as its own sub-task so the inline-ternary status: gate works independently of the always-on tool configs. When `ghostty=false`, status returns `true` and cmds: never executes..." (8 lines) | gate-isolation pattern | none | no | keep -- D-04 rule (a) non-obvious WHY (isolation rationale) |
| taskfiles/links.yml:321-323 | "REVW-05 (Plan 13-05): inline readlink -f target-match check inside the ghostty feature-gate wrapper." (3 lines) | (same as :244-248) | none | no | strip after Plan 14-01 -- duplicate of :244-248 lesson |
| taskfiles/links.yml:336-360 | "Shell-block validator (D-08 / 08-01 P1 rewrite). Iterates EXPECTED_TARGETS line-by-line and verifies each symlink with the same three-condition logic as helpers.yml _:check-link (lines 48-73) BUT increments an explicit failures counter on each failing path, ending with `exit \"$failures\"`. This closes the _:check-link-always-exits-0 gap so root `task validate` (Plan 02) can bubble links:validate failures via exit code. Feature-off entries in EXPECTED_TARGETS render as empty lines... The TARGET -> SOURCE mapping uses a single case statement keyed on target path patterns..." (25 lines) | exit-code bubbling vs always-0; feature-off elision via empty lines | none (validate-specific) | no | keep -- D-04 rule (a) non-obvious WHY; rewrite to drop D-08 / 08-01 / Plan 02 tags |
| taskfiles/links.yml:431-456 | "links:reconcile (CUTV-02 + CUTV-07 + CUTV-08) -- orphan symlink detection. Three invocation modes, dispatched via CLI_ARGS... Orphan detection (D-09) walks ONLY parent dirs derived from EXPECTED_TARGETS with `find -maxdepth 2 -type l`... Security: --remove mode TTY-gates BEFORE orphan enumeration ([[ -t 0 ]]) / removal uses `unlink` (operates only on the named symlink) / parent_dirs deduplication uses word-split-safe `while IFS= read -r` pattern..." (26 lines) | mode-dispatch + TTY-gate-before-enumeration + unlink-not-rm security | none (reconcile-specific) | no | keep -- D-04 rule (b) still-live footgun (security mechanics: TTY-gate, unlink-not-rm, word-split-safe); rewrite to drop CUTV-* / D-09 tags |
| taskfiles/links.yml:480-486 | "--remove TTY-gate (D-10 / Pitfall 6). MUST appear BEFORE any orphan enumeration so non-TTY callers fail fast." (3 lines) | TTY-gate-before-enumeration rule | (same as :431-456) | no | rewrite -- preserve "MUST appear BEFORE enumeration" WHY, strip D-10 / Pitfall 6 tags |
| taskfiles/links.yml:520-527 | "NOTE on `set -e` interaction: the root Taskfile.yml sets [errexit, pipefail] globally and the include inherits it. `(( expr ))` evaluates to exit-1 when expr is zero -- under errexit that aborts the script. So we use `if ... ; then ... ; fi`..." (8 lines) | `(( ))` arithmetic vs errexit interaction | none (bash idiom) | no | keep -- D-04 rule (b) still-live footgun |
| taskfiles/links.yml:597-606 | "WR-03: defensive catch-all. The three valid modes (detect, warn, remove) are the only values $mode can take given the initialization at line 408 + the two case mutators above; this branch surfaces a future flag-parsing bug or refactor mistake as a hard error instead of a silent no-op (exit 0 with no orphan action taken)." (10 lines) | defensive catch-all for unknown-mode | none | no | keep -- D-04 rule (b) still-live footgun (silent no-op on future refactor); rewrite to drop WR-03 tag |
| taskfiles/lint.yml:1-35 | "lint suite for v2 dotfiles conventions. Enforces structural correctness rules that prevent recurring v1 bug classes. - LINT-02: $VAR in status: blocks (the macos:shell:145 $BREW_ZSH bug class) - LINT-03a: cmds: without status: ... LINT-08 is REUSED per D-13: banner-parity check... Self-exemption: taskfiles/lint.yml tasks intentionally omit status: blocks..." (35 lines) | LINT-NN catalogue documentation | §Rules > Every install task has a status: block + §Rules > Symlinks via _:safe-link (partial coverage; full LINT-NN catalogue NOT in CLAUDE.md) | yes -- add LINT-NN catalogue (LINT-01..08) to CLAUDE.md or .claude/rules/lint.md | rewrite -- collapse to D-01 3-label banner; once LINT-NN catalogue lands in CLAUDE.md/lint.md, the per-rule prose here can compress further |
| taskfiles/lint.yml:71-75 | "Default aggregator (LINT-06). NOTE: lint tasks intentionally have no status: block -- they are always re-run. This is by design; do not add status: here." (5 lines) | self-exemption rationale | once LINT-NN catalogue lands | no | keep -- D-04 rule (c) cites a lint-suite policy + (a) non-obvious WHY |
| taskfiles/lint.yml:80-87 | "Each sub-check carries ignore_error: true so the aggregator runs every check regardless of prior failures (run-all-aggregate semantics, mirrors the root validate: aggregator at Taskfile.yml:177-190). Without this, one early failure (e.g., a pre-existing LINT-02 baseline in manifest.yml) aborts the pipeline and downstream checks like LINT-08 banner-parity never execute." (8 lines) | run-all-aggregate semantics + ignore_error rationale | none | no | keep -- D-04 rule (a) non-obvious WHY |
| taskfiles/lint.yml:100-103 | "LINT-07: zsh -n parse errors + task --list-all --json YAML parse baseline" (4 lines) | section header | once LINT-NN catalogue lands | no | strip after Plan 14-01 -- header redundant with the per-task desc |
| taskfiles/lint.yml:108-114 | "YAML parse check: task --list-all --json exits non-zero on parse errors. Exit-1 with empty task list (all internal: true) is NOT a parse error; we detect real failures by checking stderr for `Failed to parse`." (5 lines) | task --list-all exit-code disambiguation | none | no | keep -- D-04 rule (b) still-live footgun |
| taskfiles/lint.yml:141-156 | "LINT-02 + LINT-03a + LINT-03b" + "Taskfile convention checks: $VAR in status:, cmds: without status:, bare symlink outside helpers..." + "All three checks run in a single block so all produce output even when one fails." (16 lines including section header + desc + intro) | LINT-02/03a/03b combined-pass + accumulate-failures rationale | once LINT-NN catalogue lands | no | rewrite -- collapse section header once catalogue exists |
| taskfiles/lint.yml:158-160 | "LINT-02: $VAR-style shell variables inside status: blocks. The macos:shell:145 bug class: $BREW_ZSH is unset in status eval context so the check always fails and the task always re-runs." (3 lines) | LINT-02 rationale | §Rules > Every install task has a status: block | no | strip after Plan 14-01 -- restatement of CLAUDE.md §Rules and immediate code body |
| taskfiles/lint.yml:175-177 | "LINT-03b: bare ln -s outside taskfiles/helpers.yml. Allowlist: helpers.yml is the only sanctioned location for bare ln. Comment lines (preceded only by whitespace + #) are excluded." (3 lines) | LINT-03b rationale | §Rules > Symlinks via _:safe-link (lines 115-119) | no | strip after Plan 14-01 -- restatement of CLAUDE.md §Rules |
| taskfiles/lint.yml:189-211 | "LINT-03a: tasks with cmds: but no status: block. Exemptions: internal: true tasks... tasks whose cmds: are ALL task: delegations... taskfiles/lint.yml itself. Self-exemption: lint.yml tasks are intentionally always-re-run. Skip internal: true tasks. Skip tasks whose cmds are ALL task: delegations. Use `map(has(\"task\")) | all` (no-arg `all` builtin) because the `all(filter)` form returns a yq syntax error -- and that error was masked here behind 2>/dev/null + ||echo false, so the exemption silently failed and orchestrator tasks were mis-flagged. Bracket-indexing `.tasks[\"$task_name\"]` keeps the query robust when $task_name collides with a yq builtin name such as `all`." (23 lines spanning multiple inline blocks) | yq `all(filter)` syntax-error footgun; bracket-indexing for builtin-name collisions | none (yq idiom) | no | keep -- D-04 rule (b) still-live footgun (silently-masked yq error); rewrite to compress |
| taskfiles/lint.yml:219-221 | "LINT-04: executable .zsh files missing set -euo pipefail" (3 lines) | section header | once LINT-NN catalogue lands | no | strip after Plan 14-01 |
| taskfiles/lint.yml:249-251 | "LINT-05: portability hints (warn-only, always exit 0)" (3 lines) | section header | once LINT-NN catalogue lands | no | strip after Plan 14-01 |
| taskfiles/lint.yml:258-264 | "Scans shell/ and os/ directories for macOS-only commands. These directories do not exist in P2 (Phase 3 ships shell/, Phase 6 ships os/). The ggrep invocations tolerate missing directories via 2>/dev/null and || true." (7 lines) | scan scope + Phase-N forward look (now obsolete) | none | no | rewrite -- strip Phase 3 / Phase 6 / P2 tags; preserve "tolerates missing directories" WHY if non-obvious |
| taskfiles/lint.yml:279-283 | "LINT-08 (NEW per D-13): banner-parity check -- default:'s cmd block must list every non-internal top-level task (no `:` in the name). Catches drift the moment someone adds a public top-level task without updating the banner. The LINT-08 rule number was deprecated (see file header) and is reused here." (5 lines) | LINT-08 rationale + reuse-of-number provenance | once LINT-NN catalogue lands | no | rewrite -- strip D-13 + "reused" provenance; LINT-NN catalogue documents this |
| taskfiles/lint.yml:291-296 | "The root Taskfile.yml `default:` task is the curated operator surface banner; every non-internal top-level task (no `:` in the name) MUST appear in its cmd block, otherwise operators miss commands rendered by `task --list` but not by bare `task`." (6 lines) | banner-parity contract | once LINT-NN catalogue lands | no | rewrite -- compress to 1-2 lines |
| taskfiles/lint.yml:314-316 | "Self-test runner (Plan 02-05 will replace this body)" (3 lines) | obsolete forward-look | none | no | strip after Plan 14-01 -- Plan 02-05 is past tense; the body is current |
| taskfiles/lint.yml:391-396 | "LINT-08 (D-13): banner-parity check against THIS fixture's Taskfile.yml (not the project root). Replicates the banner-parity rule body: extract public top-level tasks (no `:` in name; internal: true excluded), grep each name against default.cmds[0]. fail if any missing." (6 lines) | fixture replicates banner-parity rule body | none (test-runner specific) | no | rewrite -- strip D-13 tag |
| taskfiles/macos.yml:1-48 | "Phase 6 macOS OS-defaults + shell-registration layer. Purpose / Dependencies / Status-block convention (LINT-02 enforcement) / Per-concern feature-gate pattern (RESEARCH Pattern 2) / Bug-class structural fix (v1 macos:shell:145)" (48 lines) | banner + LINT-02 + per-concern gate + v1 macos:shell:145 history | §Rules > Every install task has a status: block (lines 86-101; v1 macos:shell:145 cited) | no | rewrite -- collapse to D-01 3-label banner; per-concern-gate-pattern WHY can stay as inline 3-line note at first apply-defaults:dock task; bug-class history strips |
| taskfiles/macos.yml:63-91 | "Resolve DOTFILEDIR from this taskfile's parent directory. WR-03 fallback: emit a stderr warning when resolved.json is missing... empty `features` map so `{{if index .MANIFEST.features \"<key>\"}}` evaluates to false instead of erroring out at template-parse time on a fresh machine." (29 lines) | same empty-features-stub lesson (third occurrence) | §Rules > kebab-case feature names | no | rewrite -- preserve 1-2-line empty-features-stub WHY; strip WR-03 tag |
| taskfiles/macos.yml:113-118 | "install -- macOS aggregator (D-09 <ns>:install pattern). Replaces the two-call pattern in Taskfile.yml install: body (macos:apply-defaults + macos:install-shell). Internal per D-01 (per-component install is internal); platforms: [darwin] matches the sub-tasks." (6 lines) | macos-install aggregator scope | none | no | rewrite -- strip D-09 / D-01 tags |
| taskfiles/macos.yml:130-137 | "apply-defaults -- aggregator (was: defaults; renamed per D-10 -- macOS uses apply- for non-install actions). Calls each macos:apply-defaults:<concern> in declared order (matches OSCF-01: dock -> finder -> input -> screenshots -> security)." (8 lines) | rename history + invocation order | none | no | rewrite -- strip D-10 / OSCF-01 + "was: defaults; renamed" history |
| taskfiles/macos.yml:157-163 | "apply-defaults:<concern> -- five per-concern tasks, identical shape (was: defaults:<concern>; renamed in lockstep with the apply-defaults parent per B-9 / D-10 verb-first consistency). RESEARCH Pattern 2: single-shell-block status with template-time short-circuit." (7 lines) | single-shell-block status with template-time short-circuit | none (subtle template idiom) | no | keep -- D-04 rule (b) still-live footgun (broken two-line ANDed pattern); rewrite to drop "was: defaults; renamed" history + RESEARCH / B-9 / D-10 tags |
| taskfiles/macos.yml:235-244 | "install-shell -- always-on (was: shell; renamed per D-10 -- shell registration IS an install action). No feature gate. Adds Homebrew zsh to /etc/shells and runs chsh. RESEARCH Pattern 5 -- the structural LINT-02 fix..." (10 lines) | install-shell purpose + LINT-02 structural fix | §Rules > Every install task has a status: block | no | rewrite -- collapse to 2-3 lines preserving "BREW_ZSH heredoc + status uses template var" WHY |
| taskfiles/macos.yml:252-258 | "USER_NAME hoisted out of the status: block to close a LINT-02 blind spot. The LINT-02 regex skips lines containing `{{`, so a $VAR shell-var on a line that ALSO carries a {{.TEMPLATE_VAR}} reference slips through unscanned. Hoisting via `sh: id -un` at task-graph build time gives the status block a pure template var..." (7 lines) | LINT-02 blind-spot footgun for mixed-token lines | (LINT-NN catalogue would land near this) | no | keep -- D-04 rule (b) still-live footgun (blind-spot in lint regex); rewrite to drop LINT-02 tag if catalogue exists |
| taskfiles/macos.yml:264-268 | "LINT-02 contract: every status condition uses {{.TEMPLATE_VAR}} references only -- zero shell-var (dollar-prefixed) references to BREW_ZSH or USER appear in this block. Structural fix for the v1 macos:shell:145 bug class (CONCERNS.md lines 15-19)." (5 lines) | LINT-02 contract restatement | §Rules > Every install task has a status: block | no | strip after Plan 14-01 -- restatement of CLAUDE.md §Rules |
| taskfiles/macos.yml:272-279 | "validate -- always-rerun aggregator. Sources every enabled concern's script and runs verify_<concern>; sources shell-registration.zsh always and runs verify_shell_registration. Prints check/cross output via messages.zsh. Enumerate-all (no fast-fail) per OSCF-05 + D-07 pattern: each verify ORs into failed flag; final exit \"$failed\" returns 0 (clean) or 1 (any drift). status: [false] satisfies LINT-03a always-rerun; marker is documentation." (8 lines) | enumerate-all + status: [false] always-rerun | once LINT-NN catalogue lands | no | rewrite -- strip OSCF-05 / D-07 / LINT-03a tags, preserve enumerate-all WHY |
| taskfiles/macos.yml:288-300 | "All sourced scripts use zsh-specific syntax (typeset -ga; 1-indexed arrays). Run the validate body in an explicit `zsh -c` so the script interpreter matches the scripts' contract. DOTFILEDIR and BREW_ZSH forwarded via env; the inner script-single-quotes keep ${DOTFILEDIR}, ${BREW_ZSH}, $failed unexpanded by the outer mvdan/sh shell." (13 lines including messages-source note) | zsh-vs-mvdan/sh interpreter dispatch + quoting | none | no | keep -- D-04 rule (a) non-obvious WHY |
| taskfiles/manifest.yml:1-20 | "taskfiles/manifest.yml -- go-task module: manifest engine orchestration. Exposes the manifest engine (install/resolver.zsh) to humans and downstream taskfiles. Wires Plan 02's resolver against Plan 01's fixtures... Phase 12 Plan 07: the `manifest:test` + `manifest:test:add-machine` task bodies were moved to `taskfiles/test.yml`..." (20 lines) | banner + Phase 12 Plan 07 move history | §Conventions Not Captured Above (file-level comment block) | no | rewrite -- collapse to D-01 3-label banner; strip Phase 12 Plan 07 move history (now baked into current code) |
| taskfiles/manifest.yml:25-58 | "Vars block -- self-contained so `task -t taskfiles/manifest.yml ...` works without the root Taskfile.yml being loaded... Resolve DOTFILEDIR from this taskfile's parent directory. go-task 3.37+ provides {{.TASKFILE_DIR}}... NOTE: the prior `dirname dirname realpath ${TASKFILE:-$0}` pattern silently fell back to cwd when $TASKFILE is not exported..." (34 lines) | TASKFILE vs TASKFILE_DIR + standalone-invocation contract | §Tooling Versions (go-task 3.37) | no | rewrite -- collapse to 5-line "vars: standalone-invocation contract" + one-line TASKFILE_DIR note; strip "prior pattern" history |
| taskfiles/manifest.yml:71-77 | "RESEARCH §6.1: sh: block evaluated at task invocation time. WR-08 / IN-02 fix: previous `cat | tr -d '[:space:]'` would silently rewrite `bad name\\n` -> `badname` by stripping ALL whitespace, not just edges. Read just the first line of the state file (it is single-line by contract) and trim leading/trailing whitespace via sed; this preserves any embedded whitespace so the downstream MACHINE_NAME_RE check rejects malformed values cleanly." (7 lines) | first-line+trim-edges idiom (same as resolver.zsh:587-594) | none (state-file parse convention) | no | keep -- D-04 rule (b) still-live footgun (silent malformed-state corruption); rewrite to drop RESEARCH §6.1 / WR-08 / IN-02 tags |
| taskfiles/manifest.yml:83-103 | "Resolved manifest JSON (used for fromJson consumption, MFST-06). WR-03 fix: emit a stderr warning when the resolved.json file is missing or empty so Phase 2+ tasks that rely on {{.MANIFEST.identity.git}} fail loudly instead of silently evaluating to <no value>..." (21 lines) | empty-features stub (fourth occurrence; same lesson) | §Rules > kebab-case feature names | no | rewrite -- preserve 1-2-line empty-features-stub WHY; strip MFST-06 / WR-03 / Phase 2+ tags |
| taskfiles/manifest.yml:105-109 | "fromJson ref: verified syntax (RESEARCH §6.1, PATTERNS Pattern 2). Downstream: {{.MANIFEST.identity.git}}. Kebab keys: {{index .MANIFEST.features \"one-password-ssh\"}}" (5 lines) | fromJson usage shape | §Rules > kebab-case feature names | no | rewrite -- strip RESEARCH / PATTERNS tags |
| taskfiles/manifest.yml:124-135 | "requires: is a hard-fail when the var is absent (no arg passed). CR-03 fix: pass CLI_ARGS via the environment instead of templating it into shell-quoted text. Go-task substitutes {{.CLI_ARGS}} into the rendered shell *before* the shell parses it, so any apostrophe in user input would unterminate the surrounding single-quoted string..." (12 lines) | CLI_ARGS via env vs template-substitution footgun | none | no | keep -- D-04 rule (b) still-live footgun (user-input apostrophe breaks shell parse); rewrite to drop CR-03 tag |
| taskfiles/manifest.yml:137-150 | "preconditions: enforces T-MAN-02 (path-traversal guard) and manifest file existence. Both checks run before any state-file write. RESEARCH §12 security hardening: kebab-case regex then file-existence. NOTE: the setup task allows underscore-prefixed names (e.g. _addmachine-test) so the regex is slightly wider than pure kebab-case..." (14 lines) | path-traversal guard + underscore allowance | §Rules > Manifests as source of truth (close) | no | keep -- D-04 rule (b) still-live footgun (path traversal); rewrite to drop T-MAN-02 / RESEARCH §12 tags |
| taskfiles/manifest.yml:165-174 | "resolve. D-16: missing-state = hard fail with actionable error (PATTERNS Pattern 5). preconditions: test -f {{.STATE_FILE}}; msg: no machine selected / run: task setup..." (10 lines including preconditions block) | missing-state hard-fail with actionable error | none (manifest UX convention) | no | rewrite -- preserve actionable-error WHY, strip D-16 / PATTERNS tag |
| taskfiles/manifest.yml:180-186 | "Idempotency via BSD-find mtime check (RESEARCH §7, PATTERNS Pattern 4). S4: status blocks use {{.X}} template vars only -- never $X shell vars." (7 lines) | mtime-based idempotency status check | §Rules > Every install task has a status: block | no | rewrite -- strip RESEARCH §7 / PATTERNS / S4 tags; preserve mtime-idempotency WHY |
| taskfiles/manifest.yml:197-235 | "show. No preconditions -- resolver hard-fails with actionable error if no state. No status -- this is a read/print task; always re-run. CR-03 fix: pass CLI_ARGS via env... Parse --machine NAME from CLI_ARGS if present (D-17). Use sed for extraction -- portable across gosh/sh/zsh (no zsh match[]). WR-06 fix: also accept a bare machine name... WR-08 fix: read first line + trim edges only..." (39 lines) | bare-name vs --machine NAME accept; sed-vs-zsh-match[] portability; first-line+trim-edges (third occurrence) | none | no | keep -- D-04 rule (b) still-live footguns (apostrophe, malformed state); rewrite to drop CR-03 / D-17 / WR-06 / WR-08 tags |
| taskfiles/manifest.yml:241-275 | "validate. CR-03 fix: pass CLI_ARGS via env... Parse --machine NAME from CLI_ARGS if present. Use sed for extraction -- portable across gosh/sh/zsh. WR-06 fix: accept bare machine name as a friendly alternative. WR-08 fix: read first line + trim edges only..." (35 lines) | duplicate of show: block (same lessons) | none | no | rewrite -- factor out shared helper OR collapse comments to one-line citations of helper |
| taskfiles/manifest.yml:288-291 | "Delegate schema validation to resolver (D-03, D-04, T-MAN-01). Resolver's non-zero exit propagates the failure through go-task." (4 lines) | delegation + exit-code propagation | none | no | rewrite -- strip D-03 / D-04 / T-MAN-01 tags |
| taskfiles/packages.yml:1-35 | "Phase 5 manifest-driven package layer. Purpose / Dependencies / Status-block convention (LINT-02 enforcement)" (35 lines) | banner + LINT-02 | §Rules > Every install task has a status: block | no | rewrite -- collapse to D-01 3-label banner |
| taskfiles/packages.yml:54-89 | "NEW in Phase 5: XDG_CACHE_HOME is the destination for the composed Brewfile (D-08; CF-07). Identity / manifest layers did not need this. WR-03 fallback: emit a stderr warning when resolved.json is missing... Derived path: the composed per-machine Brewfile (Plan 03 composer writes here; D-08 atomic compose...)" (36 lines spanning XDG_CACHE_HOME + MANIFEST_JSON + COMPOSED_BREWFILE) | XDG_CACHE_HOME rationale + empty-features stub (fifth occurrence) | §XDG everywhere (lines 121-131) + §Rules > kebab-case feature names | no | rewrite -- preserve 1-line XDG_CACHE_HOME purpose + 1-line empty-features-stub; strip Phase 5 / D-08 / CF-07 / WR-03 / Plan 03 tags |
| taskfiles/packages.yml:98-106 | "install -- single task with the D-09 two-condition status block. (a) composed Brewfile exists / (b) brew bundle check --no-upgrade exits 0. Either fails -> regenerate the composed Brewfile, then brew bundle install. Realizes PKGS-02 (compose-and-install path), PKGS-03 (brew bundle check for status), PKGS-05 (servers' core-only bundle installs cleanly)." (9 lines) | two-condition status block | §Rules > Every install task has a status: block | no | rewrite -- strip D-09 / PKGS-02 / PKGS-03 / PKGS-05 tags; preserve "two-condition status" WHY |
| taskfiles/packages.yml:119-130 | "Three-condition status (all must pass to skip cmds): 1. Composed Brewfile exists on disk. 2. Composed Brewfile is at least as fresh as ALL composer inputs: resolved.json AND every $DOTFILEDIR/packages/*.rb bundle file. The composer reads BOTH; pulling a manifest change (resolved.json ticks) OR a bundle change (packages/core.rb ticks via `git pull`) must trigger a recompose. 3. brew bundle check passes against the composed Brewfile. The freshness check (line 2) was missing before: `brew bundle check` against a stale Brewfile passes when the previously-declared packages happen to be installed, so newly-declared packages were silently ignored until $XDG_CACHE_HOME/dotfiles/Brewfile was manually deleted." (12 lines) | freshness-check footgun (stale Brewfile silently passes) | none | no | keep -- D-04 rule (b) still-live footgun (silently-ignored new packages) |
| taskfiles/packages.yml:138-146 | "1. Compose the per-machine Brewfile from manifest + bundle files. Explicit env forwarding so the composer's path constants resolve correctly when invoked through go-task (which strips many envs)." (9 lines including step 2) | go-task env-stripping footgun + explicit env forward | none | no | keep -- D-04 rule (b) still-live footgun (env strip) |
| taskfiles/packages.yml:153-158 | "compose -- inspection helper. Runs the composer only; useful for diff-ing the composed Brewfile or sanity-checking the manifest -> Brewfile pipeline without triggering brew bundle install. Force regen via `task --force packages:compose`." (6 lines) | compose-only inspection | none | no | keep -- D-04 rule (a) non-obvious WHY (operator-facing) |
| taskfiles/packages.yml:168-174 | "Two-condition status: Brewfile exists AND is at least as fresh as all composer inputs (resolved.json + packages/*.rb bundle files). Same freshness-check rationale as packages:install above -- a stale Brewfile must be regenerated when the manifest OR a bundle file changes, even though brew bundle check still passes against it." (7 lines) | same freshness rationale (cross-ref) | (same as :119-130) | no | strip after Plan 14-01 -- duplicate of :119-130 |
| taskfiles/packages.yml:185-208 | "verify -- D-07 enumerate-all + Gap 2 brew-info-driven two-layer model. Layer 1 (install-state): brew bundle check... Layer 2 (artifact-state): brew info --installed --json=v2 returns the full set... MAS layer (D-06): no brew-info equivalent for App Store apps... Aggregator-style (D-07 enumerate-all): never short-circuit; emit check/cross row per package. silent: false (task-level) -- closes Gap 3..." (24 lines) | two-layer verify model + silent override + enumerate-all | none (verify-specific architecture) | no | keep -- D-04 rule (a) non-obvious WHY; rewrite to drop D-07 / Gap-2 / Gap-3 / D-06 tags |
| taskfiles/packages.yml:230-247 | "Layer 1: brew bundle check (install-state gate). Reuses the EXACT same command as the install task's status block (D-09) so Layer 1 install-state semantics are identical: install and verify share the same gate. Continue to Layer 2 even on failure (enumerate-all; D-07 -- no short-circuit)." (18 lines including Layer 2 prelude) | install/verify share the same Layer-1 gate | none | no | rewrite -- strip D-09 / D-07 tags, preserve shared-gate WHY |
| taskfiles/packages.yml:260-269 | "Layer 2: cask artifact probe. For each cask declared in the composed Brewfile (bare `cask '<name>'` lines, no # verify: suffix post-Gap-2 pivot)..." (10 lines) | cask artifact probe loop description | none | no | rewrite -- strip Gap-2 tag |
| taskfiles/packages.yml:299-303 | "Homebrew cask binary artifacts can be: [\"<source-path>\"] -- destination name = basename(source) [\"<source-path>\", {\"target\": \"<name>\"}] -- destination name = target. The target form is required for casks like firefox where the source is a wrapper script (firefox.wrapper.sh) but the installed bin is `firefox`." (5 lines) | cask binary artifact target shape | none | no | keep -- D-04 rule (a) non-obvious WHY |
| taskfiles/packages.yml:333-344 | "Layer 2: formula linked_keg probe. For each formula declared in the composed Brewfile: Look up the installed formula record from brew info JSON. Assert .linked_keg is non-null (formula is built and linked, not just downloaded). Note: this is the top-level linked_keg on the formula record (the version string of the currently linked keg), NOT .installed[0].linked_keg which is always null in `brew info --installed --json=v2` output." (12 lines) | top-level linked_keg vs .installed[0].linked_keg distinction | none | no | keep -- D-04 rule (b) still-live footgun (subtle jq path) |
| taskfiles/packages.yml:359-362 | "keg-only formulae (e.g., whois on macOS, which would shadow /usr/bin/whois) are intentionally not symlinked into the Homebrew prefix; .linked_keg is always null. Layer 1 (brew bundle check) already confirmed installation, so accept keg-only formulae without the linked_keg probe." (4 lines) | keg-only exemption | none | no | keep -- D-04 rule (a) non-obvious WHY |
| taskfiles/packages.yml:387-392 | "MAS layer (D-06: name doubles as verify target). No brew-info equivalent for App Store apps. Use: mas list | awk '{print $1}' -> installed ids / /Applications/<name>.app probe -> artifact presence (D-06). Skip gracefully if mas is not on PATH (mas is in core.rb so should always be installed on a fully-converged machine)." (6 lines) | MAS verify shape | none | no | rewrite -- strip D-06 tags |
| taskfiles/packages.yml:416-422 | "Summary (D-07: enumerate-all + count-failures; D-10: hard-fail)" (1 strip-pattern line + body) | summary semantics | none | no | rewrite -- strip D-07 / D-10 tags |
| taskfiles/packages.yml:424-435 | "audit -- drift detection (D-11). Compares installed packages (brew leaves, brew list --cask, mas list) against declared packages (parsed from the composed Brewfile). Non-blocking by default; --strict CLI flag promotes drift to a non-zero exit. Realizes VRFY-03 (audit declared vs installed)." (12 lines) | audit drift-detection semantics | none | no | rewrite -- strip D-11 / VRFY-03 tags |
| taskfiles/packages.yml:457-486 | "Parse --strict from CLI_ARGS. Caller form: task packages:audit -- --strict. Declared sets (extracted from the composed Brewfile). Formulae: name only (strip any `# verify:` suffix)... `brew leaves` = top-level formulae only (D-11: catches `I `brew install`'d X manually` not transitive deps)." (30 lines including parsing + declared/installed sets) | brew leaves vs brew list distinction | none | no | keep -- D-04 rule (a) non-obvious WHY; rewrite to drop D-11 tag |
| taskfiles/packages.yml:533-538 | "validate -- thin wrapper composed into the root `task validate` in P8 (CUTV-01 composition slot). Delegates to packages:verify; the aggregator marker is correct here (single task: delegation)." (6 lines) | thin-wrapper validate composition | none | no | rewrite -- strip P8 / CUTV-01 tags |
| taskfiles/test.yml:1-24 | "Phase-7 smoke-test surface (TEST-01 + TEST-02). Exposes hook smoke tests for the four CLDE-02 named hooks... Phase 12 (Plan 07): hosts test:manifest + test:add-machine, moved here from taskfiles/manifest.yml per D-04 + Claude's-Discretion item 7. Callable as: task test:hooks / task test:default / task test:manifest / task test:add-machine / task test. Scope: CLDE-02 named hooks only. block-destructive, notify, and post-compact are excluded (D-17)." (24 lines) | banner + Phase 12 move history + scope | §Common Tasks (operator surface) (lines 21-42; lists `task test`) | no | rewrite -- collapse to D-01 3-label banner; strip Phase 12 Plan 07 move history (baked into current code) |
| taskfiles/test.yml:26-34 | "Vars -- self-contained fallbacks for standalone invocation: task -t taskfiles/test.yml hooks. When loaded via root Taskfile.yml, root vars take precedence. MACHINES_DIR / FIXTURES_DIR / STATE_FILE / RESOLVED_JSON_PATH / DEFAULTS_TOML / STATE_DIR mirror the analogous vars in taskfiles/manifest.yml (the test bodies were moved from there)." (9 lines) | standalone-invocation contract + mirror provenance | none | no | rewrite -- preserve standalone-invocation contract WHY; strip "moved from there" history |
| taskfiles/test.yml:61-64 | "default -- local aggregator so `task -t taskfiles/test.yml` runs hooks. Root `task test` is the canonical entry point (TEST-02)." (4 lines) | local default-task convention | none | no | rewrite -- strip TEST-02 tag |
| taskfiles/test.yml:91-93 | "manifest -- deep-merge fixtures (positive + negative). Moved from taskfiles/manifest.yml in Phase 12 (Claude's-Discretion item 7 + D-04)." (3 lines) | move provenance | none | no | strip after Plan 14-01 |
| taskfiles/test.yml:104-120 | "Cleanup tracker for negative-fixture copies in MACHINES_DIR. CR-01 fix: any failure between cp and rm -f (yq crash, SIGINT, validator coredump) would otherwise leak _invalid-* files into manifests/machines/, polluting AVAILABLE_MACHINES and breaking `task setup` autocomplete UX. go-task's mvdan/sh interpreter only supports EXIT in trap (not INT/TERM by name); go-task forwards received signals to its child shell, which then runs the EXIT trap on its way out, so EXIT alone covers Ctrl-C correctly." (17 lines) | EXIT-only trap covers SIGINT under go-task | none (go-task interpreter quirk) | no | keep -- D-04 rule (b) still-live footgun + (a) non-obvious WHY; rewrite to drop CR-01 tag |
| taskfiles/test.yml:122-124 + 136-148 | "Positive fixtures 01-06: run inline merge pipeline + diff" + "Pass 2 (conditional): union extra_packages only when either side declares it. Avoids incorrectly adding the key to fixtures that omit extra_packages." (16 lines total) | conditional-union to avoid adding absent key | none | no | keep -- D-04 rule (a) non-obvious WHY |
| taskfiles/test.yml:169-208 | "Negative fixtures: validate-only; assert non-zero exit + stderr fragment. _run_negative_fixture: shared inner-loop helper (REVW-04 / D-09, extracted in Plan 13-04). Collapses the 5 hand-rolled blocks (`_invalid-*`) into a single function called once per fixture. CR-01 fix preserved: registers the copy in neg_copies BEFORE cp so the EXIT trap cleans up on cp failure / SIGINT. Args: ... displayed_label -- the human-readable summary text used in check / cross messages (e.g. `missing overlay file in stderr` for _invalid-bad-identity where the grep pattern `no overlay file at` reads awkwardly out of context)" (40 lines) | shared helper + register-before-cp ordering + displayed_label convention | none | no | keep -- D-04 rule (b) still-live footgun (cp failure leaks); rewrite to drop REVW-04 / D-09 / Plan 13-04 / CR-01 tags |
| taskfiles/test.yml:216-225 | "Summary. WR-05 fix: derive totals from actual fixture counts so adding a 7th positive fixture does not require editing this constant." (10 lines) | derived totals vs hand-coded constant | none | no | keep -- D-04 rule (a) non-obvious WHY; rewrite to drop WR-05 tag |
| taskfiles/test.yml:235-244 | "add-machine -- MFST-09 smoke. Moved from taskfiles/manifest.yml in Phase 12 (Claude's-Discretion item 7 + D-04)." (3 lines) | move provenance + MFST-09 ref | none | no | strip after Plan 14-01 -- both MFST-09 and move-history strip |
| taskfiles/test.yml:252-264 | "Save current state for restoration. CR-02 fix: distinguish `no prior state existed` from `prior state was empty` so the cleanup function can DELETE the state file on a fresh machine instead of leaving _addmachine-test pointing at a manifest that no longer exists. ... WR-08 fix: trim only edges; embedded whitespace is preserved so an invalid prior state is not silently rewritten into a different (possibly valid) machine name on restore." (13 lines) | restoration distinguishes-empty-vs-absent; trim-edges-only | none | no | keep -- D-04 rule (b) still-live footgun; rewrite to drop CR-02 / WR-08 tags |
| taskfiles/test.yml:273-289 | "Unconditional cleanup: remove throwaway TOML and restore state. Defined as a function so trap can reference it without complex quoting." (3 lines + cleanup function body) | trap-friendly function form | none | no | keep -- D-04 rule (a) non-obvious WHY |
| shell/.zlogout:2-51 | ".zlogout - Zsh login-shell logout hook. Sourced by: login shells when they exit... Purpose: Provide a place for finalization and cleanup tasks... Typical safe contents / examples: Ensure history is flushed... Safety notes and recommendations: This file runs on login-shell exit only... Where to put per-session markers... See: Zsh manual" (50 lines) | generic zsh-startup tutorial content; not project rules | none (out of scope -- zsh tutorial content, not project rule) | no (out of scope) | strip; do not gap-fill -- replace with 3-label banner + body comment "Minimal default: flush history" |
| shell/aliases/jgrid.zsh:1-15 | "shell/aliases/jgrid.zsh -- jgrid.net allomantic-metals ssh-jump aliases. Purpose / Why source-time gate (D-08) rather than wrapper-function (D-07)" (15 lines) | banner + D-07/D-08 rationale (source-time gate vs wrapper-function) | §Adding Things > Alias bullet (D-07 / D-08) (lines 88-91) | no | rewrite -- collapse to D-01 3-label banner; strip D-07 / D-08 tags (already in CLAUDE.md §Adding Things) |
| shell/aliases/ghostty.zsh:1-13 | "shell/aliases/ghostty.zsh -- Ghostty launcher alias. Purpose / Why wrapper-function (D-07) rather than source-time gate (D-08)" (13 lines) | banner + D-07 rationale | §Adding Things > Alias bullet (D-07 / D-08) | no | rewrite -- collapse to D-01 3-label banner; strip D-07 / D-08 tags |
| shell/aliases/finder.zsh:1-11 | "shell/aliases/finder.zsh -- Finder GUI aliases. Purpose / Why wrapper-function (D-07) rather than source-time gate (D-08)" (11 lines) | banner + D-07 rationale | §Adding Things > Alias bullet (D-07 / D-08) | no | rewrite -- collapse to D-01 3-label banner; strip D-07 / D-08 tags |
| shell/aliases/dotfiles.zsh:1-9 | "shell/aliases/dotfiles.zsh -- dotfiles-related aliases. Purpose / Why this replaces v1: the `update` alias below replaces v1's zsh/functions/update.zsh wrapper. Per CF-06 and Phase 2 D-10, `task install` IS `task update`..." (9 lines) | banner + v1-replacement history | §Quick Reference / §Common Tasks (install IS update already documented) | no | rewrite -- collapse to D-01 3-label banner; strip CF-06 / Phase 2 / D-10 / v1-replacement tags |
| shell/functions/_dotfiles_feature.zsh:1-26 | "shell/functions/_dotfiles_feature.zsh -- lazy manifest feature query (D-06). Purpose / Callers / Reads / Side effects / Graceful degrade / Usage" (26 lines) | banner + feature-query mechanics | §Adding Things > Feature flag (lines 99-102) | no | rewrite -- collapse to D-01 3-label banner; strip D-06 tag |
| shell/functions/_dotfiles_require_feature.zsh:1-34 | "shell/functions/_dotfiles_require_feature.zsh -- feature-gate guard for wrapper-function aliases (D-07 Class B helper). Purpose / Callers / Reads / Side effects / Arg shape / Class" (34 lines) | banner + D-07 helper rationale + class | §Adding Things > Alias / Feature flag | no | rewrite -- collapse to D-01 3-label banner; strip D-07 / Class B / REVW-04 / D-09 / Plan 13-04 tags |

## Summary

- **Total rows:** 162 distinct annotation blocks enumerated across 13 strip-pass files + 7 heavy-banner files.
- **Rows with `action=strip after Plan 14-01`:** 22 (pure tag-only or duplicate; no WHY survives strip).
- **Rows with `action=keep` (D-04 rule (a)/(b)/(c)):** 49 (still-live footgun, non-obvious WHY, or lint citation -- the rationale survives in inline comments).
- **Rows with `action=rewrite -- preserve WHY, strip planning tag`:** 90 (most banners + most teaching blocks; the lesson stays as ~1-3 line inline comment after the planning tag is stripped).
- **Rows with `action=strip; do not gap-fill`:** 1 (`shell/.zlogout:2-51` -- zsh tutorial content, out of scope for CLAUDE.md).

### NEEDS-ADD rows (3 distinct lessons; multiple in-code sites per lesson)

**Each NEEDS-ADD row has its CLAUDE.md insertion point named below. Plan 14-01 Task 2 lands each before Plan 14-02 strips the in-code annotation.**

1. **DOTFILEDIR-leak Don't-Do bullet** — surfaced by `Taskfile.yml:73-82` + `Taskfile.yml:131-136` + `Taskfile.yml:224-247`. The `DOTFILEDIR: { sh: dirname "{{.TASKFILE_DIR}}" }` definitions in included taskfiles (manifest.yml:54, identity.yml:64-65, lint.yml:59-61, links.yml:60-61, macos.yml:68-69, packages.yml:60-61, test.yml:40-41) all leak DOTFILEDIR into root scope under include-merge. The root must source messages.zsh via `{{.TASKFILE_DIR}}` (NOT `{{.DOTFILEDIR}}` / NOT the `DOTFILES_MESSAGES` var) to stay render-stable. **Insertion point:** `CLAUDE.md §Don't Do` (after the last bullet, line 177). **Proposed one-line bullet:** "Don't define `DOTFILEDIR: { sh: dirname ... }` in included taskfiles -- it leaks into root scope and competes with the root Taskfile.yml definition. Source `install/messages.zsh` via `{{.TASKFILE_DIR}}` (per the Taskfile.yml comment block warning the same)."

2. **Cross-field validation rule paragraph** — surfaced by `install/resolver.zsh:212-238`. The resolver enforces conditional rules across manifest sections (e.g., `identity.ssh in {personal, work}` requires `features.one-password-ssh = true`; `identity.git in {personal, work}` requires `features.one-password-signing = true`). Failing rules surface at `task setup` time. This concept is not in CLAUDE.md today; readers learn it only via the D-16 tag in resolver.zsh. **Insertion point:** `CLAUDE.md §Rules > Manifests are the source of truth for install state` (new paragraph after line 50). **Proposed text (~3 sentences):** "Cross-field validation: the resolver enforces conditional rules across manifest sections (e.g., `identity.ssh in {personal,work}` requires `features.one-password-ssh = true`). Failing rules surface at `task setup` time with a clear error from `install/resolver.zsh`. Add new rules to the `validate_manifest` block in `resolver.zsh`."

3. **LINT-NN catalogue (LINT-01..08)** — surfaced by `taskfiles/lint.yml:1-35` plus 6 LINT-NN section-header repeats throughout lint.yml. CLAUDE.md mentions LINT-02 by name (lines 86-101) but does not document the full LINT-01..LINT-08 catalogue that the in-code citations (`# LINT-02:`, `# LINT-03b:`, `# LINT-08:` etc.) reference. **Insertion point:** `CLAUDE.md §Rules` (between §kebab-case feature names and §Every install task has a status:, around line 86) IF the inline table keeps CLAUDE.md <= 220 lines; OTHERWISE extract to `.claude/rules/lint.md` and add a one-line cross-reference under §Rules per R6 mitigation. **Proposed catalogue:**

| ID | Scope | What it checks |
|----|-------|----------------|
| LINT-01 | Taskfiles | Every install task has a `status:` block |
| LINT-02 | Taskfiles | `status:` uses `{{.X}}` template vars, not `$X` shell vars |
| LINT-03a | Taskfiles | Tasks with `cmds:` have `status:` (or exempt via `internal: true` / all-task-delegates) |
| LINT-03b | Repo-wide | No bare `ln -s` outside `taskfiles/helpers.yml` |
| LINT-04 | Executable .zsh | `set -euo pipefail` in first 30 lines |
| LINT-05 | shell/ + os/ | Portability-sensitive commands surface as warnings (non-blocking) |
| LINT-07 | All .zsh | `zsh -n` parse-check (Tier-0 syntax) |
| LINT-08 | Root Taskfile.yml | `default:` banner lists every public top-level task |

## Plan 14-02 Strip Manifest

Per-file work breakdown Plan 14-02 consumes. The table aggregates the action columns above per file.

| File | strip | keep | rewrite | total rows |
|------|------:|-----:|--------:|-----------:|
| Taskfile.yml | 2 | 5 | 3 | 10 |
| identity/ssh/cloudflared.zsh | 0 | 0 | 1 | 1 |
| install/compose-brewfile.zsh | 1 | 6 | 4 | 11 |
| install/resolver.zsh | 4 | 6 | 9 | 19 |
| os/shell-registration.zsh | 0 | 3 | 2 | 5 |
| taskfiles/claude.yml | 0 | 3 | 7 | 10 |
| taskfiles/identity.yml | 1 | 6 | 7 | 14 |
| taskfiles/links.yml | 2 | 8 | 7 | 17 |
| taskfiles/lint.yml | 4 | 4 | 7 | 15 |
| taskfiles/macos.yml | 1 | 3 | 7 | 11 |
| taskfiles/manifest.yml | 0 | 4 | 8 | 12 |
| taskfiles/packages.yml | 1 | 8 | 9 | 18 |
| taskfiles/test.yml | 2 | 5 | 3 | 10 |
| shell/.zlogout | 1 (strip; do not gap-fill) | 0 | 0 | 1 |
| shell/aliases/jgrid.zsh | 0 | 0 | 1 | 1 |
| shell/aliases/ghostty.zsh | 0 | 0 | 1 | 1 |
| shell/aliases/finder.zsh | 0 | 0 | 1 | 1 |
| shell/aliases/dotfiles.zsh | 0 | 0 | 1 | 1 |
| shell/functions/_dotfiles_feature.zsh | 0 | 0 | 1 | 1 |
| shell/functions/_dotfiles_require_feature.zsh | 0 | 0 | 1 | 1 |
| **Total** | **19** | **61** | **79** | **162** (some rows span multiple line ranges; total reflects distinct annotation blocks) |

(Per-file totals are the count of distinct annotation blocks in this inventory; the actual edit count per file may be smaller when adjacent rows collapse into one rewrite operation. Plan 14-02 will reconcile per-file rewrite vs strip choices into per-file commits per the per-file-commit discipline established in Phase 13.)

### Strip vs keep vs rewrite semantics

- **strip** -- the entire annotation block is removed; the lesson is either restated in CLAUDE.md or self-evident from the current code shape.
- **keep** -- the block stays verbatim; rationale falls under D-04 rule (a) non-obvious WHY, (b) still-live footgun, or (c) lint-rule citation. Planning tags may remain inside kept blocks because the lesson body is load-bearing.
- **rewrite** -- the planning tags (`Phase N`, `D-NN`, `CR-NN`, `WR-NN`, `RESEARCH §`, `Gap N`, `v1 (bug|finding|leftover)`, `UAT [Gg]ap`, `PORT-NN`, `BTSP-NN`, `MFST-NN`, `IDNT-NN`, `T-NN`, `CUTV-NN`, `OSCF-NN`, `CLDE-NN`, `IN-NN`, `REVW-NN`, `Pitfall N`, etc.) are stripped; a 1-3 line essential-WHY comment survives in the same position. SC#5 grep gate passes after the rewrite.

Plan 14-02 enforces SC#5's grep gate after the last strip/rewrite commit lands. The 3 NEEDS-ADD rows above must be amended into CLAUDE.md (Task 2 of this plan) before any in-code strip commit lands -- D-09 gates the entire phase on this.
